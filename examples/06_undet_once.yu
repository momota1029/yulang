// Nondeterminism: search for the first Pythagorean triple.

use std::control::nondet::*

once:
    my a = each 1..
    my b = each a<..
    my c = each b<..

    guard: a * a + b * b == c * c

    (a, b, c)
