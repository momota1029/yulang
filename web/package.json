{
  "name": "yulang-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "npm --prefix playground run build && npm --prefix docs run build",
    "deploy:dir": "npm run build && node scripts/deploy-dir.mjs"
  }
}
