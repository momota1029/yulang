my path = {{file}}

my meta = std::io::file::meta path

(
    meta.kind,
    meta.size,
    meta.readonly,
    std::io::file::exists path,
    std::io::file::is_file path,
    std::io::file::is_dir path,
)
