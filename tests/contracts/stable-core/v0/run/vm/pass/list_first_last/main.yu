my empty: list int = []
my singleton = [7]
my many = [1, 2, 3]

(
    empty.first,
    empty.last,
    singleton.first,
    singleton.last,
    many.first,
    many.last,
    std::data::list::last empty,
    std::data::list::last singleton,
    std::data::list::last many
)
