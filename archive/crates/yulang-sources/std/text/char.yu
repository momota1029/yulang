pub type char with:
    pub c.to_string = std::text::char::to_string c
    pub c.is_whitespace = std::text::char::is_whitespace c
    pub c.is_punctuation = std::text::char::is_punctuation c
    pub c.is_word = std::text::char::is_word c
