# Binding current-Item typed recovery

Status: Authoritative; private O3b construction complete

Date: 2026-09-08

Approved-by: user through the recovery-selection and simplification delegation
in `2026-09-08-successor-recovery-authority-amendment.md`

Scope: Binding Target/inline Body recovery in `rewrite/binding.rs` and minimal
explicit initial-role transport through `rewrite/pattern.rs`. Indented Binding
body remains the completed child owner. Statement dispatch, Pattern ordinary
entry, declaration bypasses and public integration remain separate.

Authority: recovery authority §§1--3; typed-output §§3--5; architecture D1
Binding recovery table; Pattern-primary current-Item contract.

## Publication and handoff

Binding owns initial `Declaration(Binding(Target))` Missing/Error, expecting
Pattern. Its explicit role is transported only into Pattern's initial primary
kernel; ordinary Pattern callers retain `Pattern(Primary)`, and admitted nested
Pattern/Type owners retain their own roles. The direct Binding target boundary
bypass publishes the same role inside its existing Pattern wrapper.

Inline Body Missing/Error publishes `Declaration(Binding(Body))`, expecting
Expression. Classify protected boundary before leading emission; separator,
caller stop, shallow/dedent line and abstract fence retain complete Item/leading.
Ordinary EOF alone emits permitted horizontal leading into BindingBody then
anchors at successor. A maximal lexical Body Error emits initial leading outside
Error, internal leading inside, and leaves retry/boundary leading pending; a
post-Error boundary adds no duplicate Missing. Sealed scanning is lexical only.

Keep exact equals selection, accepted inline/indented body routing, threshold,
ML, stops, line/fence, ambient and sequence propagation. No accepted grammar,
new role or wrapper changes.

## Evidence and execution

Cover Target Missing/Error to NUD/equals/boundary; Body EOF/semicolon/close/
newline/fence before and after Error; indented child role; nested Pattern/Type/
Expression ownership; UTF-8/CRLF shifted fresh/frozen records and effect-free
selection. M2 used one implementation and a test-only fence-range repair:
abstract fence anchors at its inspected coordinate after CRLF. Binding tests
passed 11; Pattern 54; indented recovery 7; recovery-output 25; package check,
scoped format and diff passed. Specification/recovery and regression audits
passed. One forward scan; zero benchmark processes.
