# Act current-Item typed recovery

Status: Authoritative; private O3b construction complete

Date: 2026-09-08

Approved-by: user through the recovery-selection and simplification delegation
in `2026-09-08-successor-recovery-authority-amendment.md`

Scope: raw `Act(BodyIntroducer)` Error and `Act(Body)` Missing/Error recovery
in `rewrite/act_decl.rs`. Head/Source Type episodes, bodyless terminal success,
Derives/Companion phases, block children, callers and public integration remain
otherwise unchanged.

Authority: recovery authority §§1--3; typed-output §§3--5; architecture
`ACT-R`, `ACTDRV-G/J` and companion owner matrix; adoption matrix D9 corrected
below.

## Matrix correction and records

D9c's retained `act A` owner-boundary source is implicit bodyless success with
zero recovery under `ACT-G`/`ACT-R`, not `Act(BodyIntroducer)` Missing. Retain
it as a zero-recovery control; this corrects the matrix attribution only and
changes no accepted grammar.

`Act(BodyIntroducer)` Error expects ordered `[Semicolon, Open(Brace), Colon]`,
primary zero. `Act(Body)` Missing/Error expects Statement. There is no new
BodyIntroducer Missing for terminal absence. Missing is zero-width with no
unexpected fact. A maximal lexical Error has one nonempty actual extent, one
OtherCharacter fact, committed expectations and primary zero.

## Boundary, retry and ownership

Initial Error leading belongs to Act, internal leading to Error, and retry or
boundary leading remains pending, including ordinary EOF after Error. The
sealed lexical total Item scanner stops BodyIntroducer Error before an actual
starter or protected boundary; Body Error stops before lexical canonical
Statement admission or its boundary. Close Error before grammar retry; no
boundary-after-Error duplicate Missing.

Inline Body absence, including EOF, retains the full Item and leading and
anchors at remaining-start or inspected abstract coordinate. Colon equal/shallow
indentation first acquires its protected Item, publishes Body Missing at
remaining-start, and returns CRLF/newline, token, suffix, successor coordinate
and LineEntry unchanged. Nonlocal closes, active stops, ambient companions,
fences and layout boundaries stay protected.

Completed Head/Source alone permits header Derives. Companion terminates Act
before later source/body selection. Recovery must not re-enter those phases.
Braced/indented/canonical Statement children and trailing Derives retain their
existing owners and conditions.

## Evidence and execution

Cover fresh/frozen union records and all starter retries; zero-record bodyless
boundary; no Head/Source cascade; Body EOF/semicolon/comma/close/fence/CRLF;
leading and malformed Statement retry; child ownership and accepted attachment
controls. M2 uses one implementation pass and specification/regression audits.
The accepted test-only repair adds the D9c closing-owner fresh/frozen witness.
Static cost is O(bytes + structural work); zero benchmark processes were used.
