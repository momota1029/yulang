# CaseLike structural current-Item recovery

Status: Authoritative; private O3b construction complete

Date: 2026-09-08

Approved-by: user through the recovery-selection and simplification delegation
in `2026-09-08-successor-recovery-authority-amendment.md`

Scope: Case/Catch structural entry only: missing Block introducer,
same-or-shallower post-colon Arm, initial arm Pattern, and Catch handler Pattern
after its comma. Arrow/Body recovery, arm separators, Catch close, guards,
scrutinee and accepted grammar remain separate owners or later gates.

Authority: E12b/c/d/k in the SCC amendment, Case/Catch architecture recovery
contract, Pattern-primary current-Item contract and recovery-authority §§1--3.
Existing CaseLike roles are sufficient. This records their immediate-owner use
without adding vocabulary or a duplicate Pattern scan.

## Construction result

The bounded private construction is complete. Block and shallow Arm publish
their selected records, while first-arm and Catch-handler Pattern admission
uses the existing Pattern kernel once with its finite CaseLike role. The
verified controls cover fresh/shifted/frozen/seeded records, UTF-8/CRLF,
fences, outer delimiters, and following statements; compiler/recovery and
regression reviews found no defect. Arrow/Body, separator, and Catch-close
remain distinct completed or separately recorded CaseLike owner work, not part
of this original structural slice.

## Roles and records

| site | role | expected |
| --- | --- | --- |
| Case/Catch missing block introducer | `CaseLike(Block)` | punctuation Colon |
| same-or-shallower post-colon arm | `CaseLike(Arm)` | Pattern |
| first Case or Catch arm Pattern | `CaseLike(Pattern)` | Pattern |
| Catch Pattern after handler comma | `CaseLike(Handler)` | Pattern |

All Missing records are zero-width with one singleton mapped expectation,
`COMMITTED_RECOVERY_RULE`, primary zero and no unexpected facts. Initial Pattern
Error uses the same externally supplied CaseLike Pattern/Handler role, one
Pattern expectation and one `OtherCharacter` token fact over the existing
nonempty lexical run. The Pattern kernel publishes this record exactly once.
After it admits a Pattern, every nested alias, alternation, delimiter and Type
owner selects its own role; CaseLike never wraps or duplicates it.

Catch's missing introducer expects colon only. An accepted `{` continues the
existing Catch braced branch unchanged; this does not make brace punctuation an
alternative Missing expectation.

## Boundary, leading and handoff

Keep accepted arm-family topology and each existing CaseInline, CatchInline,
Indented and CatchBraced policy. An admitted arm-entry emits its leading
directly into the family Block before Arm opens. A rejected or protected entry
keeps the complete Item and leading pending.

For Block introducer absence, an abstract boundary, outer close, or Item with
ordinary newline leading remains wholly pending; publish at its inspected
boundary coordinate or remaining-start without emitting that leading. At EOF,
emit the existing EOF-leading in Block before anchoring Missing. Other ordinary
same-line pending leading remains Block-owned before Missing. Return the exact
Item, origin and line entry unchanged. This corrects the former raw path only
where it consumed an outer newline; it does not alter accepted layout.

For wrong-indent Arm, publish one Arm Missing at the pending remaining-start
and return the complete scanned Item, including newline leading, to the outer
statement owner. Do not scan or emit it. Initial Pattern/Handler absence and
Error retain the Pattern kernel's current protected-stop, lexical-run, retry
and fence behavior. Handler comma remains Catch-owned and no second CaseLike
record surrounds the kernel.

The work remains one-forward `O(bytes + structural work)`: no source replay,
new scanner, error capability, Item clone, diagnostic sort or generic caller
role API.

## Required evidence

Retain E12b/c/d/k source literals and add fresh/shifted/frozen/seeded exact
records for all four roles. Cover Case and Catch missing blocks at EOF, close,
outer newline and quoted fence; wrong-indent with a real following Statement;
initial Pattern and Handler Missing/Error retry; nested Pattern and Type role
ownership; UTF-8/CRLF/foreign-prefix coordinates; and actual outer delimiter
continuation. Keep accepted family topology, handler comma, brace and
arm-entry-whitespace controls recovery-free.

M2: one implementation pass, compiler/recovery and regression review, at most
one repair bundle. Run focused CaseLike/Pattern/output controls plus package
check, format and diff. Benchmark use remains zero absent material cost
uncertainty. Synchronize task, ledger and daily record before commit.
