# Successor delimited PV EffectRow structured-Error extent addendum

Status: Authoritative; bounded construction approved by recorded delegation

Date: 2026-09-07

Drafted-by: primary after isolated base/candidate successor comparison and
direct legacy EffectRow carrier characterization

Reviewed-by: M3 independent compiler/recovery and specification review; clean
Draft closure

Approved-by: user, through the recorded recommendation delegation in
`2026-09-07-successor-delimited-pv-extent-correction-addendum.md`

Approved-at: 2026-09-07

User-directed decision: this is a comparably narrow, fully reviewed,
direct-compatibility correction under the recorded delegation.  Adopt the §2--3
recommended E-only extent/evidence disposition.  All Call admission, PV parity,
`variants.rs`, public, and O6 exclusions remain in force.

Scope: a private successor range/evidence correction for the existing
`PolymorphicVariantTagName` structured Error when it already encloses an
approved `EffectRow` ordinary-horizontal disposition.  It is limited to this
existing carrier and immediate EffectRow owner.  It grants no PV admission,
Call, Parenthesized, `variants.rs`, or public/fixture/golden change.

Depends-on:

- `2026-09-07-successor-delimited-boundary-priority-correction.md` §§1--4;
- `2026-09-07-successor-delimited-pv-extent-correction-addendum.md` only as a
  same-carrier precedent, not as an authority extension;
- `2026-09-07-successor-structured-recovery-reservation-amendment.md` §6;
- `2026-09-07-successor-structured-recovery-extent-validation-addendum.md`
  §2; and
- direct legacy evidence `0d58bf7c` in
  `crates/yu-syntax/src/grammar/type_expr.rs`.

## 1. Confirmed E-only interaction

EffectRow owns approved ordinary-horizontal leading.  The existing PV wrong-kind
TagName branch has already opened a structured Error around its nested
TypeExpression.  Its endpoint follows the returned pending Item frontier and
must equal emitted-byte coverage.  A committed E gap inside that Error cannot
remain outside the range, be clamped later, or be special-cased in the shared
delimiter helper.

Direct legacy evidence pins all six rows below as flat `Error > Unknown`, while
keeping the PV `}` native and each `::Next` form lossless.  The successor's
already-authorized native EffectRow/Missing topology differs; only outer extent,
raw PV close, and continuation are compatibility facts.

| E phase/control | source | legacy TagName Error | base successor | candidate successor |
| --- | --- | --- | --- | --- |
| `Initial` | `:{'[ }` | `2..5` | `2..5` | retained `2..5` |
| post-comma | `:{'[F, }` | `2..7` | `2..6` | `2..7` |
| post-semicolon | `:{'[F; }` | `2..7` | `2..6` | `2..7` |
| `AfterItem` | `:{'[F }` | `2..6` | `2..5` | `2..6` |
| no gap | `:{'[F}` | `2..5` | `2..5` | retained `2..5` |
| matching local | `:{'[F ]}` | `2..7` | `2..7` | retained `2..7` |

The isolated base comparison uses `ec387899`, immediately before the
unintegrated shared candidate.  It confirms that this addendum changes no E
record: TagName Error remains the sole record; fresh Item/close Missing CST
nodes remain untyped under the base delimited correction.  The three changed
extents each match legacy.  Prefix/nested legacy controls are
`:{@ '[F }` (Tag Error `2..4`, TagName Error `4..8`) and
`:{:{'[F }}` (one outer TagName Error `2..9`); their successor outcome remains
a proof obligation.

Numeric apparent-Call sources are deliberately excluded.  Legacy parses
`:{123(F }` as TagName Error `123`, then a separate Parenthesized payload and
PayloadBoundary Missing, never a Call nested in TagName Error.  That is a
pre-existing PV head-admission contradiction, not E extent authority.

## 2. Proposed same-carrier E disposition

Only after the existing TagName structured Error has opened, and only for an
authorized EffectRow `Initial`, `AfterExplicitSeparator`, or `AfterItem`
ordinary-horizontal generic-caller/outer-close row, retain owner-local gap
emission and derive the Error end from its post-emission frontier.

For the three changed table rows, existing TagName Error site, unexpected, and
expectation ranges advance to the candidate/legacy extent.  The Error contains
native E Whitespace and the applicable fresh Item-then-close Missing or
post-item close Missing CST, but no E record.  The actual PV `}` remains an
outer native token outside Error.  Record kind/count/order/IDs/sources/primary
index, PV admission, AST/remainder, and continuation remain unchanged.

`Initial`, no-gap, and matching-local rows retain their current outer Error
extent; their CST remains governed by the base correction.  This addendum does
not authorize an E row under another Error carrier, Call/P effects, prefix or
nested reservation ranges without proof, or later EffectRow T4E construction.

Supersedes if approved, and only for the three named existing private TagName
Error expectations: (a) the base correction's no-private-successor-
expected-output sentence and (b) its §3 `embedded recovery fact` exclusion,
only where either prohibits these range/evidence updates.  It retains
`NonTypeApply PV/declaration parity`, the P addendum, PV recovery scope, and
all Call interactions.

## 3. Proof, integration, and rollback

Before integration, focused fresh/frozen successor tests must prove every table
row, with and without `::Next`, including:

1. full TagName Error fields/IDs/order/diagnostics, exact range/text, direct
   Error/E/Whitespace/Missing ancestry, raw PV close exclusion, AST, remainder,
   origin, Item frontier, line entry, stops, and context;
2. exact emitted-byte coverage for each structured Error and no E
   record/slot/diagnostic beyond TagName Error;
3. retained no-gap/local and P-in-TagName controls; and
4. prefix `:{@ '[F }` and nested `:{:{'[F }}` fresh/frozen controls, proving
   record order/count and Error extent without extending this scope.

Construction changes only private successor tests and integrates the existing
shared delimiter candidate if proof holds.  It may not modify `variants.rs`,
split/special-case the helper, alter direct legacy tests, update Call/P
expectations, or change fixture/golden/public output.  An E record, consumed PV
close, bad emitted extent, changed prefix/nested order, or any Call/P/new-carrier
delta returns to architecture.

## 4. Cost, review, and recorded recommendation

The candidate adds no PV branch, traversal, allocation, clone, or range state;
the range follows existing structured-output ownership.  Timing budget is zero
samples/processes unless review finds material uncertainty.

This is M3 because it amends a structured recovery oracle.  Independent
compiler/recovery and specification review closed this Draft under the recorded
delegation.  Regression plus compiler/specification delta review close
construction.  The separate Call head-admission contradiction remains outside
that delegation pending its own evidence and reviewed disposition.

## 5. Approval record

The recorded user delegation applies because the reviewed disposition changes
only existing private TagName Error range/evidence cells to their direct legacy
values, while preserving roles, counts, ordering, admission, and every stated
scope exclusion.  It does not authorize a separate semantic decision for the
numeric Call head-admission contradiction.
