# Cast TargetIntroducer current-Item recovery

Status: Authoritative; private construction complete

Date: 2026-09-09

Approved-by: user, through the active recovery-selection delegation.
Reviewed-by: scoped M2 read-only preflight.

Scope: the four raw Missing sites and one malformed Error run in
`cast_target_introducer_normalized`. Cast Pattern close, TargetType, form/body,
and the no-local-frame path retain their current owners. No accepted Cast
syntax, target Type grammar, wrapper, operator selection, delimiter ownership,
public dispatch, or generic recovery API changes.

Authority: `CAST-R` TargetIntroducer rows in
`2026-08-20-yu-syntax-chasa-architecture.md`, the current recovery-authority
amendment's truthful typed-record requirements, and the user's explicit
recovery-selection delegation. Yulang2 remains accepted-input evidence only.

All publications use `Declaration(Cast(TargetIntroducer))` with
`Punctuation(Colon)`. Each Missing is zero-width with no unexpected
facts. Each Error is one maximal nonempty native lexical run with exactly one
`OtherCharacter` token fact over its full emitted run. Every record has one
mapped expectation, `COMMITTED_RECOVERY_RULE`, and primary index zero.

Exact allowed `:` consumes first and starts `CastTarget` plus its existing
TargetType owner. A reusable Type NUD without `:` receives one TargetIntroducer
Missing inside `CastTarget` and retries unchanged as TargetType. `;` and `=`
publish only TargetIntroducer recovery, stay unread for form ownership, and do
not cascade TargetType Missing. The former Cast-local `)` is already popped:
it is outer/unowned, stays unread, and has one TargetIntroducer recovery with
no target/form cascade. Active stops, separators, unread `]`/`}`, abstract or
fence boundary, EOF, active statement companion, line stop, and disallowed or
equal-or-shallower newline are the same protected outer boundaries.

The Error scan reads lexical current Items with the ordinary Type vocabulary,
stopping before the first allowed `:`, form starter, reusable Type NUD, or
protected boundary. Internal run leading belongs to Error; retry and boundary
leading stays outside. Error then retries `:` or a Type NUD through ordinary
append logic, or returns form/boundary unchanged with no second
TargetIntroducer Missing. Nested malformed Type recovery remains Type-owned.

The selected EOF-leading rule matches the immediately preceding Cast gates: a
nonempty same-line EOF-leading suffix belongs to the still-open Error and
extends its node, typed record, and unexpected fact together. Empty or
newline/CRLF EOF leading remains pending. This is a malformed-only successor
recovery selection, not accepted-syntax policy.

Before closure verify exact fresh, shifted, frozen and seeded records for all
four Missing paths; a multi-Item Error run; retry to colon and Type NUD;
semicolon/equal form handoff; outer and active `)`; active stops, bracket/brace,
line/fence, UTF-8 and CRLF boundaries; same-line versus newline EOF leading;
and no TargetType record before distinct target evidence. Retain accepted target
and nested Type-owner controls. Run the Cast owner suite, direct
Pattern/Type/normalized/recovery-output cone, one package check, scoped format
and diff. Measurement budget: zero samples/processes unless material
uncertainty appears.

Construction completed 2026-09-09. `TargetIntroducer` now has no dead-code
exemption. M2 preflight and delta review found no concrete defect; the delta
review requested, and then confirmed, direct CST/record evidence for same-line
and newline EOF-leading ownership plus protected bracket/brace and CRLF
boundaries. Focused Cast tests (16), direct Pattern/Type/normalized/
recovery-output tests (358), package check, format check, and diff check
passed. Benchmark use: zero samples/processes.
