# Impl current-Item typed recovery

Status: Authoritative; private O3b construction complete

Date: 2026-09-08

Approved-by: user through the recovery-selection and simplification delegation
in `2026-09-08-successor-recovery-authority-amendment.md`

Scope: raw `ImplRole::BodyIntroducer` and inline `Body` recovery in
`rewrite/impl_decl.rs`. Head and Description required Type episodes, indented
and braced Statement children, callers and public integration are excluded.

Authority: recovery authority §§2--3; typed-output §§3--5; architecture
`IMD-G` and `IMD-R`; adoption matrix D7 as corrected below.

## Phase correction and records

D7f's source `impl T:` reaches the first same-line colon phase and therefore
publishes `Impl(Description)` Missing, expected TypeExpression, with no
same-boundary BodyIntroducer cascade. It is retained as a Description control,
not Body evidence. `impl T: D:` is the Body witness. This corrects only D7f's
phase attribution under `IMD-G`/`IMD-R`; accepted grammar is unchanged.

`Impl(BodyIntroducer)` Missing and Error expect ordered `[Semicolon,
Open(Brace), Colon]`, primary zero. `Impl(Body)` expects Statement. Missing is
zero-width with no unexpected fact; every maximal lexical Error has one
nonempty actual extent, one OtherCharacter fact, committed expectations and
primary zero. Head and Description keep their Type recovery roles; braced,
indented and canonical Statement children keep theirs.

## Boundary, leading and retry

Initial Error leading belongs to Impl, internal run leading to Error, and
retry/protected-boundary leading remains pending. Use a sealed lexical-only
total Item scan; finish Error before grammar retry. Error returning a boundary
does not add same-slot Missing.

BodyIntroducer ordinary EOF emits residual owner leading then anchors Missing
at EOF. Fence, close, active stop, equal/shallow newline and ambient boundary
retain the full Item and leading, anchoring at remaining-start or inspected
abstract coordinate. Inline Body absence, including EOF, retains its complete
Item/leading and leaves an absent semicolon unconsumed. Colon dedent first
acquires its Item, then publishes Body Missing at remaining-start and returns
newline, suffix, successor coordinate and LineEntry unchanged. Existing local
starter precedence and first-colon Description selection are retained.

## Evidence and execution

Cover fresh/frozen records, D7f and the real second-colon Body witness, starter
retries, leading, EOF/fence/close/stops, CRLF/UTF-8, no-cascade, accepted forms,
semicolon and child ownership. M2 implementation and specification/regression
audits are complete; the one accepted boundary repair proves fresh/frozen CRLF
handoff. Static cost remains O(bytes + structural work); zero benchmark
processes.
