# Successor delimited PV structured-Error extent correction addendum

Status: Authoritative; bounded private construction complete

Implementation update (2026-09-08):
`2026-09-08-successor-recovery-authority-amendment.md` §§4--6 records the
completed successor ownership, extent and continuation proof. Its scoped
supersession removes legacy-malformed equality as an integration prerequisite.
New typed EffectRow ownership and public/O6 certification remain separate.

Date: 2026-09-07

Drafted-by: primary after direct legacy characterization of the excluded
PolymorphicVariantTagName Error carrier

Reviewed-by: M3 independent compiler/recovery and specification review; one
batched Draft repair followed by clean compiler/specification delta review

Approved-by: user

Approved-at: 2026-09-07

User-directed decision: select §5 option 1.  Authorize exactly the §2--3
same-carrier, three-phase private successor range/evidence correction and its
integration proof.  The user further directs that future comparably narrow,
fully reviewed, direct-compatibility corrections proceed under their documented
recommended option; a material semantic, public-surface, or scope expansion
still requires an explicit fresh decision.

Scope: one causal private successor range correction for the existing
`PolymorphicVariantTagName` structured Error when it already encloses an
authorized `ParenthesizedGroup` ordinary-horizontal gap emission in its
`Initial`, `AfterExplicitSeparator`, or `AfterItem` phase.  It neither changes
PV admission/recovery construction nor grants a general PV, Call, or EffectRow
extension.  It authorizes no code or expected-output change until a fresh user
decision is recorded.

Depends-on:

- `2026-09-07-successor-delimited-boundary-priority-correction.md` §§1--4;
- `2026-09-07-successor-structured-recovery-reservation-amendment.md` §6;
- `2026-09-07-successor-structured-recovery-extent-validation-addendum.md`
  §2; and
- direct legacy evidence `df0fdec7` in
  `crates/yu-syntax/src/grammar/type_expr.rs`.

## 1. Confirmed interaction and authority boundary

The approved delimited correction makes the immediate Parenthesized owner emit
its eligible ordinary horizontal gap before returning a raw outer close.  The
existing PV tag-name branch has already opened a structured Error around that
nested TypeExpression.  The reservation amendment derives that Error's end
from the returned pending Item's remaining start; the extent-validation
addendum independently requires its range length to equal every byte emitted
inside the Error.  Therefore a Parenthesized-owned gap emitted inside the open
Error necessarily extends that Error's complete range.  Clamping the Error
range, adding a PV special case to the shared delimiter helper, or moving the
gap back outside the Error would each violate an existing ownership contract.

The original delimited correction deliberately excluded expected-output and
NonTypeApply PV recovery-oracle changes.  Its user approval therefore does not
authorize accepting the enclosing-record delta merely because the local P
frontier change is approved.

Direct legacy evidence now execution-pins the relevant outer fact without
claiming topology equality:

| source | legacy tag-name Error | actual PV `}` | legacy malformed topology |
| --- | --- | --- | --- |
| `:{(A}` | `2..4`, `(A` | native at `4..5` | `Error > Unknown` |
| `:{(A }` | `2..5`, `(A ` | native at `5..6` | `Error > Unknown` |
| `:{( }` | `2..4`, `( ` | native at `4..5` | `Error > Unknown` |
| `:{(A, }` | `2..6`, `(A, ` | native at `6..7` | `Error > Unknown` |
| `:{(A )}` | `2..6`, `(A )` | native at `6..7` | `Error > Unknown` |

The retained prefix control `:{@ (A}` has Tag Error `2..4` followed by TagName
Error `4..6`.  Every row, including its `::Next` continuation form, is
lossless.  Legacy publishes no inner Parenthesized node, native Whitespace,
Missing node, or inner recovery record; the successor's already-authorized
owner-local topology remains distinct.  The legacy outer Error extent, native
PV close, and continuation are the compatibility facts relevant here.

## 2. Proposed same-carrier three-phase successor disposition

Only when all of the following are true may the existing structured TagName
Error range advance with the authorized Parenthesized frontier:

1. the existing PV wrong-kind tag-name branch has already opened its structured
   Error;
2. its nested TypeExpression reaches one of the approved Parenthesized
   `Initial`, `AfterExplicitSeparator`, or `AfterItem`
   generic-caller/outer-close ordinary-horizontal dispositions; and
3. Parenthesized emits that one eligible horizontal Whitespace before handing
   the same raw outer `}` back.

The following rows are one existing TagName Error carrier, not distinct PV
recovery producers.  The first three are the known shared-helper consequences
that this addendum resolves; the fourth is a retained matching-local control.

| P phase | source | TagName Error / direct extent | P direct disposition | complete record order |
| --- | --- | --- | --- | --- |
| `Initial` | `:{( }` | `2..4`, `( ` | Whitespace `3..4`, Item Missing then close Missing `4..4`, raw PV `}` `4..5` | TagName Error ID 0, P close Missing ID 1 at `4..4` |
| `AfterExplicitSeparator` | `:{(A, }` | `2..6`, `(A, ` | Whitespace `5..6`, Item Missing then close Missing `6..6`, raw PV `}` `6..7` | TagName Error ID 0, P close Missing ID 1 at `6..6` |
| `AfterItem` | `:{(A }` | `2..5`, `(A ` | Whitespace `4..5`, close Missing `5..5`, raw PV `}` `5..6` | TagName Error ID 0, P close Missing ID 1 at `5..5` |
| matching local | `:{(A )}` | retained `2..6`, `(A )` | Whitespace `4..5`, actual `)` `5..6`, PV `}` `6..7` | retained TagName Error only; no P close record |

For every changing row, the private successor expectation changes only as
follows:

- the existing TagName Error extent, direct text, and singleton
  unexpected/expectation ranges are exactly the changing table row;
- the Error contains the Parenthesized owner; Parenthesized directly contains
  the table's native Whitespace and its table-specified Missing sequence;
- the actual PV `}` remains an outer native token at the table-specified range,
  outside that Error, and continuation/remainder remain unchanged; and
- recovery role, kind, count, order, IDs, sources, primary index, and all
  other fields remain the existing TagName Error followed by the existing
  Parenthesized close Missing.  No PV record, Error terminal, payload, or
  admission behavior is added.

The two fresh Parenthesized rows retain the delimited correction's Item-then-
close Missing CST sequence and Parenthesized's existing record distinction:
the Item remains untyped and the close is the existing typed P close record.
This addendum claims no legacy topology equality for them.  `:{@ (A}` remains a
retained three-record structured-reservation control, not an excuse to broaden
this same-carrier disposition.

Supersedes if approved, and only for the three tabled existing private
structured TagName Error expectations: (a) the original delimited correction's
sentence that authorizes no private successor expected-output change, and (b)
the `embedded recovery fact` exclusion in its §3 scope list, solely insofar as
either would prohibit the tabled range/evidence updates.  The separate
`NonTypeApply PV/declaration parity` exclusion is retained and is not
superseded.  This addendum also does not authorize any new PV recovery producer,
a change to `variants.rs`, a Call or EffectRow PV interaction, public output,
fixture or golden changes, O4/O6 credit, or legacy-parser modification.

## 3. Bounded proof, construction, and rollback

Before a successor expectation changes, retain the direct legacy controls in
§1 and add focused fresh/frozen successor proof for every changing table row:

1. full TagName Error and Parenthesized close records, IDs/order, diagnostics,
   error extent/text, direct CST ancestry, Whitespace/close ranges, raw outer
   close, remainder, AST, origin, Item frontier, line entry, stop/context,
   and continuation;
2. equality of the emitted structured Error extent with its direct emitted-byte
   coverage, while proving the actual PV `}` is excluded;
3. no-gap and matching-local-close controls plus retained `:{@ (A}` three-
   record reservation evidence; and
4. a bounded sibling audit of every reachable Parenthesized ordinary-horizontal
   generic/outer row enclosed by a TagName structured Error.  It must prove
   unchanged record identities/order/count and must return to architecture if
   another outer range, Error carrier, Call, or EffectRow path changes.

If approved, construction changes only the affected private successor tests
and integrates the already-reviewed owner-local delimiter candidate if all
proof obligations hold.  The unintegrated candidate's current failure against
the old `:{(A }` TagName Error `2..4` expectation is not accepted before that
approval.  Construction may not patch `variants.rs`, clamp an Error range,
special-case PV in the shared delimiter helper, alter the direct legacy test,
or change public/fixture/golden output.

Any contradictory legacy extent, changed record count/order/role, consumed PV
close, range unequal to emitted Error extent, or additional outer carrier
returns this work to architecture without integration.

## 4. Cost and review

The candidate adds no PV branch, traversal, allocation, clone, range storage,
or timing work.  This addendum's only output delta is the existing structured
record's mechanically derived range/evidence coordinate.  Timing budget is
zero samples/processes unless review finds material uncertainty.

This is M3 because it amends a structured recovery oracle.  Independent
compiler/recovery and specification review must close the Draft before a fresh
user decision.  After approval and construction, independent regression review
must cover the bounded structured-PV sibling surface; compiler/specification
delta review then closes the gate.

## 5. User decision

The user selected option 1 on 2026-09-07.  The existing TagName Error may
advance across the §2 same-carrier three-phase Parenthesized-owned horizontal
bytes, matching direct legacy extent while preserving every other PV recovery
fact.

Option 2 was not selected.  It would retain the existing PV recovery oracle,
leave the base delimited correction Authoritative outside this interaction, and
return this P-in-structured-PV interaction to architecture without clamping
the Error or creating a PV special case.
