# 2026-09-10

## CST slot-schema inventory preflight

- A read-only census found 123 direct grammar recovery-publication calls in 39
  source files, plus the special newline-Missing path. This is publication-site
  coverage, not a claim of 123 semantic slots: shared helpers and caller roles
  still need ordered child-phase and ancestor-context mapping.
- The inventory partitions dedicated tails, shared expression/type/pattern
  entries, delimited and structured owners, statement/layout containers,
  literal/rule families, declaration headers, and declaration payload/import
  owners. Only dedicated child positions are locally distinguishable; all
  context-sensitive rows remain open until their parent-phase schema is
  specified and reviewed.
- The witness `{([)]:A}` has two temporary legacy records but one contiguous
  raw Error group beneath `NamedRecordType`. Under the approved CST projection,
  it becomes one documented malformed sequence-slot diagnostic; old record
  splitting is neither reconstructed from hidden provenance nor used to justify
  extra structure. No parser/API migration has started.

## AssignmentTail direct-inline schema draft repair

- Specification and compiler/recovery audits reviewed the representative
  direct-inline Assignment RHS proposal. They found no implementation defect;
  they required the Draft to name its concrete Rowan children and trivia
  placement rather than use an abstract `Expression`/`InlineRhs` shorthand.
- The Draft now distinguishes direct `AssignmentTail` leading before an
  accepted or initially rejected RHS from retry leading inside the new
  `OperatorChain`; it names the excluded `IndentedStatementBlock` dependency.
  Missing and a maximal raw Error group both project the existing
  `Assignment(Rhs)` `Expression` expectation with primary index zero. Nested
  recovery keeps its own grammar-owner slots.
- This remains a Draft, not an Authoritative completed slot schema. Exact CST
  evidence for valid RHS, Missing, terminal Error, Error-to-retry leading and
  nested recovery remains required before it can be promoted or reflected in
  the public construct page. No parser/API/test change or measurement ran.

## AssignmentTail direct-inline CST evidence

- Added focused Rowan assertions for direct `AssignmentTail` children and
  ranges: accepted `Equals`/leading/`OperatorChain`, protected Missing,
  terminal Error, Error-to-retry with retry leading under the RHS child, a
  contiguous three-leaf Error group with interior leading, and nested
  FieldTail Missing. The assertions deliberately ignore temporary recovery
  records as structural evidence while retaining their existing controls.
- Specification and compiler/recovery delta audits closed the narrow Draft
  proof with no remaining finding. Indented RHS, the full slot inventory,
  executable CST diagnostic interpretation, parser-ledger retirement and
  Authoritative promotion remain open. Focused module, format and diff checks
  passed; benchmark use was zero.

## AssignmentTail direct-inline schema approval

- The user approved the focused AssignmentTail direct-inline RHS slice as
  Authoritative. The approval covers its concrete `Equals`/leading/
  `OperatorChain` topology, Missing and raw Error projection, retry and nested
  ownership only. It authorizes reconstruction of that public EN/JA construct
  page from the slice.
- The surrounding schema document remains Draft elsewhere. Indented RHS, all
  other slots, the complete inventory, CST diagnostic interpreter,
  parser-ledger retirement and public diagnostic API remain explicitly open.

## AssignmentTail public reference reconstruction

- Reconstructed the paired EN/JA AssignmentTail pages from the Authoritative
  direct-inline RHS slice. The pages now specify exact Rowan child/trivia
  order, Missing and raw Error forms, retry and nested ownership, projection,
  and the indented-RHS exclusion without exposing parser migration machinery.
- Independent public-reference review required one repair to remove temporary
  parser-record and ledger provenance. The repaired EN/JA pair is conformant;
  both mdBook builds and scoped diff checks passed. No compiler/API change or
  benchmark ran.

## Fixed-tail name-slot schema draft

- Added a Draft slice for the dedicated `FieldTail(Name)` and
  `PathTail(Segment)` slots. It uses concrete `Dot`/`ColonColon` Rowan tokens,
  one zero-width Missing or one maximal raw Error group per name slot, and
  preserves the existing no-replacement-name outer-tail handoff.
- Specification and compiler/recovery preflight repaired token naming,
  Field-only projection priority for `.{`/`.(`, Path `::{` raw recovery,
  lone-colon priority, raw-run terminators and unchanged outer context. Exact
  direct-child evidence and user promotion remain open; no parser/API/test
  change or measurement ran.

## Fixed-tail direct CST evidence

- Added direct Rowan evidence for the FieldTail/PathTail Draft: exclusive child
  alternatives, native leading ownership, Missing/Error ranges, sibling outer
  continuations, projection precedence, UTF-8, CRLF, quoted fence, threshold
  and ML handoff. It never uses recovery records to prove CST topology.
- Specification and compiler/recovery review repairs closed the evidence. The
  quoted-fence control distinguishes root-relative CST range, inspected fence
  boundary and true successor-coordinate CRLF extent. Focused tails tests
  passed 20/20 with format and diff checks; promotion remains open.

## StringLiteral terminator schema draft

- Added a Draft for the outer non-Rule StringLiteral terminator: its final
  direct `StringEnd` or zero-width Missing, opener-leading ownership, exact
  heredoc width, and nested escape/interpolation ordering. It excludes outer
  Error/Invalid and leaves all child schemas separate.
- Specification and compiler/recovery preflight aligned trivia-elided notation
  and quote-run ownership across ordinary text, Unicode Error and format text.
  Direct CST proof and user promotion remain open; no parser/API/test change or
  measurement ran.

## Dedicated Rule-slot schema draft

- Added a Draft for RuleBody/parenthesized RuleItem closes, RuleCapture RHS,
  RuleField/RulePath names and RuleSequence raw items. It uses the atom opener
  discriminator, terminal missing-close/capture branches, exact leading/Error
  ownership and explicit ExpressionList/String/Virtual delegation.
- Specification and compiler/recovery preflight corrected post-close
  over-admission, capture terminality, context stops and parent-local trivia.
  Direct CST evidence and promotion remain open; no parser/API/test change or
  measurement ran.

## Dedicated Rule-slot direct CST evidence

- Added direct Rowan evidence for close discriminators, capture terminality and
  retry, nested same-offset Missing, Field/Path one-item Error and outer
  sibling postfixes, RuleSequence Error grouping, newline/fence/UTF-8/CRLF and
  public Root leading conservation. Recovery records remain separate controls.
- Specification and compiler/recovery review repairs closed the evidence.
  Focused Rule controls, format and diff checks passed; Draft promotion remains
  open and delegated ExpressionList/String/Virtual schemas are unchanged.

## StringInterpolationBody sequence schema draft

- Added a Draft limited to the interpolation's root-style statement sequence;
  it deliberately does not generalize `BlockStatementSeparator` to other
  block owners. It distinguishes `Statement(Missing)` after separators from a
  direct body separator Missing, accepted separator nodes and raw body Errors.
- Specification preflight corrected both direct-child topology and successor
  leading/newline phase rules. Direct CST evidence and promotion remain open;
  no parser/API/test change or measurement ran.

## StringLiteral direct CST evidence and root-losslessness follow-up

- Added direct terminator evidence for normal/heredoc, UTF-8/CRLF EOF, fence,
  Pattern/Rule callers, Unicode and format child ownership, and token-inclusive
  final Missing placement. Focused string recovery tests passed 13/13.
- The isolated interpolation EOF witness `"%{  ` leaves two trailing spaces in
  its pending EOF Item while its local CST ends at byte 3. This is the existing
  owner contract, not yet evidence of production loss: SourceRoot owns terminal
  EOF leading. Public Root witnesses now prove those spaces, CRLF and a trailing
  line comment are native Root tokens in source order and `green.to_string()`
  equals the full source. String terminator promotion remains separate; no
  literal-local trivia was fabricated.

<!-- daily-append-anchor: 2026-09-10 -->
## TypePathTail segment schema and fence repair

- Added a Draft for the Type `ColonColon` segment, retaining its distinct
  retry/application/contextual-boundary contract rather than reusing expression
  PathTail. Direct CST evidence covers the specified slot forms.
- That evidence found a post-Error abstract-fence panic. TypePathTail now
  short-circuits boundary Items before lexical token observation and hands the
  exact Item/leading to the existing boundary path. Focused TypePathTail tests
  passed 9/9; package check, format and diff checks passed. Compiler/recovery
  review approved the bounded repair. Promotion remains open.
## StringInterpolationBody direct CST evidence

- Added direct Rowan evidence for all sequence forms, including immediate
  Error-to-Statement retry without a separator Missing, separator-leading
  phases, nested Missing paths, fence and Root source conservation.
- Specification and compiler/recovery delta reviews closed. Focused virtual
  tests passed 16 with format and diff checks; promotion remains open.

## TypeArrowTail actual-arrow RHS schema and shallow-newline repair

- Added a Draft limited to the RHS following an actual Type `Arrow`; the
  distinct BracketRowArrow omitted-arrow slot remains excluded. The Draft
  records direct Arrow/Missing/Error topology, retry ownership and the
  phase-specific boundary rules without depending on temporary recovery
  records.
- Preflight exposed a post-Error shallow-newline conformance defect: the retry
  caller admitted a Type NUD after lexical recovery had already stopped at an
  equal-or-shallower newline. The owner now preserves that complete Item and
  emits no cascade; deeper continuation remains a retry. Direct LF/CRLF
  shallower/equal/deeper Rowan regressions passed in the focused ArrowRhs set
  (11 tests), with format and diff checks. Independent compiler/recovery review
  approved the bounded correction.
- Direct Rowan evidence now covers the actual-arrow suffix's child order,
  native leading, raw Error/retry ownership, terminal handoff, LF/CRLF/fence,
  foreign quote-prefix ranges, BracketRowArrow discrimination, nested Missing
  and public Root conservation. It proves topology from the tree and pending
  Items only, without reading recovery records. Regression review closed the
  initial protected-boundary, caller-stop/separator and phase-order proof gaps;
  focused tests passed 10. Draft evidence is complete and user promotion remains
  open.

## BracketRow required-arrow continuation schema draft

- Added and independently audited a Draft for the ordered required-arrow slot
  after a trailing BracketRow, including its incomplete-row exit. It keeps the
  direct `TypeArrowTail` children, separates arrow punctuation recovery from
  the later actual-arrow RHS, and specifies native leading, Error retry,
  boundary handoff and same-offset nested row-close ownership.
- Direct CST proof and user promotion remain open. No parser/API/test change or
  measurement ran for this Draft-only preflight.

## BracketRow required-arrow direct CST evidence

- Added direct Rowan proof for the required-arrow continuation: ordered row,
  Missing, Error, actual Arrow and RHS children; leading and retry ownership;
  protected punctuation/caller/outer/fence handoff; LF/CRLF depth; same-offset
  nested row-close versus direct arrow Missing; recovered ArrowRhs separation;
  UTF-8 Error byte ranges and public Root conservation.
- Regression review initially required exact protected-item/phase evidence and
  then approved the repaired suite. The tests derive proof from the CST and
  pending Items, never recovery records. Focused BracketRowArrow tests passed
  7; Draft evidence is complete and user promotion remains open.

## LeadingEffectTypeHead schema draft

- Added and independently audited a Draft for the required Type head following
  a leading BracketRow. It is a named `TypeExpression` slot, not a new node;
  the Draft preserves direct Rowan leading/Error ownership, disabled-second-row
  raw Error behavior, nested descendants and caller/boundary rules.
- The Draft now explicitly records empty- versus nested-depth separator
  ownership and the fact that a CST-derived Missing uses its Rowan zero-width
  range even where a temporary legacy fence record used a later boundary
  coordinate. Direct CST proof and user promotion remain open. No parser/API/
  test change or measurement ran for this Draft-only preflight.

## LeadingEffectTypeHead direct CST evidence and PV terminal repair

- Added direct Rowan evidence for leading-row scalar/structured heads, nested
  same-offset Missing, disabled-row Error grouping and retry, UTF-8 byte ranges,
  protected boundary handoff, LF/CRLF, fence local ranges versus pending
  coordinates, outer Invalid and Root source conservation. The proof uses only
  the tree and pending Items, not recovery records.
- The terminal-primary controls found an existing owner defect: an incomplete
  PolymorphicVariantType was passed into outer tail scanning and could consume a
  deeper newline as TypeApply. Its wrapper now continues tails only after its
  consumed-close success exit; every incomplete exit is returned unchanged.
  Direct/recovered/leading-row LF/CRLF, caller, outer-close, EOF, fence and
  closed-PV continuation controls passed. Compiler/recovery and regression
  reviews approved. Focused leading-row tests passed 10; package check, format
  and diff checks passed. Draft evidence is complete and user promotion remains
  open.

## TypeCall close-slot schema architecture return

- Preflight found that current direct `TypeCallTail` Error leaves cannot
  structurally distinguish an argument Error from a close Error. The
  source-derived witnesses `T(@)` and `T(])` have the same direct
  `LParen, Error, RParen` topology despite distinct expectations, while
  `T(@])` loses the adjacent argument-to-close transition.
- Opaque Error-payload relexing contradicts the current schema proposal and a
  retained parser ledger contradicts the CST-derived diagnostic destination.
  This is a structural-representation decision, not a test or production bug:
  no TypeCall close Draft or implementation change has started. A user-selected
  structural boundary is required before this slice can proceed.

## TypeCall close-slot structural decision

- The user selected a `TypeCallClose` Rowan node for the terminal TypeCall
  close phase. It separates direct argument Error from close Error while
  retaining raw Error leaves, no `Invalid`, no Error relexing and no parser
  ledger. The new authoritative amendment specifies its ordered children,
  source/boundary ownership, diagnostic projection and direct-CST evidence.
- Construction remains pending one required proof: the generic post-child
  fallback must be unreachable for Call before every committed TypeCall can be
  asserted to have a final close node. A reachable path is an architecture
  contradiction, not permission to fabricate Missing.

## TypeCall post-argument fallback contradiction

- The required reachability proof found a concrete Call path: `T(A@)` emits
  only `T(A`, leaves `@` as an unchanged pending Unknown Item with the closing
  `)` still in the suffix, and publishes no close slot. The focused witness
  proves this from direct CST, Item, origin, line and remainder only; recovery
  records are intentionally not observed.
- This triggers the close-node amendment's stop condition. The `TypeCallClose`
  topology remains user-approved, but construction is blocked on a separate
  user-selected policy for Call's unclassified post-argument Item. It is not
  authorized to fabricate a close Missing or wrap this incomplete path merely
  to satisfy the new topology.

## TypeCall residual close-recovery policy

- The user selected policy A for the proven `T(A@)` residual: after existing
  Call dispatch declines it, the residual enters irreversible terminal close
  recovery. It and following nonboundary Items become raw `TypeCallClose` Error
  content through matching `)` or the established protected/EOF exit.
- The authoritative amendment excludes fabricated separator/argument recovery,
  Error relexing, state and replay. Existing matching-close, boundary,
  separator, pipe, layout and admitted-argument priority remains ahead of the
  new final fallback.

## TypeCall close-node and residual-policy construction

- Implemented `TypeCallClose` as the one terminal close-phase node for Call
  only. Arguments, separators and argument Error stay direct `TypeCallTail`
  children; terminal `RParen`/Missing, close trivia and close Error live under
  the new node. Existing syntax kinds were preserved by appending the kind.
- The approved residual fallback now enters the existing irreversible close
  scan only after all normal Call dispatch declines it. Direct CST controls
  cover argument-versus-close Error separation, `T(A@)`/residual runs, Missing,
  EOF/protected handoff, fences, Root conservation and external tails. Compiler/
  recovery review found one selector-ownership weakening; its test-only repair
  closed in the same M2 round. Focused TypeCall tests passed 21, package check,
  format and diff passed. `tests::type_expr` additionally had three unrelated
  PV newline failures, and syntax-kind tests retain the pre-existing stale
  Unknown discriminant assertion; neither was changed. Benchmark use: zero.

<!-- daily-append-anchor: 2026-09-10 -->
