# 2026-09-09

## CaseLike Arrow/Body typed recovery

- Completed the private M2 Arrow/Body gate under
  `2026-09-08-successor-case-like-arrow-body-current-item-recovery.md`.
  Simultaneous Arrow/Body absence now emits one Arrow-primary Missing node and
  record with ordered Arrow punctuation and Body Expression expectations;
  separate actual-arrow absence remains Body Missing.
- Body malformed runs now publish a typed `CaseLike(Body)` Error through the
  sealed lexical scan. Protected boundaries and their leading remain pending;
  an Error reaching any boundary no longer adds a second Body Missing. Guard
  successor anchoring advances after consumed guard input.
- E12f/g/j literals, fresh/shifted/frozen/seeded reconciliation, Pattern/
  Handler/Guard ordering, UTF-8/CRLF/foreign-prefix, close/opener/fence and
  native operator/prefix controls are covered. Ten normalized Case/Catch
  shared-cause Missing counts decrease by one; text, remainder, boundary and
  line assertions are unchanged.
- Compiler/recovery and regression delta reviews found no issues. Primary
  focused CaseLike/normalized/recovery-output run passed 129 tests; package
  check, format and diff passed. Benchmark use: zero samples/processes.
  Indented-body ownership, aggregate/public certification and Yumark remain
  open.

## CaseLike Separator and Catch-close typed recovery

- Completed the private M2 gate under
  `2026-09-09-successor-case-like-separator-close-current-item-recovery.md`.
  Same-Item inter-arm retry now publishes `CaseLike(Separator)` with comma
  expectation; CatchBlock centrally consumes its matching `}` or publishes one
  `CaseLike(Block)` close-Brace record while preserving the caller Item.
- The centralized completion repairs trailing-comma close ownership. A review
  control exposed that empty `catch x {}` sent its matching brace to Pattern;
  one bounded repair propagates only CatchBraced's local RBrace stop, retaining
  mandatory Pattern and Arrow/Body recovery before Catch consumes the brace.
  An unclaimed `]` remains native Pattern recovery, so no stop set was widened.
- Compiler/recovery and regression reviews closed after that repair. Primary
  CaseLike/normalized/recovery-output run passed 59 tests; package check,
  format and diff passed. Benchmark use: zero samples/processes.

## For structural typed recovery

- Completed the private M2 For Pattern/InKeyword/BodyIntroducer and shallow
  Body gate under `2026-09-09-successor-for-structural-current-item-recovery.md`.
  Added finite `KeywordEvidence::In`, ordered colon/open-brace introducer
  expectations, and narrow initial Pattern role transport; nested Pattern
  recovery remains native.
- Completed Pattern EOF now publishes its one required InKeyword record. The
  lexical introducer Error and shallow-colon Body return protected Items without
  cascades. A review repair adds exact fence/foreign-prefix and nested
  AliasBinding no-cascade evidence in fresh/shifted/frozen/seeded modes.
- Compiler/recovery and regression reviews found no production defect. Primary
  For/Pattern/normalized/output run passed 98 tests; package check, format and
  diff passed. Benchmark use: zero samples/processes.

## TypeDeclaration header typed recovery

- Completed the private M2 Name/DefinitionIntroducer gate under
  `2026-09-09-successor-type-declaration-header-current-item-recovery.md`.
  Both raw slots now publish atomic declaration records while retaining the
  existing nominal/equality classifier, raw-header identifier priority and
  exact lone-equals routing.
- Selected malformed leading ownership emits retry and ordinary EOF leading in
  TypeDeclaration outside Error. The approved assertion deltas preserve all
  source literals, continuation and cardinality; protected rejected `with`/
  `impl` Items now retain their newline leading.
- Pre-write spec audit and compiler/recovery review found no production defect.
  Primary TypeDeclaration/Type/normalized/output run passed 346 tests; package
  check, format and diff passed. Benchmark use: zero samples/processes.

## Enum/Error header typed recovery

- Completed the private M2 owner-local Enum/Error Name and BodyIntroducer gate
  under `2026-09-09-successor-enum-error-header-current-item-recovery.md`.
  The four body-starter expectations retain the existing Enum/Error companion
  asymmetry; no shared header abstraction was introduced.
- Initial body-starter priority over active stops stays intact. Header Error
  leading is selected as declaration-owned outside Error for retry/EOF, while
  terminal sigil and Bodyless no-cascade controls remain unchanged.
- Review required paired multi-token native Error and foreign-prefix evidence;
  the test-only repair closes it in fresh/shifted/frozen/seeded modes. Primary
  Enum/Error/Variant/normalized/output cone passed 160 tests; package check,
  format and diff passed. Benchmark use: zero samples/processes.

## Shared declaration named-field internal recovery

- Completed the private M2 Field/FieldName/FieldColon gate under
  `2026-09-09-successor-declaration-named-field-current-item-recovery.md`.
  The shared field driver now receives finite Struct or Enum/Error variant role
  transport, so it never labels a variant record as Struct; existing FieldType
  transport still leaves nested malformed Type records native.
- A review repair made every Error fact cover its actual emitted Item, gives an
  active stop priority over colon/Type retry, and limits Type retry to
  same-line Items. Initial leading is field-owned and retry/EOF leading is
  outside Error; the two affected old field Error-text assertions were updated
  under the recorded successor rule without removing their source literals.
- Pre-write and repair-delta specification audit closed. Struct: 30;
  declaration variant: 24; Equals: 5; normalized/recovery-output: 108;
  package check, format and diff passed. Benchmark use: zero samples/processes.
  At this checkpoint the shared field sequence/separator/close owner and its
  coherent `declaration/fields.rs` move were still open; the later entry below
  records their completion.

## Shared declaration field sequence and topology

- Completed `2026-09-09-successor-declaration-field-sequence-current-item-recovery.md`.
  The shared driver now publishes caller-selected field, separator, and close
  records for Struct and Enum/Error payloads; variant tuple separators retain
  `NamedFieldSeparator` without borrowing Struct identity or adding a role.
- `declaration/fields.rs` owns the neutral driver, recovery helpers, lexical
  current-Item acquisition, predicates, and next-field candidate. `struct_decl.rs`
  retains Struct header/body role selection and trailing ownership; no reverse
  Struct dependency remains.
- Two read-only specification passes closed origin, protected-leading, Borrow
  close, and topology findings. Struct 31, variant 25, Equals 5, normalized/
  recovery-output 108, package check, format, and diff passed. Benchmark use:
  zero samples/processes.

## Cast PatternIntroducer current-Item recovery

- Completed `2026-09-09-successor-cast-pattern-introducer-current-item-recovery.md`.
  The Cast opener slot now publishes its existing typed role for all Missing
  and maximal Error paths; malformed retry to a bare Pattern does not append a
  duplicate Missing.
- M2 preflight plus two delta reviews closed Error extent ownership at
  same-line EOF. A nonempty EOF-leading suffix extends the Error node, record,
  and unexpected fact together; empty/newline EOF leading stays pending.
  Cast 12; Pattern/required-Type/recovery-output 84; package check, format and
  diff passed. Benchmark use: zero samples/processes.

## Cast Pattern current-Item recovery

- Completed `2026-09-09-successor-cast-pattern-current-item-recovery.md`.
  An initially absent Pattern now publishes `Cast(Pattern)` before the native
  Pattern child and follows existing local-close/phase handoff; malformed
  nonempty Pattern recovery retains its native owner.
- M2 review passed. Cast 13; Pattern/normalized/recovery-output 162; package,
  format and diff passed. Benchmark use: zero.

## Cast Pattern close current-Item recovery

- Completed `2026-09-09-successor-cast-pattern-close-current-item-recovery.md`.
  The local close's two Missing sites and bounded Error run publish the typed
  `ClosingDelimiter(CastPattern, Parenthesis)` role. Local close is consumed
  first; target/form and outer boundaries remain unread; Error has no
  same-slot Missing cascade.
- The selected same-line EOF suffix extends Error node/record/fact together,
  while empty/newline EOF leading stays pending. M2 preflight and delta review
  found no concrete defect. Cast 14; direct Pattern/normalized/recovery-output
  162; package check, format and diff passed. Benchmark use: zero.

## Cast TargetIntroducer current-Item recovery

- Completed `2026-09-09-successor-cast-target-introducer-current-item-recovery.md`.
  Its four Missing paths and bounded Error run now publish the typed
  `Declaration(Cast(TargetIntroducer))` role with a colon expectation. Colon
  and Type retries remain distinct; form and protected boundaries stay unread;
  Error does not cascade a second TargetIntroducer record.
- Same-line EOF leading extends Error node/record/fact together; empty/newline
  EOF leading stays pending. M2 preflight and delta review found no concrete
  defect. Cast 16; direct Pattern/Type/normalized/recovery-output 358; package
  check, format and diff passed. Benchmark use: zero.

## Cast BodyIntroducer current-Item recovery

- Completed `2026-09-09-successor-cast-body-introducer-current-item-recovery.md`.
  `cast_form_normalized` now publishes the typed semicolon-expectation role
  for its raw Missing and Error sites. TargetType and post-equals Body remain
  separate owners.
- The lexical-only form-punctuation scan leaves exact `;` and `=` outside
  Error for the existing form paths, while `==` remains malformed. Boundary
  Items stay unread, Error has no Body cascade, and same-line EOF leading
  extends the Error node/record/fact together.
- M2 preflight and delta review found no production defect. Cast 19;
  Pattern/Type/normalized/recovery-output 358; package check, format and diff
  passed. Benchmark use: zero samples/processes.

## Cast Body current-Item recovery

- Completed `2026-09-09-successor-cast-body-current-item-recovery.md`. The
  post-equals shallow-newline, inline-boundary, and malformed-run sites now
  publish `Declaration(Cast(Body))` with Expression; the deeper indented path
  keeps its existing `Cast(IndentedStatement)` child role.
- The sealed lexical Expression Error run retries a real NUD without a second
  Body record. Same-line EOF leading remains Cast-owned; LF/CRLF and protected
  boundary leading remain pending. The unreferenced raw generic Missing helper
  was removed.
- M2 preflight and delta review found no production defect. Cast 22; expression
  recovery 6; normalized 83; recovery-output 25; package check, format and
  diff passed. A later owner-contract audit corrected the stale Role/Impl
  statement-dispatch assertion that had exposed the unrelated failure.
  Benchmark use: zero samples/processes.

## Cast owner audit and ledger reconciliation

- All Cast phase publishers are now audited: opener, Pattern, local close,
  target, target Type child, form, Body, and indented Statement child publish
  typed recovery or retain their established nested owner. No raw Cast
  Missing/Error publisher remains.
- Removed the stale `CastRole::Pattern` dead-code exemption. Reconciled the
  typed-recovery ledger's detailed completed declaration inventory with its
  stale broad open-group wording; next recovery selection requires a fresh
  raw-site inventory rather than a guessed declaration slice.

## Production publisher inventory and live-role cleanup

- A strict production scan now finds no Missing/Error publisher without a
  typed committed record. Rule ExpressionList's manual newline callback is
  included: it commits its exact `ExpressionList(Item)` Missing draft in the
  callback that starts the node.
- Removed only stale deferred-owner `dead_code` exemptions from live For, If,
  and CaseLike recovery roles. Deferred vocabulary remains annotated. This was
  M0 cleanup with no recovery-policy change; package check, format, and diff
  passed. Benchmark use: zero samples/processes.

## Statement dispatch owner-contract repair

- The stale combined pending-Equals test for `my role = value` and
  `my impl = value` is now split into their direct declaration-owner controls.
  Neither required-Type owner claims Equals as a head boundary, so both retain
  lossless recovery through their established declaration paths. This changed
  test evidence only; focused statement dispatch, format, and diff passed.

## Operator compilation topology

- Completed `2026-09-09-operator-compilation-topology.md`. The immutable
  table/trie owner is now separate from HeaderOperator conversion,
  imported/local merge, and conflict collection in `operator_compilation`.
  Scanner and Pratt grammar owners remain unchanged.
- M2 split preflight and independent delta review found no defect. Scoped
  table/compilation/environment/diagnostic/full-parse/public controls passed
  31 tests with one existing manual measurement harness ignored; package check,
  format, and diff passed. Benchmark use: zero samples/processes.

## Source-root statement topology

- Completed `2026-09-09-root-statement-topology.md`. `source_file` now keeps
  Root product/frozen-reconciliation setup, and `root_statement` owns the
  unchanged source-root statement loop, operator body, root recovery, and
  concrete header scopes.
- M2 topology preflight and independent delta review found no defect. Scoped
  root/header/full-parse/public-boundary/recovery controls passed 63 tests with
  one existing manual measurement harness ignored; package check, format, and
  diff passed. Fence boundary and opaque Root Error changes remain separate
  Yumark gates. Benchmark use: zero samples/processes.

## Root opaque no-copy preparation

- Replaced the opaque Root Error tail's temporary `Box<str>` with a borrowed
  slice of its entry source remainder, delimited by the existing lexical
  skipper's consumed byte count. Unfenced CST, records, source ranges, origin,
  and line behavior remain unchanged.
- M1 implementation and independent regression review were clean. Root
  controls passed 12 tests; format and diff passed. This removes one local
  allocation/copy per opaque Error and adds no fence-aware scan or cell entry;
  that lexical-owner gate remains next. Benchmark use: zero samples/processes.

## Coherent successor syntax baseline

- After the related test-contract corrections and Root no-copy preparation,
  `cargo test -p yu-syntax --lib -- --test-threads=1` passed 1039 tests,
  ignored one existing manual measurement harness, and failed none in 1.75s.
  This is one phase-boundary validation, not completion evidence for the
  deferred Yumark cell/fence owner.

## Fence-aware opaque Root Error recovery

- Completed the M2 lexical prerequisite under the parsed-fence addendum
  §§3--5/§8. New `lexical::opaque_region` scans only after an already accepted
  opaque opener; Root retains Error CST/record ownership. Its recursive string,
  interpolation/code, comment, opaque Yumark and nested-fence paths judge every
  LF, whole CRLF and EOF before accepting the next physical line.
- Equivalent quote prefixes use the approved one-lazy-vector/one-boxed-carrier
  path and emit source-backed `YmQuotePrefix` segments in order. Close and
  transition Items are unconsumed; no body copy, replay, grammar parse, nested
  literal recovery or outer cell dispatch was introduced.
- Compiler/recovery and regression review were clean; the one test-only repair
  added exact Starter records plus UTF-8 LF/CRLF multi-prefix order/range
  controls. Full `yu-syntax` lib: 1043 passed, 1 existing ignored, 0 failed in
  1.70s; package check, format and diff passed. Benchmark use: zero.

## Boundary-capable root statement sequence

- Completed the M2 internal sequence gate. Source root remains the sole normal
  EOF-emitting wrapper; the shared private loop returns fenced close,
  transition and EOF Items untouched before layout/recovery/header effects.
  Fence now reaches statement/operator/root-recovery paths and `my`/`lazy`
  lookahead; source header reconciliation remains concrete while fenced state
  disables it and uses ambient `None`.
- Compiler/recovery and regression review were clean after two test-only exact
  boundary closure repairs. Full `yu-syntax` lib: 1049 passed, 1 existing
  ignored, 0 failed in 1.73s; package check, format and diff passed. The next
  gate is the private `YmYulangCodeCell` wrapper, not outer Yumark dispatch.

## Isolated real-stream cell wrapper

- Added test-only real-stream `YmYulangCodeCell` construction over the shared
  root sequence. It proves exact close/transition/EOF facts, body-leading
  emission once, no nested Root, UTF-8/common-root ranges, LF/CRLF, child
  recovery order, and immutable host operators versus local nonactivation.
- Architecture review established that no production Yumark document owner or
  AST product exists yet. Keeping the wrapper production-visible would add
  dead-code warnings, so it remains an isolated proof with warning-free package
  check; outer document/frame/AST construction is the next M3 gate.

## Parenthesized operator newline control

- Corrected the stale partial-output assertion for `(a +\nb)`. The shallow
  newline rejects infix admission, emits the approved Parenthesized separator
  Error for `+`, then admits `b` in the next chain and consumes `)`.
- Added LF/CRLF exact-record and native-newline controls, plus an explicit
  deeper-indent accepted-infix zero-recovery control. This changed test
  evidence only; focused operator/delimited controls passed 20 tests, format
  and diff passed.

## Virtual Colon parent-record control

- Corrected the stale universal one-record assertion for virtual Colon errors.
  EOF and fence now assert the required ordered RHS Error,
  interpolation-close Missing, and string-terminator Missing records; actual
  close remains the Error-only control.
- All six retained literal forms preserve text/remainder/boundary/frozen replay
  evidence. This changed test evidence only; focused Colon/StringLiteral/Virtual
  controls passed 23 tests, format and diff passed.

## Syntax-phase topology

- Completed `2026-09-09-syntax-phase-topology.md`. The generic `parse.rs`
  source is gone: `syntax_environment.rs` owns imported syntax inputs and
  provenance, `syntax_diagnostic.rs` owns the diagnostic model, and
  `full_parse.rs` owns HeaderInfo/environment/root assembly and ParsedFile.
  Root exports and observable syntax behavior are unchanged.
- M2 topology preflight and delta review retained one-way dependencies, opaque
  environment identity, and owner-local tests. Environment/diagnostic 5; full
  parse 9; public boundary 8 passed with one existing ignored manual
  measurement harness; package check, format and diff passed. Benchmark use:
  zero.

## Contextual RuleExpression production entry

- Completed the bounded M2 production-entry gate under the Authoritative
  direct-literal cone §§2, 4.2 and 6 / LC-8. `rule/mod.rs` now owns the
  committed RuleExpression wrapper and its existing RuleBody; Expression and
  Pattern own only their independent contextual admission and return to their
  existing tails after Rule completion.
- Admission requires a lexical Identifier exactly spelled `rule` and a single
  successor Item with `{` payload. Every non-brace/EOF/fence successor remains
  ordinary, while registered `rule` nullfix/prefix/infix operators retain host
  classification. Exact-form reservation intentionally wins over a pending
  caller brace stop: `if rule {a}: body` has RuleExpression as the Condition,
  with Colon and body still owned by If.
- Compiler/recovery review was clean. Regression review prompted test-only
  controls for assignment/`as`/ML tails, an LED word operator and that If
  reservation; no production repair followed. Entry 7 and inherited Rule 26
  tests passed, as did package check, scoped format and diff. Benchmark use:
  zero samples/processes. Canonical materialization, outer Yumark and
  Type-attached-Impl promotion remain open.

## Canonical product inventory refinement

- Refined the M3 Draft inventory without authorizing implementation. Candidate
  closure now includes the admitted Assignment/TypeAnnotation tails, Pattern
  String/RuleExpression primaries and Case/Catch arm terminators. These remain
  candidate product choices pending complete review and user approval.
- Corrected a retained-authority conflict: a malformed first Forall binder is
  one incomplete list entry, and a later admitted binder is a distinct complete
  entry. OperatorName is an all-or-none current atom whose enclosing signature
  owns unavailable-name recovery; no partial-name grammar was introduced.
- Independent specification and compiler/recovery review found no blocking
  defect; one candidate-status wording repair was applied. Undefined helper
  leaves, complete field-to-authority locators, source-coordinate/output API,
  static performance review and user approval remain open. No code, tests or
  benchmark processes were run for this Draft-only work.
- A follow-on helper-schema delta selected the currently admitted operator
  use/path, inline, PatternName/record-default, CaseLike, visibility/parameter
  and operator-header leaves. Independent review removed a nonexistent Struct
  parameter field and made incomplete physical endpoints total, including
  braced close-recovery leading. This remains Draft-only; it authorizes neither
  materialization nor a product API. No tests or benchmark processes were run.
- Closed the next inventory pass as an evidence queue: Type core/delimited/
  recursive physical envelopes and Apply boundary; inline/pattern sequence
  slots; declaration parameter/field/variant/derives lists; and SourceRoot
  coordinate publication. Specification and compiler/recovery delta reviews
  found no overclaim. It makes the remaining product-closure work finite but
  selects no API, range convention or implementation. No tests or benchmark
  processes were run.
- Added an unselected Type physical-envelope candidate from the first queue
  item. It preserves each selected owner's actual emission, including owned
  leading/recovery, without borrowing caller or unread-boundary leading or
  deriving endpoints from diagnostics. TypeRecordField/declaration-field and
  Apply/Forall/PV boundary choices remain open. It still requires repaired
  independent review, exact range controls and user approval; no code, tests or
  benchmark processes were run.
- Repair-focused specification and compiler/recovery review closed the initial
  leading, introducer-only endpoint and PV missing-boundary omissions in that
  Type candidate. It remains Draft-only and still needs exact range controls
  plus user approval; no code, tests or benchmark processes were run.
- Added unselected InlineArguments and Pattern sequence candidates. They keep
  first/subsequent inline slots, local separator advancement, pending Pattern
  retry and protected handoff separate. Recovery review corrected Record
  wrong-close handling: a consumed unclaimed close retains the pending item;
  it does not create a sibling. Specification/recovery review is clean after
  that repair. Exact controls, trailing-comma-after-recovery adjudication and
  user approval remain open; no code, tests or benchmark processes were run.
- Added unselected declaration parameter/field/variant/derives sequence
  candidates. They retain form-specific separators and close borrowing. Review
  corrected Variant and Derives introducer-only physical endpoints; tuple
  availability, empty entered sequences, accepted interior separators and some
  malformed field/parameter branches remain open. No code, tests or benchmark
  processes were run.
- Added unselected SourceRoot physical-envelope and root/cell coordinate-entry
  candidates. Full roots include their source input; selected cells retain
  terminal leading but exclude outer fence/boundary text. Shifted inputs need
  an explicit absolute region/frontier, and frontier/boundary equality remains
  unproven. Specification/recovery review is clean. No code, tests or benchmark
  processes were run.
- Added an unselected TypeRecordField/NamedField/TupleField recovery-product
  candidate. Grammar admission controls the outer skeleton; field envelopes
  retain field-owned retry bytes but exclude enclosing-sequence and unread
  successor publication. Record/Struct/Variant close distinctions remain
  intact. Review was clean after a sequence-leading wording repair. No code,
  tests or benchmark processes were run.
- Added unselected Apply/Forall/PV recursive boundary candidates. Their ranges
  follow owner publication without changing grammar admission or recovery. A
  review repair limits an extra Forall incomplete binder to first-binder retry,
  not later colon-role Error. No code, tests or benchmark processes were run.

## Rowan CST-only redirection

- The user selected one direct Rowan CST, no AST/materializer/product output,
  no new parallel ledger/account state, and removal rather than renaming of
  `CstOutput`. Existing individual expected/recovery diagnostics remain a
  required parse result.

- The direct-Rowan amendment is Authoritative: the user approved it after
  compiler/recovery,
  specification, regression and architecture review retain individual frozen
  diagnostics and recovery ordering while rejecting a replacement output
  facade and emitted-byte account. The reviewed implementation sequence first
  removes `CstOutput` with the old tree shape, then makes `Error` token-only
  and adds node-only `Invalid`.

- The final raw-recovery rule maps every remaining Item fragment, including
  interior trivia and Yumark quote-prefix fragments, to adjacent `Error`
  tokens. Leading already emitted by the grammar owner and retry/boundary
  leading remain outside. `Invalid` is limited to the reviewed structured
  polymorphic-variant and record-pattern owners. No code, tests or benchmark
  processes were run for this design-record update.
- Created a Draft successor amendment. It holds the three canonical
  materialization/product Drafts for supersession, proposes direct builder plus
  parser-owned diagnostic migration, and records the replacement runtime extent
  validation as focused controls rather than a bypassable global counter.
- The target node notation is XML-like documentation, not runtime XML. Raw
  malformed source is `Error text`; `Invalid` is structural only for genuine
  nested recovery. `syntax-reference` will become a bilingual language/Rowan
  CST reference, not a parser implementation chronicle. No code, tests or
  benchmark processes were run pending independent design review.

## Direct Rowan sealed-cursor construction repair

- Implemented the Authoritative cursor-capability clarification. `SyntaxIn` is
  a concrete borrowed cursor with private source/recovery fields and direct
  Rowan builder access; it exposes only the construction, reborrow, token and
  lexical-map operations used by committed callers. Its lexical operations
  create a temporary operator-only `LexRecover` view. `Recover` no longer
  implements `Recoverable`, and the generic chasa capability extensions from
  the earlier candidate are removed.
- Recovery publication and scoped header restoration use private cursor
  fields. The seeded lexical rejection control includes an explicit
  `&mut LexRecover` type annotation, verifies the unchanged source and
  diagnostic sequence, and then proves frozen publication mode remains scoped.
  This is implementation evidence; independent delta review is pending.
- `cargo check -p yu-syntax --tests --quiet` passed during construction. The
  final candidate passed the following focused command with 175 passed,
  0 failed, no warnings, and 894 filtered out:

  ```text
  cargo test -p yu-syntax --lib -- cursor::recovery::scoped_tests:: tests::recovery_output:: tests::output:: tests::declaration::operator_header:: tests::root:: tests::lexical:: tests::literal:: tests::rule_expression_list_recovery:: tests::pattern::recovery:: tests::type_expr::required_recovery:: tests::type_expr::equals_recovery:: rb_t_required_type_probe_rejection_preserves_output_and_input rb_t_arrow_rhs_rejected_retry_seal_preserves_successor_vector --test-threads=1
  ```

- Existing Error-node topology is retained for this construction phase. Full
  `yu-syntax` library verification ran once after independent delta review:
  1,067 tests passed, one was ignored, and the only failure was the existing
  `SyntaxKind::Unknown` fixed-discriminant assertion (`231` actual, `229`
  expected) in an unmodified file. The Error-token/Invalid migration remains
  open. Benchmark usage: zero samples/processes. No staging, commit or push
  was performed for this repair.

## Reviewed CST-derived diagnostic successor

- The user selected structural diagnostics derived from the direct Rowan CST
  during frontend traversal and environment diagnostics derived at the relevant
  CST occurrence; neither is parser-owned recovery output. `Invalid` remains
  structural only. A changed effective operator table can change parsing and
  requires reparse; provenance-only environment changes may reuse the CST and
  rerun analysis.
- Added the now-Authoritative
  `2026-09-09-successor-cst-derived-diagnostics-amendment-draft.md`. It
  specifies slot-sensitive `Missing`/Error-group/`Invalid` interpretation,
  Invalid preorder, one unified preorder including OperatorHeader conflicts,
  no parser recovery ledger or frozen reconciliation, and retirement of
  parser-issued IDs, unexpected-token categories and source flags. The exact
  per-slot schema remains a construction prerequisite.
- M2 specification and compiler/recovery reviews closed ordering, payload,
  planner-sharing, header and continuation concerns. No code or tests were run;
  user approved the reviewed public-contract change; the per-slot schema is
  the immediate implementation prerequisite.

## Rowan CST reference vertical slice

- Reframed `syntax-reference` as the bilingual Yulang surface-syntax and
  lossless Rowan CST reference. Added paired Rowan-notation and
  Root/header/diagnostic-ownership pages, with navigation and introduction
  updates. Existing construct pages are explicitly legacy implementation
  material pending phased reconstruction.
- Independent specification review required one repair: a complete conflicting
  `OperatorHeader` emits its environment diagnostic on entry before child
  recovery diagnostics. The pages now distinguish implemented header selection
  from the pending effective-table retention and CST-analysis diagnostic move.
- `text` spelling is required to be reversible, but its exact escape convention
  remains an open notation decision. The pages do not claim ordinary XML
  attribute escaping preserves CRLF. Diff and navigation checks passed; mdBook
  build is deferred because `mdbook` is unavailable in this environment.

## Rowan CST `text` spelling

- The user approved a canonical reversible escaping rule for XML-like Rowan
  notation. `text` uses backslash escapes for reverse solidus, quotation mark,
  CR, LF, tab and the remaining U+0000--U+001F control characters. All other
  Unicode scalar values are literal; the notation is not XML and uses no XML
  entities. Decoding, rather than notation width, determines UTF-8 byte ranges.
- Recorded the narrow Authoritative escaping addendum and updated both Rowan
  notation pages. This closes the former CRLF escaping gap without adding a
  runtime representation or changing parser behavior.

## Error-token and Invalid-node ordering

- The user selected the reviewed topology-only ordering: migrate ordinary raw
  fragments to Error token leaves and the two structured owners to Invalid
  nodes now, while retaining recovery records, frozen reconciliation and public
  diagnostics temporarily. The complete slot schema remains mandatory before
  removing that temporary parser diagnostic machinery.
- Recorded the narrow Authoritative ordering addendum. Its pre-write audit
  requires fragment-aware raw emission for interior trivia, quote prefixes,
  same-line EOF and consumed retry prefixes, rather than only replacing the
  former Error wrapper.

## Error-token and Invalid-node topology construction

- Completed the bounded M2 topology migration. Ordinary malformed physical
  fragments now emit direct Rowan `Error` leaves; only polymorphic-variant
  tag-name and record-pattern wrong-kind recovery emit `Invalid` nodes.
  `Invalid` was appended without renumbering an existing kind. Recovery
  records, reservations, frozen reconciliation, diagnostic IDs and public
  diagnostic construction remain deliberately temporary and unchanged.
- Owner tests now assert same-parent contiguous raw Error groups or Invalid
  containment, never treat an Error token count as a diagnostic count. The
  `@@` Rule control retains two Error leaves in one raw group and its existing
  two record occurrences. Opaque Root controls retain direct Error leaves,
  physical ranges and record facts.
- Focused owner/Rule/opaque-root controls passed. The final single-threaded
  `yu-syntax` library run passed 1,070 tests, with one existing manual harness
  ignored; its sole failure was the pre-existing stale `SyntaxKind::Unknown`
  discriminant assertion (`231` actual, `229` expected) in an unmodified test.
  Package check, format and diff checks passed. Compiler/recovery delta review
  found no topology issue. Benchmark use: zero samples/processes.
- `mdbook v0.5.4` became available after the reference slice. Both English and
  Japanese `syntax-reference` books now build successfully.

## Recovery-topology reference slice

- Added paired English/Japanese convention pages for the completed Rowan
  recovery topology. They specify direct raw `Error` leaves, the restricted
  `Invalid` owners, physical-fragment and leading ownership, and the boundary
  between the completed CST shape and the still-pending per-slot diagnostic
  schema. Related notation, diagnostic-ownership and assignment-tail pages now
  state the implemented topology accurately.
- Both `mdbook v0.5.4` reference books and scoped diff checks passed. This
  documentation slice makes no parser/API decision and does not claim a
  construct-specific recovery schema.

<!-- daily-append-anchor: 2026-09-09 -->
