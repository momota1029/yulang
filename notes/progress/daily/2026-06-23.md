# 2026-06-23

## Emergency rollback and playground recovery

- 6/22 夜の作業で playground / std check が広くハングする状態になったため、最初に deploy 済み
  playground の更新時刻から安全圏を逆算し、`3daadaaf` まで戻して再開した。
- reflog 上の流れでは、`9c1afc92 Remove pop replay clamp` の後に effect-flow lowering 系の
  `a6c0da39` / `5b7645d2` / `ec405317` / `d9a888ec` へ進んだラインが危険側だった。
  いったん `3daadaaf` に戻して、そこから `ref_update` 問題だけを切り直した。
- 戻した後、playground std / wasm / web build を確認し、公開先も再 deploy した。
  最終的な公開 commit は `71c4e50a Stabilize effect replay boundaries`。

## Root cause

今回のハングの直接原因は、`ref_update` そのものではなく、effect subtraction の private evidence が
public scheme へ漏れて、bounds replay の static boundary が unbounded cycle になったこと。

具体的には、`std.control.var.ref.update` は data field に

```text
update_effect: () -> [ref_update a; e] ()
```

のような effectful function を持つ。旧 lowering では、この field の latent return-effect tail に
public な `e` 自体を使い、さらに `e` へ `AllExcept(ref_update ...)` の subtract fact を登録していた。
そのため synthetic getter / receiver / local helper / callback を一周したとき、public scheme 内に

```text
PWeight(pop(1); take(AllExcept(ref_update)), e) <: e
```

のような weighted self-alias が入った。

最小因子は次の三つだった。

- data-position の effectful function: `r.update_effect()`
- local wildcard handler: `my loop(x: [_] _) = catch x: ...`
- handler arm 内 callback: `ref_update::update v, k -> loop:k:f v`

この形では、lexical boundary 内では balanced だった `pop(s); take_s(...)` が、receiver の反変位置や
var-var alias replay を通って `right-pop(s)`, `right-pop(s)^2`, ... のように増殖しうる。
`pop(n) -> pop(1)` clamp を外すと、exact replay key では候補が無限に増えて solver が止まらなくなる。

6/22 の一時 guard は主に var-var direct replay の left-pop-only cycle を抑えるものだった。
今回の増殖は right-pop / ordinary bounds replay 側にも出るため、その guard だけでは止まらない。

## Implemented fix

採った方針は、`ref_update` API を捨てることではなく、data-position に埋め込まれた関数の
返り effect tail を private / projected にすること。

- Type method の戻り型注釈を、body lowering 中の raw body effect へ直接つながないようにした。
  result annotation は receiver と args を含む method scheme の結果位置に対する deferred check として扱う。
- data-position の effectful function tail は、public tail `e` ではなく private tail `κ` で lower する。
  `[ref_update a; e]` は field 内部では概念的に `[ref_update a; PWeight(s, κ)]` になり、
  `declared_subtract_fact` は `κ` 側にだけ置く。
- field row に bare public tail を混ぜる案は採らなかった。これは runtime で
  `std.control.var.ref_update.update` が unhandled effect として漏れ、list update が壊れた。
- private tail だけにして public `e` へ戻さない案も採らなかった。これは ordinary residual が戻らず、
  list update の候補衝突を起こした。
- 最終形は、field row には `κ` だけを置き、ordinary row 内容だけを `κ <: e` の片方向 projection で
  public tail へ戻す形。これは solver graph 上の双方向 equality ではなく、private evidence を閉じるための
  projection として扱う。
- Generalize の final cleanup で、構造出現と declared fact が `Empty` だけの stack id は public scheme から
  落とすようにした。`All` / `AllExcept` / `Set` を持つ id はこの cleanup の対象外。

この結果、`std.ref_update` は public 表示で次のように落ちる。

```text
std::control::var::ref('a & 'c, 'b) -> ('b -> ['c] 'b) -> ['c, 'a] ()
```

`#...` や `AllExcept(ref_update ...)` は public scheme に出ない。

## Replay stabilization

Pro 案のうち、replay 側も最後まで入れた。

- `ConstraintWeights::compose_for_replay()` は W-Mix 後の exact label を保つ。
  `pop(n) -> pop(1)` のような意味論的 clamp は入れていない。
- var-var replay を専用 direct path から通常の `SubtypeConstraint { lower, upper, weights }`
  queue へ戻した。これで ordinary replay と var-var replay の dedup 意味論を分けない。
- ただし exact queue key だけでは、act method / parser choice の same-boundary alias cycle が
  bounds table の保存時に増殖した。そのため、bounds table への保存直前だけ、
  同一 endpoint / 同一 subtract id / 同一 family の alias replay cycle を既存 bound で subsume する。
- この subsumption は surface type の等式ではない。同じ static boundary を worklist cycle で再訪したときに、
  `pop^n` / `take^n` だけを増やす bound を保存しないための停止性規則である。
  別 id / 別 family は潰さない。

追加した主な canary:

- `ref.update` が public scheme に private stack evidence を出さないこと。
- nested data function でも同じ private projection が効くこと。
- `std::control::flow::for_in` の callback annotation が止まること。
- `std::text::parse.choice` の callback annotation が止まること。
- push-only / pop-only / balanced alias replay cycle が有限で、別 boundary は区別されること。

途中で起きた receiverless role impl の回帰も直した。
receiverless impl method は requirement value upper を接続する必要があるが、receiver method では同じ接続を
入れると mono 側で `UnsupportedBoundary` になったため、`connect_impl_method_requirement` に
`connect_value_upper` を渡す形に分けた。

## What changed

主な変更範囲:

- `crates/infer/src/lowering/expr/lambda.rs`
- `crates/infer/src/lowering/expr/method_body.rs`
- `crates/infer/src/lowering/expr/tail.rs`
- `crates/infer/src/annotation/constraints.rs`
- `crates/infer/src/constraints/machine/bounds.rs`
- `crates/infer/src/constraints/machine/entry.rs`
- `crates/infer/src/constraints/mod.rs`
- `crates/infer/src/generalize/*`
- regression tests in `crates/infer/src/constraints/tests/` and `crates/infer/src/lowering/tests/`
- design/spec notes:
  - `notes/diagnostics/2026-06-23-ref_update.md`
  - `notes/design/2026-06-22-directed-stack-weight-implementation.md`
  - `spec/2026-05-31-effect-variable-subtractable.md`

最終 commit:

```text
71c4e50a Stabilize effect replay boundaries
```

## Verification

最終 commit 前に通したもの:

- `timeout 180s cargo test -q -p infer annotation::tests -- --test-threads=1`
- `timeout 180s cargo test -q -p infer constraints::tests -- --test-threads=1`
- `timeout 180s cargo test -q -p infer lowering::tests::case_05 -- --test-threads=1`
- `timeout 300s cargo test -q -p infer -- --test-threads=1`
- `timeout 180s cargo test -q -p yulang source::tests::case_02::definition_entry_source_with_std_reports_std_method_target -- --test-threads=1`
- `timeout 360s cargo test -q -p yulang -- --test-threads=1`
- `cargo fmt`
- `git diff --check`

Deploy 前に追加で通した playground 確認:

- `timeout 240s cargo test -q -p yulang source::tests::case_02::run_control_source_text_with_embedded_playground_std_runs_ -- --test-threads=1`
- `timeout 180s cargo test -q -p yulang source::tests::case_02::typed_playground_std_prefix_matches_loaded_route_for_list_update -- --test-threads=1`
- `RUST_MIN_STACK=67108864 timeout 360s cargo test -q -p wasm -- --test-threads=1`
- `timeout 900s npm --prefix web run build`
- generated wasm direct Node smoke:
  - core no-std: `42`
  - arithmetic: `3`
  - mixed numeric: `2.2`
  - struct method: `26.12`
  - local change: `15`
  - list update: `[2, 6, 4]`
  - `check` diagnostic path and `colorize` path
- local HTTP smoke for `web/dist`:
  - `/`
  - `/assets/run-worker-BWPySlZ9.js`
  - `/assets/wasm_bg-BGcxtAHH.wasm`
  - `/assets/index-CQx_YSyK.js`
  - `/assets/index-3Wzd819k.css`
  - `/ja/guide/`
  - `/ja/reference/`
- headless Chromium UI smoke:
  - Run button path with list update source produced `Result 1: [2, 6, 4]`

Deploy:

- `git push origin main` は `Everything up-to-date`。remote `main` は `71c4e50a`。
- `YULANG_DEPLOY_DIR=/home/momota1029/www/yulang timeout 900s npm --prefix web run deploy:dir`
  が成功した。
- deploy 先 `/home/momota1029/www/yulang` の `index.html`, worker JS, main JS, wasm は
  2026-06-23 12:37 JST に更新された。
- deploy 先を `python3 -m http.server 4174 --bind 127.0.0.1` で配信し、
  asset 200 と headless Chromium UI smoke を再確認した。

## Remaining risks

- private data tail は `SignatureLowerer` 内の row occurrence 共有と projection で実装している。
  専用の existential package metadata や、private var/id escape assert はまだ無い。
- local wildcard helper 専用の close/project 機構としては分けていない。
  今回は method result annotation deferral と data-tail projection の組で canary を閉じている。
- same-boundary alias-cycle subsumption は停止性規則として妥当だが、surface type 等式として説明してはいけない。
  今後の docs / spec でも `pop(n) = pop(1)` と書かない。
- WSL2 が落ちやすいので、重い test / build は引き続き `timeout` と `--test-threads=1` を付ける。
