# 2026-09-05

## C15 direct Type derives construction

Completed the isolated source-free direct-CST construction authorized by
`2026-09-05-direct-type-derives-episode-boundary-amendment.md`. Only direct
`TypeDeclaration` now constructs existing `DerivesClause` / `DerivesKw` /
`ViaKw` children: header clauses follow a complete name and parameter scan,
and trailing clauses follow an Equality RHS. No public/root parser, legacy
parser, AST, HIR, diagnostics, Struct owner, Yumark, state, `Stops`, or `Item`
surface changed.

`TypeOuterBoundary` is an immediate Copy value for one logical outer
TypeExpression. Same-episode candidate, tail, path, and recovery paths retain
it; fresh group, call item, TypeApply argument, arrow RHS, forall body, record
or row item, and variant payload entries use `NONE`. A trailing role alone
removes the pre-existing flat `STOP_WITH`; its nested TypeExpressions remain
ordinary Type syntax.

Post-write recovery review found and the repair closed three leading-trivia
ownership defects: RHS initial derives, a missing Type path segment, and
forall binder/colon recovery had each been able to consume an outer boundary's
leading newline. Those sites now emit their local zero-width Missing while
returning the unchanged pending Item. The same review also fixed header
`with` / `impl` handoff to obey the C14 attachment gap judge, preserving the
CatchBraced EqualityRecovery route. Specification review required and received
the full clause-local H matrix, zero-Catch header/trailing distinction,
recovery/handoff controls, and the active `else` companion control.

Verification was deliberately focused:

- `cargo test -p yu-syntax rewrite::tests::type_decl::type_c15_ --lib`: 15
  passed, 0 failed, 804 filtered;
- `cargo check -p yu-syntax`: passed;
- `cargo fmt --check` and `git diff --check`: passed.

Independent compiler/recovery delta review approved the repaired ownership
paths. The final specification finding added only a test-only active-companion
control; primary inspection and the focused deterministic check closed it.
No performance measurement was triggered: the accepted path adds no replay,
state, buffer, or new allocation mechanism. Package/workspace suites and all
excluded public, legacy, AST/HIR, Struct, and Yumark surfaces remain unrun.

## Parsed Yumark Yulang-fence design approval

User approved `2026-09-05-yumark-parsed-yulang-fence-addendum.md`, which is
now Authoritative. It supersedes raw-fence behavior only for a fence whose
first info atom is exact case-sensitive `yulang`; all other languages retain
the existing raw CST/close/recovery behavior. A selected cell uses a direct
root-style `Statement*` entry with the host immutable operator table, so `our`
is valid. It does not execute, create a public Root, reparse a body, dequote a
copy, or share bindings/execution state with another cell.

The critical quoted-fence contract is source-streaming: Yumark owns physical
prefix, close, source coordinates, and recovery; the Yulang cell receives the
same `&str` cursor and returns a typed borrowed fence close. An equivalent
prefix follows the existing same-depth Yumark judge; its strict close is tested
before a body `YmQuotePrefix` can be emitted. A non-equivalent prefix returns
the untouched line to outer quote handling and yields one fence Missing. A
multiline lexical item stores only a current-item split-offset sidecar, built
linearly and moved after completion; it is neither a row/body buffer nor parser
state.

Independent compiler/recovery, specification, regression, and performance
reviews found and closed the boundary algebra, AST/raw compatibility,
prefix-equivalence, close-order, and allocation-bound gaps. The design's Gate
2 requires static allocation/progress review; timing remains conditional on
that analysis finding material uncertainty. No Rust or test change occurred in
this approval record. The next authorized code slice is inert Gate 2; full
cell closure is gated behind rewrite Gates 4--7, then successor Yumark
convergence in Gate 8 and atomic production cutover in Gate 9.

## Parsed Yumark Yulang-fence Gate 2

Implemented and closed inert Gate 2 of
`2026-09-05-yumark-parsed-yulang-fence-addendum.md`. The direct rewrite gained
`YmYulangCodeCell`, exact first-info-atom selection, a pure physical-line
fence judge, coordinate-preserving typed pending boundaries, and an item-wide
move-only `PendingFragments` carrier. The carrier has one ordered split list
across every retained leading-trivia and Token/Operator payload text part; it
does not copy or concatenate text. It validates physical extent, UTF-8
boundaries, order, overlap, and replacement before attachment. Existing
scanner paths construct no carrier. Only rare Yumark fence close/transition
facts are boxed, preserving the ordinary payload-size envelope.

The first compiler/recovery review found missing boundary/source coordinates
and release-only fragment validation; the repair added explicit inspected
ranges without adding source state to ordinary items or `Recover`. Performance
review then confirmed that timing was not decision-relevant. A second delta
review found the multi-leading-trivia overwrite case; the final item-wide
carrier repair closed it with one physical-order traversal over existing boxes.
The M2 review/repair budget is exhausted; primary final inspection verified the
accepted data flow and no legacy/session/production edge. Focused direct tests
cover exact selection, strict LF/CRLF/EOF closes, quote transitions, boundary
coordinates, UTF-8/CRLF multiphase reconstruction, carrier validation, and
ordinary-scanner absence. `cargo check -p yu-syntax`, the focused module, the
SyntaxKind round-trip test, `cargo fmt --check`, and `git diff --check` passed.
No workspace suite or timing measurement ran. Next is the isolated Gate 3 cell
construction witness; no production parser is yet changed.

## Parsed Yumark Yulang-fence Gate 3

Completed the Authoritative Gate 3 isolated construction witness. The
test-only direct cell path uses the caller-owned `GreenNodeBuilder` to form one
`YmYulangCodeCell` containing root-style `Statement*`; it opens no nested
`Root`, creates no second builder, and performs no source replay, body buffer,
or public-parser call. `statement_from_item` extracts only the existing
canonical statement dispatch from scanner entry. `handoff` now returns a
pending `Payload::Boundary` as the same moved `Item`.

The witness proves empty, single, semicolon-separated multiple, and root
`our` statements. Borrowed-close, Yumark-transition, and EOF boundaries return
unchanged and emit neither their leading trivia nor fragment carrier. A
test-only accepted segmented identifier is emitted under the same
`YmYulangCodeCell > Statement > IdentifierExpression` in physical order, with
ordinary parts retaining their original syntax kind and its foreign prefix as
`YmQuotePrefix`; the immediately following borrowed close returns unchanged.
The successor branch checks `Payload::Boundary` before any token predicate.

M2 pre-write specification review was conditionally approved. The first
compiler/recovery and specification reviews found, respectively, a
successor-boundary predicate ordering defect and a detached fragment witness.
One bundled repair introduced the boundary-first successor path and the
integrated accepted-item/borrowed-close witness; both delta reviewers approved
the repair. Focused `rewrite::tests::yumark_cell` coverage passed 5 tests,
`cargo check -p yu-syntax` passed with 89 pre-existing warnings, and format and
diff checks passed. No timing or workspace suite ran. Fence-aware lexical
scanning, multiline lexical-owner closure, host operator-table proof, and all
production Yumark/legacy/public integration remain deferred to Gates 4--9.

## Fenced block-comment lexical construction approval

The user approved the next isolated Gate 4 construction slice in
`2026-09-05-yumark-fenced-block-comment-lexer-amendment.md`, now
Authoritative after independent compiler/recovery and specification review.
It introduces only a dedicated fenced block-comment scanner with immediate
part origin, `FenceBoundary`, and current-item-owned lazy split accumulator;
the ordinary scanner remains unchanged. The whole current Item separately
captures its origin and final constituent length, then completes exactly one
fragment carrier. After a comment opener, the scanner/current-item path is
total and cannot rely on rollback of the external accumulator. A legal close,
quote transition, or EOF leaves the judged line untouched as a typed boundary;
the preceding unterminated comment remains accepted `BlockComment` trivia.
Nested comment depth, previous-trivia envelope, CRLF/UTF-8 coordinates, and
non-match accumulator preservation are required controls. This approves no
production Yumark/lexer dispatch, source retention, replay, buffer, second
builder, strings/rule literals, or full cell closure.

## Fenced block-comment lexical construction

Completed the approved isolated construction. The new test-only scanner leaves
the ordinary block-comment scanner and every ordinary lexer caller unchanged.
It runs only through the immediate lexical transaction; after `/*` it is total
and its external split accumulator cannot be left behind by a later rollback.
It judges each post-LF/CRLF line and EOF before consuming the next-line byte,
preserves nested comment depth, records an accepted quote prefix only after
strict-close precedence, and returns close/transition/EOF suffixes unchanged.

The focused harness captures a whole-Item origin separately from a comment
part origin, finalizes exactly one carrier over preceding trivia plus comment,
and emits accepted `BlockComment`/`YmQuotePrefix` segments on one builder. It
also moves the scanner-built segmented borrowed-close Item through the real
`handoff` and checks full Item equality and the untouched suffix pointer. M2
post-write specification review approved directly; compiler/recovery review
found the missing handoff control, whose one-test repair received delta
approval. `rewrite::tests::lexical` passed 20 tests, `cargo check -p
yu-syntax` passed with 89 pre-existing warnings, and format/diff checks passed.
No timing or workspace suite ran. Strings/rule literals, full expression and
cell closure, and all production Yumark/legacy/public routing remain deferred.

## Direct literal-cone design approval

User approved LC-1 through LC-12 in
`2026-09-05-direct-literal-cone-addendum.md`, which is now Authoritative for
the direct-rewrite normal/heredoc string, string interpolation, `rule { ... }`,
`~"..."`, and Pattern-literal cone. Independent compiler/recovery,
specification, and regression review shaped the final ownership, grammar,
compatibility, and recovery contracts.

The authoritative route keeps one current lexical Item and its one fragment
carrier; an ordinary EOF or a fence boundary remains a separately moved pending
Item. A contextual `rule` completes exactly one successor Item: its leading
trivia belongs to that successor, `{` commits RuleExpression, and every other
successor returns to ordinary expression-tail handling without rewind, replay,
or an Item buffer. `FormatText` remains raw through quote/newline until `{`;
braced captures are spec-first raw through `=`/quote/newline until `}`; lazy
quantifiers remain active; and escaped CRLF retains the observed split public
CST grouping.

The plan records an important construction dependency rather than certifying a
false early closure: Gates 4--6 create isolated owner-side checkpoints, while
string interpolation's virtual `Statement*` body waits for the Gate-6
statement/declaration checkpoint. L7 then closes literal and Pattern delta
rows at a joint Gate 4--6 certification barrier. The only immediately
authorized code slice is isolated L0 (public literal vocabulary and its
private fragmented emitter). No Rust, tests, production dispatch, legacy
parser, or Yumark integration changed in this approval record. `git diff
--check` for the new design record passed; no build/test suite was relevant to
the documentation-only transition.

## Direct literal-cone L0 implementation

Isolated L0 completed on 2026-09-05. `SyntaxKind` gained the approved 38
append-only public literal and Rule values, with the former final `Unknown`
discriminant retained at 229 and every new value round-tripped through Rowan.
The direct rewrite now has one literal-private fragmented emitter that consumes
an already-complete lexical `Item` on the caller-owned builder, emits ordinary
pieces under an explicit literal kind, and preserves only accepted
`YmQuotePrefix` foreign pieces in physical order. It neither attaches nor
finalizes a fragment carrier, reconstructs an Item, emits a Boundary, changes
ordinary emitters, nor creates lexer/parser/production-Yumark reachability.

Focused unsplit UTF-8/CRLF and multi-prefix fragmented controls passed, as did
the `SyntaxKind` round trips, `cargo check -p yu-syntax`, format, and diff
checks. M2 compiler/recovery and regression closure reviews reported no
findings. Static inspection found no new allocation, replay, retained state,
or rescanning path, so no performance measurement was triggered. The next
authorized code slice is L1: literal lexical Item scanning and the fence
transition primitive, still with no NUD dispatch.

## Direct literal-cone L1 implementation

Isolated L1 completed on 2026-09-05. A new literal-private module now supplies
the move-only `LiteralPiece` handoff: a complete literal text `Item`, or a
separate optional accepted Item plus the exact pending Yumark boundary Item.
It keeps selected quote mode on the call stack, classifies normal versus
heredoc opener runs, accepts only an exact whole heredoc close run, and leaves
shorter/longer runs as text without partial consumption. Its sole physical-line
transition calls the existing pure fence judge after LF or CRLF; strict close
precedes same-depth prefix recording, and only an accepted body prefix is
consumed and attached to the current Item's one lazy fragment carrier.

Focused controls cover 1/3/4 quote runs and empty normal strings, exact-close
mismatches, LF/CRLF/UTF-8 suffix coordinates, unprefixed and prefixed body
lines, direct/prefixed fence closes, all four quote transitions, EOF with and
without accepted text, and lossless L0 emitter output. `cargo test -p
yu-syntax rewrite::tests::literal`, `cargo check -p yu-syntax`, formatting,
and diff checks passed; the compiler/recovery closure review found no issues.
Static accounting remains linear in input plus structural work, with only the
current Item and its lazy prefix vector retained. No lexer, driver, Yumark
judge, state, dispatch, legacy route, or production reachability changed. The
next authorized slice is L2: normal/heredoc text, escapes, terminator, and
non-interpolation recovery.

## Direct literal-cone L2 implementation

Isolated L2 completed on 2026-09-05. The literal-private test witness now
constructs normal and exact-heredoc `StringLiteral` nodes from an accepted
opener through a terminator or untouched boundary handoff. It builds simple
and Unicode escapes, including the selected LF/CRLF split, exact Missing/Error
ordering, and malformed Unicode runs that stop before `}`, a terminator, `%`,
or a Yumark boundary. A raw `%` remains an unconsumed L3 construction stop;
no interpolation node or production reachability was added.

The initial closure review found that a same-depth Yumark prefix before a
structural literal token could be assigned to the preceding text/Error Item.
One bounded repair made the judge result a call-local deferred prefix: escape
lead, Unicode end, and later structural tokens consume their own prefix plus
payload in one Item/carrier, while a prefix before `%` remains entirely in the
L3 suffix. Delta review approved that repair. Direct CST-shape controls now
also prove recovery ordering and escaped LF/CRLF across every fence transition.

`cargo test -p yu-syntax rewrite::tests::literal --no-fail-fast` passed 16
tests, and `cargo check -p yu-syntax`, focused formatting, and diff checks
passed. Static accounting remains linear in input plus structural work; the
deferred prefix is one immediate value, not persistent state, replay, or a
buffer, so no timing measurement was triggered. L3 is the next authorized
slice: percent/format/open-brace StringInterpolation construction and its
missing-open-brace path, without a complete child grammar claim.

## Direct literal-cone L3 implementation

Isolated L3 completed on 2026-09-05. `StringLiteral` now constructs percent,
raw FormatText, open brace, and `StringInterpolationBody`; a call-local injected
child witness alone returns the already-read borrowed `RBrace` Item. Its leading
trivia stays inside interpolation and normal string scanning resumes after it.
Format remains raw through quote, backslash, `}`, LF/CRLF, and UTF-8 until `{`.
Before `{`, EOF/fence produces interpolation-open Missing then literal
terminator Missing while returning the original pending Item unchanged.

The closure review corrected one invalid witness that had synthesized a close
without consuming its source byte. The repaired control consumes and returns
the physical close, proves its successor pointer, and emits one close plus only
the following `raw` StringText. Focused literal controls passed 22 tests, as
did `cargo check -p yu-syntax`, formatting, and diff checks. No timing,
production reachability, virtual-statement grammar, or child recovery was
introduced.

## Direct literal-cone L4 implementation

Isolated L4 completed on 2026-09-05. The normally compiled private
`RuleSequenceCore` constructs `RuleBody > RuleAlternation > RuleSequence`,
parenthesized `RuleItem`, identifier/sigil/integer/`..` atoms, all five
quantifiers, terminal captures, and field/path recovery without a production
`RuleExpression` route or host Pratt/ML application. Empty, trailing, and
adjacent body/paren separators produce epsilon RuleSequence nodes. Stops,
EOF, and fence boundaries return their original Item without emitting pending
leading trivia.

The private/test-only RuleIntroducerTrivia witness uses the existing fence judge
and one-current-Item carrier. Its integrated control proves a segmented `{`
emits `Newline`, `YmQuotePrefix`, `LBrace`, then `RBrace` exactly once and leaves
the live suffix pointer unchanged. A closure finding corrected the initial
test-only module gating and unopened fragmented-opener path; a second small
cleanup made Rule use lexer-owned ordinary non-Newline trivia scanning instead
of maintaining a duplicate scanner. Malformed operator-shaped runs are one
Error Item after exact DSL tokens, while exact `=` remains capture.

`cargo test -p yu-syntax rewrite::tests::rule --no-fail-fast` passed 14 tests;
`cargo check -p yu-syntax`, focused formatting, and diff checks passed, with
only pre-existing warnings. Static work is linear in source plus constructed
nodes; no timing measurement was triggered. L5 is next: string/bracket atoms,
Rule call/index through the direct expression/list checkpoint, and the stated
Pattern literal witnesses. Production dispatch, full fenced-cell parsing, and
Pattern routing remain deferred.

## Direct-rewrite Item emission ownership N0a

Completed N0a from the Authoritative
`2026-09-05-item-emission-ownership-frontier-amendment.md`. The rejected
initial representation had retained physical prefix/carrier ownership while
direct grammar still destructively moved `leading` and `payload`. N0a seals the
completed Item: one monotone `first_unemitted_leading: usize`, private physical
parts/payload/carrier, opaque ordinary `LeadingTrivia`, fenced-only
`PhysicalLeadingTrivia`, atomic `Item::finish`, read-only `LeadingView` and
borrowed spelling `PayloadView`, and the central fragment-aware phased emitter.
No `with_fragments`, post-construction carrier attachment, raw Item
leading/payload access, manual fragment walker, or ordinary compatibility caller
remains.

`YmQuotePrefix` construction now requires a matching full-part carrier split;
the converse rejects split placement in whitespace, newline, and line comments.
Only valid block-comment or payload placement remains embedded. Rule's
expression-list partial leading calls the immediate central callback for every
physical part, including repeated newlines. Boundary Items remain frontier-zero
through Yulang grammar; the sole terminal adapter emits retained body-leading
below open `YmYulangCodeCell` and returns unchanged pending facts to Yumark.

The fragmented emitter derives its cursor locally. `emit_remaining` shares one
cursor over leading and payload; after a partial-leading sequence, direct
payload emission derives its start from carrier end minus payload length and
uses one operation-local split location, so it does not take a third leading
metadata traversal. No cursor is stored in Item, and there is no source
traversal, text copy, carrier clone/rebuild, or added ordinary allocation.

Layout evidence on the 64-bit target: `size_of::<Item>()` changed 104 to 112
bytes, exactly the approved `usize`; `size_of::<LeadingTrivia>()` remains 16
bytes. Focused checks passed: lexical excluding the known L5a test 29/29;
Rule 21/21 (including repeated-newline order); Yumark 14/14; Yumark cell 5/5;
Literal 26/26; package check; format; and diff checks. The unfiltered lexical
run is 29/30: the only failure is the pre-existing L5a
`decimal_integer_core_keeps_its_direct_tail_chain` at `tails.rs`'s
`defer_distinct_owner` assertion, recorded separately from N0a. Full
`cargo test -p yu-syntax` is deferred to N0c by the design.

M3 review closed with compiler/recovery, specification, and regression/
performance review. Initial findings repaired sealed construction provenance,
invalid split placement, detached emission visibility, repeated-newline
evidence, and the linear cursor. The final performance delta confirmed the
partial -> remaining -> payload operation chain remains within the bounded
leading-metadata traversal contract. N0b is next, but the Item compatibility
caller set is currently zero; record the remaining detached-owner inventory
before starting that gate.

<!-- daily-append-anchor: 2026-09-05 -->
