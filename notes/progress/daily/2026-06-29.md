# 2026-06-29

## Evidence VM rejected later grant execution shape profile

目的:

- `ProviderEnv` later-grant native close は復活させない。
- later-grant branch が fast path として遅くなった理由を、実行意味論を変えずに
  profile する。
- 見たいものは、later grant placement 探索コスト、仮に native close を有効にした
  場合の候補数、legacy / hypothetical native の continuation shape 差。

変更:

- `provider_env_later_grant_placement_profiled` を追加した。
- later-grant placement 探索の call / frame step / `Then` step / `ProviderEnv` step /
  `MarkerFrame` stop / found / not-found を記録するようにした。
- later-grant branch が現在の条件のまま native eligible になる件数を
  profile-only で記録するようにした。
- `close_marker_frame` した legacy continuation shape と、marker close を飛ばした
  hypothetical native continuation shape を記録するようにした。
- 実行意味論は変えていない。`ProviderEnvForeignLaterGrantCert` は入れていない。

release `bench/nondet_20_discard.yu`:

- `provider_env_foreign_later_grant_candidates: 420`
- `provider_env_later_grant_placement_calls: 420`
- `provider_env_later_grant_placement_frame_steps: 10794`
- `provider_env_later_grant_placement_then_steps: 1302`
- `provider_env_later_grant_placement_provider_env_steps: 1722`
- `provider_env_later_grant_placement_marker_frame_stops: 0`
- `provider_env_later_grant_placement_found: 420`
- `provider_env_foreign_later_grant_native_eligible_if_enabled: 420`
- native shape:
  - `then_frames: 861`
  - `marker_frames: 420`
  - `provider_env_frames: 861`
  - `depth_sum: 105021`
  - `max_depth: 30`
- legacy shape:
  - `then_frames: 861`
  - `marker_frames: 840`
  - `provider_env_frames: 861`
  - `depth_sum: 115773`
  - `max_depth: 31`
- `timing.runtime_evidence_execute: 13.2ms`
- `compare.control: match`

release `examples/showcase.yu`:

- `provider_env_foreign_later_grant_candidates: 815`
- `provider_env_later_grant_placement_calls: 815`
- `provider_env_later_grant_placement_frame_steps: 16629`
- `provider_env_later_grant_placement_then_steps: 2717`
- `provider_env_later_grant_placement_provider_env_steps: 3532`
- `provider_env_later_grant_placement_marker_frame_stops: 0`
- `provider_env_later_grant_placement_found: 815`
- `provider_env_foreign_later_grant_native_eligible_if_enabled: 815`
- native shape:
  - `then_frames: 1766`
  - `marker_frames: 815`
  - `provider_env_frames: 1766`
  - `depth_sum: 109524`
  - `max_depth: 25`
- legacy shape:
  - `then_frames: 1766`
  - `marker_frames: 1630`
  - `provider_env_frames: 1766`
  - `depth_sum: 125881`
  - `max_depth: 26`
- `timing.runtime_evidence_execute: 31.5ms`
- `compare.control: match`

debug:

- `bench/nondet_20_discard.yu`
  - `provider_env_foreign_later_grant_legacy_visible: 420`
  - `provider_env_foreign_later_grant_naive_visible: 420`
  - `provider_env_foreign_later_grant_naive_match: 420`
  - `provider_env_foreign_later_grant_naive_mismatch: 0`
  - `compare.control: match`
- `examples/showcase.yu`
  - `provider_env_foreign_later_grant_legacy_visible: 815`
  - `provider_env_foreign_later_grant_naive_visible: 815`
  - `provider_env_foreign_later_grant_naive_match: 815`
  - `provider_env_foreign_later_grant_naive_mismatch: 0`
  - `compare.control: match`

読み:

- later grant の visible semantics は引き続き legacy と一致している。
- ただし placement 探索だけで nondet 10,794 frame steps、showcase 16,629 frame steps
  を踏んでいる。
- hypothetical native shape は marker frame を半分程度減らすが、`Then` と
  `ProviderEnv` の大きな形は残る。
- つまり、later grant branch が遅かった原因は close 条件の正しさではなく、
  placement 探索と continuation representation 側に寄っている可能性が高い。
- 次に触るなら later-grant native close の復活ではなく、ProviderEnv/Then
  continuation shape をどう軽くするかを見る。

確認:

- `cargo fmt --all --check`
- `cargo check -q -p evidence-vm -p yulang`
- `cargo check -q --release -p evidence-vm -p yulang`
- `cargo build -q --release -p yulang`
- `cargo test -q -p evidence-vm -- --test-threads=1`
- `cargo test -q -p yulang runtime_evidence -- --test-threads=1`
- `tests/yulang/yulang-adversarial-corpus/probe.sh`

## Evidence VM later grant deep profile gate

目的:

- rejected later-grant deep profile を release default から外す。
- normal `runtime_evidence_execute` では later-grant placement 探索を走らせない。
- 必要なときだけ `--runtime-evidence-profile-deep` で再計測できるようにする。
- 実行意味論は変えない。

変更:

- `RuntimeEvidenceRunContext` に `deep_profile` flag を追加した。
- `debug runtime-evidence-run` / `debug evidence-vm-run` に
  `--runtime-evidence-profile-deep` を追加した。
- later-grant deep profile は `cfg(debug_assertions)` または explicit flag のときだけ
  実行するようにした。
- `ProviderEnvForeignLaterGrantCert` は入れていない。

release `bench/nondet_20_discard.yu`:

- flag なし:
  - `provider_env_foreign_later_grant_candidates: 0`
  - `provider_env_later_grant_placement_calls: 0`
  - `provider_env_later_grant_placement_frame_steps: 0`
  - `provider_env_foreign_later_grant_native_eligible_if_enabled: 0`
  - `timing.runtime_evidence_execute: 13.9ms`
  - `compare.control: match`
- `--runtime-evidence-profile-deep`:
  - `provider_env_foreign_later_grant_candidates: 420`
  - `provider_env_later_grant_placement_calls: 420`
  - `provider_env_later_grant_placement_frame_steps: 10794`
  - `provider_env_foreign_later_grant_native_eligible_if_enabled: 420`
  - `timing.runtime_evidence_execute: 13.3ms`
  - `compare.control: match`

release `examples/showcase.yu`:

- flag なし:
  - `provider_env_foreign_later_grant_candidates: 0`
  - `provider_env_later_grant_placement_calls: 0`
  - `provider_env_later_grant_placement_frame_steps: 0`
  - `provider_env_foreign_later_grant_native_eligible_if_enabled: 0`
  - `timing.runtime_evidence_execute: 32.9ms`
  - `compare.control: match`
- `--runtime-evidence-profile-deep`:
  - `provider_env_foreign_later_grant_candidates: 815`
  - `provider_env_later_grant_placement_calls: 815`
  - `provider_env_later_grant_placement_frame_steps: 16629`
  - `provider_env_foreign_later_grant_native_eligible_if_enabled: 815`
  - `timing.runtime_evidence_execute: 33.3ms`
  - `compare.control: match`

判断:

- release default から deep traversal は消えた。
- flag 付きでは前回の later-grant profile を再現できる。
- 速度差は単発ではノイズ圏内だが、reject した fast path のために毎回探索する状態は
  避けられる。
- 次に進むなら、later grant native close ではなく ProviderEnv/Then continuation
  shape の compaction candidate を profile する。

確認:

- `cargo fmt --all --check`
- `cargo check -q -p evidence-vm -p yulang`
- `cargo check -q --release -p evidence-vm -p yulang`
- `cargo build -q --release -p yulang`
- `cargo test -q -p evidence-vm -- --test-threads=1`
- `cargo test -q -p yulang runtime_evidence -- --test-threads=1`
- `tests/yulang/yulang-adversarial-corpus/probe.sh`

## Evidence VM ProviderEnv/Then compaction profile

目的:

- ProviderEnv / Then continuation shape を scope-safe に軽くできる候補があるか
  観測する。
- 実行意味論は変えない。
- ProviderEnv flatten、Then queue 化、later-grant native close 復活はしない。
- deep profile path の下だけで測り、release default には探索を乗せない。

変更:

- `RuntimeEvidenceRunStats` に `provider_env_then_compaction_*` counters を追加した。
- continuation shape から次を数える profile を追加した。
  - adjacent ProviderEnv
  - exact same ProviderEnv
  - ProviderEnv identity tail
  - ProviderEnv under `Then` first / second
  - marker-frame / request-boundary / handler-boundary reject
- ProviderEnv equivalence は exact equality だけにした。
- profile は later-grant deep profile の shape 記録からだけ呼ぶ。

release `bench/nondet_20_discard.yu`:

- flag なし:
  - `provider_env_foreign_later_grant_candidates: 0`
  - `provider_env_later_grant_placement_calls: 0`
  - `provider_env_then_compaction_candidates: 0`
  - `timing.runtime_evidence_execute: 12.9ms`
  - `compare.control: match`
- `--runtime-evidence-profile-deep`:
  - `provider_env_foreign_later_grant_candidates: 420`
  - `provider_env_later_grant_placement_calls: 420`
  - `provider_env_then_compaction_candidates: 1722`
  - `provider_env_then_compaction_adjacent_provider_env: 441`
  - `provider_env_then_compaction_same_provider_env: 0`
  - `provider_env_then_compaction_provider_env_identity_tail: 0`
  - `provider_env_then_compaction_provider_env_then_first: 861`
  - `provider_env_then_compaction_provider_env_then_second: 0`
  - `provider_env_then_compaction_nested_same_env: 0`
  - `provider_env_then_compaction_nested_different_env: 441`
  - `provider_env_then_compaction_reject_different_provider_env: 441`
  - `provider_env_then_compaction_reject_marker_frame_between: 420`
  - `provider_env_then_compaction_reject_request_boundary: 1260`
  - `provider_env_then_compaction_reject_handler_boundary: 0`
  - `provider_env_then_compaction_reject_resume_delta: 0`
  - `timing.runtime_evidence_execute: 13.2ms`
  - `compare.control: match`

release `examples/showcase.yu`:

- flag なし:
  - `provider_env_foreign_later_grant_candidates: 0`
  - `provider_env_later_grant_placement_calls: 0`
  - `provider_env_then_compaction_candidates: 0`
  - `timing.runtime_evidence_execute: 30.5ms`
  - `compare.control: match`
- `--runtime-evidence-profile-deep`:
  - `provider_env_foreign_later_grant_candidates: 815`
  - `provider_env_later_grant_placement_calls: 815`
  - `provider_env_then_compaction_candidates: 3532`
  - `provider_env_then_compaction_adjacent_provider_env: 951`
  - `provider_env_then_compaction_same_provider_env: 0`
  - `provider_env_then_compaction_provider_env_identity_tail: 0`
  - `provider_env_then_compaction_provider_env_then_first: 1766`
  - `provider_env_then_compaction_provider_env_then_second: 0`
  - `provider_env_then_compaction_nested_same_env: 0`
  - `provider_env_then_compaction_nested_different_env: 951`
  - `provider_env_then_compaction_reject_different_provider_env: 951`
  - `provider_env_then_compaction_reject_marker_frame_between: 815`
  - `provider_env_then_compaction_reject_request_boundary: 2445`
  - `provider_env_then_compaction_reject_handler_boundary: 0`
  - `provider_env_then_compaction_reject_resume_delta: 0`
  - `timing.runtime_evidence_execute: 29.9ms`
  - `compare.control: match`

読み:

- release default では rejected later-grant / compaction 探索は 0 のまま。
- deep profile では ProviderEnv under `Then` first が太い。
- exact same ProviderEnv は nondet / showcase とも 0。
- adjacent ProviderEnv はあるが、観測範囲ではすべて different env。
- 単純な exact nested ProviderEnv collapse は現状の workload には刺さらない。
- 次にやるなら flatten ではなく、ProviderEnv scope の意味論を保ったまま
  continuation representation を軽くする別設計が必要。

確認:

- `cargo fmt --all --check`
- `cargo check -q -p evidence-vm -p yulang`
- `cargo check -q --release -p evidence-vm -p yulang`
- `cargo build -q --release -p yulang`
- `cargo test -q -p evidence-vm -- --test-threads=1`
- `cargo test -q -p yulang runtime_evidence -- --test-threads=1`
- `tests/yulang/yulang-adversarial-corpus/probe.sh`

## Evidence VM 独力高速化メモ

目標:

- `bench/nondet_20_discard.yu` の `runtime_evidence_execute` を一桁 ms 台へ寄せる。
- Pro に頼る前に、手元で説明できる VM hot path を順に削る。

採用した変更:

- primitive partial application の引数を `SmallVec<[SharedValue; 4]>` にした。
- effect call route / operation visibility / provider lookup を `(ExprId, ExprId)` `HashMap`
  から apply expr indexed table に寄せた。
- provider env / slot / small candidate tables を `SmallVec` 化した。
- provider env active refs/views の一時 `Vec` allocation を `SmallVec` 化した。
- `with_provider_env` で active frame 用に provider env を clone せず、pop した frame から
  close 用 env を回収する形にした。
- shared continuation frame は、scope boundary でなければ clone した spine に tail append
  する既存方針を維持した。

試して不採用にした案:

- release default から主要 runtime stats counter を外す案。
  - 速度差がほぼなく、通常 diagnostics が薄くなるので戻した。
- `Bool` / `Unit` の thread-local shared `Rc` 化。
  - `thread_local` 入口コストが勝ち、nondet / showcase とも悪化。
- `MarkedApply` の frame-less close 拡張。
  - 代表 workload では該当せず、marker frame 数が減らなかった。
- duplicate marker scope skip。
  - `marker_frame_entries` は nondet `13526 -> 7293` まで減ったが、cover 判定と
    close 形状のコストで実時間は悪化した。
- runtime value payload (`Tuple` / `DataConstructor`) の `SmallVec` 化。
  - 代表 workload ではほぼノイズで、diff が大きいので戻した。
- fat LTO + `target-cpu=native`。
  - たまに `9.7ms` は出るが平均は 10ms 台。build が重く、通常解決ではない。

最終測定:

- release `bench/nondet_20_discard.yu --compare-control`
  - `runtime_evidence_execute`: おおむね `10.5ms`〜`12.1ms`
  - `compare.control: match`
- release `examples/showcase.yu --compare-control`
  - `runtime_evidence_execute`: おおむね `24ms`〜`26ms`
  - `compare.control: match`

読み:

- 小さい allocation 削減や counter 削減では一桁台に安定して入らない。
- 残りの主戦場は `MarkerFrame` / `ProviderEnv` / `Then` を含む continuation 表現。
- duplicate marker scope は数としては太いが、単純 skip は速くならない。
- 次に進むなら、profiler が使える環境を用意するか、ProviderEnv/marker scope の
  意味論を保った continuation representation を設計し直す必要がある。

追加確認:

- `cargo fmt --all --check`
- `cargo check -q -p evidence-vm -p yulang`
- `cargo check -q --release -p evidence-vm -p yulang`
- `cargo build -q --release -p yulang`
- `cargo test -q -p evidence-vm -- --test-threads=1`
- `cargo test -q -p yulang runtime_evidence -- --test-threads=1`
- `tests/yulang/yulang-adversarial-corpus/probe.sh`

## Evidence VM DirectTail segment shadow

目的:

- `2026-06-29-evidence-vm-single-digit-pro-review.md` の Rank 1 案に沿って、
  DirectTail continuation を `EvalContSegment + ScopeDelta` へ分けられる候補を測る。
- 実行意味論は変えない。
- release default の hot path に continuation traversal を載せない。

変更:

- `EvidenceDirectTailSegmentProfile` を追加した。
- DirectTail continuation が handler arm の continuation pattern へ materialize される
  タイミングでだけ shadow profile を取る。
- profile は `runtime_breakdown_stats_enabled` のときだけ continuation tree を読む。
- 通常 release では materialized count だけ増え、segment traversal は走らない。

release `bench/nondet_20_discard.yu`:

- flag なし:
  - `direct_tail_segment_materialized_continuations: 420`
  - `direct_tail_segment_candidates: 0`
  - `timing.runtime_evidence_execute: 10.5ms`
  - `compare.control: match`
- `--runtime-evidence-profile-deep`:
  - `direct_tail_segment_candidates: 420`
  - `direct_tail_segment_materialized_continuations: 420`
  - `direct_tail_segment_created: 0`
  - `direct_tail_segment_eval_frames: 17660`
  - `direct_tail_segment_then_frames: 1281`
  - `direct_tail_segment_scope_marker_frames: 840`
  - `direct_tail_segment_scope_marker_dynamic: 840`
  - `direct_tail_segment_scope_provider_env_frames: 861`
  - `direct_tail_segment_request_boundary_rejected: 420`
  - `timing.runtime_evidence_execute: 12.0ms`
  - `compare.control: match`

release `examples/showcase.yu --runtime-evidence-profile-deep`:

- `direct_tail_segment_candidates: 829`
- `direct_tail_segment_materialized_continuations: 829`
- `direct_tail_segment_created: 0`
- `direct_tail_segment_eval_frames: 23645`
- `direct_tail_segment_then_frames: 2636`
- `direct_tail_segment_scope_marker_frames: 1678`
- `direct_tail_segment_scope_marker_dynamic: 1678`
- `direct_tail_segment_scope_provider_env_frames: 1787`
- `direct_tail_segment_request_boundary_rejected: 829`
- `timing.runtime_evidence_execute: 27.1ms`
- `compare.control: match`

読み:

- DirectTail continuation は nondet/showcase とも全件 request boundary を含む。
- scope delta へ分離すべき MarkerFrame / ProviderEnv / Then は太いが、普通の
  eval segment だけを切り出す shadow 条件では `created` が 0 になる。
- 次に segment 方式を続けるなら、CatchBody / request boundary を segment 上で
  どう扱うかを設計する必要がある。
- その設計を避けるなら、次の小さい実験は `ResumeMarkerPlanId` / marker enter-op
  precompute 側が自然。

確認:

- `cargo fmt --all --check`
- `cargo check -q -p evidence-vm -p yulang`
- `cargo check -q --release -p evidence-vm -p yulang`
- `cargo build -q --release -p yulang`
- `cargo test -q -p evidence-vm direct_tail_segment_profile_splits_eval_scope_and_request_frames -- --test-threads=1`
- `cargo test -q -p evidence-vm -- --test-threads=1`
- `cargo test -q -p yulang runtime_evidence -- --test-threads=1`
- `tests/yulang/yulang-adversarial-corpus/probe.sh`

## Evidence VM ResumeMarkerPlan shadow

目的:

- `2026-06-29-evidence-vm-single-digit-pro-review.md` の Rank 2 案に沿って、
  `ResumeMarkerPlanId` / marker enter-op precompute の余地を測る。
- 実行意味論は変えない。
- profile 用の marker slice 再走査は `runtime_breakdown_stats_enabled` のときだけ行う。

変更:

- `resume_marker_plan_*` counters を追加した。
- `resume_marker_plan` / `resume_marker_plan_for_slice` を `&mut self` 化し、plan 作成時に
  empty / pure value / dynamic scope を分類する。
- enter-op は、frame marker push、active AddId push、handler boundary frame push を
  現行 `push_marker_frame` の操作に合わせて数える。

release `bench/nondet_20_discard.yu`:

- flag なし:
  - `resume_marker_plan_empty: 0`
  - `resume_marker_plan_pure_value: 0`
  - `resume_marker_plan_dynamic_scope: 0`
  - `timing.runtime_evidence_execute: 12.1ms`
  - `compare.control: match`
- `--runtime-evidence-profile-deep`:
  - `resume_marker_plan_empty: 0`
  - `resume_marker_plan_pure_value: 0`
  - `resume_marker_plan_dynamic_scope: 4199`
  - `resume_marker_plan_enter_ops_total: 10057`
  - `resume_marker_plan_handler_boundary: 1659`
  - `resume_marker_plan_active_add_id_ops: 4199`
  - `resume_marker_plan_to_legacy_push_pop: 4199`
  - `timing.runtime_evidence_execute: 13.4ms`
  - `compare.control: match`

release `examples/showcase.yu --runtime-evidence-profile-deep`:

- `resume_marker_plan_empty: 0`
- `resume_marker_plan_pure_value: 0`
- `resume_marker_plan_dynamic_scope: 8258`
- `resume_marker_plan_enter_ops_total: 19671`
- `resume_marker_plan_handler_boundary: 3155`
- `resume_marker_plan_active_add_id_ops: 8258`
- `resume_marker_plan_to_legacy_push_pop: 8258`
- `timing.runtime_evidence_execute: 28.4ms`
- `compare.control: match`

読み:

- representative workload では pure value marker plan が 0。
- `ResumeMarkerPlan` は全部 dynamic scope で、active AddId が plan 数と同数ある。
- simple な `PureValueMark` push/pop 省略はこの workload には刺さらない。
- 次にやるなら、dynamic scope を消すのではなく、active AddId / handler boundary の
  enter-op を事前計算して push/pop を軽くする方向になる。

確認:

- `cargo fmt --all --check`
- `cargo check -q -p evidence-vm -p yulang`
- `cargo check -q --release -p evidence-vm -p yulang`
- `cargo build -q --release -p yulang`
- `cargo test -q -p evidence-vm resume_marker_plan_profile_counts_dynamic_enter_ops -- --test-threads=1`
- `cargo test -q -p evidence-vm -- --test-threads=1`
- `cargo test -q -p yulang runtime_evidence -- --test-threads=1`
- `tests/yulang/yulang-adversarial-corpus/probe.sh`

## Evidence VM RouteCert / resume pack profile

目的:

- Pro review の別案として、operation route を first-class cert 化できるかを測る。
- `k true` / `k false` 系の resumptive fork を、tree 化せず pack として共有できるかを測る。
- 実行意味論は変えない。
- continuation tree の走査は `--runtime-evidence-profile-deep` のときだけ行う。

変更:

- `route_cert_*` counters を追加した。
- static direct / provider grant / no route を route cert として数える。
- provider grant は clean / AddId shadow / scope dirty / handler dirty / missing に分ける。
- `resume_pack_*` counters を追加した。
- `RuntimeEvidenceValue::Continuation` を thunk 化する地点で pack 候補を数え、
  deep profile のときだけ continuation shape を読む。

release `bench/nondet_20_discard.yu`:

- flag なし:
  - `route_cert_none: 399`
  - `route_cert_static_direct: 0`
  - `route_cert_provider_grant: 462`
  - `route_cert_provider_grant_clean: 0`
  - `route_cert_provider_grant_dirty_add_id: 420`
  - `route_cert_request_free: 0`
  - `route_cert_legacy_request_fallbacks: 0`
  - `resume_pack_candidates: 840`
  - `resume_pack_thunks_forced: 840`
  - `resume_pack_multi_shot_required: 840`
  - `resume_pack_can_share_segment: 0`
  - `timing.runtime_evidence_execute: 10.2ms`
  - `compare.control: match`
- `--runtime-evidence-profile-deep`:
  - `resume_pack_candidates: 840`
  - `resume_pack_thunks_forced: 840`
  - `resume_pack_multi_shot_required: 840`
  - `resume_pack_can_share_segment: 0`
  - `resume_pack_eval_frames: 35320`
  - `resume_pack_then_frames: 2562`
  - `resume_pack_scope_marker_frames: 1680`
  - `resume_pack_scope_provider_env_frames: 1722`
  - `resume_pack_request_boundary_rejected: 840`
  - `timing.runtime_evidence_execute: 12.8ms`
  - `compare.control: match`

release `examples/showcase.yu --runtime-evidence-profile-deep`:

- `route_cert_none: 682`
- `route_cert_static_direct: 0`
- `route_cert_provider_grant: 1782`
- `route_cert_provider_grant_clean: 0`
- `route_cert_provider_grant_dirty_add_id: 829`
- `resume_pack_candidates: 1655`
- `resume_pack_thunks_forced: 1655`
- `resume_pack_multi_shot_required: 1655`
- `resume_pack_can_share_segment: 0`
- `resume_pack_eval_frames: 47238`
- `resume_pack_then_frames: 5251`
- `resume_pack_scope_marker_frames: 3338`
- `resume_pack_scope_provider_env_frames: 3568`
- `resume_pack_request_boundary_rejected: 1655`
- `timing.runtime_evidence_execute: 26.9ms`
- `compare.control: match`

読み:

- representative workload の provider grant は clean route ではなく、AddId shadow が支配的。
- RouteCert を first-class にするだけでは request-free path は増えない。
- resume pack 候補は多いが、全件 request boundary を含む。
- `k true` / `k false` を共有 pack にするには、continuation の tree 表現だけでなく
  CatchBody / request boundary を pack 表現へ入れる設計が必要。
- 既存 `std::control::nondet::logic` collector は int-producing workload の `.list` 代替にはならず、
  一時 fixture では `conflicting type candidates for slot 24: int vs unit` になった。

確認:

- `cargo fmt --all --check`
- `cargo check -q -p evidence-vm -p yulang`
- `cargo check -q --release -p evidence-vm -p yulang`
- `cargo build -q --release -p yulang`
- `cargo test -q -p evidence-vm -- --test-threads=1`
- `cargo test -q -p yulang runtime_evidence -- --test-threads=1`
- `tests/yulang/yulang-adversarial-corpus/probe.sh`

## ListTree black height cache

目的:

- Pro review の Rank 7 にある primitive list merge の個別削減として、
  `ListTree::concat` が毎回 `black_height()` を再帰計算している箇所を小さく削る。
- list shape / red-black invariant は変えない。

変更:

- `ListNode` に `black_height` を保存した。
- `ListTree::black_height()` は node では保存値を返すようにした。
- `ListTree::node` で、従来と同じ left-child based black height を作成時に計算する。

release `bench/nondet_20_discard.yu` 5 runs:

- `list_merge_calls: 420`
- `timing.runtime_evidence_execute: 11.8ms / 10.8ms / 10.7ms / 11.0ms / 11.2ms`
- `compare.control: match`

release `examples/showcase.yu` 3 runs:

- `list_merge_calls: 832`
- `timing.runtime_evidence_execute: 25.3ms / 25.3ms / 24.8ms`
- `compare.control: match`

読み:

- 低リスクの改善としては採用できるが、代表 workload の主犯ではない。
- `.list` の merge 自体より、continuation resume / marker / provider scope がまだ支配的。

確認:

- `cargo fmt --all --check`
- `cargo test -q -p list-tree -- --test-threads=1`
- `cargo check -q -p evidence-vm -p yulang`
- `cargo check -q --release -p evidence-vm -p yulang`
- `cargo build -q --release -p yulang`
- `cargo test -q -p evidence-vm -- --test-threads=1`
- `cargo test -q -p yulang runtime_evidence -- --test-threads=1`
- `tests/yulang/yulang-adversarial-corpus/probe.sh`

## Evidence VM CatchBoundary shadow

目的:

- RouteCert / resume pack / list merge が単体で刺さらなかったため、
  `CatchBody` が request boundary として direct-tail / pack を塞いでいるか測る。
- 実行意味論は変えない。
- `has_request_boundary` と同じ再帰規則で profile 付き判定を追加する。

変更:

- `catch_boundary_*` counters を追加した。
- `has_request_boundary_*` counters を追加し、CatchBody / RefSet の境界理由を分類する。
- direct-tail が catch boundary に入る時の visible / generic fallback / provider permission dirty を測る。
- profile 付き boundary 判定が既存 `has_request_boundary` と同じ semantics であることを単体テスト化した。

release `bench/nondet_20_discard.yu --runtime-evidence-profile-deep`:

- `direct_tail_segment_candidates: 420`
- `direct_tail_segment_request_boundary_rejected: 420`
- `resume_pack_candidates: 840`
- `resume_pack_request_boundary_rejected: 840`
- `continuation_resume_catch_steps: 840`
- `request_continuation_appends: 1197`
- `request_continuation_then_appends: 798`
- `request_continuation_append_blocked_by_marker_frame: 399`
- `request_continuation_append_blocked_by_provider_env: 399`
- `catch_boundary_entries: 2962`
- `catch_boundary_value_results: 1681`
- `catch_boundary_effect_signals: 1281`
- `catch_boundary_direct_tail_signals: 861`
- `catch_boundary_direct_abortive_signals: 21`
- `catch_boundary_generic_request_signals: 399`
- `has_request_boundary_catch_same_handler: 0`
- `has_request_boundary_catch_no_routed_handler: 399`
- `has_request_boundary_catch_foreign_handler_recurse: 399`
- `direct_tail_blocked_by_catch_boundary: 0`
- `direct_tail_target_catch_boundary_visible: 420`
- `direct_tail_target_catch_boundary_provider_grant_clean: 0`
- `direct_tail_target_catch_boundary_provider_grant_dirty: 420`
- `direct_tail_target_catch_boundary_to_generic_request: 0`
- `timing.runtime_evidence_execute: 12.5ms`
- `compare.control: match`

release `examples/showcase.yu --runtime-evidence-profile-deep`:

- `direct_tail_segment_candidates: 829`
- `direct_tail_segment_request_boundary_rejected: 829`
- `resume_pack_candidates: 1655`
- `resume_pack_request_boundary_rejected: 1655`
- `continuation_resume_catch_steps: 1644`
- `request_continuation_appends: 2054`
- `request_continuation_then_appends: 1372`
- `request_continuation_append_blocked_by_marker_frame: 690`
- `request_continuation_append_blocked_by_provider_env: 682`
- `catch_boundary_entries: 6577`
- `catch_boundary_value_results: 3291`
- `catch_boundary_effect_signals: 3286`
- `catch_boundary_direct_tail_signals: 1791`
- `catch_boundary_direct_abortive_signals: 813`
- `catch_boundary_generic_request_signals: 682`
- `has_request_boundary_catch_same_handler: 0`
- `has_request_boundary_catch_no_routed_handler: 682`
- `has_request_boundary_catch_foreign_handler_recurse: 682`
- `direct_tail_blocked_by_catch_boundary: 0`
- `direct_tail_target_catch_boundary_visible: 829`
- `direct_tail_target_catch_boundary_provider_grant_clean: 0`
- `direct_tail_target_catch_boundary_provider_grant_dirty: 829`
- `direct_tail_target_catch_boundary_to_generic_request: 0`
- `timing.runtime_evidence_execute: 26.4ms`
- `compare.control: match`

読み:

- CatchBody は hot path で太い。
- ただし direct-tail が `has_request_boundary(Some(handler))` に塞がれているわけではない。
  `direct_tail_blocked_by_catch_boundary` は代表 workload で 0。
- deep profile の request boundary reject は、direct-tail continuation / resume pack の中に
  CatchBody が残っていることを示すが、signal propagation の壁としての
  `has_request_boundary` とは別問題。
- direct-tail は catch boundary へ入れており、visible は全件成立している。
  ただし provider permission は全件 dirty なので、clean RouteCert だけでは足りない。
- 次の設計は「CatchBody を単に request boundary から外す」ではなく、
  direct-tail handler body の continuation materialization と CatchBody / MarkerFrame /
  ProviderEnv を `ScopeDelta` 側へ分ける方向が本命。

確認:

- `cargo fmt --all --check`
- `cargo check -q -p evidence-vm -p yulang`
- `cargo check -q --release -p evidence-vm -p yulang`
- `cargo build -q --release -p yulang`
- `cargo test -q -p evidence-vm request_boundary_profile_matches_catch_boundary_semantics -- --test-threads=1`

## Runtime evidence total-before-compare timing split

- `debug runtime-evidence-run` の `timing.total_before_compare` が何を含んでいるかを
  分解した。
  追加した出力は `timing.args_parse`, `timing.collect`,
  `timing.control_build_cache`, `timing.control_build_total`,
  `timing.build_poly`, `timing.specialize`, `timing.control_lower`,
  `timing.evidence_summary`, `timing.evidence_plan_build`。
- 代表 workload は control cache hit でも `collect` が支配的だった。
  - `bench/nondet_20_discard.yu`: `collect` 約 21ms,
    `control_build_total` 約 1.2ms, `evidence_plan_build` 約 0.6ms,
    `runtime_evidence_execute` 約 12.5ms, `total_before_compare` 約 35.3ms。
  - `examples/showcase.yu`: `collect` 約 22.4ms,
    `control_build_total` 約 2.2ms, `evidence_plan_build` 約 1.0ms,
    `runtime_evidence_execute` 約 26.4ms, `total_before_compare` 約 52.1ms。
- 読みとしては、`total_before_compare` を大きく縮めるには VM だけでなく
  source collection / std collection の cache bypass が必要。
  control artifact hit は効いているが、その artifact key を得るために source set を
  毎回集めている。
- operation arm の visible 判定で見つけた arm index を eval へ渡す案も試したが、
  `nondet` / `showcase` とも改善せず少し悪化したため戻した。
  arm scan の二重化は構造上あるが、現時点の支配項ではなさそう。
- 確認:
  - `cargo fmt --all --check`
  - `cargo check -q -p evidence-vm -p yulang`
  - `cargo check -q --release -p evidence-vm -p yulang`
  - `cargo build -q --release -p yulang`
  - `cargo test -q -p evidence-vm -- --test-threads=1`
  - `cargo test -q -p yulang runtime_evidence -- --test-threads=1`
  - `tests/yulang/yulang-adversarial-corpus/probe.sh`
  - temp file で 1回目 collect → 2回目 `source-key-control-hit` →
    source 変更後 collect に戻ること
  - `target/release/yulang debug runtime-evidence-run bench/nondet_20_discard.yu --compare-control`
  - `target/release/yulang debug runtime-evidence-run examples/showcase.yu --compare-control`

## Runtime evidence numeric handler lookup tables

- `RuntimeEvidenceRunContext` の catch handler lookup と handler family lookup を
  `HashMap<ExprId, ...>` / `HashMap<u32, ...>` から Vec table へ寄せた。
  `ExprId` / handler id はすでに数値なので、runtime hot path では hash lookup を避ける。
- `catch_has_allowed_handler_id` は先に `handler_id -> family` table を見て
  `request_path` と一致するかを確認し、その後 `catch_expr -> handler ids` table で
  handler id を確認する形にした。
- これは path 文字列そのものの ID 化ではなく、その前段の低リスクな numeric lookup 化。
  実測では揺れが大きいが、代表 workload で悪化は見えなかった。
  - `bench/nondet_20_discard.yu`: `runtime_evidence_execute` 約 11.6ms
    （別 run では 10.6ms）。
  - `examples/showcase.yu`: `runtime_evidence_execute` 約 24.0ms。
- 確認:
  - `cargo fmt --all --check`
  - `cargo check -q -p evidence-vm -p yulang`
  - `cargo check -q --release -p evidence-vm -p yulang`
  - `cargo build -q --release -p yulang`
  - `cargo test -q -p evidence-vm -- --test-threads=1`
  - `cargo test -q -p yulang runtime_evidence -- --test-threads=1`
  - `tests/yulang/yulang-adversarial-corpus/probe.sh`
  - `target/release/yulang debug runtime-evidence-run bench/nondet_20_discard.yu --compare-control`
  - `target/release/yulang debug runtime-evidence-run examples/showcase.yu --compare-control`

## Source-key index for perceived runtime startup

- control artifact が既にある path 実行で、artifact key を得るためだけに毎回 source
  collection / std collection を走らせる問題に対して、`source-key` sidecar を追加した。
- sidecar は entry path + resolved std root + implicit std prelude mode から引く。
  中身は前回の full source key と、収集済み実ファイルの cheap fingerprint
  （path / len / mtime）だけ。
  fingerprint が一致したときだけ既存の control artifact を読む。
- source key 自体は従来通り full source text / module path / band path /
  resolution imports から作られたものを保存する。
  sidecar は source key の代替ではなく、既存 artifact へ到達するための入口。
- realm resolution imports を含む source set は初期実装では sidecar 対象外。
  解決先が変わっても旧 target file が残る場合があるため、ここは cache bypass しない。
- path 入力の `run` / `debug runtime-evidence-run` /
  `debug runtime-evidence-bench` は source-key hit なら collect を飛ばす。
  source text 入力は従来通り。
- 実測:
  - `bench/nondet_20_discard.yu`:
    1回目 `collect: 20.8ms`, `control_build_cache: control-hit`,
    `total_before_compare: 36.7ms`。
    2回目 `collect: 0us`, `control_build_cache: source-key-control-hit`,
    `control_build_total: 1.3ms`, `runtime_evidence_execute: 11.6ms`,
    `total_before_compare: 13.5ms`。
    最新確認では `runtime_evidence_execute: 10.9ms`,
    `total_before_compare: 12.7ms`。
  - `examples/showcase.yu`:
    1回目 `collect: 22.5ms`, `control_build_cache: control-hit`,
    `total_before_compare: 53.6ms`。
    2回目 `collect: 0us`, `control_build_cache: source-key-control-hit`,
    `control_build_total: 2.3ms`, `runtime_evidence_execute: 25.1ms`,
    `total_before_compare: 28.6ms`。
    最新確認では `runtime_evidence_execute: 24.3ms`,
    `total_before_compare: 27.6ms`。
  - 通常 `run --runtime-phase-timings bench/nondet_20_discard.yu` でも
    `run.cache: source-key-control-hit`, `run.collect: 0us`,
    `run.total: 27.9ms`。
- 確認:
  - `cargo fmt --all --check`
  - `cargo check -q -p yulang`
  - `cargo check -q --release -p yulang`
  - `cargo check -q -p evidence-vm -p yulang`
  - `cargo check -q --release -p evidence-vm -p yulang`
  - `cargo build -q --release -p yulang`
  - `cargo test -q -p yulang source_key_index -- --test-threads=1`
  - `cargo test -q -p evidence-vm -- --test-threads=1`
  - `cargo test -q -p yulang runtime_evidence -- --test-threads=1`
  - `tests/yulang/yulang-adversarial-corpus/probe.sh`
  - `target/release/yulang debug runtime-evidence-run bench/nondet_20_discard.yu --compare-control`
  - `target/release/yulang debug runtime-evidence-run examples/showcase.yu --compare-control`

## ResumePlan shadow v1

- `EvidenceContinuation` を実行変更なしで `ResumePlan` 風に分類する shadow
  profile を追加した。
  既存の `direct_tail_segment_profile` は request boundary で拒否する視点だったが、
  今回の shadow は `CatchBody` / marker / provider をそれぞれ
  `RequestBoundaryDelta` / `MarkerDelta` / `ProviderDelta` 候補として数える。
- release default では deep profile が off なので通常実行に乗らない。
  `--runtime-evidence-profile-deep` のときだけ `resume_plan_shadow_*` を出す。
- deep profile 結果:
  - `bench/nondet_20_discard.yu`
    - `resume_plan_shadow_candidates: 1260`
    - `direct_tail: 420`, `resume_pack: 840`
    - `catch_boundaries: 2520`
    - `marker_dynamic_frames: 2520`
    - `marker_active_add_id_ops: 2520`
    - `marker_handler_boundary_ops: 1260`
    - `provider_env_deltas: 2583`
    - `provider_grant_dirty_add_id: 420`
    - `multi_shot: 840`
    - `eval_only: 0`
    - `with_catch_boundary / with_dynamic_marker / with_provider_delta` はすべて 1260。
  - `examples/showcase.yu`
    - `resume_plan_shadow_candidates: 2484`
    - `direct_tail: 829`, `resume_pack: 1655`
    - `catch_boundaries: 4929`
    - `marker_dynamic_frames: 5016`
    - `marker_active_add_id_ops: 5016`
    - `marker_handler_boundary_ops: 2463`
    - `provider_env_deltas: 5355`
    - `provider_grant_dirty_add_id: 829`
    - `multi_shot: 1655`
    - `eval_only: 0`
    - `with_catch_boundary / with_dynamic_marker / with_provider_delta` はすべて 2484。
- 読み:
  - 単なる eval segment 化では足りない。
    候補は全件 `CatchBody`、dynamic marker、ProviderEnv delta を同時に持つ。
  - 次に実行変更へ進むなら、`CatchBody-aware ResumePlan` と
    `MarkerDelta` / `ProviderDelta` を同じ plan に同梱する方向が本命。
  - `direct_tail_segment_request_boundary_rejected` と
    `resume_pack_request_boundary_rejected` は deep profile でそれぞれ
    nondet 420 / 840、showcase 829 / 1655。既存の拒否は
    shadow 上では `RequestBoundaryDelta` 候補として説明できる。
- 確認:
  - `cargo fmt --all --check`
  - `cargo check -q -p evidence-vm -p yulang`
  - `cargo check -q --release -p evidence-vm -p yulang`
  - `cargo test -q -p evidence-vm -- --test-threads=1`
  - `cargo test -q -p yulang runtime_evidence -- --test-threads=1`
  - `tests/yulang/yulang-adversarial-corpus/probe.sh`
  - `target/release/yulang --std-root lib debug evidence-vm-run --compare-control --runtime-evidence-profile-deep bench/nondet_20_discard.yu`
  - `target/release/yulang --std-root lib debug evidence-vm-run --compare-control --runtime-evidence-profile-deep examples/showcase.yu`

<!-- daily-append-anchor: 2026-06-29 -->
