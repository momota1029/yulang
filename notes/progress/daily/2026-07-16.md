# 2026-07-16

## Constraint replay deduplication investigation closeout

- Closed the approximately 87%-duplicate constraint-replay investigation as a negative/informative
  finding, with no implementation done or planned. The duplicate count records independent
  transitive-closure paths rediscovering already-known global consequences, not repeated local
  bound-frontier work.
- Markdown already rejects 836,705 of 836,781 duplicates (99.991%) before replay snapshot/queueing,
  and the local shadow check found 0 repeated keys among 219,232 var-var insertion candidates.
- The env-gated evidence-only-skip prototype silently changed exported public type schemes and was
  slower end to end (4.945s baseline versus 5.073s enabled). The project is not being pursued
  further at this time.
- Full evidence, current timing context, and the three ranked but not-pursued revisit options are in
  `notes/design/2026-07-16-constraint-replay-dedup-investigation.md`.

## Contract v1 ambient line-editing cross-host closeout

- Closed the last immediate file-resource parity slice by pairing the existing native and
  source-mock `$doc.lines.each` update-chain cases with
  `file_ref_lines_each_update_chain_unsupported_host`. The unsupported route fixes
  `yulang.unsupported-host-capability` plus the standard mock-handler hint and proves that eager
  `ambient_touch` fails before line access.
- Verified the three sibling cases (`contract cases ok: 3`), the full file-resource gate
  (`contract cases ok: 75`), and end-to-end release smoke against a fresh release binary with all
  three cases in its focused file-resource set.
- The first release-smoke attempt exposed two unrelated pre-existing/regression blockers: the
  embedded std omitted `std/text/yumark.yu`, and today's Stage 2 owner-scheduling change left cache
  tests accessing the now-private `role_impls` field. Those were fixed separately in `70812e58`
  and `4446d460` before this slice could be verified end to end.
- Updated the contract evidence and tracking pointers. No further immediate Contract v1
  file-resource slice is queued; bytes, range, directory, and remaining server work stay post-v1.

## Per-demand exact solve snapshots: Stage 1 closeout

- Completed all three test-only Stage 1 slices (`0a2d52b1`, `2a8a7782`, `4c51d287`). The full
  nine-case acceptance set now reports 1,019 would-be hits, 1,019 exact matches, and zero
  result/disposition/state-delta/full-path mismatches; isolated shadow-off/shadow-on runs also
  preserve schemes, residual predicates, diagnostics, selections, restart/SCC order, lowering, and
  runtime surfaces exactly.
- A fresh release build and no-cache four-fixture pass succeeded. Internal total / role-solve phase
  was Markdown 5.775s / 1.930s, HTML 1.153s / 12.3ms, repository-std-only 1.034s / 7.3ms, and
  showcase 1.150s / 8.3ms. The Stage 1 oracle is entirely `#[cfg(test)]`-gated, so these release
  runs contain no shadow work and confirm current production timing context rather than measuring a
  shadow-on release delta.
- The fresh acceptance-test instrumentation measured 1.733425s of would-be-hit full-solve time,
  13.537ms of `E/A/M/D/C` lookup/equality, 765.344ms of shadow result/live-application comparisons,
  104.663ms of snapshot cloning, and 1.943ms of retention accounting. The explicitly timed shadow
  dimensions total 885.487ms; Debug-size formatting/report allocation is not separately timed.
- Peak retention was 6 entries and 20,653,144 Debug-proxy bytes against the approved 64-entry and
  128-MiB caps, with zero cap fallback. The 24-byte increase over Stage 0's 20,653,120-byte peak is
  the three retained Markdown `proof` entries each replacing the 8-byte raw candidate generation
  with the 16-byte fail-closed guard, not a larger compact tree.
- All nine Stage 1 exit criteria are met. The measured margin and cap headroom keep the project on
  track for a separate Stage 2 activation-planning decision; Stage 2 was not started. Section 8's
  numerical same-time release improvement/no-regression thresholds remain TBD and must be explicitly
  approved before any production activation.

## Per-demand exact solve snapshots: Stage 2 activation and failed release gate

- Committed the reviewed Stage 2 numerical proposal, then wired fail-closed exact `E/A/M/D/C`
  production reuse so a hit skips only the recursive solve. The always-full-solve session and CLI
  controls remain available, and the independent test-only shadow audit remains intact.
- Correctness verification passed: isolated production/always-solve sessions preserved schemes,
  predicates, diagnostics, selections, restart/SCC order, lowering, poly, and runtime surfaces; the
  independent audit again found 1,019/1,019 exact matches with zero mismatches. The full infer lib
  suite reported 831 passed and only the five known `mark_expr_block_*` failures.
- Five same-time alternating release pairs per fixture failed the approved performance gate.
  Markdown enabled/always-solve means were 7.6860/7.6344 seconds internally end to end and
  2.7974/2.7686 seconds in `analysis.generalize_resolve_roles`, regressions of 0.676% and 1.040%
  instead of the required 10% and 35% improvements. repository-std-only regressed 2.350%, above
  its 2% bound; HTML improved 0.308% and showcase improved 12.320%.
- All enabled runs stayed at six entries and 19,764 Debug-proxy bytes with zero cap fallback, well
  inside the 64-entry and 128-MiB caps. Performance, not correctness or retention, blocks Stage 2
  release acceptance. No threshold was weakened and no post-result implementation adjustment was
  made.

<!-- daily-append-anchor: 2026-07-16 -->
