# 2026-05-06

## Playground reflection

- Reflected the newer parser/lowering surface in the playground examples:
  - newline-separated list items
  - weak-binding multiline `return`
- Added wasm regression tests for both playground-facing cases.
- Rebuilt `web/playground` and synced `dist/` to `/home/momota1029/www/yulang-playground/`.

## Verified

- `cargo fmt`
- `RUSTC_WRAPPER= cargo test -q -p yulang-wasm`
- `npm run build` in `web/playground`
- `RUSTC_WRAPPER= cargo run -q -p yulang -- --run --runtime-phase-timings examples/showcase.yu`

## Notes

- The wasm entrypoint continues to use the default principal-elaborate monomorphize path; no playground environment variable is required.
- `examples/showcase.yu` still runs and prints four results under the default path.
