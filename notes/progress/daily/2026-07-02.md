# 2026-07-02

## Public contract manifest first slice

- Context:
  - ChatGPT Pro recommended turning the Public Contract Spine into an
    executable contract rather than adding more language surface.
  - Runtime and diagnostics already had shared `.yu` fixtures, but the
    fixture set was still driven by Rust test code instead of a repository
    manifest.
- Implementation summary:
  - Added `tests/yulang/cases.toml` as the first public contract manifest.
  - The manifest currently covers focused CLI `run` cases for:
    - `nondet_once_triple`
    - `optional_record_defaults`
    - `attached_impl_pick`
    - `list_update`
  - It also covers focused CLI `check` diagnostics for:
    - `type_annotation_mismatch`
    - `unresolved_value_name`
    - `catch_missing_arm_body`
  - Added `public-signature` manifest support and canaries for:
    - data-position effectful function public signature
    - nested data-position effectful function public signature
    - `std.control.var.ref.update`
    - `std.text.parse.choice`
    - `std.control.flow.loop.for_in`
    - `std.control.nondet.nondet.#act-method:once`
    - `std.control.nondet.nondet.#act-method:list`
  - Extended the manifest to include the remaining focused public diagnostics
    fixtures for unresolved type names, unsupported type syntax, top-level
    mutable bindings, parser syntax recovery, rule lazy quantifiers, and
    malformed `catch` arms.
  - Moved representative runtime/compile error sources into shared fixtures and
    covered unhandled effects, non-call calls, non-record field access, pattern
    mismatch, missing record fields, unresolved methods, and ambiguous methods.
  - Fixed the cached runtime build path so specialization failures go through
    the same route-error formatter as the no-cache path. Without this, cached
    `run` reported raw subtype constraints instead of the public
    `compile error [yulang.*]` message and hint.
  - Added repo-root manifest entries for public examples `examples/01_*.yu`
    through `examples/13_console.yu`, plus `examples/showcase.yu`.
    Console output is checked without `--print-roots`; showcase stays a compact
    contains check rather than a large stdout snapshot.
  - Clarified standard API contract staging:
    - `tests/yulang/cases.toml` is for current executable behavior, not TODO or
      ignored placeholders.
    - file/server/host capability cases should enter the manifest only when
      native or unsupported-host public CLI behavior can be observed.
    - provisional helpers should not receive stable contract tags.
  - Added a source-level public signature invariant helper that scans every
    public signature type in selected contract-spine std modules and rejects
    private stack evidence (`#...` / `AllExcept...`) in the type part. The scan
    strips public symbol names and bodies first, so `#act-method` names remain
    valid while leaked type evidence is still caught.
  - Hardened the manifest runner schema:
    - case names must be unique;
    - every case must assert either CLI output or a public signature type;
    - hardening smoke already runs the manifest through its `cargo test -q -p
      yulang public -- --test-threads=1` gate because the manifest test is a
      public CLI test.
  - Connected diagnostics manifest entries to the structured `SourceDiagnostic`
    payload:
    - check cases can now assert diagnostic count, code, label, related count,
      and related origins;
    - current public diagnostics fixtures assert the shared payload without
      broadening stdout snapshots.
  - Removed the older inline public examples CLI bridge after the manifest
    became the stronger executable owner for `examples/01_*.yu` through
    `examples/showcase.yu`. This avoids running the same public examples twice
    in `cargo test -p yulang --test cli`.
  - Updated solver invariant docs to point at the manifest public-signature
    cases and the all-public-signature scan for contract-spine std modules.
  - Added a manifest-driven CLI integration test that reads the manifest with
    `toml`, runs each CLI case through the `yulang` binary, uses repo `lib` as
    std root for runtime cases, keeps runtime cache isolated, and checks public
    signature cases through the source dump API.
  - Updated `tests/yulang/README.md`, `docs/status.md`, `notes/todo/testing.md`,
    and `tasks/current.md` so the manifest is visible as the first executable
    contract entrypoint.
- Validation:
  - `cargo fmt --all`
  - `timeout 240s cargo test -q -p yulang public_contract_manifest_cli_cases_hold --test cli -- --test-threads=1`
    - `1 passed`
  - `timeout 240s cargo test -q -p yulang public_diagnostics -- --test-threads=1`
    - CLI diagnostics filter: `12 passed`
  - `timeout 240s cargo test -q -p yulang --test cli -- --test-threads=1`
    - `97 passed`
  - `timeout 240s cargo test -q -p yulang public_contract_manifest_cli_cases_hold --test cli -- --test-threads=1`
    - `1 passed` after adding runtime/diagnostic error cases and the cached
      specialization error formatter fix.
  - `timeout 240s cargo test -q -p yulang public_contract_manifest_cli_cases_hold --test cli -- --test-threads=1`
    - `1 passed` after adding public example manifest entries.
  - `timeout 240s cargo test -q -p yulang dump_poly_public_contract_spine_modules_hide_private_stack_evidence -- --test-threads=1`
    - `1 passed`
  - `timeout 240s cargo test -q -p yulang public_contract_manifest_cli_cases_hold --test cli -- --test-threads=1`
    - `1 passed` after adding structured diagnostic payload checks.
  - `timeout 300s cargo test -q -p yulang --test cli -- --test-threads=1`
    - `95 passed` after manifest ownership and duplicate public example bridge
      removal; wall time was 180.99s.
  - `timeout 180s scripts/public-example-smoke.sh target/debug/yulang`
    - passed
  - Added LSP diagnostic hover for structured diagnostics:
    - hover on a diagnostic primary range now reports the diagnostic code,
      message, and compact related provenance before falling back to type hover;
    - this reuses the existing LSP diagnostic conversion so CLI/LSP payloads
      keep the same code/range/related source of truth.
  - Validation:
    - `timeout 240s cargo test -q -p yulang hover_for_source_reports_diagnostic_summary_at_error_range -- --test-threads=1`
      - `1 passed`
    - `timeout 240s cargo test -q -p yulang hover_for_source -- --test-threads=1`
      - `4 passed`
    - `timeout 240s cargo test -q -p yulang diagnostics_use -- --test-threads=1`
      - `8 passed`
  - Added `SourceDiagnosticSeverity` to the shared diagnostic payload:
    - parser and lowering diagnostics now carry `Error` in the source payload;
    - CLI, LSP, wasm playground JSON, and `tests/yulang/cases.toml` manifest
      assertions read severity from that shared payload instead of hard-coding
      it separately.
  - Validation:
    - `timeout 240s cargo test -q -p yulang analyze_entry_source_reports_unresolved_name_source_range -- --test-threads=1`
      - `1 passed`
    - `timeout 240s cargo check -q -p wasm`
      - passed
    - `timeout 240s cargo test -q -p yulang diagnostics_use -- --test-threads=1`
      - `8 passed`
    - `timeout 240s cargo test -q -p yulang public_contract_manifest_cli_cases_hold --test cli -- --test-threads=1`
      - `1 passed`
  - Added the first standard API contract cases for `std::data::result`:
    - `tests/yulang/regressions/runtime/result_api.yu` covers `map`,
      `and_then`, and `unwrap_or` through the public CLI/std route;
    - manifest public-signature cases now fix `std.data.result.map`,
      `std.data.result.and_then`, and `std.data.result.unwrap_or`.
  - Validation:
    - `target/debug/yulang --std-root lib run --print-roots tests/yulang/regressions/runtime/result_api.yu`
      - `run roots [(result::ok(11), result::err("odd"), 42)]`
    - `timeout 240s cargo test -q -p yulang public_contract_manifest_cli_cases_hold --test cli -- --test-threads=1`
      - `1 passed`
  - Added the first executable `error E:` / `fail` / `E::wrap` standard API
    contract:
    - `tests/yulang/regressions/runtime/error_wrap_fail.yu` covers
      `fail (E::variant payload)` closed by `E::wrap` through the public
      CLI/std route;
    - manifest public-signature coverage now fixes `small_err.wrap` as an
      effectful action boundary, so the action argument keeps `[small_err; 'e]`
      instead of being forced before the generated catch;
    - the cause-side fix is in synthetic error helper generation: generated
      `wrap` / `up` helpers annotate their `action` parameter with the error
      row they catch, and role associated effect rows lower as exact role args
      instead of subtractable placeholder vars.
  - Validation:
    - `timeout 240s cargo test -q -p yulang run_control_source_text_with_embedded_std_runs_user_error_fail_wrap -- --test-threads=1`
      - `1 passed`
    - `cargo run -q -p yulang -- --std-root lib --no-cache run --print-roots notes/bugs/solved/finalize_error_wrap_fail_breaks.yu`
      - `run roots [result::err(fs_err::not_found("missing"))]`
    - `cargo run -q -p yulang -- --std-root lib --no-cache run --print-roots tests/yulang/regressions/runtime/error_wrap_fail.yu`
      - `run roots [(result::ok(7), result::err(small_err::nope(3)))]`
  - Extended the error standard API contract to `from` aggregation and `E::up`:
    - `tests/yulang/regressions/runtime/error_from_wrap.yu` covers a wider
      `io_err::wrap` catching a narrower `fs_err` and returning the
      `io_err::fs` variant;
    - `tests/yulang/regressions/runtime/error_up_wrap.yu` covers `io_err::up`
      lifting the narrower error effect before `io_err::wrap` closes it;
    - manifest public-signature cases now fix `io_err.wrap` and `io_err.up`
      so aggregation/lifting keeps the source and residual effect rows visible.
  - Validation:
    - `cargo run -q -p yulang -- --std-root lib --no-cache run --print-roots tests/yulang/regressions/runtime/error_from_wrap.yu`
      - `run roots [result::err(io_err::fs(fs_err::not_found("missing")))]`
    - `cargo run -q -p yulang -- --std-root lib --no-cache run --print-roots tests/yulang/regressions/runtime/error_up_wrap.yu`
      - `run roots [result::err(io_err::fs(fs_err::not_found("missing")))]`
  - Added generated `Display E` lowering and public runtime contracts:
    - `error` synthetic lowering now registers a generated `Display` impl
      alongside `Throw`, `wrap`, and `up`;
    - the generated `show` body is lowered through the ordinary role impl
      method path, so receiver typing and payload `.show` constraints use the
      existing role machinery instead of a separate display shortcut;
    - normal payload variants render `variant: payload`, unit variants render
      `variant`, tuple payloads render `variant(a, b)`, and `from` variants
      delegate to the wrapped error's `Display`;
    - `tests/yulang/regressions/runtime/error_display.yu` covers unit,
      generic, and tuple payload display;
    - `tests/yulang/regressions/runtime/error_display_from_wrap.yu` covers
      `io_err::wrap` returning a wider error and user code displaying the
      `err e` branch.
  - Validation:
    - `cargo run -q -p yulang -- --std-root lib --no-cache run --print-roots notes/bugs/solved/error_display_impl_missing.yu`
      - `run roots ["boom: x"]`
    - `cargo run -q -p yulang -- --std-root lib --no-cache run --print-roots notes/bugs/solved/error_display_from_chain_missing.yu`
      - `run roots ["not_found: x"]`
    - `cargo run -q -p yulang -- --std-root lib --no-cache run --print-roots notes/bugs/solved/wrap_does_not_traverse_from_chain.yu`
      - `run roots ["err: not_found: /missing"]`
    - `cargo run -q -p yulang -- --std-root lib --no-cache run --print-roots tests/yulang/regressions/runtime/error_display.yu`
      - `run roots [("item: 7", "empty", "pair(3, x)")]`
    - `cargo run -q -p yulang -- --std-root lib --no-cache run --print-roots tests/yulang/regressions/runtime/error_display_from_wrap.yu`
      - `run roots ["err: not_found: /missing"]`
    - `timeout 240s cargo test -q -p yulang public_contract_manifest_cli_cases_hold --test cli -- --test-threads=1`
      - `1 passed`, finished in 119.92s
    - `cargo check -q -p yulang`
      - passed
    - `cargo fmt --all -- --check`
      - passed
    - `git diff --check`
      - passed
  - Started the native CLI host contract for `std::io::file` on the default
    evidence VM route:
    - the evidence VM host bridge now handles `std::io::file::file` requests
      for `read_at`, `write_at`, `open_text_raw`, `file_get`, `file_set`,
      `file_flush`, `exists`, `is_file`, and `is_dir`;
    - `std::io::file::read_text` / `write_text` no longer escape as unhandled
      effects in normal `yulang run`;
    - `read_at` / `write_at` currently back the full-text wrappers and do not
      yet promise partial range semantics;
    - `open_text` / `open` and path-payload display are still follow-up
      standard API surface work.
  - Validation:
    - `timeout 240s cargo test -q -p yulang compatible_run_std_file_read_write_text_host_contract --test cli -- --test-threads=1`
      - `1 passed`, finished in 3.01s
  - Added the path-display bridge needed by typed file errors:
    - `BytesToPath` / `PathToBytes` now execute in mono runtime, control VM,
      and evidence VM using the current string-backed path value model;
    - `std::text::path` now implements `Display` through `path.to_bytes` and
      UTF-8 lossy text conversion;
    - `tests/yulang/regressions/runtime/path_display.yu` fixes the public
      byte-conversion/display roundtrip;
    - a CLI temp-file test fixes `std::io::file::io_err::wrap` on a missing
      file showing `not_found: <path>`.
  - Validation:
    - `cargo check -q -p mono-runtime -p control-vm -p evidence-vm -p yulang`
      - passed
    - `timeout 240s cargo test -q -p yulang compatible_run_std_file --test cli -- --test-threads=1`
      - `2 passed`
    - `cargo run -q -p yulang -- --std-root lib --no-cache run --print-roots tests/yulang/regressions/runtime/path_display.yu`
      - `run roots [("/tmp/yulang-path", "/tmp/yulang-path")]`
    - `cargo run -q -p yulang -- --std-root lib --no-cache run --control-vm --print-roots tests/yulang/regressions/runtime/path_display.yu`
      - `run roots [("/tmp/yulang-path", "/tmp/yulang-path")]`
    - `cargo run -q -p yulang -- --std-root lib --no-cache run --interpreter --print-roots tests/yulang/regressions/runtime/path_display.yu`
      - `run roots [("/tmp/yulang-path", "/tmp/yulang-path")]`
    - `timeout 240s cargo test -q -p yulang public_contract_manifest_cli_cases_hold --test cli -- --test-threads=1`
      - `1 passed`, finished in 125.05s
    - `cargo fmt --all -- --check`
      - passed
    - `git diff --check`
      - passed
  - Extended wasm playground diagnostics over the catch syntax shared
    fixtures:
    - `catch_missing_scrutinee.yu`, `catch_missing_arm_pattern.yu`, and
      `catch_missing_arm_body.yu` are now included by the playground
      diagnostics tests;
    - the test asserts code, severity, primary range, message, hint, and empty
      related ranges for all three fixtures.
  - Validation:
    - `timeout 240s cargo test -q -p wasm check_inner_returns_catch_syntax_codes_and_ranges -- --test-threads=1`
      - `1 passed`, finished in 7.94s
    - `cargo fmt --all -- --check`
      - passed
  - Tightened the public contract manifest diagnostics gate:
    - every `kind = "check"` case now must assert diagnostic count, code,
      severity, a complete primary byte range, and related diagnostic count;
    - non-check cases cannot use source-diagnostic primary range expectations;
    - related origin expectations must stay aligned with the expected related
      diagnostic count.
  - Validation:
    - `timeout 240s cargo test -q -p yulang public_contract_manifest_cli_cases_hold --test cli -- --test-threads=1`
      - `1 passed`, finished in 158.48s
    - `cargo fmt --all -- --check`
      - passed
  - Extended LSP diagnostics over the same catch syntax fixtures:
    - server diagnostics tests now include missing catch scrutinee, missing
      catch arm pattern, and missing catch arm body;
    - the test asserts LSP code, severity, range, message, hint text, and empty
      related information for all three fixtures.
  - Validation:
    - `timeout 240s cargo test -q -p yulang server::tests::diagnostics_use_catch_syntax_ranges -- --test-threads=1`
      - `1 passed`
    - `timeout 240s cargo test -q -p yulang server::tests::diagnostics_use_ -- --test-threads=1`
      - `9 passed`
    - `cargo fmt --all -- --check`
      - passed
    - `git diff --check`
      - passed
  - Added LSP diagnostic hover coverage for catch syntax:
    - hovering the `->` range in `catch_missing_arm_body.yu` now returns the
      diagnostic code, user-facing message, and hint text.
  - Validation:
    - `timeout 240s cargo test -q -p yulang server::tests::hover_for_source_reports_catch_diagnostic_summary -- --test-threads=1`
      - `1 passed`
    - `timeout 240s cargo test -q -p yulang server::tests::hover_for_source_reports_diagnostic_summary -- --test-threads=1`
      - `1 passed`
    - `cargo fmt --all -- --check`
      - passed
  - Added `std::data::result` propagation coverage:
    - `tests/yulang/regressions/runtime/result_propagation.yu` checks that
      `map` and `and_then` propagate `err` unchanged and `unwrap_or` keeps
      the `ok` value;
    - the case is registered in `tests/yulang/cases.toml` as a
      runtime/standard-api/result contract.
  - Validation:
    - `cargo run -q -p yulang -- --std-root lib --no-cache run --print-roots tests/yulang/regressions/runtime/result_propagation.yu`
      - `run roots [(result::err("bad"), result::err("bad"), 5)]`
    - `timeout 240s cargo test -q -p yulang public_contract_manifest_cli_cases_hold --test cli -- --test-threads=1`
      - `1 passed`, finished in 160.49s
    - `cargo fmt --all -- --check`
      - passed
  - Tightened public-signature manifest shape:
    - every `kind = "public-signature"` case now must carry exact
      `expect_type`, preventing substring-only public type checks from entering
      the contract manifest;
    - `tests/yulang/README.md` documents this as part of the public signature
      golden policy.
  - Validation:
    - `timeout 240s cargo test -q -p yulang public_contract_manifest_cli_cases_hold --test cli -- --test-threads=1`
      - `1 passed`, finished in 161.22s
    - `cargo fmt --all -- --check`
      - passed
  - Tightened runtime-error manifest classification:
    - runtime error cases now carry either `runtime-failure` or
      `compile-error`;
    - manifest validation requires that subtype/method compile failures stay
      distinguishable from runtime failures.
  - Validation:
    - `timeout 240s cargo test -q -p yulang public_contract_manifest_cli_cases_hold --test cli -- --test-threads=1`
      - `1 passed`, finished in 160.77s
    - `cargo fmt --all -- --check`
      - passed
  - Tightened the runtime-error tag policy further:
    - `runtime-error` cases now must carry exactly one of `runtime-failure` or
      `compile-error`;
    - `compile-error` cases must also carry `diagnostics`, keeping them visible
      as future candidates for structured source diagnostics.
  - Validation:
    - `timeout 240s cargo test -q -p yulang public_contract_manifest_cli_cases_hold --test cli -- --test-threads=1`
      - `1 passed`, finished in 161.46s
    - `cargo fmt --all -- --check`
      - passed
  - Aligned runtime-error tags with stderr expectations:
    - `compile-error` cases must assert a `compile error [` prefix;
    - `runtime-failure` cases must assert a `runtime error [` prefix.
  - Validation:
    - `timeout 240s cargo test -q -p yulang public_contract_manifest_cli_cases_hold --test cli -- --test-threads=1`
      - `1 passed`, finished in 160.25s
    - `cargo fmt --all -- --check`
      - passed
  - Recorded the remaining role/method diagnostics boundary:
    - unresolved / ambiguous role method failures are currently covered as
      `run` compile-error contracts;
    - they do not yet flow through `SourceDiagnostic` for `check` / LSP /
      playground, so `compile-error` manifest tags mark the future migration
      target.
  - Added public-signature manifest coverage for the executable
    `std::io::file` first slice:
    - `read_at`, `write_at`, `read_text`, and `write_text` keep both
      `std::io::file::file` and `std::io::file::io_err` effects visible;
    - low-level host predicates `file.exists`, `file.is_file`, and
      `file.is_dir` keep the `file` effect visible;
    - `io_err.wrap` fixes the typed failure boundary for file operations;
    - managed ref functions `open_text`, `open`, and `open_in` stay outside
      the manifest until their resource/lifetime behavior is executable.
  - Validation:
    - `timeout 240s cargo test -q -p yulang public_contract_manifest_cli_cases_hold --test cli -- --test-threads=1`
      - `1 passed`, finished in 148.00s
    - `git diff --check`
      - passed
  - Promoted basic managed file text refs into the executable contract:
    - `compatible_run_std_file_open_text_ref_host_contract` covers direct
      `open_text` get/set and `open_in path: \&txt -> ...` scoped callback
      get/set against temp files;
    - public-signature manifest cases now cover `open_text`, `open`, and
      `open_in`;
    - metadata, directory listing, range writes, locking, explicit close, and
      scope-exit write-back semantics remain provisional.
  - Validation:
    - `timeout 240s cargo test -q -p yulang compatible_run_std_file --test cli -- --test-threads=1`
      - `3 passed`, finished in 9.14s
    - `timeout 240s cargo test -q -p yulang public_contract_manifest_cli_cases_hold --test cli -- --test-threads=1`
      - `1 passed`, finished in 156.54s
    - `cargo fmt --all -- --check`
      - passed
    - `git diff --check`
      - passed
  - Wired the public contract manifest into `scripts/hardening-smoke.sh`:
    - hardening now runs `public_contract_manifest_cli_cases_hold` by default;
    - `YULANG_HARDENING_PUBLIC_CONTRACT_MANIFEST=0` can skip it for local
      focused runs.
  - Validation:
    - `bash -n scripts/hardening-smoke.sh`
      - passed
    - `timeout 240s cargo test -q -p yulang public_contract_manifest_cli_cases_hold --test cli -- --test-threads=1`
      - `1 passed`, finished in 157.43s
    - `git diff --check`
      - passed
  - Tightened `tests/yulang/cases.toml` as a contract index:
    - the manifest runner now requires `run` cases to carry a
      runtime-facing tag, `check` cases to carry `diagnostics`, and
      `public-signature` cases to carry `public-signature`;
    - `tests/yulang/README.md` documents the tag/kind rule so future contract
      entries stay searchable by surface.
  - Validation:
    - `timeout 240s cargo test -q -p yulang public_contract_manifest_cli_cases_hold --test cli -- --test-threads=1`
      - `1 passed`, finished in 157.72s
    - `cargo fmt --all -- --check`
      - passed
    - `git diff --check`
      - passed
  - Updated `spec/2026-07-01-file-resource-api.md` with the current
    executable first slice:
    - documented path byte conversion/display, native CLI text read/write,
      file predicates, typed `io_err::wrap`, and basic `open_text`/`open_in`
      text refs as the observed contract;
    - kept range semantics, metadata shape, directory listing, locking,
      scope-exit write-back, unsupported-host behavior, and non-UTF-8 path
      policy explicitly provisional;
    - stated that `read_text` / `write_text` remain executable helpers, not the
      center of the stable file resource API.
  - Validation:
    - `git diff --check`
      - passed
  - Tightened the release artifact gate around the language server:
    - `scripts/hardening-smoke.sh` now enables the `release-smoke.sh`
      `yulang server` startup check by default via
      `YULANG_SMOKE_SERVER=1`;
    - `YULANG_HARDENING_RELEASE_SERVER_SMOKE=0` can skip only that server
      startup check for focused local runs;
    - `README.md` documents that the hardening gate covers the public
      contract manifest and release server startup.
  - Validation:
    - `bash -n scripts/hardening-smoke.sh`
      - passed
    - `YULANG_SMOKE_SERVER=1 YULANG_SMOKE_SERVER_TIMEOUT=2s timeout 120s scripts/release-smoke.sh target/debug/yulang`
      - `release smoke ok: target/debug/yulang`
    - `git diff --check`
      - passed
  - Extended playground/wasm diagnostic surface coverage:
    - `check_inner_returns_diagnostic_code_and_value_name_range` now reuses
      `tests/yulang/regressions/diagnostics/unresolved_value_name.yu` and
      fixes code, severity, label, range, message, and hint;
    - `check_inner_returns_top_level_mutable_binding_code_and_range` now reuses
      `tests/yulang/regressions/diagnostics/top_level_mutable_binding.yu` and
      fixes code, severity, label, range, message, and hint;
    - this keeps more manifest diagnostics visible through the playground
      payload path instead of only the CLI contract path.
  - Validation:
    - `timeout 240s cargo test -q -p wasm check_inner_returns_ -- --test-threads=1`
      - `8 passed`, finished in 22.86s
    - `cargo fmt --all -- --check`
      - passed
    - `git diff --check`
      - passed
    - failed command, retried with one filter:
      - `timeout 240s cargo test -q -p wasm check_inner_returns_diagnostic_code_and_value_name_range check_inner_returns_top_level_mutable_binding_code_and_range -- --test-threads=1`
      - cargo rejected multiple test filters.
  - Added a manifest-level public-signature cleanliness gate:
    - every `public-signature` case now rejects `#` and `AllExcept` fragments
      in the projected public type even when the individual case forgets
      `deny_type_contains`;
    - `tests/yulang/README.md` documents the common private-evidence leak
      check and keeps surface-specific deny checks available.
  - Validation:
    - `timeout 240s cargo test -q -p yulang public_contract_manifest_cli_cases_hold --test cli -- --test-threads=1`
      - `1 passed`, finished in 158.39s
    - `cargo fmt --all -- --check`
      - passed
    - `git diff --check`
      - passed
  - Added `spec/2026-07-02-server-resource-api.md` as the server API anchor:
    - keeps the current executable slice honest: release artifact
      `yulang server` startup and LSP structured surfaces are covered, while
      `std::server` remains absent from `cases.toml`;
    - fixes the center as host event session plus request/response resource,
      not an HTTP framework;
    - records `accept` as a continuation suspend/resume boundary, request
      response as one-shot capability consumption, and host-boundary values as
      requiring an explicit wire codec;
    - links the new spec from `spec/README.md`,
      `spec/2026-07-01-stable-standard-api.md`, and `docs/status.md`.
  - Validation:
    - `test -f spec/2026-07-02-server-resource-api.md`
      - passed
    - `git diff --check`
      - passed
  - Tightened public contract manifest shape checks:
    - `runtime-error` tags now require `expect_success = false`;
    - `public-example` tags now require repo-root `examples/` fixtures;
    - `standard-api` tags now require a narrower API area tag such as
      `result`, `errors`, `path`, or `file`;
    - `tests/yulang/README.md` documents these tag shape rules.
  - Validation:
    - `timeout 240s cargo test -q -p yulang public_contract_manifest_cli_cases_hold --test cli -- --test-threads=1`
      - `1 passed`, finished in 155.58s
    - `cargo fmt --all -- --check`
      - passed
    - `git diff --check`
      - passed
  - Extended diagnostic manifest assertions with primary ranges:
    - the manifest schema now accepts `expect_diagnostic_start` and
      `expect_diagnostic_end`;
    - stable diagnostics for type mismatch, unresolved type/value names,
      unsupported type syntax, top-level mutable binding, rule lazy
      quantifier, and syntax errors now fix their structured primary byte
      range;
    - catch syntax diagnostics remain code/message-gated until their primary
      range is deliberately fixed.
  - Validation:
    - `timeout 240s cargo test -q -p yulang public_contract_manifest_cli_cases_hold --test cli -- --test-threads=1`
      - `1 passed`, finished in 157.71s
    - `cargo fmt --all -- --check`
      - passed
    - `git diff --check`
      - passed
  - Tightened release archive smoke around `yulang server`:
    - `scripts/release-archive-smoke.sh` now invokes `release-smoke.sh` with
      `YULANG_SMOKE_SERVER=1` by default;
    - `YULANG_RELEASE_ARCHIVE_SMOKE_SERVER=0` can disable only that archive
      server startup check for local archive inspection;
    - `README.md` documents the archive smoke server check alongside the
      hardening release server smoke.
  - Validation:
    - `bash -n scripts/release-archive-smoke.sh`
      - passed
    - `YULANG_SMOKE_SERVER=1 YULANG_SMOKE_SERVER_TIMEOUT=2s timeout 120s scripts/release-smoke.sh target/debug/yulang`
      - `release smoke ok: target/debug/yulang`
    - `git diff --check`
      - passed
    - not run:
      - a full `scripts/release-archive-smoke.sh <archive>` because this slice
        did not build a release archive.
  - Fixed catch syntax diagnostics to carry structured primary ranges:
    - missing catch scrutinee points at the `catch` keyword;
    - missing catch arm pattern and body point at the arm `->`;
    - the public contract manifest now asserts these ranges alongside the
      existing codes, messages, severity, and related-count checks.
  - Validation:
    - `cargo check -q -p infer -p yulang`
      - passed
    - `timeout 240s cargo test -q -p yulang public_diagnostics_check_reports_catch_missing --test cli -- --test-threads=1`
      - `3 passed`
    - `timeout 240s cargo test -q -p yulang public_contract_manifest_cli_cases_hold --test cli -- --test-threads=1`
      - `1 passed`, finished in 158.44s
    - `cargo fmt --all -- --check`
      - passed
    - `git diff --check`
      - passed
  - End-of-run validation snapshot:
    - `cargo fmt --all -- --check`
      - passed
    - `timeout 240s cargo test -q -p wasm check_inner_returns_ -- --test-threads=1`
      - `9 passed`, finished in 31.12s
    - `timeout 240s cargo test -q -p yulang server::tests::diagnostics_use_ -- --test-threads=1`
      - `9 passed`
    - `timeout 240s cargo test -q -p yulang public_contract_manifest_cli_cases_hold --test cli -- --test-threads=1`
      - `1 passed`, finished in 163.05s
    - `timeout 240s cargo test -q -p yulang public -- --test-threads=1`
      - package test filters passed: `9 passed` in lib-side filter and `16 passed` in cli-side filter
    - `timeout 300s cargo test -q -p yulang -- --test-threads=1`
      - timed out after the library suite passed `263 passed`; timeout happened while the CLI test binary was still running
    - `timeout 600s cargo test -q -p yulang --test cli -- --test-threads=1`
      - `98 passed`, finished in 269.22s
    - `YULANG_SMOKE_SERVER=1 YULANG_SMOKE_SERVER_TIMEOUT=2s timeout 120s scripts/release-smoke.sh target/debug/yulang`
      - `release smoke ok: target/debug/yulang`
    - `timeout 240s scripts/source-unit-cache-smoke.sh target/debug/yulang`
      - `source unit cache smoke ok: target/debug/yulang`
    - `cargo check -q -p yulang -p wasm`
      - passed
    - `timeout 300s cargo check -q --release -p yulang -p wasm`
      - passed
    - `timeout 600s tests/yulang/yulang-adversarial-corpus/probe.sh`
      - all adversarial probes passed
  - Synced `docs/status.md` with the current public contract spine:
    - exact public-signature manifest expectations;
    - structured `check` diagnostic manifest requirements;
    - `std::data::result` error propagation coverage;
    - the remaining role/method `SourceDiagnostic` gap.
  - Tightened manifest tag spelling:
    - every contract tag must be lowercase ASCII with digits, `.`, or `-`;
    - `tests/yulang/README.md` documents the canonical tag shape.
  - Validation:
    - `timeout 240s cargo test -q -p yulang public_contract_manifest_cli_cases_hold --test cli -- --test-threads=1`
      - `1 passed`, finished in 160.92s

## Resource lifetime spec alignment

- Context:
  - Claude (Fable 5) and the user fixed the file/server resource lifetime
    decisions in `notes/design/2026-07-02-resource-lifetime-decisions.md`.
  - The file and server API specs still contained older wording around value
    liveness, scope-exit lock release, and unspecified multi-shot `accept`.
- Implementation summary:
  - Updated `spec/2026-07-01-file-resource-api.md` to make handler extent, not
    value liveness or GC reachability, the unscoped resource lifetime boundary.
  - Documented managed lens as a snapshot transaction: scope-entry read,
    branch-local edits, commit only on normal scope exit, abort as rollback,
    and multi-shot branch commits with last-write-wins ordering.
  - Clarified that first-slice lock release may stay tied to filesystem handler
    discharge until evidence VM continuation-drop hooks exist.
  - Updated `spec/2026-07-02-server-resource-api.md` to reject `server.accept`
    inside multi-shot resume regions until a distinct server multi-shot
    capability is designed.
  - Added the file/server asymmetry: file resources can be transactional under
    multi-shot continuation, while server responses are one-shot external
    capabilities.
  - Propagated the same resource lifetime rules into
    `spec/2026-07-01-stable-standard-api.md` and clarified `docs/status.md`
    so the implementation gap is described as executable lowering/locking, not
    an unsettled meaning.
- Validation:
  - `git diff --check`
    - passed

## Host act FFI decision record

- Context:
  - Claude (Fable 5) and the user fixed the first-layer FFI direction as
    host-provided act handlers rather than direct native ABI exposure.
  - The new decision amends the earlier server `accept` multi-shot enforcement:
    `accept` itself is a suspend multi-shot host operation, while user-level
    suspend operations inside multi-shot regions are left to cheap dynamic
    checks or documented unspecified behavior.
- Implementation summary:
  - Added `notes/design/2026-07-02-host-act-ffi-decisions.md` as the
    authoritative host act FFI decision record.
  - Updated `notes/design/2026-07-02-resource-lifetime-decisions.md` so its
    server `accept` decision points at the FFI document's three-layer
    enforcement rule instead of the older typed-failure wording.
  - Aligned `spec/2026-07-02-server-resource-api.md` and
    `spec/2026-07-01-stable-standard-api.md` with the new operation-tier
    terminology: `server.accept` is suspend multi-shot, request response is
    one-shot, and user-level suspend/multi-shot composition remains outside the
    stable guarantee.
- Validation:
  - `git diff --check`
    - passed

## Contract v0 completion evidence and static proof direction

- Context:
  - External review kept repeating the same generic "make Contract v0 real"
    recommendation even after the manifest, diagnostics, public signatures, and
    release gates landed.
  - Claude (Fable 5) separately argued that the current Evidence VM proof
    system is correct but too dynamic: route/cert checks that Koka-style
    evidence passing would discharge statically are still replayed at runtime.
- Implementation summary:
  - Added `docs/language/contract-v0-evidence.md` as the evidence page for the
    current Contract v0 spine. It records that `stable-core` now has 51
    executable manifest cases: 24 `run`, 12 `check`, and 15
    `public-signature`.
  - Linked the evidence page from `docs/language/stable-core.md` and
    `docs/status.md`, and made the reviewer rule explicit: future requests to
    "finish Contract v0" must name a missing fixture, public signature,
    structured diagnostic, or release gate.
  - Recorded that file transactions, server resources, and host act FFI are
    next contract slices, not missing Contract v0 evidence.
  - Added Claude's `notes/design/2026-07-02-speedup-proof-system.md` and
    `notes/design/2026-07-02-static-route-promotion-plan.md`.
  - Updated `tasks/current.md` and `docs/status.md` so future Evidence VM
    performance work starts with the static route promotion Stage 0 coverage
    pass instead of adding more signal-by-signal dynamic cert checks.
- Validation:
  - `timeout 240s cargo run -q -p yulang -- --std-root lib contract --contract stable-core tests/yulang/cases.toml`
    - `contract cases ok: 51`
  - `git diff --check`
    - passed

## IO resource API specification

- Context:
  - Claude (Fable 5) and the user promoted the file / connect / serve API
    sketch into an authoritative IO resource API specification.
  - The spec derives the API surface from the resource lifetime and host act
    FFI decisions: `host act + session + managed view + raw`.
- Implementation summary:
  - Added `spec/2026-07-02-io-resource-api.md` as the current authoritative
    standard IO resource API spec.
  - Kept `notes/design/2026-07-02-io-api-sketch.md` as the discussion record
    and pointed it at the promoted spec.
  - Updated file/server resource specs, the stable standard API spec,
    `spec/README.md`, and `docs/status.md` so the integrated IO spec takes
    precedence where surfaces conflict.
  - Recorded the key accepted surface decisions:
    - `file::meta` does not fail; missing / denied are metadata `kind` values;
    - file managed lens remains a snapshot transaction;
    - connect can be used as a value, an ambient channel handler, or a chain;
    - serve has a first-class listener, concurrent `accept`, sequential
      `requests`, and a one-line `net::serve` spelling;
    - close / disconnect / flush / save remain outside the managed API.
- Validation:
  - `git diff --check`
    - passed

## Claude follow-up TODO tracks

- Context:
  - Claude (Fable 5) produced a final outside assessment and several follow-up
    TODO tracks after the IO resource API decision.
  - The user approved recording those TODOs so future implementation can follow
    them instead of losing the design pressure.
- Implementation summary:
  - Added `notes/design/2026-07-02-parting-assessment.md` as the source
    assessment.
  - Added focused TODO notes for:
    - deterministic record/replay;
    - structured concurrency and cancellation;
    - conservative `case` exhaustiveness;
    - deriving;
    - property testing on top of `undet`;
    - effect-aware error traces;
    - deterministic parallelism as a horizon track.
  - Updated `notes/todo/index.md` to point at the new tracks and call out the
    two time-sensitive ones: record/replay identifiers before the host act
    registry is fixed, and cancellation semantics before serve implementation.
  - Extended the IO resource spec open questions with the large-file / stream
    view issue from the assessment.
- Validation:
  - `git diff --check`
    - passed

## Contract v1 priority memo

- Context:
  - ChatGPT Pro finally framed the next work as a priority order instead of
    reopening Contract v0.
  - The user approved recording it as the implementation priority memo.
- Implementation summary:
  - Added `notes/todo/contract-v1-file-resource-priority.md`.
  - Recorded Contract v1 as File / Host Resource Contract:
    - close the file resource contract first;
    - then host act FFI registry;
    - then diagnostics/LSP parity;
    - then release artifact contract;
    - then server in-process driver;
    - keep static route work to Stage 0 until its own gate allows Stage 1.
  - Updated `notes/todo/index.md` and `tasks/current.md` so the next contract
    slice is visible from the active task context.
- Validation:
  - `git diff --check`
    - passed

## Contract v1 file resource box

- Context:
  - Work resumed at 12:09 JST with a clean `main` worktree tracking
    `origin/main`.
  - The user asked for continuous work until at least 19:00 JST, with small
    commits and pushes.
  - The next approved priority is Contract v1: File / Host Resource Contract.
- Implementation summary:
  - Added `docs/language/file-resource-contract.md` as the Contract v1 entry:
    files are durable `&` variables, managed lenses are snapshot transactions,
    and mock/native/unsupported host behavior must share one resource model.
  - Added `docs/language/contract-v1-file-evidence.md` as the evidence ledger.
    It explicitly leaves the `file-resource` subset open until executable cases
    prove mock commit/rollback/multi-shot behavior, native parity, unsupported
    host failure, public signatures, and release binary parity.
  - Updated `docs/status.md` so Public Contract Spine names File Resource
    Contract as the next slice instead of reopening Contract v0.
  - Updated `tests/yulang/README.md` with the Contract v1 tag policy:
    `file-resource`, `resource-lifetime`, `mock-host`, `host.native`, and
    `host.unsupported`.
  - Added `file-resource`, `resource-lifetime`, and `mock-host` to the known
    manifest tag taxonomy in the CLI manifest test.
- Validation:
  - `cargo fmt --all -- --check`
    - passed
  - `git diff --check`
    - passed
  - `timeout 240s cargo test -q -p yulang --test cli public_contract_manifest -- --test-threads=1`
    - `2 passed`, finished in 173.11s

## Native file scoped snapshot first slice

- Context:
  - After the Contract v1 box landed, implementation moved to the first file
    resource slice.
  - A pure mock handler was investigated first. A separate private helper act
    leaks into public callback types. Reusing the public `file` act keeps public
    types clean, but a partial `catch` over only buffer operations can hide the
    whole `file` effect at the type level while other file operations still
    escape at runtime. That is unsafe as a Contract v1 foundation.
- Implementation summary:
  - Kept direct `open_text` / `text` on the existing immediate native file
    handle path.
  - Added private native snapshot operations for scoped file text:
    - `open_text_snapshot_raw`
    - `file_snapshot_get`
    - `file_snapshot_set`
    - `file_snapshot_commit`
  - Changed `open_in` / `text_with` to open a snapshot handle, pass a managed
    ref to the callback, then commit after the callback returns normally.
  - Updated the native scoped CLI regressions so they now check:
    - callback buffer sees the edited value;
    - `read_text` inside the callback still sees the old backing file;
    - `read_text` after the callback sees the committed value.
  - Updated public-signature canaries for `open_in` and `text_with`; the public
    row keeps `file` / `io_err` on the scoped operation instead of weakening the
    standard API canary to the old immediate-write shape.
- Blockers found:
  - Dirty rollback and nondet multi-shot commit fixtures are not executable yet.
    A callback that touches the provided file ref and then escapes through an
    outer error or nondet handler currently reports:
    - `[std::io::file::file, edit_err] vs [edit_err]`
    - `[std::control::nondet::nondet, std::io::file::file] vs [std::control::nondet::nondet]`
  - Plain file residuals under `edit_err::wrap` and plain file residuals under
    `.list` work; the gap is specific to the provided ref's `file` effect under
    the outer handler.
  - The native snapshot map is runner-local, not branch-local, so it is only
    evidence for normal scope-exit commit. It must not be counted as
    multi-shot Contract v1 evidence.
- Validation:
  - `cargo fmt --all -- --check`
    - passed
  - `git diff --check`
    - passed
  - `timeout 240s cargo check -q -p evidence-vm -p yulang`
    - passed
  - `timeout 240s cargo test -q -p yulang --test cli compatible_run_std_file_open_text_ref_host_contract -- --test-threads=1`
    - `1 passed`
  - `timeout 240s cargo test -q -p yulang --test cli compatible_run_std_file_text_shorthand_host_contract -- --test-threads=1`
    - `1 passed`
  - `timeout 240s cargo test -q -p yulang --test cli public_contract_manifest -- --test-threads=1`
    - `2 passed`, finished in 174.90s

## File-resource manifest temp files

- Context:
  - The first native scoped commit behavior was still owned by Rust CLI tests.
  - Contract v1 needs executable manifest evidence, including host.native temp
    files, without hard-coded filesystem paths.
- Implementation summary:
  - Extended the Rust manifest test helper and the real `yulang contract` CLI
    with `[[case.temp_files]]` support:
    - each entry creates a temporary file;
    - the source placeholder is replaced with a Yulang string literal for that
      path;
    - `expect_contents` checks the final file text after the run.
  - Added `tests/yulang/regressions/runtime/file_text_with_commit.yu`.
  - Added `file_text_with_commit` to `tests/yulang/cases.toml` as the first
    `file-resource` / `resource-lifetime` / `host.native` case. It verifies:
    - callback buffer updates;
    - backing file reads inside the callback still see the old text;
    - scope exit commits the new text;
    - the temp file contents match the committed text after the CLI run.
  - Tightened manifest schema checks so `file-resource` runtime cases declare
    exactly one host scope and the `resource-lifetime` tag.
  - Updated the Contract v1 evidence page and status page so they no longer
    claim the `file-resource` subset is empty.
- Validation:
  - `cargo fmt --all -- --check`
    - passed
  - `git diff --check`
    - passed
  - `timeout 240s cargo check -q -p yulang`
    - passed
  - `cargo run -q -p yulang -- --std-root lib contract --case file_text_with_commit tests/yulang/cases.toml`
    - `contract cases ok: 1`
  - `cargo run -q -p yulang -- --std-root lib contract --contract file-resource tests/yulang/cases.toml`
    - `contract cases ok: 1`
  - `timeout 240s cargo test -q -p yulang --test cli public_contract_manifest_cli_cases_hold -- --test-threads=1`
    - `1 passed`, finished in 177.51s

## File-resource release gate representative

- Context:
  - The first `file-resource` manifest case was executable, but hardening and
    archive smoke still only exercised representative `stable-core` contract
    cases through the release binary surface.
- Implementation summary:
  - Added `file_text_with_commit` to the `scripts/hardening-smoke.sh` contract
    runner smoke with `--contract file-resource`.
  - Added the same representative case to `scripts/release-archive-smoke.sh`
    so packaged binaries and bundled std roots exercise the current Contract v1
    file-resource slice.
  - Updated release TODO and Contract v1 evidence docs to state the limited
    scope: this is evidence for normal native scope-exit commit only, not for
    rollback, multi-shot branch buffers, mock-host parity, or unsupported-host
    behavior.
- Validation:
  - `bash -n scripts/hardening-smoke.sh scripts/release-archive-smoke.sh`
    - passed
  - `git diff --check`
    - passed
  - `cargo fmt --all -- --check`
    - passed
  - `timeout 300s cargo build -q -p yulang --release`
    - passed
  - `timeout 240s target/release/yulang --std-root lib contract --contract file-resource --case file_text_with_commit tests/yulang/cases.toml`
    - `contract cases ok: 1`

## Static route Stage 0 first slice

- Context:
  - `notes/design/2026-07-02-static-route-promotion-plan.md` is authoritative.
    Stage 0 must not change runtime semantics; it only measures static route
    coverage and runtime hits.
  - The first slice keeps execution unchanged and records counters from the
    existing evidence VM plan, where handler arm classification already lives.
- Implementation summary:
  - Added static-route classification to `EvidenceVmOperationObjectPlan`.
  - Added plan counters:
    - `static_route_sites_total`
    - `static_route_static_handler`
    - `static_route_static_tail_resumptive`
    - `static_route_static_abortive`
    - `static_route_static_other_arm`
    - `static_route_dynamic_open_row`
    - `static_route_dynamic_multiple_candidates`
    - `static_route_dynamic_hygiene_barrier`
    - `static_route_dynamic_provider_env`
    - `static_route_dynamic_delayed_boundary`
    - `static_route_dynamic_host_escape`
    - `static_route_dynamic_unclassified`
  - Added deep-profile/debug runtime hit counters:
    - `static_route_runtime_hits_static_tail`
    - `static_route_runtime_hits_static_other`
    - `static_route_runtime_hits_dynamic_*`
  - Runtime hit writes are gated by `runtime_breakdown_stats_enabled`; normal
    release runs do not update the new runtime hit counters unless deep profile
    is active.
  - Added focused CLI assertions for direct tail-resumptive and abortive handler
    canaries.
- Counter observations:

| workload | sites | static tail sites | static other sites | dynamic host-escape sites | static tail hits | static other hits | dynamic host-escape hits | compare-control |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | --- |
| `bench/nondet_20_discard.yu` | 3 | 0 | 0 | 3 | 0 | 0 | 861 | match |
| `examples/showcase.yu` | 13 | 0 | 0 | 13 | 0 | 0 | 2464 | match |
| `examples/03_for_last.yu` | 7 | 0 | 0 | 7 | 0 | 0 | 2 | not run in compare mode |
| `examples/02_refs.yu` | 8 | 0 | 0 | 8 | 0 | 0 | 10 | not run in compare mode |

- Decision:
  - Stop before Stage 1. The representative workloads have no static tail
    runtime hits, so they do not satisfy the Stage 0 decision table's "Stage 1"
    condition.
  - The direct-handler canaries prove the counters can observe static
    tail-resumptive and abortive shapes; public std/nondet workloads are still
    classified as dynamic host escape by this first slice.
- Validation:
  - `cargo fmt --all -- --check`
    - passed
  - `git diff --check`
    - passed
  - `timeout 240s cargo check -q -p evidence-vm -p yulang`
    - passed
  - `timeout 300s cargo check -q --release -p evidence-vm -p yulang`
    - passed
  - `timeout 240s cargo test -q -p evidence-vm -- --test-threads=1`
    - `83 passed`
  - `timeout 240s cargo test -q -p yulang runtime_evidence -- --test-threads=1`
    - `30 passed`
  - `timeout 240s cargo test -q -p yulang --test cli debug_runtime_evidence_run_uses_direct_handler_evidence -- --test-threads=1`
    - `1 passed`
  - `timeout 240s cargo test -q -p yulang --test cli debug_runtime_evidence_run_classifies_unused_continuation_as_direct_abortive -- --test-threads=1`
    - `1 passed`
  - `timeout 240s target/debug/yulang --std-root lib debug evidence-vm-run --compare-control --runtime-evidence-profile-deep bench/nondet_20_discard.yu`
    - `compare.control: match`
  - `timeout 240s target/debug/yulang --std-root lib debug evidence-vm-run --compare-control --runtime-evidence-profile-deep examples/showcase.yu`
    - `compare.control: match`

## Runtime host operation table first slice

- Context:
  - `notes/design/2026-07-02-host-act-ffi-decisions.md` makes the eventual
    direction explicit: host operations should be declared, registered, and
    dispatched through structured host act metadata rather than scattered
    perform-time name checks.
  - This slice does not implement the manifest / registry system yet. It only
    moves the current native console and file request dispatch into one
    runtime operation table, preserving behavior.
- Implementation summary:
  - Added `RuntimeHostOperation` and `RUNTIME_HOST_OPERATIONS` in
    `crates/evidence-vm/src/runtime.rs`.
  - Replaced the inline console/file path chain in escaped request handling
    with a single table lookup followed by a typed operation match.
  - Kept every existing host operation and fallback unchanged:
    - console `out.write`;
    - file `read_at` / `write_at`;
    - immediate text refs;
    - snapshot text refs;
    - metadata predicates.
- Non-goals:
  - No language syntax change.
  - No host act manifest generation.
  - No capability failure conversion yet; unknown operations still fall back to
    the existing escaped-effect path.
  - No removal of path matching at runtime yet. The matching is only
    centralized so the next registry slice has one seam to replace.
- Validation:
  - `cargo fmt --all -- --check`
    - passed
  - `git diff --check`
    - passed
  - `timeout 240s cargo check -q -p evidence-vm -p yulang`
    - passed
  - `timeout 240s cargo test -q -p evidence-vm -- --test-threads=1`
    - `83 passed`
  - `timeout 240s cargo test -q -p yulang --test cli debug_runtime_evidence_run_handles_console_stdout_host_effect -- --test-threads=1`
    - `1 passed`
  - `timeout 240s cargo test -q -p yulang --test cli compatible_run_std_file_text_shorthand_host_contract -- --test-threads=1`
    - `1 passed`
  - `timeout 240s cargo run -q -p yulang -- --std-root lib contract --contract file-resource --case file_text_with_commit tests/yulang/cases.toml`
    - `contract cases ok: 1`

## Runtime host operation act/tier metadata

- Context:
  - `notes/design/2026-07-02-host-act-ffi-decisions.md` requires host act
    dispatch to move toward manifest / registry metadata rather than
    perform-time path checks.
  - The previous slice centralized current host operations in one table but
    still only carried the operation enum and full path.
- Implementation summary:
  - Added `RuntimeHostAct` and `RuntimeHostOperationTier` metadata to each
    current runtime host operation spec.
  - Kept every current operation as `Sync`; no suspend tier, scheduler queue, or
    capability failure behavior was added in this slice.
  - Added debug-only lookup invariants so the runtime checks act prefix,
    operation id, and sync tier in development builds without adding release
    hot-path writes.
  - Added unit canaries for act/tier metadata and console/file act separation.
  - Updated Contract v1 file evidence docs to record this as host act registry
    prework, not final registry support.
- Validation:
  - `cargo fmt --all -- --check`
    - passed
  - `timeout 240s cargo check -q -p evidence-vm -p yulang`
    - passed
  - `timeout 240s cargo test -q -p evidence-vm runtime_host_operation_table -- --test-threads=1`
    - `4 passed`
  - `timeout 240s cargo run -q -p yulang -- --std-root lib contract --contract file-resource --case file_text_with_commit tests/yulang/cases.toml`
    - `contract cases ok: 1`

## Runtime unsupported host capability gate

- Context:
  - Contract v1 needs unsupported hosts to fail explicitly rather than faking
    success or degrading into a generic escaped-effect crash.
  - A public `host.unsupported` manifest route is not available yet, but the
    runtime can distinguish "known host act not granted" from "ordinary
    unhandled effect".
- Implementation summary:
  - Added a native-host-operation grant gate to `RuntimeEvidenceRunContext`.
    The default context still grants current native host operations.
  - Added `RuntimeEvidenceRunError::UnsupportedHostCapability`.
  - Routed known console/file host operation requests through that error when
    native host operations are disabled in a test context.
  - Added CLI formatting for the new Evidence VM runtime error code:
    `yulang.unsupported-host-capability`.
  - Added a unit canary proving a known file host request becomes
    `UnsupportedHostCapability(std::io::file::file)` when native host
    operations are disabled.
  - Updated Contract v1 file evidence docs to mark this as an internal runtime
    gate, not the final public unsupported-host contract.
- Validation:
  - `cargo fmt --all -- --check`
    - passed
  - `timeout 240s cargo check -q -p evidence-vm -p yulang`
    - passed
  - `timeout 240s cargo test -q -p evidence-vm runtime_host_operation -- --test-threads=1`
    - `5 passed`

## Public unsupported-host file contract case

- Context:
  - The previous unsupported-host slice only proved the runtime gate from a unit
    test. Contract v1 needs the same behavior through the public CLI and
    manifest route.
- Implementation summary:
  - Added `run --host <native|unsupported>` for Evidence VM runs. `native`
    remains the default; `unsupported` disables native host operations and
    routes known console/file host requests to
    `yulang.unsupported-host-capability`.
  - Kept `--host unsupported` rejected for mono/control VM routes and
    control-VM artifacts.
  - Added manifest field `host = "unsupported"` and schema checks tying it to
    the `host.unsupported` tag.
  - Added `tests/yulang/regressions/runtime/file_unsupported_host.yu`.
  - Added `file_unsupported_host` to `tests/yulang/cases.toml` as a
    `file-resource` / `host.unsupported` runtime-failure case.
  - Updated the manifest README and Contract v1 file evidence docs.
- Validation:
  - `cargo fmt --all -- --check`
    - passed
  - `timeout 240s cargo check -q -p evidence-vm -p yulang`
    - passed
  - `timeout 240s cargo test -q -p yulang --test cli compatible_run_std_file_unsupported_host_reports_capability_error -- --test-threads=1`
    - `1 passed`
  - `timeout 240s cargo test -q -p evidence-vm runtime_host_operation -- --test-threads=1`
    - `5 passed`
  - `timeout 240s cargo run -q -p yulang -- --std-root lib contract --case file_unsupported_host tests/yulang/cases.toml`
    - `contract cases ok: 1`

## Native text_with rollback on user error

- Context:
  - Contract v1 file-resource rollback was blocked by a specialize2 conflict
    when a `text_with` callback read the managed ref and then escaped through a
    user error caught by `E::wrap`.
  - Direct `open_in` with the same callback already worked, which narrowed the
    problem away from the native host bridge and away from generated
    `E::wrap`.
- Root cause:
  - `text_with` was a thin wrapper function:
    `text_with(path, f) = open_in path f`.
  - Passing the callback function value through that extra wrapper split the
    callback's file residual from the outer wrapped error residual during
    specialization.
  - Making `text_with` a direct alias of `open_in` preserves the same
    implementation identity for both spellings and avoids the residual conflict.
- Implementation summary:
  - Changed `std::io::file::text_with` to `pub text_with = open_in`.
  - Added `file_text_with_rollback_on_error` to the contract manifest.
  - The fixture reads and updates the managed text ref, throws a user error
    caught by `edit_err::wrap`, and verifies the temp backing file remains
    unchanged.
  - Updated Contract v1 evidence docs and the historical bug note.
- Validation:
  - Probe: `text_with` + `$text` + `edit_err::wrap`
    - `run roots [result::err(edit_err::abort)]`
  - Probe: rollback with temp file
    - `run roots [("start", result::err(edit_err::abort), "start")]`
    - backing file stayed `start`
  - `timeout 240s cargo run -q -p yulang -- --no-cache --std-root lib contract --case file_text_with_commit tests/yulang/cases.toml`
    - `contract cases ok: 1`
  - `timeout 240s cargo run -q -p yulang -- --no-cache --std-root lib contract --case file_text_with_rollback_on_error tests/yulang/cases.toml`
    - `contract cases ok: 1`
  - `timeout 240s cargo run -q -p yulang -- --no-cache --std-root lib contract --contract file-resource tests/yulang/cases.toml`
    - `contract cases ok: 3`
  - `timeout 240s cargo run -q -p yulang -- --no-cache --std-root lib contract --case std_file_text_with_public_signature tests/yulang/cases.toml`
    - `contract cases ok: 1`
  - `timeout 240s cargo check -q -p yulang`
    - passed
  - `cargo fmt --all -- --check`
    - passed
  - `timeout 240s cargo test -q -p yulang --test cli public_contract_manifest -- --test-threads=1`
    - `2 passed`

## Release binary file-resource parity check

- Context:
  - After adding rollback and multi-shot native file-resource cases, the local
    release binary needed to prove that installed-performance users see the same
    file-resource subset as debug/development runs.
- Validation:
  - `timeout 300s cargo build -q --release -p yulang`
    - passed
  - `timeout 240s target/release/yulang --std-root lib contract --contract file-resource tests/yulang/cases.toml`
    - `contract cases ok: 4`
  - `timeout 240s target/release/yulang --std-root lib contract --case file_text_with_nondet_branch_snapshots tests/yulang/cases.toml`
    - `contract cases ok: 1`
  - `timeout 240s target/release/yulang --std-root lib contract --case file_text_with_rollback_on_error tests/yulang/cases.toml`
    - `contract cases ok: 1`

## Pure mock text_with lifetime blocker reduction

- Context:
  - The next Contract v1 file-resource slice is pure mock resource-lifetime
    parity, but the current `text_with` implementation is still built on
    private native snapshot helper operations.
- Findings:
  - Added `notes/bugs/file_text_with_mock_resource_lifetime_blocker.yu`.
  - A source handler outside `std::io::file` cannot catch the private
    `open_text_snapshot_raw` / `file_snapshot_get` / `file_snapshot_set` /
    `file_snapshot_commit` operations as a mock resource-lifetime host.
  - A public-only rewrite using `read_text` / `write_text` plus a local `$text`
    ref was probed separately and currently hits a `ref_update` / local var
    residual conflict when the constructed ref escapes to the callback.
  - Do not make the raw snapshot helpers public just to satisfy the mock case;
    the proper next fix is a mockable public host-act/session boundary or a
    residual-correct language-level local ref construction.
- Validation:
  - `cargo run -q -p yulang -- --std-root lib run --host unsupported --print-roots notes/bugs/file_text_with_mock_resource_lifetime_blocker.yu`
    - reports `runtime error [yulang.unsupported-host-capability]`

## Native text_with nondet branch-local blocker

- Context:
  - After `text_with` became a direct alias of `open_in`, the nondet
    interaction no longer failed during specialization.
  - Contract v1 still requires multi-shot branches to carry independent managed
    buffers before committing in arrival order.
- Probe result:
  - With a backing file containing `start`, this program:
    - branches over suffixes `" A"` and `" B"`;
    - reads `$text`;
    - updates `&text`;
    - collects the results with `.list`.
  - Current output:
    - `run roots [([(" A", "start A"), (" B", "start A B")], "start A B")]`
    - backing file: `start A B`
  - Desired Contract v1 shape:
    - each branch reads `start` on entry;
    - branch results are `(" A", "start A")` and `(" B", "start B")`;
    - final file is last-write-wins, likely `start B` for current traversal.
- Decision:
  - Added `notes/bugs/file_text_with_nondet_shared_snapshot.yu`.
  - Do not add the current behavior to `tests/yulang/cases.toml`.
  - The remaining issue is runtime state placement:
    `RuntimeEvidenceRunner::file_snapshots` is shared by resumed branches.
  - `timeout 240s cargo test -q -p yulang --test cli public_contract_manifest -- --test-threads=1`
    - `2 passed`
  - `timeout 240s cargo run -q -p yulang -- --std-root lib contract --contract file-resource tests/yulang/cases.toml`
    - `contract cases ok: 2`
  - `timeout 240s cargo test -q -p yulang --test cli contract_command -- --test-threads=1`
    - `6 passed`
  - `timeout 240s cargo test -q -p yulang --test cli public_contract_manifest_covers_status_spine_claims -- --test-threads=1`
    - `1 passed`
  - `timeout 240s cargo test -q -p yulang public_contract_manifest_cli_cases_hold -- --test-threads=1`
    - `1 passed`
  - `git diff --check`
    - passed

## Release smoke runs the file-resource subset

- Context:
  - Before `file_unsupported_host`, release / archive smoke only ran
    `file_text_with_commit` as the representative Contract v1 file-resource
    case.
  - The file-resource subset now has two executable cases: native normal
    scope-exit commit and unsupported-host runtime failure.
- Implementation summary:
  - Updated `scripts/hardening-smoke.sh` and
    `scripts/release-archive-smoke.sh` to run `--contract file-resource`
    without narrowing to `--case file_text_with_commit`.
  - Updated release TODO and Contract v1 file evidence docs.
- Validation:
  - `bash -n scripts/hardening-smoke.sh scripts/release-archive-smoke.sh`
    - passed
  - `timeout 300s cargo build -q -p yulang --release`
    - passed
  - `timeout 240s target/release/yulang --std-root lib contract --contract file-resource tests/yulang/cases.toml`
    - `contract cases ok: 2`
  - `timeout 240s target/release/yulang --std-root lib contract --contract stable-core --case optional_record_defaults tests/yulang/cases.toml`
    - `contract cases ok: 1`
  - `git diff --check`
    - passed

## File-resource rollback fixture probe

- Context:
  - Contract v1 requires `text_with` rollback when the callback exits through an
    effect before normal scope exit.
- Probe:
  - A minimal native fixture shape was tested locally:
    - read initial file text;
    - enter `std::io::file::text_with`;
    - update the managed ref buffer;
    - `fail edit_err::abort`;
    - close the error with `edit_err::wrap` outside the `text_with` call;
    - read the backing file again.
- Result:
  - `check` succeeds with quoted temp-file substitution.
  - mono specialization fails with:
    `conflicting type candidates for slot 0: [std::io::file::file, edit_err] vs [edit_err]`
- Decision:
  - Do not add the fixture to `tests/yulang/cases.toml` yet. The manifest must
    stay executable and should not carry expected-failure placeholders.
  - Do not add a broad effect-row exception in specialization. The failure is
    narrower: generated error wrapping and `text_with` callback residuals are
    valid at the public type boundary, but specialize still collapses the
    callback file residual against the wrapped error residual in this shape.
  - The next fix belongs near specialize2's existing effect-subtraction /
    callback-residual handling, not in the file API wrapper.

## Runtime host operation table canaries

- Context:
  - The first host operation table slice needed direct tests before the table is
    used as the replacement point for future host act registry dispatch.
- Implementation summary:
  - Added unit coverage that every current console/file operation spec resolves
    through `runtime_host_operation`.
  - Added a duplicate-path canary for `RUNTIME_HOST_OPERATIONS`.
- Validation:
  - `cargo fmt --all -- --check`
    - passed
  - `timeout 240s cargo test -q -p evidence-vm runtime_host_operation_table -- --test-threads=1`
    - `2 passed`

## Contract manifest schema validation in the CLI

- Context:
  - The public contract manifest policy had Rust-side checks in
    `crates/yulang/tests/cli.rs`, but a packaged `yulang contract` binary could
    still start from a malformed manifest shape.
  - Contract v0 / v1 evidence should fail at the release CLI boundary too, not
    only in repository tests.
- Implementation summary:
  - Added manifest validation to `crates/yulang/src/contract.rs` immediately
    after TOML loading and before case / contract filters are applied.
  - The CLI now rejects:
    - duplicate case names;
    - non-canonical or unknown tags;
    - kind/tag mismatches;
    - `stable-core` cases mixed with preview / migration canary /
      compile-error tags;
    - malformed `runtime-error` tag combinations;
    - `standard-api` cases without exactly one stability tag and a narrower API
      area tag;
    - `file-resource` runtime cases without exactly one host scope and
      `resource-lifetime`;
    - public-signature cases without `expect_type`;
    - check cases without compact diagnostic code / severity / primary range /
      related count expectations.
  - Added CLI tests for unknown manifest tags and file-resource runtime cases
    missing host scope.
- Validation:
  - `cargo fmt --all -- --check`
    - passed
  - `timeout 240s cargo test -q -p yulang --test cli contract_command -- --test-threads=1`
    - `6 passed`
  - `timeout 240s cargo run -q -p yulang -- --std-root lib contract --contract file-resource --case file_text_with_commit tests/yulang/cases.toml`
    - `contract cases ok: 1`
  - `timeout 240s cargo test -q -p yulang public_contract_manifest_cli_cases_hold -- --test-threads=1`
    - `1 passed`
  - `git diff --check`
    - passed

## Runtime host operation module split

- Context:
  - Host act FFI / registry work needs a small replacement point for the
    current native host operation table.
  - Keeping operation metadata inside the large Evidence VM runtime obscured
    the boundary between request recognition and operation execution.
- Implementation summary:
  - Moved the current console/file host operation table, act metadata, sync tier
    metadata, path matcher, and table canaries into
    `crates/evidence-vm/src/runtime/host.rs`.
  - Left operation execution in the existing `RuntimeEvidenceRunner` path.
  - No runtime semantics changed; the runner still falls back through the same
    escaped request / unsupported capability behavior.
- Validation:
  - `cargo fmt --all -- --check`
    - passed
  - `timeout 240s cargo check -q -p evidence-vm -p yulang`
    - passed
  - `timeout 240s cargo test -q -p evidence-vm runtime_host_operation -- --test-threads=1`
    - `5 passed`
  - `timeout 240s cargo run -q -p yulang -- --std-root lib contract --contract file-resource tests/yulang/cases.toml`
    - `contract cases ok: 2`
  - `git diff --check`
    - passed

## Runtime host registry responsibility split

- Context:
  - Host act FFI decision F3 says capability-layer failures should be raised by
    the registry before host operation code runs.
  - The previous Evidence VM slice still detected disabled native host
    operations in `RuntimeEvidenceRunner::handle_escaped_request`.
- Implementation summary:
  - Added a small `RuntimeHostRegistry` around the current static console/file
    operation table.
  - `RuntimeHostRegistry::resolve` now returns either an operation or an
    unsupported-capability resolution for known host acts when native host
    operations are disabled.
  - `RuntimeEvidenceRunner` now owns the registry and only executes resolved
    operations or turns registry capability failures into the existing
    `UnsupportedHostCapability` runtime error.
  - Unknown effects still fall through to `EscapedEffect`; no public runtime
    semantics changed.
- Validation:
  - `cargo fmt --all -- --check`
    - passed
  - `timeout 240s cargo check -q -p evidence-vm -p yulang`
    - passed
  - `timeout 240s cargo test -q -p evidence-vm runtime_host -- --test-threads=1`
    - `7 passed`
  - `timeout 240s cargo run -q -p yulang -- --std-root lib contract --case file_unsupported_host tests/yulang/cases.toml`
    - `contract cases ok: 1`
  - `timeout 240s cargo run -q -p yulang -- --std-root lib contract --contract file-resource tests/yulang/cases.toml`
    - `contract cases ok: 2`

## Runtime host registry carries operation specs

- Context:
  - Host act FFI decision F2 requires host dispatch to keep the act/op identity
    resolved by the registry, instead of dropping back to anonymous operation
    enums after path recognition.
  - This is still a bridge over the current escaped request path, not the final
    lowering-produced manifest route.
- Implementation summary:
  - `RuntimeHostRegistry::resolve` now returns the resolved
    `RuntimeHostOperationSpec` for executable host operations.
  - `RuntimeEvidenceRunner` passes that spec into host operation execution.
  - Invalid-handle / malformed-payload errors for current console/file
    operations now use the spec's path metadata rather than hardcoded operation
    path vectors in `runtime.rs`.
  - Added a registry canary that enabled host dispatch resolves to the operation
    spec and preserves the current path metadata.
- Validation:
  - `cargo fmt --all -- --check`
    - passed
  - `timeout 240s cargo check -q -p evidence-vm -p yulang`
    - passed
  - `timeout 240s cargo test -q -p evidence-vm runtime_host -- --test-threads=1`
    - `8 passed`
  - `timeout 240s cargo run -q -p yulang -- --std-root lib contract --contract file-resource tests/yulang/cases.toml`
    - `contract cases ok: 2`

## Runtime host registry distinguishes known-act missing ops

- Context:
  - Host act FFI decision F1/F2 says a host act operation that reaches the host
    registry but has no registered host implementation is a capability-layer
    failure, not a generic escaped-effect crash.
  - Unknown user effects should still remain unresolved and use the existing
    escaped-effect path.
- Implementation summary:
  - Added a small known-host-act prefix table to the runtime host registry.
  - Exact operation paths still resolve to the operation spec.
  - Unknown operation names under a known host act now resolve to
    `UnsupportedCapability` for that act.
  - Paths outside the known host act table still return `None` from the
    registry and fall through to `EscapedEffect`.
  - Added focused unit coverage for the known-act / unknown-effect split.
- Validation:
  - `cargo fmt --all -- --check`
    - passed
  - `timeout 240s cargo check -q -p evidence-vm -p yulang`
    - passed
  - `timeout 240s cargo test -q -p evidence-vm runtime_host -- --test-threads=1`
    - `9 passed`
  - `timeout 240s cargo run -q -p yulang -- --std-root lib contract --contract file-resource tests/yulang/cases.toml`
    - `contract cases ok: 2`

## Runtime host manifest view canary

- Context:
  - Host act FFI decision F2 eventually wants a lowering-produced manifest, but
    the current runtime bridge still starts from escaped request paths.
  - The interim registry table should already expose the same act/op/tier shape
    that the future manifest will provide.
- Implementation summary:
  - Added a test manifest view over `RUNTIME_HOST_OPERATIONS`.
  - The canary fixes each current host operation's stable act id, operation id,
    sync tier, and path reconstruction rule.
  - Updated Contract v1 file evidence docs to record the new registry evidence:
    resolved operation specs are preserved through host execution, known-act
    unknown ops become capability failures, and unrelated user effects still
    fall through to escaped-effect behavior.
- Validation:
  - `cargo fmt --all -- --check`
    - passed
  - `timeout 240s cargo check -q -p evidence-vm -p yulang`
    - passed
  - `timeout 240s cargo test -q -p evidence-vm runtime_host -- --test-threads=1`
    - `10 passed`
  - `timeout 240s cargo run -q -p yulang -- --std-root lib contract --contract file-resource tests/yulang/cases.toml`
    - `contract cases ok: 2`

## Mock file host act canary

- Context:
  - Host act FFI decision F1 requires ordinary Yulang handlers to intercept host
    acts before the host registry.
  - The full `text_with` mock resource-lifetime fixture still hits the same
    callback residual specialize blocker as native rollback cases, so this
    slice keeps the mock proof to act routing only.
- Implementation summary:
  - Added `tests/yulang/regressions/runtime/file_mock_read_text_handler.yu`.
  - Added `file_mock_read_text_handler` to `tests/yulang/cases.toml`.
  - The case runs with `host = "unsupported"` and catches
    `std::io::file::file::read_at` in source, proving that mock handlers run
    before unsupported-host registry fallback.
  - Updated Contract v1 file evidence docs to mark this as mock-host act
    routing evidence, not resource-lifetime mock completion.
- Validation:
  - `cargo fmt --all -- --check`
    - passed
  - `timeout 240s cargo run -q -p yulang -- --std-root lib contract --case file_mock_read_text_handler tests/yulang/cases.toml`
    - `contract cases ok: 1`
  - `timeout 240s cargo run -q -p yulang -- --std-root lib contract --contract file-resource tests/yulang/cases.toml`
    - `contract cases ok: 2`
  - `timeout 240s cargo run -q -p yulang -- --std-root lib contract --case file_unsupported_host tests/yulang/cases.toml`
    - `contract cases ok: 1`

## Native text_with multi-shot branch snapshots

- Context:
  - `text_with` under `nondet.each` specialized after the alias fix, but the managed file buffer was still shared across resumed branches.
  - The reduced probe in `notes/bugs/file_text_with_nondet_shared_snapshot.yu` showed branch B reading `start A` and committing `start A B`.
- Implementation summary:
  - Added Evidence VM `FileSnapshots` continuation frames that carry native managed file buffers and the next file handle through resumed continuations.
  - File snapshot frames run synchronous host requests inside the restored branch state, so file get/set/commit requests do not escape and create smaller stale snapshot scopes.
  - File snapshot continuation tails append into the frame's inner continuation, so later caller frames stay inside the same managed resource scope.
  - Added a normal-return closed snapshot sidecar so callback-local file edits survive until the surrounding managed-lens commit operation; abortive/error exits do not record that sidecar and still roll back.
  - Added `tests/yulang/regressions/runtime/file_text_with_nondet_branch_snapshots.yu` and the `file_text_with_nondet_branch_snapshots` manifest case.
  - Updated Contract v1 file evidence docs and marked the original bug note resolved.
- Validation:
  - `cargo check -q -p evidence-vm`
    - passed
  - temp probe from `notes/bugs/file_text_with_nondet_shared_snapshot.yu`
    - `run roots [([(" A", "start A"), (" B", "start B")], "start B")]`
    - final temp file: `start B`
  - `cargo fmt --all -- --check`
    - passed
  - `timeout 240s cargo check -q -p evidence-vm -p yulang`
    - passed
  - `cargo run -q -p yulang -- --std-root lib contract --case file_text_with_nondet_branch_snapshots tests/yulang/cases.toml`
    - `contract cases ok: 1`
  - `cargo run -q -p yulang -- --std-root lib contract --case file_text_with_rollback_on_error tests/yulang/cases.toml`
    - `contract cases ok: 1`
  - `cargo run -q -p yulang -- --std-root lib contract --contract file-resource tests/yulang/cases.toml`
    - `contract cases ok: 4`
  - `timeout 240s cargo test -q -p yulang --test cli public_contract_manifest -- --test-threads=1`
    - `2 passed`

## Packaged archive file-resource parity

- Context:
  - Contract v1 file-resource evidence still listed packaged archive parity as
    open even though `scripts/release-archive-smoke.sh` had already been wired
    to run the filtered `file-resource` subset.
  - The missing piece was an actual local archive smoke run through bundled std.
- Implementation summary:
  - Built `target/release/yulang`.
  - Packaged a local Linux release archive under `target/release-contract-v1`.
  - Ran the archive smoke against the generated tarball; the smoke expanded the
    package, verified `release-manifest.txt`, ran `release-smoke.sh`, and ran
    the current `file-resource` contract subset through the packaged binary and
    bundled standard library.
  - Updated Contract v1 file evidence and the priority memo so packaged archive
    parity is no longer listed as an open slice for the current subset.
- Validation:
  - `timeout 300s cargo build -q --release -p yulang`
    - passed
  - `scripts/package-release.sh --version contract-v1-smoke --target x86_64-unknown-linux-gnu --binary target/release/yulang --out target/release-contract-v1`
    - produced `target/release-contract-v1/yulang-x86_64-unknown-linux-gnu.tar.gz`
  - `timeout 360s scripts/release-archive-smoke.sh target/release-contract-v1/yulang-x86_64-unknown-linux-gnu.tar.gz`
    - `release smoke ok: .../yulang-contract-v1-smoke-x86_64-unknown-linux-gnu/bin/yulang`

## Runtime host manifest owner

- Context:
  - Host act FFI decision F2 wants host dispatch to move toward manifest +
    registry ownership.
  - The current Evidence VM bridge already had operation metadata, but the
    registry still reached directly into the static operation and known-act
    tables.
- Implementation summary:
  - Added `RuntimeHostManifest` as the owner for the current static host
    operation specs and known host acts.
  - `RuntimeHostRegistry` now resolves operation specs and known-act capability
    failures through that manifest.
  - No public semantics changed: source handlers still intercept before the
    host registry, known unregistered host ops still become capability
    failures, and unknown user effects still fall through to escaped-effect
    behavior.
- Validation:
  - `cargo fmt --all -- --check`
    - passed
  - `timeout 240s cargo check -q -p evidence-vm -p yulang`
    - passed
  - `timeout 240s cargo test -q -p evidence-vm runtime_host -- --test-threads=1`
    - `10 passed`
  - `timeout 240s cargo run -q -p yulang -- --std-root lib contract --contract file-resource tests/yulang/cases.toml`
    - `contract cases ok: 4`
  - `git diff --check`
    - passed

## Public ref-constructor mock blocker check

- Context:
  - Pure mock `file::text_with` still has two blockers: the private native
    snapshot helper operations and a missing public-only local-ref construction
    shape for the callback surface.
  - The second blocker should be tracked independently from file host behavior.
- Findings:
  - Replaced the attempted reduction with
    `notes/bugs/ref_constructor_public_path_blocker.yu`.
  - The attempted program annotates a local value as `std::control::var::ref _
    str`, then tries to construct `std::control::var::ref { get,
    update_effect }` over a local `my &text` binding.
  - `check` currently succeeds, but `dump-poly` reports:
    `error: unresolved value name in root expression: std::control::var::ref`.
  - Because the constructor expression is lowered as `<missing>`, the later
    `run --print-roots` failure with
    `conflicting type candidates for slot 0: std::text::str::str vs {get: 'open7}`.
    is not trustworthy proof of a specialize2 bug.
  - This keeps the fix away from `std::io::file` private helper visibility while
    avoiding the wrong claim: the mock lifetime path needs a mockable public
    host-act/session boundary or a documented public ref view shape that carries
    local-var residuals through callbacks.
- Validation:
  - `timeout 240s cargo run -q -p yulang -- --std-root lib check notes/bugs/ref_constructor_public_path_blocker.yu`
    - exit 0
  - `timeout 240s cargo run -q -p yulang -- --std-root lib dump-poly notes/bugs/ref_constructor_public_path_blocker.yu`
    - reports the unresolved value constructor above
  - `timeout 240s cargo run -q -p yulang -- --std-root lib run --print-roots notes/bugs/ref_constructor_public_path_blocker.yu`
    - reports the downstream conflict above; this is recorded only as a
      symptom after an unresolved constructor, not as a specialize2 reduction
  - `git diff --check`
    - passed

## Static route Stage 0 representative profile

- Context:
  - Claude's `notes/design/2026-07-02-static-route-promotion-plan.md` requires
    Stage 0 coverage numbers before any Stage 1 static direct execution work.
  - The Stage 0 plan/runtime counters already exist in the current tree:
    `evidence.static_route_*` and
    `runtime_evidence.static_route_runtime_hits_*`.
- Added:
  - `scripts/static-route-stage0-profile.sh`
    - Runs the required representative workloads through release `yulang` with
      `debug evidence-vm-run --compare-control --runtime-evidence-profile-deep`.
    - Prints only compare-control, static-route plan counters, static-route
      runtime-hit counters, and roots.
  - A narrower Stage 0 classification rule for generic fallback operation
    calls:
    - known runtime host acts stay `HostEscape`;
    - non-host generic fallbacks become `Unclassified` instead of being counted
      as host capability escapes.
    - non-host generic fallbacks with one matching handler object become
      `ProviderEnvDependent`;
    - non-host generic fallbacks with multiple matching handler objects become
      `MultipleCandidates`.
- Observed with `target/release/yulang`:

| workload | sites | static tail | provider-env sites | multi-candidate sites | host escape sites | unclassified sites | runtime provider-env hits | runtime multi-candidate hits | runtime host escape hits | runtime unclassified hits | result |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | --- |
| `bench/nondet_20_discard.yu` | 3 | 0 | 3 | 0 | 0 | 0 | 861 | 0 | 0 | 0 | match |
| `examples/showcase.yu` | 13 | 0 | 6 | 7 | 0 | 0 | 1634 | 830 | 0 | 0 | match |
| `examples/03_for_last.yu` | 7 | 0 | 6 | 1 | 0 | 0 | 1 | 1 | 0 | 0 | match |
| `examples/02_refs.yu` | 8 | 0 | 6 | 0 | 0 | 2 | 8 | 0 | 0 | 2 | match |

- Decision:
  - Stage 1 is not allowed from these representative std workloads: static
    tail runtime hits are 0%.
  - The generic fallback classification hole is now much smaller: nondet,
    showcase, and `03_for_last` have no unclassified runtime hits after handler
    candidate counting.
  - `examples/02_refs.yu` still has 2 unclassified runtime hits. Changing
    runtime behavior would be the wrong move until those operation objects are
    explained.
- Validation:
  - `timeout 360s scripts/static-route-stage0-profile.sh`
    - passed with `target/release/yulang`; the four commands run with
      `--compare-control`, so a mismatch would fail the script
  - `cargo fmt --all -- --check`
    - passed
  - `timeout 240s cargo test -q -p evidence-vm static_route_classifies_only_known_host_generic_fallbacks_as_host_escape -- --test-threads=1`
    - passed
  - `timeout 240s cargo test -q -p evidence-vm static_route_classifies -- --test-threads=1`
    - `2 passed`
  - `timeout 240s cargo check -q -p evidence-vm -p yulang`
    - passed
  - `timeout 600s cargo build -q -p yulang --release`
    - passed
  - `timeout 240s cargo test -q -p yulang --test cli debug_runtime_evidence_run -- --test-threads=1`
    - `29 passed`
  - `timeout 240s cargo run -q -p yulang -- --std-root lib debug evidence-vm-run --compare-control --runtime-evidence-profile-deep bench/nondet_20_discard.yu`
    - compare-control matched; static route runtime provider-env hits 861,
      unclassified hits 0
  - `timeout 240s cargo run -q -p yulang -- --std-root lib debug evidence-vm-run --compare-control --runtime-evidence-profile-deep examples/showcase.yu`
    - compare-control matched; static route runtime provider-env hits 1634,
      multi-candidate hits 830, unclassified hits 0
  - `timeout 240s cargo run -q -p yulang -- --std-root lib debug evidence-vm-run --compare-control --runtime-evidence-profile-deep examples/03_for_last.yu`
    - compare-control matched; static route runtime provider-env hits 1,
      multi-candidate hits 1, unclassified hits 0
  - `timeout 240s cargo run -q -p yulang -- --std-root lib debug evidence-vm-run --compare-control --runtime-evidence-profile-deep examples/02_refs.yu`
    - compare-control matched; static route runtime provider-env hits 8,
      unclassified hits 2

## Static route plan dump evidence

- Context:
  - The Stage 0 representative profile left 2 unclassified runtime hits in
    `examples/02_refs.yu`.
  - The existing `debug evidence-vm-plan` dump listed operation objects but did
    not show each object's static-route classification, so the profile hole
    still required cross-reading counters and operation lists.
- Implementation summary:
  - `debug evidence-vm-plan` operation-object rows now include
    `static_route ...` next to the execution plan.
  - The output confirms the remaining `02_refs` unclassified call sites are the
    two `std::control::var::ref_update::update` calls. The compiler-generated
    local ref get/set calls classify as `dynamic-provider-env`, while
    `ref_update::update` remains a higher-order helper call without a matching
    handler object.
- Validation:
  - `cargo fmt --all -- --check`
    - passed
  - `timeout 240s cargo test -q -p yulang debug_evidence_vm_plan_prints_handler_passing_plan --test cli -- --test-threads=1`
    - `1 passed`
  - `timeout 240s cargo check -q -p evidence-vm -p yulang`
    - passed
  - `cargo run -q -p yulang -- --std-root lib debug evidence-vm-plan examples/02_refs.yu`
    - operation-object rows show `static_route dynamic-provider-env` for
      synthetic local ref get/set objects and `static_route
      dynamic-unclassified` for `ref_update::update` objects.

## File metadata non-throwing canary

- Context:
  - `spec/2026-07-02-io-resource-api.md` defines `file::meta` as a
    non-throwing metadata query: missing / denied / other inaccessible targets
    are metadata kinds, not `io_err` effects.
  - The current first slice already returned `file_kind::missing` for absent
    paths, but the public signature still carried `io_err` because the wrapper
    threw on nonzero `meta_raw` codes.
- Implementation summary:
  - Added `file_kind::denied` to `std::io::file`.
  - Changed `std::io::file::meta` to always return `file_meta` from
    `meta_raw`'s kind code instead of converting metadata failures into
    `io_err`.
  - Changed the native evidence VM `meta_raw` bridge to encode:
    - `missing` for not-found paths;
    - `denied` for permission denied;
    - `other` for other metadata failures;
    - `file` / `dir` / `symlink` / `other` for successful metadata.
  - Updated the public-signature canary so `std.io.file.meta` projects as
    `path -> [file] file_meta`.
- Validation:
  - `cargo fmt --all -- --check`
    - passed
  - `timeout 240s cargo test -q -p yulang compatible_run_std_file_meta_reports_file_dir_and_missing_kind --test cli -- --test-threads=1`
    - `1 passed`
  - `timeout 240s cargo test -q -p yulang public_contract_manifest_cli_cases_hold --test cli -- --test-threads=1`
    - `1 passed`, finished in 188.65s
  - `timeout 240s cargo check -q -p evidence-vm -p yulang`
    - passed
  - `timeout 240s cargo run -q -p yulang -- --std-root lib contract --contract file-resource tests/yulang/cases.toml`
    - `contract cases ok: 4`
  - `git diff --check`
    - passed

## File metadata manifest runtime canary

- Context:
  - After `std::io::file::meta` became non-throwing, the runtime behavior was
    still mostly covered by a Rust CLI test and a public-signature manifest
    case.
  - The contract manifest only knew how to create temp files, so a focused
    missing-path metadata case needed a small materialization extension.
- Implementation summary:
  - Added `missing = true` support to `[[case.temp_files]]` in both the CLI
    manifest test helper and the public `yulang contract` runner.
  - Added `metadata` as a canonical contract tag. A `file-resource` runtime
    case can now be categorized as either `resource-lifetime` or `metadata`
    when it is not an unsupported-host case.
  - Added `tests/yulang/regressions/runtime/file_meta_kind.yu`, which checks
    `file_kind::file` and `file_kind::missing` through manifest-managed temp
    paths.
  - Added the `file_meta_kind` manifest case with
    `file-resource` / `metadata` / `host.native` tags.
- Validation:
  - `cargo fmt --all -- --check`
    - passed
  - `timeout 240s cargo run -q -p yulang -- --std-root lib contract --case file_meta_kind tests/yulang/cases.toml`
    - `contract cases ok: 1`
  - `timeout 240s cargo run -q -p yulang -- --std-root lib contract --contract file-resource tests/yulang/cases.toml`
    - `contract cases ok: 5`
  - `timeout 240s cargo test -q -p yulang public_contract_manifest_cli_cases_hold --test cli -- --test-threads=1`
    - `1 passed`, finished in 190.29s
  - `timeout 240s cargo test -q -p yulang contract_command_rejects_file_resource_runtime_case_without_host_scope --test cli -- --test-threads=1`
    - `1 passed`
  - `timeout 240s cargo check -q -p yulang`
    - passed

## File mock read/write act canary

- Context:
  - Contract v1 still needs pure mock resource-lifetime parity, but the public
    host act routing evidence only covered `read_text` / `read_at`.
  - Before adding a larger mock session API, the manifest should prove that
    source handlers intercept both read and write file helper operations before
    the unsupported native host registry reports a capability failure.
- Implementation summary:
  - Added `tests/yulang/regressions/runtime/file_mock_read_write_text_handler.yu`.
  - The fixture runs with `--host unsupported`, handles `read_at` and
    `write_at` in source, and observes `("start", "start!")`.
  - Added the fixture to `tests/yulang/cases.toml` as a
    `mock-host` / `host.unsupported` migration canary.
  - Documented the canary as mock-host act routing evidence rather than full
    resource-lifetime mock evidence.
- Validation:
  - `cargo fmt --all -- --check`
    - passed
  - `timeout 240s cargo run -q -p yulang -- --std-root lib contract --case file_mock_read_write_text_handler tests/yulang/cases.toml`
    - `contract cases ok: 1`
  - `timeout 240s cargo test -q -p yulang public_contract_manifest_cli_cases_hold --test cli -- --test-threads=1`
    - `1 passed`, finished in 191.88s
  - `timeout 240s cargo check -q -p yulang`
    - passed

## File-resource mock-host tag policy

- Context:
  - The manifest had mock-host file act canaries, but `--contract
    file-resource` did not include them.
  - The old file-resource policy counted `mock-host` and `host.unsupported` as
    two host scopes even though mock cases intentionally run with the native
    host disabled so the source handler must intercept the operation first.
- Implementation summary:
  - Added `host-act` as a canonical contract tag for cases that prove
    source-level host act interception.
  - Updated the manifest schema checks in both `yulang contract` and the CLI
    manifest test helper:
    - native/unsupported file-resource cases still choose exactly one native
      host mode;
    - mock-host file-resource cases use `mock-host` plus `host.unsupported`;
    - mock-host file-resource cases must carry either `host-act` or
      `resource-lifetime`.
  - Promoted `file_mock_read_text_handler` and
    `file_mock_read_write_text_handler` into the `file-resource` subset with
    `host-act`.
  - Updated `tests/yulang/README.md`,
    `docs/language/file-resource-contract.md`, and
    `docs/language/contract-v1-file-evidence.md` with the tag policy.
- Validation:
  - `cargo fmt --all -- --check`
    - passed
  - `timeout 240s cargo run -q -p yulang -- --std-root lib contract --contract file-resource tests/yulang/cases.toml`
    - `contract cases ok: 7`
  - `timeout 240s cargo test -q -p yulang public_contract_manifest_cli_cases_hold --test cli -- --test-threads=1`
    - `1 passed`, finished in 198.62s
  - `timeout 240s cargo check -q -p yulang`
    - passed

## Public ref-view update blocker narrowed

- Context:
  - Pure mock resource-lifetime parity for `file::text_with` still needs a
    public way to build the managed `ref` view over handler-local state without
    exposing private file snapshot operations.
  - The previous reduction used fully-qualified
    `std::control::var::ref { ... }`, but `dump-poly` showed that constructor
    expression as unresolved, so the downstream `run` conflict was not
    trustworthy evidence.
- Implementation summary:
  - Added `notes/bugs/ref_constructor_short_value_probe.yu`.
  - The probe imports `std::control::var::*` and uses the short `ref { ... }`
    constructor.
  - Corrected the probe to use current mutable-binding syntax:
    `my $text = ...`, not the design-note spelling `my &text = ...`.
  - Added `notes/bugs/ref_update_local_buffer_public_probe.yu`; this also
    passes, proving direct public `ref.update` over a local `$buffer` is not the
    remaining blocker.
  - Added `notes/bugs/file_text_with_mock_function_boundary_blocker.yu` as the
    current reduced blocker for the reusable helper shape.
  - Added `tests/yulang/regressions/runtime/file_mock_public_ref_view_commit.yu`
    and registered it in `tests/yulang/cases.toml` as a
    `file-resource` / `resource-lifetime` / `mock-host` canary.
  - Updated the old blocker notes and Contract v1 evidence to point at the
    corrected reductions.
- Findings:
  - `check` accepts the short-constructor probe.
  - `dump-poly` resolves the constructor to `d171:"std.control.var.ref"`.
  - `run --print-roots` now succeeds for the constructor probe.
  - A public `ref` view over local `$buffer` can be built, read, and updated
    through `r.update`.
  - Inline pure resource-lifetime commit works with native host capabilities
    disabled.
  - Moving the same shape behind `text_with_mock(backing, f)` still fails with a
    callback residual conflict:
    `[&buffer#...:0(std::text::str::str), std::control::var::ref_update(...)]`
    vs `[&buffer#...:0(std::text::str::str)]`.
  - The remaining blocker is therefore the reusable function-boundary residual
    rule for callbacks updating public ref views over local resource effects.
- Validation:
  - `timeout 240s cargo run -q -p yulang -- --std-root lib check notes/bugs/ref_constructor_short_value_probe.yu`
    - passed
  - `timeout 240s cargo run -q -p yulang -- --std-root lib run --print-roots notes/bugs/ref_constructor_short_value_probe.yu`
    - `run roots ["start"]`
  - `timeout 240s cargo run -q -p yulang -- --std-root lib dump-poly-std notes/bugs/ref_constructor_short_value_probe.yu | rg -n "<missing>|unresolved|root expression|std::control::var::ref|ref_constructor|notes/bugs|error|\\{get"`
    - showed the root expression using `d171:"std.control.var.ref"` and no
      unresolved root constructor for the short form
  - `timeout 240s cargo run -q -p yulang -- --std-root lib run --print-roots notes/bugs/ref_update_local_buffer_public_probe.yu`
    - `run roots ["next"]`
  - `timeout 240s cargo run -q -p yulang -- --std-root lib run --print-roots notes/bugs/file_text_with_mock_function_boundary_blocker.yu`
    - failed with the expected current callback residual conflict
  - `timeout 240s cargo run -q -p yulang -- --std-root lib contract --case file_mock_public_ref_view_commit tests/yulang/cases.toml`
    - `contract cases ok: 1`
  - `timeout 240s cargo run -q -p yulang -- --std-root lib contract --contract file-resource tests/yulang/cases.toml`
    - `contract cases ok: 8`
  - `timeout 240s cargo test -q -p yulang public_contract_manifest_cli_cases_hold --test cli -- --test-threads=1`
    - `1 passed`, finished in 195.45s
  - `timeout 240s cargo check -q -p yulang`
    - passed
  - `cargo fmt --all -- --check`
    - passed
  - `git diff --check`
    - passed

## Host act dispatch helper

- Time check:
  - Started this slice at 16:28 JST.
- Blocker evidence carried forward:
  - Re-ran
    `notes/bugs/file_text_with_mock_function_boundary_blocker.yu`; it still
    fails with the expected callback residual conflict.
  - `dump-poly-std` shows the conflict is already present in the generalized
    helper surface:
    `text_with_mock` exposes handler-local `&buffer#...` evidence, with a lower
    residual containing `std::control::var::ref_update(...)` and an upper
    residual without it.
  - Native `std.io.file.open_in` does not have the same shape because its public
    ref view is parameterized by the public `std::io::file::file` act, not by a
    helper-local private resource effect.
  - Decision: do not patch this by dropping `ref_update` in specialization or by
    adding a fixture/path/name special case. The remaining fix belongs to the
    scoped private resource effect boundary or an equivalent public session
    representation.
- Implementation summary:
  - Added `RuntimeHostRequestHandling` inside the Evidence VM runtime.
  - Centralized runtime host request resolution and capability-failure handling
    in `try_handle_runtime_host_request`.
  - Kept caller-specific unknown-effect behavior unchanged:
    normal escaped requests still become `EscapedEffect`, while snapshot resume
    still rewraps the unhandled request.
- Validation:
  - `cargo fmt --all -- --check`
    - passed
  - `timeout 240s cargo test -q -p evidence-vm runtime_host -- --test-threads=1`
    - `10 passed`
  - `timeout 240s cargo check -q -p evidence-vm -p yulang`
    - passed

## Debug host act manifest surface

- Time check:
  - Started this slice at 16:31 JST.
- Context:
  - Host act FFI decision F2 wants a manifest + registry boundary that can
    replace escaped-effect path dispatch.
  - The current `RuntimeHostManifest` was visible to unit tests only.
- Implementation summary:
  - Promoted the runtime host manifest operation view to a public evidence-vm
    API: `runtime_host_manifest_operations`.
  - Added `yulang debug host-act-manifest`.
  - The command prints each current host operation as stable act id, operation
    id, tier, and reconstructed path.
  - Added a CLI regression that fixes the console write, file exists, and file
    meta raw manifest lines.
  - Updated Contract v1 file evidence docs to record this as release-binary
    observable interim manifest evidence.
- Validation:
  - `cargo fmt --all -- --check`
    - passed
  - `timeout 240s cargo test -q -p evidence-vm runtime_host -- --test-threads=1`
    - `10 passed`
  - `timeout 240s cargo test -q -p yulang debug_host_act_manifest_prints_runtime_registry_view --test cli -- --test-threads=1`
    - `1 passed`
  - `timeout 240s cargo check -q -p evidence-vm -p yulang`
    - passed
  - `git diff --check`
    - passed

## Release smoke host manifest gate

- Time check:
  - Started this slice at 16:34 JST.
- Context:
  - `debug host-act-manifest` should be observable in release artifacts, not
    only in Rust CLI tests.
  - `scripts/release-archive-smoke.sh` already delegates to
    `scripts/release-smoke.sh`, so the direct smoke is the narrow gate to extend.
- Implementation summary:
  - Added representative host act manifest checks to
    `scripts/release-smoke.sh`.
  - The smoke now checks the current console `write` line and file `exists`
    line from `yulang debug host-act-manifest`.
  - Updated Contract v1 file evidence docs to record release/archive smoke
    coverage for the debug manifest surface.
- Validation:
  - `bash -n scripts/release-smoke.sh`
    - passed
  - `timeout 180s scripts/release-smoke.sh target/debug/yulang`
    - `release smoke ok: target/debug/yulang`

## File metadata unsupported-host contract

- Time check:
  - Started this slice at 16:36 JST.
- Context:
  - `file_unsupported_host` covered a public predicate helper, but metadata is
    closer to the intended file resource center.
  - Unsupported hosts should not return fake metadata.
- Implementation summary:
  - Added `tests/yulang/regressions/runtime/file_meta_unsupported_host.yu`.
  - Registered `file_meta_unsupported_host` as a `file-resource` /
    `metadata` / `host.unsupported` runtime-failure case.
  - Updated Contract v1 file evidence docs.
- Validation:
  - `timeout 240s cargo run -q -p yulang -- --std-root lib contract --case file_meta_unsupported_host tests/yulang/cases.toml`
    - `contract cases ok: 1`
  - `timeout 240s cargo run -q -p yulang -- --std-root lib contract --contract file-resource tests/yulang/cases.toml`
    - `contract cases ok: 9`
  - `timeout 240s cargo test -q -p yulang public_contract_manifest_cli_cases_hold --test cli -- --test-threads=1`
    - `1 passed`, finished in 198.01s
  - `cargo fmt --all -- --check`
    - passed

## Role/method LSP diagnostic parity canary

- Time check:
  - Started this slice at 16:41 JST.
- Context:
  - `unresolved_method_diagnostic` and `ambiguous_method_diagnostic` already
    fix the CLI/check `SourceDiagnostic` payload in `tests/yulang/cases.toml`.
  - LSP diagnostics should read the same source payload rather than rebuilding a
    separate role/method error.
- Implementation summary:
  - Added a server diagnostic unit test for unresolved and ambiguous method
    failures.
  - The unresolved case fixes LSP code/range/hint and verifies no related
    entries.
  - The ambiguous case fixes LSP code/range/hint and verifies two candidate
    related entries.
  - This does not move role impl satisfaction out of the specialization oracle;
    it only locks the current bridge at the LSP boundary.
- Validation:
  - `cargo fmt --all -- --check`
    - passed
  - `timeout 240s cargo test -q -p yulang diagnostics_use_role_method_ranges_and_related_candidates -- --test-threads=1`
    - `1 passed`
  - `timeout 240s cargo check -q -p yulang`
    - passed

## Role/method playground diagnostic parity canary

- Time check:
  - Started this slice at 16:45 JST.
- Context:
  - The previous slice fixed LSP boundary coverage for the existing
    role/method `SourceDiagnostic` bridge.
  - Playground/wasm should expose the same diagnostic code, user-source range,
    hint, and related candidate entries.
- Implementation summary:
  - Added `unresolved_method_error` and `ambiguous_method_error` to the wasm
    diagnostics fixture helper.
  - Added a focused wasm test for unresolved and ambiguous role method payloads.
  - The unresolved playground range is the user source range `10..14`, not the
    implicit-prelude-adjusted CLI byte range `39..43`.
  - The ambiguous case fixes two related candidate entries in the playground
    payload.
- Validation:
  - `cargo fmt --all -- --check`
    - passed
  - `timeout 240s cargo test -q -p wasm check_inner_returns_role_method_diagnostic_payloads -- --test-threads=1`
    - `1 passed`
  - `timeout 240s cargo check -q -p wasm -p yulang`
    - passed

## Static route Stage 0 profile refresh

- Time check:
  - Started this slice at 16:45 JST.
- Context:
  - The static route promotion plan requires a representative Stage 0 profile
    before any Stage 1 direct execution work.
  - This run is measurement-only; no runtime semantics changed.
- Representative profile:

| workload | sites | static tail hits | provider-env hits | multi-candidate hits | host escape hits | unclassified hits | result |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | --- |
| `bench/nondet_20_discard.yu` | 3 | 0 | 861 | 0 | 0 | 0 | match |
| `examples/showcase.yu` | 13 | 0 | 1634 | 830 | 0 | 0 | match |
| `examples/03_for_last.yu` | 7 | 0 | 1 | 1 | 0 | 0 | match |
| `examples/02_refs.yu` | 8 | 0 | 8 | 0 | 0 | 2 | match |

- Decision:
  - Do not proceed to Stage 1. Static-tail runtime hits are still 0 on the
    representative workloads.
  - `examples/02_refs.yu` still has 2 unclassified runtime hits, so Stage 0
    classification also remains incomplete for that example.
- Validation:
  - `timeout 360s scripts/static-route-stage0-profile.sh`
    - passed; all four `--compare-control` runs matched.

## Runtime backend contract manifest cases

- Time check:
  - Started this slice at 16:48 JST.
- Context:
  - Runtime backend failure formatting had focused CLI unit coverage, but the
    public contract manifest could not choose a run backend.
  - Contract v0 release evidence should be executable from the manifest when a
    runtime behavior depends on the public backend flag.
- Implementation summary:
  - Added optional `backend = "control-vm" | "interpreter"` support to the
    `yulang contract` runner.
  - Added matching manifest tags `backend.control-vm` and
    `backend.interpreter`, including schema checks that prevent tag/backend
    drift.
  - Mirrored the schema in the Rust CLI manifest helper.
  - Added two manifest runtime-failure cases for `not_callable_error.yu` that
    assert the non-default backends report
    `yulang.unsupported-runtime-feature` with the user-facing hint.
- Validation:
  - `cargo fmt --all -- --check`
    - passed
  - `cargo check -q -p yulang`
    - passed
  - `timeout 240s cargo test -q -p yulang public_contract_manifest_cli_cases_hold --test cli -- --test-threads=1`
    - `1 passed`, finished in 201.95s
  - `cargo run -q -p yulang -- --std-root lib contract --case not_callable_interpreter_unsupported_runtime_error tests/yulang/cases.toml`
    - `contract cases ok: 1`
  - `cargo run -q -p yulang -- --std-root lib contract --case not_callable_control_vm_unsupported_runtime_error tests/yulang/cases.toml`
    - `contract cases ok: 1`
  - `cargo run -q -p yulang -- --std-root lib contract --contract backend.interpreter tests/yulang/cases.toml`
    - `contract cases ok: 1`
  - `cargo run -q -p yulang -- --std-root lib contract --contract backend.control-vm tests/yulang/cases.toml`
    - `contract cases ok: 1`

## Release gates cover runtime backend contracts

- Time check:
  - Started this slice at 16:56 JST.
- Context:
  - The manifest can now select non-default runtime backends, but release gates
    should also exercise those tags through the public binary surface.
- Implementation summary:
  - Added `backend.interpreter` and `backend.control-vm` filtered contract
    runs to `scripts/hardening-smoke.sh`.
  - Added the same filtered contract runs to
    `scripts/release-archive-smoke.sh`, so packaged binaries with bundled std
    also cover the backend failure contract.
- Validation:
  - `bash -n scripts/hardening-smoke.sh scripts/release-archive-smoke.sh`
    - passed
  - `timeout 300s cargo build -q --release -p yulang`
    - passed
  - `target/release/yulang --std-root lib contract --contract backend.interpreter tests/yulang/cases.toml`
    - `contract cases ok: 1`
  - `target/release/yulang --std-root lib contract --contract backend.control-vm tests/yulang/cases.toml`
    - `contract cases ok: 1`
  - `cargo fmt --all -- --check`
    - passed

## Runtime backend/host manifest schema hardening

- Time check:
  - Started this slice at 16:57 JST.
- Context:
  - `run --host unsupported` is an Evidence VM-only public CLI route.
  - The manifest now knows about runtime backends, so it should reject
    non-native host modes combined with non-evidence backends before spawning a
    child process.
- Implementation summary:
  - Added manifest validation that rejects `host = "unsupported"` with
    `backend = "control-vm"` or `backend = "interpreter"`.
  - Mirrored the same validation in the Rust CLI manifest helper.
  - Added a focused CLI regression for the invalid host/backend combination.
- Validation:
  - `cargo fmt --all -- --check`
    - passed
  - `timeout 120s cargo test -q -p yulang contract_command_rejects_unsupported_host_with_non_evidence_backend --test cli -- --test-threads=1`
    - `1 passed`
  - `cargo check -q -p yulang`
    - passed
  - `timeout 240s cargo test -q -p yulang public_contract_manifest_cli_cases_hold --test cli -- --test-threads=1`
    - `1 passed`, finished in 201.24s

## Missing index argument diagnostic range

- Time check:
  - Started this slice at 17:03 JST.
- Context:
  - The diagnostics TODO calls for small "直せる" syntax/lowering failures with
    stable code, primary range, hint, and CLI/LSP/playground parity.
  - `MissingIndexArgument` already had a user-facing code and hint, but no
    source range, so it could not enter the contract manifest.
- Implementation summary:
  - Changed `LoweringError::MissingIndexArgument` to carry the source range for
    the empty index bracket.
  - The range is taken from the trimmed bracket node, so the diagnostic points
    at `[]` and does not include trailing newline trivia.
  - Added `missing_index_argument.yu` and a `stable-core` / `diagnostics`
    manifest case.
  - Added LSP and playground parity canaries for the same fixture.
  - Updated Contract v0 evidence counts to 52 total stable-core cases:
    24 `run`, 13 `check`, and 15 `public-signature`.
- Validation:
  - `cargo fmt --all -- --check`
    - passed
  - `timeout 180s cargo test -q -p yulang diagnostics_use_missing_index_argument_range -- --test-threads=1`
    - `1 passed`
  - `timeout 180s cargo test -q -p wasm check_inner_returns_missing_index_argument_code_and_range -- --test-threads=1`
    - `1 passed`
  - `timeout 120s cargo run -q -p yulang -- --std-root lib contract --case missing_index_argument tests/yulang/cases.toml`
    - `contract cases ok: 1`
  - `timeout 240s cargo check -q -p infer -p yulang -p wasm`
    - passed
  - `timeout 180s cargo run -q -p yulang -- --std-root lib contract --contract stable-core tests/yulang/cases.toml`
    - `contract cases ok: 52`
  - `timeout 300s cargo test -q -p yulang public_contract_manifest_cli_cases_hold --test cli -- --test-threads=1`
    - `1 passed`, finished in 207.41s

<!-- daily-append-anchor: 2026-07-02 -->
