# 2026-07-02

## Public contract manifest first slice

- Context:
  - ChatGPT Pro recommended turning the Public Contract Spine into an
    executable contract rather than adding more language surface.
  - Runtime and diagnostics already had shared `.yu` fixtures, but the
    fixture set was still driven by Rust test code instead of a repository
    manifest.
- Implementation summary:
  - Added `tests/yulang/cases.toml` as the first public contract manifest.
  - The manifest currently covers focused CLI `run` cases for:
    - `nondet_once_triple`
    - `optional_record_defaults`
    - `attached_impl_pick`
    - `list_update`
  - It also covers focused CLI `check` diagnostics for:
    - `type_annotation_mismatch`
    - `unresolved_value_name`
    - `catch_missing_arm_body`
  - Added `public-signature` manifest support and canaries for:
    - data-position effectful function public signature
    - nested data-position effectful function public signature
    - `std.control.var.ref.update`
    - `std.text.parse.choice`
    - `std.control.flow.loop.for_in`
    - `std.control.nondet.nondet.#act-method:once`
    - `std.control.nondet.nondet.#act-method:list`
  - Extended the manifest to include the remaining focused public diagnostics
    fixtures for unresolved type names, unsupported type syntax, top-level
    mutable bindings, parser syntax recovery, rule lazy quantifiers, and
    malformed `catch` arms.
  - Moved representative runtime/compile error sources into shared fixtures and
    covered unhandled effects, non-call calls, non-record field access, pattern
    mismatch, missing record fields, unresolved methods, and ambiguous methods.
  - Fixed the cached runtime build path so specialization failures go through
    the same route-error formatter as the no-cache path. Without this, cached
    `run` reported raw subtype constraints instead of the public
    `compile error [yulang.*]` message and hint.
  - Added repo-root manifest entries for public examples `examples/01_*.yu`
    through `examples/13_console.yu`, plus `examples/showcase.yu`.
    Console output is checked without `--print-roots`; showcase stays a compact
    contains check rather than a large stdout snapshot.
  - Clarified standard API contract staging:
    - `tests/yulang/cases.toml` is for current executable behavior, not TODO or
      ignored placeholders.
    - file/server/host capability cases should enter the manifest only when
      native or unsupported-host public CLI behavior can be observed.
    - provisional helpers should not receive stable contract tags.
  - Added a source-level public signature invariant helper that scans every
    public signature type in selected contract-spine std modules and rejects
    private stack evidence (`#...` / `AllExcept...`) in the type part. The scan
    strips public symbol names and bodies first, so `#act-method` names remain
    valid while leaked type evidence is still caught.
  - Hardened the manifest runner schema:
    - case names must be unique;
    - every case must assert either CLI output or a public signature type;
    - hardening smoke already runs the manifest through its `cargo test -q -p
      yulang public -- --test-threads=1` gate because the manifest test is a
      public CLI test.
  - Connected diagnostics manifest entries to the structured `SourceDiagnostic`
    payload:
    - check cases can now assert diagnostic count, code, label, related count,
      and related origins;
    - current public diagnostics fixtures assert the shared payload without
      broadening stdout snapshots.
  - Removed the older inline public examples CLI bridge after the manifest
    became the stronger executable owner for `examples/01_*.yu` through
    `examples/showcase.yu`. This avoids running the same public examples twice
    in `cargo test -p yulang --test cli`.
  - Updated solver invariant docs to point at the manifest public-signature
    cases and the all-public-signature scan for contract-spine std modules.
  - Added a manifest-driven CLI integration test that reads the manifest with
    `toml`, runs each CLI case through the `yulang` binary, uses repo `lib` as
    std root for runtime cases, keeps runtime cache isolated, and checks public
    signature cases through the source dump API.
  - Updated `tests/yulang/README.md`, `docs/status.md`, `notes/todo/testing.md`,
    and `tasks/current.md` so the manifest is visible as the first executable
    contract entrypoint.
- Validation:
  - `cargo fmt --all`
  - `timeout 240s cargo test -q -p yulang public_contract_manifest_cli_cases_hold --test cli -- --test-threads=1`
    - `1 passed`
  - `timeout 240s cargo test -q -p yulang public_diagnostics -- --test-threads=1`
    - CLI diagnostics filter: `12 passed`
  - `timeout 240s cargo test -q -p yulang --test cli -- --test-threads=1`
    - `97 passed`
  - `timeout 240s cargo test -q -p yulang public_contract_manifest_cli_cases_hold --test cli -- --test-threads=1`
    - `1 passed` after adding runtime/diagnostic error cases and the cached
      specialization error formatter fix.
  - `timeout 240s cargo test -q -p yulang public_contract_manifest_cli_cases_hold --test cli -- --test-threads=1`
    - `1 passed` after adding public example manifest entries.
  - `timeout 240s cargo test -q -p yulang dump_poly_public_contract_spine_modules_hide_private_stack_evidence -- --test-threads=1`
    - `1 passed`
  - `timeout 240s cargo test -q -p yulang public_contract_manifest_cli_cases_hold --test cli -- --test-threads=1`
    - `1 passed` after adding structured diagnostic payload checks.
  - `timeout 300s cargo test -q -p yulang --test cli -- --test-threads=1`
    - `95 passed` after manifest ownership and duplicate public example bridge
      removal; wall time was 180.99s.
  - `timeout 180s scripts/public-example-smoke.sh target/debug/yulang`
    - passed
  - Added LSP diagnostic hover for structured diagnostics:
    - hover on a diagnostic primary range now reports the diagnostic code,
      message, and compact related provenance before falling back to type hover;
    - this reuses the existing LSP diagnostic conversion so CLI/LSP payloads
      keep the same code/range/related source of truth.
  - Validation:
    - `timeout 240s cargo test -q -p yulang hover_for_source_reports_diagnostic_summary_at_error_range -- --test-threads=1`
      - `1 passed`
    - `timeout 240s cargo test -q -p yulang hover_for_source -- --test-threads=1`
      - `4 passed`
    - `timeout 240s cargo test -q -p yulang diagnostics_use -- --test-threads=1`
      - `8 passed`
  - Added `SourceDiagnosticSeverity` to the shared diagnostic payload:
    - parser and lowering diagnostics now carry `Error` in the source payload;
    - CLI, LSP, wasm playground JSON, and `tests/yulang/cases.toml` manifest
      assertions read severity from that shared payload instead of hard-coding
      it separately.
  - Validation:
    - `timeout 240s cargo test -q -p yulang analyze_entry_source_reports_unresolved_name_source_range -- --test-threads=1`
      - `1 passed`
    - `timeout 240s cargo check -q -p wasm`
      - passed
    - `timeout 240s cargo test -q -p yulang diagnostics_use -- --test-threads=1`
      - `8 passed`
    - `timeout 240s cargo test -q -p yulang public_contract_manifest_cli_cases_hold --test cli -- --test-threads=1`
      - `1 passed`
  - Added the first standard API contract cases for `std::data::result`:
    - `tests/yulang/regressions/runtime/result_api.yu` covers `map`,
      `and_then`, and `unwrap_or` through the public CLI/std route;
    - manifest public-signature cases now fix `std.data.result.map`,
      `std.data.result.and_then`, and `std.data.result.unwrap_or`.
  - Validation:
    - `target/debug/yulang --std-root lib run --print-roots tests/yulang/regressions/runtime/result_api.yu`
      - `run roots [(result::ok(11), result::err("odd"), 42)]`
    - `timeout 240s cargo test -q -p yulang public_contract_manifest_cli_cases_hold --test cli -- --test-threads=1`
      - `1 passed`
  - Added the first executable `error E:` / `fail` / `E::wrap` standard API
    contract:
    - `tests/yulang/regressions/runtime/error_wrap_fail.yu` covers
      `fail (E::variant payload)` closed by `E::wrap` through the public
      CLI/std route;
    - manifest public-signature coverage now fixes `small_err.wrap` as an
      effectful action boundary, so the action argument keeps `[small_err; 'e]`
      instead of being forced before the generated catch;
    - the cause-side fix is in synthetic error helper generation: generated
      `wrap` / `up` helpers annotate their `action` parameter with the error
      row they catch, and role associated effect rows lower as exact role args
      instead of subtractable placeholder vars.
  - Validation:
    - `timeout 240s cargo test -q -p yulang run_control_source_text_with_embedded_std_runs_user_error_fail_wrap -- --test-threads=1`
      - `1 passed`
    - `cargo run -q -p yulang -- --std-root lib --no-cache run --print-roots notes/bugs/solved/finalize_error_wrap_fail_breaks.yu`
      - `run roots [result::err(fs_err::not_found("missing"))]`
    - `cargo run -q -p yulang -- --std-root lib --no-cache run --print-roots tests/yulang/regressions/runtime/error_wrap_fail.yu`
      - `run roots [(result::ok(7), result::err(small_err::nope(3)))]`
  - Extended the error standard API contract to `from` aggregation and `E::up`:
    - `tests/yulang/regressions/runtime/error_from_wrap.yu` covers a wider
      `io_err::wrap` catching a narrower `fs_err` and returning the
      `io_err::fs` variant;
    - `tests/yulang/regressions/runtime/error_up_wrap.yu` covers `io_err::up`
      lifting the narrower error effect before `io_err::wrap` closes it;
    - manifest public-signature cases now fix `io_err.wrap` and `io_err.up`
      so aggregation/lifting keeps the source and residual effect rows visible.
  - Validation:
    - `cargo run -q -p yulang -- --std-root lib --no-cache run --print-roots tests/yulang/regressions/runtime/error_from_wrap.yu`
      - `run roots [result::err(io_err::fs(fs_err::not_found("missing")))]`
    - `cargo run -q -p yulang -- --std-root lib --no-cache run --print-roots tests/yulang/regressions/runtime/error_up_wrap.yu`
      - `run roots [result::err(io_err::fs(fs_err::not_found("missing")))]`
  - Added generated `Display E` lowering and public runtime contracts:
    - `error` synthetic lowering now registers a generated `Display` impl
      alongside `Throw`, `wrap`, and `up`;
    - the generated `show` body is lowered through the ordinary role impl
      method path, so receiver typing and payload `.show` constraints use the
      existing role machinery instead of a separate display shortcut;
    - normal payload variants render `variant: payload`, unit variants render
      `variant`, tuple payloads render `variant(a, b)`, and `from` variants
      delegate to the wrapped error's `Display`;
    - `tests/yulang/regressions/runtime/error_display.yu` covers unit,
      generic, and tuple payload display;
    - `tests/yulang/regressions/runtime/error_display_from_wrap.yu` covers
      `io_err::wrap` returning a wider error and user code displaying the
      `err e` branch.
  - Validation:
    - `cargo run -q -p yulang -- --std-root lib --no-cache run --print-roots notes/bugs/solved/error_display_impl_missing.yu`
      - `run roots ["boom: x"]`
    - `cargo run -q -p yulang -- --std-root lib --no-cache run --print-roots notes/bugs/solved/error_display_from_chain_missing.yu`
      - `run roots ["not_found: x"]`
    - `cargo run -q -p yulang -- --std-root lib --no-cache run --print-roots notes/bugs/solved/wrap_does_not_traverse_from_chain.yu`
      - `run roots ["err: not_found: /missing"]`
    - `cargo run -q -p yulang -- --std-root lib --no-cache run --print-roots tests/yulang/regressions/runtime/error_display.yu`
      - `run roots [("item: 7", "empty", "pair(3, x)")]`
    - `cargo run -q -p yulang -- --std-root lib --no-cache run --print-roots tests/yulang/regressions/runtime/error_display_from_wrap.yu`
      - `run roots ["err: not_found: /missing"]`
    - `timeout 240s cargo test -q -p yulang public_contract_manifest_cli_cases_hold --test cli -- --test-threads=1`
      - `1 passed`, finished in 119.92s
    - `cargo check -q -p yulang`
      - passed
    - `cargo fmt --all -- --check`
      - passed
    - `git diff --check`
      - passed
  - Started the native CLI host contract for `std::io::file` on the default
    evidence VM route:
    - the evidence VM host bridge now handles `std::io::file::file` requests
      for `read_at`, `write_at`, `open_text_raw`, `file_get`, `file_set`,
      `file_flush`, `exists`, `is_file`, and `is_dir`;
    - `std::io::file::read_text` / `write_text` no longer escape as unhandled
      effects in normal `yulang run`;
    - `read_at` / `write_at` currently back the full-text wrappers and do not
      yet promise partial range semantics;
    - `open_text` / `open` and path-payload display are still follow-up
      standard API surface work.
  - Validation:
    - `timeout 240s cargo test -q -p yulang compatible_run_std_file_read_write_text_host_contract --test cli -- --test-threads=1`
      - `1 passed`, finished in 3.01s
  - Added the path-display bridge needed by typed file errors:
    - `BytesToPath` / `PathToBytes` now execute in mono runtime, control VM,
      and evidence VM using the current string-backed path value model;
    - `std::text::path` now implements `Display` through `path.to_bytes` and
      UTF-8 lossy text conversion;
    - `tests/yulang/regressions/runtime/path_display.yu` fixes the public
      byte-conversion/display roundtrip;
    - a CLI temp-file test fixes `std::io::file::io_err::wrap` on a missing
      file showing `not_found: <path>`.
  - Validation:
    - `cargo check -q -p mono-runtime -p control-vm -p evidence-vm -p yulang`
      - passed
    - `timeout 240s cargo test -q -p yulang compatible_run_std_file --test cli -- --test-threads=1`
      - `2 passed`
    - `cargo run -q -p yulang -- --std-root lib --no-cache run --print-roots tests/yulang/regressions/runtime/path_display.yu`
      - `run roots [("/tmp/yulang-path", "/tmp/yulang-path")]`
    - `cargo run -q -p yulang -- --std-root lib --no-cache run --control-vm --print-roots tests/yulang/regressions/runtime/path_display.yu`
      - `run roots [("/tmp/yulang-path", "/tmp/yulang-path")]`
    - `cargo run -q -p yulang -- --std-root lib --no-cache run --interpreter --print-roots tests/yulang/regressions/runtime/path_display.yu`
      - `run roots [("/tmp/yulang-path", "/tmp/yulang-path")]`
    - `timeout 240s cargo test -q -p yulang public_contract_manifest_cli_cases_hold --test cli -- --test-threads=1`
      - `1 passed`, finished in 125.05s
    - `cargo fmt --all -- --check`
      - passed
    - `git diff --check`
      - passed
  - Extended wasm playground diagnostics over the catch syntax shared
    fixtures:
    - `catch_missing_scrutinee.yu`, `catch_missing_arm_pattern.yu`, and
      `catch_missing_arm_body.yu` are now included by the playground
      diagnostics tests;
    - the test asserts code, severity, primary range, message, hint, and empty
      related ranges for all three fixtures.
  - Validation:
    - `timeout 240s cargo test -q -p wasm check_inner_returns_catch_syntax_codes_and_ranges -- --test-threads=1`
      - `1 passed`, finished in 7.94s
    - `cargo fmt --all -- --check`
      - passed
  - Tightened the public contract manifest diagnostics gate:
    - every `kind = "check"` case now must assert diagnostic count, code,
      severity, a complete primary byte range, and related diagnostic count;
    - non-check cases cannot use source-diagnostic primary range expectations;
    - related origin expectations must stay aligned with the expected related
      diagnostic count.
  - Validation:
    - `timeout 240s cargo test -q -p yulang public_contract_manifest_cli_cases_hold --test cli -- --test-threads=1`
      - `1 passed`, finished in 158.48s
    - `cargo fmt --all -- --check`
      - passed
  - Extended LSP diagnostics over the same catch syntax fixtures:
    - server diagnostics tests now include missing catch scrutinee, missing
      catch arm pattern, and missing catch arm body;
    - the test asserts LSP code, severity, range, message, hint text, and empty
      related information for all three fixtures.
  - Validation:
    - `timeout 240s cargo test -q -p yulang server::tests::diagnostics_use_catch_syntax_ranges -- --test-threads=1`
      - `1 passed`
    - `timeout 240s cargo test -q -p yulang server::tests::diagnostics_use_ -- --test-threads=1`
      - `9 passed`
    - `cargo fmt --all -- --check`
      - passed
    - `git diff --check`
      - passed
  - Added LSP diagnostic hover coverage for catch syntax:
    - hovering the `->` range in `catch_missing_arm_body.yu` now returns the
      diagnostic code, user-facing message, and hint text.
  - Validation:
    - `timeout 240s cargo test -q -p yulang server::tests::hover_for_source_reports_catch_diagnostic_summary -- --test-threads=1`
      - `1 passed`
    - `timeout 240s cargo test -q -p yulang server::tests::hover_for_source_reports_diagnostic_summary -- --test-threads=1`
      - `1 passed`
    - `cargo fmt --all -- --check`
      - passed
  - Added `std::data::result` propagation coverage:
    - `tests/yulang/regressions/runtime/result_propagation.yu` checks that
      `map` and `and_then` propagate `err` unchanged and `unwrap_or` keeps
      the `ok` value;
    - the case is registered in `tests/yulang/cases.toml` as a
      runtime/standard-api/result contract.
  - Validation:
    - `cargo run -q -p yulang -- --std-root lib --no-cache run --print-roots tests/yulang/regressions/runtime/result_propagation.yu`
      - `run roots [(result::err("bad"), result::err("bad"), 5)]`
    - `timeout 240s cargo test -q -p yulang public_contract_manifest_cli_cases_hold --test cli -- --test-threads=1`
      - `1 passed`, finished in 160.49s
    - `cargo fmt --all -- --check`
      - passed
  - Tightened public-signature manifest shape:
    - every `kind = "public-signature"` case now must carry exact
      `expect_type`, preventing substring-only public type checks from entering
      the contract manifest;
    - `tests/yulang/README.md` documents this as part of the public signature
      golden policy.
  - Validation:
    - `timeout 240s cargo test -q -p yulang public_contract_manifest_cli_cases_hold --test cli -- --test-threads=1`
      - `1 passed`, finished in 161.22s
    - `cargo fmt --all -- --check`
      - passed
  - Tightened runtime-error manifest classification:
    - runtime error cases now carry either `runtime-failure` or
      `compile-error`;
    - manifest validation requires that subtype/method compile failures stay
      distinguishable from runtime failures.
  - Validation:
    - `timeout 240s cargo test -q -p yulang public_contract_manifest_cli_cases_hold --test cli -- --test-threads=1`
      - `1 passed`, finished in 160.77s
    - `cargo fmt --all -- --check`
      - passed
  - Tightened the runtime-error tag policy further:
    - `runtime-error` cases now must carry exactly one of `runtime-failure` or
      `compile-error`;
    - `compile-error` cases must also carry `diagnostics`, keeping them visible
      as future candidates for structured source diagnostics.
  - Validation:
    - `timeout 240s cargo test -q -p yulang public_contract_manifest_cli_cases_hold --test cli -- --test-threads=1`
      - `1 passed`, finished in 161.46s
    - `cargo fmt --all -- --check`
      - passed
  - Aligned runtime-error tags with stderr expectations:
    - `compile-error` cases must assert a `compile error [` prefix;
    - `runtime-failure` cases must assert a `runtime error [` prefix.
  - Validation:
    - `timeout 240s cargo test -q -p yulang public_contract_manifest_cli_cases_hold --test cli -- --test-threads=1`
      - `1 passed`, finished in 160.25s
    - `cargo fmt --all -- --check`
      - passed
  - Recorded the remaining role/method diagnostics boundary:
    - unresolved / ambiguous role method failures are currently covered as
      `run` compile-error contracts;
    - they do not yet flow through `SourceDiagnostic` for `check` / LSP /
      playground, so `compile-error` manifest tags mark the future migration
      target.
  - Added public-signature manifest coverage for the executable
    `std::io::file` first slice:
    - `read_at`, `write_at`, `read_text`, and `write_text` keep both
      `std::io::file::file` and `std::io::file::io_err` effects visible;
    - low-level host predicates `file.exists`, `file.is_file`, and
      `file.is_dir` keep the `file` effect visible;
    - `io_err.wrap` fixes the typed failure boundary for file operations;
    - managed ref functions `open_text`, `open`, and `open_in` stay outside
      the manifest until their resource/lifetime behavior is executable.
  - Validation:
    - `timeout 240s cargo test -q -p yulang public_contract_manifest_cli_cases_hold --test cli -- --test-threads=1`
      - `1 passed`, finished in 148.00s
    - `git diff --check`
      - passed
  - Promoted basic managed file text refs into the executable contract:
    - `compatible_run_std_file_open_text_ref_host_contract` covers direct
      `open_text` get/set and `open_in path: \&txt -> ...` scoped callback
      get/set against temp files;
    - public-signature manifest cases now cover `open_text`, `open`, and
      `open_in`;
    - metadata, directory listing, range writes, locking, explicit close, and
      scope-exit write-back semantics remain provisional.
  - Validation:
    - `timeout 240s cargo test -q -p yulang compatible_run_std_file --test cli -- --test-threads=1`
      - `3 passed`, finished in 9.14s
    - `timeout 240s cargo test -q -p yulang public_contract_manifest_cli_cases_hold --test cli -- --test-threads=1`
      - `1 passed`, finished in 156.54s
    - `cargo fmt --all -- --check`
      - passed
    - `git diff --check`
      - passed
  - Wired the public contract manifest into `scripts/hardening-smoke.sh`:
    - hardening now runs `public_contract_manifest_cli_cases_hold` by default;
    - `YULANG_HARDENING_PUBLIC_CONTRACT_MANIFEST=0` can skip it for local
      focused runs.
  - Validation:
    - `bash -n scripts/hardening-smoke.sh`
      - passed
    - `timeout 240s cargo test -q -p yulang public_contract_manifest_cli_cases_hold --test cli -- --test-threads=1`
      - `1 passed`, finished in 157.43s
    - `git diff --check`
      - passed
  - Tightened `tests/yulang/cases.toml` as a contract index:
    - the manifest runner now requires `run` cases to carry a
      runtime-facing tag, `check` cases to carry `diagnostics`, and
      `public-signature` cases to carry `public-signature`;
    - `tests/yulang/README.md` documents the tag/kind rule so future contract
      entries stay searchable by surface.
  - Validation:
    - `timeout 240s cargo test -q -p yulang public_contract_manifest_cli_cases_hold --test cli -- --test-threads=1`
      - `1 passed`, finished in 157.72s
    - `cargo fmt --all -- --check`
      - passed
    - `git diff --check`
      - passed
  - Updated `spec/2026-07-01-file-resource-api.md` with the current
    executable first slice:
    - documented path byte conversion/display, native CLI text read/write,
      file predicates, typed `io_err::wrap`, and basic `open_text`/`open_in`
      text refs as the observed contract;
    - kept range semantics, metadata shape, directory listing, locking,
      scope-exit write-back, unsupported-host behavior, and non-UTF-8 path
      policy explicitly provisional;
    - stated that `read_text` / `write_text` remain executable helpers, not the
      center of the stable file resource API.
  - Validation:
    - `git diff --check`
      - passed
  - Tightened the release artifact gate around the language server:
    - `scripts/hardening-smoke.sh` now enables the `release-smoke.sh`
      `yulang server` startup check by default via
      `YULANG_SMOKE_SERVER=1`;
    - `YULANG_HARDENING_RELEASE_SERVER_SMOKE=0` can skip only that server
      startup check for focused local runs;
    - `README.md` documents that the hardening gate covers the public
      contract manifest and release server startup.
  - Validation:
    - `bash -n scripts/hardening-smoke.sh`
      - passed
    - `YULANG_SMOKE_SERVER=1 YULANG_SMOKE_SERVER_TIMEOUT=2s timeout 120s scripts/release-smoke.sh target/debug/yulang`
      - `release smoke ok: target/debug/yulang`
    - `git diff --check`
      - passed
  - Extended playground/wasm diagnostic surface coverage:
    - `check_inner_returns_diagnostic_code_and_value_name_range` now reuses
      `tests/yulang/regressions/diagnostics/unresolved_value_name.yu` and
      fixes code, severity, label, range, message, and hint;
    - `check_inner_returns_top_level_mutable_binding_code_and_range` now reuses
      `tests/yulang/regressions/diagnostics/top_level_mutable_binding.yu` and
      fixes code, severity, label, range, message, and hint;
    - this keeps more manifest diagnostics visible through the playground
      payload path instead of only the CLI contract path.
  - Validation:
    - `timeout 240s cargo test -q -p wasm check_inner_returns_ -- --test-threads=1`
      - `8 passed`, finished in 22.86s
    - `cargo fmt --all -- --check`
      - passed
    - `git diff --check`
      - passed
    - failed command, retried with one filter:
      - `timeout 240s cargo test -q -p wasm check_inner_returns_diagnostic_code_and_value_name_range check_inner_returns_top_level_mutable_binding_code_and_range -- --test-threads=1`
      - cargo rejected multiple test filters.
  - Added a manifest-level public-signature cleanliness gate:
    - every `public-signature` case now rejects `#` and `AllExcept` fragments
      in the projected public type even when the individual case forgets
      `deny_type_contains`;
    - `tests/yulang/README.md` documents the common private-evidence leak
      check and keeps surface-specific deny checks available.
  - Validation:
    - `timeout 240s cargo test -q -p yulang public_contract_manifest_cli_cases_hold --test cli -- --test-threads=1`
      - `1 passed`, finished in 158.39s
    - `cargo fmt --all -- --check`
      - passed
    - `git diff --check`
      - passed
  - Added `spec/2026-07-02-server-resource-api.md` as the server API anchor:
    - keeps the current executable slice honest: release artifact
      `yulang server` startup and LSP structured surfaces are covered, while
      `std::server` remains absent from `cases.toml`;
    - fixes the center as host event session plus request/response resource,
      not an HTTP framework;
    - records `accept` as a continuation suspend/resume boundary, request
      response as one-shot capability consumption, and host-boundary values as
      requiring an explicit wire codec;
    - links the new spec from `spec/README.md`,
      `spec/2026-07-01-stable-standard-api.md`, and `docs/status.md`.
  - Validation:
    - `test -f spec/2026-07-02-server-resource-api.md`
      - passed
    - `git diff --check`
      - passed
  - Tightened public contract manifest shape checks:
    - `runtime-error` tags now require `expect_success = false`;
    - `public-example` tags now require repo-root `examples/` fixtures;
    - `standard-api` tags now require a narrower API area tag such as
      `result`, `errors`, `path`, or `file`;
    - `tests/yulang/README.md` documents these tag shape rules.
  - Validation:
    - `timeout 240s cargo test -q -p yulang public_contract_manifest_cli_cases_hold --test cli -- --test-threads=1`
      - `1 passed`, finished in 155.58s
    - `cargo fmt --all -- --check`
      - passed
    - `git diff --check`
      - passed
  - Extended diagnostic manifest assertions with primary ranges:
    - the manifest schema now accepts `expect_diagnostic_start` and
      `expect_diagnostic_end`;
    - stable diagnostics for type mismatch, unresolved type/value names,
      unsupported type syntax, top-level mutable binding, rule lazy
      quantifier, and syntax errors now fix their structured primary byte
      range;
    - catch syntax diagnostics remain code/message-gated until their primary
      range is deliberately fixed.
  - Validation:
    - `timeout 240s cargo test -q -p yulang public_contract_manifest_cli_cases_hold --test cli -- --test-threads=1`
      - `1 passed`, finished in 157.71s
    - `cargo fmt --all -- --check`
      - passed
    - `git diff --check`
      - passed
  - Tightened release archive smoke around `yulang server`:
    - `scripts/release-archive-smoke.sh` now invokes `release-smoke.sh` with
      `YULANG_SMOKE_SERVER=1` by default;
    - `YULANG_RELEASE_ARCHIVE_SMOKE_SERVER=0` can disable only that archive
      server startup check for local archive inspection;
    - `README.md` documents the archive smoke server check alongside the
      hardening release server smoke.
  - Validation:
    - `bash -n scripts/release-archive-smoke.sh`
      - passed
    - `YULANG_SMOKE_SERVER=1 YULANG_SMOKE_SERVER_TIMEOUT=2s timeout 120s scripts/release-smoke.sh target/debug/yulang`
      - `release smoke ok: target/debug/yulang`
    - `git diff --check`
      - passed
    - not run:
      - a full `scripts/release-archive-smoke.sh <archive>` because this slice
        did not build a release archive.
  - Fixed catch syntax diagnostics to carry structured primary ranges:
    - missing catch scrutinee points at the `catch` keyword;
    - missing catch arm pattern and body point at the arm `->`;
    - the public contract manifest now asserts these ranges alongside the
      existing codes, messages, severity, and related-count checks.
  - Validation:
    - `cargo check -q -p infer -p yulang`
      - passed
    - `timeout 240s cargo test -q -p yulang public_diagnostics_check_reports_catch_missing --test cli -- --test-threads=1`
      - `3 passed`
    - `timeout 240s cargo test -q -p yulang public_contract_manifest_cli_cases_hold --test cli -- --test-threads=1`
      - `1 passed`, finished in 158.44s
    - `cargo fmt --all -- --check`
      - passed
    - `git diff --check`
      - passed
  - End-of-run validation snapshot:
    - `cargo fmt --all -- --check`
      - passed
    - `timeout 240s cargo test -q -p wasm check_inner_returns_ -- --test-threads=1`
      - `9 passed`, finished in 31.12s
    - `timeout 240s cargo test -q -p yulang server::tests::diagnostics_use_ -- --test-threads=1`
      - `9 passed`
    - `timeout 240s cargo test -q -p yulang public_contract_manifest_cli_cases_hold --test cli -- --test-threads=1`
      - `1 passed`, finished in 163.05s
    - `timeout 240s cargo test -q -p yulang public -- --test-threads=1`
      - package test filters passed: `9 passed` in lib-side filter and `16 passed` in cli-side filter
    - `timeout 300s cargo test -q -p yulang -- --test-threads=1`
      - timed out after the library suite passed `263 passed`; timeout happened while the CLI test binary was still running
    - `timeout 600s cargo test -q -p yulang --test cli -- --test-threads=1`
      - `98 passed`, finished in 269.22s
    - `YULANG_SMOKE_SERVER=1 YULANG_SMOKE_SERVER_TIMEOUT=2s timeout 120s scripts/release-smoke.sh target/debug/yulang`
      - `release smoke ok: target/debug/yulang`
    - `timeout 240s scripts/source-unit-cache-smoke.sh target/debug/yulang`
      - `source unit cache smoke ok: target/debug/yulang`
    - `cargo check -q -p yulang -p wasm`
      - passed
    - `timeout 300s cargo check -q --release -p yulang -p wasm`
      - passed
    - `timeout 600s tests/yulang/yulang-adversarial-corpus/probe.sh`
      - all adversarial probes passed
  - Synced `docs/status.md` with the current public contract spine:
    - exact public-signature manifest expectations;
    - structured `check` diagnostic manifest requirements;
    - `std::data::result` error propagation coverage;
    - the remaining role/method `SourceDiagnostic` gap.
  - Tightened manifest tag spelling:
    - every contract tag must be lowercase ASCII with digits, `.`, or `-`;
    - `tests/yulang/README.md` documents the canonical tag shape.
  - Validation:
    - `timeout 240s cargo test -q -p yulang public_contract_manifest_cli_cases_hold --test cli -- --test-threads=1`
      - `1 passed`, finished in 160.92s

## Resource lifetime spec alignment

- Context:
  - Claude (Fable 5) and the user fixed the file/server resource lifetime
    decisions in `notes/design/2026-07-02-resource-lifetime-decisions.md`.
  - The file and server API specs still contained older wording around value
    liveness, scope-exit lock release, and unspecified multi-shot `accept`.
- Implementation summary:
  - Updated `spec/2026-07-01-file-resource-api.md` to make handler extent, not
    value liveness or GC reachability, the unscoped resource lifetime boundary.
  - Documented managed lens as a snapshot transaction: scope-entry read,
    branch-local edits, commit only on normal scope exit, abort as rollback,
    and multi-shot branch commits with last-write-wins ordering.
  - Clarified that first-slice lock release may stay tied to filesystem handler
    discharge until evidence VM continuation-drop hooks exist.
  - Updated `spec/2026-07-02-server-resource-api.md` to reject `server.accept`
    inside multi-shot resume regions until a distinct server multi-shot
    capability is designed.
  - Added the file/server asymmetry: file resources can be transactional under
    multi-shot continuation, while server responses are one-shot external
    capabilities.
  - Propagated the same resource lifetime rules into
    `spec/2026-07-01-stable-standard-api.md` and clarified `docs/status.md`
    so the implementation gap is described as executable lowering/locking, not
    an unsettled meaning.
- Validation:
  - `git diff --check`
    - passed

## Host act FFI decision record

- Context:
  - Claude (Fable 5) and the user fixed the first-layer FFI direction as
    host-provided act handlers rather than direct native ABI exposure.
  - The new decision amends the earlier server `accept` multi-shot enforcement:
    `accept` itself is a suspend multi-shot host operation, while user-level
    suspend operations inside multi-shot regions are left to cheap dynamic
    checks or documented unspecified behavior.
- Implementation summary:
  - Added `notes/design/2026-07-02-host-act-ffi-decisions.md` as the
    authoritative host act FFI decision record.
  - Updated `notes/design/2026-07-02-resource-lifetime-decisions.md` so its
    server `accept` decision points at the FFI document's three-layer
    enforcement rule instead of the older typed-failure wording.
  - Aligned `spec/2026-07-02-server-resource-api.md` and
    `spec/2026-07-01-stable-standard-api.md` with the new operation-tier
    terminology: `server.accept` is suspend multi-shot, request response is
    one-shot, and user-level suspend/multi-shot composition remains outside the
    stable guarantee.
- Validation:
  - `git diff --check`
    - passed

## Contract v0 completion evidence and static proof direction

- Context:
  - External review kept repeating the same generic "make Contract v0 real"
    recommendation even after the manifest, diagnostics, public signatures, and
    release gates landed.
  - Claude (Fable 5) separately argued that the current Evidence VM proof
    system is correct but too dynamic: route/cert checks that Koka-style
    evidence passing would discharge statically are still replayed at runtime.
- Implementation summary:
  - Added `docs/language/contract-v0-evidence.md` as the evidence page for the
    current Contract v0 spine. It records that `stable-core` now has 51
    executable manifest cases: 24 `run`, 12 `check`, and 15
    `public-signature`.
  - Linked the evidence page from `docs/language/stable-core.md` and
    `docs/status.md`, and made the reviewer rule explicit: future requests to
    "finish Contract v0" must name a missing fixture, public signature,
    structured diagnostic, or release gate.
  - Recorded that file transactions, server resources, and host act FFI are
    next contract slices, not missing Contract v0 evidence.
  - Added Claude's `notes/design/2026-07-02-speedup-proof-system.md` and
    `notes/design/2026-07-02-static-route-promotion-plan.md`.
  - Updated `tasks/current.md` and `docs/status.md` so future Evidence VM
    performance work starts with the static route promotion Stage 0 coverage
    pass instead of adding more signal-by-signal dynamic cert checks.
- Validation:
  - `timeout 240s cargo run -q -p yulang -- --std-root lib contract --contract stable-core tests/yulang/cases.toml`
    - `contract cases ok: 51`
  - `git diff --check`
    - passed

## IO resource API specification

- Context:
  - Claude (Fable 5) and the user promoted the file / connect / serve API
    sketch into an authoritative IO resource API specification.
  - The spec derives the API surface from the resource lifetime and host act
    FFI decisions: `host act + session + managed view + raw`.
- Implementation summary:
  - Added `spec/2026-07-02-io-resource-api.md` as the current authoritative
    standard IO resource API spec.
  - Kept `notes/design/2026-07-02-io-api-sketch.md` as the discussion record
    and pointed it at the promoted spec.
  - Updated file/server resource specs, the stable standard API spec,
    `spec/README.md`, and `docs/status.md` so the integrated IO spec takes
    precedence where surfaces conflict.
  - Recorded the key accepted surface decisions:
    - `file::meta` does not fail; missing / denied are metadata `kind` values;
    - file managed lens remains a snapshot transaction;
    - connect can be used as a value, an ambient channel handler, or a chain;
    - serve has a first-class listener, concurrent `accept`, sequential
      `requests`, and a one-line `net::serve` spelling;
    - close / disconnect / flush / save remain outside the managed API.
- Validation:
  - `git diff --check`
    - passed
