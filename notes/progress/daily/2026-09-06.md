# 2026-09-06

## Direct-rewrite Item emission ownership N0c attempt

The known L5a ordinary-path assertion was repaired by restoring the original
ordinary parenthesized/call/index/dot/path owners at the shared dispatch
points. L5a alone continues through the complete-Item owners and retains its
`Deferred` propagation. Independent compiler/recovery review found no
ordinary route into an L5a-only assertion.

N0c then exposed one omitted N0a migration in the `if` condition owner. The
pre-sealing code emitted scanned leading trivia and cleared the same Item's
raw leading field before parsing the condition. The sealed implementation now
constructs that Item and consumes its leading frontier once before opening
`Condition`; this preserves the original token placement and recovery order
without detached or physical Item mutation. Independent compiler/recovery
review approved the repair and found no sibling scan/emit/Item-transfer shape.

Focused evidence is green: direct lexical (30), owners (23), tails (11),
struct declaration (11), type declaration (31), and use declaration (10).

The required final `cargo test -p yu-syntax` is not green: 903 passed, 3
failed, 2 ignored. The remaining failures are in the independent old
`grammar/` tree, while the candidate diff changes only `rewrite/if_expr.rs`:

- `grammar::declaration::companion::tests::gate3_isolated_companion_form_recovery_and_state_table`;
- `grammar::declaration::tests::struct_named_field_sequence_owns_leading_and_repeated_empty_comma_slots`;
- `grammar::expression::tests::if_and_braced_mandatory_rejections_restore_error_sink`.

N0c remained blocked until those failures were causally isolated and repaired.
They were not direct-rewrite frontier failures.

## N0c legacy-grammar closure

The three independent failures were repaired at their owning sites:

- the shared named-field driver now probes a pending comma but emits an empty
  slot before consuming that comma, retaining the Struct/Enum/Error topology;
- every failed Call argument chain probe restores its ErrorSink checkpoint.
  A non-embedded canonical Statement republishes only an accepted retry's
  first invalid character, preserving the established nested companion
  expectation; an active EmbeddedYulang frame records only its canonical fact;
- the `if` companion frame still begins before condition recognition and stays
  live through body/continuation ownership. After a recovered initial missing
  condition closes that frame, its opaque allocation is released so the legacy
  local-state snapshot remains literal.

M2 compiler/recovery and specification delta reviews approved the final Call
ownership/transaction repair. Focused controls covered the Call wrapper,
embedded Yumark bridge, declaration-companion nested expectation, missing-if
state restoration, and named-field empty slots. The designated final
`cargo test -p yu-syntax` passed: 906 passed, 0 failed, 2 ignored. No
performance measurement was warranted; the repair adds no traversal, replay,
cache, or allocation path.

<!-- daily-append-anchor: 2026-09-06 -->

## Fenced current-Item normalization N1

N1 isolated normalized lexical kernel is complete. `rewrite/current_item.rs`
owns one transactional current-Item construction: a call-local `LineEntry`,
one physical-leading accumulator, lazy per-Item split storage, physical-line
fence decisions, and exactly one final Item construction. A boundary skips the
payload callback and remains the unchanged next acquisition; EOF reports the
terminal in-line fact while close/transition retain physical-start.

The fenced block-comment scanner is now the constructor's compiled private
lexical dependency rather than a test-only wrapper. `operator.rs` has a pure
fence-aware source observer for dynamic operator and follower evidence. Its
`None` branch retains the previous raw-trivia scanner, so ordinary operators do
not gain fence coordinates, fragment allocation, or a different scan path.
No production fence dispatch or grammar owner uses the new capability yet.

Focused controls passed: lexical 38, dynamic operators 11, and Rule 21;
`cargo check -p yu-syntax` also passed. The N1 implementation adds no cache,
source root, retained cursor/context, replay, parser state, Rowan effect, or
performance measurement requirement. N2 remains responsible for the first
ledgered Pratt/tail/delimited migration.

## N2 expression payload seam

The normalized constructor now has direct raw payload functions for expression
NUD, LED, and path-segment Items. Dynamic operator selection receives the
immediate payload coordinate and fence capability, while the payload functions
remain free of Rowan and grammar recovery effects. The existing ordinary Item
scanners now reuse this same raw vocabulary rather than retaining duplicate
token/operator branches. An accepted operator can
observe a following fence boundary as local EOF without consuming that
boundary.

The constructor itself now materializes ordinary physical EOF after its owned
leading trivia, rather than requiring an impossible EOF variant from
`CurrentPayload`. Optional NUD callers remain able to roll this result back in
their enclosing lexical transaction when expression absence is required.

No production grammar branch or owner-local fenced wrapper was added, so no N2
frontier ledger row is open yet. Focused lexical (40), operator (11), and owner
(23) tests passed. `cargo check -p yu-syntax` also passed. The next slice connects the
Pratt/tail/delimited owner body to this seam and deletes the superseded
`L5aExit` / `CompleteItemSite` callback layer atomically.

## N2 normalized Pratt core

The Pratt core now consumes NUD/LED Items through `current_item` and transports
only immediate `item_origin`, `LineEntry`, and `Option<&FenceBoundary>` function
arguments. Prefix/infix/suffix recursion and ML arguments synchronize their
coordinate from the live child-entry and child-return suffix markers. No
cursor, context object, retained source root, parser trait, request enum, or
forwarded acquisition callback was introduced.

The superseded `L5aExit`, `CompleteItemSite`, generic acquisition callback, and
their duplicate tails/delimited functions were removed. The temporary
`NormalizedExit` is exactly `Complete(TailExit, LineEntry) |
Deferred(Item, LineEntry)`. Its open frontiers are recorded in
`tasks/current.md`; only those fenced branches defer before entering an
unmigrated direct-rewrite child. Ordinary callers enter the same grammar body
with `None` and retain the existing children.

Ten focused fenced controls cover exact boundary handoff, infix RHS parsing,
mandatory prefix-RHS Missing ownership, and every currently ledgered deferred
owner. Ordinary operator (11), tails (11), owners (23), case-like (12), and if
(8) controls passed, as did `cargo check -p yu-syntax`. The static performance
bound is unchanged: no replay, cache, retained carrier, or extra traversal was
added. The M2 phase-boundary `cargo test -p yu-syntax --lib` candidate also
passed: 926 passed, 0 failed, 2 ignored. The next slice normalizes fixed call/index/dot/path tails and the shared
delimited owner, closing their ledger rows.

The coherent commit remains pending because the current sandbox rejects
creation of `.git/index.lock` as read-only; all worktree changes are retained.

## N2 fixed tails and shared delimiters

The fixed expression tails and their complete shared-delimiter cone now use
the same normalized grammar body in ordinary and fenced calls. This includes
call, index, field, path, tuple/record projection, record-spread items, local
delimiter recovery, and parenthesized NUD ownership. The temporary frontier
rows for parenthesized expressions and fixed tails were deleted atomically;
the remaining rows are the statement-owning case/catch, if, braced, with, and
colon paths.

The first delimiter item's baseline is obtained through the existing pure
fence-aware trivia observer before the one `current_item` acquisition. Every
later Item is acquired once. Child owners and the adjacent operator-shaped
record-spread recovery synchronize only from live suffix pointer/length
deltas. No cursor, context/state object, parser trait, request enum, replay,
stored callback, or ordinary/fenced grammar duplicate was added.

Thirteen fenced controls cover exact parenthesized handoff, `x[a b(c)]` with
nested call/index and ML application, field/path tails, tuple and record
projections, record spread, line-by-line quote-prefix streaming through nested
delimiters, and mandatory close recovery without consuming or emitting the
boundary. Ordinary owners (23), tails (11), and operators (11) passed, as did
`cargo check -p yu-syntax`, `cargo fmt --all -- --check`, and
`git diff --check`. The 926-test M2 phase-boundary run immediately preceded
this delta, so the broad suite was not repeated. No performance measurement
was warranted: the slice adds no retained structure, allocation loop, replay,
or additional grammar traversal.

An independent specification delta audit reported no findings. Its closure
scope was parenthesized NUD, fixed call/index/dot/path tails, tuple/record
projections, record-spread recovery, and the shared delimiter owner. Pattern,
TypeExpression, remaining statement owners, multiline literals, the terminal
Yumark adapter, and the full-package regression surface remain outside that
audit.

The coherent commit is still pending because `.git/index.lock` is read-only in
the current sandbox. The next N2 slice is the Pattern and TypeExpression raw
payload seam and normalized owner migration.

## N2 Pattern and TypeExpression payload seam

Pattern and TypeExpression now expose direct raw NUD and successor payload
functions for `current_item`, and their ordinary scanners reuse those same
functions. Pattern preserves the primary-only `:identifier` Symbol priority
and active arrow-stop classification. Type preserves its deliberate
asymmetry: the optional top-level entry transaction accepts only a valid Type
NUD, while mandatory and recovery NUD acquisition remains total and returns
unknown, separator, and close Items to the grammar owner.

The first parity run caught an overly narrow mandatory Type NUD implementation
because `@Int` was incorrectly treated as EOF. The fix moved the valid-kind
filter back inside only the optional lexical transaction; Pattern (12) and
TypeExpression (49) ordinary controls then passed, along with the focused
fenced raw-vocabulary control and `cargo check -p yu-syntax`. No production
grammar branch or Deferred frontier was added, so the open ledger is
unchanged. TypeExpression normalization comes before Pattern because Pattern's
type-annotation tail depends on it.

## N2 normalized TypeExpression core

The optional TypeExpression entry and its bare-primary, path, spaced
application, arrow, and local retry paths now share one normalized grammar
body. Each successor is constructed once from the Type-specific raw payload
vocabulary. Recursive Type application advances its immediate coordinate only
from the live suffix pointer/length delta; no root source, cursor, retained
carrier, parser wrapper, or acquisition callback was added.

Boundary Items are handled before Type tail classification and before every
retry emission. A boundary in a mandatory path-segment or arrow-RHS slot
causes exactly that owner's Missing node and is returned unchanged, including
its `LineEntry`; it is never placed inside Error or emitted as trivia. The
superseded ordinary path/application/arrow bodies were deleted rather than
kept as fenced duplicates.

Four temporary frontier rows now cover only the unmigrated compound Type
owners: the shared delimiter cone, named record, forall, and polymorphic
variant. Six new fenced controls bring the normalized set to 19 and cover the
complete `List::Result Arg -> Out` chain, mandatory arrow recovery at a fence,
and every new Deferred site. All 49 ordinary TypeExpression controls passed,
as did `cargo check -p yu-syntax`, formatting, and `git diff --check`. The next
slice normalizes the shared delimiter cone and removes that frontier row.

The independent specification audit found one evidence-only gap: the initial
Deferred controls did not prove exact suffix preservation or the absence of
zero-width recovery nodes. Every Type head and tail Deferred control now
asserts its exact post-Item suffix and rejects `Error`/`Missing`. The focused
19-test suite passed again, and the audit's delta re-review closed cleanly.

## N2 shared Type delimiter cone

Parenthesized Type groups, adjacent Type calls, leading BracketRows, postfix
bracket-arrow rows, effect rows, and their shared delimiter/retry cone now use
one normalized grammar body. The five corresponding Deferred creator sites
were removed. A nested named record, forall, or polymorphic variant is still
propagated through the existing ledgered frontier without emitting its leading
parts; no other compound Type owner is deferred.

All logical successor Items come from `current_item` and the Type raw payload
vocabulary. Child returns advance the caller's coordinate from the live suffix
delta. The one balanced malformed-head suffix is a direct lexical consumption,
so its return point performs the same explicit live-suffix synchronization.
Boundary checks precede close/separator/retry logic and mandatory recovery
emits only its owned Missing before returning the unchanged boundary.

Focused fenced coverage now contains 26 tests, including all five closed
owners, multiline quote-prefix streaming, mandatory close-at-boundary,
nested Deferred leading preservation, and balanced-head coordinate recovery.
All 49 ordinary TypeExpression tests passed, along with `cargo check -p
yu-syntax`, formatting, and `git diff --check`.

Independent specification review found two blockers. The balanced malformed-
head scanner could cross a fenced close or transition line while seeking `]`,
and delimiter paths created `Deferred` outside the finite ledgered frontier.
The scanner now returns a dedicated complete-or-boundary algebra with accepted
raw text and the exact pending boundary in separate Items, judging every
physical successor line before its outer bytes. Ordinary no-fence scanning
retains its transactional behavior. New Type `Deferred` construction is now
centralized in `defer_type_primary` at the three ledgered primary arms; the
five delimiter acquisition/retry paths only route and propagate before Item
effects. Close, transition, outer-`]`, continued quote-prefix, unclosed block-
comment, and all five routing-position controls passed. The same auditor's
delta review closed both blockers. The next implementation candidate is named
Type record normalization.

## N2 named Type record

The named record field, separator, malformed-name, colon, RHS, and successor
recovery cone now uses one ordinary/fenced normalized grammar body. The `{`
Deferred frontier is removed. Every logical successor Item comes from the Type
normalized current-Item vocabulary. Recursive records parse in place; only the
still-unmigrated `for` and `:{` primaries propagate unchanged before their
leading parts are emitted. The two source-only field probes are local pure
observers and judge each physical line before inspecting its outer bytes.

A fenced coordinate control exposed a stale `item_origin` at child-to-
successor acquisition. The record loop now synchronizes from the live suffix
at that owning call site. Independent specification review found two evidence
gaps but no further production defect: missing-colon and malformed-colon paths
to the remaining Deferred owners, and outer-boundary isolation for the multi-
item malformed-name probe. Four compact Deferred cases and a reduced-quote
transition control closed those gaps; the same auditor approved the test-only
delta.

The ordinary TypeExpression suite passed 49 tests and the focused normalized
suite passed 31, together with `cargo check -p yu-syntax`, formatting, and
`git diff --check`. No broad suite was repeated after the earlier 926-test N2
phase boundary. No timing budget was used: the slice adds no retained state,
carrier, allocation class, replay, or asymptotically new traversal. The next
Type frontier is forall, followed by polymorphic variants.

## N2 forall Type

The forall head, binder, first and continuation separator, colon/body, and
malformed-phase recovery cone now uses one ordinary/fenced normalized grammar
body. The `for` Deferred frontier is removed. All Item acquisition uses the
normalized Type current-Item vocabulary. Nested forall Types parse in place;
the four paths that actually enter a polymorphic-variant child propagate the
remaining `:{` frontier before emitting its leading parts. The malformed-
binder source probe remains local and pure and judges each physical line
before inspecting outer bytes.

Independent specification review adjudicated the producer's two residual
questions. `for :{A}` is malformed first-binder material in both the old and
new grammar, while `for, :{A}` is a normal handoff after the local comma's
recovered-binder episode. Neither opens a polymorphic-variant child, so neither
is a missing Deferred path. The four actual child-entry paths are covered.

Valid and nested streaming, eight CRLF phase-boundary cases, a reduced-quote
transition probe, and four remaining-frontier cases bring the focused
normalized suite to 34 passing tests. All 49 ordinary TypeExpression tests,
`cargo check -p yu-syntax`, formatting, and `git diff --check` also passed.
The independent review approved the slice. No broad suite was repeated after
the earlier 926-test N2 phase boundary. No measurement budget was used because
the slice adds no retained state, carrier, allocation, replay, or additional
grammar traversal; its malformed-span observer preserves the existing linear
probe bound. The polymorphic-variant owner is the final Type frontier.

## N2 polymorphic variants and Type frontier closure

The polymorphic-variant adjacent open, tag-position, separator, payload,
wrong-kind and malformed tag, and malformed payload cone now uses one
ordinary/fenced normalized grammar body. The adjacent `{` and every later
logical Item come through the normalized Type current-Item vocabulary. Nested
variants parse recursively. The final `:{` Deferred frontier is removed.

The closure also removes `defer_type_primary`,
`is_deferred_type_primary`, `dispatch_deferred_type_primary`, and all
frontier-only branches from delimiter, record, and forall owners. Static
inventory finds no `NormalizedExit::Deferred` reference in the Type modules.
A new CRLF reduced-quote control exposed a boundary-first omission in the
malformed-payload child-exit matcher. The owning matcher now returns Boundary
before payload classification or emission.

Independent specification review approved TagPosition Missing transitions,
local/outer separator and close ownership, node closure, child-origin
synchronization, the malformed-payload repair, and the Type-wide frontier
cleanup. Five PV-focused tests cover 19 cases inside the 35-test normalized
suite; all 49 ordinary TypeExpression tests, `cargo check -p yu-syntax`,
formatting, and `git diff --check` also passed. No broad suite was repeated
after the earlier 926-test N2 phase boundary. No measurement budget was used:
the slice adds no retained state, buffer, allocation, replay, rescan, or new
carrier and keeps one current-Item acquisition per logical Item.

The TypeExpression normalized-owner migration is complete. Pattern
normalization is the next N2 dependency.

## N2 normalized Pattern cone

Pattern primary recognition, recovery, alias and alternation tails, Type
annotations, parenthesized/list/record delimiters, and record-default
expressions now share one ordinary/fenced normalized grammar body. Every
logical acquisition uses the Pattern current-Item vocabulary; ordinary
adapters enter the same body with no fence capability.

Pattern constructs no new `NormalizedExit::Deferred`. The three remaining
matches only propagate the existing case/if/braced expression frontiers from
record defaults after closing the Pattern-owned nodes. `PatternCompletion`
retains its prior non-monotone local assignment and merge semantics rather
than becoming parser state. Mandatory annotations enter the normalized
TypeExpression owner directly.

Independent specification and regression reviews approved boundary-first
handling, exact origin and `LineEntry` propagation, node/Missing ownership,
ordinary consumer behavior, completion semantics, and the Type/expression
child bridges. Focused evidence passed: normalized 41, Pattern 12,
TypeExpression 49, for-statement 12, binding 7, and case-like 12, plus
formatting and diff checks. No broad suite was repeated after the earlier
926-test N2 phase boundary. No timing budget was used because the gate adds no
retained state, buffer, allocation, replay, or additional traversal.

Pattern normalized-owner migration is complete. The temporary frontier now
contains only the five statement-owned case/catch, if, braced, with, and colon
rows. A separate quoted-fence observation found that the current prefix judge
absorbs every horizontal byte after the final `>`; whether extra trailing
bytes must instead become Yulang indentation is an authority question because
the parsed-fence and current-Item documents do not define a unique structural
split. No grammar expectation was weakened to hide it.

## N2 normalized case/catch and if owners

Case/catch and if now each use one ordinary/fenced normalized grammar body.
The migrated cones cover labels, scrutinees and conditions, Pattern arms and
Catch handlers, guards, arrows, inline bodies, separators and recovery,
Catch braces, and streamed elsif/else continuations. Direct Pratt dispatch no
longer defers either owner. All logical acquisition uses current-Item or the
normalized expression and Pattern entries.

Only the two ledgered deeper canonical-statement body entries construct new
`Deferred` values. They return the exact acquired Item and `LineEntry` before
entering the still-ordinary statement block; every parent closes only its
already-open node. The existing braced, with, and colon statement frontiers
remain unchanged, so the ledger now has five rows.

Independent specification and regression review found two defects. A nested
case with inherited elsif/else stops could feed a pending fence Boundary to a
contextual-word assertion, and a Boundary immediately after an optional Catch
handler bypassed mandatory arrow/body recovery. The repair makes the sequence
boundary predicate short-circuit on every Boundary and routes the handler
successor through the same mandatory slots as ordinary parsing. Delta review
closed both findings. Additional colon/braced Catch controls cover present and
missing handlers at close and transition boundaries with exact Missing counts.

Focused evidence passed: normalized 48, ordinary case-like 12, if 8, Pattern
12, package check, formatting, and diff checks. The previous 926-test N2 phase
boundary was not repeated, and no timing budget was used because no retained
state, buffer, allocation, replay, or additional traversal was added.

Active-prefix post-prefix indentation remains a separate authority-blocked
residual. Deeper-statement deferral is certified with an unquoted selected
fence, while active depth-two controls still certify inline case/catch/if
streaming without encoding the indentation defect as desired behavior.

## Active-prefix trailing whitespace ownership

- Recorded the user-approved authoritative split in
  `notes/design/2026-09-06-yumark-prefix-trailing-whitespace-amendment.md`.
- Kept the full observed trailing-horizontal extent for strict close probes,
  while accepted body prefixes own at most one horizontal byte after the final
  `>`; residual bytes now remain ordinary Yulang whitespace and indentation.
- Updated every accepted-prefix consumer, including segmented block comments,
  balanced raw suffixes, literals, Rule, and operator observation, without new
  state, allocation, replay, or traversal.
- Focused checks passed: Yumark 19, lexical 43, literal 26, rule 21, normalized
  48, `cargo check -p yu-syntax`, formatting, and diff checks.
- Independent specification and regression audits approved the implementation
  with no code defect. Performance measurement budget consumed: zero.

## N2 canonical statement owner normalization

- Unified direct, indented, and braced canonical statements plus colon/with
  tails and case/if deeper bodies under one ordinary/fenced current-Item body.
- Deleted the five prior statement-owned Deferred frontiers. The only Deferred
  constructor in this cone now sits before `Statement` and the selected Struct,
  Mod, Use, Type, bare For, or visibility Binding child.
- Replaced source-only visibility admission with pure fence-aware observers;
  unsupported visibility heads retain ordinary handoff behavior. Removed the
  superseded non-fenced helper chains.
- Independent specification/regression review found and closed stale LBrace
  deferral on required/retry operands and initial braced-boundary leading
  emission. Delta review approved the repairs.
- Focused checks passed: normalized 56, tails 11, affected ordinary owners 77,
  `cargo check -p yu-syntax`, formatting, and diff checks. No broad suite was
  repeated. Performance measurement budget consumed: zero.
- The active temporary frontier is now the six declaration families recorded
  in `tasks/current.md`.

## N2 Binding owner normalization

- Replaced Binding target, exact equals, inline RHS/retry, deeper statement
  body, and wrong-indent handoff with one ordinary/fenced current-Item body.
- Connected normalized Pattern, expression, and statement children directly;
  all origin updates use the live suffix and boundaries retain leading trivia.
- Deleted the Binding Deferred row. Struct, Mod, Use, Type, and bare For are the
  only remaining declaration frontiers.
- Independent specification review approved the owner. Regression review found
  two dead ordinary adapters; they were removed, and a CRLF active-prefix
  end-to-end Binding row was added. Delta review approved the closure.
- Focused checks passed: Binding 7, normalized 61, `cargo check -p yu-syntax`,
  formatting, and diff checks. No broad suite or performance measurement was
  used.

## N2 bare For owner normalization

- Replaced optional label, Pattern, exact `in`, iterable, all body forms, and
  malformed recovery with one ordinary/fenced normalized For body.
- The label probe tentatively acquires label and successor Items inside a
  unit-state lexical transaction, retains only a boolean, always rolls back,
  and reacquires only a committed label. No tentative Item or fragment carrier
  survives the transaction.
- Deleted the For Deferred row. For only propagates a nested declaration
  frontier; Struct, Mod, Use, and Type are now the four active rows.
- Independent specification review approved the implementation. Regression
  review requested one accepted-label active-prefix CRLF control; it was added
  and delta-approved.
- Focused checks passed: For 12, normalized 66, `cargo check -p yu-syntax`,
  formatting, and diff checks. No broad suite or performance measurement was
  used.

## N2 Use owner normalization

- Replaced visibility/header parsing, recursive Use tree/group and exclusion
  handling, path/operator-name parsing, qualifiers, local recovery, and final
  statement-successor acquisition with one ordinary/fenced current-Item body.
- Deleted the Use Deferred row. Group/exclusion openers emit inside their owning
  CST nodes; mandatory `mod`, `without`, `as`, and `with` slots are
  boundary-first; and the path post-separator `(` probe is a rollback-only,
  fence-aware immediate-operator observation with no retained Item.
- Independent specification and regression delta reviews approved CST topology,
  active-fence EOF handoff, strict `a::(` recovery, recursive siblings, and
  CRLF `my use = value` dispatching as Binding rather than Use.
- Focused checks passed: ordinary Use 11, normalized Use 6, full normalized 72,
  workspace `cargo check`, formatting, and diff checks. No broad suite or
  performance measurement was used. The remaining declaration frontiers are
  Struct, Mod, and Type.

## N2 Mod owner normalization

- Replaced visibility/Gmod, raw name and test-marker recognition, all body
  forms, local recovery, and final successor acquisition with one
  ordinary/fenced current-Item body. Module names acquire a raw identifier
  before statement payload fallback, preserving their independence from the
  dynamic operator table.
- Deleted the Mod Deferred row and its dead ordinary adapter. Fenced
  boundary/close/transition Items retain zero-frontier leading trivia, while
  ordinary EOF header gaps remain emitted within `ModDeclaration`, preserving
  ordinary CST topology.
- Independent specification and regression delta reviews approved anonymous
  `mod test` versus EOF second-name recovery, three body forms, strict
  boundary ownership, nested remaining-frontier handoff, operator invariance,
  and the ordinary EOF repair.
- Focused checks passed: ordinary Mod 9, full normalized 76,
  `cargo check -p yu-syntax`, formatting, and diff checks. No broad suite or
  performance measurement was used. The remaining declaration frontiers are
  Struct and Type.

## N2 Struct owner normalization

- Replaced Struct header/name, body selection, named and tuple fields,
  recovery, close handling, and successor acquisition with one
  ordinary/fenced current-Item body. Raw declaration and field names remain
  independent of the dynamic operator table; `:{` remains a Type
  polymorphic-variant entry rather than a Struct colon body.
- Deleted the Struct Deferred row. The TypeApply pre-admission check for a
  stronger next named field is now a fence-aware observer over immediate
  item/origin/line facts, so it cannot inspect past a close or transition.
- Independent specification review approved the owner. Regression review
  found ordinary malformed-EOF whitespace falling outside its Error owner in
  four recovery paths; each now emits ordinary EOF leading locally while all
  fenced Boundary paths remain zero-frontier. The repair's CST topology table
  and delta review approved the closure.
- Focused checks passed: ordinary Struct 13, full normalized 80,
  `cargo check -p yu-syntax`, formatting, and diff checks. No broad suite or
  performance measurement was used. Type is the sole remaining declaration
  frontier.

## N2 Derives normalized child

- Normalized one accepted `DerivesClause`: role/comma iteration, raw `via`
  target, local recovery, and first pending successor now share one
  current-Item body. The role entry receives both caller stops and its
  immediate `TypeOuterBoundary`; fresh nested Type entries retain the
  established `NONE` boundary behavior.
- The existing Type owner still has exactly two live ordinary callers, so its
  `None`/`InLine` adapter remains thin. The final Type owner/dispatch and the
  Type Deferred row intentionally remain unchanged until the following gate.
- Independent specification and regression review approved C14 line handoff,
  raw identifier targets, comma/via recovery, outer pending handoff,
  quote-prefix CRLF behavior, fenced boundary preservation, and nested
  boundary suspension.
- Focused checks passed: direct Derives 4, existing C15 15,
  `cargo check -p yu-syntax`, formatting, and diff checks. No broad suite or
  performance measurement was used.

## N2 Type owner normalization

- Replaced the final ordinary/raw Type declaration owner with one fenced
  current-Item body. Statement dispatch now enters Type directly, so the final
  declaration-family Deferred frontier is deleted.
- Header/name/raw parameters, form/retry, mandatory RHS, and all
  header/trailing/repeated Derives handoffs carry live origin, `LineEntry`, and
  fence facts. Raw `_x` remains an Identifier parameter; fence boundaries stay
  nominal and pending; C14/C15 line and outer-episode rules remain intact.
- Independent specification review approved TD/TND/C14/C15 conformance.
  Regression review found a dead ordinary Derives adapter and two malformed
  ordinary-EOF recovery paths that emitted trailing whitespace outside their
  Type Error owner. The adapter was removed; the two loops now emit ordinary
  EOF leading inside Error while fence Boundaries remain untouched; delta review
  approved the repair.
- Focused checks passed: Type 32, Derives 4, normalized 82,
  `cargo check -p yu-syntax`, formatting, and diff checks. No broad suite or
  performance measurement was used. The N2 temporary normalized-owner frontier
  ledger is empty; Rule's separate Deferred algebra is unchanged.

## Gate 4–6 dynamic NUD statement admission

- Canonical Statement admission now uses the existing expression NUD predicate,
  so dynamic Prefix and Nullfix operators can start direct, strict-indented, and
  braced statements. Declaration-first selection, visibility Binding exclusion,
  and all existing expression owners are unchanged.
- Focused controls assert losslessness, Statement/operator-node cardinality,
  indented dedent handoff, and braced matching-close ownership for both roles.
  Independent regression review approved the narrow delta.
- Focused checks passed: dynamic-NUD tests 3, tails 14,
  `cargo check -p yu-syntax`, formatting, and diff checks. No broad suite or
  performance measurement was used. This is isolated Gate 4–6 construction,
  not Gate 4/6 certification or public cutover.

## N3/L5 isolated Rule and literal normalization

- Replaced Rule's duplicate ordinary/fenced Item scanners with one raw Rule
  payload callback to shared `current_item`; direct origin, `LineEntry`, and
  optional fence capability cross every Rule child and exit without retained
  state.
- RuleAlternation now owns each nonboundary leading LF/CRLF as its own
  separator, preserving leading, trailing, and repeated epsilon alternatives.
  A pending boundary's leading trivia remains un-emitted and returns unchanged
  to the outer Yumark owner.
- Rule call/index `ExpressionList` now uses normalized Pratt acquisition and
  accepts simple/compound expressions, canonical braced statement/declaration
  children, and dynamic operators under the existing list recovery contract.
  Noninterpolating String and RuleLiteral crossings carry exact Item plus
  `LineEntry`; interpolation remains a zero-effect later-gate deferral.
- M2 specification and regression reviews approved exact boundary/line/CST
  ownership and confirmed no legacy/public/Yumark production reachability.
  Focused checks passed: Rule 24, literal 26, Pattern 12,
  `cargo check -p yu-syntax`, formatting, and diff checks. No broad suite or
  performance measurement was used: no retained state, buffer, replay,
  allocation, or additional traversal was introduced.
- This closes isolated N3/L5 construction only. L6 virtual statement
  interpolation, L7 RuleLiteral/Pattern production closure, and later Yumark
  integration/public cutover remain open.

## Gate 6 isolated declaration-variant core and scoped Type Pipe lexing

- Audited the L6 prerequisite and found the Gate 6 root statement/declaration
  construction checkpoint incomplete: direct canonical Statement has no
  Enum/Error/Role/Impl/Cast/Act shell, so a partial `%{ Statement* }` would
  violate the Authoritative virtual-block contract.
- Built the required shared-before-shell declaration-variant core as an
  isolated direct witness. One streaming owner covers Braced, ColonIndented,
  EqualsInline, and EqualsIndented sequence forms plus unit, `from`, named,
  tuple, and positional payloads. The existing Struct field loop is now a
  small parameterized private owner; no declaration shell, statement admission,
  public dispatch, legacy bridge, AST, or HIR was added.
- The first Type Pipe representation was rejected in review: a lexical marker
  must not be stored in `TypeOuterBoundary`, whose C15 contract is active
  current-episode boundaries only. After architecture re-adjudication, the
  implementation uses one separate immediate `pipe_lexical: bool`: all
  declaration-variant payload episodes retain lexical Pipe recognition, while
  only non-braced outer episodes activate the Pipe stop. Fresh nested Type
  episodes receive `TypeOuterBoundary::NONE` but retain the lexical flag;
  ordinary Type parsing keeps Pipe unowned/Unknown.
- Independent specification and compiler/recovery reviews approved the final
  delta. Focused checks passed: declaration-variant 17, TypeExpression 50,
  Struct 13, `cargo check -p yu-syntax`, formatting, and diff checks. No
  retained state, buffer, replay, allocation, or extra traversal was added, so
  no performance measurement was required. Workspace-wide tests remain deferred
  to a coherent phase boundary.
- Remaining work: Enum/Error shells and the other Gate 6 declaration owners;
  L6 virtual statement interpolation, L7 literal closure, Yumark integration,
  and public cutover remain outside this isolated construction slice.

## Gate 6 isolated declaration-companion core

- Added the shared-before-shell, normal-private `DeclarationCompanion` owner.
  Its only current caller is a `#[cfg(test)]` witness: the production parser and
  legacy dispatch remain untouched. The owner begins after exact contextual
  `with` selection and builds colon-inline, strict-deeper colon-indented, and
  empty-permitted braced canonical-Statement forms directly into Rowan.
- Gate 3 remains Statement-only. The implementation emits the dedicated
  companion/indented-body CST vocabulary and deliberately emits neither
  `DerivesClause` nor the generic `WithBodyTail`, indented statement-block, or
  braced-statement-block-expression vocabulary. Attachment gaps, declaration
  shells, typed legacy diagnostics, AST/HIR, and public wiring remain later
  gates.
- The first compiler/recovery review found two real DC-R failures: a malformed
  introducer lost the maximal line-comment recovery extent at its first newline,
  and a missing braced close could consume caller-owned `:` or contextual
  `else`. The one repair round now emits only leading trivia before the first
  newline into the active Error while returning the same pending Item, and
  decides local `}`/separators before returning caller stops without creating an
  extra Error or Statement. A third correction aligns exact contextual `with`
  with the dynamic `OperatorUse` representation; suffixed `with?` rolls back.
- Compiler/recovery delta review and static performance delta review approved.
  Focused companion tests: 7 passed; shared normalized-owner cone: 82 passed;
  `cargo check -p yu-syntax`, formatting, and diff checks passed. There is no
  normal/public call edge, retained collection, clone, cache, replay, rescan,
  or added traversal, so static review justified no timing measurement. The
  next Gate 6 prerequisite is the companion Derives item/owner handoff before
  declaration shells.

## Gate 6 isolated declaration-companion Derives item

- Extended the normal-private companion owner with Gate 4's direct
  `DerivesClause+` item priority. An exact Identifier `derives` is tested only
  in committed companion slots before canonical Statement parsing; each clause
  is a direct companion child and an adjacent run contains no statement
  separator. The existing Derives driver remains the sole owner of role, comma,
  `via`, and malformed-role recovery.
- The call carries only `TypeOuterBoundary::DERIVES | VIA`, deliberately not
  Gate 5's `WITH` handoff. Since comma is a Derives-internal role separator, the
  companion retains its full local stops while the driver call masks only
  `STOP_COMMA`. The existing Identifier-only driver contract also means a
  dynamic `OperatorUse` spelling `derives` remains a non-Derives companion
  item; widening that contract is out of this gate.
- Two recovery corrections closed review findings. After a terminal `via`, a
  deeper recognizable successor now receives exactly one companion Missing
  Separator before retry; a malformed successor remains inner-Derives-owned.
  The new recovery decision first applies the companion's terminal predicate,
  so deeper caller-owned `else` or `{` remains an exact pending Item with no
  local recovery. Separate runs across equal-indent/braced separators are also
  distinguished from an adjacent `DerivesClause+` run.
- M2 compiler/recovery and regression reviews, including final delta review,
  approved. Focused companion tests: 16 passed; direct Derives: 4 passed;
  normalized statement cone: 82 passed; `cargo check -p yu-syntax`, formatting,
  and diff checks passed. No ordinary/public edge, allocation, retained
  collection, clone, cache, replay, rescan, or ambient state was introduced;
  no timing measurement was required. Gate 5 typed episode handoffs remain
  next, followed by declaration shells.
