# 2026-09-06

## Direct-rewrite Item emission ownership N0c attempt

The known L5a ordinary-path assertion was repaired by restoring the original
ordinary parenthesized/call/index/dot/path owners at the shared dispatch
points. L5a alone continues through the complete-Item owners and retains its
`Deferred` propagation. Independent compiler/recovery review found no
ordinary route into an L5a-only assertion.

N0c then exposed one omitted N0a migration in the `if` condition owner. The
pre-sealing code emitted scanned leading trivia and cleared the same Item's
raw leading field before parsing the condition. The sealed implementation now
constructs that Item and consumes its leading frontier once before opening
`Condition`; this preserves the original token placement and recovery order
without detached or physical Item mutation. Independent compiler/recovery
review approved the repair and found no sibling scan/emit/Item-transfer shape.

Focused evidence is green: direct lexical (30), owners (23), tails (11),
struct declaration (11), type declaration (31), and use declaration (10).

The required final `cargo test -p yu-syntax` is not green: 903 passed, 3
failed, 2 ignored. The remaining failures are in the independent old
`grammar/` tree, while the candidate diff changes only `rewrite/if_expr.rs`:

- `grammar::declaration::companion::tests::gate3_isolated_companion_form_recovery_and_state_table`;
- `grammar::declaration::tests::struct_named_field_sequence_owns_leading_and_repeated_empty_comma_slots`;
- `grammar::expression::tests::if_and_braced_mandatory_rejections_restore_error_sink`.

N0c remained blocked until those failures were causally isolated and repaired.
They were not direct-rewrite frontier failures.

## N0c legacy-grammar closure

The three independent failures were repaired at their owning sites:

- the shared named-field driver now probes a pending comma but emits an empty
  slot before consuming that comma, retaining the Struct/Enum/Error topology;
- every failed Call argument chain probe restores its ErrorSink checkpoint.
  A non-embedded canonical Statement republishes only an accepted retry's
  first invalid character, preserving the established nested companion
  expectation; an active EmbeddedYulang frame records only its canonical fact;
- the `if` companion frame still begins before condition recognition and stays
  live through body/continuation ownership. After a recovered initial missing
  condition closes that frame, its opaque allocation is released so the legacy
  local-state snapshot remains literal.

M2 compiler/recovery and specification delta reviews approved the final Call
ownership/transaction repair. Focused controls covered the Call wrapper,
embedded Yumark bridge, declaration-companion nested expectation, missing-if
state restoration, and named-field empty slots. The designated final
`cargo test -p yu-syntax` passed: 906 passed, 0 failed, 2 ignored. No
performance measurement was warranted; the repair adds no traversal, replay,
cache, or allocation path.

<!-- daily-append-anchor: 2026-09-06 -->

## Fenced current-Item normalization N1

N1 isolated normalized lexical kernel is complete. `rewrite/current_item.rs`
owns one transactional current-Item construction: a call-local `LineEntry`,
one physical-leading accumulator, lazy per-Item split storage, physical-line
fence decisions, and exactly one final Item construction. A boundary skips the
payload callback and remains the unchanged next acquisition; EOF reports the
terminal in-line fact while close/transition retain physical-start.

The fenced block-comment scanner is now the constructor's compiled private
lexical dependency rather than a test-only wrapper. `operator.rs` has a pure
fence-aware source observer for dynamic operator and follower evidence. Its
`None` branch retains the previous raw-trivia scanner, so ordinary operators do
not gain fence coordinates, fragment allocation, or a different scan path.
No production fence dispatch or grammar owner uses the new capability yet.

Focused controls passed: lexical 38, dynamic operators 11, and Rule 21;
`cargo check -p yu-syntax` also passed. The N1 implementation adds no cache,
source root, retained cursor/context, replay, parser state, Rowan effect, or
performance measurement requirement. N2 remains responsible for the first
ledgered Pratt/tail/delimited migration.
