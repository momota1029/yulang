# 2026-09-21

## Constraint/SCC foundation F0 complete

F0 construction under the approved structure-first inference design is
complete. `ConstraintBatch::collect` now freezes artifact-branded
definition-order and dependency-only use records through one HIR traversal plus
one endpoint pass. Forward, backward, self, and mutual uses are retained without
adding Name components or subtype facts; direct-root, ambiguous, and unresolved
names add no false dependency.

Collection availability failures are structured, and ambiguous/unresolved
binding bodies remain local error statuses. Production counters cover endpoint
indexes, workspace/peak bytes, and hash/equality payload-byte incidences,
including 1,000/2,000/4,000 chain, repeated-target, and long-identity witnesses.
Compiler, specification, and performance implementation reviews are clean after
two repair rounds. Focused HIR/solver tests and workspace check pass; F1 static
SCC planning is next.

## Constraint/SCC foundation F1 complete

F1 now provides a standalone sealed static SCC kernel over F0 definition and
use identities. It builds `parent -> target` arcs, runs iterative Kosaraju,
chooses the minimum definition order as component identity, partitions exact
internal and incoming use IDs, and schedules condensation sinks first with a
deterministic canonical-key queue. It does not yet attach the plan to
`ConstraintBatch`, expose batch queries, solve components, generalize, or
publish schemes; those remain F2 and later semantic gates.

The final kernel includes structured foreign/missing/duplicate identity
failures, raw-plan insertion/alpha/path determinism witnesses, and 1,000/2,000/
4,000 chain, diamond, duplicate-payload, independent, long-cycle, self-cycle,
and mutual-cycle resource witnesses. Production counters cover graph, SCC,
condensation, stable-handle clones, and phase-exact logical peak workspaces.
Instrumentation remains linear outside separately counted canonical sort and
ready-queue comparisons. Three bounded M3 review/repair rounds closed the F2
boundary, accounting, witness, and instrumentation-cost findings; final
compiler, specification, and performance reviews are clean. All 22 `yu-solver`
library tests, formatting, and the workspace check pass. F2 collected-batch
integration is next.

## Constraint/SCC foundation F2 complete

F2 integrates the complete F1 plan into `ConstraintBatch` only after F0 has
resolved every retained endpoint and validated both total maps. Completed F0
endpoint workspaces are dropped before graph construction, so production peak
accounting follows the actual non-overlapping lifetimes. The builder borrows
collected definition identities directly and introduces no second HIR walk or
temporary definition-ID vector.

The batch now owns one immutable plan and exposes crate-private,
artifact-checked indexed queries for dependency-first components, definition
membership, component members, and full internal/incoming use-ID slices.
Foreign and missing identities remain distinct failures, and cloning a batch
preserves its artifact brand and immutable plan contract. F2 adds no component
execution, name constraint, equality, reverse edge, root projection, scheme,
generalization, instantiation, or publication behavior.

Integrated 1,000/2,000/4,000 collection witnesses cover chain,
repeated-target, and isolated families, while F1 retains its seven synthetic
graph families. Compiler, specification, and performance reviews are clean
after one repair round. All 27 `yu-solver` library tests, formatting, and the
warning-free workspace all-target check pass. F0-F2 are complete; the next
work requires an approved semantic SCC execution gate.

## R2 withdrawn after direct oracle audit; F3a complete

Direct inspection of `yulang2-oracle@a58eefc3` established that recoverable
lowering/type errors do not produce Failed/Blocked SCC outcomes. Registered
erroneous definitions retain roots, finish, generalize through ordinary type
semantics, and release dependent instantiations; unconstrained roots may yield
ordinary bottom/`Never`. The oracle uses one concrete retained
`AnalysisSession`, not a generic backend or compact lifecycle result.

The user approved the independently reviewed oracle-aligned successor and
clarified that `Never` is simply bottom, never a special recovery sentinel. R2
is superseded in full. F3a archived the entire uncommitted R2 solver diff at
`/tmp/yulang-f3a-r2.QDvJz5`, attributed every hunk, restored `yu-solver` to the
exact F0-F2 blobs, and removed the untracked executor. All 27 solver library
tests, the package check, formatting, and diff check pass. F3b concrete-session
extraction is next.

## Oracle-aligned F3b concrete session complete

`SolvedModule::solve` now delegates to one private concrete
`InferenceSession`, whose exact path is `new -> run -> admit_all_collected_facts
-> finish`. The extraction moves the existing mutable admission, local
`CrossKind` handling, projection/root construction, diagnostics, provenance,
and counter ownership without changing their algorithms or ordering. It adds
no SCC traversal, placeholder drain, scheme state, lifecycle result, generic
backend, public item, or source-sized work.

Independent specification and regression review are clean. The committed
baseline and candidate both pass the same 27 solver tests; normalized rustdoc
public paths are identical (60/60); downstream probes compile the exhaustive
four-variant `SolveAvailabilityError` match, existing trait bounds, and exact
solve signature on both revisions. Formatting, solver tests, workspace
all-target checking, and diff checking pass. F4 semantic SCC design is next.
