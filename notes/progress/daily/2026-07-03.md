# 2026-07-03

## File Resource Stage 2 native protocol evidence

- Started from clean `main` at commit `123424e4`.
- Kept Contract v0 closed and worked on Contract v1 File Resource only.
- Fixed `lib/std/io/file.yu` so `open_text`'s `else` branch aligns with the
  `if`, restoring public roots after `open_text` (`open`, `open_in`, `text`,
  `text_with`).
- Renamed the remaining raw integer error mapper to `legacy_err_from_code` and
  removed unused `kind_from_code`, `legacy_meta`, `legacy_read_text`, and
  `legacy_write_text`; public `read_text`, `write_text`, `meta`, and
  `text_with` stay on public `file::load` / `file::store` / `file::meta`.
- Added native `file-resource` contract cases:
  - `file_native_protocol_load_store_meta`
  - `file_text_with_native_commit`
  - `file_text_with_native_rollback_on_error`
- Left raw/snapshot operations in place as the compatibility window for
  `open_text` / `open_in`; removing them still needs a separate public session
  replacement.

## File buffer native ambient registration

- Added `std::io::file::file_buffer` to the runtime host manifest with
  `ambient_get` and `ambient_set` sync operations.
- Added a native handler-extent ledger for unscoped `file::text`. `ambient_get`
  reads once into the ledger, `ambient_set` updates the ledger, and successful
  run completion flushes dirty ambient buffers back to disk.
- Added `file_text_unscoped_native_handler_extent` to the `file-resource`
  contract. It proves the edit is visible through the unscoped `&` view, the
  backing file still has the old content during the handler extent, and the
  temp file is written at run completion.
- Updated `debug host-act-manifest` CLI coverage and `scripts/release-smoke.sh`
  so release smoke checks the `file_buffer` host act surface.
- Known remaining problem: native ambient read/write failures still surface as
  escaped effects because unscoped ambient operations currently return plain
  `str` / `unit`, not `result` or typed `io_err`. This needs the next Stage 2
  host-boundary decision instead of another local fallback.
- Validation:
  - `cargo fmt --all -- --check`
  - `cargo check -q -p evidence-vm -p yulang`
  - `cargo build -q --release -p yulang`
  - `cargo run -q -p yulang -- --std-root lib contract --contract file-resource
    tests/yulang/cases.toml` (`contract cases ok: 39`)
  - `timeout 240s cargo test -q -p yulang public -- --test-threads=1`
  - `timeout 240s cargo test -q -p yulang --test cli -- --test-threads=1`
    (`113 passed`)
  - `cargo test -q -p evidence-vm -- --test-threads=1` (`96 passed`)
  - `scripts/release-smoke.sh target/debug/yulang`
  - `scripts/package-release.sh --version contract-v1-file-buffer-ambient
    --target x86_64-unknown-linux-gnu --binary target/release/yulang --out
    target/release-contract-v1-file-buffer-ambient`
  - `scripts/release-archive-smoke.sh
    target/release-contract-v1-file-buffer-ambient/yulang-x86_64-unknown-linux-gnu.tar.gz`
- Problem observed while validating: running the CLI test binary in parallel
  with the public test target produced one transient
  `compatible_run_with_std_prefix_cache_matches_full_build_for_direct_ref_update`
  failure with `conflicting type candidates for slot 10`. The same test passed
  in isolation and the full CLI suite passed when rerun sequentially. I did not
  change code for this flake in this slice.

## Remove unused metadata raw host bridge

- Removed the private `file::meta_raw` declaration from `lib/std/io/file.yu`.
  Public metadata already goes through `file::meta`, so the raw integer-code
  bridge no longer had a caller.
- Removed `FileMetaRaw` from the Evidence VM host manifest and runtime
  dispatch, and updated the host manifest tests for the smaller file act table.
- Validation:
  - `cargo fmt --all -- --check`
  - `cargo check -q -p evidence-vm -p yulang`
  - `cargo run -q -p yulang -- --std-root lib contract --contract file-resource
    tests/yulang/cases.toml` (`contract cases ok: 39`)
  - `timeout 120s cargo test -q -p yulang --test cli
    debug_host_act_manifest_prints_runtime_registry_view -- --test-threads=1`
  - `cargo test -q -p evidence-vm
    runtime_host_operation_table_separates_console_and_file_acts --
    --test-threads=1`
  - `cargo test -q -p evidence-vm -- --test-threads=1` (`96 passed`)

## Move range file helpers off integer error codes

- Changed the `file::read_at` host act operation from `(int, str, range)` to
  `result (str, range) io_err`.
- Changed the `file::write_at` host act operation from `int` to
  `result unit io_err`.
- Kept the public `std::io::file::read_at` / `write_at` wrappers on the same
  throwing public surface by routing through `unwrap_io`.
- Added `file_read_write_at_native_result` to the `file-resource` contract.
  This proves the native range helper boundary executes through typed result
  values. The runtime still treats range semantics as provisional and writes the
  whole file for this first slice.
- Validation:
  - `cargo fmt --all -- --check`
  - `cargo check -q -p evidence-vm -p yulang`
  - `cargo run -q -p yulang -- --std-root lib contract --case
    file_read_write_at_native_result tests/yulang/cases.toml`
  - `cargo run -q -p yulang -- --std-root lib contract --case
    std_file_read_at_public_signature tests/yulang/cases.toml`
  - `cargo run -q -p yulang -- --std-root lib contract --case
    std_file_write_at_public_signature tests/yulang/cases.toml`
  - `cargo run -q -p yulang -- --std-root lib contract --contract
    file-resource tests/yulang/cases.toml` (`contract cases ok: 40`)
  - `timeout 120s cargo test -q -p yulang --test cli
    debug_host_act_manifest_prints_runtime_registry_view -- --test-threads=1`
  - `cargo test -q -p evidence-vm -- --test-threads=1` (`96 passed`)

## Remove remaining file host integer error codes

- Changed `file::open_text_raw` and `file::open_text_snapshot_raw` from
  `(int, file_handle)` to `result file_handle io_err`.
- Changed `file::file_flush` and `file::file_snapshot_commit` from `int` to
  `result unit io_err`.
- Removed `legacy_err_from_code` from `lib/std/io/file.yu` and removed the
  now-unused runtime `runtime_file_error_code` helper. The remaining raw /
  snapshot operations are still provisional compatibility operations, but they
  no longer use integer error-code translation.
- Validation:
  - `cargo fmt --all -- --check`
  - `cargo check -q -p evidence-vm -p yulang`
  - `cargo run -q -p yulang -- --std-root lib contract --case
    file_text_with_native_commit tests/yulang/cases.toml`
  - `cargo run -q -p yulang -- --std-root lib contract --case
    file_text_with_native_rollback_on_error tests/yulang/cases.toml`
  - `cargo run -q -p yulang -- --std-root lib contract --case
    std_file_open_text_public_signature tests/yulang/cases.toml`
  - `cargo run -q -p yulang -- --std-root lib contract --case
    std_file_open_in_public_signature tests/yulang/cases.toml`
  - `cargo run -q -p yulang -- --std-root lib contract --contract
    file-resource tests/yulang/cases.toml` (`contract cases ok: 40`)
  - `timeout 120s cargo test -q -p yulang --test cli compatible_run_std_file
    -- --test-threads=1` (`6 passed`)
  - `timeout 120s cargo test -q -p yulang --test cli
    debug_host_act_manifest_prints_runtime_registry_view -- --test-threads=1`
  - `cargo test -q -p evidence-vm -- --test-threads=1` (`96 passed`)
- Problem observed while validating: I first passed multiple test filters to
  `cargo test`, which cargo rejects. I reran the intended CLI filters
  separately.

## Refresh release archive evidence after result bridge

- Rebuilt the release binary after removing file host integer error codes.
- Repackaged the local Linux archive as `contract-v1-file-result-bridge`.
- Validation:
  - `cargo build -q --release -p yulang`
  - `scripts/package-release.sh --version contract-v1-file-result-bridge
    --target x86_64-unknown-linux-gnu --binary target/release/yulang --out
    target/release-contract-v1-file-result-bridge`
  - `scripts/release-archive-smoke.sh
    target/release-contract-v1-file-result-bridge/yulang-x86_64-unknown-linux-gnu.tar.gz`

## Expose host manifest surface classes

- Added `RuntimeHostOperationSurface` to the runtime host manifest.
- Classified current host operations as:
  - `contract`: console write, public file protocol operations, and ambient
    file buffer operations;
  - `raw-compat`: open/snapshot handle operations kept for `open_text` /
    `open_in` compatibility.
- Updated `debug host-act-manifest` to print `surface=...` and updated release
  smoke to check a representative `raw-compat` line.
- Validation:
  - `cargo fmt --all -- --check`
  - `cargo check -q -p evidence-vm -p yulang`
  - `cargo test -q -p evidence-vm
    runtime_host_operation_table_separates_contract_and_raw_surfaces --
    --test-threads=1`
  - `timeout 120s cargo test -q -p yulang --test cli
    debug_host_act_manifest_prints_runtime_registry_view -- --test-threads=1`
  - `scripts/release-smoke.sh target/debug/yulang`
  - `cargo test -q -p evidence-vm -- --test-threads=1` (`97 passed`)

## Move file predicates onto metadata

- Removed `file::exists`, `file::is_file`, and `file::is_dir` from the native
  host act.
- Reintroduced the public helpers as pure Yulang wrappers over `meta(path).kind`.
  This keeps the public helper spelling while making metadata the single host
  boundary for existence/kind checks.
- The runtime host manifest now has only `contract` and `raw-compat` surfaces.
- Validation:
  - `cargo fmt --all -- --check`
  - `cargo check -q -p evidence-vm -p yulang`
  - `cargo run -q -p yulang -- --std-root lib contract --case
    std_file_exists_public_signature tests/yulang/cases.toml`
  - `cargo run -q -p yulang -- --std-root lib contract --case
    std_file_is_file_public_signature tests/yulang/cases.toml`
  - `cargo run -q -p yulang -- --std-root lib contract --case
    std_file_is_dir_public_signature tests/yulang/cases.toml`
  - `cargo run -q -p yulang -- --std-root lib contract --case
    file_native_protocol_load_store_meta tests/yulang/cases.toml`
  - `cargo run -q -p yulang -- --std-root lib contract --contract
    file-resource tests/yulang/cases.toml` (`contract cases ok: 40`)
  - `timeout 120s cargo test -q -p yulang --test cli compatible_run_std_file
    -- --test-threads=1` (`6 passed`)
  - `timeout 120s cargo test -q -p yulang --test cli
    debug_host_act_manifest_prints_runtime_registry_view -- --test-threads=1`
  - `scripts/release-smoke.sh target/debug/yulang`
  - `cargo test -q -p evidence-vm -- --test-threads=1` (`97 passed`)

## Refresh release archive evidence after metadata predicates

- Rebuilt the release binary after moving file predicates onto `meta.kind`.
- Repackaged the local Linux archive as `contract-v1-metadata-predicates`.
- Validation:
  - `cargo build -q --release -p yulang`
  - `scripts/package-release.sh --version contract-v1-metadata-predicates
    --target x86_64-unknown-linux-gnu --binary target/release/yulang --out
    target/release-contract-v1-metadata-predicates`
  - `scripts/release-archive-smoke.sh
    target/release-contract-v1-metadata-predicates/yulang-x86_64-unknown-linux-gnu.tar.gz`

## Host ABI Stage alpha skeleton

- Added `crates/evidence-vm/src/runtime/host_abi.rs` with the ABI v0 boundary
  types: `BoundaryValue`, `CtorRef::Label`, `HostOutcome`, `HostCtx`,
  `HostOpFn`, and `HostOpRegistration`.
- Moved the current console/file host implementations behind `HostOpFn`
  registrations while keeping path resolution on the existing static runtime
  host manifest. This preserves WP1's "call target only" migration boundary.
- Added a runtime-owned builtin host state for stdout and ambient file buffers,
  with `HostCtx::state_mut<T>()` as the only public host callback access.
- Added runtime codecs between `RuntimeEvidenceValue` and `BoundaryValue`.
  Constructors cross the ABI as labels and are resolved back through
  `RuntimeEvidenceHostConstructors`; `file_meta` keeps its existing runtime
  record/constructor shape through a dedicated decode bridge.
- Added `RuntimeEvidenceRunError::HostAbiError` and CLI formatting as
  `runtime error [yulang.host-abi-error]`. Existing native ambient file I/O
  failures still map back to `yulang.host-io-error`.
- Added ABI Stage alpha evidence-vm tests for:
  - `BoundaryValue` primitive/tuple/constructor-label roundtrip;
  - file operation mock registration without filesystem access;
  - panic inside a registered `HostOpFn` becoming structured failure.
- Validation:
  - `cargo fmt --all -- --check`
  - `cargo check -q -p poly -p infer -p specialize -p evidence-vm -p yulang`
  - `cargo test -q -p poly` (`48 passed`)
  - `cargo test -q -p infer` (`513 passed`)
  - `cargo test -q -p evidence-vm -- --test-threads=1` (`109 passed`)
  - `cargo test -q -p yulang --test cli -- --test-threads=1` (`115 passed`)
  - `cargo run -q -p yulang -- --std-root lib contract --contract
    file-resource tests/yulang/cases.toml` (`contract cases ok: 57`)

## Host manifest surface transport

- Added optional compiler-produced host manifest transport to
  `specialize::RuntimeEvidenceSurface` and `evidence_vm::EvidenceVmPlan`.
- Carried host manifests through `BuildPolyOutput`, poly cache artifacts
  (`POLY_CACHE_FORMAT = 8`), and control specialization paths.
- Updated evidence-vm static route classification to prefer the generated host
  manifest when present, with the existing static runtime manifest retained as
  fallback for legacy/cache paths.
- Added CLI evidence that the compiler-generated std manifest is attached to
  `RuntimeEvidenceSurface` and still matches the Stage 1 runtime table.
- Source-unit prefix routes now reuse the prefix artifact's host manifest
  instead of regenerating one from the merged prefix+suffix compiled unit.
  Attempting to generate the manifest from the merged compiled unit exposed a
  typed-surface seam: public signature formatting can see scheme indices that
  do not belong to the reconstructed type arena
  (`poly::types::TypeArena::pos` out-of-bounds while formatting a host op
  scheme). This remains the next WP3 blocker for user-defined host acts in
  suffix code, and should be fixed at the compiled typed-surface/import
  boundary before full runtime registry switch-over.
- Validation:
  - `cargo fmt --all -- --check`
  - `cargo check -q -p poly -p infer -p specialize -p evidence-vm -p yulang`
  - `cargo test -q -p poly host_manifest -- --test-threads=1` (`4 passed`)
  - `cargo test -q -p infer host_act -- --test-threads=1` (`2 passed`)
  - `cargo test -q -p evidence-vm -- --test-threads=1` (`109 passed`)
  - `cargo test -q -p yulang --test cli
    compiler_generated_host_manifest_matches_runtime_stage1_table --
    --test-threads=1`
  - `cargo run -q -p yulang -- --std-root lib contract --case
    file_text_with_commit tests/yulang/cases.toml`
  - `cargo run -q -p yulang -- --std-root lib contract --contract
    file-resource tests/yulang/cases.toml` (`contract cases ok: 57`)

## Structured concurrency decision before serve

- Added `notes/design/2026-07-03-structured-concurrency-decisions.md` as the
  implementation decision for serve first slice.
- The decision fixes branch ownership and cancellation before any server
  in-process driver work:
  - `accept` resumes create child branches owned by the serving handler extent.
  - cancellation is queued through the scheduler as `Cancel { branch_id,
    reason }`, not as host-to-runtime reentry.
  - the first implementation slice only drops suspended branches immediately;
    running-branch cooperative cancellation is a later slice.
  - branch drop means scope exit is not reached, so file managed lens updates
    rollback under the existing resource-lifetime contract.
- Updated `spec/2026-07-02-server-resource-api.md`,
  `spec/2026-07-02-io-resource-api.md`, `spec/README.md`, `docs/status.md`,
  `notes/todo/structured-concurrency.md`, and
  `notes/todo/contract-v1-file-resource-priority.md` to point at the new
  decision.

## Expose suspend host operation tiers

- Added `suspend-one-shot` and `suspend-multi-shot` to the runtime host
  operation tier vocabulary.
- Kept all currently registered console/file operations on the `sync` tier; no
  server operation is registered yet.
- Updated `debug host-act-manifest` to print the supported tier ids and added
  CLI/unit coverage for the tier vocabulary.
- Updated `scripts/release-smoke.sh` so packaged binaries prove that
  `suspend-multi-shot` is visible before the server first slice starts.
- Validation:
  - `cargo fmt --all -- --check`
  - `cargo test -q -p evidence-vm
    runtime_host_manifest_exposes_suspend_tiers_before_server_ops --
    --test-threads=1`
  - `timeout 120s cargo test -q -p yulang --test cli
    debug_host_act_manifest_prints_runtime_registry_view -- --test-threads=1`
  - `cargo check -q -p evidence-vm -p yulang`
  - `scripts/release-smoke.sh target/debug/yulang`
  - `cargo run -q -p yulang -- debug host-act-manifest | tail -n 8`

## Add host scheduler branch table first slice

- Added `crates/evidence-vm/src/runtime/scheduler.rs`.
- The scheduler owns a root branch, can spawn suspended child branches, queues
  `Cancel { branch_id, reason }`, drops suspended branches immediately, and
  marks running branches as cancel-pending.
- The scheduler also assigns branch-local host operation sequence ids
  `(branch_id, seq)`, matching the record/replay TODO's requirement to reserve
  operation instance identity before the registry and server driver are wired.
- `RuntimeEvidenceRunner` now owns a root scheduler and asserts that ordinary
  sync-only runs leave only the root branch. No server host operation is
  registered yet, so this does not change normal runtime behavior.
- Validation:
  - `cargo fmt --all -- --check`
  - `cargo test -q -p evidence-vm scheduler -- --test-threads=1`
    (`5 passed`, including branch-local operation sequence ids)
  - `cargo check -q -p evidence-vm -p yulang`
  - `timeout 120s cargo test -q -p yulang --test cli
    debug_host_act_manifest_prints_runtime_registry_view -- --test-threads=1`
  - `timeout 240s target/debug/yulang --std-root lib contract --contract
    file-resource tests/yulang/cases.toml` (`contract cases ok: 40`)
  - `scripts/release-smoke.sh target/debug/yulang`
- Problem note: I first started the same file-resource contract through
  `cargo run -q -p yulang` without a timeout and interrupted it after about two
  minutes with no output. The direct `target/debug/yulang` run then completed,
  so the earlier interruption was treated as an ambiguous compile-plus-contract
  delay rather than a confirmed runtime hang.

## Refresh release archive evidence after scheduler ids

- Rebuilt the release binary after adding suspend tier output and scheduler
  branch/sequence ids.
- Repackaged the local Linux archive as `contract-v1-scheduler-ids`.
- Validation:
  - `cargo build -q --release -p yulang`
  - `scripts/package-release.sh --version contract-v1-scheduler-ids --target
    x86_64-unknown-linux-gnu --binary target/release/yulang --out
    target/release-contract-v1-scheduler-ids`
  - `scripts/release-archive-smoke.sh
    target/release-contract-v1-scheduler-ids/yulang-x86_64-unknown-linux-gnu.tar.gz`

## Broad validation after host scheduler slices

- Validation:
  - `cargo test -q -p evidence-vm -- --test-threads=1` (`103 passed`)
  - `timeout 240s cargo test -q -p yulang public -- --test-threads=1`
    (`9 passed` plus `18 passed` in the public filtered groups)
  - `timeout 240s cargo test -q -p yulang --test cli -- --test-threads=1`
    (`113 passed`)

## Align IO spec with text_with protocol

- Updated `spec/2026-07-02-io-resource-api.md` so `text_with` is written as
  the implemented state-passing protocol:
  `str -> [e] ('a, str)` rather than the older `ref '[file] str` callback
  sketch.
- Updated `docs/language/file-resource-contract.md` and
  `docs/language/contract-v1-file-evidence.md` to call out the same protocol
  shape and leave `\my &text ->` as future sugar over that shape.
- Validation:
  - `rg -n "file::text_with path \\\\&text|file::text_with \"notes.txt\" \\\\&text|ref '\\[file\\] str\\) -> \\[e\\]" spec docs notes/design/2026-07-02-file-session-boundary-plan.md -S`
    returned no matches.
  - `git diff --check`

## Focus release smoke on file-resource protocol evidence

- Added a focused `file-resource` contract list to `scripts/release-smoke.sh`.
  The list runs the Stage 1 state-passing protocol cases, native
  load/store/meta, native `text_with` commit/rollback, and unsupported-host
  failure through the smoke binary and installed standard library.
- Taught `yulang contract` to accept repeated `--case` filters, so the smoke
  can run that focused set in one contract invocation instead of spawning one
  process per case.
- Hardened the smoke check to assert `contract cases ok: <selected case count>`,
  so older binaries that silently honor only the last repeated `--case` cannot
  pass the file resource smoke by running a single case.
- Updated `notes/todo/contract-v1-file-resource-priority.md` so future planning
  treats direct release-smoke representative file-resource coverage as done,
  while leaving full tag-filter evidence, raw/provisional isolation, and native
  ambient failure typing open.
- Kept the full `--contract file-resource` run out of direct release smoke. It
  remains archive-smoke/local validation evidence because it is materially
  heavier than a smoke check.
- Timing/problem note:
  - `timeout 120s target/debug/yulang --std-root lib contract --contract
    file-resource tests/yulang/cases.toml` exited `124` with no output on this
    run.
  - The focused 10-case loop finished in about `36s` against
    `target/debug/yulang --std-root lib`.
  - The first one-invocation smoke attempt took about `40s` and hit the general
    `YULANG_SMOKE_TIMEOUT=30s` command limit. `scripts/release-smoke.sh` now
    gives the focused file-resource contract run its own
    `YULANG_SMOKE_FILE_RESOURCE_TIMEOUT` defaulting to `180s`.
- Validation:
  - `git diff --check`
  - `cargo fmt --all -- --check`
  - `cargo build -q -p yulang`
  - `timeout 180s cargo test -q -p yulang --test cli
    contract_command_runs_multiple_filtered_cases -- --test-threads=1`
  - `scripts/release-smoke.sh target/debug/yulang`

## Native text_with parity expansion

- Added `file_text_with_native_undet_last_write_wins`, a native CLI
  `file-resource` contract case proving that `text_with` under `.list`
  gets branch-local callback buffers and arrival-order last-write-wins
  write-back through public `file::load` / `file::store`.
- Added `file_text_with_native_nested_cross_file`, a native CLI
  `file-resource` contract case proving that nested `text_with` sessions over
  two backing files commit independently while the inner callback can close over
  the outer entry snapshot.
- Added `file_text_with_native_nested_state_var`, a native CLI `file-resource`
  contract case proving that callback-local `&` state can be captured and
  mutated from an inner native `text_with` callback, with both backing files
  committed through the public protocol.
- Added that case to the focused release-smoke file-resource set. The smoke
  asserts the focused contract runner reports all selected cases, so the
  representative set now covers native commit, rollback, nondet branch-local
  write-back, nested cross-file sessions, nested state-var capture, native
  load/store/meta, mock protocol cases, and unsupported-host failure.
- Full `file-resource` timing clarification:
  - `time timeout 240s target/debug/yulang --std-root lib contract --contract
    file-resource tests/yulang/cases.toml` passed in about `2m21s`
    (`contract cases ok: 40`) before this new case was added. The earlier
    120-second timeout was too short for the full tag filter, not a confirmed
    hang.
  - After the native parity expansion, `time timeout 300s target/debug/yulang
    --std-root lib contract --contract file-resource tests/yulang/cases.toml`
    passed in about `2m35s` (`contract cases ok: 43`).
- Validation:
  - `timeout 180s target/debug/yulang --std-root lib contract --contract
    file-resource --case file_text_with_native_undet_last_write_wins
    tests/yulang/cases.toml`
  - `timeout 180s target/debug/yulang --std-root lib contract --contract
    file-resource --case file_text_with_native_nested_cross_file
    tests/yulang/cases.toml`
  - `timeout 180s target/debug/yulang --std-root lib contract --contract
    file-resource --case file_text_with_native_nested_state_var
    tests/yulang/cases.toml`
  - `timeout 180s cargo test -q -p yulang --test cli
    public_contract_manifest_cli_cases_hold -- --test-threads=1`
  - `git diff --check`
  - `scripts/release-smoke.sh target/debug/yulang` with the 13-case focused
    file-resource set (`release smoke ok`, about `58s`)

## Ambient unsupported-host coverage

- Added `file_text_unsupported_host`, a runtime-failure `file-resource` case for
  unscoped `file::text` under `--host unsupported`.
- This proves the ambient `file_buffer` act reports
  `yulang.unsupported-host-capability` instead of fake text when native file
  capabilities are unavailable. It does not solve typed ambient `io_err`; that
  remains a Stage 2 blocker.
- Added the case to the focused release-smoke file-resource set.
- Validation:
  - `timeout 180s target/debug/yulang --std-root lib contract --contract
    file-resource --case file_text_unsupported_host tests/yulang/cases.toml`
  - `timeout 180s cargo test -q -p yulang --test cli
    public_contract_manifest_cli_cases_hold -- --test-threads=1`
  - `git diff --check`
  - `scripts/release-smoke.sh target/debug/yulang` with the 14-case focused
    file-resource set (`release smoke ok`, about `1m01s`)

## Native missing metadata coverage

- Added `file_native_meta_missing`, a native CLI `file-resource` metadata case.
- The case proves `file::meta` reports a missing target through
  `file_meta.kind` instead of throwing and that `exists`, `is_file`, and
  `is_dir` stay wrappers over metadata.
- Added the case to the focused release-smoke file-resource set.
- Validation:
  - `timeout 180s target/debug/yulang --std-root lib contract --contract
    file-resource --case file_native_meta_missing tests/yulang/cases.toml`
  - `timeout 180s cargo test -q -p yulang --test cli
    public_contract_manifest_cli_cases_hold -- --test-threads=1`
  - `git diff --check`
  - `scripts/release-smoke.sh target/debug/yulang` with the 15-case focused
    file-resource set (`release smoke ok`, about `1m04s`)

## Full file-resource run after metadata coverage

- Re-ran the full local `file-resource` tag filter after adding native missing
  metadata coverage.
- Validation:
  - `timeout 300s target/debug/yulang --std-root lib contract --contract
    file-resource tests/yulang/cases.toml` passed in about `2m19s`
    (`contract cases ok: 45`)

## Refresh release archive evidence after metadata coverage

- Rebuilt the release binary after adding native missing metadata coverage to
  the focused file-resource smoke set.
- Repackaged the local Linux archive as `contract-v1-file-metadata`.
- Validation:
  - `cargo fmt --all -- --check`
  - `cargo build -q --release -p yulang`
  - `scripts/package-release.sh --version contract-v1-file-metadata --target
    x86_64-unknown-linux-gnu --binary target/release/yulang --out
    target/release-contract-v1-file-metadata`
  - `scripts/release-archive-smoke.sh
    target/release-contract-v1-file-metadata/yulang-x86_64-unknown-linux-gnu.tar.gz`

## Range host ops marked raw-compat

- Moved `file::read_at` and `file::write_at` from the runtime host manifest
  `contract` surface to `raw-compat`.
- This does not change dispatch or public wrappers. It records the current
  boundary honestly: range operations already return typed `result ... io_err`,
  but exact range semantics remain provisional and should not be counted as the
  Contract v1 center.
- Updated the CLI manifest golden, release smoke manifest check, and evidence
  docs to match the new classification.
- Validation:
  - `cargo fmt --all -- --check`
  - `git diff --check`
  - `cargo test -q -p evidence-vm runtime::host -- --test-threads=1`
  - `cargo test -q -p yulang --test cli
    debug_host_act_manifest_prints_runtime_registry_view -- --test-threads=1`
  - `cargo build -q -p yulang`
  - `scripts/release-smoke.sh target/debug/yulang`
  - `timeout 180s target/debug/yulang --std-root lib contract --contract
    file-resource --case file_read_write_at_native_result tests/yulang/cases.toml`

## Refresh release archive evidence after raw range classification

- Rebuilt the release binary after the host manifest classified
  `read_at` / `write_at` as `raw-compat`.
- Repackaged the local Linux archive as `contract-v1-raw-range`.
- Validation:
  - `cargo build -q --release -p yulang`
  - `scripts/package-release.sh --version contract-v1-raw-range --target
    x86_64-unknown-linux-gnu --binary target/release/yulang --out
    target/release-contract-v1-raw-range`
  - `scripts/release-archive-smoke.sh
    target/release-contract-v1-raw-range/yulang-x86_64-unknown-linux-gnu.tar.gz`

## Native ambient missing read reports host I/O error

- Added `RuntimeEvidenceRunError::HostIoError` and routed native
  `file_buffer::ambient_get` read failures plus ambient flush write failures
  through it.
- Added `file_text_native_missing_host_io_error`, a native CLI
  `file-resource` runtime-failure case proving missing backing files for
  unscoped `file::text` report `yulang.host-io-error` instead of
  `yulang.unhandled-effect`.
- This does not solve typed ambient `io_err` propagation. The earlier local
  result-wrapping attempt still leaks row constraints, so typed ambient failure
  remains a session/error-boundary task.
- Added the case to the focused release-smoke file-resource set.
- Validation:
  - `cargo fmt --all -- --check`
  - `cargo check -q -p evidence-vm -p yulang`
  - `cargo build -q -p yulang`
  - `timeout 180s target/debug/yulang --std-root lib contract --contract
    file-resource --case file_text_native_missing_host_io_error
    tests/yulang/cases.toml`
  - `timeout 180s cargo test -q -p yulang --test cli
    public_contract_manifest_cli_cases_hold -- --test-threads=1`
  - `scripts/release-smoke.sh target/debug/yulang` with the 16-case focused
    file-resource set
  - `git diff --check`
  - `timeout 300s target/debug/yulang --std-root lib contract --contract
    file-resource tests/yulang/cases.toml` passed in about `2m58s`
    (`contract cases ok: 46`)

## Refresh release archive evidence after ambient host I/O error

- Rebuilt the release binary after adding `yulang.host-io-error` for native
  ambient file I/O failures.
- Repackaged the local Linux archive as `contract-v1-ambient-host-io`.
- Validation:
  - `cargo build -q --release -p yulang`
  - `scripts/package-release.sh --version contract-v1-ambient-host-io --target
    x86_64-unknown-linux-gnu --binary target/release/yulang --out
    target/release-contract-v1-ambient-host-io`
  - `scripts/release-archive-smoke.sh
    target/release-contract-v1-ambient-host-io/yulang-x86_64-unknown-linux-gnu.tar.gz`

## Metadata unsupported host enters focused release smoke

- Added `file_meta_unsupported_host` to the focused release-smoke
  `file-resource` set.
- The focused set now covers unsupported-host capability failure for the public
  `file` act through both normal helper and metadata paths, plus the ambient
  `file_buffer` path.
- Validation:
  - `scripts/release-smoke.sh target/debug/yulang` with the 17-case focused
    file-resource set (`release smoke ok`)

## Refresh release archive evidence after metadata unsupported smoke

- Rebuilt the release binary after adding metadata unsupported-host coverage to
  focused release smoke.
- Repackaged the local Linux archive as `contract-v1-meta-unsupported-smoke`.
- Validation:
  - `cargo build -q --release -p yulang`
  - `scripts/package-release.sh --version contract-v1-meta-unsupported-smoke
    --target x86_64-unknown-linux-gnu --binary target/release/yulang --out
    target/release-contract-v1-meta-unsupported-smoke`
  - `scripts/release-archive-smoke.sh
    target/release-contract-v1-meta-unsupported-smoke/yulang-x86_64-unknown-linux-gnu.tar.gz`

## Native directory metadata coverage

- Extended contract temp materialization with `directory = true` in both
  `yulang contract` and the Rust manifest helper.
- Added `file_native_meta_directory`, a native CLI `file-resource` metadata case
  proving public `file::meta` and `exists` / `is_file` / `is_dir` wrappers
  report native directories as `file_kind::dir`.
- The fixture intentionally does not assert directory size because that value
  is filesystem-dependent.
- Added the case to the focused release-smoke file-resource set.
- Validation:
  - `cargo fmt --all -- --check`
  - `cargo check -q -p yulang`
  - `cargo build -q -p yulang`
  - `timeout 180s target/debug/yulang --std-root lib contract --contract
    file-resource --case file_native_meta_directory tests/yulang/cases.toml`
  - `timeout 180s cargo test -q -p yulang --test cli
    public_contract_manifest_cli_cases_hold -- --test-threads=1`
  - `scripts/release-smoke.sh target/debug/yulang` with the 18-case focused
    file-resource set
  - `timeout 330s target/debug/yulang --std-root lib contract --contract
    file-resource tests/yulang/cases.toml` passed in about `2m52s`
    (`contract cases ok: 47`)

## Refresh release archive evidence after directory metadata

- Rebuilt the release binary after adding directory temp materialization and
  native directory metadata coverage.
- Repackaged the local Linux archive as `contract-v1-directory-metadata`.
- Validation:
  - `cargo build -q --release -p yulang`
  - `scripts/package-release.sh --version contract-v1-directory-metadata
    --target x86_64-unknown-linux-gnu --binary target/release/yulang --out
    target/release-contract-v1-directory-metadata`
  - `scripts/release-archive-smoke.sh
    target/release-contract-v1-directory-metadata/yulang-x86_64-unknown-linux-gnu.tar.gz`

## Native file metadata predicate coverage

- Added `file_native_meta_file`, an isolated native CLI `file-resource`
  metadata case for normal files.
- The case proves `file::meta` reports `file_kind::file`, preserves the byte
  size for a small text file, and keeps `exists` / `is_file` / `is_dir` as
  metadata wrappers.
- Added the case to the focused release-smoke file-resource set.
- Validation:
  - `cargo fmt --all -- --check`
  - `git diff --check`
  - `timeout 180s target/debug/yulang --std-root lib contract --contract
    file-resource --case file_native_meta_file tests/yulang/cases.toml`
  - `timeout 180s cargo test -q -p yulang --test cli
    public_contract_manifest_cli_cases_hold -- --test-threads=1`
  - `scripts/release-smoke.sh target/debug/yulang` with the 19-case focused
    file-resource set
  - `timeout 360s target/debug/yulang --std-root lib contract --contract
    file-resource tests/yulang/cases.toml` passed in about `2m59s`
    (`contract cases ok: 48`)

## Refresh release archive evidence after file metadata predicates

- Rebuilt the release binary after adding the positive native file metadata
  predicate case to focused release smoke.
- Repackaged the local Linux archive as `contract-v1-file-metadata-predicates`.
- Validation:
  - `cargo build -q --release -p yulang`
  - `scripts/package-release.sh --version contract-v1-file-metadata-predicates
    --target x86_64-unknown-linux-gnu --binary target/release/yulang --out
    target/release-contract-v1-file-metadata-predicates`
  - `scripts/release-archive-smoke.sh
    target/release-contract-v1-file-metadata-predicates/yulang-x86_64-unknown-linux-gnu.tar.gz`

## Focused file-resource smoke timeout raised

- Raised the default `YULANG_SMOKE_FILE_RESOURCE_TIMEOUT` from `120s` to
  `180s`.
- The focused file-resource release smoke is now 19 cases and runs in about
  85-90 seconds locally, so the old default had too little margin for slower CI
  runners.
- Validation:
  - `git diff --check`
  - `scripts/release-smoke.sh target/debug/yulang`

## Broad validation after file-resource updates

- Ran broader validation after native metadata, ambient host I/O error, and
  focused release-smoke changes.
- Validation:
  - `cargo check -q --release -p evidence-vm -p yulang`
  - `cargo test -q -p evidence-vm -- --test-threads=1` (`103 passed`)
  - `cargo test -q -p yulang runtime_evidence -- --test-threads=1`
    (`30 passed` in the CLI runtime-evidence slice)
  - `tests/yulang/yulang-adversarial-corpus/probe.sh`
  - `timeout 360s cargo test -q -p yulang --test cli -- --test-threads=1`
    (`114 passed`, about `2m51s`)

## Native typed file operation failures

- Added a native `file-resource` / `host-act` / `typed-failure` contract case
  for `file::load` on a missing file and `file::store` on a directory.
- This records that the native host path constructs domain `io_err`
  constructors (`not_found`, `failed`) instead of exposing integer error code
  bridges.
- Relaxed the file-resource manifest category rule narrowly so native
  runtime cases may be categorized by `host-act` in addition to
  `resource-lifetime` and `metadata`.
- Updated release TODO wording so the release-smoke evidence matches the
  current focused subset instead of the older two-case description.
- Validation:
  - `cargo run -q -p yulang -- --std-root lib contract --contract
    file-resource --case file_native_protocol_typed_failures
    tests/yulang/cases.toml` (`1` passed)
  - `cargo fmt --all -- --check`
  - `git diff --check`
  - `cargo run -q -p yulang -- --std-root lib contract --contract
    file-resource tests/yulang/cases.toml` (`49` passed)
  - `scripts/release-smoke.sh target/debug/yulang`

## Host surface isolation canary

- Added an Evidence VM unit canary that fixes the exact current contract host
  operation set (`console.out.write`, `file.load`, `file.store`, `file.meta`,
  `file_buffer.ambient_get`, `file_buffer.ambient_set`) separately from the
  raw-compat range/raw/snapshot set.
- This guards the Contract v1 boundary: raw range and legacy snapshot
  operations must not silently drift back into the contract surface while they
  remain provisional.
- Validation:
  - `cargo fmt --all -- --check`
  - `cargo test -q -p evidence-vm runtime::host -- --test-threads=1`
    (`13 passed`)
  - `git diff --check`

## Focused file-resource contract intersections

- Ran focused contract intersections after the typed-failure and host surface
  canaries to make the current evidence easier to audit.
- Validation:
  - `cargo run -q -p yulang -- --std-root lib contract --contract
    file-resource --contract public-signature tests/yulang/cases.toml`
    (`19 passed`)
  - `cargo run -q -p yulang -- --std-root lib contract --contract
    file-resource --contract host-act tests/yulang/cases.toml` (`13 passed`)

## Manifest raw-compat tag

- Added `raw-compat` to the manifest taxonomy and applied it to current
  range/raw/snapshot helper canaries: the `read_at` / `write_at` native runtime
  case, `read_at` / `write_at` public signatures, and legacy `open_text` /
  `open` / `open_in` public signatures.
- This mirrors the runtime host manifest `surface=raw-compat` split at the
  public contract manifest level without changing language semantics.
- Validation:
  - `cargo fmt --all -- --check`
  - `cargo run -q -p yulang -- --std-root lib contract --contract
    file-resource --contract raw-compat tests/yulang/cases.toml` (`6 passed`)
  - `cargo run -q -p yulang -- --std-root lib contract --contract
    file-resource --case file_read_write_at_native_result
    tests/yulang/cases.toml` (`1` passed)
  - `git diff --check`

## CLI manifest taxonomy mirror fix

- Problem: after adding the `raw-compat` manifest tag, `cargo test -q -p yulang
  public -- --test-threads=1` failed in
  `public_contract_manifest_cli_cases_hold` because the CLI regression test has
  a second manifest taxonomy mirror and did not yet know `raw-compat`.
- Problem: after adding the tag there, the same test failed again because the
  CLI mirror still required every native file-resource runtime case to be
  categorized only as `resource-lifetime` or `metadata`; it did not include the
  newly accepted `host-act` category used by typed operation-failure cases.
- Fix: updated `crates/yulang/tests/cli.rs` to mirror the contract runner
  taxonomy and file-resource category rule.
- Validation:
  - `cargo fmt --all -- --check`
  - `git diff --check`
  - `cargo test -q -p yulang public -- --test-threads=1` (initial failure
    recorded above, then `9 passed` in lib public slice and `18 passed` in CLI
    public slice after the mirror fix)
  - `cargo build -q --release -p yulang`
  - `scripts/package-release.sh --version contract-v1-raw-compat-tags --target
    x86_64-unknown-linux-gnu --binary target/release/yulang --out
    target/release-contract-v1-raw-compat-tags`
  - `scripts/release-archive-smoke.sh
    target/release-contract-v1-raw-compat-tags/yulang-x86_64-unknown-linux-gnu.tar.gz`

## Broad CLI validation after manifest taxonomy work

- Ran the full CLI integration test after the raw-compat manifest taxonomy and
  CLI mirror fixes.
- Validation:
  - `timeout 360s cargo test -q -p yulang --test cli -- --test-threads=1`
    (`114 passed`, about `2m47s`)

## Native helper typed file failures

- Added a public helper counterpart to the protocol typed-failure case:
  `read_text` on a missing file and `write_text` on a directory are wrapped by
  `io_err::wrap` and observed as `io_err::not_found` / `io_err::failed`.
- Added the case to the focused release-smoke file-resource set so direct smoke
  covers both host-act protocol failures and public helper propagation.
- Validation:
  - `cargo fmt --all -- --check`
  - `cargo run -q -p yulang -- --std-root lib contract --contract
    file-resource --case file_native_helper_typed_failures
    tests/yulang/cases.toml` (`1` passed)
  - `git diff --check`
  - `scripts/release-smoke.sh target/debug/yulang`
  - `cargo run -q -p yulang -- --std-root lib contract --contract
    file-resource --contract host-act tests/yulang/cases.toml` (`14 passed`)
  - `cargo run -q -p yulang -- --std-root lib contract --contract
    file-resource tests/yulang/cases.toml` (`50 passed`)

## Public/no-cache validation after helper typed failures

- Ran focused follow-up validation after adding the helper typed-failure case.
- Validation:
  - `cargo run -q -p yulang -- --std-root lib --no-cache contract --contract
    file-resource --case file_native_helper_typed_failures
    tests/yulang/cases.toml` (`1` passed)
  - `cargo test -q -p yulang public -- --test-threads=1` (`9 passed` in the
    lib public slice and `18 passed` in the CLI public slice)

## Unsupported host read_text helper

- Added `file_read_text_unsupported_host` so the public `read_text` helper has
  explicit sandbox/fake-success coverage, not only the lower `file::meta` and
  ambient `file::text` paths.
- Added it to the focused release-smoke file-resource set.
- Validation:
  - `cargo fmt --all -- --check`
  - `cargo run -q -p yulang -- --std-root lib contract --contract
    file-resource --case file_read_text_unsupported_host
    tests/yulang/cases.toml` (`1` passed)
  - `git diff --check`
  - `scripts/release-smoke.sh target/debug/yulang`
  - `cargo run -q -p yulang -- --std-root lib contract --contract
    file-resource tests/yulang/cases.toml` (`51 passed`)

## Unsupported host write_text helper

- Added the symmetric `file_write_text_unsupported_host` case so public
  `write_text` cannot fake success under `--host unsupported`.
- Added it to the focused release-smoke file-resource set.
- Validation:
  - `cargo fmt --all -- --check`
  - `cargo run -q -p yulang -- --std-root lib contract --contract
    file-resource --case file_write_text_unsupported_host
    tests/yulang/cases.toml` (`1` passed)
  - `git diff --check`
  - `scripts/release-smoke.sh target/debug/yulang`
  - `cargo run -q -p yulang -- --std-root lib contract --contract
    file-resource tests/yulang/cases.toml` (`52 passed`)

## File io_err constructor public signatures

- Added public-signature canaries for `std.io.file.io_err.not_found` and
  `std.io.file.io_err.failed`, matching the constructors observed by the native
  protocol/helper typed-failure runtime cases.
- Validation:
  - `cargo fmt --all -- --check`
  - `cargo run -q -p yulang -- --std-root lib contract --contract
    file-resource --contract typed-failure tests/yulang/cases.toml`
    (`4 passed`)
  - `cargo run -q -p yulang -- --std-root lib contract --contract
    file-resource --contract public-signature tests/yulang/cases.toml`
    (`21 passed`)
  - `git diff --check`
  - `cargo run -q -p yulang -- --std-root lib contract --contract
    file-resource tests/yulang/cases.toml` (`54 passed`)

## Archive/public validation after io_err signature canaries

- Ran release/archive and public regression validation after adding the
  `io_err` constructor public-signature cases.
- Validation:
  - `cargo test -q -p yulang public -- --test-threads=1` (`9 passed` in the
    lib public slice and `18 passed` in the CLI public slice)
  - `scripts/package-release.sh --version contract-v1-io-err-signatures
    --target x86_64-unknown-linux-gnu --binary target/release/yulang --out
    target/release-contract-v1-io-err-signatures`
  - `scripts/release-archive-smoke.sh
    target/release-contract-v1-io-err-signatures/yulang-x86_64-unknown-linux-gnu.tar.gz`

## Resolve stale pure mock blocker note

- Updated `notes/bugs/file_text_with_mock_resource_lifetime_blocker.yu` from
  open to resolved.
- The note now points at the production `text_with` state-passing protocol over
  public `file::load` / `file::store` and lists the current source mock
  resource-lifetime fixtures.
- Remaining blockers are narrowed to `raw-compat` session replacement and typed
  native ambient failures.

## Refresh release archive evidence after file parity

- Rebuilt the release binary after focused release-smoke gained multi-case
  contract filtering and file-resource parity cases grew to 43 full cases.
- Repackaged the local Linux archive as `contract-v1-file-parity`.
- Validation:
  - `cargo build -q --release -p yulang`
  - `scripts/package-release.sh --version contract-v1-file-parity --target
    x86_64-unknown-linux-gnu --binary target/release/yulang --out
    target/release-contract-v1-file-parity`
  - `scripts/release-archive-smoke.sh
    target/release-contract-v1-file-parity/yulang-x86_64-unknown-linux-gnu.tar.gz`

## Refresh release archive evidence after host surface classes

- Rebuilt the release binary after adding `surface=...` to
  `debug host-act-manifest`.
- Repackaged the local Linux archive as `contract-v1-host-surface`.
- Validation:
  - `cargo build -q --release -p yulang`
  - `scripts/package-release.sh --version contract-v1-host-surface --target
    x86_64-unknown-linux-gnu --binary target/release/yulang --out
    target/release-contract-v1-host-surface`
  - `scripts/release-archive-smoke.sh
    target/release-contract-v1-host-surface/yulang-x86_64-unknown-linux-gnu.tar.gz`

## Ambient failure typing blocked

- Tried the next obvious cleanup: make `file_buffer::ambient_get` return
  `result str io_err`, make `ambient_set` return `result unit io_err`, and have
  `file::text` unwrap those results.
- Rejected that change before commit. It made `std.io.file.text` project as:
  `path -> ref(file_buffer | io_err | '[io_err] <: file_buffer & io_err, str)`,
  which leaks an unsolved row constraint into the public signature. The mock
  unscoped fixture also failed with `conflicting type candidates` after changing
  the handler arms to resume with `result::ok`.
- Conclusion: native ambient read/write failure typing cannot be solved by
  locally wrapping `file_buffer` operations in `result`. It needs a real
  session/error boundary or a solver/lowering change that reuses the row
  variable cleanly before public projection. I restored the previous
  `str` / `unit` ambient operations and left the blocker open.
- Validation after revert:
  - `cargo fmt --all -- --check`
  - `cargo check -q -p evidence-vm -p yulang`

## Broad validation after file contract evidence

- Re-ran the broad runtime and adversarial validation slices after the file
  resource contract evidence commits.
- Validation:
  - `cargo check -q -p evidence-vm -p yulang`
  - `cargo test -q -p evidence-vm -- --test-threads=1` (`104 passed`)
  - `cargo test -q -p yulang runtime_evidence -- --test-threads=1`
    (`30 passed`)
  - `tests/yulang/yulang-adversarial-corpus/probe.sh` (`all adversarial
    probes passed`, logs at `/tmp/yulang-adversarial-probe.1311121`)

## Native invalid path typed failure

- Changed the native host `io_err` classification so `InvalidInput` and
  `InvalidData` host failures construct `io_err::invalid_path` instead of
  falling through to the generic `io_err::failed` constructor.
- Added `file_native_invalid_path_typed_failure`, which observes invalid host
  paths through direct `file::load` / `file::store` and through public
  `read_text` / `write_text` wrapped by `io_err::wrap`.
- Added public signature canaries for `std.io.file.io_err.denied` and
  `std.io.file.io_err.invalid_path`.
- Validation:
  - `cargo fmt --all -- --check`
  - `cargo run -q -p yulang -- --std-root lib contract --case
    file_native_invalid_path_typed_failure tests/yulang/cases.toml`
  - `cargo test -q -p evidence-vm runtime::host -- --test-threads=1`
    (`13 passed`)
  - `cargo run -q -p yulang -- --std-root lib contract --contract
    file-resource --contract typed-failure tests/yulang/cases.toml`
    (`7 passed`)
  - `cargo run -q -p yulang -- --std-root lib contract --contract
    file-resource --contract public-signature tests/yulang/cases.toml`
    (`23 passed`)
  - `cargo run -q -p yulang -- --std-root lib contract --contract
    file-resource tests/yulang/cases.toml` (`57 passed`)
  - `scripts/release-smoke.sh target/debug/yulang`
  - `cargo test -q -p yulang public -- --test-threads=1` (`9 passed` in the
    lib public slice and `18 passed` in the CLI public slice)

## Archive validation after invalid path typing

- Rebuilt the release binary after native invalid path host failures started
  constructing `io_err::invalid_path`.
- Repackaged the local Linux archive as `contract-v1-invalid-path`.
- Validation:
  - `cargo build -q --release -p yulang`
  - `scripts/package-release.sh --version contract-v1-invalid-path --target
    x86_64-unknown-linux-gnu --binary target/release/yulang --out
    target/release-contract-v1-invalid-path`
  - `scripts/release-archive-smoke.sh
    target/release-contract-v1-invalid-path/yulang-x86_64-unknown-linux-gnu.tar.gz`

## Full CLI validation after invalid path typing

- Re-ran the full CLI integration suite after adding the invalid-path typed
  failure case and release smoke coverage.
- Validation:
  - `timeout 360s cargo test -q -p yulang --test cli -- --test-threads=1`
    (`114 passed`)

## Catch raw brace diagnostic parity

- Confirmed `catch {:` is already a `stable-core` manifest diagnostic case
  (`catch_raw_brace_scrutinee`) and reports
  `yulang.missing-catch-scrutinee` instead of crashing.
- Added that fixture to the LSP/server catch syntax range table so CLI
  manifest diagnostics and server diagnostics share the same code and primary
  range for the raw brace scrutinee regression.
- Validation:
  - `cargo fmt --all -- --check`
  - `cargo test -q -p yulang server::tests::diagnostics_use_catch_syntax_ranges
    -- --test-threads=1`
  - `cargo run -q -p yulang -- --std-root lib contract --case
    catch_raw_brace_scrutinee tests/yulang/cases.toml`

## Host manifest CLI surface count canary

- Strengthened the CLI `debug host-act-manifest` regression so the user-facing
  manifest output must expose exactly 6 `contract` operations and 10
  `raw-compat` operations.
- This mirrors the Evidence VM unit surface-set canary at the CLI boundary and
  keeps legacy range/raw/snapshot file operations visibly isolated from the
  Contract v1 protocol surface.
- Validation:
  - `cargo fmt --all -- --check`
  - `cargo test -q -p yulang --test cli
    debug_host_act_manifest_prints_runtime_registry_view -- --test-threads=1`

## Raw-compat blocker wording narrowed

- Updated the Contract v1 evidence/status notes so they no longer describe
  raw/provisional isolation itself as missing. That isolation is now covered by
  manifest tags, `debug host-act-manifest`, CLI surface counts, and Evidence VM
  unit canaries.
- The remaining blocker is the public replacement or removal of `raw-compat`
  file session operations, plus the separate native ambient failure typing
  blocker.
- Validation:
  - `cargo test -q -p yulang public -- --test-threads=1` (`9 passed` in the
    lib public slice and `18 passed` in the CLI public slice)

## Host scheduler dropped-branch canary

- Added an Evidence VM scheduler unit canary that refuses to allocate host
  operation instance ids or child branches for a dropped branch.
- This keeps the structured-concurrency first slice and future record/replay
  ids from accidentally treating dropped suspended branches as live host
  execution lanes.
- Validation:
  - `cargo fmt --all -- --check`
  - `cargo test -q -p evidence-vm runtime::scheduler -- --test-threads=1`
    (`6 passed`)
  - `cargo test -q -p evidence-vm runtime::host -- --test-threads=1`
    (`13 passed`)

## Ambiguous method related origin

- Added `SourceDiagnosticRelatedOrigin::ImplCandidate` and attached it to
  ambiguous role method candidate related ranges.
- Extended the public contract manifest to require
  `["impl-candidate", "impl-candidate"]` on `ambiguous_method_diagnostic`.
- Extended the wasm playground role/method diagnostic payload test so candidate
  related entries expose `origin = "impl_candidate"`.
- Validation:
  - `cargo fmt --all -- --check`
  - `cargo run -q -p yulang -- --std-root lib contract --case
    ambiguous_method_diagnostic tests/yulang/cases.toml`
  - `cargo test -q -p yulang ambiguous -- --test-threads=1`
  - `cargo test -q -p wasm check_inner_returns_role_method_diagnostic_payloads
    -- --test-threads=1`
  - `cargo test -q -p yulang public -- --test-threads=1` (`9 passed` in the
    lib public slice and `18 passed` in the CLI public slice)

## Catch raw brace wasm diagnostic parity

- Added `catch_raw_brace_scrutinee` to the wasm playground catch syntax
  diagnostics table.
- The raw brace catch regression is now covered through CLI manifest, LSP/server
  diagnostics, and wasm playground diagnostics with the same code and primary
  range.
- Validation:
  - `cargo fmt --all -- --check`
  - `cargo test -q -p wasm check_inner_returns_catch_syntax_codes_and_ranges
    -- --test-threads=1`
  - `cargo run -q -p yulang -- --std-root lib contract --case
    catch_raw_brace_scrutinee tests/yulang/cases.toml`

## Console unsupported-host capability canary

- Added `console_unsupported_host`, a small runtime-failure fixture for
  `std::io::console::out::write` under `--host unsupported`.
- This proves the unsupported-host capability contract is not file-specific:
  console host acts now have the same manifest-level denial shape as file host
  acts, with `yulang.unsupported-host-capability` rather than fake success or an
  escaped raw effect.
- Validation:
  - `cargo fmt --all -- --check`
  - `cargo run -q -p yulang -- --std-root lib contract --case
    console_unsupported_host tests/yulang/cases.toml` (`1 passed`)
  - `cargo run -q -p yulang -- --std-root lib contract --contract
    host.unsupported tests/yulang/cases.toml` (`22 passed`)
  - `cargo run -q -p yulang -- --std-root lib contract --contract host-act
    --contract host.unsupported tests/yulang/cases.toml` (`4 passed`)
  - `cargo test -q -p yulang public -- --test-threads=1` (`9 passed` in the
    lib public slice and `18 passed` in the CLI public slice)

## Console mock-host routing canary

- Added `console_mock_out_handler`, a source-handler fixture for
  `std::io::console::out::write` under `--host unsupported`.
- The fixture proves console host acts follow the same first routing rule as
  file acts: source handlers intercept before the host registry, and disabled
  native host operations are only consulted if no source handler catches the
  operation.
- Problem encountered: the first fixture shape returned directly from the
  operation arm without resuming `k`, which currently hits
  `yulang.unsupported-runtime-feature` for direct abortive continuation
  binding. The committed fixture uses the existing recursive handler pattern
  and resumes `k ()`, matching `examples/10_effect_handler.yu`.
- Validation:
  - `cargo fmt --all -- --check`
  - `cargo run -q -p yulang -- --std-root lib contract --case
    console_mock_out_handler tests/yulang/cases.toml` (`1 passed`)
  - `cargo run -q -p yulang -- --std-root lib contract --contract host-act
    --contract mock-host tests/yulang/cases.toml` (`4 passed`)
  - `cargo run -q -p yulang -- --std-root lib contract --contract console
    tests/yulang/cases.toml` (`3 passed`)
  - `cargo test -q -p yulang public -- --test-threads=1` (`9 passed` in the
    lib public slice and `18 passed` in the CLI public slice)

## Console host-act public signature canary

- Added `std_console_out_write_public_signature`, an exact public type canary
  for `std::io::console::out.write`.
- This keeps the console host-act boundary under the same public-signature
  cleanliness rule as the file host-act cases: no `#...`, no `AllExcept(...)`,
  and no placeholder-like private evidence fragments.
- Problem encountered: tagging this case as `standard-api` failed the manifest
  taxonomy check because `standard-api` currently requires a narrower API-area
  tag from the existing result/error/path/file groups. The committed case keeps
  the narrower `console` / `host-act` tags instead of broadening the taxonomy in
  the same change.
- Validation:
  - `cargo fmt --all -- --check`
  - `cargo run -q -p yulang -- --std-root lib contract --case
    std_console_out_write_public_signature tests/yulang/cases.toml`
    (`1 passed`)
  - `cargo run -q -p yulang -- --std-root lib contract --contract console
    --contract public-signature tests/yulang/cases.toml` (`1 passed`)
  - `cargo test -q -p yulang public -- --test-threads=1` (`9 passed` in the
    lib public slice and `18 passed` in the CLI public slice)

## Typed ambient failure direct-row attempt rejected

- Tried the direct small change for the remaining native ambient failure typing
  blocker:
  - change `file_buffer::ambient_get` to `path -> result str io_err`;
  - change `file_buffer::ambient_set` to `(path, str) -> result unit io_err`;
  - make `file::text` unwrap those results and expose
    `ref '[file_buffer, io_err] str`;
  - return `result::ok` / `result::err io_err` from the native ambient host
    operations.
- Result: rejected and reverted. This reproduces the known reason the blocker
  cannot be solved by mixing `io_err` directly into the ambient ref row:
  - `file_text_unscoped_native_handler_extent` failed with
    `conflicting type candidates for slot 1: [std::io::file::io_err, std::io::file::file_buffer] vs []`;
  - `std_file_text_public_signature` leaked a constraint-shaped public type:
    `ref(std::io::file::file_buffer | std::io::file::io_err | '[std::io::file::io_err] <: std::io::file::file_buffer & std::io::file::io_err, str)`.
- Conclusion: typed ambient failures need the planned public session/resource
  boundary or another upstream representation change. Do not retry the direct
  `ref '[file_buffer, io_err]` row mix as the fix.
- Rejected validation commands:
  - `cargo run -q -p yulang -- --std-root lib contract --case
    file_text_unscoped_native_handler_extent tests/yulang/cases.toml`
  - `cargo run -q -p yulang -- --std-root lib contract --case
    file_text_native_missing_host_io_error tests/yulang/cases.toml`
  - `cargo run -q -p yulang -- --std-root lib contract --case
    std_file_text_public_signature --case
    std_file_buffer_ambient_get_public_signature --case
    std_file_buffer_ambient_set_public_signature tests/yulang/cases.toml`

## Release smoke covers console host-act canaries

- Added a focused `host-act` contract block to `scripts/release-smoke.sh`.
- The direct smoke binary now runs:
  - `console_unsupported_host`;
  - `console_mock_out_handler`;
  - `std_console_out_write_public_signature`.
- This keeps release smoke from proving only file host acts while the
  Contract v1 host-act track says console and file should share one routing
  shape.
- Problem encountered: the first `scripts/release-smoke.sh target/debug/yulang`
  run failed in `file_text_unscoped_handler_discharge` with the same
  `conflicting type candidates` shape as the rejected ambient typing attempt.
  The cause was a stale `target/debug/yulang` binary built during that rejected
  experiment. After `cargo build -q -p yulang`, the same release smoke passed.
- Validation:
  - `cargo fmt --all -- --check`
  - `cargo run -q -p yulang -- --std-root lib contract --contract host-act
    --case console_unsupported_host --case console_mock_out_handler --case
    std_console_out_write_public_signature tests/yulang/cases.toml`
    (`3 passed`)
  - `cargo build -q -p yulang`
  - `scripts/release-smoke.sh target/debug/yulang`

## Broad validation after console host-act coverage

- Re-ran the broad contract filters after adding console host-act runtime,
  public-signature, and release-smoke coverage.
- Validation:
  - `cargo run -q -p yulang -- --std-root lib contract --contract host-act
    tests/yulang/cases.toml` (`18 passed`)
  - `cargo run -q -p yulang -- --std-root lib contract --contract host-act
    --contract public-signature tests/yulang/cases.toml` (`8 passed`)
  - `cargo run -q -p yulang -- --std-root lib contract --contract
    file-resource tests/yulang/cases.toml` (`57 passed`)
  - `cargo test -q -p yulang public -- --test-threads=1` (`9 passed` in the
    lib public slice and `18 passed` in the CLI public slice)
  - `cargo test -q -p evidence-vm runtime::host -- --test-threads=1`
    (`13 passed`)

## Archive smoke after console host-act release coverage

- Rebuilt the release binary and packaged a local archive after
  `release-smoke.sh` gained the focused console `host-act` contract block.
- Validation:
  - `cargo build -q --release -p yulang`
  - `scripts/package-release.sh --version
    contract-v1-console-host-act-smoke --target x86_64-unknown-linux-gnu
    --binary target/release/yulang --out
    target/release-contract-v1-console-host-act-smoke`
  - `scripts/release-archive-smoke.sh
    target/release-contract-v1-console-host-act-smoke/yulang-x86_64-unknown-linux-gnu.tar.gz`

## Full CLI validation after host-act release smoke

- Ran the full CLI integration suite after the console host-act canaries,
  release-smoke change, full file-resource filter, and archive smoke.
- Validation:
  - `timeout 360s cargo test -q -p yulang --test cli -- --test-threads=1`
    (`114 passed`)

## Full package validation after host-act work

- Ran broader package tests after the host-act and release/archive smoke work.
- Problem encountered: `timeout 360s cargo test -q -p yulang -- --test-threads=1`
  exited `124` after the lib slice had passed (`271 passed`) and the CLI slice
  was near completion. This was a timeout budget issue rather than a test
  failure; the full command now takes longer because it includes both lib and
  CLI integration tests.
- Validation:
  - `timeout 240s cargo test -q -p evidence-vm -- --test-threads=1`
    (`105 passed`)
  - `timeout 720s cargo test -q -p yulang -- --test-threads=1`
    (`271 passed` in the lib slice and `114 passed` in the CLI slice)

## Hardening smoke after host-act work

- Ran the hardening smoke with the current release binary after the console
  host-act and release-smoke changes.
- The run covered the focused infer canary, public contract tests, release
  binary stable-core cases, full release-binary `file-resource` filter
  (`57 passed`), backend interpreter/control-vm filters, cache smoke, wasm
  diagnostics, public examples, release smoke with server startup, source-unit
  cache smoke, replay-skip comparison, evidence-only replay smoke, and the
  adversarial corpus.
- Validation:
  - `timeout 900s scripts/hardening-smoke.sh`

## Release gate after host-act work

- Ran the full release gate after the host-act, release-smoke, archive-smoke,
  full package, and hardening-smoke validations.
- Problem encountered: the default `scripts/release-gate.sh` uses
  `YULANG_RELEASE_GATE_TEST_TIMEOUT=300s`, which was too short for
  `cargo test -q -p parser -p infer -p yulang -- --test-threads=1` because the
  command reaches the `yulang` CLI integration slice after the `yulang` lib
  slice. The first run exited `124` after the lib slice passed (`271 passed`)
  and the CLI slice was already in progress.
- Raised the release-gate default test timeout to `720s`, matching the passing
  validation run.
- Validation:
  - `timeout 1500s env YULANG_RELEASE_GATE_TEST_TIMEOUT=720
    scripts/release-gate.sh`

## Stage 2 closeout spec received

- Noted external commit `56a3a346`:
  `notes/design/2026-07-03-contract-v1-stage2-closeout.md`.
- The spec gives the proper fix for the rejected typed ambient row attempt:
  add an eager `file_buffer::ambient_touch : path -> result unit io_err` so
  `file::text` can throw typed `io_err` at lens creation time, while the
  returned `ref` remains `ref '[file_buffer] str`.
- It also decides that snapshot `raw-compat` helpers retire for Contract v1,
  while `read_at` / `write_at` remain provisional raw helpers.
- Next implementation start point: Stage A in that document, not another direct
  `ref '[file_buffer, io_err]` attempt.

## Stage 2 closeout implementation with integrated file ambient ops

- Applied the updated Stage 2 closeout spec after the D4 user decision:
  ambient file operations are no longer a separate `file_buffer` act. They now
  live under the unified `std::io::file::file` act as `ambient_touch`,
  `ambient_get`, and `ambient_set`.
- Retired the legacy snapshot raw-compat bridge:
  `open_text_raw`, `file_get`, `file_set`, `file_flush`,
  `open_text_snapshot_raw`, `file_snapshot_get`, `file_snapshot_set`,
  `file_snapshot_commit`, and the public `open_text` / `open` / `open_in`
  helpers. The runtime manifest now leaves only `read_at` / `write_at` as
  `raw-compat`.
- Replaced `file::text` with the eager-touch shape from the spec:
  `ambient_touch` runs at lens creation and returns typed `io_err`, while the
  returned ref remains `ref '[file] str`.
- Added native and mock contract coverage:
  - `file_text_native_missing_typed_io_err`
  - `file_ambient_get_untouched_missing_host_io_error`
  - `file_text_mock_ambient_typed_not_found`
- Updated public-signature canaries so `std.io.file.text` projects
  `[file, io_err] ref '[file] str`, and ambient operation canaries now target
  `std.io.file.file.ambient_*`.
- Problem encountered: the first mock typed ambient fixture returned a tuple
  mixing `path` and `str` in different case arms. The fix was to assert only
  the observed constructor tag, keeping the golden stable and type-consistent.
- Validation:
  - `cargo fmt --all -- --check`
  - `timeout 240s cargo check -q -p evidence-vm -p yulang`
  - `timeout 240s cargo test -q -p evidence-vm runtime::host -- --test-threads=1`
    (`13 passed`)
  - `timeout 240s cargo run -q -p yulang -- --std-root lib contract --contract
    file-resource tests/yulang/cases.toml` (`57 passed`)
  - `timeout 240s cargo run -q -p yulang -- --std-root lib contract --contract
    host-act tests/yulang/cases.toml` (`19 passed`)
  - `timeout 240s cargo test -q -p yulang public -- --test-threads=1`
    (`9 focused public tests, then 18 CLI public tests passed`)
  - `timeout 240s cargo test -q -p yulang --test cli
    debug_host_act_manifest_prints_runtime_registry_view -- --test-threads=1`
  - `timeout 240s cargo test -q -p yulang --test cli
    compatible_run_std_file_text_with_host_contract -- --test-threads=1`
  - `timeout 360s scripts/release-smoke.sh target/debug/yulang`
  - `timeout 360s cargo test -q -p yulang --test cli -- --test-threads=1`
    (`114 passed`)

## Contract v1 file-resource stable classification

- Closed the post-Stage-2 classification gap in the manifest and docs:
  Contract v1 protocol-center file cases are now `stable-api`, while
  `raw-compat` remains `migration-canary`.
- Promoted the executable file-resource center in `tests/yulang/cases.toml`:
  public `load` / `store` / `meta`, `text_with`, ambient `text`,
  unsupported-host denial, typed file failures, source mock routing, and exact
  public signatures.
- Kept only the provisional range surface on `migration-canary` /
  `raw-compat`: `file_read_write_at_native_result`,
  `std_file_read_at_public_signature`, and
  `std_file_write_at_public_signature`.
- Added a CLI manifest-shape assertion so future file-resource cases cannot
  silently drift: non-`raw-compat` file-resource cases must be `stable-api`, and
  `raw-compat` cases must remain `migration-canary`.
- Updated `docs/status.md`, `docs/language/file-resource-contract.md`,
  `docs/language/contract-v1-file-evidence.md`, `tests/yulang/README.md`, and
  this priority memo so they no longer describe the file protocol center as a
  migration canary or describe the current ambient path as a separate
  `file_buffer` act.
- Problem encountered: the first raw-compat revert patch matched the first
  identical tag shape and accidentally put `file_text_with_commit` back on
  `migration-canary`. The new CLI manifest assertion caught it immediately.
  I corrected `file_text_with_commit` to `stable-api` and the named
  `file_read_write_at_native_result` raw block to `migration-canary`.
- Validation:
  - `cargo fmt --all -- --check`
  - `timeout 240s cargo run -q -p yulang -- --std-root lib contract
    --contract file-resource --contract stable-api tests/yulang/cases.toml`
    (`54 passed`)
  - `timeout 240s cargo run -q -p yulang -- --std-root lib contract
    --contract file-resource --contract migration-canary tests/yulang/cases.toml`
    (`3 passed`)
  - `timeout 240s cargo run -q -p yulang -- --std-root lib contract
    --contract file-resource --contract raw-compat tests/yulang/cases.toml`
    (`3 passed`)
  - `timeout 300s cargo run -q -p yulang -- --std-root lib contract
    --contract file-resource tests/yulang/cases.toml` (`57 passed`)
  - `timeout 240s cargo test -q -p yulang --test cli
    public_contract_manifest_cli_cases_hold -- --test-threads=1`
  - `timeout 240s cargo test -q -p yulang public -- --test-threads=1`
    (`9 focused public tests, then 18 CLI public tests passed`)
  - `timeout 360s scripts/release-smoke.sh target/debug/yulang`
  - `timeout 360s cargo test -q -p yulang --test cli -- --test-threads=1`
    (`114 passed`)

## Host ABI manifest WP2 Stage 1

- Committed the host ABI/manifest handoff documents first, then started the
  compiler-produced manifest path without changing runtime dispatch or public
  behavior.
- Added contextual `pub host act` parsing while keeping `host` usable as an
  ordinary identifier. `host` is emitted as a modifier token inside `ActDecl`,
  so `type_decl_name` still sees the real act name.
- Threaded the host flag through module registration, compiled namespace
  serialization, compiled namespace import/merge restore, and namespace cache
  hashing.
- Marked `std::io::file::file` and `std::io::console::out` as host acts; left
  `err`, `warn`, and `die` unchanged.
- Added `poly::host_manifest` with deterministic operation ordering, column
  assignment, stable non-crypto hash, surface/tier enums, and ABI v0
  length-prefixed symbol mangling.
- Added `infer::host_acts` to generate a manifest from compiled namespace,
  lowering, and typed surfaces. The signature field uses existing
  `poly::dump::format_scheme` over compiled typed schemes, not a new printer.
- Added the Stage 1 CLI-side equivalence test comparing generated std manifest
  with the current evidence-vm static table on `(act_id, operation_id, path,
  tier, surface)`.
- Validation:
  - `cargo fmt --all -- --check`
  - `cargo check -q -p poly -p infer -p specialize -p evidence-vm -p yulang`
  - `cargo test -q -p parser host -- --test-threads=1`
  - `cargo test -q -p poly host_manifest -- --test-threads=1`
  - `cargo test -q -p poly` (`48 passed`)
  - `cargo test -q -p infer host_acts -- --test-threads=1`
  - `cargo test -q -p infer` (`513 passed`)
  - `cargo test -q -p evidence-vm` (`105 passed`)
  - `cargo test -q -p yulang --test cli compiler_generated_host_manifest_matches_runtime_stage1_table -- --test-threads=1`
  - `cargo test -q -p yulang --test cli -- --test-threads=1` (`115 passed`)
  - `cargo run -q -p yulang -- --std-root lib contract --contract
    file-resource tests/yulang/cases.toml` (`57 passed`)
- WP2 Stage 1 is complete. WP3 remains blocked on ABI Stage α, as required by
  the handoff.

## Host manifest prefix-route arena closeout

- Closed the source-unit prefix-route blocker that appeared when generating a
  host manifest from a prefix+suffix compiled unit instead of reusing the prefix
  manifest.
- Root cause: `CompiledTypedSurface::from_lowering` stored value schemes from
  `poly.defs` but serialized the type arena from `infer.constraints()`. Normal
  finalized schemes live in `poly.typ`; prefix+suffix lowering makes this
  visible because cached prefix schemes can reference type IDs outside the
  suffix infer arena.
- Fix: serialize compiled typed surfaces from the final `poly.typ` arena, and
  add a regression that formats both a prefix-exported scheme and a suffix
  scheme from the same prefixed lowering.
- Restored prefix+suffix routes to carry a generated manifest from the current
  lowering/compiled unit, rather than silently falling back to the prefix
  manifest. This keeps suffix-defined host acts visible to later stages.
- Adjusted raw-compat overrides so they annotate matching operations when those
  operations exist; absence is not itself a manifest-build error. The repo std
  full-manifest equivalence test remains the drift guard for the expected
  `read_at` / `write_at` raw-compat operations.
- Validation:
  - `cargo fmt --all -- --check`
  - `timeout 240s cargo check -q -p poly -p infer -p specialize -p evidence-vm -p yulang`
  - `timeout 240s cargo test -q -p infer typed_surface -- --test-threads=1`
  - `timeout 240s cargo test -q -p infer host_acts -- --test-threads=1`
  - `timeout 240s cargo test -q -p yulang --test cli compiler_generated_host_manifest_matches_runtime_stage1_table -- --test-threads=1`
  - `timeout 240s cargo test -q -p yulang --test cli compatible_run_reuses_std_compiled_unit_prefix_for_new_entry -- --test-threads=1`
  - `timeout 240s cargo test -q -p yulang --test cli compatible_run_with_std_prefix_cache_matches_full_build_for_mock_ref_view -- --test-threads=1`
  - `timeout 240s cargo test -q -p yulang --test cli public_contract_manifest_cli_cases_hold -- --test-threads=1`
  - `timeout 240s cargo test -q -p yulang --test cli contract_command_runs_multiple_filtered_cases -- --test-threads=1`
  - `timeout 240s cargo test -q -p yulang --test cli -- --test-threads=1` (`115 passed`)
  - `timeout 360s cargo run -q -p yulang -- --std-root lib contract --contract file-resource tests/yulang/cases.toml` (`57 passed`)

## Alpha 7 release prep docs

- Added `.github/release-notes/v0.1.0-alpha.7.md` as the File / Host Resource
  Contract alpha release note.
- Updated English and Japanese README install snippets to
  `v0.1.0-alpha.7`.
- Synchronized `README.ja.md` with the current runtime story: evidence VM is
  the default `run` backend, control VM remains an explicit fallback, and
  mono runtime remains the interpreter/oracle route.
- Bumped the nested `yulang-zed` extension repo to `0.0.12`, updated its
  install guidance to alpha 7, committed it as `9b5f257`, and advanced the
  parent submodule pointer.
- Validation:
  - `cargo fmt --all -- --check`
  - `cargo check -q` in `yulang-zed`
  - `timeout 360s scripts/release-smoke.sh target/debug/yulang`

## Alpha 7 release gate cache fix

- First full `scripts/release-gate.sh` attempt stopped during `cargo test -q
  -p parser -p infer -p yulang -- --test-threads=1`.
- Fixed stale test initializers for `RuntimeEvidenceSurface` and
  `CachedPolyArtifact` after the host-manifest field additions.
- Fixed `RuntimeEvidenceSurface.host_manifest` serialization for binary cache
  envelopes. `skip_serializing_if = "Option::is_none"` is unsafe for bincode
  struct layouts because the reader still expects a field slot; `None` must be
  encoded explicitly.
- Bumped `CONTROL_CACHE_FORMAT` from 8 to 9 because runtime-evidence cache
  layout changed.
- Validation:
  - `cargo fmt --all -- --check`
  - `timeout 240s cargo test -q -p yulang cache_round_trips_binary_artifact_envelope -- --test-threads=1`
  - `timeout 240s cargo check -q -p specialize -p yulang`
  - `scripts/release-gate.sh`

## Alpha 7 Windows archive smoke fix

- First `v0.1.0-alpha.7` tag run failed on the Windows release archive smoke.
- Root cause: `file_native_protocol_typed_failures` and
  `file_native_helper_typed_failures` used write-to-directory to prove native
  typed failure propagation, but that OS operation is reported as
  `io_err::failed` on Unix/macOS and `io_err::denied` on Windows.
- Kept the contract on typed failure propagation by accepting both
  `io_err::denied` and `io_err::failed` as the same compact observation, while
  still rejecting catch-all `store-other-error` / `write-other-error`.
- Validation:
  - `cargo fmt --all -- --check`
  - `timeout 240s cargo run -q -p yulang -- --std-root lib contract --contract file-resource --case file_native_protocol_typed_failures tests/yulang/cases.toml`
  - `timeout 240s cargo run -q -p yulang -- --std-root lib contract --contract file-resource --case file_native_helper_typed_failures tests/yulang/cases.toml`
  - `timeout 360s cargo run -q -p yulang -- --std-root lib contract --contract file-resource tests/yulang/cases.toml`
  - `timeout 360s scripts/release-smoke.sh target/debug/yulang`

## Lambda my-ref binder sugar

- Implemented `\my &x -> body` as the lambda-side state-passing protocol sugar
  from `notes/design/2026-07-02-my-binder-sugar.md`.
- The parser now recognizes `my` only in lambda binder position when followed
  by a non-empty `&` sigil pattern, preserving ordinary `my` identifiers in
  pattern syntax.
- Lowering treats protocol lambda binders as synthetic init parameters and
  reuses the existing synthetic `var` act / `var_ref` / `run` machinery. For
  comma-listed binders, all init parameters are bound first and the var
  handlers are installed at the innermost body, so outer refs remain handled
  when the final body runs.
- Added parser canaries and a runtime contract case covering unary and
  comma-listed `\my &...` binders.
- Closed out the design/spec wording that still treated the lambda form as
  future work:
  - `notes/design/2026-07-02-my-binder-sugar.md` now records the λ form as
    implemented and calls out the comma-listed lowering shape.
  - `spec/2026-07-02-io-resource-api.md` now describes `\my &text ->` as the
    implemented notation for the existing `text_with` protocol, not a future
    semantic change.
- Added `file_text_with_commit_lambda_my`, proving `file::text_with` accepts
  the lambda sugar with the same public observation as the existing do-binding
  sugar case.
- Adjusted the spec example and lambda fixture to prefer the trailing lambda
  spelling `file::text_with path \my &text -> ...` for with-style resource
  callbacks.
- Validation:
  - `cargo fmt --all -- --check`
  - `cargo check -q -p parser -p infer -p yulang`
  - `cargo test -q -p parser expr_lambda_my -- --test-threads=1`
  - `timeout 180s cargo run -q -p yulang -- --std-root lib contract --case lambda_my_binder_state_protocol tests/yulang/cases.toml`
  - `timeout 180s cargo run -q -p yulang -- --std-root lib contract --case file_text_with_commit_lambda_my tests/yulang/cases.toml`
  - `cargo test -q -p yulang --test cli public_contract_manifest_cli_cases_hold -- --test-threads=1`
  - `cargo test -q -p infer protocol -- --test-threads=1`
  - `cargo test -q -p parser`
  - `cargo test -q -p infer`
  - `git diff --check`

## Host manifest runtime capability boundary

- Carried the compiler-produced host manifest from `EvidenceVmPlan` into
  `RuntimeEvidenceRunContext`, then into `RuntimeHostRegistry`.
- Updated registry resolution so a path under an act declared by the plan
  manifest becomes `UnsupportedHostCapability` when the operation is unknown
  to the current VM implementation or has no registration. Paths outside the
  manifest still remain real `EscapedEffect` / unhandled-effect cases.
- Kept builtin std operation execution on the existing static spec table for
  this slice. Full WP3 deletion of `RUNTIME_HOST_MANIFEST` /
  `RuntimeHostOperation` and construction of a path lookup table remains
  separate work.
- Added a source-unit prefix regression proving a cached prefix that declares a
  user-defined `pub host act` reaches Evidence VM runtime as a capability
  boundary, not as an unhandled effect.
- Validation:
  - `cargo fmt --all -- --check`
  - `cargo check -q -p evidence-vm -p yulang`
  - `cargo test -q -p evidence-vm runtime_host_registry -- --test-threads=1`
  - `cargo test -q -p yulang source::tests::case_02::compiled_unit_prefix -- --test-threads=1`
  - `git diff --check`

## Debug host manifest compiler output

- Switched `yulang debug host-act-manifest` from the Evidence VM static runtime
  table to a compiler-produced manifest built by compiling std plus a trivial
  root program.
- The output now starts with `compiler host manifest:` and prints compiler
  signature strings plus the replay `column=` value for each operation.
- Kept the supported tier vocabulary printout from Evidence VM for this slice;
  full deletion of the static runtime operation table remains WP3 follow-up.
- Updated the CLI canary, release smoke expectation, and file-contract docs for
  generated signatures and column output.
- Validation:
  - `cargo fmt --all -- --check`
  - `timeout 180s cargo test -q -p yulang --test cli debug_host_act_manifest_prints_runtime_registry_view -- --test-threads=1`
  - `timeout 180s cargo run -q -p yulang -- --std-root lib debug host-act-manifest | sed -n '1,16p'`
  - `git diff --check`
  - `timeout 360s scripts/release-smoke.sh target/debug/yulang`

## Generated host manifest operation dispatch

- Updated `RuntimeHostRegistry` so a supplied compiler-generated host manifest
  is the dispatch source of truth, not just the capability-boundary classifier.
- A request under a generated manifest act now resolves to a registered
  `HostOpRegistration` by `(act_id, operation_id)` and executes through the
  Host ABI when the registration is present.
- Requests under declared generated acts still become
  `UnsupportedHostCapability` when native host operations are disabled or no
  registration exists; paths outside the manifest still remain ordinary
  unhandled escaped effects.
- Kept the static runtime operation table as the fallback for legacy/no-manifest
  paths and as the current built-in std registration metadata. Full WP3 static
  table deletion remains separate.
- Validation:
  - `cargo fmt --all -- --check`
  - `cargo test -q -p evidence-vm
    host_abi_registration_can_serve_custom_manifest_act -- --test-threads=1`
  - `cargo test -q -p evidence-vm runtime_host_registry -- --test-threads=1`
    (`6 passed`)
  - `cargo test -q -p yulang
    source::tests::case_02::compiled_unit_prefix_host_manifest_reaches_runtime_capability_boundary
    -- --test-threads=1`
  - `cargo check -q -p evidence-vm -p yulang`
  - `git diff --check`

## Generated host manifest registry lookup

- Changed the generated-manifest side of `RuntimeHostRegistry` from per-escape
  linear scans to a construction-time lookup:
  - operation path -> resolved generated host operation;
  - act path set -> capability-boundary prefix detection.
- Generated manifest operations bind to `HostOpRegistration` once during
  registry construction. Escape-time resolution now only looks up the request
  path and checks native-host enablement.
- Added registry canaries for registered generated operations, unknown
  operations under a generated act, and same-act stale registrations.
- Kept registrations whose act is absent from a custom manifest as inert rather
  than a debug failure. Source-unit prefix tests can produce a custom-only host
  manifest while the runtime still carries builtin std registrations.
- Validation:
  - `cargo fmt --all -- --check`
  - `cargo test -q -p evidence-vm runtime_host_registry -- --test-threads=1`
    (`9 passed`)
  - `cargo test -q -p evidence-vm
    host_abi_registration_can_serve_custom_manifest_act -- --test-threads=1`
  - `cargo test -q -p yulang
    source::tests::case_02::compiled_unit_prefix_host_manifest_reaches_runtime_capability_boundary
    -- --test-threads=1`
  - `cargo check -q -p evidence-vm -p yulang`
  - `git diff --check`

## Remove public static host operation manifest view

- Removed the public `evidence_vm::runtime_host_manifest_operations()` API and
  `RuntimeHostManifestOperation` type. The CLI manifest path already prints the
  compiler-generated manifest, so the old runtime static operation view should
  no longer be an external comparison source.
- Replaced the WP2 Stage 1 equivalence test with a generated-manifest shape
  canary. It now checks that the compiled std manifest reaches
  `RuntimeEvidenceSurface`, has the expected std console/file operation keys,
  keeps only `read_at` / `write_at` on `raw-compat`, and leaves all current
  operations on `sync`.
- Kept `runtime_host_manifest_tiers()` for the current debug CLI tier
  vocabulary output. Removing that final static tier helper belongs with the
  remaining static runtime table cleanup.
- Validation:
  - `cargo fmt --all -- --check`
  - `cargo check -q -p evidence-vm -p yulang`
  - `cargo test -q -p evidence-vm runtime::host -- --test-threads=1`
    (`17 passed`)
  - `cargo test -q -p yulang --test cli
    compiler_generated_host_manifest_reaches_runtime_evidence_surface --
    --test-threads=1`
  - `cargo test -q -p yulang --test cli
    debug_host_act_manifest_prints_runtime_registry_view -- --test-threads=1`
  - `git diff --check`

## Static route host classification uses only plan manifest

- Removed the Evidence VM runtime static host-act fallback from static route
  classification.
- `GenericFallback` now becomes `HostEscape` only when the `EvidenceVmPlan`
  carries a compiler-generated host manifest containing the operation's act.
  Plans without a manifest no longer consult the runtime static host table for
  this classification.
- This moves the plan-time host route decision onto the compiler manifest
  source of truth. The runtime static table still remains only for legacy
  no-manifest dispatch fallback and tier vocabulary output.
- Validation:
  - `cargo fmt --all -- --check`
  - `cargo check -q -p evidence-vm -p yulang`
  - `cargo test -q -p yulang
    source::tests::case_02::compiled_unit_prefix_host_manifest_reaches_runtime_capability_boundary
    -- --test-threads=1`
  - `cargo test -q -p evidence-vm runtime_host_registry -- --test-threads=1`
    (`9 passed`)
  - `cargo test -q -p yulang --test cli
    compiler_generated_host_manifest_reaches_runtime_evidence_surface --
    --test-threads=1`
  - `git diff --check`

## Remove Evidence VM static host operation table

- Removed the remaining static runtime host operation table from
  `crates/evidence-vm/src/runtime/host.rs`: `RuntimeHostAct`,
  `RuntimeHostOperation`, `RuntimeHostOperationSpec`, `RuntimeHostManifest`,
  `RUNTIME_HOST_OPERATIONS`, `RUNTIME_HOST_ACTS`, path constants, and
  handwritten signature strings.
- `RuntimeHostRegistry` now resolves only through the compiler-generated
  manifest lookup. If no manifest is present, host escape resolution returns
  `None` instead of consulting a legacy static table.
- Moved the last tier vocabulary print in `debug host-act-manifest` off
  evidence-vm's static helper and onto the shared `poly::host_manifest`
  tier enum.
- Updated runtime unit tests to pass small hand-built manifest fixtures instead
  of relying on no-manifest static fallback. Updated the static-route canary to
  pass a manifest fixture as the host-act source of truth.
- Validation:
  - `cargo fmt --all -- --check`
  - `cargo check -q -p evidence-vm -p yulang`
  - `cargo test -q -p evidence-vm -- --test-threads=1` (`104 passed`)
  - `cargo test -q -p yulang
    source::tests::case_02::compiled_unit_prefix_host_manifest_reaches_runtime_capability_boundary
    -- --test-threads=1`
  - `cargo test -q -p yulang --test cli
    debug_host_act_manifest_prints_runtime_registry_view -- --test-threads=1`
  - `cargo test -q -p evidence-vm
    host_abi_registration_can_serve_custom_manifest_act -- --test-threads=1`
  - `git diff --check`
  - `cargo build -q -p yulang` (`0m0.077s`)
  - `scripts/release-smoke.sh target/debug/yulang` (`release smoke ok`,
    `1m56.020s`)

## WP3 host manifest acceptance gate

- Ran the host ABI manifest WP3 acceptance gate after removing the Evidence VM
  static host operation table.
- Updated `notes/2026-07-03-host-abi-manifest-codex-handoff.md` to record WP3
  Stage 2 as complete and explicitly leave Stage 3 constructor type tables
  unstarted.
- Push note: `git push origin main` failed once after `2m20.207s` because the
  environment could not connect to `github.com:443`. The local branch remains
  ahead and should be retried at the next commit boundary.
- Validation:
  - `cargo fmt --all -- --check`
  - `cargo check -q -p poly -p infer -p specialize -p evidence-vm -p yulang`
    (`0m0.077s`)
  - `cargo test -q -p poly` (`48 passed`, `0m0.231s`)
  - `cargo test -q -p infer` (`514 passed`, `0m5.395s`)
  - `cargo test -q -p evidence-vm -- --test-threads=1`
    (`104 passed`, `0m0.247s`)
  - `cargo test -q -p yulang --test cli -- --test-threads=1`
    (`115 passed`, `3m0.036s`)
  - `cargo run -q -p yulang -- --std-root lib contract --contract
    file-resource tests/yulang/cases.toml` (`57 passed`, `3m24.052s`)

## Specialize2 residual tail hardening

- Release gate exposed a nondeterministic failure in
  `compatible_run_with_std_prefix_cache_matches_full_build_for_direct_ref_update`:
  local `&buffer` evidence could conflict with an empty effect row during
  specialize2 slot resolution.
- Root cause was solver-side early closing of upper-only effect slots to pure,
  combined with partial resolution collapsing unresolved union/intersection
  tails. When the same slot later appeared as an effect row residual tail, the
  local `&buffer` lower lost the open tail path and met a pure upper.
- Kept the normal `solve_slots` defaulting behavior intact, but preserved
  upper-only effect variables while resolving an effect row tail. This keeps
  residual tails open only in the row-tail context where the openness is
  semantically relevant.
- Treated union / bounded intersection shapes as effect row tails when they can
  accept the missing lower item. Bounded tails such as `tail & [&buffer]` can
  accept matching local evidence; `tail & []` cannot.
- Sorted `TaskSolver::finish` expression iteration by `ExprId` so emitted
  boundary resolution no longer depends on `HashMap` iteration order.
- Validation:
  - `cargo fmt --all`
  - `cargo fmt --all -- --check`
  - `cargo check -q -p specialize -p yulang`
  - `cargo test -q -p specialize specialize2::tests:: -- --test-threads=1`
    (`31 passed`)
  - `cargo build -q -p yulang`
  - `for i in $(seq 1 20); do target/debug/yulang --std-root lib --no-cache
    run --print-roots /tmp/yulang-cli-run-std-prefix-direct-ref-update-1783049610053699432/main.yu
    ...; done` (`repeated-direct-narrow-ok:20`, `1m6.274s`)
  - `timeout 240s cargo test -q -p yulang --test cli
    compatible_run_with_std_prefix_cache_matches_full_build_for_direct_ref_update
    -- --test-threads=1` (`1 passed`)
  - `timeout 420s cargo test -q -p yulang --lib -- --test-threads=1`
    (`272 passed`, `3m29.919s`)
  - `timeout 1800s env YULANG_RELEASE_GATE_TEST_TIMEOUT=720
    scripts/release-gate.sh` (`release gate ok`, `12m14.004s`)

## Release archive manifest smoke hardening

- Tightened `scripts/release-archive-smoke.sh` so the archive manifest check
  validates required value contracts, not only key presence.
- The smoke now requires `name=yulang` and `contract_runner=1`, checks
  non-empty version / target / stdlib fields, verifies the stdlib source hash
  shape, and ensures cache format fields are numeric.
- Validation:
  - `bash -n scripts/release-archive-smoke.sh scripts/package-release.sh
    scripts/release-smoke.sh`
  - `scripts/package-release.sh --version release-manifest-strict-smoke
    --target x86_64-unknown-linux-gnu --binary target/release/yulang --out
    target/release-manifest-strict-smoke`
  - `timeout 600s scripts/release-archive-smoke.sh
    target/release-manifest-strict-smoke/yulang-x86_64-unknown-linux-gnu.tar.gz`

## Release package std hash guard

- Refactored `scripts/package-release.sh` so the std source hash calculation is
  shared between the repository `lib/` tree and the staged bundle.
- After `install std` expands the release binary's embedded std into the
  archive root, packaging now hashes the staged `lib/` tree and rejects a
  mismatch with a rebuild hint. This prevents stale release binaries from
  producing archives whose manifest describes the current source std while the
  bundle contains older embedded std content.
- Validation:
  - `bash -n scripts/package-release.sh scripts/release-archive-smoke.sh`
  - `scripts/package-release.sh --version release-stdlib-hash-smoke --target
    x86_64-unknown-linux-gnu --binary target/release/yulang --out
    target/release-stdlib-hash-smoke`
  - `timeout 600s scripts/release-archive-smoke.sh
    target/release-stdlib-hash-smoke/yulang-x86_64-unknown-linux-gnu.tar.gz`

## Release archive bundled std hash check

- Added the same deterministic std source hashing to
  `scripts/release-archive-smoke.sh`.
- Archive smoke now compares `release-manifest.txt`'s `stdlib_source_hash`
  against the unpacked bundle under `lib/`, so a manually assembled or
  corrupted archive cannot claim one std snapshot while shipping another.
- Validation:
  - `bash -n scripts/release-archive-smoke.sh`
  - `timeout 600s scripts/release-archive-smoke.sh
    target/release-stdlib-hash-smoke/yulang-x86_64-unknown-linux-gnu.tar.gz`

## Static route M0 surface conduit

- Added `RuntimeEvidenceSurface.static_routes` and a specialize-side static
  route classifier for the M0 conduit.
- The classifier currently emits conservative L1 results: operation calls
  directly inside a catch body can resolve to `StaticHandler`, and operations
  outside such a lexical proof remain dynamic. Host-manifest operations are
  emitted as `HostEscape`; other non-L1 cases remain `Unclassified`.
- Moved static route dynamic reason ownership out of Evidence VM. Evidence VM
  now consumes the compiler surface and only joins `StaticHandler` routes to
  mono handler objects to recover the handler arm class.
- The mono join handles the current poly/control expr-id split by looking up
  call routes through the apply expr, then joining to the operation's mono
  candidate handler. A missing or mismatched join increments
  `static_route_mono_join_failures` and falls back to `dynamic-unclassified`.
- Left L2 propagation, `OpenRow`, `MultipleCandidates`, `HygieneBarrier`,
  `ProviderEnvDependent`, and `DelayedBoundary` production for the next static
  route slice.
- Validation:
  - `cargo fmt --all -- --check`
  - `cargo check -q -p specialize -p evidence-vm -p yulang`
  - `cargo test -q -p evidence-vm static_route -- --test-threads=1`
    (`3 passed`)
  - `timeout 180s cargo test -q -p yulang --test cli
    debug_runtime_evidence_run_classifies_unused_continuation_as_direct_abortive
    -- --test-threads=1` (`1 passed`)
  - `timeout 180s cargo test -q -p yulang --test cli runtime_evidence --
    --test-threads=1` (`31 passed`)

## Static route M0 L2 propagation

- Extended the specialize-side static route classifier with L2 context
  propagation through mono instances.
- Reconstructed App-to-instance edges from the callee def and runtime-closed
  signature, using the mono `Program.instances` table as the instance key
  source.
- Propagated handler context over the SCC condensation graph once. The context
  tracks the maximum applied lambda depth per handler, so `make()` returning a
  lambda does not promote operations inside that returned lambda body unless the
  caller actually applies that lambda depth.
- Added specialize regressions for:
  - helper called from a single catch context -> `StaticHandler`
  - helper called from two catch contexts -> `Dynamic(MultipleCandidates)`
  - catch returning a lambda with an operation body -> `Dynamic(Unclassified)`
- Validation:
  - `cargo fmt --all -- --check`
  - `cargo check -q -p specialize -p evidence-vm -p yulang`
  - `cargo test -q -p specialize runtime_evidence_static_routes --
    --test-threads=1` (`3 passed`)
  - `cargo test -q -p specialize runtime_evidence -- --test-threads=1`
    (`7 passed`)
  - `cargo test -q -p evidence-vm static_route -- --test-threads=1`
    (`3 passed`)
  - `timeout 180s cargo test -q -p yulang --test cli runtime_evidence --
    --test-threads=1` (`31 passed`)
  - `cargo test -q -p evidence-vm -- --test-threads=1` (`105 passed`)
  - `timeout 300s cargo test -q -p yulang --test cli -- --test-threads=1`
    (`115 passed`)
  - `git diff --check`

## Static route M1 remeasure

- Rebuilt `target/release/yulang` after the M0 L2 classifier change.
- Ran the Stage M1 representative profile over:
  - `bench/nondet_20_discard.yu`
  - `examples/showcase.yu`
  - `examples/03_for_last.yu`
  - `examples/02_refs.yu`
- Also reran the same four commands with `--no-cache` to avoid stale cache
  decode warnings in the recorded numbers.
- Old 2026-07-02 Stage 0 numbers remain an invalid comparison baseline per the
  2026-07-03 mono-resolution plan; the table below is the new classifier's
  Stage M1 input.

| workload | sites | static_handler | dynamic_provider_env | dynamic_unclassified | static_tail_hits | static_other_hits | provider_env_hits | unclassified_hits | compare-control |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | --- |
| `bench/nondet_20_discard.yu` | 3 | 0 | 3 | 0 | 0 | 0 | 861 | 0 | match |
| `examples/showcase.yu` | 13 | 0 | 11 | 2 | 0 | 0 | 2457 | 7 | match |
| `examples/03_for_last.yu` | 7 | 0 | 7 | 0 | 0 | 0 | 2 | 0 | match |
| `examples/02_refs.yu` | 8 | 0 | 8 | 0 | 0 | 0 | 10 | 0 | match |

- `static_route_mono_join_failures` was 0 for all four workloads.
- Stage 1 is not enabled by these M1 numbers: the representative workloads
  still have 0 static handler sites and 0 static runtime hits. The M0 L2
  synthetic fixtures prove the conduit and helper propagation, and the
  provider-env reason pass now explains most former unclassified routes, but
  the representative programs still need more route precision before shadow or
  execution promotion is justified.
- Validation:
  - `cargo build -q --release -p yulang`
  - `timeout 1200s scripts/static-route-stage0-profile.sh`
  - no-cache rerun of the same four `debug evidence-vm-run --compare-control
    --runtime-evidence-profile-deep` commands

## Static route profile script cleanup

- Updated `scripts/static-route-stage0-profile.sh` to run with `--no-cache` by
  default. `YULANG_STATIC_ROUTE_STAGE0_USE_CACHE=1` keeps the previous cached
  mode available.
- Fixed the grep pattern so `  compare.control: match` is printed in the
  profile output. The script's previous pattern missed the leading spaces.
- Validation:
  - `bash -n scripts/static-route-stage0-profile.sh`
  - `timeout 1200s scripts/static-route-stage0-profile.sh`

## Static route provider-env reason classification

- Added `operation_def` to `RuntimeEvidenceStaticRoute` so Evidence VM can join
  dynamic route reasons across poly/control expr-id shifts by operation
  definition and family.
- The def/family fallback is restricted to Dynamic routes whose reason agrees
  across matching compiler routes; it is not used to synthesize
  `StaticHandler`.
- Classified unresolved operation calls with slot-backed runtime evidence as
  `Dynamic(ProviderEnvDependent)` after host escape is ruled out.
- Validation:
  - `cargo fmt --all -- --check`
  - `cargo check -q -p specialize -p evidence-vm -p yulang`
  - `cargo test -q -p specialize runtime_evidence_static_routes --
    --test-threads=1` (`4 passed`)
  - `cargo test -q -p specialize runtime_evidence -- --test-threads=1`
    (`8 passed`)
  - `cargo test -q -p evidence-vm static_route -- --test-threads=1`
    (`4 passed`)
  - `cargo test -q -p evidence-vm -- --test-threads=1` (`106 passed`)
  - `timeout 180s cargo test -q -p yulang --test cli runtime_evidence --
    --test-threads=1` (`31 passed`)
  - `timeout 300s cargo test -q -p yulang --test cli -- --test-threads=1`
    (`115 passed`)
  - `cargo build -q --release -p yulang`
  - `timeout 1200s scripts/static-route-stage0-profile.sh`

## Static route callback hygiene reason classification

- Classified unresolved operation calls under lambdas passed through application
  arguments as `Dynamic(HygieneBarrier)`. This keeps direct L1/L2 route
  promotion conservative: callback invocation sites are not represented by the
  direct specialized call graph edges used by this stage.
- Left returned-lambda bodies as `Dynamic(Unclassified)` unless they also flow
  through an application argument; source arity is not recorded in `poly::Def`,
  so the classifier does not guess from the curried mono type shape.
- Added a focused callback-lambda regression alongside the existing
  returned-lambda non-promotion case.
- Re-profiled Stage M1:
  - `bench/nondet_20_discard.yu`: 3 sites, 3 provider-env, 0 hygiene-barrier,
    0 unclassified; runtime hits provider-env 861; compare-control matched.
  - `examples/showcase.yu`: 13 sites, 11 provider-env, 2 hygiene-barrier,
    0 unclassified; runtime hits provider-env 2457, hygiene-barrier 7;
    compare-control matched.
  - `examples/03_for_last.yu`: 7 sites, 7 provider-env, 0 hygiene-barrier,
    0 unclassified; runtime hits provider-env 2; compare-control matched.
  - `examples/02_refs.yu`: 8 sites, 8 provider-env, 0 hygiene-barrier,
    0 unclassified; runtime hits provider-env 10; compare-control matched.
- Validation:
  - `cargo fmt --all -- --check`
  - `cargo test -q -p specialize runtime_evidence_static_routes --
    --test-threads=1` (`5 passed`)
  - `cargo check -q -p specialize -p evidence-vm -p yulang`
  - `cargo test -q -p evidence-vm static_route -- --test-threads=1`
    (`4 passed`)
  - `cargo test -q -p evidence-vm -- --test-threads=1` (`106 passed`)
  - `timeout 180s cargo test -q -p yulang --test cli runtime_evidence --
    --test-threads=1` (`31 passed`)
  - `cargo build -q --release -p yulang`
  - `timeout 1200s scripts/static-route-stage0-profile.sh`

## Static route delayed-boundary reason classification

- Investigated `OpenRow` next, but the concrete operation-call sites checked
  so far still expose closed operation thunk rows in runtime evidence. The
  visible open tails are residual/subtraction evidence around handler
  boundaries, not a direct operation-site row tail that the classifier can
  label without guessing.
- Classified direct call edges reached through an application-argument context
  as delayed boundaries for L2 propagation. If such an edge reaches an SCC,
  unresolved non-L1 operation routes in that SCC stay Dynamic; host and
  slot-backed provider-env certificates still take precedence so def/family
  fallback remains stable.
- Added a focused effect-thunk argument regression:
  `run action()` where `run(x: [_] int)` catches the thunk and `action()`
  performs the operation. The route is now `Dynamic(DelayedBoundary)`.
- Confirmed `examples/10_effect_handler.yu` now reports 2 delayed-boundary
  static route sites and 0 unclassified static route sites.
- Re-profiled Stage M1 representative workloads; counts are unchanged from
  the callback-hygiene pass and all compare-control checks matched:
  - `bench/nondet_20_discard.yu`: 3 provider-env, 0 unclassified; provider-env
    runtime hits 861.
  - `examples/showcase.yu`: 11 provider-env, 2 hygiene-barrier,
    0 unclassified; provider-env runtime hits 2457, hygiene-barrier hits 7.
  - `examples/03_for_last.yu`: 7 provider-env, 0 unclassified; provider-env
    runtime hits 2.
  - `examples/02_refs.yu`: 8 provider-env, 0 unclassified; provider-env
    runtime hits 10.
- Validation:
  - `cargo fmt --all -- --check`
  - `cargo test -q -p specialize runtime_evidence_static_routes --
    --test-threads=1` (`6 passed`)
  - `cargo check -q -p specialize -p evidence-vm -p yulang`
  - `cargo test -q -p evidence-vm -- --test-threads=1` (`106 passed`)
  - `timeout 180s cargo test -q -p yulang --test cli runtime_evidence --
    --test-threads=1` (`31 passed`)
  - `cargo build -q --release -p yulang`
  - `timeout 1200s scripts/static-route-stage0-profile.sh`

## Static route delayed-boundary CLI regression

- Added a `runtime-evidence-run --compare-control` CLI regression for an effect
  thunk passed into a `[_]` handler argument. The test checks the surfaced
  delayed-boundary count, delayed-boundary runtime hit, zero unclassified hits,
  and control/runtime output equality.
- Validation:
  - `cargo fmt --all -- --check`
  - `timeout 120s cargo test -q -p yulang --test cli
    debug_runtime_evidence_run_classifies_thunk_argument_as_delayed_boundary --
    --test-threads=1` (`1 passed`)
  - `cargo test -q -p specialize runtime_evidence_static_routes --
    --test-threads=1` (`6 passed`)
  - `timeout 180s cargo test -q -p yulang --test cli runtime_evidence --
    --test-threads=1` (`32 passed`)
  - `timeout 300s cargo test -q -p yulang --test cli -- --test-threads=1`
    (`115 passed`, before adding this focused CLI regression)

## Instant and file metadata modified time

- Added `std::time` with public `instant { epoch_nanos }`,
  `duration { nanos }`, named instant/duration arithmetic helpers,
  `Eq` / `Ord` impls, and the `pub host act clock.now`.
- Kept heterogeneous operator spellings (`instant - instant = duration`,
  `instant + duration = instant`) out of this slice because the current
  `Add` / `Sub` roles only express same-type operands and outputs. The named
  helpers exercise the spec data model without broad role redesign.
- Extended `std::io::file::file_meta` to
  `{ kind, size, readonly, modified: opt instant }`.
- Updated the native file metadata Host ABI bridge so successful filesystem
  metadata returns `modified = just instant(...)` when the host exposes a
  modification time, while missing / denied / other metadata keeps
  `modified = nil`.
- Added native `std.time.clock.now` registration through the generated host
  manifest / Host ABI path, and added `std/time.yu` to the embedded std bundle
  used by `install std`.
- Updated the generated host manifest canaries and release smoke so
  `std.time.clock.now` appears as sync contract `column=9`.
- Added contract cases:
  - `instant_duration_core`
  - `clock_now_native`
  - `file_native_meta_modified`
  - `std_time_clock_now_public_signature`
  - `std_time_instant_since_public_signature`
- Validation:
  - `cargo fmt --all -- --check`
  - `cargo check -q -p evidence-vm -p yulang`
  - `cargo build -q -p yulang`
  - `timeout 180s cargo run -q -p yulang -- --std-root lib contract --case instant_duration_core tests/yulang/cases.toml`
  - `timeout 180s cargo run -q -p yulang -- --std-root lib contract --case file_meta_kind tests/yulang/cases.toml`
  - `timeout 240s cargo run -q -p yulang -- --std-root lib contract --case instant_duration_core --case clock_now_native --case file_native_meta_modified --case std_time_clock_now_public_signature --case std_time_instant_since_public_signature tests/yulang/cases.toml`
  - `timeout 240s cargo run -q -p yulang -- --std-root lib contract --case std_file_meta_public_signature --case std_file_file_meta_public_signature --case file_native_meta_file --case file_native_meta_missing --case file_native_meta_directory tests/yulang/cases.toml`
  - `timeout 300s cargo run -q -p yulang -- --std-root lib contract --contract time tests/yulang/cases.toml` (`5 passed`)
  - `timeout 360s cargo run -q -p yulang -- --std-root lib contract --contract file-resource tests/yulang/cases.toml` (`58 passed`)
  - `cargo test -q -p evidence-vm -- --test-threads=1` (`106 passed`)
  - `timeout 180s cargo test -q -p yulang --test cli debug_host_act_manifest_prints_runtime_registry_view -- --test-threads=1`
  - `timeout 180s cargo test -q -p yulang --test cli compiler_generated_host_manifest_reaches_runtime_evidence_surface -- --test-threads=1`
  - `timeout 180s cargo test -q -p yulang --test cli install_std_writes_explicit_std_root -- --test-threads=1`
  - `timeout 300s cargo test -q -p yulang --test cli public_contract_manifest_cli_cases_hold -- --test-threads=1`
  - `timeout 180s cargo test -q -p yulang --test cli public_contract_manifest_covers_status_spine_claims -- --test-threads=1`
  - `timeout 360s scripts/release-smoke.sh target/debug/yulang`

## Embedded std time bundle follow-up

- `scripts/release-smoke.sh target/debug/yulang` caught that `install std`
  wrote the updated `std.yu` / `prelude.yu` but did not yet bundle the new
  `std/time.yu` file.
- Added `std/time.yu` to `EMBEDDED_STD_FILES` and extended
  `install_std_writes_explicit_std_root` to assert the installed time module.
- Validation:
  - `cargo fmt --all -- --check`
  - `cargo build -q -p yulang`
  - `timeout 180s cargo test -q -p yulang --test cli install_std_writes_explicit_std_root -- --test-threads=1`
  - `timeout 360s scripts/release-smoke.sh target/debug/yulang`

## Clock host boundary cases

- Added focused runtime contract cases for `std::time::clock.now` under an
  unsupported host runtime:
  - `clock_now_unsupported_host` pins the native host-missing diagnostic for
    `std::time::clock`.
  - `clock_mock_now_handler` proves a source handler can intercept the host
    act and resume with a synthetic `instant`.
- Validation:
  - `cargo fmt --all -- --check`
  - `timeout 180s cargo run -q -p yulang -- --std-root lib contract --case clock_now_unsupported_host --case clock_mock_now_handler tests/yulang/cases.toml`
    (`2 passed`)
  - `timeout 300s cargo run -q -p yulang -- --std-root lib contract --contract time tests/yulang/cases.toml`
    (`7 passed`)
  - `timeout 300s cargo test -q -p yulang --test cli public_contract_manifest_cli_cases_hold -- --test-threads=1`
    (`1 passed`)

<!-- daily-append-anchor: 2026-07-03 -->
