# 2026-06-06

## CompactBounds enum migration

- `notes/refactors/2026-06-06-compactbounds-enum-migration.md` に従い、`CompactBounds`
  の struct -> enum 移行を実施する。
- 既存の compact/cooccur/scc 差分は前提として扱い、戻さずに作業する。
- 完了。`CompactBounds` は `Interval` だけの enum になり、既存アクセスは accessor /
  mut accessor / `CompactBounds::Interval` へ移行済み。
- 確認:
  - `env RUSTC_WRAPPER= cargo build -p yulang-infer` 成功。
  - `env RUSTC_WRAPPER= cargo test -p yulang-infer --lib --no-fail-fast -- --skip compiled`
    は `481 passed; 16 failed`。失敗名は refactor note の指定リストと一致。
