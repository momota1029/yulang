# 2026-09-25

## F5c flat indexed stack-independent design direction

The user chose to explore deep F5c support with explicit work stacks instead
of a fixed structural-depth limit. A Sol architect review found that true
stack independence must cover ownership and destruction as well as visitation:
F5c drafts need to remain flat from production through normalization,
finalization input, and checked-error cleanup. Pair the solver-owned flat
draft with a `yu-types`-owned indexed finalization transaction; an iterative
callback over the existing recursive boxes is insufficient.

Prepared the design-only M3 draft in
`notes/design/2026-09-25-f5c-flat-indexed-stack-independent-draft.md`. It
preserves practical-input semantics, Q/R and normalized order, §44's
first-member representative, and atomic route/publication behavior. It does
not yet settle a deterministic charged-work limit, and it does not weaken the
existing F5 §26/§34 resource contracts. The M3 review and two focused delta
rounds are complete with no remaining blocking/major finding in assigned
scope. The proposal is Reviewed, not implementation-authoritative. The user
approved the first-stage practical-source, charge-site, and scale/resource
investigation only; numeric limits and separate approval remain prerequisites
to production code. No production-path prototype, API, or semantic change is
authorized.

## F5c first-stage source/scale investigation

The read-only source audit found 73 stable-core cases (16 public-signature
fixtures); their source/expected-signature surface is small, but there is no
compiler CLI/corpus harness connecting it to expanded F5c graph size or work.
Repository evidence supports only a repository-bounded envelope, not the
distribution of external Oracle inputs. Existing stress tests cover selected
deep traversals and capacity ledgers but not exact independent counts for
shared-DAG path amplification, deep candidate deduplication, R fixed-point
replay, or a proposed indexed finalizer.

The source/charge-site and indexed-validation findings are recorded in §13 of
the design draft. They require one checked solver work meter charging candidate
pairs and structural comparison visits, actual summary-edge expansions,
root-local analysis/replay/substitution/R visits, and copied replay elements.
The proposed indexed validation `O(V+E+B+Q)` bound is conditional on direct
dense Q/R indexing and iterative three-color DFS; the current callback's nested
linear searches do not establish it. Existing F5a callback acceptance of
ordered R-bound subsets remains unchanged.

Verification: `cargo test -p yu-solver --lib f5c_ -- --test-threads=1` passed
162 tests (0.64 s); `cargo test -p yu-solver --lib f5c_deep --
--test-threads=1` passed 2 tests. These are correctness checks, not benchmark
samples. No process-memory or timing experiment was run; no code/API, numeric
boundary, or F5 clause changed in that audit slice. The follow-up below adds
one test-only normalizer probe; shared-expansion and R/replay scale evidence
remain open. F5c/F5e closure remains open.

## F5c normalizer-only scale probe

Added an ignored test-only probe in
`crates/yu-solver/src/tests/f5c_resource_probe.rs`; the only `lib.rs` change is
its test-module declaration. It varies Function-chain depth, unique and
duplicate-heavy Union width, and independent-root count, and prints existing
normalizer counters plus its tracked lane capacities. It changes no production
behavior and is excluded from ordinary test runs.

Largest observed cases: depth-4,096 Function chain -> 8,193 raw normalizer
nodes, 8,192 child slots, 28,673 word comparisons, 1,972,544 bytes of tracked
normalizer-lane peak; width-1,024 duplicate-heavy Union -> 3,073 raw nodes,
3,072 child slots, 1,023 removed duplicates, 56,312 word comparisons, and
547,176 tracked bytes. These byte totals omit recursive boxed input/output
payloads and are not process memory or a proposed flat-path estimate.

The manual probe was executed three times while removing one test-harness
warning; all passed. This used 3 of the 8 diagnostic process invocations and
collected no timing samples. The regular F5c filter passes 162 tests with the
manual probe ignored. No numeric limit follows from these measurements. Next,
inspect whether the existing test-visible counters can support probes for
shared-summary expansion and R/replay work without production instrumentation.

## F5c shared-summary and replay probe follow-up

Extended the same ignored test-only probe to cover shared-summary
materialization and replay. A synthetic binary summary DAG at depth 12 stores
13 unique nodes and 24 child edges, then materializes to 8,191 nodes and 8,190
edges. Its task lane records 12,286 slot requests and a 512-byte tracked peak.
This is strong path-amplification evidence for the current materializer, but
it is synthetic and excludes the output tree from that lane's bytes.

A depth-4,096 Function replay records 12,289 task-slot requests and 8,193
value-slot requests; the respective lane peaks are 131,072 and 262,144 bytes.
Static inspection of the R fixed-point owner loop shows the same lanes can
aggregate replay task/value scheduling, but no current counter records round
count, candidate clone/retain work, owner and trace checks, or reachability
frontier visits. Those remain unmeasured, along with substitution and indexed
finalizer work.

The R candidate set in `F5cGeneralizer::build_inner` only shrinks. Therefore
every non-final round removes at least one owner and there are at most C+1
rounds for C initial candidates. This is a source-level bound, not a measured
round count or per-round cost; each round can still replay and inspect large
bounds/traces. The future work meter should charge the round, owner and trace
records, trace hops, copied candidate entries, and underlying replay/analysis
visits.

The combined manual probe was invoked seven times: five completed captures
and two compile attempts fixed during probe development. One initial warning
was removed. This consumes 7 of the 8 diagnostic process invocations; zero
timing or process-memory samples were collected. The exact renamed probe
command passes. `cargo test -p yu-solver --lib f5c_ -- --test-threads=1`
finished with 162 passed and 1 ignored. No production behavior or API changed;
no numeric limit follows from these synthetic lane counts. I did not add an
R-loop probe: spending the remaining single invocation on a new observer would
not establish an external practical-input envelope. Carry the missing R-loop
measurements as an explicit review item instead.

## F5c full-slice review blocker (2026-09-25)

The full M3 review of the flat/indexed stack-independent F5c proposal found one
major sequencing contradiction: the draft requires a numeric supported-work
boundary before implementation, while F5 §§26/34 require physical capacity and
co-resident peak evidence from the implemented solver/finalizer lanes. Existing
surface-corpus and synthetic probe data do not establish a practical numeric
margin. Compiler and performance reviewers found no issue in their scopes; the
specification reviewer also found stale status wording, corrected by retaining
Draft status. Read-only architect/specification/performance consultation did
not identify evidence for a numeric cap. No prototype or additional probe ran;
the prior diagnostic campaign used seven of eight process invocations, leaving
one invocation that is insufficient for prototype work and practical-margin
evidence.

Record-only synchronization updated the design draft, index, task navigation,
and handoff. The solve-wide meter-lifetime choice remains approved. No new
design order, numeric boundary, API, or production implementation is approved.
Next: ask whether to authorize a bounded non-shipping test-only measurement
prototype with a fresh budget, or defer while another evidence path is
developed. No tests or probes were rerun for this documentation-only update.

<!-- daily-append-anchor: 2026-09-25 -->

## F5c charge-site map closure (primary audit)

Completed a source-level map from current F5c owners to proposed logical work
charges and separate storage admissions. It now explicitly covers summary
memo construction/row invalidation/active propagation, root-local walk and
trace operations, candidate-pair plus structural dedup visits, tree analysis
and non-generic closure, every R fixed-point stage and post-convergence pass,
replay/substitution/materialization output, normalization/compaction, and the
proposed indexed finalizer's bounded validation/planning/commit passes.

Corrected the scope boundary: the Cartesian Function product in
`InferenceSession::closed_parts` runs after closed-scheme finalization, so it is
not work in the F5c scheme-draft meter. Its checked multiplication still does
not bound pair generation; keep it in the separate F5e closed-DAG
instantiation/resource gate. §44's per-use transaction rollback remains a
separate gate too.

The meter lifetime is still unresolved: solve-wide accumulation enforces a
ceiling on charged F5c draft work across the solve but can reject many individually small components;
per-component reset admits those but leaves aggregate work proportional to
component count. No numeric threshold or implementation authority was chosen.
This is a primary-authored source audit, not a fresh independent review. No
probe was rerun and no production code changed. Next: focused independent review
of the charge units, followed by a user decision on meter lifetime and the
numeric support envelope.

## F5c source-map documentation repair

Repaired draft §§5/13 after focused review found one blocking and multiple
major findings. The text now uses authoritative §36 `O(N+W+C)` and the
counter-invariance addendum's `O(N+W)` preordering, separates private work
charges from exact public counters, specifies missing scheduling/copy/scan
charges, and states the proposed exhaustion/rollback proof obligation.
Expanded the minimum co-resident lane inventory and its future §26 snapshot
ledger requirements. Current probes measure neither that peak nor R-loop cost.
The earlier M3 architecture review does not certify this extension; a fresh
focused M3 delta review is next. Meter lifetime, numeric limit, API,
implementation authority, F5c/F5e closure, and §44 per-use rollback remain
open. Documentation only; no probes or cargo checks rerun.
Scheduled-versus-popped early mismatch, epoch-wrap, exhaustion restoration,
co-resident lane ledger, and §36 counter-oracle parity witnesses are future
implementation/review requirements; none was run or measured in this repair.

The second focused M3 source-map delta review accepted the corrected stale §34
claim and §36 `O(N+W+C)` boundary but found new BLOCKING/major omissions.
Primary documentation repair now specifies both epoch-wrap fills, reentry's
three distinct short-circuit counts, individual normalization operations,
separate journal admissions, and future allocation-free rollback proof.
Restoration allocation and simultaneous peak are unverified. Future witnesses
must cover both wraps, reentry, normalization work/public-counter parity,
exhaustion restoration, and the simultaneous lane ledger; none ran here.
Another fresh focused M3 delta review is next. No gate closed; meter lifetime,
numeric caps, API, implementation, F5e `closed_parts`, and §44 per-use remain open.

Third focused M3 source-map delta round found no new charge-map omission and
accepted the mutation-to-journal visibility gap as an open implementation
requirement. The draft now requires a focused failure/exhaustion witness at
`memo.admit` → `observe_walker()?` → `admitted_keys`, prior-state journaling
for partial admission failure, separate undo-journal admission, and recovery
without fallible allocation. Invalidation/reinsertion capacity proof and the
co-resident peak remain outstanding; the peak was not measured. Fresh focused
At that point, review of this documentation repair was next. Only source-map
wording received these rounds; full design, meter lifetime, numeric cap, API,
rollback, F5e, and §44 remain open. No implementation authority or gate closure.

The follow-up documentation repair passed a fresh focused M3 delta review by
specification, compiler, and performance roles with no blocking, major, or
minor finding. This did not certify the full design. A read-only architect
consultation recommends a solve-wide work meter: it bounds aggregate repeated
F5c draft work across components, while a per-component reset can allow such work
to scale with component count. The solve-wide tradeoff is possible rejection
of a large ordinary solve made of individually small components. This remains
a recommendation for user approval, not an approved decision; available
evidence selects no numeric cap or supported-input boundary. Next: focused
independent review in the full design context, followed by presenting the
tradeoff and remaining boundary to the user. Rollback capacity and peak-memory
proof remain open. Documentation only; no tests or probes were run.

The latest focused M3 review addressed the separate meter-lifetime
recommendation and found major scope and approval-sequencing issues, plus a
minor wording issue. The source-map repair's fourth focused review remains clean. The
provisional solve-wide meter would cap charged F5c draft work across components
and prevent component-count bypass, with possible exhaustion for many small
components; per-component reset admits those but leaves aggregate charged F5c
draft work proportional to component count. Indexed-finalizer-local work, F5e
Function products, §44 per-use routing, and physical peak/storage remain
separately scoped and accounted; total invocation work and peak memory are not
capped by this meter. After review, the user can decide meter lifetime without
a numeric cap. A later measured numeric supported boundary needs scale and
practical-input evidence, focused review, and separate user approval before
implementation. Rollback/invalidation capacity and co-resident peak remain
open; the full design and implementation are unapproved.

After the batched repair, a fresh focused M3 delta review found no remaining
blocking, major, or minor issue; the minor historical wording was corrected.
This closes only the meter-lifetime documentation delta. The user approved
solve-wide accumulation for charged F5c draft work across components on
2026-09-25. This does not choose a numeric cap or supported-input boundary.
Next: continue the resource subgate with scale evidence and practical-input
margin; focused independent review and separate user approval remain required
before implementation. Full design approval, rollback proof, and co-resident
peak evidence remain open.
