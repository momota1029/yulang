# Daily progress — 2026-09-12

## Shared initial Pattern direct CST schema

- Added direct Rowan-only evidence for the finite initial Pattern role table:
  Primary, AlternationRhs, five delimited element/spread/nested slots, Binding
  Target, For Pattern, Case/Catch first Pattern, and Catch handler Pattern
  after its native comma.
- The proof covers Missing, terminal Error and Error-to-admitted-primary retry.
  It fixes native retry leading outside Error, UTF-8/CRLF extents, delimiter
  closes outside Error, the protected For `in` handoff, Catch's second-Pattern
  ordinal, and the outer TypeAnnotation after an AlternationRhs Error.
- The initial direct proof was independently found incomplete; its one M1
  repair bundle added the missing alternatives, then a delta audit closed
  cleanly. No parser behavior, CST topology, public API, recovery ledger or
  benchmark process changed.
- The catalog and coverage manifest now mark only this bounded initial-slot
  slice as an evidence-complete Draft. Delimiter sequence/close rows, Record
  default Expression, other Pattern rows, the global collector and ledger
  retirement remain open.

## Cast PatternIntroducer direct CST schema

- Added direct Rowan proof for the required opener after CastKw. The row has
  two structural forms: outer CastDeclaration Missing/Error before CastPattern,
  and the initial Missing inside an implicit bare-pattern CastPattern.
- The proof separates that inner opener Missing from the later value Missing
  after native LParen, verifies Error-to-bare retry has no duplicate opener
  Missing, and retains direct Error/trivia/EOF/CRLF plus form/close handoff.
- A post-write audit found the original outer-only selector incomplete. Its one
  test-only repair added the initial CastPattern child table; delta review then
  closed cleanly. No parser behavior, CST topology, records, API or benchmark
  process changed.
- This promotes only the bounded PatternIntroducer row to a catalog-audited
  evidence-complete Draft. All other Cast rows, global collection and ledger
  retirement remain separate.

## Cast TargetIntroducer direct CST schema

- Added direct Rowan evidence for the required colon after completed
  CastPattern. Direct declaration Missing/Error covers terminal/form/boundary
  routes, while `CastTarget(Missing TypeExpression)` identifies reusable-Type
  retry without a colon.
- The child-order proof distinguishes that inner Missing from Type-owned nested
  recovery, fixes multi-leaf Error grouping and same-line EOF versus CRLF
  behavior, and keeps colon/Type retry plus Semicolon/Equals handoff outside
  Error.
- Independent M1 audit was clean. No parser behavior, CST topology, record,
  API or benchmark process changed. The bounded row excludes explicit
  active-RParen and all remaining Cast phases.

## Cast BodyIntroducer direct CST schema

- Added direct Rowan proof for the required semicolon after completed Pattern
  and Target. Native Semicolon is bodyless, while native Equals selects later
  CastBody; neither becomes Error content.
- The tests fix terminal/multi-leaf Error grouping, same-line EOF versus CRLF,
  form retry and protected punctuation. A Missing beneath CastBody after Equals
  is directly shown as later Body-owned recovery.
- Independent M1 audit was clean. No parser behavior, CST topology, record,
  API or benchmark process changed. Active-close/fence/UTF-8 extensions and
  later Cast rows remain open.

## Cast inline Body direct CST schema

- Added Rowan-only evidence for direct CastBody Missing/Error and retry to
  OperatorChain after actual Equals, including UTF-8 fragments, EOF/CRLF split
  and protected handoff without duplicate Missing.
- Strictly-deeper indentation remains a separate Statement owner. M1 audit was
  clean; no parser/API/ledger change or benchmark process occurred.

## Cast Pattern value direct CST schema

- Added direct Rowan proof for initial Pattern absence after CastPattern LParen,
  preserving local close and phase punctuation. Nonempty malformed input stays
  nested Pattern-owned. M1 audit was clean; no parser/API/ledger change.

## Cast Pattern local-close CST schema repair

- Fixed close Error acquisition so exact `=` remains a later form starter.
  `==`, `=>` and `=>>` remain whole non-form Error input, preventing false
  suffix retries. Direct CST and frozen-record controls passed referee review.

## ModDeclaration direct CST schema

- Added Rowan-only evidence for Name, TestName, BodyIntroducer and actual-Colon
  inline Body. The first `test` is structurally a marker, whereas a second
  `test` is an ordinary Identifier; anonymous marker bodies cover semicolon,
  braced and inline-colon entry without a fabricated second name.
- The proof fixes direct Missing/Error/retry grouping, UTF-8 and CRLF/EOF
  ownership, body-starter handoff and Statement-owned retry leading. Nested
  `BindingBody(Missing)` remains beneath the child Statement rather than being
  relabelled as Mod Body.
- An M2 regression audit found the marker-specific braced/inline pair absent.
  The focused test-only repair added both; specification, regression and delta
  audits are clean. No parser behavior, CST topology, public API, ledger or
  benchmark process changed.

## RoleDeclaration direct CST schema

- Added Rowan-only proof for direct BodyIntroducer after a completed Head and
  inline Body after actual Colon. It fixes Missing/Error, all body starters,
  terminal/retry boundaries, UTF-8, CRLF and leading ownership without records
  or Error spelling.
- Head exclusion exposed two existing CST forms: absent Head is
  `TypeExpression(Missing)`, while malformed Head Error occurs before completed
  Type; a successful Type retry owns its leading internally. They are now
  explicitly kept out of BodyIntroducer selection.
- Nested `BindingBody(Missing)` remains under the child Statement. M1
  specification audit was clean. No parser behavior, CST topology, public API,
  ledger or benchmark process changed.

## ImplDeclaration direct CST schema

- Added Rowan-only proof for BodyIntroducer and inline Body. The ordinary first
  same-line Colon is internal to `ImplDescription`; the following direct Colon
  is the Body path. Missing or malformed Description remains upstream.
- The direct body proof covers completed-Head starter/retry context as well as
  completed Description, then fixes Missing/Error, UTF-8, CRLF/protected
  boundaries, retry-leading and child BindingBody ownership without records or
  Error spelling.
- M1 specification audit was clean. No parser behavior, CST topology, public
  API, ledger or benchmark process changed.

## Declaration Variant core CST schema

- Added Rowan-only proof for shared Enum/Error Variant Item, Name and Separator.
  Terminal Error/Missing and Error-to-Identifier retry are separated by direct
  Variant child order; Separator is a declaration-shell sibling Missing.
- Required Item before comma remains nested and distinct. M1 specification
  audit was clean. No parser behavior, CST topology, API, ledger or benchmark
  process changed.

## Enum/Error declaration header CST schema

- Added paired Rowan-only proof for Name and BodyIntroducer. Missing Name does
  not cascade at an actual body starter; clean header termination is Bodyless,
  so no BodyIntroducer Missing is fabricated.
- Name retry and post-name BodyIntroducer Error retry retain native leading and
  all four starters outside Error. M1 specification audit was clean; no parser,
  CST topology, API, ledger or benchmark process changed.

## StructDeclaration header CST schema

- Added Rowan-only Name/BodyIntroducer proof. Struct child order and native
  starter prefix select the header slots; Colon-following fields remain outside
  this header row. M1 specification audit was clean; no parser/API/ledger change.

## ActDeclaration direct body CST schema

- Added Rowan-only direct BodyIntroducer/Body proof; valid bodyless termination
  remains diagnostic-free. M1 specification audit was clean.

## DeclarationCompanion direct CST schema

- Added Rowan-only proof for six direct companion roles across five shells and
  seven attachment paths. M2 specification, regression and delta audits closed.

## UseDeclaration independent CST schema

- Added Rowan-only Path, Alias and group-entry proof with nested/exclusion and
  UTF-8 controls. Foreign/local close remains explicitly pending topology choice.

## Rule ExpressionList fence CST schema

- Added caller-local RuleItem/RuleCall/RuleIndex fence-close proof. Fence bytes
  remain outside CST; M1 specification audit was clean.

## BracedStatementBlock raw Error CST schema

- Added Rowan-only direct raw Error proof with retry, separator, newline and
  UTF-8 topology. M1 specification audit was clean; Missing/Close remain separate.

## ColonApplication indented Statement entry CST schema

- Added Rowan-only proof for the first required Statement under the complete
  ColonApplication tail ancestry: native leading, Missing, raw Error tokens,
  retry-leading ownership, accepted control and UTF-8/CRLF ranges. M2 pre-write
  specification and post-write regression/delta audits were clean; other
  transported callers and later/layout slots remain open.

## Use group foreign-close topology selection

- Recorded the user's option A selection as the Authoritative
  `UseGroupForeignClose := Error+` topology. It wraps one existing locally
  consumed mismatched close under UseGroup/UseExclusionGroup; direct GroupEntry
  Error, outer-close handoff, OperatorName and temporary records remain
  unchanged. Specification and compiler/recovery pre-write audits were clean.
- Completed private M2 construction with append-only kind 282 and one owner-local
  start/finish pair. UseDeclaration 20/20, SyntaxKind 3/3 and package check
  passed; scoped closure review was clean. No benchmark process was used. Local
  Missing close, OperatorName, interpreter/API migration and ledger retirement
  remain open.

## Parenthesized/EffectRow foreign-close topology

- Recorded the user's option A selection as the Authoritative shared
  `TypeDelimitedForeignClose := Error+` topology. The immediate
  ParenthesizedTypeGroup or EffectRowType parent selects its matching close;
  the wrapper itself is transparent and one existing consumed foreign close
  produces one node.
- Completed private M2 construction with append-only kind 283 and one balanced
  start/finish pair around the existing P/E close-Error emitter. Direct Item
  Error, Call/BracketRow, accepted syntax, leading, records, frozen replay and
  handoff remain unchanged.
- Direct Rowan proof covers both former collisions, mixed and contiguous
  repeated closes, ranges/trivia, shifted origins, boundary/nested paths and
  malformed sibling exclusions. P/E 9/9, SyntaxKind 3/3 and package check
  passed. Compiler/recovery review was clean; a regression finding added the
  central slot row and boundary controls, and a fresh specification delta audit
  closed cleanly. Benchmark use was zero samples/processes.
- The P/E catalog row is evidence-complete in its bounded scope. Other Type
  rows, global CST interpretation, parser API migration and ledger retirement
  remain open.

## If arm direct Rowan schema

- Added one Rowan-only matrix with 19 witnesses for Condition no-cascade,
  BodyIntroducer, If/Elsif Body, bare/colon ElseBody and the transported
  IndentedStatement block. Classification uses only owner ancestry, direct
  sibling order, ranges and arm occurrence ordinal.
- The nested `if if:` witness fixes preorder for three occurrences: inner
  Condition Missing, then inner Body Missing, then outer BodyIntroducer Missing;
  the latter pair share the same byte offset but have distinct occurrence
  paths. Raw Error grouping/retry, native trivia, accepted controls and one
  UTF-8/CRLF range are also direct evidence.
- The exact new test passed 1/1 and the existing `if_c6_` family passed 8/8.
  M1 expected-output pre-write and post-write regression audits were clean.
  No production, SyntaxKind, parser API, ledger or benchmark process changed.
- The central catalog/coverage row is independently audited and evidence-
  complete in this bounded scope after correcting two source-ownership
  descriptions. Delegated Condition, nested grammar, exhaustive boundaries,
  other transported callers and the global interpreter remain open.

## For direct Rowan schema

- Added one records-free 15-case matrix for initial Pattern, InKeyword,
  Iterable, BodyIntroducer, shallow/inline Body and transported
  IndentedStatement. Ordered direct children and owner ancestry distinguish
  the flat For phases without Error spelling or new topology.
- Three initially drafted direct-child expectations were corrected against
  existing authority: iterable and inline expression retry leading belongs to
  the admitted expression node, and indented retry leading belongs to the
  admitted Statement. The tests now pin those native descendants and the
  preceding Error group boundary directly.
- The exact new test passed 1/1 and existing `for_c13_` controls passed 12/12.
  M1 expected-output pre-write and post-write specification audits were clean.
  No production, SyntaxKind, parser API, ledger or benchmark process changed.
- The central catalog/coverage row is independently audited after replacing a
  compulsory full-header sketch with the actual terminal/continuing grammar
  union. It is evidence-complete in this bounded scope. Labels, braced
  internals, nested grammar, exhaustive boundaries and the global interpreter
  remain open.

## CaseLike direct Rowan schema

- Added three records-free Rowan matrices for Case/Catch Scrutinee, Guard,
  Block, Arm, Pattern/Handler, Arrow/Body, Separator, Catch close and
  transported indented Statement recovery. Classification uses only complete
  ancestry, direct sibling order, native punctuation, ranges and Error-token
  adjacency.
- The terminal Arrow/Body absence remains one Missing occurrence with two
  ordered diagnostic alternatives. Direct LBrace distinguishes Catch's local
  close from the same Block role's missing Colon; preorder distinguishes the
  child Body/parent Separator and nested arm/parent close at equal offsets.
- New `case_schema_` tests passed 3/3 and all focused CaseLike tests passed
  29/29. M1 pre-write and post-write specification audits were clean after
  correcting new-test OperatorChain shells and adding the indented Missing
  branch. No production, SyntaxKind, parser API, recovery ledger or benchmark
  process changed.
- The central catalog/coverage row is evidence-complete in this bounded scope.
  Exhaustive boundary/layout alternatives, nested child schemas, the global
  interpreter and ledger/API retirement remain open.

## TypeAnnotationTail required-Type primary Error schema

- Added the separate central row for nonempty malformed Type primary input
  after exact `as`. Direct `TypeAnnotationTail` child order selects one maximal
  adjacent Error-token group and its optional TypeExpression retry without
  using parser records or Error spelling.
- The existing structural-tail test already proves terminal Error, admitted
  retry, four adjacent UTF-8 Error fragments, accepted Type and protected-close
  handoff. A pre-write specification audit found that evidence sufficient, so
  no duplicate test, production change or benchmark process was added.
- This recovery projects Type(Primary), never Expression(TypeAnnotation); the
  existing `TypeExpression(Missing)` annotation row and other required-Type
  callers remain separate. Global interpretation and ledger/API retirement
  remain open.

## PatternTypeAnnotation required-Type primary Error schema

- Added one direct-Rowan test for terminal Error, admitted Type retry, a
  multi-leaf adjacent Error group and protected shallow-newline handoff after a
  Pattern annotation Colon. It asserts exact direct order, parentage and byte
  ranges without Error spelling or recovery records.
- The new test and the existing Pattern annotation control each passed 1/1.
  M1 pre-write, post-write and bounded catalog-delta specification audits were
  clean; no production, SyntaxKind, parser API, recovery ledger or benchmark
  process changed.
- This bounded row projects Type(Primary), not Pattern(TypeAnnotation). Fresh
  Missing, accepted-only/nested Type, other caller/boundary forms, UTF-8/CRLF,
  the global interpreter and ledger/API retirement remain open.

## PatternTypeAnnotation fresh required-Type Missing schema

- Added a second direct-Rowan test for the distinct fresh absence after Pattern
  annotation Colon. It fixes `Colon` followed by an empty
  `TypeExpression(Missing)`, the sole zero-width insertion at `2..2`, and EOF
  versus protected shallow-newline handoff without recovery records.
- The new Missing test, the existing annotation compatibility control and the
  sibling Type-primary Error test each passed 1/1. M1 pre-write and post-write
  specification audits and the bounded catalog-delta audit were clean; no
  production, SyntaxKind, parser API, recovery ledger or benchmark process
  changed.
- This bounded row projects Pattern(TypeAnnotation), not Type(Primary).
  Nonempty Error/retry, accepted/nested Type, other boundary/origin/layout
  forms, UTF-8/CRLF/Yumark, global interpretation and ledger retirement remain
  open.

## TypeDeclaration equality-RHS fresh required-Type Missing schema

- Added one full-shell direct-Rowan test for fresh Type absence after an actual
  declaration Equals. Exact declaration child order and the nested empty
  `TypeExpression(Missing)` distinguish Declaration(Type(Rhs)) from the direct
  DefinitionIntroducer Missing without parser records.
- The EOF and protected-Semicolon witnesses fix direct trivia ownership,
  zero-width ranges, complete ancestry, one-Missing cardinality and pending
  boundary handoff. The new test passed 1/1 and the focused TypeDeclaration
  module passed 45/45.
- M1 pre-write, post-write and catalog-delta specification audits were clean.
  No production, SyntaxKind, parser API, recovery ledger or benchmark process changed.
  Malformed/retried/accepted/nested Type, other header/caller/boundary forms,
  global interpretation and ledger retirement remain open.

## TypeDeclaration equality-RHS Type-primary Error schema

- Added one full-shell direct-Rowan test for nonempty Type-primary Error after
  actual declaration Equals. Terminal EOF, admitted TypeExpression retry and
  protected Semicolon witnesses fix exact order/ranges, native leading,
  retry-owned leading and handoff without records or Error spelling.
- The new test passed 1/1 and the focused TypeDeclaration module passed 46/46.
  M1 pre-write, post-write and catalog-delta specification audits were clean
  after correcting the retry locator; no production, SyntaxKind, parser API,
  recovery ledger or benchmark process changed.
- This row projects Type(Primary), never Declaration(Type(Rhs)) or
  DefinitionIntroducer. The direct caller witnesses each contain one Error
  leaf; multi-leaf grouping retains the shared publisher authority and existing
  expression-annotation support. Broader Type/caller/global work remains open.

## DerivesClause initial RoleReference fresh Missing schema

- Added one direct-Rowan test for the first required RoleReference after
  DerivesKw. Four witnesses fix the exact
  `DerivesClause(DerivesKw TypeExpression(Missing))` topology, `7..7` Rowan
  insertion, and EOF/trivia/RBracket/newline handoff without recovery records.
- The new test passed 1/1 and the focused Derives module passed 11/11. M1
  pre-write, post-write and catalog-delta specification audits were clean; no
  production, SyntaxKind, parser API, recovery ledger or benchmark process changed.
- Spaced EOF deliberately keeps the old record's physical `9..9` coordinate
  while CST-derived projection uses the direct Missing range `7..7`. Comma
  episodes, malformed/nested Type, ViaTarget, attachment/global work and ledger
  retirement remain open.

## DerivesClause post-comma RoleReference fresh Missing schema

- Added one direct-Rowan test for the second required RoleReference after an
  accepted first TypeExpression and direct Comma. Sibling order and ordinal
  select the distinct occurrence of the same Declaration(Derives(RoleReference))
  role without parser records.
- EOF and protected-RBracket witnesses fix exact children/ranges, the sole
  `11..11` Missing, pending leading, successor origin and remainder. The new
  test passed 1/1 and the focused Derives module passed 12/12.
- M1 pre-write, post-write and catalog-delta specification audits were clean;
  no production, SyntaxKind, parser API, recovery ledger or benchmark process changed.
  Further roles, malformed/nested Type, ViaTarget, attachment/global work and
  ledger retirement remain open.

## DerivesClause initial RoleReference Type-primary Error schema

- Added one direct-Rowan test for nonempty malformed initial RoleReference Type
  input after DerivesKw. It fixes one maximal adjacent direct Error group,
  optional direct TypeExpression retry and protected-RBracket handoff without
  record or Error-spelling classification.
- The new test passed 1/1 and the focused Derives module passed 13/13. M1
  pre-write, post-write and catalog-delta specification audits were clean after
  correcting shifted ViaTarget evidence locators; no production, SyntaxKind,
  parser API, recovery ledger or benchmark process changed.
- This row projects Type(Primary), not Declaration(Derives(RoleReference));
  caller-role transport remains fresh-Missing-only. Post-comma malformed/nested
  Type, other boundaries, ViaTarget, attachments and global work remain open.

## DerivesClause post-comma RoleReference Type-primary Error schema

- Added one direct-Rowan test for nonempty malformed second RoleReference Type
  input after an accepted first TypeExpression and direct Comma. It fixes the
  distinct occurrence path, maximal direct Error group, optional retry and
  protected-RBracket handoff without records or Error spelling.
- The new test passed 1/1 and the focused Derives module passed 14/14. M1
  pre-write, post-write and catalog-delta specification audits were clean after
  correcting shifted ViaTarget locators; no production, SyntaxKind, parser API,
  recovery ledger or benchmark process changed.
- This row projects Type(Primary), not the fresh-Missing caller role. Later
  commas, nested Type, other boundaries, ViaTarget, attachments and global
  interpreter/ledger work remain open.

## DerivesClause compositional RoleReference repetition schema

- Rejected third/fourth ordinal-specific catalog rows after independent
  architecture and specification audits confirmed that the existing comma loop
  already defines one arbitrary repetition. `RoleEpisode` is notation only;
  no Rowan wrapper, parser phase state or new diagnostic vocabulary was added.
- Added one seven-row direct-Rowan composition test covering consecutive fresh
  Missing episodes, Error-to-comma, Type retry-to-comma with native comma
  leading, Via transition after Missing/Error/retry, Via termination with an
  outer comma, and a protected close after the second repetition. A referee
  corrected the pre-write `derives,,` sketch to retain both commas and all three
  TypeExpression(Missing) wrappers.
- The new test passed 1/1 and the focused Derives module passed 15/15. The
  post-write and catalog-delta specification audits were clean after repairing
  one off-by-one ordinal sentence. They confirmed the finite transition set
  plus the identical source loop entry as an induction proof for arbitrary
  ordinals. No production, SyntaxKind, parser API, recovery ledger or benchmark
  process changed. Nested Type/ViaTarget composition, other caller/layout/fence
  families, repeated clauses and global migration remain open.

## Use group local terminal-close schema

- Selected the shared `UseGroup` / `UseExclusionGroup` terminal close as the
  next finite production gap. The row is occurrence-conditional: propagated
  nested-`UseTree` or raw-recovery exits have no local close child, and no
  parser Missing was added to make the schema look total.
- Added four direct-Rowan tests. A 36-cell finite transition matrix covers the
  three opener/owner combinations, six distinct preceding group phases, and
  native matching close versus direct terminal Missing. Separate witnesses fix
  UTF-8 ranges, CRLF pending-leading ownership, both borrowed outer-close
  directions and no-local-close propagated exits without records or Error
  spelling.
- The four new tests passed 4/4 and the focused UseDeclaration module passed
  24/24. M1 pre-write, post-write and catalog-delta specification audits were
  clean; format and scoped diff checks passed, with zero benchmark processes.
  The bounded row is catalog-audited and evidence-complete within its stated
  scope. OperatorName, nested UseTree schemas, additional fences, global
  interpretation and ledger/API retirement remain open.

## ForallType direct-head composition schema

- Re-ranked the next production gap against current catalog statuses and
  rejected the stale BracedStatementBlock raw-Error suggestion because that
  bounded row was already catalog-audited. Selected the Forall direct head as
  a composition of its three audited phase rows, not another isolated slot.
- Added three Rowan-only composition tests. The full witness crosses first
  Binder Error, grammar-empty actual-binder gaps, two separate separator
  placeholders, Colon Error and recovered binders, actual Colon, Body Error and
  TypeExpression retry. EOF/space/newline variants prove a covered Colon is not
  restored; `for'a:T` fixes the first actual-binder gap; five terminal controls
  fix current-slot-only Missing/Error and no cascades.
- The new tests passed 3/3 and the focused Forall recovery module passed 19/19.
  M1 pre-write, post-write and catalog-delta specification audits, format and
  scoped diff checks were clean, with zero benchmark processes. The catalog
  audit corrected one overbroad leading-owner sentence to distinguish
  first-binder, separator and direct Colon/Body ownership. The aggregate is
  catalog-audited and evidence-complete within its stated scope. Nested Type,
  broader caller/fence and public paths, the global interpreter and ledger/API
  retirement remain open.
- The refreshed gap ranking selects the complete direct `TypeCallTail`
  composition next. Its three phase rows are already individually audited, but
  their repeated CallArgument/Separator sequence and terminal TypeCallClose
  interaction still lack one production-level schema. This is an M2 mapping
  gate because it spans the T3, close-node and residual-close authorities.

## TypeCallTail direct production composition schema

- Added six Rowan-only transition tests spanning explicit separator repetition,
  item retry, close epsilon, same-offset item/close Missing, inherited and
  ordinary-layout continuation, pipe-special item Error, residual close and a
  protected stop. Focused TypeCall fallback passed 15/15.
- M2 specification and compiler pre/post reviews were clean after one bounded
  repair added explicit node/token identity to the shared assertion helper.
  Production behavior, existing expectations and SyntaxKind were unchanged;
  benchmark use was zero. Catalog-delta audit closed after clarifying that
  explicit separators, inferred separators, ordinary-layout re-entry and
  pipe-special post-item recovery advance occurrences, while same-item retry
  preserves one. The aggregate is catalog-audited and evidence-complete within
  its stated scope.

## ML separator-leading ownership decision

- The new full `CastDeclaration` accepted-composition witness exposed a shared
  CST ownership conflict: `pub cast(x: int): user_id = user_id { raw: x }`
  requires the separator space as a direct outer `OperatorChain` child, but the
  current `ml_argument` opens `MlArgument` before the admitted Item emits its
  leading.
- The user selected option A: all accepted ML separator leading is direct
  native content of the enclosing `OperatorChain`. The Authoritative successor
  record `2026-09-12-successor-ml-separator-leading-ownership.md` supersedes
  only E5's narrower inside-leading topology, retaining its one-Item/no-rescan,
  Index close and sibling-continuation contracts.
- Construction is complete. `ml_argument` now moves the same admitted Item's
  remaining leading before the existing `MlArgument` wrapper; no scanner,
  replay, state, SyntaxKind or diagnostic vocabulary was introduced. Four ML,
  four Index, two Cast aggregate, nine normalized Pratt, one normalized quoted
  fence and the repaired fixed-tail controls passed, as did `cargo check`,
  scoped formatting and diff checks. The package check retained three existing
  warnings; benchmark use was zero.
- Independent pre-write specification, compiler/recovery and regression audits
  were clean. Regression found one stale fixed-tail assertion that still
  expected ML-leading inside the argument; focused specification review
  authorized its exact outer-chain replacement, and a fresh delta review was
  clean. Two unrelated existing `owners` recovery-group assertions remain
  unverified as baseline failures and were not changed.

## CastDeclaration full phase composition schema

- The two direct aggregate tests now cover four accepted Cast shells and seven
  ordered Error/retry shells. They compose the existing PatternIntroducer,
  Pattern-close, TargetIntroducer, BodyIntroducer and inline Body rows without
  inventing an aggregate publisher; Pattern primary, TargetType, nested
  Expression/ML and indented Statement recovery remain delegated.
- A post-write regression audit found that the aggregate's inline brace ML
  sample did not exercise ordinary braced statement dispatch. Added the
  separately authorized `{ cast(x): T; }` direct Rowan witness through the real
  statement path: the semicolon is Cast-owned and enclosing whitespace/RBrace
  remain block-owned. The focused control and full Cast declaration module
  passed; the fresh delta review was clean.
- The central CST slot catalog now has a bounded CastDeclaration aggregate
  Draft and repaired three stale inline-Body proof locators. Catalog-delta
  specification review was clean. The aggregate is evidence-complete only for
  its four accepted, seven malformed and one ordinary-braced witness; broader
  boundaries/fences, multi-Error paths, public collection and ledger/API
  retirement remain open.

## Delimited DeclarationFieldSequence composition schema

- Added four direct Rowan composition tests spanning eleven Struct, Enum and
  Error cases. They join audited named/tuple FieldItem and head phases with
  separator recovery, same-offset nested/local/outer close preorder, UTF-8
  Struct foreign-close separation, and variant Borrow handoff. Ancestors now
  distinguish StructDeclaration, EnumDeclaration and ErrorDeclaration rather
  than relying on their shared EnumVariant payload node.
- A post-write review required two evidence repairs: matching tuple close now
  proves native RParen plus Struct companion/Enum-or-Error outer continuation,
  while EOF ordering remains a separate control; the companion witness places
  its gap inside `DeclarationCompanion` under the established DC-G grammar.
  Pre-write, compiler/recovery, regression, repair-delta and catalog-delta
  audits are clean after an explicit matching-close proof locator correction.
- Focused composition controls passed 7/7; the Struct module passed 53/53 and
  the variant module passed 43/43. Package check, scoped formatting and diff
  checks passed with three existing warnings; benchmark use was zero. The
  catalog aggregate remains bounded to delimited lists, excluding indented
  fields, other boundaries/fences, full declaration progression, the global
  interpreter and ledger/API retirement.

## Braced Enum/Error VariantSequence composition schema

- Added three direct Rowan tests across 26 Enum/Error braced witnesses. They
  compose Item/Name retry, completed-payload Separator, FromType Error retry,
  fresh FromType Missing, empty/trailing-comma controls, and native versus
  missing outer closes without reading records or Error spelling.
- Regression added the native outer-close companion to the EOF FromType
  control: the nested TypeExpression Missing precedes an actual RBrace with no
  outer Missing, while the EOF witness retains its equal-offset nested-then-
  outer Missing preorder. M2 pre-write, compiler/recovery, regression and
  repaired-delta audits were clean.
- Focused braced controls passed 4/4; declaration-variant, Enum and Error
  modules passed 47/47, 19/19 and 18/18. Package check, scoped format and diff
  checks passed with three existing warnings; benchmark use was zero. The new
  catalog aggregate excludes fields, inline/indented forms, companions, pipes,
  fences, caller stops, full Type internals, global interpretation and ledger
  retirement.

## Enum/Error header to braced VariantSequence composition schema

- Added one direct Rowan 56-product matrix spanning both Enum/Error shells,
  UTF-8 Name and BodyIntroducer Error retries, seven bounded VariantSequence
  body classes, and native/absent outer closes. Header Error remains direct
  declaration content; body recovery begins only after native LBrace.
- Compiler/recovery and regression audits were clean. The matrix keeps `@ {`
  out of the Name-retry route, preserves FromType nested-before-outer EOF
  preorder, and never uses records or Error spelling for selection.
- Focused matrix, declaration-variant, Enum and Error modules passed 1/56,
  48/48, 19/19 and 18/18. Package check and scoped format/diff passed with
  three existing warnings; benchmark use was zero. The catalog aggregate
  remains bounded to braced header/body composition.

## ColonIndented Enum/Error header to VariantSequence composition schema

- Added a 32-product direct Rowan matrix over both owners, header retries,
  four body classes and EOF/dedent. Initial layout is Variant-owned, later
  implicit separator layout is declaration-owned, and dedent stays pending.
- Focused control and both header suites passed; M1 pre-write and regression
  reviews were clean. No production state or diagnostic record was added.

## EqualsIndented Enum/Error header to VariantSequence composition schema

- Added 32 direct Rowan products proving native Equals, layout ownership,
  retries and EOF/dedent handoff for both shells. EqualsInline remains separate.
- Focused test and header controls passed; M1 pre-write and regression review
  were clean. No production state or record was added.

## EqualsInline Enum/Error VariantSequence and `with` successor composition schema

- Added 146 direct Rowan products for header/core recovery, pipe controls and
  the exact `with` successor boundary. Enum attaches the qualifying successor
  as `DeclarationCompanion`; Error preserves the pending Item for Statement.
- M2 pre-write, compiler/recovery and regression audits were clean. Focused
  tests, declaration-variant and EqualsInline controls, scoped formatting/diff,
  and package check passed; the package check retained three existing warnings.
  No production state, diagnostic record or recovery publisher was added.

## EqualsInline Enum Companion-slot and Error handoff composition schema

- Added 528 paired direct Rowan products connecting the admitted successor to
  direct Enum Companion slots while preserving Error's exact unread `with`
  handoff. The proof keeps `with: @` under nested Statement rather than
  fabricating a Companion Body Error, and retains zero Missing after `A | with`.
- M2 pre-write, compiler/recovery and regression audits were clean. Focused
  companion/declaration checks, scoped formatting/diff and package check
  passed; package check retained three existing warnings. No production state,
  diagnostic record or recovery publisher was added.

## TypeDeclaration actual-Equals phase composition schema

- Added five direct Rowan products connecting header Error through native
  Equals to fresh RHS Missing or terminal/retried Type-primary Error. The two
  Error groups remain direct TypeDeclaration tokens, with their distinct slots
  selected by ordered Equals context; retry-leading belongs to TypeExpression.
- M1 pre-write adjudication and regression review were clean. Focused
  TypeDeclaration/required-Type tests and scoped formatting/diff passed. No
  production state, diagnostic record or recovery publisher was added.

## TypeDeclaration required-Name header composition schema

- Added five direct Rowan full-shell products for fresh Name Missing, terminal
  Error, raw-name retry, direct Equals retry and accepted Name. Exact Equals
  remains native outside Error and RHS internals remain delegated.
- M1 pre-write and regression audits were clean. Focused TypeDeclaration tests
  and scoped formatting/diff passed. No production state, diagnostic record or
  recovery publisher was added.

## TypeDeclaration attachment-continuation composition schema

- Added direct Rowan attachment evidence for header/trailing derives, equality
  versus nominal continuation, Companion attachment and header impl handoff.
- Header STOP_WITH remains excluded because C15 and TND conflict on the
  DefinitionIntroducer outcome. M2 reviews and repair-delta review were clean;
  focused checks and package check passed with three existing warnings.

## Mod marker retry-leading ownership repair

- Corrected `mod @ test;` so retry-leading trivia is direct ModDeclaration
  content and `TestModuleMarker` contains only the `test` Identifier.
- Exact CST/range, fresh/frozen and public-root fidelity controls passed; M1
  regression review was clean. Full Mod shell composition remains separate.

## ModDeclaration full direct shell composition

- Added 25 direct Rowan statement-harness products across Name, marker/TestName,
  BodyIntroducer and inline Body, including EOF ownership and `mod 名:;` Body
  Missing before native Semicolon.
- M1 expected-output adjudication and regression review were clean. Focused Mod
  checks and scoped formatting/diff passed. Source-root/public topology and
  nested Statement recovery remain excluded.

## RoleDeclaration completed-head direct shell composition

- Added nine direct Rowan statement-shell products for BodyIntroducer and
  actual-colon Body, including terminal Error/no-cascade and Root EOF suffixes.
- Clarified the catalog grammar's terminal Error alternatives. M1 review and
  leading-ownership delta review were clean; focused Role checks passed.

## ImplDeclaration completed head/description direct shell composition

- Added direct Rowan shell evidence for both body entry contexts, two-colon
  distinction, terminal/retry recovery and EOF/pending ownership.
- Clarified terminal Error alternatives in the Impl catalog grammar. M1
  pre-write and regression review were clean; focused Impl checks passed.

## StructDeclaration header-to-body and actual-close attachment composition

- Added three direct Rowan tests for the complete bounded Struct shell: bodyless,
  named, tuple and colon-indented forms; header derives-to-companion; and
  actual-close derives/companion continuation. The evidence fixes direct child
  order, node/token form, parentage, byte ranges, companion-leading ownership,
  pending successor and normalized exit distinctions.
- Field-local Missing/Error recovery remains delegated. It still permits a
  trailing companion after an actual matching close, while missing/foreign
  closes, CRLF equal-line handoff and active `STOP_WITH` attach nothing.
- M2 pre-write, compiler/recovery and regression reviews were clean. Focused
  attachment 3/3, Struct declaration 56/56 and normalized Struct 4/4 checks,
  scoped format/diff, and package check passed. The package check retained
  three existing warnings; benchmark use was zero.

## ActDeclaration required-Type Head and Equals-selected Source

- Added a seven-witness direct Rowan matrix for fresh/retried Head and Source
  Type slots. Native `Equals` selects the slot; fresh absence is a childless
  Missing under TypeExpression, while malformed input remains an opaque direct
  Error followed by Type-owned retry leading.
- Existing compatibility-record assertions remain unchanged and are not used by
  the new proof. No parser behavior, diagnostic state or interpreter changed.
- M1 pre-write and post-write specification audits were clean. The focused new
  test passed 1/1, the Act module 25/25, and scoped format/diff passed.

## Act actual-brace trailing `derives` attachment-gap repair

- Repaired the Act-owned attachment predicate to admit the already-authorized
  empty gap after an actual braced close; it no longer requires ordinary trivia
  before `derives`. This fixes `act A{}derives Eq` without changing the distinct
  Companion guard.
- Added direct CST evidence for empty, same-line, deeper LF and deeper CRLF
  gaps, with gap trivia inside DerivesClause; equal-indent LF/CRLF still return
  `derives` pending with its complete leading gap.
- M2 pre-write, compiler/recovery and regression reviews were clean. Focused
  regression 1/1, Act declaration 26/26, scoped format/diff and package check
  passed; the package check retained three existing warnings. Benchmark use was
  zero.

## ActDeclaration attachment-continuation composition

- Added five direct Rowan composition tests joining completed Head/Source/body
  phases to derives and the terminating Companion. The matrix keeps fresh and
  malformed required-Type recovery delegated, requires an actual brace close
  for trailing derives, and fixes pending bodyless/colon/foreign-close/CRLF/
  STOP_WITH handoff.
- Review found and repaired two evidence-only gaps: direct child form/text/end
  now supplement range/parent checks, and boundary coordinate proof observes
  real input suffix advancement before deriving ItemExtent payload start. No
  parser exit field, retained state or diagnostic mechanism was added.
- M2 pre-write, compiler/recovery, regression and delta specification reviews
  were clean. Attachment 5/5, Act 31/31, scoped formatting/diff and package
  check passed; package check retained three existing warnings. Benchmark use
  was zero.

## DeclarationCompanion direct recovery schema

- Completed the bounded six-fact catalog row for Introducer, Body,
  Item/IndentedItem, Separator and local Brace Close. It records direct ordered
  grammar, retry/grouping, delegated ownership, protected handoff, CST-derived
  projection and scoped status without selecting attachment owners.
- Added two full-child Rowan matrices to preserve trivia, exact node/token
  ranges and lossless source reconstruction. They distinguish ordinary
  post-Colon admission from malformed Statement retry and pin CRLF/Japanese
  pending leading/payload byte extents before emission.
- M2 pre-write, compiler/recovery, regression and delta reviews were clean.
  Full-child 2/2, Companion module 31/31, scoped format/diff and package check
  passed; package check retained three existing warnings. Benchmark use was
  zero.

## UseDeclaration OperatorName local close schema

- Added direct Rowan evidence for the Parenthesis-close slot after an admitted
  Operator across initial Path, separator Path and `without` exclusion callers.
  Native adjacent close is clean; EOF emits the empty local Missing without
  Error or Invalid.
- Continuation checks retain Path tail, Alias and UseGroup ownership, while a
  spaced close remains pending. Existing C9 missing-spelling negatives remain
  unchanged and do not construct OperatorName.
- M1 pre-write and regression reviews were clean. Local-close 2/2, Use module
  26/26 and scoped format/diff passed. Benchmark use was zero.

## UseDeclaration initial OperatorName spelling schema

- Added a six-witness direct Rowan matrix for the initial `LParen, Missing`
  required operator spelling slot, its pending successors and accepted `(+)`
  control. Lossless reconstruction proves following punctuation, identifiers
  and whitespace remain outside OperatorName.
- The row remains separate from the admitted-operator local close and from C9
  post-separator Path failures; neither existing negative behavior nor records
  changed.
- M1 pre-write and post-write specification audits were clean. Spelling 1/1,
  Use module 27/27 and scoped format/diff passed. Benchmark use was zero.

## UseGroup nested OperatorName spelling schema

- Added a five-outcome direct Rowan matrix for the recursive group-item
  `LParen, Missing` spelling slot. It distinguishes same-offset spelling versus
  group close/separator Missing by parent and preorder, preserves foreign-close
  wrapper ownership, and checks protected semicolon leading/payload/remainder.
- No C9, group-close, foreign-close or admitted-operator local-close expectation
  changed. The row remains narrower than complete recursive/exclusion topology.
- M1 pre-write and post-write specification audits were clean. Nested spelling
  1/1, Use module 28/28 and scoped format/diff passed. Benchmark use was zero.

## UseExclusionGroup nested OperatorName spelling schema

- Added a twelve-witness direct Rowan matrix for both exclusion-group openers.
  It fixes inner spelling Missing versus same-offset group close/separator
  occurrences, opener-selected foreign-close wrapper behavior, protected
  semicolon handoff, and accepted inner operator spelling.
- Existing C9 and direct exclusion local-close controls remain unchanged. The
  bounded row does not claim full recursive/exclusion coverage.
- M1 pre-write and post-write specification audits were clean. Exclusion
  spelling 1/1, Use module 29/29 and scoped format/diff passed. Benchmark use
  was zero.

## Use group GroupEntry and post-child separator schema

- Extended direct Rowan evidence with parenthesized exclusion-group controls
  for the initial GroupEntry Missing before Comma and the post-UseTree separator
  Missing before the next UseTree. Direct order distinguishes both from terminal
  close recovery.
- No existing Error, foreign-close, local-close, OperatorName or recursive child
  behavior changed; those remain distinct catalog rows.
- M1 pre-write and post-write specification audits were clean. Focused row 1/1,
  Use module 29/29 and scoped format/diff passed. Benchmark use was zero.

## Use group direct GroupEntry Error schema

- Added direct Rowan evidence for the shared malformed initial-entry phase in
  braced UseGroup and brace- or parenthesis-opened UseExclusionGroup. The
  parenthesis rows retain separating whitespace so the immediate OperatorName
  probe does not select a different grammar route.
- Terminal, UseTree retry, Comma retry, foreign-close-in-run and UTF-8 trivia
  rows prove immediate-parent Error adjacency and lossless byte ranges. Native
  trivia and admitted children terminate the group; a foreign close already in
  the raw run remains direct Error rather than `UseGroupForeignClose`.
- M1 pre-write and post-write specification audits were clean. Focused row 1/1,
  Use module 30/30 and scoped format/diff passed. No repair or package check was
  required; benchmark use was zero.

## Use group post-child direct GroupEntry Error schema

- Added one 21-witness direct Rowan matrix for raw GroupEntry recovery after an
  admitted UseTree in braced UseGroup and both UseExclusionGroup opener forms.
  The Error stays Import(GroupEntry)/Path and is grouped only by direct sibling
  adjacency.
- Direct UseTree retry retains the admitted-child phase and therefore receives
  the separately established Comma Missing; native Comma clears it. Matching
  close, foreign-close-in-run, UTF-8 trivia and protected semicolon rows prove
  completion, ownership and lossless pending-source reconstruction.
- M1 pre-write and post-write specification audits were clean. Focused row 1/1,
  Use module 31/31 and scoped format/diff passed. One test-harness constructor
  typo was repaired before successful compilation; no expectation or production
  behavior changed. Package check was deferred by the M1 budget; benchmark use
  was zero.

## UseExclusion form-dispatch aggregate

- Added one eleven-form direct Rowan matrix for the finite UseExclusion
  dispatcher. Every occurrence owns exactly one Identifier, Star, OperatorName
  or UseExclusionGroup payload; enclosing WithoutKw and trivia remain direct
  UseGlob children.
- The matrix fixes the immediate-parenthesis boundary: `(+)` and incomplete
  `(+` select OperatorName, while `()`, `(name)` and `( +)` select a group.
  Recursive UseGlob ancestry and delegated group/Error or close/Missing
  ownership are explicit; the dispatcher itself projects no diagnostic.
- M1 pre-write and post-write specification audits were clean. Focused row 1/1,
  Use module 32/32 and scoped format/diff passed. No repair or package check was
  required; benchmark use was zero.

## UseGlob first required-exclusion admission

- Added one eight-source direct Rowan matrix for the first mandatory exclusion
  after WithoutKw. Direct UseGlob Missing/Error remain Import(Path)/Path;
  identifier and OperatorName retries delegate their payload and remaining
  native leading to UseExclusion.
- Pre-write audit corrected comma from raw Error content to a protected pending
  boundary and retained reserved `with` after its leading had been emitted.
  Focused execution then distinguished original ItemExtent leading from empty
  remaining leading without replaying source bytes.
- Post-write specification review found only an overbroad fresh-dispatch trivia
  exclusion in the preceding catalog row; it was narrowed and cross-linked to
  this recovery row. Focused row 1/1, Use module 33/33 and scoped format/diff
  passed. No production behavior or expected diagnostic changed; package check
  was deferred by the M1 budget and benchmark use was zero.

## UseGlob repeated-exclusion episode

- Added one ten-source direct Rowan matrix for the horizontal outer-comma loop.
  One and two successful repetitions share the same Episode topology rather
  than acquiring ordinal-specific roles.
- Direct outer Comma/Missing/Error, fresh and retry-leading, protected comma and
  semicolon, nested group punctuation, OperatorName delegation and normal
  qualifier termination retain their immediate owners. The qualifier witness
  fixes `UseAnchor > UsePath > Whitespace, Identifier` nesting.
- M1 pre-write and post-write specification audits were clean after correcting
  that test-only nested Anchor expectation. Focused row 1/1, Use module 34/34
  and scoped format/diff passed. No production behavior changed; package check
  was deferred by the M1 budget and benchmark use was zero.

## UseQualifiers anchor-path aggregate

- Added one eleven-source direct Rowan aggregate for Version-only, Anchor-only
  and combined qualifiers plus the anchor-owned word path. The initial UseTree
  path and nested anchor UsePath are selected by ancestry rather than kind.
- Initial Missing/Path, post-separator Missing/Identifier and direct Error/Path
  remain distinct ordered occurrences under the nested UsePath. UTF-8 retry,
  ColonColon/Slash ownership and two protected semicolon handoffs are lossless;
  UseQualifiers and UseAnchor themselves remain diagnostic-free.
- M1 pre-write and post-write specification audits were clean after correcting
  the proposed Slash case from Missing to Error. Focused row 1/1, Use module
  35/35 and scoped format/diff passed. No production behavior changed; package
  check was deferred by the M1 budget and benchmark use was zero.

## Realm/Band marker-target dispatcher

- Added one direct Rowan matrix for the shared `realm/` / `band::` dispatcher:
  sixteen paired group/path controls, ordinary identifier-path controls,
  delegated Path/OperatorName recovery and a nested marker group.
- Pre-write inspection corrected the proposed Realm glob witness. `realm/*`
  begins a block comment before marker admission and a spaced Star cannot meet
  empty-leading admission, so immediate accepted glob evidence is Band-only.
  No lexical or parser behavior changed.
- M1 post-write specification review was clean. Focused row 1/1, Use module
  36/36 and scoped format/diff passed. Package check was deferred by the M1
  budget; benchmark use was zero.

## Mod form-head required-path ownership

- Repaired `parse_mod_target` to restore the preserved canonical accepted CST:
  admitted form-head inline trivia is emitted directly under UseTree and the
  following UsePath begins at Identifier. The repair caches the original gap and
  boundary facts, so malformed, reserved and protected leading stays path-owned.
- Added direct Rowan evidence for accepted whitespace/comment/recursive forms,
  unchanged raw-error/reserved/protected controls and delegated group/glob/
  OperatorName tails. A test-only tail expectation was corrected to retain
  ColonColon inside UsePath before its sibling group/glob target.
- M1 pre-write specification and compiler/recovery closure reviews were clean.
  Focused row 1/1, Use module 37/37 and scoped format/diff passed. Package check
  was deferred by the M1 budget; benchmark use was zero.

## UseGlob full phase composition

- Added a bounded M2 direct Rowan aggregate for bare/repeated aliases, first and
  repeated exclusion episodes, interleaved Alias/Glob recovery, nested group or
  operator payloads, qualifier return and protected handoff. Component rows
  retain their own diagnostic ownership; the aggregate adds no new Glob role.
- Regression review found missing marker-caller and two qualifier-return
  transitions. A single repair bundle added Band-only direct Glob controls for
  alias-only and alias-plus-repeated-exclusion closure; Realm's comment lexical
  boundary remains unchanged.
- M2 pre-write, specification and regression closure reviews were clean after
  repair. Use module 39/39, scoped format/diff and `cargo check -p yu-syntax`
  passed; the package check retained three existing warnings. Benchmark use was
  zero.

## UseTree terminal join ownership

- Repaired `parse_path_tail` to match the retained Authoritative CST contract:
  a terminal ColonColon or Slash is now a direct UseTree child between UsePath
  and its immediately admitted group/glob target. Nonterminal separators remain
  under UsePath; marker form-head separators remain independent direct children.
- The owner retains the separator while acquiring one lexical next Item, whose
  unit builder state cannot emit CST. It closes UsePath before emitting only an
  empty-leading terminal target, preserving token order, one scan, recovery
  ranges, projection accounting and protected-boundary handoff otherwise.
- Added direct Rowan controls for ordinary, Realm/Band path-tail, Slash-group,
  recursive group/exclusion and nonterminal cases, with source reconstruction
  and fresh/frozen equality. Corrected earlier lower-authority expectations
  that had placed terminal ColonColon inside UsePath.
- M2 pre-write specification, authority, post-write specification and
  compiler/recovery reviews were clean. Use module 40/40, `cargo check -p
  yu-syntax`, scoped formatting and diff checks passed; package check retained
  three existing warnings. Benchmark use was zero.

## UseTree full composition

- Added two bounded direct-Rowan aggregate matrices covering ordered accepted
  children across initial group/operator, Mod, Realm/Band markers, ordinary
  path and terminal group/glob routes, suffix composition and recursion.
- Recovery remains delegated to the selected component. The second matrix proves
  component-owned recovery and protected semicolon handoff without any direct
  UseTree Missing, Error, Invalid, wrapper or diagnostic role.
- M2 pre-write, post-write specification and regression reviews were clean.
  Focused 2/2, Use module 42/42, `cargo check -p yu-syntax`, scoped formatting
  and diff checks passed; package check retained three existing warnings.
  Benchmark use was zero.

## UsePath required-segment frame balance

- Repaired the two owner-local required-segment Err exits in `parse_path_tail`
  and `parse_marker_target`: each closes its open UsePath before returning the
  existing boundary. This restores a public Root green tree without changing
  recovery, source order or pending-boundary behavior.
- Added direct Rowan and public fresh/frozen controls for UTF-8 separator EOF,
  Error/retry, protected newline/close, marker absence and accepted targets.
- M1 pre-write authority and post-write compiler/recovery reviews were clean.
  Focused rows, Use module 44/44, scoped format/diff passed; benchmark use zero.

## Mod required-word frame balance

- Repaired `parse_mod_target` to close its owned UsePath before propagating an
  unchanged required-word failure. EOF, semicolon and newline recovery retain
  their existing Item and diagnostic ownership while public Root stays balanced.
- Added public/fresh/frozen topology and continuation controls. M1 pre-write
  and compiler/recovery reviews were clean; Use module 45/45 passed.

## UseGlob post-comma newline

- Restored `Comma G* UseExclusion` admission for LF, CRLF and comment trivia.
  Leading is direct UseGlob content; protected boundaries and pre-comma/newline
  separation remain unchanged. M1 review clean; Use module 47/47 passed.

<!-- daily-append-anchor: 2026-09-12 -->
