# 2026-06-21

## Public regression fixtures

- list update playground smoke は `case_02.rs` の inline source として既にあった。
- 将来の Yulang test runner と共有できるように、入力を `tests/yulang/regressions/runtime/list_update.yu` へ移した。
- 既存の playground std run smoke と typed playground std prefix comparison は、同じ fixture を読む形へ変更した。
- 完全実行系の public regression は、内部 helper ではなく `yulang` CLI 起動を第一経路にする方針へ切り替えた。
- list update fixture を `crates/yulang/tests/cli.rs` から `--std-root lib run --print-roots` で実行し、
  isolated `YULANG_CACHE_DIR` に poly / control artifact が出ることまで見る test を追加した。
- 確認:
  - `cargo test -q -p yulang run_control_source_text_with_embedded_playground_std_runs_list_update_example -- --test-threads=1`
  - `cargo test -q -p yulang typed_playground_std_prefix_matches_loaded_route_for_list_update -- --test-threads=1`
  - `cargo test -q -p yulang --test cli public_regression_list_update_runs_through_cli_cache -- --test-threads=1`
  - `cargo fmt --check`
  - `timeout 120s cargo test -q -p yulang --test cli -- --test-threads=1`
  - `timeout 240s cargo test -q -p yulang -- --test-threads=1`
