# 2026-09-26

## F5c solve-wide checked logical-work meter

Added a session-owned checked work meter for the current F5c producer, memo,
analysis, materialization, replay, and substitution paths. Its total is
solve-wide and monotonic: a failed component's attempted operations remain
charged, while the component's published memo state still follows its separate
transactional rollback. Checked arithmetic exhaustion returns
`IdentityExhausted`; no numeric work cap was selected. This is logical
repeat-work admission, not a bound on depth, allocations, capacity, or elapsed
time.

Charges are placed before boxed Function output construction and before the
five typed finish/drain families mutate their source lanes. First-seen owner
registration accounts for lookup, set insertion, and order append separately;
revisits count the lookup. Overflow tests cover both polarities across summary
and raw materialization, row/generalizer exit, replay, and substitution, plus
owner-order and output-construction boundaries. They assert no operation past
the rejected charge and successful retry.

M2 compiler-referee, spec-auditor, and performance-auditor delta findings were
repaired and converged with no open blocker. The performance review is static:
it identifies constant-factor per-operation meter/lookup overhead but makes
no successful-path timing claim. No benchmark or §15 resource probe ran;
measurement budget remains zero. `lib.rs` contains only session wiring and
test registration; the implementation remains in F5c modules.

Verification so far: work-meter filter (15 passed), F5c filter (207 passed,
1 ignored), `cargo check -p yu-solver --lib` without warnings,
`cargo fmt --check`, and `git diff --check`. The single-threaded
no-default-feature `yu-solver` library suite passed (292 passed, 1 ignored;
952.91 seconds). Next: commit and push this coherent logical-meter subgate,
then continue the uncalled flat sink. Physical capacity/peak accounting, §15
plan and measurements, production cutover, indexed finalization, §44 per-use
rollback, F5e, and overall F5c closure remain open.

## F5c shared producer approval and owner relocation

The user approved the reviewed shared-walker/flat-sink architecture for staged
internal implementation. The first slice moved the existing boxed generalizer,
component expansion memo/transaction, task walker, and `build_inner` into
`f5c_generalization.rs`; `lib.rs` retains component invocation and outer
orchestration. Behavior, production callers, and the boxed route remain
unchanged; the flat sink is still uncalled.

M2 specification review found a minor overbroad visibility surface. The primary
made unused epoch/checkpoint/walker-state fields private; `active_set` stays
crate-internal because the materialization sibling and existing tests consume
it. Regression review found no issue. Verification: `cargo fmt --check`,
`cargo check -p yu-solver --tests`,
`cargo test -p yu-solver --lib f5c_ -- --test-threads=1` (183 passed, 1
ignored), and `git diff --check` pass. No workspace-wide suite, benchmark, or
resource probe ran. Measurement budget remains zero.

Next: close the producer memo transaction/active-state rollback gaps with
focused failure/interleaving witnesses before adding the shared flat sink.
Production cutover, resource limits, §15 probes, indexed finalization, F5e,
§44 rollback, and overall F5c/F5e acceptance remain open.

## F5c memo rollback and active-state gate

Closed the next approved gate in `f5c_generalization.rs`. One chronological
root undo log now reverses interleaved admissions and invalidations in reverse
publication order. Failure cleanup restores persistent roots/edges and returns
transient active rows, conflicts, work, visit marks, and local mirrors to idle
before truncating appended memo nodes. `push_children` now returns a failed
fallible reservation before extending the lane; retained physical capacity and
its growth charge remain observable. Peak accounting samples all simultaneously
live memo/generalizer lanes only when capacity changes, with an independent
test ledger folding capacity snapshots.

Failure witnesses cover warm Shared lookup after rollback, nonzero entry marks,
retry, interleaved root undo, and reserve failure without a partial child
append. New focused tests are in
`crates/yu-solver/src/tests/f5c_generalization_transactions.rs`; `lib.rs`
retains outer resource-ledger changes and transaction test wiring/updates.

M2 compiler-referee and performance-auditor delta review found no blocking or
major issue. Verification: `cargo fmt --check`;
`cargo test -p yu-solver --lib f5c_ -- --test-threads=1` (192 passed, 1
ignored); and the single-threaded no-default-feature `yu-solver` library suite
(277 passed, 1 ignored; 717.77 seconds). `git diff --check` passes. No
benchmark or resource probe ran; measurement budget is zero.

Next: implement the uncalled flat candidate sink through the shared producer
walker. Production remains boxed. §15 probes, production cutover, indexed
finalization, remaining §44 route rollback, F5e, and overall F5c/F5e
acceptance remain open.

## F5c fixture-only flat binder substitution candidate

Added a non-production `FlatDraft` binder-substitution helper in
`crates/yu-solver/src/f5c_binder_substitution.rs` with focused coverage in
`crates/yu-solver/src/tests/f5c_binder_substitution.rs`. It walks only the
predicate and retained lower/upper bound roots using an explicit stack,
preflights all reachable Variables, then mutates the reachable nodes in place.
The existing boxed implementation remains the oracle and production route.

M2 compiler/performance review found and closed candidate-local issues: root
scope rather than all stored nodes, boundary-only positive/negative parity,
duplicate scheduling, avoidable root/position scratch, and checked ID
conversion. The preflight preserves failure atomicity for an unmapped
reachable Variable. No `lib.rs` change was made.

One integration blocker remains before producer use: unreachable raw Variable
scratch intentionally remains untouched, but `normalize_flat` rejects every
Variable in its full input arrays. The next step is selected-root isolation or
compaction before normalization, with boxed-path normalization-counter parity;
the existing post-normalization compaction remains required. No production
behavior or API changed.

Verification: the single-threaded `f5c_binder_substitution` library filter
passed (6); `cargo fmt --check`, `git diff --check`, and
`cargo check -p yu-solver --message-format short` passed. No broad suite,
benchmark, resource/scale/capacity probe, or §15 plan ran. Measurement budget
consumed: zero. The candidate walk has O(N+E+B) logical work and O(N) scratch;
actual capacities, co-resident peak, repeat-work admission, and a numeric
support boundary remain open under §§5/15. The broader F5c/F5e and §44 gates
remain open.

## F5c selected-root normalization handoff

Made `normalize_flat` composable with `substitute_flat`'s retained orphan
scratch. It starts from the predicate and each bound's lower/upper root, marks
the selected graph in one reverse topological insertion-order pass, and reuses
the two per-polarity source maps for selection state and normalized IDs. The
forward pass still validates every source node and edge against its insertion
prefix, including unreachable scratch; only selected nodes enter ranking and
the five logical normalization counters. The existing post-normalization
compaction remains. `lib.rs` and production call paths are unchanged.

The integration fixture covers a positive Function/Union predicate, positive
and negative Function bounds, orphan compound scratch with Variables, all five
boxed-counter comparisons, and complete flat output/root/bound assertions.
Additional witnesses reject malformed orphan and selected Union/Intersection
edges and both Function polarities. M2 compiler/performance delta review found
no blocking or major finding. The two maps still scale with all raw nodes;
co-resident peak and admission remain §5/§15 work, not certified here. The
selected-root scans are statically O(N+E+B); no benchmark or resource probe
ran.

Verification: `cargo test -p yu-solver --lib f5c_ -- --test-threads=1` passed
(176 passed, 1 ignored); flat tests passed (9); binder-substitution tests
passed (7); `cargo check -p yu-solver --tests --message-format short`,
`cargo fmt --check`, and `git diff --check` passed. A full single-threaded
yu-solver lib suite was stopped after the unrelated F4 4k bounded-cycle
matrix passed and the F4 chain scale matrix began; no prior failure appeared,
but the suite is not complete. The branch candidate remains unconnected to
production. Next: flat replay candidate and boxed parity, then continue the
first §7 producer gate. F5c/F5e closure, resource certification, indexed
finalizer, and §44 rollback remain open.

## F5c fixture-only flat replay candidate

Added `replay_flat` in the replay module, uncalled by production. It reads an
immutable source draft so the same graph can be replayed under changing R
candidate masks. Explicit tasks preserve boxed polarity and traversal order;
shared source edges expand as separate output occurrences. Checked failure
truncates all appended flat node, child, and insertion-order lanes. No `lib.rs`
or production route changed.

Tests compare both polarities directly with boxed replay, cover a duplicate
edge within one root, polarity elimination and Function/Union/Intersection
order, reachable positive↔negative cycle rollback after a completed child
Union, and depth 4,096 on a 64 KiB stack. Rollback asserts the full logical
FlatDraft pre-state, including bounds, predicate and quantifier count.

M2 compiler-referee and performance-auditor delta review found no blocker to
the fixture-only checkpoint. The performance review's prospective production
risks remain open: each replay allocates two active arrays sized to all source
nodes; occurrence-expanded shared DAGs can grow exponentially and are
re-expanded under each mask; current task/value counters omit those arrays
and output-draft growth. These need §5 admission and §15 co-resident peak
evidence before wiring. Truncation also retains allocation capacity. No
resource probe or benchmark ran; measurement budget used: zero.

Verification: `cargo test -p yu-solver --lib f5c_flat_replay --
--test-threads=1` passed (4); `cargo test -p yu-solver --lib f5c_replay --
--test-threads=1` passed (6); `cargo fmt --check` and `git diff --check`
passed. Next: compose replay with flat substitution and selected-root
normalization against the boxed oracle before production wiring.

## F5c composed flat replay/substitution/normalization fixture

Composed flat replay of predicate and both bound endpoints, `substitute_flat`,
and `normalize_flat` in one fixture. The boxed oracle uses the same source,
candidate masks, Q/R maps, and eliminations; final predicate, bound roots,
quantifier count, ordinal, and the five normalization counters match.

The fixture checks repeated source edges have distinct output IDs before
normalization, includes Q row 2 → Q0 and R row 3 → R1, and sends separate
positive/negative Variables through substitution-time elimination. It walks
all retained roots to prove every normalized node/child entry is live, checks
dense IDs and duplicate removal, and confirms canonical R1 reuse across
predicate and bounds.

This exposed a candidate bug in flat normalization: equal `(height, rank)`
nodes received separate output IDs. The rebuild now uses its already allocated
sort scratch as a canonical representative map, remapping equal nodes to one
output ID. Added cost is one O(N) pass and no new allocation. The selected-root
normalization test expectations now assert the shared canonical IDs.

M2 compiler/performance delta review found no actionable findings. Verification:
the `flat_tests` filter passed (10), `f5c_replay` passed (6), and
`f5c_binder_substitution` passed (7), all single-threaded. `cargo check -p
yu-solver --tests --message-format short`, `cargo fmt --check`, and
`git diff --check` passed. No broad suite, benchmark, or resource probe ran;
measurement budget remains zero. Production/resource gates remain open. Next:
compose flat summary materialization into this fixture pipeline, with §15
review before any resource probe.

## F5c summary-to-flat composed fixture

Replaced the composed test's hand-built FlatDraft source with a synthetic
`F5cSummaryNode` DAG materialized through `materialize_summary_flat` for
predicate and both bound roots. Before replay, each flat root is expanded and
compared directly with the summary memo's boxed value, preventing downstream
lossy transforms from concealing root-variable misassociation. Repeated
positive-Union and negative-Intersection edges remain distinct occurrences.

The existing composed checks still pass: full normalized scheme and counters
match the boxed oracle; all output nodes/child entries are reachable; IDs are
dense; duplicates collapse; canonical R1 is shared across roots. M2
spec-auditor review had no finding. Compiler-referee's test-gap finding was
closed by the raw-root parity assertions and a clean focused delta review.

Verification: the focused composed test and `flat_tests` (10) passed;
`cargo check -p yu-solver --tests --message-format short`, `cargo fmt --check`,
and `git diff --check` passed. No broad suite, benchmark, or resource probe
ran. Measurement budget remains zero; production gates remain open. Next:
continue the producer/downstream candidate bridge and check raw roots before
lossy transformations.

## F5c actual producer memo bridge

The composed fixture now builds its memo DAG via `positive_node` and
`negative_node`, preserving repeated `Shared` edges for occurrence expansion.
A separate test drives actual `F5cGeneralizer::positive_row` and
`negative_row` paths on a small pure-Function fixture, materializes their
cached summary roots to flat drafts, and compares with boxed materialization
before replay or substitution. Both full incidence mark sequences are
compared in order; a separate explicit-worklist walk compares shape, polarity,
Function fields/effects, and ordered Union/Intersection members.

This covers cached summary fragments only, not full `build_component` roots,
full scheme finalization, or deep boxed-tree drop. The structural parity helper
is documented for shallow tests. `lib.rs` stayed untouched. M1
compiler-referee findings on incomplete incidence coverage and the helper's
drop caveat were repaired; fresh spec-auditor delta review was clean.

Verification: `cargo test -p yu-solver --lib f5c_materialization:: --
--test-threads=1` passed (8); `cargo fmt --check` and `git diff --check`
passed. No broad library suite, benchmark, or resource probe ran. Measurement
budget remains zero. Next: bridge full producer roots without boxed
`GeneralizationDraft`; keep production unchanged and resource probes behind
the reviewed §15 plan.

## F5c pre-replay producer roots into FlatDraft

Added a shallow guarded-self fixture using real `F5cGeneralizer` pre-replay
work: root predicate, dynamically discovered reentry owners, owner lower/upper
expansion, and the existing Bottom/Top defaults for absent source sides. The
boxed predicate and bounds are re-encoded through the summary memo into one
`FlatDraft`, including predicate and owner-ordered bound roots.

The fixture compares each raw root with both boxed summary materialization and
the expanded boxed producer tree. It checks complete incidence sequences,
polarity, Function fields/effects, and ordered members. A nested positive row
must be admitted, appear as a `Shared` ID exactly once in predicate and lower,
and generate one incidence mark. This remains a boxed-to-summary-to-flat
bridge; it stops before replay/Q/R/normalization/finalization and is not direct
flat construction. `lib.rs` remains untouched.

Compiler-referee review found a minor gap in the nested-summary evidence. The
primary added exact Shared-ID and incidence counts and wired the roots into
FlatDraft. The `f5c_materialization::` library-test filter passed (9), with
`cargo fmt --check` and `git diff --check`. No broad library suite, benchmark,
or resource probe ran. Measurement budget remains zero. An architect
exploration recommends the next slice be an uncalled module-local flat sink
sharing the real producer walk, creating IDs at leaf/exit tasks rather than
copying the walker. The feasibility of sharing private producer state without
moving traversal remains unverified. Next: review that interface at code
level; preserve active-state/memo rollback and do not touch the active
production path before independent M3 review and explicit user approval.

## F5c shared producer-walker design review

Code-level inspection confirmed that the active `F5cGeneralizer::walk`
produces boxed values, so a separate module cannot consume its leaf/exit
decisions as flat IDs without either converting after Box construction or
copying the task interpreter. The latter would create a second producer
source of truth. A Reviewed proposal now uses one shared task interpreter with
the existing boxed compatibility sink and an uncalled tagged Local/Shared
flat candidate sink. It moves the generalizer, summary memo/transaction,
raw-root forest coordination, and `build_inner` ownership into
`f5c_generalization.rs`; `lib.rs` keeps component invocation and outer solver
installation orchestration.

Three independent M3 reviewers (compiler-referee, specification, performance)
found blockers in the first draft. The primary repaired and delta-reviewed:
ordered full raw-root retention/materialization; deterministic first-reentry
owner order with lower-before-upper (the bounds map is lookup-only); an
edge-index chronological memo undo log; idle active-conflict restoration;
pure/invalid effect and complete Q/R/raw-forest witnesses; and explicit
source/memo/draft/scratch accounting categories. All blocking/major findings
closed in the final focused delta round. This is design review, not
implementation certification: rollback, deep flat drop, boxed hot-path cost,
and §15 physical/resource measurements still need implementation evidence.

No code or tests ran. `git diff --check` passed; benchmark/resource-probe
budget used remains zero. The design is not yet Authoritative: explicit user
approval is required before changing the active internal producer path. §44,
indexed finalization, F5e, and F5c/F5e closure remain open.

# F5c explicit raw owner ordering

The boxed producer's raw reentry-bound forest now carries a dedicated
first-encounter owner sequence. Recursive-bound materialization and the
pre-pruning incidence census traverse that sequence, with lower before upper;
the map remains a lookup table. The new sequence's work and retained capacity
are charged through the checked work meter and generalizer scratch accounting.

Focused ordering and injected-reserve-failure witnesses pass, including
rollback/retry. An independent `spec_auditor` review and repair delta review
found no remaining issue. The helper-level test covers map insertion-order
independence; full producer callback/Q/R coverage with warm Shared and a later
bound remains part of the candidate raw-forest gate. No broad suite, benchmark,
or §15 resource probe ran. The next step is extracting a boxed sink behind the
shared task interpreter, with the boxed route remaining production.

## F5c boxed sink extraction

The boxed producer operations now live behind `F5cBoxedWalkSink`, while the
single generic `walk_with<S: F5cWalkSink>` interpreter owns task scheduling
and row state. Existing wrappers and production callers remain on the boxed
sink. Generic value-lane accounting records the active value type's slot size.
M2 spec and performance reviews found no confirmed defect in this intermediate
extraction. Focused checks passed: formatting, library check, generalization
filter (17), and F5c filter (209 passed, 1 ignored).

There is still no flat sink or flat raw-root forest. Optimized code size and
timing remain unmeasured pending the reviewed §15 plan. Next is the uncalled
tagged flat sink and complete ordered-forest/parity gate; production cutover
and resource probes remain unauthorized.

## F5c fallible sink constructors

The five `F5cWalkSink` value constructors now return `Result`, with errors
propagated through the shared walker's existing cleanup and component rollback.
The boxed sink still returns the same values and preserves task/work order.
The M1 compiler-referee delta review found no issue. Formatting, solver library
check, and diff checks passed. No test was added because only the future flat
sink can exercise constructor allocation failure; its rollback witnesses
remain open.

Next: build the checked tagged source arena and promotion/materialization
bridge, then connect it through the shared walker and ordered root forest.

## F5c sink construction context

The value constructors on `F5cWalkSink` now receive mutable generalizer
context, allowing candidate sinks to charge source creation and reserve their
own lanes. The boxed implementation ignores it; cacheability remains
value-only. The M1 spec-auditor delta review found no issue. Solver library
check and diff check passed. No flat arena was added; the candidate must still
respect sink ownership and transaction rollback boundaries.

Next: create the checked tagged source arena and its explicit accounting lanes.

## F5c tagged flat source arena substrate

Added a non-production polarity-specific arena for Local and Shared refs,
scalar/Function nodes, and ordered tagged child spans. Its four flat lanes
reserve fallibly before publication, charge checked work, and participate in
retained/simultaneous memo accounting. Rollback truncates lengths; release
drops lanes. Independent spec and performance reviews found no blocker.
Focused tests (4), solver library check, formatting, and diff checks passed.

The arena is not wired into the walker. Sink semantics, structural
comparison/dedup, promotion, full raw forest, component integration, and
source/memo/draft co-resident evidence remain open. Next: implement Local/
Shared sink behavior on this substrate without production cutover.

## F5c candidate flat sink over shared walker

Added the test-only `F5cFlatWalkSink` through the same task interpreter as the
boxed route. The generalizer owns the arena across successive walks; early
release/commit before full-forest materialization is no longer exposed. The
sink implements tagged Local/Shared equality, first-seen ordered dedup,
cacheability, Function construction, and memo promotion. Failure rollback
restores memo/source state and component result counters while keeping
solve-wide work monotonic. Promotion peak evidence now snapshots memo, source,
and both worklists at the same observation point; reverse-parent construction
uses O(1) extra space.

Compiler-referee and performance delta reviews converged after repairs.
Focused checks passed: sink filter (13), solver tests compile, fmt, and diff
checks. No benchmark or §15 probe ran. The full ordered raw forest, flat root
materialization, callback/Q/R parity, FlatDraft co-residency, and remaining §5
witnesses remain open; production remains boxed.

## F5c checked Shared-to-FlatDraft materializer

Added a candidate-only iterative occurrence materializer for Shared memo roots.
Its incidence callback is fallible, and errors restore all six FlatDraft
lengths. Repeated Shared refs become distinct occurrences and preserve callback
order. Exact-size lanes now track flat tasks/values and every FlatDraft Vec;
pre-reserve plus publication is witnessed not to grow twice. A same-time
snapshot covers live source, memo, draft, and scratch capacities. Union and
Intersection work is charged before drain, with both-polarity overflow and
retry witnesses. Independent spec/performance delta reviews found no blocker.

Focused materialization tests (17), flat sink tests (14), solver test compile,
co-resident sample, fmt, and diff checks passed. No benchmark or §15 probe ran.
Local source materialization, complete ordered raw forest, Q/R, whole-component
rollback, and final lifecycle remain open.

## F5c checked Local/Shared batch materializer

Added iterative caller-ordered materialization from the retained flat source
arena to FlatDraft. Both polarities preserve Function and child ordering;
Shared roots use checked occurrence expansion. Batch errors restore all six
draft lengths and output length, while source nodes and monotonic work charges
remain. Task, value, and output-root capacity lanes are tracked; preexisting
output capacity is observed even for empty input and can be released only after
the caller drops the output vector.

M2 spec/performance delta audits closed after the output lifecycle repair.
Focused materialization tests (3), solver test-target check, formatting, and
diff checks passed. No benchmark or §15 probe ran. The full ordered root forest,
callback/Q/R parity, whole-component rollback, and production cutover remain
open.

## F5c test-only ordered raw forest

Added a candidate raw-forest path that retains the predicate and each unique
reentry owner's lower/upper roots in producer encounter order, applies the
existing empty-side defaults, rejects invalid effects before one batch
materialization, and returns source owner IDs separate from final binder IDs.
Tracked lanes cover root/order/map/seen/callback storage. The checked Shared
callback can reserve its trace lane against the current memo byte count, so
growth is observed alongside live source, draft, and traversal scratch.

M2 spec-auditor and compiler-referee reviews closed after repairing committed
memo checkpoint reuse and preflighting owner-table counters before allocation.
The flat sink filter passed 19 tests, materialization passed 17, solver tests
compiled, and fmt/diff checks passed. No benchmark or §15 probe ran. Complete
callback/Q/R parity, downstream component rollback, indexed FlatDraft analysis,
and the remaining §5 witnesses remain open. Production stays boxed.

## F5c candidate non-test compilation cleanup

A non-test solver library check exposed two candidate-only type paths that
were available only through test-gated re-exports. The raw-root value slot now
uses the flat sink's defining module, and the checked summary materializer
imports its resource owner directly. This preserves the candidate's private
scope and restores non-test compilation.

`RUSTC_WRAPPER= cargo check -p yu-solver --lib --message-format short` passes.
This is a separate compile-fix slice; the flat producer remains test-only and
production remains boxed.

## F5c FlatDraft tree-analysis adapter

The explicit-stack F5c analysis walker now accepts FlatDraft IDs through its
existing event scheduler. Positive/negative repeated child references retain
each occurrence, Function edges set the same guard state, and early exits
produce the same trace as equivalent boxed trees. Flat-only IDs and spans use
checked access; failure clears tasks and supports a valid retry. Repeated-edge
work charges match the boxed traversal.

The M1 specification review found two major test gaps; the repair added full
ordered event traces, invalid-root/child/span and retry cases, and exact work
charge parity. The focused delta review is clean. The 4,096-depth small-stack
test passed after moving a large `ConstraintStore` capture to the heap before
the 64 KiB worker starts. Focused trace and stack tests, solver test-target
check, formatting, and diff checks passed. No broad suite or resource probe
ran. The full Q/R orchestration and downstream component rollback remain next;
production is boxed.

## F5c raw-forest memo transaction precursor

The test-only raw-forest builder no longer commits its component memo
transaction when it returns the candidate forest. `release_raw_forest` commits
the root log and advances memo checkpoints; `abort_raw_forest` drops the
candidate lanes and rolls back roots and appended memo state. Rollback failure
is recorded at the shared abort owner, and both candidate entrypoints reject
reuse after any failed rollback.

The warm-root witness invalidates a retained key, lets raw-forest construction
re-admit it, aborts, compares roots/root heads/edges/marks/undo and
nodes/children/parent heads/reverse parents/incidence heads/incidences, checks
idle transient state, then retries the same forest. Construction-error and
explicit-abort corruption witnesses cover poison behavior. M2
compiler-referee/spec-auditor delta reviews found no remaining issue in this
precursor.

Eight focused raw-forest tests, `cargo check -p yu-solver --lib`,
`cargo check -p yu-solver --tests`, `cargo fmt --check`, and `git diff --check`
passed. No broad suite, benchmark, or §15 resource probe ran. This does not
prove rollback after actual Q/R, replay, substitution, or normalization: those
stages are not yet connected to the FlatDraft candidate. Full callback/Q/R
parity and later-stage rollback remain the next shared-core gate; production
stays boxed.

## F5c shared raw-incidence census slice

Boxed `build_inner` and the test-only FlatDraft raw forest now call one
predicate-first incidence census through the existing Walker scheduler. It
visits raw owners in their declared order and lower before upper, with the
raw-bound-owner logical-work charge at its prior site. A paired exact-set test
covers predicate contributions, two owners, positive and negative roots, and
Function guard edges. FlatDraft cannot encode a Shared node directly; the
existing warm Shared callback fixture remains separate.

M3 compiler-referee/spec-auditor/performance reviews found no blocking or major
issue. The minor evidence gap closed with the exact-set parity witness and
primary diff review. Nine focused raw-forest tests, library and test-target
checks, `cargo fmt --check`, and `git diff --check` passed. No benchmark or §15
resource probe ran; measurement budget remains zero. Only the census is shared:
R fixed-point filtering, Q/R assignment, flat replay/substitution/normalization,
complete callback/Q/R parity, and actual later-stage rollback remain open.

## F5c shared R fixed-point slice

Boxed production and the test-only FlatDraft adapter now share one R-candidate
fixed-point loop for eligibility, guarded-bound replay, trace pruning,
predicate replay, raw-bound reachability, and convergence. The boxed path keeps
the existing order and work charges. A test-only raw-forest owner holds the
component memo transaction through R; an error after a replay emitted output
aborts the transaction, restores persistent memo state, checks idle scratch
and monotonic work, then succeeds on a warm lookup retry.

Flat replay charges active-array initialization before allocation and tracks
both active arrays plus five retained output lanes in co-resident accounting.
Temporary storage is dropped before its lane ledger is released, including at
direct replay fixture call sites. M3 compiler-referee/spec-auditor/performance
delta reviews found no blocker or major finding. Minor evidence/lifecycle
points closed with full output-lane release assertions and error-path drop
ordering.

Focused flat-walker (25), replay (6), generalization-transaction (11), and
composed replay/substitution/normalization (1) tests passed. Solver library and
test-target checks, formatting, and diff checks passed. No broad suite,
measurement, or §15 probe ran; measurement count is zero. Only the R fixed
point is shared. Post-convergence retained-R assembly, Q/R ordinal assignment,
substitution/normalization integration, full callback/Q/R parity, and rollback
through those later stages remain open. Production remains boxed.

## F5c shared post-convergence R/Q selector

The boxed producer and test-only FlatDraft adapter now share post-convergence
retained-bound replay, guard filtering, surviving trace selection, recursive
owner ordering, retained occurrence traversal, and Q/R ordinal assignment.
Retained bounds replay in raw-owner order; R follows first surviving trace
order with duplicate owners removed; Q follows predicate occurrences then each
R owner's lower and upper bound occurrences. Output ordinals never depend on
hash iteration.

The candidate uses checked fallible growth and separate capacity lanes for
retained bounds, survivor sets, recursive owner order/set, occurrence order/
seen set, and Q/R maps. Temporary lanes release after the owning collection
drops, and the candidate wrapper retains selected lanes with replay output and
the open raw forest until release or abort. A paired fixture checks raw order
`[1,2]` against trace order `[2,1,2]`, Q order across the predicate and both
lower/upper roots, R offsets, and missing-owner rejection. A late injected
failure after post-R replay output checks complete memo restoration, lane
release, idle scratch, and warm retry.

M3 compiler-referee/spec-auditor/performance-auditor delta reviews closed with
no actionable findings. The `post_r_` filter passed 4 tests, the
`f5c_tree_analysis` filter passed 4, and solver test-target compilation,
formatting, and diff checks passed. No broad suite, benchmark, resource probe,
or §15 measurement ran; measurement budget remains zero. Substitution and
normalization integration, full callback/Q/R parity, complete later-stage
rollback, §5 resource certification, and the reviewed §15 measurement gate
remain open. Production stays boxed.

## F5c selected FlatDraft substitution and normalization

The test-only candidate now follows the shared R/Q selector with flat binder
substitution and selected-root normalization. Recursive bounds follow R-owner
order; substitution applies R, Q, then polarity-specific elimination. A paired
fixture matches boxed structure, Q/R ordinals, and all five normalization
counters, and checks that orphan Variable scratch is omitted.

The memo transaction remains open through normalization and final capacity
observation. A late failure restores memo state, releases transient lanes, and
permits a warm retry. Successful normalized output stays accounted until
explicit release.

Requested-slot accounting covers 27 observer lanes: 13 normalizer lanes,
eight additional scratch lanes, and six emitted-output lanes. The aggregate
includes them; publication observes the same output vectors' retained capacity
without counting their requests twice. M2 compiler-referee review found no
semantic or rollback issue. The performance requested-slot gap and duplicate
publication count are closed by the implementation repair and primary diff
review.

Checks passed: RUSTC_WRAPPER= cargo test -p yu-solver --lib selected_flat_ --
--test-threads=1 (5); RUSTC_WRAPPER= cargo test -p yu-solver --lib flat_tests
-- --test-threads=1 (15); RUSTC_WRAPPER= cargo check -p yu-solver --tests
--message-format short; cargo fmt --all --check; git diff --check. No broad
suite, benchmark, §15 probe, or resource measurement ran. Full callback/Q/R
parity, remaining physical resource reconciliation, the reviewed §15 plan,
indexed finalization, and production cutover remain open; production stays
boxed.

<!-- daily-append-anchor: 2026-09-26 -->
