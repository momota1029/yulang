# 2026-08-31

## Declaration companion Gate 3 isolated form closure

Gate 3 implemented the production-unreachable declaration-companion form boundary around the Gate
2 statement-only sequence adapters. The isolated AST/direct entries recognize exact contextual
`with`, construct dedicated colon-inline, colon-indented, and empty/nonempty braced forms, emit the
dedicated companion CST nodes, own companion-specific inline/indent/brace scopes, and implement the
DC-R introducer/body/item/separator/closing-delimiter recovery matrix. No declaration owner calls
these entries yet; companion Derives priority and every Gate 4–10 production attachment remain
absent.

Independent review found and closed two cause-level recovery defects. Colon first-slot ownership now
uses a companion-owned neutral judge for EOF, visible ambient/If ownership, comma, semicolon, and all
fixed closes before creating an AST indented body or opening its CST node. Malformed introducer
recovery consumes comments atomically, so comment-internal starters, closes, newlines, and Statement
candidates cannot escape. The consolidated focused table pins exact AST/direct recovery roles,
ranges, order, remainder, losslessness, direct-child CST topology, and fully seeded ParseLocal state
for normal, Missing, Error, retry, retained boundary, repeated/trailing separator, mismatched/missing
close, comment, and nested Statement rows.

The focused evidence also exposed a pre-existing generic candidate defect:
`expression_nud_candidate_input` restored input and ParseLocal after speculative NUD recognition but
not the separate Chasa ErrorSink checkpoint. Under real braced `ml_arg(false)` state, a successful
`with { first }tail` therefore leaked `Unexpected('}')` at the companion-owned close. The existing
Gate 2 zero-added-work authority did not permit the owning generic repair, so architecture re-entry
stopped Gate 3. The user selected option 1 on 2026-08-31, recorded as Authoritative in
`notes/design/2026-08-31-declaration-companion-gate3-nud-sink-amendment.md` at `42d0ca1e`.

The approved production fix is exactly two lines: one ErrorSink checkpoint before the existing
speculative `recognize_nud` and one rollback after the existing input/local rollback. All five
existing call sites remain unchanged. Static performance audit records exactly two bounded sink
transaction operations per existing call, with no new call, branch, recognizer pass, rescan,
allocation, cache, replay, dispatch, or companion runtime mode. The six Gate 2 protected bodies and
their remaining operation ledger stay unchanged; optional timing was unnecessary.

Verification stayed bounded:

- the single consolidated
  `gate3_isolated_companion_form_recovery_and_state_table` passed with 571 tests filtered;
- scoped rustfmt and `git diff --check` passed;
- the final `cargo test -p yu-syntax` ran exactly once: 571 passed, 0 failed, 1 ignored; doc-tests 0;
- no workspace suite, repeated package suite, or timing process ran.

Independent compiler review approved sink transaction semantics, nested committed expectation
preservation, DC-R/state/CST ownership, and Gate 4+ exclusion. Independent performance review
approved the exact call-site/operation ledger statically. Gate 3 is complete. Gate 4 is next and
owns companion-only Derives item priority while standalone Statement grammar remains unchanged.

## Declaration companion Gate 4 Derives item closure

Gate 4 added exact companion-only `derives` priority to the isolated declaration-companion item
adapters. The shared Derives driver now separates its existing attachment context from a neutral
declaration-companion layout context. Attachment owner, Header/Trailing position, owner base, and
owner-tail classifier remain attachment-only; companion clauses never fabricate that metadata.
Exact maximal `derives` is tested before Canonical Statement only in a committed companion item
slot. Adjacent clauses without a companion sequence separator form one
`DeclarationCompanionItem::Derives(Vec<DerivesClause>)`; direct CST streams sibling
`DerivesClause` nodes directly under the companion body without a `Statement` or attachment
wrapper.

The shared RoleReference/Via core retains its existing typed recovery and depth-fenced nested
TypeExpression ownership. Companion layout judges decide sequence newline, semicolon, close,
dedent, and ambient boundaries, while comma remains clause-local. Review caught and closed two
ownership errors before integration: mandatory RoleReference/Via slots now consume same-line
trivia before a retained boundary and recover at the boundary byte, and the new ambient early
return is restricted to the companion context so all five attachment owners preserve their
historical trivia behavior. Repeated clauses after `via`, and adjacent runs in inline, indented,
and braced layouts, are pinned in the same table.

Verification remained deliberately bounded:

- `cargo test -p yu-syntax gate4_companion_derives_priority_recovery_and_layout_table`: 1 passed,
  0 failed, 572 filtered;
- scoped rustfmt and `git diff --check` passed;
- no package/workspace suite, timing run, fixture rewrite, snapshot update, or diagnostic
  expectation change ran.

Independent compiler, spec, and regression audits approved recovery ownership, DC-G/J/T/R and
Gate 4 conformance, existing owner/Statement behavior, direct CST topology, and Gate 5 exclusion.
Gate 4 is complete. Gate 5 typed episode handoffs are next; no declaration owner calls the
isolated companion entry yet.

## Declaration companion Gate 5 exceptional final repair authorization

Gate 5's initial M2 implementation and its bundled isolation repair established the intended
production-zero-work boundary: existing Derives, Act, Type, and variant production adapters remain
unchanged, while the new typed handoff entries are isolated and test-only. A focused Gate 5 table
then exposed one residual companion-only recovery defect. After an adjacent `@with` retry in a
variant positional or `from` payload, the isolated sequence loop sees the retained `:` and appends
a second incomplete variant instead of terminating the completed recovered variant. The same
cause applies to AST and direct CST and is not a new grammar or owner-attachment decision.

The M2 review/repair budget was otherwise exhausted. On 2026-08-31 the user explicitly authorized
one exceptional additional repair round, limited to carrying a companion-only typed
item-continuation fact from the recovered payload back to the isolated variant sequence driver.
The repair must preserve local `@with` retry, retain the `:` boundary, keep one variant with its
single Type Primary Error, and add no owner wiring, companion parsing, generic `WithBodyTail`, or
production-path work. It uses only the existing focused Gate 5 table plus scoped format/diff checks;
no timing or workspace suite is authorized. Existing compiler and performance reviewers perform a
delta review after the repair. The one package-level check remains deferred until Gate 5 actually
closes.

The continuation repair removed the second-variant failure, but the same focused table then failed
only because its newly introduced `A @with:` TypeExpression assertion expected `2..7`. The malformed
`@` is already the separately asserted Type Primary Error at `2..3`; the retried `with` TypeExpression
therefore appears to require `3..7`. On 2026-08-31 the user authorized one final, expectation-only
exception: an exact spec audit before changing that range and one rerun of the same focused table.
This exception resolves no new semantic decision. If the audit finds any disagreement beyond the
single retry-range contract, work stops for architecture re-entry rather than accumulating ad hoc
patches.

The pre-write spec audit confirmed that the retry TypeExpression begins after its separately owned
malformed byte: `A @with:` is `3..7`, not `2..7`. It also found the paired `A from @with:` assertion
through the same helper, where the independently asserted `@` Error is `7..8` and the retried Type
is therefore `8..12`, not `7..12`. On 2026-08-31 the user explicitly included this paired assertion
in the same expectation-only exception. No parser code, recovery/CST contract, or test outside the
single focused Gate 5 table is authorized to change.

## Declaration companion Gate 5 typed episode-handoff closure

Gate 5 is complete. It adds only production-unreachable typed handoff seams: eligible Derives
attachment positions yield owner/position metadata; Act exposes post-Head and post-Source tails;
Type Equality exposes its tail; and separate Enum/Error equals-inline paths yield neutral owner
identity. Existing declaration owner adapters still do not call the companion-form parser or fill a
companion field; Gate 6 owns the first Type wiring.

The isolated TypeExpression recovery seam distinguishes adjacent `@with` from a qualifying
`@ with` gap. The former owns one Type Primary Error and retries `with` locally; the latter leaves
the gap and word for the typed tail. In the companion-only variant loop, the recovered positional
or `from` payload now returns a one-shot item-continuation fact. The loop consumes it after exactly
one completed variant and returns the retained `:` to its owner instead of creating a second
variant/Error. The paired retry ranges remain separate from their malformed-byte recoveries:
`A @with:` is Error `2..3` plus Type `3..7`, while `A from @with:` is Error `7..8` plus Type `8..12`.

The normal M2 repair budget exposed this recovery path, so the user explicitly authorized the two
bounded exceptions recorded above: one companion-only continuation repair and one pre-write-spec-
audited paired expectation correction. Both were limited to the same focused table. The compiler
delta review approved AST/direct recovery, range, remainder, nested-suspension, and Gate 6+
isolation; the performance delta review confirmed that the new context state has no production edge,
branch, rescan, checkpoint, allocation, or runtime mode. No broader architectural mismatch was
found, so architecture re-entry was not needed.

Verification remained bounded:

- `cargo test -p yu-syntax gate5_typed_companion_handoffs_are_isolated_and_state_neutral`: 1 passed,
  0 failed, 573 filtered;
- scoped `rustfmt --edition 2024 --check` and `git diff --check`: passed;
- one final, captured `cargo test -p yu-syntax`: 573 passed, 0 failed, 1 ignored; doc-tests 0;
- no workspace suite or timing run.

Gate 6 Type-owner integration is next. It alone may wire Type Header/Equality handoffs into the
owner adapter while preserving AttachedImpl priority and the fixed RHS/Derives recovery rows.

<!-- daily-append-anchor: 2026-08-31 -->

## Declaration companion Gate 7 Struct-owner closure

Gate 7 wires only Struct Header and actual-complete named-brace/tuple trailing positions into the production Struct AST and direct-CST adapters. At Header, typed Header derives handoff or exact companion evidence creates `StructBody::CompanionIntroduced` and the singular companion field. Otherwise the existing body-introducer path remains in charge: bare EOF/boundary still owns its BodyIntroducer recovery, and a literal semicolon remains the only Bodyless form.

Trailing derives and companion handoff are gated by a completed matching `}` or `)` from Struct's own named-brace or tuple body. Named-indented, Bodyless, missing-close, and mismatched-close forms cannot reach that branch. Predecessor derives recovery remains owned by the typed Gate 5 driver; the unchanged isolated companion parser owns all companion recovery and direct item topology.

Canonical Statements inside a now-reachable Struct companion receive the caller's existing operator table. The added Struct operator-aware AST/direct entrypoints retain the former empty-table wrappers for tableless callers; only the direct root and canonical Statement call sites that already have a table pass it through.

M1 verification remained focused:

- captured `cargo test -p yu-syntax gate7_struct_owner_wires_header_and_actual_close_companions`: 1 passed, 0 failed, 575 filtered;
- scoped `rustfmt --edition 2024 --check` and `git diff --check`: passed;
- no package/workspace suite or timing run.

The focused table covers Header/header-derives, braced/tuple actual-close trailing and derives handoff, bodyless/indented/bare/missing-close/mismatched-close rejection, exact recovery/remainder, lossless direct prefix, one-record/one-node parity, and direct owner-child topology. Independent compiler delta review approved the owner ordering, actual-close condition, recovery ownership, operator propagation, and Gate 8+ exclusion. Gate 7 is complete; Gate 8 Enum/Error owner integration is next.

## Declaration companion Gate 6 Type-owner closure

Gate 6 wires only Type Header and Equality companion positions into the production Type AST and
direct-CST adapters. Header derives now yields the typed Gate 5 tail when applicable, but the
unchanged post-header classifier still gives AttachedImpl priority before a companion is accepted.
Equality consumes its mandatory RHS, then its existing trailing derives run, and only then accepts a
typed companion tail. Both AST and direct paths invoke the existing isolated companion adapter,
populate the singular Type companion field or emit one direct `DeclarationCompanion` child while the
Type declaration node is open.

The scoped Type RHS handoff wrapper fences `With` and `Derives` only for its outer episode; nested
TypeExpression episodes continue to suspend them. Missing RHS and missing Derives RoleReference
remain predecessor-owned before the companion handoff, with no added cascade. No other declaration
owner, generic With behavior, SyntaxKind, public dispatch, or loop was changed.

M1 verification remained focused:

- `cargo test -p yu-syntax gate6_type_owner_wires_header_and_equality_companions`: 1 passed, 0
  failed, 574 filtered;
- scoped `rustfmt --edition 2024 --check` and `git diff --check`: passed;
- no package/workspace suite or timing run.

The focused table initially exposed a prefix-CST harness mismatch and then an old Type Primary label
for the two `derives with` rows. The first was corrected to use prefix output because those cases
retain an outer remainder. A pre-write spec audit established the second as the fixed predecessor
contract: one Missing `Derives(RoleReference)`, not Type Primary. No parser behavior was changed for
that expectation correction. Independent compiler delta review approved Header/Equality ordering,
recovery ownership, AST/direct CST topology, and the absence of Gate 7+ reachability. Gate 6 is
complete; Gate 7 Struct owner integration is next.
