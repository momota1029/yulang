# 2026-09-03

## Recursive-descent rewrite Gate 2 direct expression/tail pilot

Gate 2 is complete under the authoritative expression-tail handoff addendum.
The isolated `rewrite/` pilot replaces the rejected generic execution shell
with direct recursive `expr`/`tail` procedures. Accepted branches publish the
existing flat `OperatorChain`, Rowan CST, and typed recovery directly; the
pilot has no production dispatch edge, legacy fallback, replay, or generic
action/materializer layer.

The pilot retains root-pointer-derived ranges, UTF-8/CRLF logical positions,
EOF-after-trivia, a 23-field `ParseLocal` dependency-cone map, recoverable
expectation/diagnostic state, and committed recovery/CST publication. Its
tail protocol is the approved three-route `TailExit`: normal child completion,
same-item unread handoff, or boundary propagation. Binary, prefix, and ML
argument recovery retain their child level/mode; grouped, ML, and call forms
own nested AST/CST chains. Layout boundary resumption validates its retained
payload cursor and current frame. Close/stop conversion requires a matching
owner capability and lexical evidence.

M2 review had exhausted its repair budget after finding child-control and
boundary-ownership gaps. The user authorized one M3 scoped repair/review.
The first M3 delta review found two further local omissions: released dedent
at EOF still needed `EofAfterTrivia`, and the ML recovery witness observed only
the enclosing tail. The second scoped repair changes only that payload at EOF
and proves the actual ML child exit, cursor, scan identities, and nested chain.
Both compiler/recovery and specification M3 delta reviews approved the final
candidate.

Verification was deliberately focused:

- `cargo test -p yu-syntax rewrite::tests -- --test-threads=1`: 11 passed,
  0 failed, 604 filtered.
- `cargo fmt --package yu-syntax -- --check`: passed.
- `cargo check -p yu-syntax`: passed with existing warnings.
- scoped `git diff --check`: passed.

No package/workspace suite, performance measurement, production dispatch, or
legacy crossing was run. Gate 3 remains next: its E2/E3 fixed-tail and
borrowed-outer-call evidence is not supplied by this direct-expression pilot;
Gate 4's Expression cells and RB-E remain deferred as assigned.

## Recursive-descent rewrite Gate 3 fixed-tail pilot

Gate 3 is complete. The isolated direct pilot adds atomic Field (`.`) and Path
(`::`) current items and emits the existing flat `FixedPostfixTail::Field` /
`Path` AST products and `FieldTail` / `PathTail` CST directly. Field and Path
own their typed Missing/Error recovery locally, including primary `Identifier`,
source order, and same-slot retry. A projection-family dot (`.(`, `.{`, `..`)
is returned unchanged as one Deferred item for Gate 4; it publishes no recovery
or committed output.

The E3 slice is limited to a present outer close with attached leading trivia.
An isolated args adapter accepts an already-owned opener, receives the child
expression's `End(Item)`, and alone converts/emits the retained `)` under
`YmYulangArgs` or `YmInlineApplyArgs`. It covers `\\ref(x. )tail` and
`[d]:f(x. )tail`; an adjacent close is a typed unclaimed outcome, never an
adapter-emitted `RParen`. This is not a Yumark production bridge or a migration
of E3 CallArgument, separator, or missing-close recovery.

M3 review found and closed four ownership/evidence gaps: adjacent fixed tails
must remain in a spaced ML argument while dynamic `+` stays unscanned; only a
leading-trivia close qualifies for this E3 witness; E3 needs caller-prefix
logical positions; and the Deferred-dot rejection must itself prove full
`R`/frame/item/cursor/committed-`S` preservation. Compiler/recovery and
specification review approved the final candidate; the last review was
test-only and checked the Deferred-dot transaction witness.

Focused verification:

- `cargo test -p yu-syntax rewrite:: --no-fail-fast`: 18 passed, 0 failed,
  604 filtered.
- `cargo check -p yu-syntax`: passed with existing warnings.
- scoped rewrite `rustfmt --check` and `git diff --check`: passed.

No package/workspace suite, performance measurement, legacy crossing, Yumark
production change, or public dispatch ran. Gate 4 remains next: projection,
full call sequencing, and the complete Expression/RB-E closure are deferred.

## G4b E5 primary-witness correction

Later on 2026-09-03, the user corrected the E5 primary witness from `x[a(b)]`
to `x[a b]`. The latter must remain one `IndexItem`: `a` receives the already
scanned leading-horizontal-whitespace `b` Item as an ML argument, with no
rescan and no `IndexSeparator` recovery. `x[a(b)]` remains the supplemental
control for `CallTail` ownership of `)` and `IndexTail` ownership of `]`.
The authoritative successor record is
`notes/design/2026-09-03-yu-syntax-g4b-e5-index-ml-application-correction.md`.
The isolated valid witness then completed. `x[a b]` has one IndexItem and one
ML argument, while `x[a b c]` and `x[a b(c) d]` retain sibling ML arguments
rather than nesting the later argument in the child. This required no state,
rescan, or recovery machinery. M1 specification pre-write and semantic
closure reviews were clean after two bounded continuation-propagation repairs.
Focused rewrite tests passed 11 cases; package check, scoped format, and diff
checks passed. Package/workspace suites, performance, production dispatch,
AST parity, Yumark, and the remaining G4b owner/recovery controls were not
run.

## G4b ordinary delimited controls

The isolated source-free rewrite now accepts a parenthesized primary plus the
ordinary empty and comma/semicolon-separated forms of parenthesized groups,
call arguments, and index items. One private `delimited_items` procedure
receives only the closing token and the optional existing `IndexItem` wrapper;
it emits immediately to the caller-owned Rowan builder. A matching close is
emitted, then its owner node is finished, and only then does the enclosing
tail scan its successor. The focused `(a,b;c) d` control proves that the
successor ML argument is outside the `ParenthesizedExpression`; `f(a,b;c)[x,y;z]`
proves the independent call/index close owners; and `()`/`f()`/`x[]` prove the
empty forms. Missing-item/separator/close recovery, mismatched close, newline
layout, comment/other-NUD ML, projection, production promotion, AST parity,
and Yumark remain deferred. Focused rewrite tests passed 14 cases; package
check, format check, and diff check passed.

## G4b ordinary fixed tails

The same isolated rewrite now emits valid field and path tails directly. An
accepted `.` plus an adjacent identifier becomes `FieldTail`; a greedily
scanned `::` plus the following identifier Item (including its owned leading
trivia) becomes `PathTail`. Each owner closes before its enclosing tail scans
again. The focused `a .field:: name b` control fixes the flat
FieldTail/PathTail/MlArgument topology and exact tokens, while `a..` and
`a...` stay outside the field branch. Fixed-tail RHS recovery, projection,
sigil path segments, layout, production promotion, AST parity, Yumark, and
the remaining G4b/Gate 4 controls remain deferred. Focused rewrite tests
passed 16 cases; package check, format check, and diff check passed.
