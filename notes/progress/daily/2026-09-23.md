# 2026-09-23

## F5c per-use failure lane continuation

The closed-DAG incoming traversal and scratch-accounting gate remains closed;
this continuation added missing reserve-failure witnesses to the open per-use
rollback audit. Structured incoming routes now exercise `ExtrusionStack`, and
Bottom incoming routes exercise both `RoutedUses` and `RoutedUsePositions`.
Fresh-row `ValueBounds` and `ValueExactUpper` have direct transaction-owner
witnesses, explicitly not production-route coverage. The initial
`ValueBounds` test injected before `with_route_transaction` and hit the begin
journal reserve; the injection was moved into the transaction closure directly
before `fresh_value_at_level`. A focused specification delta review confirms
that it now reaches the intended reserve. F5 pure Function effects are closed
`EmptyEffect`/`EffectBottom`, so live effect-row mutation lanes are excluded
from the incoming per-use lane matrix.

The full focused command `cargo test -p yu-solver --lib f5c_ --
--test-threads=1` passes 70 tests. `cargo fmt --check` and `git diff --check`
pass. No full `yu-solver` suite, workspace check, benchmark, or F5e scale matrix
was run.

The per-use rollback gate is still open on physical resource accounting. A
local partial fix in `lib.rs` journals and reconciles nested bound Vec
capacities, but independent performance review found stale aggregate
retained/peak metrics and test-ledger state after a failed route; top-level row
table capacity also survives truncation. Exact conformance review determined
that §§26/34 do not explicitly permit a separate post-rollback sample. The
user has an open choice between a narrow conditional sample addendum, strict
sampling with explicit journaled physical-delta reconciliation, or deferring
that resource gate. The exact-alpha versus bounded-normalization decision is
also still awaiting user direction. Continue only independent stack-safety
work until those decisions arrive; do not alter alpha behavior or the sample
boundary by assumption.

Current branch: `yulang3`, `784fa502` equals `origin/yulang3`. No changes are
staged, committed, or pushed. The dirty code and records are preserved for the
next session; see the [F5c handoff](../../handoffs/2026-09-22-f5c-scheme-closure-handoff.md).

## F5c stack-safety accounting adjudication

The first two bounded implementation attempts stopped before editing because
the root-local indexed-walker scratch did not yet have a concrete accounting
owner. A read-only architect review resolved this within current authority:
§§14/26/34 permit private production accounting during F5c, with independent
per-lane test-ledger reconciliation and correct existing aggregate retained/
peak totals. Root-local generalizer/walker scratch must stay separate from the
`component_expansion_memo` family. Public `generalization_scratch_*` accessors
and family certification remain deferred to F5e; this does not alter the
separate pending choice on post-rollback sampling. No code, tests, or design
authority record changed in this adjudication. The stack-safe indexed
row/Term walker, postorder summary construction, and its private accounting
remain open; downstream recursive conversions also remain outside a partial
walker claim.

## F5c iterative walker sub-slice and review

`crates/yu-solver/src/lib.rs` now uses explicit frames for polarity-aware
positive/negative row and Term expansion, postorder summary construction and
materialization, and iterative exact structural comparison. Eleven private
walker lanes account transient task/value/direct-edge/set/comparison/parts and
materialization buffers separately from `component_expansion_memo`. The first
walker reserve is within the cleanup path; bounds are borrowed and direct
target dedup reuses accounted storage. The output Union/Intersection ownership
transfer still needs physical accounting; do not treat this as a closed
resource family.

Focused witnesses include a 1,024-row chain through normal draft construction,
a 2,048-level alternating Function walker/comparator case, same-memo failure
and retry, per-lane checked overflow/release/ledger reconciliation, and
first-seen dedup order in both polarities. `cargo test -p yu-solver --lib
f5c_ -- --test-threads=1` passes 75 tests; `cargo check -p yu-solver --tests`,
`cargo fmt -p yu-solver`, `cargo fmt --check`, and `git diff --check` pass.
No benchmark, full solver suite, workspace check, or F5e scale matrix was run.

Independent M2 delta review: the specification auditor found no remaining
BLOCKING/major issue within the walker slice. The performance auditor found
major residuals for deep Functions in guarded-owner/replay/reference/incidence,
normalization/key and finalizer paths; recursive owned-tree clone/drop; and
Union/Intersection vectors whose capacity is released from the walker lane
after moving into the result tree. It also flagged quadratic repeated
structural dedup. An architect follow-up confirms the next slice should retain
an indexed draft representation through those operations, add exact ordered
structural IDs without changing alpha ordering, and track output capacity
through ownership transfer. Exact-alpha and failed-route sampling user choices
remain pending and unchanged.

## F5c indexed-draft scope check

A read-only architect follow-up confirmed that the full indexed-draft consumer
migration is too broad for one writer pass. A GPT-6 Sol implementer tested the
bounded arena-substrate scope and stopped before edits because there is no safe
production seam: the iterative walker and summary registration still return
boxed trees consumed immediately by `build_inner`. Reconstructing those trees
would retain the stack risk, while shallow-only conversion could reject deep
inputs. A follow-up architect review says a useful production slice must carry
IDs through walk, summary registration, build_inner, and finalization; stopping
at build_inner would reconstruct recursive trees for finalization. A second
GPT-6 Sol writer attempted the narrower iterative-finalizer slice and stopped
without edits because frame/value scratch needs exact simultaneous accounting
with yu-types-owned closed-finalizer storage. F5b accounting amendment §9
requires returning to design for a cross-crate accounting callback, and §6
forbids solver-owned mutation/allocation in the callback; doc-hidden status
does not avoid the exact API contract in §24. Preallocating all traversal state
before the callback remains a candidate but mapping/handle feasibility is
unproven. User approval of an accounting-boundary addendum or deferral of this
gate is required before proceeding on that route. No code/tests/measurements
changed in these attempts. Exact-alpha and failed-route sampling decisions
remain pending.

The initial design fork considered callback-side writes to a preallocated
solver map, a yu-types-owned indexed operation, or deferral. The later M2 and
architect reviews rejected the callback-slot route and found the indexed
operation under-specified; the current decision is recorded in the following
review section. `doc(hidden)` does not avoid the exact API contract.

## Indexed finalizer boundary review

The Candidate B draft was made concrete enough for a fresh M2 review. The
independent specification and performance reviews both found it not ready for
user approval. The primary accepted their blockers, and an architect
adjudication confirmed that current production still creates boxed trees,
normalizes predicate and R bounds separately, and recursively finalizes those
trees; no ID-preserving compact/orphan-free producer is specified.

The current solver baseline is sampled before all indexed inputs and member
drafts coexist, so it cannot be combined as an exact peak with the
yu-types-attached checkpoint. F5 §26 explicitly authorizes an O(1)
all-drafts-coexist sample; the current call path does not take it, and exact
lane lifetimes, growth, release, and per-call refresh remain to be specified.
Do not add a post-failure sample without a separate §26/§34/F5b decision. The
draft's public-surface/array counts and complexity wording were corrected;
producer parity, error boundary, and lane reconciliation remain open.

The stack-safety gate remains open while the bounded architecture repair
continues. This does not approve the proposed §24 API. Exact-alpha ordering and
failed-route physical-resource sampling remain separate user decisions. No
code, tests, measurements, commits, or pushes changed in this review round.

## Indexed producer architecture follow-up

A second bounded architect pass proposed a four-phase ID-preserving pipeline:
import completed root-neutral summaries into a root-local polarity graph; run
guarded R analysis, replay, incidence and eligibility on IDs; preserve current
alpha/permutation ranking, separate predicate/R normalization and first-Q
occurrence paths; then compact from the final predicate plus retained R bounds
in deterministic postorder, remapping typed edges and rebuilding child spans.
The component-summary namespace remains separate from root-local Q/R IDs.

The proposal is still not certified: one shared node may appear at multiple
paths/owner contexts, so graph-key reuse may not match current unfolded
alpha-normal trees, R ranking and Q paths. Structural dedup, R fixed-point and
alpha-key work also lack a proven linear bound. The §26 all-drafts-coexist
sample is authorized; the full physical lane ledger is still outstanding.
Candidate B remains Draft; no implementation, tests, or measurements ran.

## F5c accounting design closure and restart decisions

The indexed-finalization accounting-boundary Draft now defines a checked
event-time semantic/session ledger, capacity tickets that follow lane/owner
transfers, six physical-owner groups, a closed-world tracked-allocation rule,
overflow/unwind behavior, and the §26-authorized all-drafts-coexist sample.
Fresh independent M2 specification and performance delta reviews found no
remaining preapproval blocker within this accounting-design slice. The review
does not certify implementation: constructor/growth/event hooks, complete
physical-lane enumeration, failed-reserve/unwind reconciliation, and the
`yu-types` same-time checkpoint remain source/test proof after approval.

An architect follow-up found that current exact unordered alpha ranking costs
`k!` permutations for `k` distinct non-owner variables; path-expanding an
O(d)-node shared Function DAG can visit `2^d` occurrence paths. These are
counterexamples to the current method/bound, not a proof that no exact bounded
algorithm exists. Candidate B remains Draft and its proposed §24 API is not
approved. The user must choose the alpha/order contract (unrestricted exact
alpha with revised worst-case complexity; bounded work with a restricted and
proved production forest; or weakened alpha/order independence) and the
failed-route resource policy (conditional post-rollback O(1) sample;
journal/reconcile physical changes within the existing sample whitelist; or
defer that gate). Then producer parity and a clean full-candidate M2 review
must precede explicit API approval. §44's canonical first-member projection
remains unchanged. No code, tests, benchmarks, commits, or pushes changed in
this continuation.

## Normalized-Union logical rollback recheck

Re-ran `cargo test -p yu-solver --lib f5c_incoming_union_ --
--test-threads=1`: all 3 matching tests passed. The two failure witnesses use
distinct `Int` and `Function` members. One injects a diagnostic-edge reserve
failure during private decomposition; the other injects representative
provenance failure after private constraints have been admitted. Both capture
and compare the relevant live/session and store state, including facts,
canonical entries, receipts, provenance, route markers, bounds, typed-pair
memo, diagnostic scratch, and logical counters. Physical capacity retained by
failure is handled separately and remains subject to the pending resource
accounting decision. No source code changed; no broad suite or benchmark ran.

## F5c producer-order and failed-route sampling decision

The user's delegated direction was adjudicated in the authoritative
[`producer-order and failed-route-sampling addendum`](../../design/2026-09-23-f5c-producer-order-and-failed-route-sampling-addendum.md).
Q/R assignment follows first surviving producer encounter; cross-order
alpha/output equality is not required when numbering or positional recursive
bounds change. The full Union relation and §44 first-normalized-member
projection/transactionality remain. A failed incoming route takes exactly one
fixed-size O(1) sample after complete rollback iff a physical capacity or
counted-owner/retention transition occurred.

Architect preflight and final M3 specification, compiler, and performance
delta reviews are clean. This authorizes implementation only. Producer-order
fixtures, bounded stack-safe post-Q/R normalization, complete failed-route
physical lane accounting/sample coverage, component sharing, iterative
producer/finalizer work, and F5e certification remain open. Candidate B's
proposed §24 API remains unapproved. No source, test, benchmark, commit, or
push changed in this design adjudication.

## Fixed-assignment normalizer order blocker

An architect preflight and independent specification audit found an unresolved
mixed-height order between F5 §25's structural-first child order and §36's
postorder-height descriptor ranks. The producer-order addendum only relaxes
cross-input equality when Q/R assignment changes; it does not authorize this
fixed-assignment change. The user was asked to choose either preserving §25
with a clarified globally comparable structural rank, or making §36's
height-major order explicitly supersede §25. Q/R/canonicalization work is
paused pending that answer. The independent failed-route sample implementation
continues in parallel. No code or tests changed for this audit.

## Failed-route sampler implementation checkpoint

The fixed-size sampler now prepares checked totals before publishing counters,
sample count, or independent test ledger. Instantiation-scratch metric flush is
also checked and atomic, separated from its growth-event sample. Focused
overflow and route tests pass. The M3 spec/performance delta reviews are clean
for this subgate; compiler review closed the panic/partial-publication finding
but confirmed the full trigger is still incomplete. It currently notices only
instantiation growth, so a partial begin where the first seen-vector grows and
the second reserve fails can still skip the required post-rollback sample. A
private all-lane attempt flag/event-hook design pass is underway. No broad suite
or benchmark ran.

## Nested failed-route capacity accounting closure

The incoming nested value/effect-bound capacity subgate now preserves each
physical growth/rebuild event across rollback, including repeated growth of one
surviving lane, multiple successful events on a fresh row that is later
dropped, and a mixed case with two successful events on one existing row plus
a second row whose changed capacity fails checked preflight. Retained bytes are
still derived once from surviving row capacities, independently enumerated in
tests.

The preflight-overflow witness now reaches the actual incoming
old-capacity/changed-capacity branch through a Function scheme with a recursive
bound. It verifies complete RouteCheckpoint restoration and one conditional
post-rollback sample. A consuming run drives the same test-only preflight
injection through that branch, observes one outer attempt, returns
IdentityExhausted, and yields no SolvedModule. An M3 compiler concern that an
unrepresentable event total must nevertheless publish an event peak was
adjudicated by the architect as not a blocker: the checked preflight is the
failed event-accounting attempt, and publishing stale or partial peak data
would violate §3's atomic-overflow clause. No event peak is claimed for that
unrepresentable case; the final rollback sample and terminal error remain.

M3 specification review is clean for the repaired nested delta. M3 compiler
review found the mixed-event undercount, which was repaired and independently
rechecked. `cargo test -p yu-solver --lib f5c_incoming_ --
--test-threads=1` passes 26 tests; `cargo test -p yu-solver --lib f5c_ --
--test-threads=1` passes 91 tests. `cargo check -p yu-solver`,
`cargo fmt --package yu-solver --check`, and `git diff --check` pass.

This closes only the nested capacity subgate. Typed-pair/diagnostic scratch,
outer row tables, extrusion storage, inference Term arena, store/provenance/
receipt/routed-use lanes, and active/spare journal ownership still lack full
event-hook and failure-reconciliation proof. No benchmark or full yu-solver
library suite ran. Next bounded slice: typed-pair plus diagnostic owners.
Producer/normalizer work remains paused on the distinct §25/§36 mixed-height
order decision. The alpha/order relaxation and conditional post-rollback sample
decisions remain authoritative after a fresh architect re-examination; no
choice was overturned.

## Typed-pair and diagnostic failed-route owner slice

Implemented the next M2 lane group under the Authoritative producer-order /
failed-route-sampling addendum §3. Actual capacity growth in typed-pair memo,
worklist, diagnostic delta/index, diagnostic edge payload, diagnostic
completion/bucket scratch, error/report indexes, and typed-pair/report-error
journal undo vectors now triggers the fixed-size event sample and the
attempt-local physical-change flag. Nested diagnostic-edge byte accounting is
checked before publication. Rollback preserves physical pair-cache
growth/rebuild events while restoring logical pair admissions and private route
state.

The real incoming-Union witnesses now assert that finalization retains two
distinct Int/Function members. One completes both private member constraints
before representative-provenance admission fails; the other completes the
first member and fails on a capacity-changing DiagnosticEdges reserve in the
second. Both verify complete RouteCheckpoint restoration and exactly one final
post-rollback sample. The first also proves nonzero transient edge-payload
growth, diagnostic scratch capacity growth, event peak, and post-rollback
retained totals against the independent ledger. A test-only post-reserve error
seam covers Err-after-capacity-change; test-only route_many observers identify
the private-member stage without adding production behavior.

The first M2 specification review found two major witness gaps; the primary
accepted and batched both into one implementer repair round. A fresh
post-repair M2 spec_auditor review found no remaining issue. The independent
performance_auditor found no input-sized scan/allocation in the aggregate
sample, but the event-sample count is O(G) for G actual capacity-growth events,
with no fixed per-route bound; successful-path measurement is required to
close its cost. No measurement has run. Deferred the single warm-up plus three
paired samples at one representative input until all physical-owner event
hooks are integrated (maximum eight process invocations / ten minutes).

Verification:

- `cargo test -p yu-solver --lib f5c_incoming_ -- --test-threads=1` — 28 passed.
- `cargo test -p yu-solver --lib f5c_ -- --test-threads=1` — 93 passed.
- `cargo check -p yu-solver` — passed.
- `cargo fmt --package yu-solver --check` — passed.
- `git diff --check` — passed.

No full yu-solver library suite, workspace suite, or benchmark ran. Worktree
remains unstaged/uncommitted/unpushed on `yulang3` at `784fa502`; no design
record changed. F5c/F5e remain incomplete; the separate fixed-Q/R §25/§36
order decision is still pending.

## Outer-row and extrusion failed-route owner slice

Implemented the next M2 owner group under F5 §§26/34 and the Authoritative
failed-route sampling addendum §3. Fresh value/effect outer-row reserves and
extrusion-stack pushes now pass through the existing fixed-size event sample
and route-change flag. Startup reservations remain outside incoming attempts;
no row/map scan or new production allocation was added. Effect-row growth is
still excluded from end-to-end incoming tests because current closed-pure
Function schemes carry only closed `EmptyEffect`/`EffectBottom` effects.

The incoming route witnesses cover a post-reserve failure after outer
value-row capacity has actually grown, a later private-member failure with
row capacity retained after rollback, and an extrusion-stack growth from zero
capacity followed by a post-reserve injected failure. The exact-lane test-only
observer is written only after the changed-capacity event sample succeeds.
Both witnesses assert complete logical RouteCheckpoint restoration, exactly
one successful post-rollback sample, no public route/fact/provenance, and
retained semantic/session totals matching the independent ledger.

The initial M2 spec review found a major gap: the first row test proved
post-rollback capacity and final sampling but did not prove a prior event
sample. The primary accepted it; a fresh implementer added a targeted
post-reserve failure witness and lane observer without weakening journal
generation/checkpoint assertions. The fresh post-repair spec delta review found
no remaining finding. The performance review found no material new successful-
path work beyond fixed field-capacity comparisons and O(1) sampling per growth;
the required bounded successful-path measurement remains deferred until all
owner hooks cohere (one warm-up plus three paired samples, one input size,
maximum eight processes/ten minutes).

Verification:

- `cargo test -p yu-solver --lib f5c_incoming_ -- --test-threads=1` — 31 passed.
- `cargo fmt --package yu-solver --check` — passed.
- `git diff --check` — passed.

The full `f5c_` suite was not rerun; its last result remains 93 tests before
these two witnesses. No benchmark, full library suite, workspace suite, or
F5e matrix ran. Changes remain unstaged, uncommitted, and unpushed. Next:
instrument event-time sampling for the inference Term arena, then close store,
provenance/receipts, routed-use, and remaining active/spare journal owners.
F5c/F5e remain open.

## Inference Term failed-route owner group

Implemented event-time accounting for all six inference Term owners: fixed page
backing, page descriptors, page-position map, interner map, interned journal,
and claimed-page journal. Each actual changed reserve records a fixed-size
snapshot before constructor failure can propagate. The independent test ledger
enumerates the physical owners directly and reconciles per-lane lengths,
requests, capacities, growths, retained/peak bytes, and active/spare journal
transfers against Term bytes and the semantic/session aggregates.

Focused witnesses cover changed post-growth failures in the journal lanes,
proving the event-time sample precedes rollback, spare capacity remains
retained, and exactly one conditional post-rollback sample follows. The
checked-overflow witness reaches both the private route and consuming `run()`
path, which returns no `SolvedModule`. The first M2 specification review found
two major gaps (no direct lane-4/5 witnesses and an aggregate-only Term ledger);
one repair round closed both, and the fresh specification delta review found no
remaining issue. Performance review found no production scan or allocation;
event samples remain O(G) for G actual capacity growths, so the previously
planned bounded successful-path measurement remains open until all owner hooks
are complete.

Verification:

- `cargo test -p yu-solver --lib f5c_incoming_term_ -- --test-threads=1` — 5 passed.
- `cargo check -p yu-solver --tests` — passed.
- `cargo check -p yu-solver` — passed without new warnings.
- `cargo fmt --package yu-solver --check` — passed.
- `git diff --check` — passed.

The all-`f5c_` and all-`f5c_incoming_` suites, benchmark, workspace suite, and
F5e matrix were not rerun. Changes remain unstaged, uncommitted, and unpushed.
Next: cover store/provenance/receipts/routed uses and remaining journal-owner
transitions, then run the single bounded successful-path measurement after the
owner hooks cohere. F5c/F5e remain open.

## Inference store and routed-use failed-route owner group

Closed the ConstraintStore/routed-use owner group under F5 §26 and the
Authoritative failed-route sampling addendum §3. The four store lanes now
capture fixed-size same-time capacity snapshots at growth events; incoming
routes reserve fallibly, including accounting a changed capacity when reserve
returns an error. Store growth/event counts use checked arithmetic. The
aggregate sampler consumes the event snapshot, and the independent ledger
reconciles the four owner lanes. Existing routed-use record/index event hooks
remain in the same route boundary.

The focused evidence includes all four sequential store snapshots, changed
failed reserves on every store lane, injected local admission failure after
growth, logical rollback, retained capacities, retry, overflow rejection, and
one outer post-rollback sample. M2 specification and performance delta reviews
found no blocking issue. Static inspection confirms O(1) aggregation per event
with no new production scan or per-route allocation; total event samples are
O(G), so the planned bounded successful-path measurement remains open.

Verification:

- `cargo test -p yu-solver f5c_ -- --test-threads=1` — 109 passed.
- `cargo test -p yu-solver --lib f5c_store_ -- --test-threads=1` — 5 passed.
- `cargo check -p yu-solver` — passed without warnings.
- `cargo check -p yu-solver --tests` — passed.
- `cargo fmt --package yu-solver --check` and `git diff --check` — passed.

The full solver library/workspace suites, benchmark, and F5e matrix were not
run. Next, map the remaining active/spare journal setup and value/effect-row
journal owners before implementation. F5c/F5e remain open.

<!-- daily-append-anchor: 2026-09-23 -->

## Incoming scratch changed-reserve sample closure

Found and repaired the remaining incoming-scratch failure case: a reserve can
change physical capacity and then return an error. The changed capacity now
increments checked scratch growth evidence and sets an O(1) pending-event
marker before the error unwinds. `route_incoming_inner` samples the event while
the scratch owner is attached, then preserves the separately named §26
scratch-exit sample. After transaction rollback, the attempt-local physical
change flag triggers exactly one additional O(1) sample. Pre-reserve failure
and no-growth failure do not trigger that post-rollback sample.

The test-only incoming sample trace originally consumed all pending owner
events at the first sample. Term constructors can publish several capacity
changes and then produce one snapshot/sample per event; the trace now pairs one
event with each ordered snapshot sample. A new test sidecar exercises all
seven scratch lanes, proves the target lane's event sample, counts the separate
scratch-exit and post-rollback samples, checks `RouteCheckpoint` logical
restoration and retained-ledger totals, and verifies retry. Companion focused
tests cover no-growth with exhausted counters, preserving the physical-change
marker on counter overflow, pre-reserve scratch failure, and no outer sample
without scratch growth.

The first writer pass fixed growth accounting/postrollback triggering but did
not distinguish the actual-growth sample from the later §26 boundary. Primary
review found this because the trace rejected a capacity event paired with the
scratch-exit label. The repair now samples the event first, preserves the
scratch-exit sample, and the independent M2 spec review found no blocking issue.

Verification:

- `cargo test -p yu-solver --lib f5c_scratch_ -- --test-threads=1` — 3 passed.
- `cargo test -p yu-solver --lib f5c_incoming_scratch_reserve_failures_preserve_physical_accounting_and_retry -- --test-threads=1` — passed.
- `cargo test -p yu-solver --lib f5c_incoming_failed_route_without_scratch_growth_has_no_outer_sample -- --test-threads=1` — passed.
- `cargo check -p yu-solver --tests` — passed.
- `cargo test -p yu-solver --lib -- --test-threads=1` — 202 passed in 675.10s.
- `cargo fmt --package yu-solver`, `cargo fmt --package yu-solver --check`, and `git diff --check` — passed.

No benchmark or workspace test was run. The required bounded successful-path
event-sample measurement remains open. The crate has no dedicated benchmark
target, so select a production-path comparison before consuming the
one-warm-up/three-paired-sample/eight-process/ten-minute budget. Remaining F5c
work includes complete end-to-end per-use failure-lane evidence, component
sharing, iterative producer/finalizer stack safety, and the paused mixed-height
§25/§36 normalization decision; F5c/F5e are not complete. `lib.rs` is 28,441
lines; this fix keeps its focused 139-line test in
`crates/yu-solver/src/tests/f5c_scratch_reserve.rs` and does not bundle a broad
module split. Changes remain unstaged, uncommitted, and unpushed.

## ValueExactUpper per-use closure and sampler measurement attempt

Added `f5c_incoming_value_exact_upper_growth_samples_before_rollback_and_retries`
in `crates/yu-solver/src/tests/f5c_value_exact_upper_route.rs`. It reaches the
`ValueExactUpper` reserve through one real `route_incoming` call, injects a
post-reserve failure, and checks event-time lane sampling before propagation,
RouteCheckpoint restoration, no surviving fact/provenance/routed marker, one
conditional post-rollback sample, retained-ledger reconciliation, and retry.
The independent M1 specification delta review found no issue. The existing
`f5c_incoming_fresh_outer_row_growth_samples_before_rollback` already covers
fresh-row `ValueBounds` end to end; the earlier resume summary understated that
coverage. The new test closes `ValueExactUpper` specifically. The wider
owner-to-route lane matrix remains open pending reconciliation of every
reachable value/store/diagnostic/journal lane. Live effect-row mutation remains
unreachable under the current closed-pure Function scope and is excluded rather
than silently certified.

Attempted the bounded successful-path sample-cost comparison, but accepted no
timing result. The conservative eight-process limit was consumed. The final
temporary no-sample control bypassed 26 IncomingRoute aggregate calls while
the resource sample counter still advanced by 26, so the control did not
isolate sampling cost and the first printed pair was invalid. The prescribed
one-warmup/three-pair schedule did not complete. All temporary feature, API,
example, and test harness changes were removed. A future measurement requires
a path-complete ablation or a different valid baseline under a new budget.

`lib.rs` is 28,442 lines and grew by only the one test-module declaration; the
test itself remains in the sidecar. Read-only mapping identified a safe
63-line `ResourceSampleChecked` extraction candidate, deferred until the open
accounting/measurement checkpoint closes.

Verification:

- Focused `ValueExactUpper` incoming-route test — passed.
- `cargo fmt --package yu-solver --check` — passed.
- `cargo check -p yu-solver --tests` — passed.
- `git diff --check` — passed.

The previous full library-suite result remains 202 passed; it was not rerun for
this test-only lane extension. Full §3 accounting/measurement, F5c, F5e,
component-sharing, iterative producer/finalizer, and the user-pending mixed-
height §25/§36 choice remain open. Changes are unstaged, uncommitted, and
unpushed.

## ValueLevels and ValueMetadata incoming-route lane witnesses

Added changed-post-reserve `route_incoming` failure witnesses for `ValueLevels`
and `ValueMetadata` in `crates/yu-solver/src/tests/f5c_value_exact_upper_route.rs`.
Each isolates one capacity lane, matches the event-time sample, verifies exact
retained bytes and peak behavior, checks one conditional post-rollback sample,
asserts logical/public checkpoint restoration, and retries successfully. The
metadata witness now captures successful named-boundary snapshots and compares
the post-rollback snapshot against a fresh `IndependentResourceLedger`
recomputed from live capacities and independently enumerated surviving nested
rows.

The lane audit then found `ExtrusionValueMarks` had only pre-reserve
coverage. Added an isolated fourth-reserve changed-failure witness for that
lane, with the same exact event/retained/peak assertions, independent
post-rollback enumeration, one-sample check, checkpoint restoration, and
retry. The fresh M1 spec delta review is clean. Diagnostic scratch still needs
lane-specific changed-capacity evidence; the whole owner-to-route matrix
remains open.

Both focused M1 specification delta reviews are clean after repairs. The
ValueLevels review required isolation from unrelated growth and event peak
preservation. The ValueMetadata review required post-rollback event snapshots,
peak evidence, and a fresh post-state ledger enumeration. The test trace
extension is cfg(test)-only; the lane slice added no code to `lib.rs`.
`lib.rs` remains 28,456 lines; defer the separate 63-line
`ResourceSampleChecked` extraction until the accounting/measurement checkpoint
closes.

Verification:

- `cargo test -p yu-solver --lib f5c_value_exact_upper_route -- --test-threads=1` — 6 passed.
- `cargo check -p yu-solver --tests` — passed.
- `cargo fmt --all -- --check` and `git diff --check` — passed.

The full `yu-solver` library suite remains at the last 202-pass run and was not
rerun. No benchmark or resource matrix was run; the prior eight-process
sampler-cost budget remains consumed without valid timing data. The remaining
reachable owner-to-route sampling lanes, full §3 gate, F5c, and F5e remain
open. Changes remain unstaged, uncommitted, and unpushed.
