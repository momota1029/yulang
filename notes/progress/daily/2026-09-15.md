# 2026-09-15

## Staged-state integration

- The user selected the staged state as authoritative for the transitional
  worktree conflict. Commit `cc98c8e0` integrated that state and removed the
  later equal-indent statement test and its synchronized records. Only the
  unrelated untracked `logs/` directory remained afterward.

## Ordinary expression-delimited Missing audit

- Mode: M0 record synchronization after one completed independent read-only
  schema audit; no producer, repair round, test run or benchmark process.
- The existing direct Rowan proof in
  `crates/yu-syntax/src/tests/delimited_recovery.rs:604–814` is sufficient for
  a bounded Draft row covering Item, Separator and terminal Close `Missing` in
  ParenthesizedExpression, CallTail and IndexTail, with empty and single-item
  accepted controls and fresh/frozen record compatibility.
- ProjectionTupleTail and ProjectionRecordTail were excluded because their
  direct owner structure lacks equivalent real-shell proof. They are selected
  as the next M1 evidence slice with ten bounded witnesses. No compiler code or
  test changed in this checkpoint.

## Projection-tail ordinary Missing closure

- Mode: M1 with one Astra implementer, one independent Astra specification
  auditor and no repair round. The implementer owned only
  `crates/yu-syntax/src/tests/delimited_recovery.rs`; the primary synchronized
  records after the clean audit.
- One additive test supplies ten real-shell witnesses across
  ProjectionTupleTail and ProjectionRecordTail: leading-comma Item, omitted
  Separator, EOF Close, accepted empty and accepted single-item. It derives
  owner-specific roles from ordered CST before comparing exact records and
  proves ranges, parentage, losslessness, forbidden-node absence and
  fresh/frozen equality.
- `rustfmt --check --edition 2024 crates/yu-syntax/src/tests/delimited_recovery.rs`
  and `git diff --check` passed. The wrapper-disabled, one-job, one-thread
  focused test passed 1 with 1,422 filtered out under the 180-second cap. No
  broad suite or performance measurement was run.
- The post-write audit found no blocking, major or minor issue. Spread RHS,
  Error, nesting, boundary, repeated/semicolon/trivia variants, the global
  interpreter and ledger/API migration remain excluded.

## RuleLiteral indirect Missing audit

- Mode: M1 with one independent Astra specification auditor, no producer and
  no repair round; the primary performed only the resulting M0 record update.
- The audit cleanly mapped five indirect publishers to four direct structural
  slots: interpolation Close through two exits, braced lazy Close, unbraced
  lazy Name and outer RuleLiteral terminator. Existing direct Rowan and
  fresh/frozen compatibility evidence was sufficient, so no test rerun or
  benchmark process was used.
- The row is limited to direct ASCII witnesses. Existing UTF-8 recovery records
  do not certify direct-CST UTF-8 Missing ranges. RuleSequence Error, nested
  Rule grammar, broader boundary variants, global interpreter construction and
  ledger/API migration remain open.

## RuleCapture required-RHS closure

- Mode: M1 with one independent Astra pre-write specification audit, one Astra
  implementer, one delta audit by the original reviewer and no repair round.
- The pre-write audit found missing proof assertions rather than a parser
  defect. The existing `{a=;}` and `{a=; b?}` witnesses now assert full source,
  the native closing brace, direct childless Missing, and the exact admitted
  RHS Whitespace/Identifier token plus RuleQuantifier node/token shell while
  retaining terminal Capture ownership.
- Scoped rustfmt and diff checks passed. The wrapper-disabled exact focused
  test passed 1 with 1,422 filtered out under the 180-second, one-job,
  one-thread budget. No broad suite or performance measurement was used.
- The delta audit was clean. Only the witnessed Error-to-Missing and
  Error-to-RuleItem forms are catalog-audited evidence-complete Draft; other
  RuleCapture variants, other Rule slots and later construction gates remain
  open.

## RuleField/RulePath required-name closure

- Mode: M1 with one independent Astra pre-write specification audit, one Astra
  implementer, one delta audit by the original reviewer and no repair round.
- The pre-write audit found that existing named-tail witnesses did not pin
  immediate ancestry or the following accepted Identifier. The test now proves
  the full owner chain, direct introducer/Error tokens and UTF-8 byte ranges,
  separate outer RuleItem Whitespace/Identifier tokens, full source and native
  close. The existing quantifier-outside-RuleField witness stayed unchanged.
- Scoped rustfmt and diff checks passed. The wrapper-disabled exact focused
  test passed 1 with 1,422 filtered out under the 180-second, one-job,
  one-thread budget. Independent delta review was clean; no broad suite or
  performance measurement was used.
- Only the witnessed Error/continuation forms are catalog-audited
  evidence-complete Draft. Missing/accepted-name, broader boundaries, other
  Rule slots and later construction gates remain open.

## Nested Rule close-slot closure

- Mode: M1 with one independent Astra pre-write specification audit, one Astra
  implementer, one delta audit by the original reviewer and no repair round.
- The `{(a` witness now proves exact immediate nested CST and native token
  ranges, two direct childless Missing nodes at `3..3`, and unsorted natural
  preorder from the inner parenthesis close to the outer body close. Close
  roles and singleton expectations are derived structurally before records.
- Scoped rustfmt and diff checks passed. The wrapper-disabled exact focused
  test passed 1 with 1,422 filtered out under the 180-second, one-job,
  one-thread budget. Independent delta review was clean; no broad suite or
  performance measurement was used.
- Only this nested EOF composition is catalog-audited evidence-complete Draft.
  Accepted closes/postfix, pending leading, fences/callers, recursive forms,
  other Rule slots and later construction gates remain open.

## RuleSequence terminal Error-group closure

- Mode: M1 with one independent Astra pre-write specification audit, one Astra
  implementer, one delta audit by the original reviewer and no repair round.
- The terminal RuleBody `{;💥}` witness now proves the complete frame and native
  close, absence of Missing/Invalid, two direct UTF-8 Error leaves and one
  maximal `1..6` group, plus CST-only singleton `Literal(RuleItem)` projection
  with primary zero.
- Scoped rustfmt and diff checks passed. The wrapper-disabled exact focused
  test passed 1 with 1,422 filtered out under the 180-second, one-job,
  one-thread budget. Independent delta review was clean; no broad suite or
  performance measurement was used.
- Only this terminal Body witness is catalog-audited evidence-complete Draft.
  Retry, other callers, boundary variants, nested grammar, ExpressionList and
  later construction gates remain open.

## Rule ExpressionList pending-fence handoff closure

- Mode: M1 with one independent Astra pre-write specification audit, one Astra
  implementer, one post-write delta audit and no repair round.
- A richer test-only helper now exposes the existing normalized witness exit and
  LineEntry. The three empty-list RuleItem/RuleCall/RuleIndex fence witnesses
  prove the unchanged returned boundary Item, `PhysicalStart`, complete Yumark
  fence facts/coordinates, exact CRLF leading ownership, terminal-leading
  emission and full source reconstruction. Production APIs are unchanged.
- Scoped rustfmt and diff checks passed. The wrapper-disabled exact focused
  test passed 1 with 1,422 filtered out under the 180-second, one-job,
  one-thread budget. Independent delta review was clean; no broad suite or
  performance measurement was used.
- Stale fence-proof and pending-Item-open record text was reconciled. Outer
  Yumark construction, nonempty/nested lists, other fences and later gates
  remain excluded.

## StringLiteral outer-terminator closure

- Mode: M1 with one independent Astra pre-write specification audit, one Astra
  implementer, one post-write delta audit and no repair round.
- The existing named test now proves exact Root/StringLiteral ownership,
  ordered token/node spellings and UTF-8 ranges, source flattening, accepted
  normal/heredoc StringEnd and final childless Missing across five bounded
  witnesses. The mismatched two-quote run remains native StringText, and slot
  selection precedes temporary record comparison.
- Scoped rustfmt and diff checks passed. The wrapper-disabled exact focused
  test passed 1 with 1,422 filtered out under the 180-second, one-job,
  one-thread budget. Independent delta review was clean; no broad suite or
  performance measurement was used.
- Only these five outer-terminator forms are catalog-audited evidence-complete
  Draft. StringPiece interiors, arbitrary heredoc, expanded caller/fence and
  later construction gates remain excluded.

## RuleLiteral UTF-8 equal-offset composition closure

- Mode: M1 with one independent Astra pre-write specification audit, one Astra
  implementer, one post-write delta audit and no repair round.
- The single `~"é{α` witness proves exact UTF-8 native topology and byte ranges,
  full source/remainder, and distinct childless interpolation-Close and outer
  terminator Missing occurrences at `7..7` in natural preorder. Expectations
  and primary zero are derived from immediate CST context before records. The
  fence-aware EOF exit remains the established `EofAfterTrivia` boundary.
- Scoped rustfmt and diff checks passed. The wrapper-disabled exact focused
  test passed 1 with 1,422 filtered out under the 180-second, one-job,
  one-thread budget. Independent delta review was clean; no broad suite or
  performance measurement was used.
- UTF-8 lazy capture, other boundary/leading forms, broader nested recovery and
  later construction gates remain excluded.

## RuleLiteral UTF-8 braced-lazy composition closure

- Mode: M1 with one independent Astra pre-write specification audit, one Astra
  implementer, one post-write delta audit and no repair round.
- The single `~":{α` witness proves exact native topology and UTF-8 byte ranges,
  full source/remainder, and distinct childless lazy-Close and outer-terminator
  Missing occurrences at `6..6` in natural preorder. Direct Colon+OpenBrace
  selects Close rather than unbraced Name; expectations and primary zero are
  derived before records. The EOF exit remains `EofAfterTrivia`.
- Scoped rustfmt and diff checks passed. The wrapper-disabled exact focused
  test passed 1 with 1,422 filtered out under the 180-second, one-job,
  one-thread budget. Independent delta review was clean; no broad suite or
  performance measurement was used.
- UTF-8 unbraced Name, other boundaries/leading, CRLF/fences, nested Rule and
  later construction gates remain excluded.

## RuleLiteral UTF-8 unbraced-Name composition closure

- Mode: M1 with one independent Astra pre-write specification audit, one Astra
  implementer, one post-write delta audit and no repair round.
- The single `~"é:` witness proves exact native topology and UTF-8 byte ranges,
  full source/remainder, and distinct childless lazy-Name and outer-terminator
  Missing occurrences at `5..5` in natural preorder. Exact direct
  `[Colon token, Missing node]` structure selects Name and excludes the braced
  Close path; expectations and primary zero are derived before records.
- Scoped rustfmt and diff checks passed. The wrapper-disabled exact focused
  test passed 1 with 1,422 filtered out under the 180-second, one-job,
  one-thread budget. Independent delta review was clean; no broad suite or
  performance measurement was used.
- The four RuleLiteral indirect Missing slots now have bounded UTF-8 direct-CST
  evidence through the three recorded EOF compositions. Other boundaries,
  leading/CRLF/fence forms, nested Rule and later construction gates remain
  excluded.

## RuleField/RulePath native-close Missing closure

- Mode: M1 with one independent Astra pre-write specification audit, one Astra
  implementer, one post-write delta audit and no repair round.
- Paired `{a.}` / `{a::}` witnesses prove exact full ancestry and native token
  ranges, a unique direct childless Name Missing at `3..3` / `4..4`, preserved
  body close, and field/path role plus singleton Identifier/primary-zero
  projection derived before records.
- Scoped rustfmt and diff checks passed. The wrapper-disabled exact focused
  test passed 1 with 1,422 filtered out under the 180-second, one-job,
  one-thread budget. Independent delta review was clean; no broad suite or
  performance measurement was used.
- Accepted names, newline/EOF/fence/interpolation callers, recursive Rule and
  later construction gates remain excluded.

## RuleCapture bare required-RHS Missing closure

- Mode: M1 with one independent Astra pre-write specification audit, one Astra
  implementer, one post-write delta audit and no repair round.
- The `{a=}` witness proves the full terminal Capture tree, direct Equals plus
  childless RHS Missing at `3..3`, native body close, unique Missing and
  CST-derived singleton `Literal(RuleItem)`/primary zero before records.
- Scoped rustfmt and diff checks passed. The wrapper-disabled exact focused
  test passed 1 with 1,422 filtered out under the 180-second, one-job,
  one-thread budget. Independent delta review was clean; no broad suite or
  performance measurement was used.
- Accepted-only RHS, longer Error runs, recursive Rule, other boundaries and
  later construction gates remain excluded.

## RuleField LF required-Name Missing closure

- Mode: M1 with one independent Astra pre-write specification audit, one Astra
  implementer, one post-write delta audit and no repair round.
- The `{a.\nnext}` witness now proves the complete direct tree/token sequence,
  unique childless RuleField Name Missing at `3..3`, native LF under
  RuleAlternation and `next` in the second RuleSequence, with singleton
  Identifier/primary-zero projection derived before records.
- Scoped rustfmt and diff checks passed. The wrapper-disabled exact focused
  test passed 1 with 1,422 filtered out under the 180-second, one-job,
  one-thread budget. Independent delta review was clean; no broad suite or
  performance measurement was used.
- RulePath/Capture LF, CRLF/EOF/fence/interpolation, accepted-name and later
  construction gates remain excluded.

## RuleCapture CRLF required-RHS Missing closure

- Mode: M1 with one independent Astra pre-write specification audit, one Astra
  implementer, one post-write delta audit and no repair round.
- The `{a=\r\nnext}` witness now proves the full direct tree/token inventory,
  terminal Capture with unique childless RHS Missing at `3..3`, native CRLF
  `3..5` under RuleAlternation and `next` in the second RuleSequence, with
  singleton `Literal(RuleItem)`/primary-zero projection derived before records.
- Scoped rustfmt and diff checks passed. The wrapper-disabled exact focused
  test passed 1 with 1,422 filtered out under the 180-second, one-job,
  one-thread budget. Independent delta review was clean; no broad suite or
  performance measurement was used.
- Accepted RHS, other boundary callers, recursive Rule and later construction
  gates remain excluded.

## Public Root Rule EOF-leading composition closure

- Mode: M1 with one independent Astra pre-write specification audit, one Astra
  implementer, one post-write delta audit and no repair round.
- The existing public-Root `~"{a=  ` witness now proves the complete exact
  Rowan topology, three childless Missing occurrences at CST frontier `5..5`
  in Capture-RHS, interpolation-Close and outer-terminator preorder, and direct
  Root-owned Whitespace `5..7`. Singleton expectations and primary zero are
  derived from distinct CST owners before compatibility records; existing
  scanned-EOF record coordinates remain unchanged.
- Scoped rustfmt and diff checks passed. The wrapper-disabled exact focused
  test passed 1 with 1,422 filtered out under the 180-second, one-job,
  one-thread budget. Independent delta review was clean; no broad suite or
  performance measurement was used.
- Other leading/comment/CRLF/fence and caller variants, recursive Rule and
  later construction gates remain excluded.

## RulePath LF required-Name Missing closure

- Mode: M1 with one independent Astra pre-write specification audit, one Astra
  implementer, one post-write delta audit and no repair round.
- The existing `{a::\nnext}` witness now proves the complete exact nine-node
  and six-token Rowan topology, direct RulePath with ColonColon and unique
  childless Name Missing at `4..4`, LF `4..5` under RuleAlternation, `next` in
  the second RuleSequence/RuleItem at `5..9`, native braces and absence of
  Error/Invalid. The RulePathName singleton Identifier/primary-zero projection
  is derived from CST ownership before compatibility records.
- Scoped rustfmt and diff checks passed. The wrapper-disabled exact focused
  test passed 1 with 1,422 filtered out under the 180-second, one-job,
  one-thread budget. Independent delta review was clean; no broad suite or
  performance measurement was used.
- Accepted names, CRLF/parenthesis/EOF/fence/interpolation, recursive Rule and
  later construction gates remain excluded.

## Gate-1 publication-mechanism completeness audit

- Mode: read-only M1 with one independent Astra specification auditor, one
  pass, no implementer or repair round and zero build/test/benchmark processes.
- The direct census remains 123 sites in 39 files: 59 Missing, 9 Error-item,
  53 Error-run and 2 structured Invalid. The audit separated that four-emitter
  search baseline from the fifth newline-callback entry mechanism, ten
  `emit_rule_missing` caller expressions and two structured Invalid callers
  yielding three semantic roles.
- Confirmed mapped evidence was retained rather than re-tested. Stale catalog
  navigation now recognizes the bounded three-caller fence proof and the three
  recorded RuleLiteral UTF-8 EOF range compositions.
- Gate 1 remains incomplete. The interpolation-frame RuleSequence Error
  residual is closed below; delegated BracketRow Item/Close internals remain
  next, and not every discriminator behind the 123 direct sites was
  independently re-certified by this pass.

## RuleLiteralInterpolation RuleSequence Error closure

- Mode: M1 with one independent Astra pre-write specification audit, one Astra
  implementer, one post-write delta audit and no repair round.
- The bounded `~"{| if ] a}"` witness proves exact five-node/eleven-token
  ownership, five adjacent direct Error leaves for interpolation-specific
  `|`/`if`/`]` controls as one maximal group `3..9`, and an admitted retry
  RuleItem `9..11` before native interpolation/literal completion. There is no
  Missing, Invalid or Error node.
- Direct RuleSequence/interpolation ancestry derives singleton
  `Literal(RuleItem)`, primary zero and the combined range before three
  unchanged per-Item compatibility records. A separate bounded catalog row was
  added after independent clean delta review.
- Scoped rustfmt and diff checks passed. The wrapper-disabled exact focused
  test passed 1 with 1,422 filtered out under the 180-second, one-job,
  one-thread budget. No broad suite or performance measurement was used.
- EOF/fence/outer-quote, newline/CRLF/UTF-8, nested Rule/public caller forms and
  later construction gates remain excluded. BracketRow internals are next.

## BracketRow Item/Close CST-role collision proof

- Mode: M1 with one independent Astra pre-write specification/reachability
  audit, one Astra implementer, one post-write delta audit and no repair round.
- `T [A@] -> U` and `T [A)] -> U` now have a bounded direct-CST collision
  witness: identical six-element BracketRow topology, normalized Error range
  `4..5`, one direct Error group and identical TypeArrowTail ancestry, but
  distinct retained Item/TypeExpression and Close/`]` singleton roles with
  primary zero. The helper also pins fresh/frozen replay, sentinel shifts and
  full source.
- Scoped rustfmt and diff checks passed. The collision test and the neighboring
  Missing/local-close and Item-retry exact tests each passed 1 with 1,423
  filtered out under the 180-second, one-job, one-thread budget. No broad suite
  or performance measurement was used.
- The Item/Close owner mapping is BLOCKING pending an approved structural
  resolution. Error spelling and role merging are forbidden substitutes.
  Separator reachability is confirmed through explicit branches and remains a
  separate open schema/proof obligation.

## BracketRow no-gap Separator Missing closure

- Mode: M1 with one Astra pre-write specification/reachability audit, one Astra
  implementer, one independent delta audit and one repair round.
- The bounded `T [A{}] -> U` witness proves direct ordered BracketRow children,
  one childless Separator Missing at `4..4` between two TypeExpression children,
  authoritative nested `NamedRecordTypeClose`, no other Missing/Error/Invalid,
  full sentinel/source and the accepted Arrow/RHS suffix. Direct structure
  selects `Type(BracketRowSeparator)`, singleton
  `DelimitedSequenceSeparator`, primary zero before records.
- The one repair corrected only the pre-write inference that had omitted the
  accepted record-close wrapper; production behavior and existing expectations
  remained unchanged. Scoped rustfmt/diff checks, the new exact test and two
  neighboring controls each passed 1 with 1,424 filtered out under the
  180-second, one-job, one-thread budget. No broad suite or measurement ran.
- The no-gap slice did not itself prove the deeper-newline branch; that branch
  is closed below. The independent Item/Close CST role collision remains
  BLOCKING pending an approved structural decision.

## BracketRow deeper-newline Separator Missing closure

- Mode: M1 with one explorer reachability pass, one Astra pre-write
  specification audit, one Astra implementer, one independent delta audit and
  no repair round.
- The real returned-NUD witness `T [:{A\n  B] -> U` proves nested PV Close
  Missing `6..6`, BracketRow-owned Newline `6..7` and Whitespace `7..9`, then
  Separator Missing `9..9` before the next direct TypeExpression. Exact
  subtree/tail ownership, two childless Missing occurrences, no Error/Invalid,
  full source and fresh/frozen green equality are pinned; structural roles and
  singleton/primary-zero projections precede records.
- Scoped rustfmt/diff and the deeper, no-gap and accepted-control exact tests
  each passed 1 with 1,425 filtered out under the 180-second, one-job,
  one-thread budget. Independent delta audit was clean; no broad suite or
  measurement ran.
- Both explicit Separator publisher branches now have bounded evidence.
  Exhaustive callers/boundaries remain open; Item/Close remains BLOCKING.

## Required-Type fresh Missing caller reconciliation

- Mode: read-only M1 with one Astra specification auditor, one pass, no
  implementer/repair and zero build/test/benchmark processes.
- The shared required-Type entry has 12 production call expressions and 20
  role/shape transports. Five cannot reach fresh Missing; 15 production role
  contexts remain reachable. Eight had bounded mappings and seven were
  initially unmapped: Role Head, Impl Head/Description, Cast TargetType and
  Struct/Enum/Error named-field Type. Role Head is closed below.
- The audit did not certify all boundary alternatives or nested Type. It
  refreshed the stale named-field required-Type source locator only.

## RoleDeclaration fresh required-Head Missing closure

- Mode: M1 with one Astra pre-write audit, one Astra implementer, one
  independent delta audit and no repair round.
- Existing `role ;` topology now derives one `Declaration(Role(Head))`,
  singleton TypeExpression, primary zero and direct CST range `5..5` from the
  ordered Role shell before the retained shifted record at `105..105`. Unique
  childless Missing, no Error/Invalid, semicolon/EOF handoff and fresh/frozen
  seeded replay remain unchanged.
- Scoped rustfmt/diff and the wrapper-disabled exact test passed 1 with 1,425
  filtered out under the 180-second, one-job, one-thread budget. Independent
  delta audit was clean; no broad suite or measurement ran.
- Status: evidence-complete Draft for `role ;` only. Other Role Head boundaries,
  malformed/retried/nested Type and later construction gates remain open.

## ImplDeclaration fresh required-Type Missing closure

- Mode: M1 with one Astra pre-write audit, one Astra implementer, one
  independent delta audit and no repair round.
- Existing `impl ;` Head and `impl T:` Description witnesses now derive their
  distinct roles from ordered declaration versus actual-Colon ImplDescription
  ancestry before records. Each projects singleton TypeExpression, primary
  zero and its direct childless Missing range `5..5` / `7..7`; no-cascade and
  fresh/frozen evidence remain unchanged.
- Scoped rustfmt/diff and the wrapper-disabled exact test passed 1. Independent
  delta audit was clean; no broad suite or measurement ran.
- Status: evidence-complete Draft for these two witnesses only. Cast TargetType
  and Struct/Enum/Error named-field Type remain the four unmapped reachable
  required-Type contexts; broader Impl/Type and later gates remain open.

## CastTarget fresh required-Type Missing closure

- Mode: M1 with one Astra pre-write audit, one Astra implementer, one repaired
  independent audit and one repair round.
- The bounded `cast(x): ;` witness proves complete canonical topology, a unique
  childless TypeExpression Missing `9..9`, no Error/Invalid, semicolon/EOF
  handoff and structure-derived `Declaration(Cast(TargetType))` with singleton
  TypeExpression/primary zero before records. Canonical and isolated harnesses
  compare their CastDeclaration subtrees; typed fresh/frozen whole trees agree.
- The repair corrected only an unsupported cross-entry Root equality and kept
  all CST/record expectations unchanged. Scoped rustfmt/diff and the exact test
  passed 1 with 1,426 filtered out. Independent closure audit was clean; no
  broad suite or measurement ran.
- Status: evidence-complete Draft for this witness only. The three named-field
  Type contexts remain unmapped; broader Cast/Type and later gates remain open.

## Named-field required-Type fresh Missing closure

- Mode: M1 with one Astra pre-write audit, one Astra implementer, one repaired
  independent delta audit and one repair round.
- A three-owner matrix proves complete Struct/Enum/Error named-brace ancestry,
  actual field Colon, one childless TypeExpression Missing at `12..12`,
  `14..14` or `15..15`, native closes, EOF and sentinel fresh/frozen replay.
  Ancestry selects Struct FieldType versus Enum/Error Variant NamedFieldType;
  singleton TypeExpression/primary zero is asserted before records.
- The repair added only the explicit structural projection omitted from the
  first test version. Scoped rustfmt/diff and exact test passed 1 with 1,427
  filtered out. Independent closure audit was clean; no broad suite or
  measurement ran.
- All 15 reachable role contexts in the bounded required-Type fresh-Missing
  transport inventory now have mapped witnesses. Broader layouts/boundaries,
  nested Type and later gates remain open; Gate 1 is not complete.

## Case/Catch WhereKw Guard required-expression closure

- Mode: M1 with one read-only caller census, one Astra pre-write audit, one
  Astra implementer and one independent post-write delta audit; no repair
  round.
- The additive six-case Rowan-only matrix covers Case/Catch WhereKw Guard
  Missing, terminal maximal Error and IdentifierExpression retry. Complete
  ancestry, direct trivia ownership, exact ranges, Error grouping, Arrow/body
  handoff and `CaseLike(Guard)`/Expression/primary-zero projection are selected
  before records and without Error spelling.
- Scoped rustfmt/diff checks and the four focused `case_schema_` tests passed:
  4 passed, 0 failed, 1,425 filtered out. No production change, broad suite or
  benchmark ran; measurement budget usage was zero.
- These six alternatives are evidence-complete Draft only. The bounded
  12-class/24-cell required-expression reconciliation is closed below for its
  identified direct-proof gaps. Gate 1 and construction gates 2–5 remain open.

## Elsif Condition required-expression closure

- Mode: M1 with one Astra pre-write audit, one Astra implementer and one
  independent post-write specification audit; no repair round.
- The additive three-case test proves direct Rowan Missing, terminal maximal
  Error and IdentifierExpression retry before actual Colon. Full ancestry,
  second-arm ordinal, direct ElsifKw, exact grouping/ranges, leading ownership,
  accepted body handoff and recovery census derive
  Condition/Expression/primary zero without records or Error spelling.
- Scoped rustfmt/diff and the wrapper-disabled exact test passed: 1 passed, 0
  failed, 1,429 filtered out. No production change, broad suite or benchmark
  ran; measurement budget usage was zero.
- These three alternatives are evidence-complete Draft only. EOF/fence
  absence, missing introducer, later companion/layout cases, nested Nud and the
  broader required-expression surface remain open. Gate 1 and construction
  gates 2–5 remain open.

## Production infix-operand Missing discriminator closure

- Mode: M1 with one pre-write audit, one Astra implementer and one independent
  post-write specification audit; one formatting-only repair.
- The additive ordinary-EOF `a +` witness proves the exact direct
  IdentifierExpression, InfixOperatorUse and final childless Missing chain.
  Full ancestry/order, exact node/token ranges, sole recovery, source fidelity
  and EOF Item extent derive Expression(Nud)/Expression/primary zero at `3..3`
  without records or operator spelling.
- Scoped rustfmt/diff and the wrapper-disabled exact test passed: 1 passed, 0
  failed, 1,430 filtered out. No production change, broad suite or benchmark
  ran; measurement budget usage was zero.
- The identified gaps in the bounded 12-class/24-cell required-expression
  caller reconciliation are now closed. Recursive/boundary combinations,
  nested owners, the global Gate 1 schema and construction gates 2–5 remain
  open.

## Shared initial Pattern Cast caller closure

- Mode: M1 with one Astra read-only finite caller audit, one Astra implementer
  and one independent post-write specification audit; no repair round.
- The audit found the omitted parenthesized Cast mandatory-policy entry. Three
  additive witnesses prove current-tail Pattern Primary Missing, singleton
  initial Error-to-Identifier retry and Primary-to-AlternationRhs reset from
  complete Rowan ancestry/order without records or Error spelling.
- Direct Cast-owned absence stays separate; bare accepted/retry Cast and
  guarded structured Record entries admit a NUD before shared delegation.
- Scoped rustfmt/diff and the wrapper-disabled exact test passed: 1 passed, 0
  failed, 1,431 filtered out. No production change, broad suite or benchmark
  ran; measurement budget usage was zero.
- The bounded reachable initial Pattern caller table is reconciled. Other
  Pattern phases, boundary/layout combinations, nested grammar, global Gate 1
  and construction gates 2–5 remain open.

## Required-Type initial Error inventory and Impl closure

- Mode: M1 with one Astra read-only 20-transport audit, one Astra implementer
  and one independent post-write specification audit; one formatting-only
  repair.
- Unlike the closed 15-context fresh-Missing inventory, all 20 owner/shape
  transports can reach initial nonempty Type Error, including five
  tuple/positional transports. Existing rows fully mapped six contexts and
  bounded Act retry evidence covered two more.
- Additive assertions now map Impl Head and Description terminal,
  singleton-retry and three-leaf-retry groups. Ordered declaration versus
  actual-Colon Description ancestry derives
  `Type(Primary)`/TypeExpression/primary zero, maximal ranges, retry-leading
  ownership and no Missing cascade before records.
- Scoped rustfmt/diff and the wrapper-disabled exact test passed: 1 passed, 0
  failed, 1,431 filtered out. No production change, broad suite or benchmark
  ran; measurement budget usage was zero.
- Impl closes two contexts and ten still lack dedicated mapped Error coverage.
  Act terminal evidence remains separately bounded. Broader boundaries/layout,
  nested Type, global Gate 1 and construction gates 2–5 remain open.

## Role Head initial required-Type Error closure

- Mode: M1 with one Astra pre-write audit, one Astra implementer and one
  independent post-write specification audit; one formatting-only repair.
- Additive assertions map the protected-semicolon terminal Error, singleton
  retry and three-leaf UTF-8 retry in the existing Role Head schema test.
  Complete ordered ancestry derives `Type(Primary)`/TypeExpression/primary zero
  over `5..6` or `5..9`; retry leading stays inside TypeExpression and no
  caller Missing occurs.
- The terminal witness preserves the pending semicolon and its leading; it is
  not treated as an EOF Error witness. Existing fresh/frozen and seeded-record
  compatibility evidence remains unchanged.
- Scoped rustfmt/diff and the wrapper-disabled exact test passed: 1 passed, 0
  failed, 1,431 filtered out. No production change, broad suite or benchmark
  ran; measurement budget usage was zero.
- Role closes one context, leaving nine of 20 required-Type Error transports
  without dedicated mapped coverage. Act terminal, broader boundaries/layout,
  nested Type, global Gate 1 and construction gates 2–5 remain open.

## Cast TargetType initial required-Type Error closure

- Mode: M1 with one Astra pre-write audit, one Astra implementer and one
  independent post-write specification audit; no repair round.
- A three-case matrix proves form-boundary terminal, singleton retry and
  three-leaf retry after completed CastPattern and actual target Colon. Direct
  CastTarget grouping derives `Type(Primary)`/TypeExpression/primary zero over
  `9..10` or `9..13` before records.
- Retry leading belongs to TypeExpression; terminal form-leading and semicolon
  belong to CastDeclaration. No other recovery occurs, declaration subtrees
  agree across harnesses, typed fresh/frozen trees agree and all paths reach
  EOF.
- Scoped rustfmt/diff and the wrapper-disabled exact test passed: 1 passed, 0
  failed, 1,432 filtered out. No production change, broad suite or benchmark
  ran; measurement budget usage was zero.
- Cast closes one context, leaving eight of 20 required-Type Error transports
  without dedicated mapping. Act terminal, recovered-Colon/body paths, broader
  boundaries/layout/nested Type, global Gate 1 and construction gates 2–5
  remain open.

## Named-field initial required-Type Error closure

- Mode: M1 with one Astra pre-write audit, one Astra implementer and one
  independent post-write audit; one compatibility-assertion repair.
- Nine witnesses cross Struct/Enum/Error named-brace ancestors with terminal,
  singleton-retry and three-leaf-retry malformed Type. Complete ordered CST
  derives `Type(Primary)`/TypeExpression/primary zero before records; retry
  leading and all native closes retain their direct owners.
- The repair changed only the new legacy unexpected-fact expectation: three
  CST Error leaves represent two lexical Items, with the second fact including
  internal leading. CST topology/group/ranges remained unchanged.
- Final scoped rustfmt/diff and the wrapper-disabled exact test passed: 1
  passed, 0 failed, 1,433 filtered out. Two test invocations ran; no production
  change, broad suite or benchmark, and measurement budget usage was zero.
- Three contexts close, leaving five of 20 required-Type Error transports
  unmapped: three tuple-field and two positional-payload contexts. Act terminal,
  broader boundaries/layout/nested Type, global Gate 1 and construction gates
  2–5 remain open.

## Tuple-field and positional-payload initial required-Type Error closure

- Mode: M1 with one Astra pre-write audit, one Astra implementer and one
  independent post-write specification audit; no repair round.
- Fifteen witnesses cover Struct/Enum/Error tuple-field and Enum/Error direct
  positional-payload required-Type entry across terminal, singleton-retry and
  three-leaf-retry malformed primaries. Actual parentheses and direct
  `StructField` select tuple ownership; direct `EnumVariant` children without
  `FromKw`, parentheses or a field wrapper select positional ownership.
- Both rows derive `Type(Primary)`/TypeExpression/primary zero before records,
  with maximal ranges, retry-leading ownership, native closes, no Missing or
  Invalid cascade, EOF handoff and fresh/frozen agreement.
- Scoped rustfmt/diff and the wrapper-disabled exact test passed: 1 passed, 0
  failed, 1,434 filtered out. One test invocation ran; no repair, production
  change, broad suite or benchmark, and measurement budget usage was zero.
- The final five contexts close bounded mapped-ready evidence for all 20
  reachable required-Type initial Error owner/shape transports. Act terminal
  remains a separate residual; broader boundaries/layout, recursive Type,
  occurrence combinations, global Gate 1 and construction gates 2–5 remain
  open.

## Act Head/Source semicolon-terminal required-Type Error closure

- Mode: M1 with one Astra pre-write audit, one Astra implementer and one
  independent post-write specification audit; no repair round.
- The finite audit confirmed that Head and Source already count among the 20
  mapped transports through retry evidence; only their no-retry termination at
  an actual semicolon remained a separate shape residual.
- Additive `act @;` and `act A = @;` witnesses select Head versus completed Head
  plus Equals Source from direct order, then structurally derive one
  `Type(Primary)` group, singleton TypeExpression expectation and primary zero
  before records. The actual semicolon is Act-owned and both fresh/frozen paths
  reach EOF without Missing, Invalid, retry or BodyIntroducer cascade.
- Scoped rustfmt/diff and the wrapper-disabled exact test passed: 1 passed, 0
  failed, 1,435 filtered out. Two test invocations ran; no repair, production
  change, broad suite or benchmark, and measurement budget usage was zero.
- This closes only the bounded Act semicolon-terminal residual and leaves the
  20-transport count unchanged. Multileaf, EOF-terminal, other
  boundaries/layout/attachments, recursive Type, global Gate 1 and construction
  gates 2–5 remain open.

## BracedStatementBlock direct Statement Error proof closure

- Mode: M1 with one Astra architect/pre-write audit, one Astra implementer and
  one independent post-write specification audit; one assertion repair.
- A current-state audit found that the existing short Draft claimed direct raw
  Error evidence while the seven-witness test lacked explicit maximal-group and
  `BracedStatementBlock(Statement)` projection assertions. Coverage also
  overclaimed terminal/protected prefixes despite the catalog exclusion.
- The strengthened test derives the role from complete shell ancestry/direct
  sequence order before records and proves singleton, three-leaf ASCII,
  UTF-8/LF and CRLF-separated group ranges, Statement expectation/primary zero,
  separator/retry/native-close ownership, no Missing/Invalid cascade, full
  source/empty remainder and fresh/frozen parity.
- One failed new assertion assumed retry leading was an immediate Statement
  child. The single repair follows existing ownership: leading is contained in
  the retry Statement subtree, except where the separator owns it. Fixtures and
  prior semantic expectations were unchanged.
- Final scoped rustfmt/diff and the wrapper-disabled exact test passed: 1
  passed, 0 failed, 1,435 filtered out. Three test invocations ran; no
  production change, broad suite or benchmark, and measurement usage was zero.
- Closure is bounded to the seven ordinary matching-close witnesses. Terminal/
  protected-close/fence variants, exact exit metadata, nested recovery, other
  Statement sequences, global Gate 1 and construction gates 2–5 remain open.

## Shared indented first-Statement transport reconciliation

- Mode: read-only M1 finite caller census, independent specification audit and
  M0 record synchronization; no implementer or repair round.
- The shared kernel has exactly 12 production call expressions: Assignment,
  Colon, With, If, CaseLike, For, Binding, Mod, Role, Impl, Act and Cast. Eleven
  already had bounded linked first-slot rows; Assignment alone remained
  without a dedicated link despite an existing five-case direct Rowan test.
- Independent audit confirmed that the Assignment evidence covers accepted
  Statement, EOF Missing `6..6`, terminal maximal Error `6..9`, retry with
  Statement-owned leading and protected `]` handoff, including exact ancestry,
  exits/remainders and fresh/frozen equality. AssignmentTail/block ancestry
  with direct preceding Equals selects `Assignment(IndentedStatement)`,
  singleton Statement expectation and primary zero before compatibility
  evidence.
- Catalog/coverage now link the final transport and close this bounded census
  at 12/12. This record-only step changed no code or tests and ran no test,
  broad suite or benchmark; measurement usage was zero.
- Later sequence slots, nested Statements, caller-local introducers, other
  boundaries/layout/fences, global Gate 1 and construction gates 2–5 remain
  open.

## BracedStatementBlock terminal Error-to-Close composition closure

- Mode: M1 with one Astra architect/pre-write audit, one Astra implementer and
  one independent post-write specification audit; one mechanical repair and
  one comment correction.
- Four non-fence witnesses now compose required-Statement Error `1..2` with the
  separate terminal closing-Brace Missing at `2..2` or horizontal-EOF `4..4`.
  Complete shell ancestry and direct order select
  `BracedStatementBlock(Statement)` then `ClosingDelimiter`, each with its
  singleton expectation and primary zero before records.
- EOF paths preserve full source, block-owned trailing whitespace, `InLine`,
  empty remainder and EOF payload. Protected `)`/`]` paths keep only `{@` in
  CST and retain exact leading, token, extent, `InLine` and `tail`. No extra
  Statement Missing, retry, separator or Invalid occurs; fresh/frozen trees,
  records and handoffs agree.
- The mechanical repair consumed the pending non-cloneable Item to inspect its
  leading. A separate comment correction removed a misleading role name.
  Fixtures and existing semantic expectations were unchanged.
- Final scoped rustfmt/diff and the wrapper-disabled exact test passed: 1
  passed, 0 failed, 1,435 filtered out. Four test invocations ran; no production
  change, broad suite or benchmark, and measurement usage was zero.
- This closure excludes the fifth quoted Yumark fence, which is mapped
  separately below, plus other boundaries/trivia, nested recovery, global Gate
  1 and construction gates 2–5.

## BracedStatementBlock quoted-Yumark-fence composition closure

- Mode: M1 with one Astra architect/pre-write audit, one Astra implementer and
  one independent post-write specification audit; one mechanical helper repair.
- The fifth terminal-prefix witness now derives owned CST `{@`, Statement Error
  `1..2` and closing-delimiter Missing `2..2` before compatibility records. The
  pending CRLF remains `2..4`, while the borrowed boundary/temporary record is
  at byte 4 and inspected quote/fence/newline facts occupy `4..10`.
- Exact boundary facts, pending Item equality/extents, `PhysicalStart`,
  remainder and source conservation are proved separately from Rowan range.
  Fence-aware fresh/frozen replay agrees without duplicate recovery, retry,
  separator, native close or Invalid.
- One mechanical repair replaced an ordinary leading emitter that rejects
  boundary Items with the existing terminal-boundary helper; structural and
  range expectations were unchanged.
- Final scoped rustfmt/diff and the wrapper-disabled exact test passed: 1
  passed, 0 failed, 1,435 filtered out. Three test invocations ran; no production
  change, broad suite or benchmark, and measurement usage was zero.
- Other fences/prefixes, outer Yumark construction, other braced callers,
  nested recovery, global Gate 1 and construction gates 2–5 remain open.

## BracedStatementBlock direct Statement Error caller reconciliation

- Mode: M1 with one Astra architect/pre-write audit, one Astra implementer and
  one independent post-write specification audit; no repair round.
- The shared braced kernel has six direct calls: one expression NUD plus Mod,
  Role, Impl, Act and For. NUD evidence was already mapped; the five non-NUD
  callers had only record compatibility evidence before this slice.
- Full caller/header/block structure now derives one direct maximal Error group
  with invariant `BracedStatementBlock(Statement)`, singleton Statement and
  primary zero. Native block content and matching close remain block-owned; no
  Missing, Invalid, retry, duplicate Error or caller-body recovery occurs.
- Mod/Role/Impl/Act retain EOF completion, while For retains `Ok(())`; all five
  assert `InLine`, full source, empty remainder and fresh/frozen tree/record/
  exit equality after structural projection.
- Final scoped rustfmt/diff and the wrapper-disabled exact test passed: 1
  passed, 0 failed, 1,435 filtered out. Three test invocations ran; no production
  change, broad suite or benchmark, and measurement usage was zero.
- Scope is one matching-close Error alternative per five non-NUD transports.
  Additional NUD variants beyond separately mapped rows, protected/fence,
  header/attachment, Missing/Separator/Close, nested recovery, global Gate 1
  and construction gates 2–5 remain open.

## Nested For-body BracedStatementBlock Error composition closure

- Mode: M1 with one Astra architect/pre-write audit, one Astra implementer and
  one independent post-write specification audit; no repair round.
- The existing `{for x in xs {@}; use a}` witness now proves complete nested
  Root/Statement/OperatorChain/outer-block/first-Statement/For/inner-block
  topology and the distinct accepted outer Use sibling after `; `.
- Outer direct recovery is zero; the one maximal inner Error `14..15` projects
  `BracedStatementBlock(Statement)`, singleton Statement and primary zero. No
  Missing, Invalid, duplicate outer recovery or caller recovery occurs.
- Inner/outer closes and separator retain native owners. Outer completion is
  EOF `24..24`, `InLine` and empty remainder; fresh/frozen trees, records and
  exits agree after structural projection.
- Final scoped rustfmt/diff and the wrapper-disabled exact test passed: 1
  passed, 0 failed, 1,435 filtered out. Three test invocations ran; no production
  change, broad suite or benchmark, and measurement usage was zero.
- This remains one nested For/Use composition only. Deeper recursion, malformed
  headers, other siblings, protected/fence exits, other block slots, global
  Gate 1 and construction gates 2–5 remain open.
