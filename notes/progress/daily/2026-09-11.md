# Daily progress — 2026-09-11

## BindingBody inline direct CST evidence

- Added direct Rowan proof for inline body after accepted `=`: `my x =` has a
  direct Body Missing, `my x = @ value` has direct Error followed by direct
  OperatorChain retry, and `my x = @;` retains its semicolon boundary without a
  second Missing. The controls assert parentage, source partition and no
  Invalid without consulting recovery records.
- The repaired proof fixes the enclosing direct sibling sequence to
  `BindingHeader, BindingBody`, requires the header's actual Equals, and shows
  `my x = @  ` emitting terminal EOF whitespace directly after Body Error
  without a Missing.
- The bounded row excludes target/equals admission, indented bodies, outer
  caller/fence and nested expression recovery. Independent catalog audit closed
  cleanly, so it is now an evidence-complete Draft; global projection and ledger
  retirement remain separate.

## DerivesClause ViaTarget direct CST evidence

- Added a direct Rowan-only control for the required raw Identifier immediately
  after `ViaKw`: `derives Eq via` gives a direct zero-width Missing, while
  `derives Eq via @ target` gives direct adjacent Error tokens followed by a
  direct Identifier retry. Both retain source, direct parentage, completion and
  no Invalid without examining recovery records.
- The trailing-space control fixes the CST insertion coordinate at `14..14` and
  leaves the two spaces on pending EOF outside the tree. This deliberately
  differs from the temporary legacy ledger's EOF-based range; future diagnosis
  derives from the Rowan occurrence.
- The test harness now gives that deliberately malformed CST control its own
  discard-only mode. Existing `Reject` controls retain their strict
  no-recovery assertion. Independent catalog audit closed cleanly; the bounded
  row is now an evidence-complete Draft. RoleReference, comma/caller/fence
  routes and global diagnostics/ledger migration remain separate.

## OperatorHeader required-slot phase evidence

- Added direct Rowan proof for the four ordered required header slots: Fixity
  Error then retry, Name Missing, binding-power Error then Equals, and
  DefinitionIntroducer safe-point Missing versus actual Equals. All use direct
  OperatorHeader Error/Missing children without Error spelling or recovery
  records as identity.
- Review required preservation of the pending safe-point leading before `body`;
  the focused control now proves that exact source partition and re-review
  closed cleanly. The resulting bounded catalog candidate reconstructs
  Fixity/Name/fixity-selected binding-power/DefinitionIntroducer only from the
  direct OperatorHeader child sequence, including Error retry and pending-Item
  phase transfer; independent catalog audit closed cleanly. The row is now an
  evidence-complete Draft. Header fact/conflict, Root body and global
  diagnostics/ledger migration remain separate.

## Root operator-header/body phase evidence

- Added direct Rowan proof for the previously open Root distinction between a
  malformed operator header that still contains an actual `=` and a header with
  no `=`. The first enters the direct Root operator-body Error/Missing phase;
  the second leaves the later run as Root trailing input. A terminal body Error
  then precedes its direct Root Missing, newline and independently nested Use
  recovery, while a completed operator body and a standalone OperatorChain are
  separated by their ordered header context rather than Error spelling.
- Focused test 1/1, format and diff checks passed; independent regression
  review was clean. This closes a missing prerequisite for a bounded raw-Root
  schema audit, but Root repetition/trivia grammar, full projection and global
  ledger retirement remain open.

## Root direct raw Error bounded candidate

- The bounded candidate now specifies direct Root Error groups by reconstructed
  sibling phase: Starter, Separator, accepted-statement trailing and
  operator-body. Actual `=` inside OperatorHeader is the CST-visible body
  selector; a completed body keeps OperatorDefinition trailing ownership.
  Native separators/trivia, nested recovery and terminal handoff are explicit
  exclusions or boundaries, never Error-spelling inputs.
- Independent catalog audit found and repaired initial-leading ownership and the
  direct InlineTrivia-versus-body Missing distinction, then closed cleanly.
  The bounded row is now catalog-audited evidence-complete Draft. It does not
  map full Root grammar, ordinary Root Missing, outer Yumark, global projection,
  parser API migration or ledger retirement.

## Root raw Error extent evidence

- Extended the partial source-root ordered-context evidence without changing
  parser behavior or CST topology. One direct Rowan control proves two distinct
  Root Error-token groups remain separated by a native semicolon and CRLF
  successor leading before an accepted next statement. A second proves ordered
  direct Error-token fragments and UTF-8 byte ranges through an opaque quoted
  malformed run, ending before an accepted `use` declaration.
- Focused Root evidence 2/2, format and diff checks passed; independent
  regression review was clean. No parser/API/SyntaxKind/benchmark change
  occurred. The catalog remains partial: LF/EOF/fence handoff, full
  trivia/repetition grammar, retry/separator progression, diagnostic projection
  and global ledger retirement remain open.

## Root raw Error private-cell boundary evidence

- Added a test-only active-prefix cell matrix for all 16 Root raw Error
  contexts, each across borrowed close, quote transition and EOF with LF/CRLF.
  It proves every Error leaf is directly owned by `YmYulangCodeCell`, excludes
  terminal leading/markers from the Error extent, returns the judged pending
  boundary byte-identically, and keeps markers outside the cell tree. The
  operator-body row also fixes its `OperatorDefinitionBody` record and direct
  recovered `value` child.
- Focused matrix 1/1, format and diff checks passed; a regression review found
  two test-coverage gaps, both repaired and clean on re-review. This is inner
  cell handoff evidence only: no outer Yumark document ownership, public fence
  hierarchy, API, parser topology or benchmark changed.

## Root raw Error retry and terminal-leading evidence

- Added five direct Root controls for semicolon retry, root-column CRLF retry,
  horizontal and indented-newline terminal Error runs, and EOF CRLF ownership.
  They fix the direct Rowan order/ranges, no-Invalid topology, and the retained
  non-lexical Starter record contract while deliberately wildcarding the
  spelling-derived legacy head.
- Focused control 1/1, format and diff checks passed. Regression review found
  the initial record assertion incomplete; it now covers all six ordered keyword
  expectations with role/range/source and primary index, and re-review is
  clean. This advances Root evidence only; full repetition/trivia grammar,
  projection and ledger retirement remain open.

## TypeCall CallArgument phase evidence

- Added direct Rowan phase controls for initial Missing, post-comma Missing,
  direct raw Error retry into TypeExpression, and accepted TypeExpression before
  terminal TypeCallClose. The observed retry `@` and its space are adjacent
  direct Error token leaves under TypeCallTail, not an Error/Invalid wrapper or
  native Unknown/Whitespace child; the focused control corrects that rejected
  prewrite assumption.
- Focused test 1/1, format and diff checks passed. An independent specification
  review is clean against T3, TypeCallClose and Error/Invalid authority. This is
  evidence for the bounded CallArgument catalog row only; separator, close,
  nested Type, global diagnostic projection and ledger retirement remain open.

## TypeCall CallArgument catalog row

- The bounded required-item row is now catalog-audited evidence-complete Draft:
  initial/post-comma Missing, direct Error retry, accepted TypeExpression and
  separately parented TypeCallClose are distinguished by direct TypeCallTail
  order. The audit corrected three documentation details: it is an item grammar
  rather than a complete tail grammar, initial leading stays outside Error, and
  fresh horizontal boundary leading advances the Missing frontier before the
  remaining Item is returned.
- No parser/API/SyntaxKind/test/benchmark change occurred in this record-only
  status promotion. Separator, terminal close beyond its separately mapped row,
  nested Type, global projection and ledger retirement remain open.

## TypeCall separator phase evidence

- Added direct CST/fresh-frozen controls for accepted comma and semicolon,
  inherited-ML omitted separator Missing after horizontal/LF/CRLF leading,
  CallArgument Error before an explicit comma, terminal-close residual Error,
  and doubled-comma item Missing. The residual form fixes three ordered close
  Error leaves/records under TypeCallClose while TypeCallTail itself has no
  direct Error child.
- Focused test 1/1, format and diff checks passed. Review initially found
  incomplete residual count/order and missing frozen coverage; both were
  repaired and re-reviewed cleanly. This is test evidence only; a bounded
  separator catalog row and its audit remain next, with no parser/API/SyntaxKind
  or benchmark change.

## TypeCall separator catalog row

- The bounded TypeCall separator row is catalog-audited evidence-complete Draft.
  It distinguishes accepted comma/semicolon, inherited-ML separator Missing and
  ordinary layout continuation without using Error spelling. Audit corrected the
  completed-item horizontal-boundary disposition: TypeCallTail emits ordinary
  gap leading before returning an advanced payload Item, whereas an abstract
  boundary keeps its whole gap pending.
- No parser/API/SyntaxKind/test/benchmark change occurred in this record-only
  promotion. CallArgument, TypeCallClose and nested Type remain separate rows;
  full TypeCall grammar, global projection and ledger retirement remain open.

## Forall first-binder phase evidence

- Added direct Rowan proof for the required binder immediately after `for`:
  empty first binder owns its direct Missing, while malformed `,@` is one raw
  Error-token group inside that first binder before literal colon/body retry.
  The comparison after an accepted binder demonstrates that the same comma text
  is structurally distinguished by its ordered position, without classifying a
  later BinderBoundary role.
- Focused test 1/1, format and diff checks passed. Regression review required
  the direct Missing child and full colon/body sequence, then closed cleanly.
  This is evidence only; the bounded catalog row, later binder boundary,
  colon/body, nested Type, global projection and ledger retirement remain open.

## Forall first-binder catalog row

- The bounded first-binder row is catalog-audited evidence-complete Draft. Its
  existing ForallTypeBinder wrapper distinguishes first Missing/Error before
  any accepted binder from later binder-boundary recovery. Audit corrected
  matching-local-close layout behavior, top-level-only retry and fresh versus
  recovered colon/binder leading; nested caller/contextual stops stay protected.
- No parser/API/SyntaxKind/test/benchmark change occurred in that record-only
  promotion. Later binders, colon/body, nested Type, caller continuation, global
  projection and ledger retirement remain open.

## Forall later BinderBoundary evidence

- Added direct Rowan controls for the later binder sequence only: a
  grammar-empty next binder contains direct Missing then SigilIdentifier; a
  separator is a one-item Error placeholder before either a leading-sensitive
  retried binder, an admitted literal colon, or the separately owned missing
  colon/body sequence. The source remains lossless and no Error node or Invalid
  is introduced.
- Focused test 1/1, format and diff checks passed. Audit found and corrected
  two contract gaps: the separator does not suppress a grammar-empty retried
  binder's distinct Missing, and an admitted literal colon does not publish a
  colon Missing. Re-audit then closed cleanly, so the bounded row is now
  catalog-audited evidence-complete Draft. First binder, colon/body, other
  malformed head content, nested Type, global projection and ledger retirement
  remain open.

## Forall terminal emitted-colon/body evidence

- Added direct Rowan controls for the terminal emitted-colon/body phase:
  accepted structured body, missing colon with body retry, missing body after
  colon, and raw Error retry on each side of the colon. The raw Error token is
  direct ForallType content; its ordered position relative to `Colon`, not its
  text, selects colon versus body.
- Focused test 1/1, format and diff checks passed; regression review and
  independent catalog audit were clean. The bounded catalog row is now a
  catalog-audited evidence-complete Draft. A subsequent lexical audit confirmed
  that ordinary and polymorphic-variant colon both emit the same Rowan `Colon`,
  so they stay one CST row rather than retaining a nonexistent lexical
  distinction. Recovered binder, nested Type, caller handoff, global projection
  and ledger retirement remain open.

## Shared named declaration-field head direct CST evidence

- Added direct Rowan-only controls for the shared required Name and Colon
  phases of `StructField`. Five ordered shapes distinguish Name Missing/Error
  before direct Colon from Colon Missing/Error after direct Identifier, and
  prove Colon and Type retries without duplicate Missing.
- The matrix runs through named-brace and UTF-8/CRLF indented Struct fields plus
  actual Enum/Error named variant payloads. A separate dedent control leaves
  the CRLF and successor pending after terminal Colon Error. Assertions use
  immediate Rowan children, byte ranges, source losslessness, Error tokens,
  Missing nodes and absence of `Invalid`; they never inspect recovery records.
- Three focused tests passed. Independent specification audit found no CST
  collision. Accepted record findings corrected both payload ancestors to
  `EnumVariant` under `EnumDeclaration`/`ErrorDeclaration`, initial malformed
  Name leading to the sequence before field entry, and initial malformed Colon
  leading to direct field content. The Colon phase ends at actual Colon or
  Type retry; the entire required-Type RHS is excluded, including direct
  Type-owned Error leaves before optional `TypeExpression`.
- Delta specification audit closed all three record findings. The bounded row
  is now a catalog-audited evidence-complete Draft. Fresh FieldItem,
  separator/close, tuple fields,
  required-Type RHS/nested Type, global projection and ledger retirement remain
  open. Record-only repair verification: `git diff --check` passed; unchanged
  tests were not rerun. Benchmark use was zero.

## Fresh named declaration FieldItem direct CST evidence

- Added direct Rowan-only controls for the shared named field sequence at its
  fresh NeedField phase. Struct, Enum and Error witnesses distinguish terminal
  `StructField(Missing)` and bare `StructField(Error)` from a separately
  admitted `StructField(Identifier, Colon, TypeExpression)`.
- The malformed-start retry fixes the outer sequence order as Error field,
  native whitespace, independent Separator Missing, then the accepted field.
  That Missing is the retried field's ordinary separator obligation, not a
  duplicate FieldItem recovery. A UTF-8/CRLF indented control leaves the
  dedented successor and its leading pending without Missing or `Invalid`.
- Three focused tests passed, format and diff checks passed, and independent
  test audit found no CST collision or topology decision. The bounded
  evidence-complete Draft is bounded to NeedField comma Missing,
  malformed-start raw-name retry and terminal indented dedent. The accepted
  catalog admission finding was repaired: boundary/close/separator dispatch
  precedes malformed-start admission, so semicolon and foreign close retain
  their owners. `Native* Missing` remains a possible shape; other required-
  absence positions and EOF/active-stop variants remain outside this evidence.
  Catalog delta audit closed the admission finding, so this is an
  evidence-complete Draft for the bounded witnessed alternatives. Tuple
  FieldItem, FieldSeparator,
  closes, RHS Type, global projection and ledger retirement remain open.
  Benchmark use was zero.

## Fresh tuple declaration FieldItem Missing direct CST evidence

- Added direct Rowan-only controls for tuple NeedField Missing across Struct,
  Enum and Error. Initial comma and post-comma EOF produce the nested
  `StructField(TypeExpression(Missing))`; terminal field Missing precedes the
  separate local-close and variant outer-close Missing at the same byte offset.
- Matching `)` after a trailing comma creates no field. `=T` instead produces
  direct Type-owned Error followed by TypeExpression, and `T U` remains one
  field with one TypeApplyArgument. The controls assert exact sequence order,
  zero-width empty nodes, ranges, source losslessness, ancestor selection and
  no `Invalid` without inspecting recovery records.
- Two focused tests passed, format and diff checks passed, and independent test
  audit found no CST collision. The catalog admission wording now limits
  required Type to candidates admitted after boundary/close/comma/separator
  dispatch. Catalog delta audit closed that finding, so this is a
  catalog-audited evidence-complete Draft for the witnessed alternatives.
  Other absence/leading/active-stop/fence, Type, separator/close, global
  projection and ledger retirement remain open. Benchmark use was zero.

## Declaration field Separator/Close CST collision

- Added a direct counterexample proving four Struct field-list pairs have the
  same complete Rowan kind/range/node-token projection when only Error spelling
  is opaque, while temporary recovery records retain distinct Separator/Close
  roles or one-run/two-run partitions. The control uses records only to prove
  information loss; they are not a future interpreter input.
- Compiler/recovery review confirmed that the old topology blocked a general
  FieldSeparator Error row and ledger retirement. FieldSeparator Missing before
  an admitted field was already structurally distinct.
- Architecture selected the minimal malformed-only repair: one transparent
  `StructFieldForeignClose(Error+)` around each existing maximal Struct
  delimited Recover run. Specification and compiler/recovery pre-approval
  reviews are clean. The user approved option B on 2026-09-11, including its
  one-wrapper-per-run cardinality and narrow supersession. The Authoritative
  gate preserves accepted CST, Missing, records/frozen replay, leading/handoff,
  Enum/Error Borrow and indented Struct. Construction appended raw kind 278 and
  wraps one unchanged maximal Struct delimited foreign-close run. Declaration
  (294), SyntaxKind (3), normalized (83), recovery-output (26), package check,
  format and diff checks passed. Compiler/recovery review was clean; one
  specification test-evidence finding was repaired with real indented and
  direct protected-fence controls, and delta review closed. Benchmark use was
  zero.

## Shared declaration FieldSeparator direct CST evidence

- The direct named/tuple Struct/Enum/Error controls cover the zero-width
  FieldSeparator Missing before an admitted same-line field, malformed raw
  separator behavior, accepted comma/newline, retry and active-stop handoff.
- The catalog maps only the structurally unique direct sequence Missing:
  `StructField Native* Missing StructField`, with owner-selected Struct or
  variant role and `DelimitedSequenceSeparator`. Fresh FieldItem and nested
  field/Type recovery remain independent.
- FieldSeparator Error is not mapped from the current CST. The opaque-spelling
  collision proved that its direct Error group was indistinguishable from
  Struct foreign-close recovery before `StructFieldForeignClose`. The completed
  topology now separates those occurrences, but their separate Error/Close
  catalog rows remain open. Independent Missing-row audit closed after
  correcting the projection input wording and the Missing-emitter locator. The
  bounded Missing row is now catalog-audited evidence-complete Draft. Benchmark
  use was zero.

## Declaration FieldSeparator Error and Struct foreign-close schema evidence

- Added a direct Rowan control for the retained
  `struct S:\n  x:T;\n  y:U` literal. It proves a real colon-introduced indented
  field list with the semicolon as one direct Error token between two direct
  StructField children; retry newline/indent remains native sequence content.
  The exact test, format and diff checks passed, and pre-write plus closure
  specification audits are clean.
- Candidate catalog rows now keep the two raw classes separate: maximal direct
  field-list `Error+` projects owner-selected FieldSeparator, while one Error
  group inside `StructFieldForeignClose` projects the Struct opener-selected
  local Close and the wrapper itself projects nothing. Both avoid Error spelling
  and parser records.
- Independent catalog audit found the foreign-close row clean and one
  Separator admission overstatement. The repaired row limits matching/foreign
  close stops to delimited lists and records that indented lists have no local
  close; delta audit closed. Both rows are now catalog-audited
  evidence-complete Drafts. FieldSeparator Missing, Close Missing, accepted
  close, outer variant close, the global interpreter and ledger retirement
  remain separate. Benchmark use was zero.

## Declaration field-list local Close Missing EOF evidence

- Added direct Rowan controls for empty Struct and actual Enum/Error variant
  named/tuple lists at EOF, plus accepted matching-close controls. Empty lists
  end with direct `Open Missing`, fabricate no field and preserve source/EOF;
  accepted lists have no Missing/Error/Invalid.
- Strengthened retained tuple post-comma EOF controls to fix equal-coordinate
  preorder as fresh `StructField(TypeExpression(Missing))`, local field-list
  Close Missing, then any outer declaration Close Missing. Parent/ancestor and
  opener select the four local roles without recovery records.
- Focused empty-list (2) and fresh-tuple (2) tests, format and diff checks
  passed; pre-write, closure and catalog specification audits were clean. The
  bounded row is now a catalog-audited evidence-complete Draft. Borrow, active
  stop/fence, nonempty leading, outer-close schema, global interpreter and
  ledger retirement remain separate. Benchmark use was zero.

## Declaration field-list local Close Missing boundary extension

- Added direct CST-only Borrow controls at the isolated variant field-list
  return for Enum and Error named/tuple payloads. Local Missing is the terminal
  direct EnumVariant child; the foreign close and its leading remain pending.
  One tuple witness retains ` \r\n ` outside the tree.
- Added Struct controls for a completed tuple field at active colon stop and an
  empty tuple list at a protected fence. Both emit one direct local Missing and
  retain exact boundary, leading and remainder. Rowan zero-width ranges and
  pending coordinates are asserted separately; records are discarded.
- The initial test pass exposed only an unsupported direct-Root assertion.
  Accepted CST evidence selected `StructDeclaration > Statement > Root`; one
  repair changed only that assertion. The three focused tests passed and
  independent delta review closed. Independent catalog audit was clean, so the
  expanded Close Missing row is a catalog-audited evidence-complete Draft.
  Post-comma/qualifying-newline protected boundaries, other leading
  combinations, outer-close schema, interpreter and ledger retirement remain
  separate. Benchmark use was zero.

## Braced Enum/Error variant-body outer Close Missing evidence

- Added shell-level direct Rowan matrices for Enum and Error: empty braced EOF,
  post-variant EOF, accepted RBrace and foreign RParen with CRLF leading. The
  terminal Missing is empty and direct under the declaration; accepted close
  emits no recovery; foreign Item/leading stays pending.
- Local payload Close Missing remains under EnumVariant, so nested fresh field,
  local payload and outer body Missing retain structural preorder even at one
  coordinate. Both shells select the shared `EnumBracedVariantBody/Brace` role;
  this owner has no Close Error path.
- The two focused tests, format and diff checks passed. Independent review found
  two minor evidence gaps; one repair added direct Root parentage and exact
  `Err(Left(Item))` handoff, and delta review closed. Independent catalog audit
  closed after correcting the empty-body wording. The row is now a
  catalog-audited evidence-complete Draft.
- Extended the same row through active Colon, protected quoted fence and one
  trailing-comma fence for both Enum/Error shells where applicable. The active
  witness exposed accepted positional/FromType payload admission consuming a
  caller stop before the sequence could publish its outer Close Missing.
- Repaired the owner path with an immediate variant-scoped Type boundary. The
  scope remains visible at outer fresh-primary, malformed-retry and completed
  tail safe points, but is suspended in nested Type episodes. A selective
  provenance component preserves established ordinary Arrow RHS boundaries;
  the rejected blanket `NONE` repair was caught by four unchanged Arrow tests.
- Focused scoped-boundary tests, shared declaration-variant (33), Arrow (46),
  outer-close (4), Enum (17), Error (16), package check, format and diff checks
  passed. Final compiler and regression reviews were clean. The catalog row is
  expanded through these protected handoffs; other leading combinations,
  non-braced forms, remaining nested variant slots, the global CST interpreter
  and ledger retirement remain separate. Benchmark use was zero.

## CST diagnostic atomic-cutover readiness audit

- Recomputed the current runtime dependency graph. The parser recovery ledger
  still has 41 production grammar producer modules and feeds Header recovery,
  frozen root reconciliation, `RootCandidate`, `SyntaxDiagnostic` and
  `ParsedFile::diagnostics`. No production crate outside `yu-syntax` currently
  consumes that diagnostic API.
- Confirmed that `yu-hir` and `yu-types` do not yet implement the future
  mandatory CST traversal. The first executable interpreter therefore belongs
  in `yu-syntax` as a total whole-tree collector plus a reusable local
  occurrence callback. `ParsedFile` will eventually retain the selected
  environment/effective operator table rather than a parser-built diagnostic
  array.
- The 123 recovery-publication source sites are a navigation census, not a
  semantic-slot denominator. Atomic removal still requires a complete audited
  ordered-CST projection; a partial dispatcher would silently lose nested or
  delegated diagnostics.
- Two information-loss blockers are proved and now have user-approved
  Authoritative construction authorities (2026-09-11):
  NamedRecordType's `{([)]:A}` merges Field and Close recovery into one direct
  Error run, while PolymorphicVariantType's `:{;}` and `:{]}` give identical
  direct Error topology for Separator and foreign Close. NamedRecordType's M2
  construction is now complete; PV remains its own next, unstarted M2 gate.
- BracedStatementBlock's direct Missing examples establish a reused parent
  kind, but ordered siblings may still distinguish the occurrences. Treat it
  as an evidence gate, not an approved topology blocker, until that decoder or
  a true ordered-context collision is proved. No tests or benchmark processes
  were run for this read-only readiness audit.

## NamedRecordType recovery-slot topology construction

- Recorded the user's 2026-09-11 approval as Authoritative, then completed the
  private M2 construction of `NamedRecordTypeSeparator` and
  `NamedRecordTypeClose` (appended kinds 279/280). Separator wrappers occur
  only for the existing Separator Missing/Error publications; exactly one Close
  wraps the accepted or recovered terminal close of every committed record.
  Field/Name recovery, accepted comma ownership and recovery records remain
  unchanged.
- The direct `{([)]:A}` proof now separates the direct Field Error from the
  Close Error group structurally. Accepted, EOF, shifted/frozen, caller/outer
  close and quoted-fence controls cover leading, range and pending-Item
  behavior. A required committed-Close fence witness exposed an old local panic
  from asking `token_kind` of an abstract boundary; a boundary guard fixes that
  owner defect while preserving its normal EOF, accepted-close and handoff
  paths.
- Pre-write specification/compiler reviews, closure review and delta review
  closed clean. Focused `record_` tests (86), the Type-expression module (250),
  sparse SyntaxKind round-trip and `cargo check -p yu-syntax` passed. The check
  retains three unrelated existing warnings. Benchmark use was zero.

## PolymorphicVariantType foreign-close topology construction

- Completed the separately approved private M2 gate. Appended
  `PolymorphicVariantForeignClose` as kind 281, enclosing exactly the existing
  locally consumed foreign-close Error emission; direct semicolon Separator
  Error, TagPosition, records, ranges, leading and handoff remain unchanged.
- The old `:{;}` / `:{]}` same-shape witness now proves the intended structural
  distinction. Position, repeated/mixed grouping, UTF-8/CRLF, tail, caller and
  quoted-fence tests preserve fresh/frozen records. `else` remains an admitted
  tag name after a foreign close, so the new protected-caller witness uses
  `:` rather than altering owner dispatch.
- Pre-write specification/compiler and closure reviews were clean. Focused PV
  (23), polymorphic-variant (19), sparse-kind (1) and Type-expression module
  (252) tests passed; package check is recorded with this gate. Benchmark use
  was zero.

## BracedStatementBlock and Root ordered-CST evidence

- Strengthened the three retained BracedStatementBlock Missing witnesses with
  every direct child kind and node/token distinction, plus selected Missing,
  leading and close ranges and terminal position.
  A following `BlockStatementSeparator` identifies required Statement, a
  following `Statement` identifies missing Separator, and no later child
  identifies local Close. Nested `for` continuation retains the accepted outer
  RBrace and the same Separator pattern. Focused exact and module tests passed
  1 and 9; independent specification review was clean.
- This evidence refutes the ancestry-only premise of the unapproved
  `BracedStatementBlockClose` Draft. The Draft and INDEX now mark that topology
  rejected as unnecessary for these bounded slots; no SyntaxKind or production
  behavior changed.
- Strengthened the Root 16-role matrix to retain complete direct native tokens,
  node/token identity and byte ranges. Semicolon-separated raw runs remain
  separate, UTF-8 opaque fragments coalesce only while physically adjacent,
  actual OperatorHeader Equals selects body, and InlineTrivia/Body Missing stay
  distinct by ordered children. Focused exact and Root-module tests passed 1
  and 17; independent audit was clean.
- The Root and Braced rows are catalog-audited evidence-complete Drafts only for
  these bounded occurrences. Remaining Root terminal/fence phases, braced Error
  and boundary variants, nested owners, the global interpreter and ledger
  retirement remain open. Benchmark use was zero.

## Declaration Variant core ordered-CST evidence

- Added a bounded M1 direct-Rowan proof for the shared Variant core under full
  Enum and Error declaration ancestors. `EnumVariant(Missing)` and terminal
  `EnumVariant(Error+)` are Item; direct `Error+ Native* Identifier` retry is
  Name; a declaration-child Missing between completed `A()` and `B` is
  Separator. The completed payload prevents a positional-Type ambiguity.
- The test covers braced, inline Equals, indented Colon and indented Equals
  forms, UTF-8/native leading, terminal Error and zero-width Missing. Existing
  record/frozen controls remain unchanged and were not used to classify slots.
  Focused declaration-variant tests (34), scoped format and diff checks passed;
  no benchmark process was used.
- Pre-write and closure specification audits were clean. The catalog and source
  coverage manifest now mark only these witnessed alternatives as a
  catalog-audited evidence-complete Draft. Payload Type/field/local-close,
  outer close, terminal handoff, other separator forms, global projection and
  ledger retirement remain open.

## Declaration Variant `FromType` Missing ordered-CST evidence

- Added the bounded M1 full-shell proof for a required Type immediately after
  direct `from`. Both Enum and Error shells prove the native initial Variant
  leading, Identifier/FromKw/TypeExpression ordering and exactly one empty
  `TypeExpression(Missing)` at EOF and at an active Colon handoff.
- The active-Colon witnesses retain the exact Colon Item, pending
  ` /*pending*/ ` leading and ` tail` remainder. Their source-local Rowan
  ranges derive from the accepted prefix; classification reads no recovery
  record. Focused FromType evidence (1), scoped format and diff checks passed;
  no benchmark process was used.
- Pre-write and closure specification audits were clean. This is a
  catalog-audited evidence-complete Draft only for EOF/active-Colon Missing.
  FromType Error/retry, other boundaries, positional payload, fields/closes,
  global projection and ledger retirement remain open.

## Declaration Variant `FromType` terminal primary ordered-CST evidence

- Added the bounded M1 proof for terminal `from@` at EOF under both full Enum
  and Error shells. The direct `FromKw → Error` suffix is Type(Primary), not
  Variant(FromType): only fresh absence receives the caller role.
- The direct Rowan test retains source-local Error range, full ancestry and
  native token order, while excluding TypeExpression/Missing/Invalid. It reads
  no recovery record. Focused declaration-variant tests (36), scoped format
  and diff checks passed; no benchmark process was used.
- Pre-write and closure specification audits were clean. This is a
  catalog-audited evidence-complete Draft only for terminal EOF; retry, caller
  boundaries, trailing trivia, other forms, positional payload, fields/closes,
  global projection and ledger retirement remain open.

## Declaration Variant `FromType` primary-retry ordered-CST evidence

- Added the bounded M1 direct Rowan proof for `from @ T` under full Enum and
  Error shells. The direct malformed primary remains Type(Primary); its retry
  leading belongs inside the succeeding TypeExpression and not in Error.
- The test asserts the full direct child order, token/node identity and
  source-local Error/TypeExpression ranges, without recovery-record input.
  Focused declaration-variant tests (37), scoped format and diff checks passed;
  no benchmark process was used.
- Pre-write and closure specification audits were clean. This is a
  catalog-audited evidence-complete Draft only for this admitted retry;
  terminal/boundary/trivia/forms, nested Type, other payload rows, fields,
  global projection and ledger retirement remain open.

## TypeAnnotationTail required-Type Missing ordered-CST evidence

- Added the bounded M1 direct Rowan proof for exact `x as` at EOF and before a
  protected RBracket. `AsKw → TypeExpression(Missing)` is direct tail content;
  pre-tail whitespace remains at OperatorChain and the protected close/leading
  remains pending.
- The test fixes token/node order and zero-width range without recovery-record
  input. `as @` remains a delegated Type(Primary) control, not an annotation
  Error. Focused structural-tail tests (10), scoped format and diff checks
  passed; no benchmark process was used.
- Pre-write and closure specification audits were clean. This is a
  catalog-audited evidence-complete Draft only for those two Missing paths;
  Type Error/retry, accepted Type, other boundaries/tails, global projection
  and ledger retirement remain open.

## Pattern SymbolName Missing ordered-CST evidence

- Added bounded M1 direct Rowan proof for `:` at EOF and `: x` with a pending
  Identifier. The direct `SymbolPattern(Colon Missing)` structure selects
  Pattern(SymbolName); the lexical PatternSymbolColon distinction remains the
  same emitted Colon token.
- The test retains token/node order, zero-width range and pending leading
  without recovery-record input. Focused pattern proof (1), scoped format and
  diff checks passed; no benchmark process was used.
- Pre-write and closure specification audits were clean. This is a
  catalog-audited evidence-complete Draft only for these two witnesses; broader
  Pattern recovery, global projection and ledger retirement remain open.

## Pattern AliasBinding ordered-CST evidence

- Added bounded M1 direct Rowan proof for immediate `A as` Missing, `A as @ x`
  Error/retry and terminal `A as @` Error. Direct PatternAliasTail children
  keep pre-`as` whitespace outside the tail and initial/retry leading outside
  Error.
- The test fixes parentage, token/node order, ranges and terminal cardinality
  from CST only. Focused Pattern recovery tests (35), scoped format and diff
  checks passed; no benchmark process was used.
- Pre-write and closure specification audits were clean. This is a
  catalog-audited evidence-complete Draft only for the three direct witnesses;
  boundary/layout variants, other Pattern rows, global projection and ledger
  retirement remain open.

## TypeDeclaration DefinitionIntroducer ordered-CST evidence

- Added bounded M1 direct Rowan proof for the completed-name/no-parameter
  DefinitionIntroducer phase. It distinguishes `type T (A)` Missing,
  terminal `type T @ ` Error, and `type T @ = A` Error-to-exact-Equals retry.
- The test fixes direct child order, leading ownership, ranges and no-cascade
  behavior without recovery-record input. Focused TypeDeclaration proof (1),
  scoped format and diff checks passed; no benchmark process was used.
- Pre-write and closure specification audits were clean. This is a
  catalog-audited evidence-complete Draft only for those three forms;
  parameters, nominal forms, `==`/`=>`, RHS internals, global projection and
  ledger retirement remain open.

## CST schema completion-gap audit

- Re-ran the four-emitter census: it remains 123 sites in 39 files, but it is
  not a complete ledger-retirement inventory. `literal/rule_literal.rs` has
  four indirect `emit_rule_missing` publications outside that census and enters
  shared Rule helpers under a distinct RuleLiteralInterpolation ancestor.
- The audit also found stale `type_expr/mod.rs` links in the non-authoritative
  coverage manifest. No status was promoted or changed from locator text alone.
  The manifest now records both gaps explicitly; they block a complete-schema
  claim until mapped/revalidated. No compiler code, tests or benchmarks changed.

<!-- daily-append-anchor: 2026-09-11 -->

## NamedRecordType six-slot ordered-CST evidence

- Added direct Rowan-only evidence for the existing six recovery slots:
  whole Field, field Name, field Colon, required field Type, Separator and
  committed Close. Ordered native/`Missing`/raw-`Error` children distinguish
  them without recovery records, Error spelling or a parallel parser state.
- The witnesses retain error-to-Type retry without fabricated Colon/Type
  Missing, nested Type ownership, the `{([)]:A}` Field/Close split, the fresh
  semicolon Field/Separator split, terminal close cardinality and equal-offset
  nested Close occurrence paths. The latter deliberately uses ancestor paths,
  not Rowan green-node or range equality.
- Focused record tests (22), the Type-expression module (254), scoped format,
  diff and `cargo check -p yu-syntax` passed; the package check retains three
  unrelated existing warnings. M2 post-write specification and recovery audits
  were clean. This is a catalog-audited evidence-complete Draft only for the
  six witnessed NamedRecordType rows; wider Type coverage, the global CST
  collector and recovery-ledger/API migration remain open. Benchmark use was
  zero samples/processes.
