# Daily progress — 2026-09-27

## F5c post-R boxed physical lanes

Post-R physical-lane accounting now covers boxed production and FlatDraft
candidate collections. Boxed retained bounds have a slot-size-specific lane;
post-R owner/trace sets, recursive owners/set, occurrence order/seen,
Q/R maps, and positive/negative elimination sets use fallible capacity
accounting. Spare-capacity map/set insertions still count requested slots and
skip reserve calls. Some occurrence/elimination admissions still recompute the
fixed memo-capacity summary per novel item; §15 must assess this fixed
per-entry cost. The retained-bound lane releases immediately after its map
drops. The returned recursive-bound payload remains part of the
later source-draft/co-resident ledger.

M2 spec/performance review found and closed the retained-map lifetime mismatch
and missing live-capacity/error-retry evidence. The focused test samples all
four temporary collections while live against their recorded capacities,
fails at Q-map admission after earlier lanes grow, checks idle cleanup, then
retries with unchanged Q/R order. Focused checks passed:
`RUSTC_WRAPPER= cargo check -p yu-solver --tests --message-format short`,
`RUSTC_WRAPPER= cargo test -p yu-solver --lib post_r_ -- --test-threads=1`
(4), `cargo fmt --all --check`, and `git diff --check`. No broad suite,
resource probe, benchmark, or timing measurement ran.

This closes only the post-R temporary-lane slice. Next close boxed normalizer
failed-reserve observation and the solver/draft/finalizer same-time ledger,
then independently review a fresh §15 plan before any probe. Production stays
boxed; no numeric boundary, cutover, §44, F5e, or overall F5c acceptance is
claimed.

## F5c shared R fixed-point physical lanes

Six typed lanes now account the R candidate set, per-round previous set,
surviving bounds, reachable owners, frontier Vec, and per-owner referenced
set. Candidate storage remains live through post-R selection; round-local
collections drop before lane release on success/error. Boxed and FlatDraft
reference discovery share physical-set insertion accounting. A focused live
witness compares actual capacities to all six reported lanes during
co-residency and covers transient cleanup, reserve failure, rollback, and
retry.

The M2 spec-auditor/performance-auditor review found a major stale copy charge:
`previous` and the initial frontier had been charged before fallible insertion.
The repair now charges each copy after storage admission and just before its
insertion. A failure after the first copy charges only that completed copy;
fresh spec delta review closed the finding. Performance review also noted the
existing `candidates == previous` comparison is an unmetered linear pass. It is
a minor bounded pass alongside charged copy and owner-scan work; include it in
the later full §5 work-accounting reconciliation.

Focused checks passed after repair: solver test-target check, tree-analysis
tests (4), flat-walk tests (36), work-meter tests (13), R parity/lane witness
(1), formatting, and diff checks. No broad suite, probe, benchmark, or timing
measurement ran. Next close post-R selection lanes, then boxed normalizer
failed-reserve observation and the solver/draft/finalizer same-time ledger.
Independent physical-lane reconciliation and a fresh §15 plan review remain
required before any probe. Production stays boxed; no numeric boundary,
cutover, §44, F5e, or overall F5c acceptance is claimed.

## F5c raw owner-loop physical lanes

Six typed lanes now account `build_inner_work`'s raw bounds map,
completed-owner set, owner-to-trace map, aggregate nested trace-index vectors,
and positive/negative-only sets. The map and nested vectors are distinct
physical lanes; capacities stay live through R selection and release only
after the local collections drop. `raw_owner_order` remains accounted by its
existing scratch slot 3. The four-vector `VariableBounds` clone is gone; its
two lower/upper emptiness facts are read directly from the frozen session row.

M2 spec-auditor and performance-auditor review identified a major stale work
charge in both boxed and flat paths: bound endpoints were charged as copied
after the clone had been removed. Two focused repair rounds removed both
precharges, and fresh spec delta review confirmed the shared walkers retain
actual endpoint-visit charges. Focused checks passed: solver test-target check,
transaction tests (15), flat-walk tests (36), work-meter tests (13), boxed/flat
Q/R/normalization parity (1), formatting, and diff checks. No broad suite,
probe, benchmark, or timing measurement ran.

The live-capacity test reconstructs all six lanes from physical collections
and covers a nested-index reserve failure, rollback, and retry. Remaining are
R fixed-point/post-R lanes, boxed normalizer failed-reserve observation, and
the solver/draft/finalizer same-time ledger. Independent full-ledger review
and fresh §15 plan review remain open.

## F5c persistent producer-state lanes

Seven remaining persistent `F5cGeneralizer` states now use typed lanes in the
existing `F5cWalkerResources`: uncacheable and provisional-row sets, path,
ordered rows and its seen set, outer guarded reentries, and aggregate nested
trace-path capacities. Reserves are fallible; actual capacity is observed after
reserve success or failure. A trace copy's peak includes its live source path
and all retained trace paths. Component cleanup drops each collection before
releasing its lane and preserves request, growth, and peak history.

M2 compiler-referee and performance-auditor reviews found no blocking or major
issue. The performance review identified a bounded evidence gap: the focused
aggregate test partly sums the mirrored lane ledger instead of independently
enumerating every physical collection. This remains a required check in the
later full §15 ledger and is recorded as open. Focused checks passed:
`RUSTC_WRAPPER= cargo check -p yu-solver --tests`, transaction tests (14),
work-meter tests (13), flat-walk tests (36), nested-path test (1),
`cargo fmt --check`, and `git diff --check`. No broad suite, benchmark, or
resource probe ran.

This closes persistent producer state only. Remaining are R fixed-point and
post-R locals, boxed normalizer failed-reserve observation, and
solver/draft/finalizer same-time peak. After those owners close, prepare and
independently review a fresh §15 plan before probing.

## F5c closure and raw-incidence resource lanes

The first solver-side physical-lane slice now accounts the `non_generic_closure`
adjacency Vec, aggregate nested-neighbor capacities, reusable connected set,
returned closure set, and frontier, together with positive/negative raw-incidence
sets. The seven lanes extend the existing private `F5cWalkerResources` owner.
Every actual growth uses fallible reserve and records the post-reserve capacity
and memo co-peak before propagating the reserve result. Cleanup releases lanes
after their collections drop; boxed and flat raw-incidence paths share the same
set accounting.

Duplicate insertion attempts remain in requested-slot counts. Spare-capacity
inserts do not perform a separate membership search; at full capacity, one
novelty check prevents duplicate-triggered growth. A full-table novel value
recomputes memo retention, reserves fallibly, observes its actual capacity, and
then inserts.

The M2 compiler-referee review found no actionable issue. The first
performance-auditor review's HashSet-control-byte concern was rejected under
F5 §34, which excludes allocator metadata/control bytes from the slot model.
Its duplicate-growth finding was accepted and repaired. A follow-up found and
closed repeated novelty probes on full tables. Fresh delta reviews closed both
repairs without further findings.

Checks passed: `RUSTC_WRAPPER= cargo test -p yu-solver --lib
f5c_flat_walk_sink -- --test-threads=1` (36), `RUSTC_WRAPPER= cargo test -p
yu-solver --lib f5c_non_generic_closure -- --test-threads=1` (2),
`RUSTC_WRAPPER= cargo check -p yu-solver --lib --message-format short`,
`cargo fmt --all --check`, and `git diff --check`. The lib check retains one
pre-existing dead-code warning for `F5cPostRSelection::{retained_bounds,
retained_predicate}`; that warning is assigned a separate M0 cleanup. No broad
suite, resource probe, or benchmark ran.

This closes only the closure/raw-incidence lane slice. Remaining physical
owners are persistent generalizer state and nested reentry paths, other
`build_inner`/R-selection locals, boxed normalizer failed-reserve observation,
and all-draft/finalizer same-time peaks. Next, close those in bounded slices,
then prepare and independently review the fresh §15 plan before any probe.
Production remains boxed; no numeric boundary, cutover, §44 closure, F5e
acceptance, or overall F5c completion is claimed.

## F5c production-only warning cleanup

The `yu-solver --lib` check exposed two pre-existing `F5cPostRSelection`
fields used only by the test-only FlatDraft fixture path. Field-local
`cfg_attr(not(test), allow(dead_code))` now records that configuration split
at the owning fields without changing runtime behavior.

`RUSTC_WRAPPER= cargo check -p yu-solver --lib --message-format short`,
`cargo fmt --all --check`, and `git diff --check` pass; the package check is
warning-free. This M0 cleanup is separate from the closure/raw-incidence lane
commit and introduces no new resource or semantic decision.
