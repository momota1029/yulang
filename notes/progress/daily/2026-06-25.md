# 2026-06-25

## Control VM handler hygiene

`tests/yulang/yulang-adversarial-corpus/02_repeated_callback_hygiene.yu` exposed
a control-vm/interpreter mismatch:

```text
control-vm:  run roots [0]
interpreter: run roots [6]
```

The mono output already carried the callback adapter evidence:

```text
hygiene[add_id[0, tick, own]]
```

So the failure was in runtime guard transport, not in inference or
specialization.

The root cause was in `crates/control-vm/src/runtime/flow.rs`.
When the VM created a carried guard for an adapter marker, it treated every
handler frame outside the marker entry as already exposed. In nested same-path
handlers this exposed the inner `strip_tick` handler too early, so it was
allowed to capture a callback-origin `tick::ping`.

The interpreter already used the narrower rule: expose a matching handler alias
only when an earlier guard at the marker entry proves that alias is visible.
The VM now mirrors that rule and only exposes the outermost matching handler in
that alias case.

Added regression coverage:

```text
cargo test -q -p yulang run_control_source_text_with_embedded_std_keeps_repeated_callback_hygiene -- --test-threads=1
```

Manual checks after the fix:

```text
./target/release/yulang run --no-cache --print-roots tests/yulang/yulang-adversarial-corpus/02_repeated_callback_hygiene.yu
./target/release/yulang run --no-cache --print-roots --interpreter tests/yulang/yulang-adversarial-corpus/02_repeated_callback_hygiene.yu
tests/yulang/yulang-adversarial-corpus/probe.sh
```

Both runtimes now return `run roots [6]`, and the adversarial probe passes.

## Evidence-only replay skip

After the VM hygiene fix, the evidence-only replay smoke was meaningful again.
Both skip limits 16 and 32 passed:

```text
env YULANG=./target/release/yulang YULANG_REPLAY_EVIDENCE_ONLY_SKIP_LIMIT=16 scripts/evidence-only-replay-smoke.sh
env YULANG=./target/release/yulang YULANG_REPLAY_EVIDENCE_ONLY_SKIP_LIMIT=32 scripts/evidence-only-replay-smoke.sh
```

On `examples/showcase.yu`, limit 16 was slightly faster in the latest release
measurement:

```text
default: infer 519.4ms, total 582.0ms
limit 16: infer 425.3ms, total 490.0ms
limit 32: infer 440.6ms, total 506.0ms
```

The opt-in default for `YULANG_REPLAY_EVIDENCE_ONLY_SKIP_LIMIT` was moved from
32 to 16. The optimization remains env-gated; ordinary solver behavior is
unchanged unless `YULANG_REPLAY_EVIDENCE_ONLY_SKIP=1` is set.

Added `scripts/replay-skip-compare.sh` to compare default inference against the
evidence-only replay prototype across several effect-heavy examples. The first
run with the release binary and skip limit 16 showed the same shape across all
default cases:

```text
examples/showcase.yu: default total 585.4ms, evidence total 475.8ms
effect-hygiene/01: default total 541.9ms, evidence total 479.5ms
effect-hygiene/02: default total 537.2ms, evidence total 456.5ms
effect-hygiene/03: default total 548.6ms, evidence total 471.2ms
public_type_display_order_signatures: default total 556.1ms, evidence total 467.8ms
```

The important pattern is not the exact time, but that `constraint.drain` drops
from about 198--220ms to 94--103ms on each case while public/effect smoke still
passes.

Expanded `scripts/replay-skip-compare.sh` to include public-signature
regressions:

- `data_position_effect_function_public_signature.yu`
- `nested_data_position_effect_function_public_signature.yu`
- `nested_handler_contract_public_signatures.yu`
- `sub_return_callback_public_signature.yu`

Added `scripts/hardening-smoke.sh` as the regular post-change gate. It runs:

- solver/public/effect targeted cargo tests,
- docs-tour and playground embedded-std smoke tests,
- public example smoke,
- replay skip comparison,
- evidence-only replay smoke, including the adversarial corpus.

`YULANG_HARDENING_DOCS_BUILD=1` additionally runs the VitePress docs build.
The first default hardening run passed with `YULANG=./target/release/yulang`.
The docs build also passed separately:

```text
npm --prefix web/docs run build
```
