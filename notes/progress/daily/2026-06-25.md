# 2026-06-25

## Control VM handler hygiene

`tests/yulang/yulang-adversarial-corpus/02_repeated_callback_hygiene.yu` exposed
a control-vm/interpreter mismatch:

```text
control-vm:  run roots [0]
interpreter: run roots [6]
```

The mono output already carried the callback adapter evidence:

```text
hygiene[add_id[0, tick, own]]
```

So the failure was in runtime guard transport, not in inference or
specialization.

The root cause was in `crates/control-vm/src/runtime/flow.rs`.
When the VM created a carried guard for an adapter marker, it treated every
handler frame outside the marker entry as already exposed. In nested same-path
handlers this exposed the inner `strip_tick` handler too early, so it was
allowed to capture a callback-origin `tick::ping`.

The interpreter already used the narrower rule: expose a matching handler alias
only when an earlier guard at the marker entry proves that alias is visible.
The VM now mirrors that rule and only exposes the outermost matching handler in
that alias case.

Added regression coverage:

```text
cargo test -q -p yulang run_control_source_text_with_embedded_std_keeps_repeated_callback_hygiene -- --test-threads=1
```

Manual checks after the fix:

```text
./target/release/yulang run --no-cache --print-roots tests/yulang/yulang-adversarial-corpus/02_repeated_callback_hygiene.yu
./target/release/yulang run --no-cache --print-roots --interpreter tests/yulang/yulang-adversarial-corpus/02_repeated_callback_hygiene.yu
tests/yulang/yulang-adversarial-corpus/probe.sh
```

Both runtimes now return `run roots [6]`, and the adversarial probe passes.

## Evidence-only replay skip

After the VM hygiene fix, the evidence-only replay smoke was meaningful again.
Both skip limits 16 and 32 passed:

```text
env YULANG=./target/release/yulang YULANG_REPLAY_EVIDENCE_ONLY_SKIP_LIMIT=16 scripts/evidence-only-replay-smoke.sh
env YULANG=./target/release/yulang YULANG_REPLAY_EVIDENCE_ONLY_SKIP_LIMIT=32 scripts/evidence-only-replay-smoke.sh
```

On `examples/showcase.yu`, limit 16 was slightly faster in the latest release
measurement:

```text
default: infer 519.4ms, total 582.0ms
limit 16: infer 425.3ms, total 490.0ms
limit 32: infer 440.6ms, total 506.0ms
```

The opt-in default for `YULANG_REPLAY_EVIDENCE_ONLY_SKIP_LIMIT` was moved from
32 to 16. The optimization remains env-gated; ordinary solver behavior is
unchanged unless `YULANG_REPLAY_EVIDENCE_ONLY_SKIP=1` is set.

Added `scripts/replay-skip-compare.sh` to compare default inference against the
evidence-only replay prototype across several effect-heavy examples. The first
run with the release binary and skip limit 16 showed the same shape across all
default cases:

```text
examples/showcase.yu: default total 585.4ms, evidence total 475.8ms
effect-hygiene/01: default total 541.9ms, evidence total 479.5ms
effect-hygiene/02: default total 537.2ms, evidence total 456.5ms
effect-hygiene/03: default total 548.6ms, evidence total 471.2ms
public_type_display_order_signatures: default total 556.1ms, evidence total 467.8ms
```

The important pattern is not the exact time, but that `constraint.drain` drops
from about 198--220ms to 94--103ms on each case while public/effect smoke still
passes.

Expanded `scripts/replay-skip-compare.sh` to include public-signature
regressions:

- `data_position_effect_function_public_signature.yu`
- `nested_data_position_effect_function_public_signature.yu`
- `nested_handler_contract_public_signatures.yu`
- `sub_return_callback_public_signature.yu`

Added `scripts/hardening-smoke.sh` as the regular post-change gate. It runs:

- solver/public/effect targeted cargo tests,
- docs-tour and playground embedded-std smoke tests,
- public example smoke,
- replay skip comparison,
- evidence-only replay smoke, including the adversarial corpus.

`YULANG_HARDENING_DOCS_BUILD=1` additionally runs the VitePress docs build.
The first default hardening run passed with `YULANG=./target/release/yulang`.
The docs build also passed separately:

```text
npm --prefix web/docs run build
```

Added `scripts/replay-skip-public-diff.sh`. It runs `dump-poly-std` twice,
once with the default solver and once with `YULANG_REPLAY_EVIDENCE_ONLY_SKIP=1`,
then byte-compares the dumps. This is stricter than the metrics comparison:
the replay prototype must not change public or internal poly output on the
selected public-signature fixtures.

The first run passed on:

- `examples/effect-hygiene/01_higher_order_inference.yu`
- `examples/effect-hygiene/02_local_sub_handler.yu`
- `examples/effect-hygiene/03_data_position_private_tail.yu`
- `tests/yulang/regressions/effect/data_position_effect_function_public_signature.yu`
- `tests/yulang/regressions/effect/nested_data_position_effect_function_public_signature.yu`
- `tests/yulang/regressions/effect/nested_handler_contract_public_signatures.yu`
- `tests/yulang/regressions/effect/public_type_display_order_signatures.yu`
- `tests/yulang/regressions/effect/sub_return_callback_public_signature.yu`

`scripts/hardening-smoke.sh` now includes this public diff by default. A
shortened integration run passed with public examples, replay metrics, and the
full evidence smoke disabled, leaving the new public-diff branch enabled:

```text
YULANG=./target/release/yulang \
YULANG_HARDENING_PUBLIC_EXAMPLES=0 \
YULANG_HARDENING_REPLAY_COMPARE=0 \
YULANG_HARDENING_EVIDENCE_SMOKE=0 \
scripts/hardening-smoke.sh
```

Extended `scripts/hardening-smoke.sh` to run the public example smoke under
`YULANG_REPLAY_EVIDENCE_ONLY_SKIP=1` as well as the default solver. The public
diff check already guards `dump-poly-std`; this extra gate checks that the
prototype does not change the execution behavior of the public examples.

Direct evidence-only public smoke passed:

```text
YULANG_REPLAY_EVIDENCE_ONLY_SKIP=1 \
YULANG_REPLAY_EVIDENCE_ONLY_SKIP_LIMIT=16 \
scripts/public-example-smoke.sh ./target/release/yulang
```

A shortened hardening run also passed with default public examples, replay
metrics, public diff, and full evidence smoke disabled, leaving the new
evidence-only public example branch enabled:

```text
YULANG=./target/release/yulang \
YULANG_HARDENING_PUBLIC_EXAMPLES=0 \
YULANG_HARDENING_REPLAY_COMPARE=0 \
YULANG_HARDENING_REPLAY_PUBLIC_DIFF=0 \
YULANG_HARDENING_EVIDENCE_SMOKE=0 \
scripts/hardening-smoke.sh
```

Removed raw fan-out preallocation from bound replay plans. The replay action
buffers are `SmallVec<[SubtypeConstraint; 4]>`; reserving
`replay_input_count` used the opposite-bound scan size rather than the number
of accepted actions after duplicate/trivial/evidence-only prefiltering. This
keeps the snapshot allocation proportional to materialized actions instead of
raw candidates.

Validation:

```text
timeout 240s cargo test -q -p infer replay -- --test-threads=1
timeout 300s cargo build -q -p yulang --release
timeout 360s env YULANG=./target/release/yulang scripts/replay-skip-compare.sh
timeout 360s env YULANG=./target/release/yulang scripts/replay-skip-public-diff.sh
```

The release comparison still shows the same default/evidence-only shape:
`constraint.drain` stays around 205--254ms in the default solver and
98--113ms with evidence-only skip on the measured public cases. Public poly
dumps remained byte-identical between default and evidence-only modes.

## CLI control VM stack gate

Reproduced the `for` / ref-update stack overflow noted on 2026-06-24 with a
minimal CLI workload:

```yu
{
    my $x = 0
    for i in 0..:
        if i == 100: last
        &x = $x + 1
    $x
}
```

Before the fix, the release CLI aborted with:

```text
thread 'main' has overflowed its stack
fatal runtime error: stack overflow, aborting
```

The source-level runtime tests already run selected control VM cases on a
larger helper stack. The CLI path did not: normal `run`, `run-control`,
`run-control-std`, and artifact execution called the control VM on the process
main stack. The control VM still has recursive eval/apply paths, so this was a
CLI stack policy gap rather than a new type/effect regression.

Added a CLI-only control runner that executes the control VM and root
formatting on a dedicated 64MiB stack, then returns only printable output,
errors, stats, and timing data across the thread boundary. Runtime values stay
inside the VM thread; this avoids making `control_vm::Value` cross-thread.

This is not the final trampoline fix. It makes CLI/release behavior match the
known runtime limitation and keeps the remaining runtime rewrite explicit.

Regression coverage:

- `public_regression_ref_update_loop_runs_on_cli_vm_stack`
- `scripts/release-smoke.sh` now runs the same 100-iteration ref-update loop.
- `scripts/hardening-smoke.sh` now calls `scripts/release-smoke.sh` by default.

Validation:

```text
bash -n scripts/hardening-smoke.sh
bash -n scripts/release-smoke.sh
timeout 120s cargo fmt --check
timeout 300s cargo test -q -p yulang --test cli public_regression_ref_update_loop_runs_on_cli_vm_stack -- --test-threads=1
timeout 300s cargo build -q -p yulang --release
timeout 180s scripts/release-smoke.sh ./target/release/yulang
timeout 420s cargo test -q -p yulang public -- --test-threads=1
timeout 420s env YULANG=./target/release/yulang \
  YULANG_HARDENING_PUBLIC_EXAMPLES=0 \
  YULANG_HARDENING_EVIDENCE_PUBLIC_EXAMPLES=0 \
  YULANG_HARDENING_REPLAY_COMPARE=0 \
  YULANG_HARDENING_REPLAY_PUBLIC_DIFF=0 \
  YULANG_HARDENING_EVIDENCE_SMOKE=0 \
  scripts/hardening-smoke.sh
```

## Trampoline experiment and explicit callback contracts

Measured the current CLI stack workaround before attempting a real trampoline.
The 100-iteration ref-update loop showed:

```text
runtime_execute: 190.4ms / 181.9ms / 180.2ms
marker_scope_max_depth: 709
request_resume_steps: 3904
continuation_frames_cloned: 134524
marker_scope_frame_touches: 1294750
```

`examples/showcase.yu` showed:

```text
runtime_execute: 53.7ms / 55.3ms / 54.4ms
marker_scope_max_depth: 54
request_resume_steps: 13530
continuation_frames_cloned: 100409
marker_scope_frame_touches: 436413
```

A temporary call-depth probe found `rust_max_call_depth=1903` for the ref loop
and `176` for showcase. The probe itself added measurable overhead, so it was
not kept.

Then tried a local trampoline shape that collected nested `Value::Marked`
callee layers and closed them in reverse after applying the inner callee once.
That passed `control-vm` unit tests, but broke the parser combinator smoke:

- `run_control_source_text_with_embedded_std_choice_recovers_from_parse_fail`
- `run_control_source_text_with_embedded_std_repeats_parser_until_delimiter`
- `run_control_source_text_with_embedded_std_repeats_parser_until_eof`

The failure was semantic, not just a missing frame pop. Parser backtracking
depends on the exact order in which marker frames are opened, closed, and
carried across requests. Batching marked layers changes which handler boundary
is visible to `parse::fail`. Do not retry this as the trampoline shape.

After reverting the experiment, the same parser smoke still failed on clean
HEAD. The underlying regression was that explicit argument-effect contracts
were carried as ordinary own-path guards. For `std.text.parse.choice`,
`p: () -> [parse] _` should allow the `choice` body's `catch p()` to catch
`parse::fail`; instead the carried guard blocked that matching handler and let
the failure escape to `run_str`.

The runtime now treats `PreserveMatchingPath` contract markers as explicit
callback contracts: when they carry a request, all matching handlers visible at
the marker entry are exposed to that carried guard. Ordinary own-path guards do
not get this exposure. This lets `choice` catch `fail`, while operations that
`choice` does not handle, such as `snapshot` and `item`, still flow to outer
handlers like `run_str`.

Updated the design note:

```text
notes/design/2026-06-24-explicit-effect-contract-metadata.md
```

Validation:

```text
timeout 360s cargo test -q -p yulang run_control_source_text_with_embedded_std_choice_recovers_from_parse_fail -- --test-threads=1
timeout 360s cargo test -q -p yulang run_control_source_text_with_embedded_std_repeats_parser_until_delimiter -- --test-threads=1
timeout 360s cargo test -q -p yulang run_control_source_text_with_embedded_std_repeats_parser_until_eof -- --test-threads=1
timeout 360s cargo test -q -p yulang run_control_source_text_with_embedded_std_keeps_repeated_callback_hygiene -- --test-threads=1
timeout 480s cargo test -q -p yulang run_control_source_text_with_embedded -- --test-threads=1
timeout 300s cargo test -q -p control-vm --lib -- --test-threads=1
timeout 300s cargo test -q -p mono-runtime --lib -- --test-threads=1
timeout 120s cargo fmt --check
timeout 300s cargo build -q -p yulang --release
timeout 120s ./target/release/yulang --std-root lib --no-cache run --print-roots <choice-fixture>.yu
timeout 120s ./target/release/yulang --std-root lib --no-cache run --print-roots --interpreter <choice-fixture>.yu
timeout 240s scripts/release-smoke.sh ./target/release/yulang
```

## `act copy` source visibility

Stabilized the source-level `act copy = source with:` visibility rule:

- A source-level copy inherits only the source body's `pub` / `our` surface.
- Source `my` operation signatures and companion members are not registered in
  the destination companion.
- The destination `with` body is still a direct body of the destination act, so
  its own `my` members remain available to its own declarations.
- Compiler-internal synthetic copies keep their implementation-copy behavior.
  This is needed for generated `var` / label-sub acts, where private helper
  methods such as `std.control.var.var.var_ref` and `run` are implementation
  details consumed by lowering rather than source-level exports.

The original leak was that `materialize_act_copy` registered the source body
verbatim into the destination companion, and `lower_act_body_contents` then
lowered the same source declarations verbatim. A `with` body could therefore
resolve a source-private name such as `hidden` as `copy.hidden`.

The fix adds an explicit copied-source mode to both module-map registration and
act body lowering. Source-level copies use an export-only mode; synthetic
compiler copies use an internal mode. The operation signature collection follows
the same mode, so private source operations do not remain visible in the copied
effect family.

Added `spec/2026-06-25-act-copy.md` and linked it from `spec/README.md`.

Validation:

```text
cargo fmt --check
timeout 240s cargo test -q -p infer act_copy -- --test-threads=1
timeout 420s cargo test -q -p infer -- --test-threads=1
cargo run -q -p yulang -- --no-cache check-poly-std /tmp/yulang-act-copy-private.yu
```

The CLI reproduction now reports unresolved `hidden` in `copy.visible` and
`copy.leak` instead of materializing `copy.hidden`.

## Web docs implementation sync

Updated `web/docs` to track the current implementation instead of the older
standard-library namespace:

- Added source-level `act copy = source with:` visibility notes to the English
  and Japanese effect/module references.
- Documented that `act copy` creates a distinct effect family and inherits only
  the source `pub` / `our` surface; source `my` members do not become visible in
  the destination companion or `with:` body.
- Replaced stale module paths such as `std::list`, `std::range`, `std::ops`,
  `std::undet`, `std::console`, and `std::fs` with the current paths:
  `std::data::*`, `std::core::ops`, `std::control::*`, and `std::io::*`.
- Updated VitePress sidebar labels to show the current standard-library module
  paths while keeping the existing page URLs stable.
- Updated filesystem docs from the old `fs` / `fs_err` surface to the current
  `file` effect and `io_err` error.
- Renamed remaining tutorial-only `fs_err` examples to `path_err`, so no docs
  example looks like the old standard filesystem API.
- Updated console docs away from removed `print_native` / `println_native`
  names toward the current `out` / `err` / `warn` / `die` effect families and
  wrappers.
- Normalized source-facing effect row syntax: concrete effect heads use commas
  (`[file, io_err]`), while row tails use semicolon plus a quoted type variable
  (`[file, io_err; 'e]`).

Validation:

```text
npm --prefix web/docs run build
rg -n 'fs_err|std::(ops|flow|range|list|opt|result|str|path|fs|undet|junction|console|var|fold|error|index)|fs::|\[fs([,; \]])|print_native|println_native' web/docs
cargo run -q -p yulang -- --no-cache check-poly-std /tmp/yulang-doc-file-type.yu
cargo run -q -p yulang -- --no-cache check-poly-std /tmp/yulang-doc-file-tail-type.yu
```

## Performance localization plan

Added `notes/todo/performance-localization.md` as the performance roadmap after
the hardening work. The plan frames optimization as localizing semantic cost,
not weakening the language semantics. It orders the work as:

1. one-command performance / hardening release gate,
2. compiled-unit cache on the normal path,
3. generalize restart locality,
4. runtime `ScopeState` / marker state,
5. evidence-preserving replay frontier,
6. direct-style / pure islands only after the above.

The TODO index now links this plan next to `static-analysis-speed.md`.

## Performance gate

Added `scripts/performance-gate.sh` as the first executable step of the
performance localization plan.

The script writes a timestamped run directory under `target/performance-gate/`
and keeps both complete command logs and a `summary.txt` containing the key
metrics needed for the next optimization steps:

- inference: `infer`, `constraint.drain`, replay counters, and generalize timing;
- runtime: `run.runtime_execute`, continuation clone counters, request resume
  steps, active-add scans, and marker-scope touches;
- cache comparison: no-cache / warmup / cache-hit runs for
  `bench/nondet_20_discard.yu`.

The default gate runs hardening smoke, the adversarial corpus, release smoke,
source metrics, and the existing static-analysis bench. Each section can be
disabled with `YULANG_PERF_GATE_*` environment switches for shorter local runs.

Validation:

```text
bash -n scripts/performance-gate.sh
YULANG_PERF_GATE_HARDENING=0 YULANG_PERF_GATE_ADVERSARIAL=0 \
  YULANG_PERF_GATE_RELEASE_SMOKE=0 YULANG_PERF_GATE_STATIC_BENCH=0 \
  YULANG_PERF_GATE_RUN_TIMEOUT=180s scripts/performance-gate.sh
```

The lightweight validation wrote:

```text
target/performance-gate/20260625-105601/summary.txt
```

Full baseline run:

```text
scripts/performance-gate.sh
target/performance-gate/20260625-105747/summary.txt
```

Highlights from the full run:

- hardening smoke: passed in 31.96s wall;
- adversarial corpus: passed in 12.20s wall;
- release smoke: passed in 0.30s wall;
- `examples/showcase.yu` check-poly: `infer` 534.1ms,
  `constraint.drain` 224.8ms, `analysis.quantify_generalize` 172.5ms,
  `constraint.replay_duplicate` 585618;
- `bench/nondet_20_discard.yu` no-cache run: `run.runtime_execute` 31.3ms,
  `run.active_add_scans` 32634, `run.marker_scope_frame_touches` 566762;
- cached nondet hit: wall 0.04s with runtime execution still around 32.2ms.

## Compiled-unit syntax surface

Started Phase 1 of the compiled-unit cache plan without changing the normal
compile path yet.

Implemented:

- `parser::op::BpVec::from_parts` / `as_slice`, so parser binding-power vectors
  can round-trip through artifact-owned `Vec<i8>` data without exposing
  `SmallVec` layout.
- `sources::CompiledSyntaxSurface`, `CompiledSyntaxFile`, `CompiledOpExport`,
  and `CompiledOpDef`.
- `sources::load_suffix_with_syntax_prefix`, which parses downstream source
  using only a serialized syntax prefix rather than dependency `LoadedFile`
  CSTs.
- `yulang::cache` `.yuunit` artifact read/write support containing
  `CompiledUnitManifest` and the syntax surface.

The syntax surface intentionally stores only public / our `use` declarations
and operators. `my` operators remain local to the original unit and do not
become visible to downstream suffix parsing through the artifact.

Validation:

```text
cargo fmt --check
timeout 240s cargo test -q -p sources compiled_syntax -- --test-threads=1
timeout 240s cargo test -q -p yulang compiled_unit_cache_round_trips_manifest_and_syntax_surface -- --test-threads=1
timeout 240s cargo test -q -p sources -- --test-threads=1
timeout 240s cargo test -q -p yulang cache -- --test-threads=1
timeout 240s cargo test -q -p parser -- --test-threads=1
timeout 300s cargo check -q -p yulang
timeout 420s cargo build -q -p yulang --release
YULANG_PERF_GATE_HARDENING=0 YULANG_PERF_GATE_ADVERSARIAL=0 \
  YULANG_PERF_GATE_RELEASE_SMOKE=0 YULANG_PERF_GATE_STATIC_BENCH=0 \
  YULANG_PERF_GATE_RUN_TIMEOUT=180s scripts/performance-gate.sh
```

The lightweight gate wrote:

```text
target/performance-gate/20260625-110834/summary.txt
```

Next step: namespace surface. Syntax artifacts can now make downstream parsing
work, but lowering still needs a stable imported namespace surface before any
dependency source can be skipped on the normal path.

## Compiled-unit namespace surface

Continued Phase 1 by adding the namespace half of the `.yuunit` artifact. The
normal compile path is still unchanged; this is the serialized surface needed
before a cached dependency unit can participate in name resolution without
re-lowering its source body.

Implemented:

- `infer::CompiledNamespaceSurface` with module, value, type, child-module,
  and alias entries.
- Stable per-artifact module/value/type IDs plus source declaration order and
  visibility.
- Lazy operator flags from `infer::ModuleTable`, so cached operator value
  entries keep the parser/runtime distinction needed by later lowering.
- Resolved import views for values, types, and child modules, represented as
  unit-local symbol/module references rather than process-local `DefId`s.
- `infer::CompiledNamespaceIndex`, which resolves exported value/type/module
  symbols from the serialized surface, including reexports.
- `namespace_hash` in `CompiledUnitManifest` and a `.yuunit` format bump.
- Cache round-trip coverage that stores both syntax and namespace surfaces.
- The namespace surface type lives in `infer`, not `yulang::cache`, so the
  future lowerer/import route can consume it without a crate cycle.

Validation:

```text
cargo fmt --check
cargo test -q -p yulang compiled_unit_cache_round_trips_manifest_and_syntax_surface -- --test-threads=1
cargo test -q -p yulang cache -- --test-threads=1
cargo check -q -p infer
cargo check -q -p yulang
```

Next step: build a namespace-surface import route. The artifact can now store
the names exported by a unit, but the lowerer still only knows how to resolve
against a live `ModuleTable` built from source.

## Compiled-unit typed surface

Added the next compiled-unit layer for exported schemes. This still does not
change the normal compile path; it only makes `.yuunit` artifacts carry the
typed information that a future cache hit will need.

Implemented:

- `infer::CompiledTypedSurface`, mapping namespace value symbols to
  `poly::types::Scheme`.
- `infer::CompiledTypeArena`, a serialized arena form that stores only
  `pos` / `neg` / `neu` node vectors and avoids serializing hash-cons maps.
- `infer::CompiledTypedIndex` for symbol-to-scheme lookup, plus
  `CompiledTypeArena::to_type_arena` for process-local arena reconstruction.
- `CompiledTypeImporter`, which remaps serialized `PosId` / `NegId` /
  `NeuId`, type variables, and stack subtract IDs into a live `infer::Arena`.
- `CompiledTypedSurface::import_value_defs`, which allocates fresh bodyless
  `poly::Def::Let` entries carrying imported schemes for exported value
  symbols.
- Typed surface extraction from `BodyLowering`, using the live `ModuleTable`
  only to map namespace symbol/order back to the source `DefId`.
- `.yuunit` envelope support for typed surface data, plus `typed_hash` and a
  compiled-unit format bump.
- Deterministic typed-surface hashing over the serialized type node vectors
  and scheme contents.

Validation:

```text
cargo fmt --check
cargo test -q -p infer typed_surface_records_exported_value_schemes_by_namespace_symbol -- --test-threads=1
cargo test -q -p yulang compiled_unit_cache_round_trips_manifest_and_syntax_surface -- --test-threads=1
cargo test -q -p yulang cache -- --test-threads=1
cargo check -q -p infer
cargo check -q -p yulang
```

Next step: import route. The artifact can now store parser syntax, namespace
symbols/reexports, and exported schemes, but the lowerer still does not
allocate fresh process-local ids from those surfaces.

## Compiled-unit typed import view

Added the first typed import route for cached dependency units. The normal
compile path is still unchanged; this only turns serialized namespace/typed
surfaces into process-local ids that a future lowerer import path can consume.

Implemented:

- `infer::CompiledUnitImport`, which owns imported value definitions plus an
  exported `(module path, name)` lookup view.
- `CompiledTypedSurface::import_unit`, which combines namespace declarations,
  namespace reexports, and typed schemes into fresh bodyless `poly::Def::Let`
  entries.
- `CompiledUnitImport::exported_value_def`, using a two-level
  module-path/name map so later name resolution can look up borrowed paths and
  names without allocating a composite key each time.
- Regression coverage that checks a reexported value resolves to the same
  imported `DefId`, while a `my` binding stays hidden.

Validation:

```text
cargo fmt --check
cargo test -q -p infer typed_surface_records_exported_value_schemes_by_namespace_symbol -- --test-threads=1
cargo test -q -p yulang cache -- --test-threads=1
cargo check -q -p yulang
```

Next step: wire this import view into the normal lowerer/name-resolution route.
At that point a cached dependency unit should provide parser syntax, namespace
exports/reexports, and imported typed schemes without lowering dependency
source bodies.

## Compiled-unit runtime surface

Before wiring cached dependency units into the normal lowerer, checked what the
typed import view is missing. Value schemes are not enough for execution:
dependency bodies, `poly::Arena::effect_operations`, constructors, field
projection metadata, runtime roots, and resolved refs/selects all live in the
lowered poly graph. Connecting only bodyless imported `Def::Let`s would make
name lookup appear to work while leaving runtime/specialization metadata behind.

Added:

- `infer::CompiledRuntimeSurface`, currently the lowered per-unit
  `poly::Arena` plus `DumpLabels`.
- `.yuunit` envelope support for the runtime surface, with compiled-unit format
  bumped to 4.
- Cache round-trip coverage that checks the runtime surface's def graph and
  labels survive serialization.

Added `runtime_hash` to the `.yuunit` manifest. The hash walks the lowered
`poly::Arena` structure directly instead of hashing serialized bytes, and sorts
HashMap/HashSet-backed metadata before hashing. Labels are intentionally kept
out of this semantic runtime hash; they still round-trip as part of the runtime
surface.

The next clean step is a remap/merge path that can bring cached dependency
bodies into a live arena.

Also generalized `CompiledTypeImporter` so it can import into both
`infer::Arena` and `poly::Arena`. Runtime remap will need to copy schemes and
type graph nodes directly into a live poly arena; this keeps that path on the
same remapping code as the typed-surface import.

Validation:

```text
cargo fmt --check
cargo test -q -p infer typed_surface_records_exported_value_schemes_by_namespace_symbol -- --test-threads=1
cargo test -q -p yulang compiled_unit_cache_round_trips_manifest_and_syntax_surface -- --test-threads=1
cargo test -q -p yulang cache -- --test-threads=1
cargo check -q -p infer
cargo check -q -p yulang
```

## Compiled-unit runtime import

Added the first runtime-surface merge primitive for the compiled-unit cache.
`CompiledRuntimeSurface::import_into` now imports a cached dependency's lowered
`poly::Arena` into a live arena by remapping arena-local `DefId`, `ExprId`,
`PatId`, `RefId`, and `SelectId`, while importing schemes and role/cast type
IDs through `CompiledTypeImporter`.

The import keeps runtime metadata attached to the remapped definitions:

- roots / runtime roots / root expression defs
- refs and select resolutions
- cast rules and role impl candidates
- effect operations and constructors
- argument effect contracts and field projections
- dump labels

This is still a merge primitive, not yet the full dependency-SCC cache path.
The next step is to wire namespace/typed/runtime imported units into
`lower_root_loaded_file_with_prefix` or its successor so unchanged dependency
SCCs can skip body lowering and inference.

Validation:

```text
cargo fmt --check
timeout 120s cargo check -q -p infer
timeout 120s cargo check -q -p yulang
timeout 120s cargo test -q -p infer compiled_ -- --test-threads=1
timeout 120s cargo test -q -p infer -- --test-threads=1
timeout 120s cargo test -q -p poly -- --test-threads=1
timeout 120s cargo test -q -p yulang cache -- --test-threads=1
```

## Parser choice effect contract sharing

Fixed the `std.text.parse.choice` public signature regression where the two
parser callback arguments and the result exposed different `parse` effect
variables unless the implementation carried extra annotations.

The root issue was that closed effect rows in callback annotations were lowered
as separate stack-inner variables each time they appeared. For `choice`, the
same annotation row:

```text
[parse 'item 'err 'pos 'snap]
```

appears in both callback arguments and in the result effect. Those appearances
are the same annotation contract, so the lowerer now interns closed annotation
effect rows within the annotation scope and reuses the same `TypeVar`.

The fix is deliberately limited to closed rows. Tail rows such as
`[loop; 'e]` still use the existing projection/erasure path, so canaries like
`for_in` and the small callback-tail tests keep their residual-only public
surface.

Specialization also needed a runtime-side fix. Once the public type is shared,
the callback argument can become boundary-equivalent to the expected type, but
the explicit annotation contract still has to install function-adapter hygiene
markers. `specialize2` now reads the expected callee argument's
`ArgEffectContract` from the call spine and keeps a function boundary even when
the value types are equivalent if that boundary carries contract markers.

During the fix, a visibility-based branch (`pub`/`my`) was tried and removed.
That branch was too ad hoc: visibility is not the reason the type should
change. The remaining rule is based on annotation row structure and call-site
contract metadata instead.

Regression coverage:

- `dump_poly_std_parse_choice_public_signature_hides_stack_evidence`
- `run_control_source_text_with_embedded_std_choice_recovers_from_parse_fail`
- `dump_poly_std_flow_for_in_public_signature_hides_stack_evidence`
- `dump_poly_std_ref_update_public_signature_hides_stack_evidence`

Validation:

```text
cargo check -q -p infer -p specialize
timeout 300s cargo test -q -p infer -- --test-threads=1
timeout 240s cargo test -q -p specialize -- --test-threads=1
timeout 300s cargo test -q -p yulang -- --test-threads=1
git diff --check
```

## Runtime surface prefix import

Added the first bridge from `CompiledRuntimeSurface` back into the normal root
lowering route. `BodyLoweringPrefix::from_runtime_surface` now imports a cached
runtime arena into a fresh live `poly::Arena` and remaps the matching
`ModuleTable` with the same `DefId` correspondence.

The remap covers ordinary/module declarations and the lowering-time tables that
can point at definitions:

- imported values and lazy operators
- act operation definitions and act methods
- constructors and error operation maps
- casts, role impls, role methods, type methods, and field methods
- synthetic act uses, root expression owners, and source spans

This is still not the full persistent dependency-SCC cache. The remaining
missing piece is a serializable lowering-environment surface for `ModuleTable`
semantics such as act operations, constructors, methods, role metadata, and
error helpers. The current slice proves that, once that environment exists, the
runtime surface can seed `lower_root_loaded_file_with_prefix` without leaving
stale `DefId`s behind.

Validation so far:

```text
cargo fmt --check
cargo check -q -p infer
cargo check -q -p yulang
timeout 120s cargo test -q -p infer compiled_runtime::tests::runtime_surface_prefix_remaps_module_defs_for_later_root_lowering -- --test-threads=1
timeout 120s cargo test -q -p infer compiled_runtime::tests::runtime_surface_import_remaps_lowered_arena_ids -- --test-threads=1
timeout 300s cargo test -q -p infer -- --test-threads=1
timeout 120s cargo test -q -p yulang compiled_unit_cache_round_trips_manifest_and_syntax_surface -- --test-threads=1
git diff --check
```
