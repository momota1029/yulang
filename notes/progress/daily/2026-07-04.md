# 2026-07-04

## Host suspend one-shot runtime slice

- Wired suspend one-shot host operations through the existing host ABI registry
  and runtime scheduler queue.
- `HostCtx::suspend_one_shot()` now issues an opaque resume token. The token can
  only enqueue a `BoundaryValue` resume event; it has no perform/eval/runtime
  reentry API.
- Runtime host dispatch now branches by manifest tier:
  - `sync` keeps the existing immediate `HostOutcome::Return` path.
  - `suspend-one-shot` parks the escaped request continuation and resumes it
    from a scheduler `Resume` event.
  - `suspend-multi-shot` remains a reserved tier and returns an explicit
    unimplemented runtime error.
- Added in-process fake host tests for normal suspend/resume, cancel-before-
  resume rejection, one-shot double-resume rejection, and multi-shot reservation.
- Validation:
  - `RUSTC_WRAPPER= cargo test -p evidence-vm -- --test-threads=1`
  - `RUSTC_WRAPPER= cargo check -q -p evidence-vm -p yulang`
  - `cargo fmt --all -- --check`

## Parser pattern Stage P2 rest slice

- Added rule-body `..` parsing for explicit `rule { ... }` and rule literal
  interpolation (`~"{..}"` / `~"{r = ..}"`). Bare `..` inside `~"..."` remains
  literal text.
- Added pure std parser `std::text::parse::rest`, implemented with
  `snapshot` + `advance input.len`, and lowered rule rest to that parser.
- Kept rest terminal: `..` after further rule items reports
  `yulang.rule-rest-position`.
- Changed parser-pattern capture binding from synthetic field selections to a
  record-pattern destructure of the parser result. This fixes capture guard dot
  method selection such as `id.starts_with "A"` without changing general method
  resolution.
- Extended `parser_pattern_case` with lazy capture, interpolation capture, bare
  parser reference capture, guard fallthrough, rest capture/discard, string
  interpolation rest capture, and dot-guard regression.
- Validation:
  - `RUSTC_WRAPPER= timeout 120s cargo test -p parser -- --test-threads=1`
  - `RUSTC_WRAPPER= timeout 180s cargo test -p infer -- --test-threads=1`
  - `RUSTC_WRAPPER= timeout 900s cargo test -p yulang source::tests -- --test-threads=1`
  - `RUSTC_WRAPPER= timeout 180s cargo run -q -p yulang -- contract --case parser_pattern_case tests/yulang/cases.toml`
  - `cargo fmt --all -- --check`

## Host suspend multi-shot runtime slice

- Added `HostCtx::suspend_multi_shot()` and multi-shot resume token handling.
- Multi-shot resume tokens can enqueue more than one resume. Each processed
  resume spawns a structured child branch under the branch that suspended the
  host operation, then runs a cloned continuation with the `BoundaryValue`
  payload.
- Added token `close()`. Close rejects later resumes and removes the runtime
  continuation template; it does not synthesize a final parent value.
- Parent cancel/drop drops the token and later resumes return `Dropped`.
  Child cancel stays on the existing cancel queue and does not affect sibling
  children or the parent token.
- Kept server/net public surface, manifest tier assignment, and std unchanged.
- Validation:
  - `RUSTC_WRAPPER= cargo test -p evidence-vm host_abi_suspend_multi_shot -- --test-threads=1`
  - `RUSTC_WRAPPER= cargo test -p evidence-vm scheduler -- --test-threads=1`
  - `RUSTC_WRAPPER= cargo test -p evidence-vm host_abi_suspend_multi_shot_parent_cancel_rejects_resume_and_cascades_children -- --test-threads=1`
  - `cargo fmt --all -- --check`

## Host server manifest and in-process driver slice

- Added declaration-sourced host operation tiers for `pub host act` members.
  Operations without an explicit tier remain `sync`; `pub suspend_one_shot`
  and `pub suspend_multi_shot` are contextual host-operation modifiers.
- Added `std.io.net.server` with `accept` as suspend multi-shot and `respond`
  as sync result-returning response-slot consumption. Public signatures hide
  private stack evidence.
- Added evidence-vm test-only fake server support using host handles for
  listener/request metadata/respond slots and record-shaped request payloads.
  The fake driver covers two accept resumes, request resource delivery,
  one-shot response, and structured double-respond failure.
- Extended contract tags and cases for the server public signatures. The new
  sorted server operations move `std.io.clock.now` from column 9 to column 11
  in manifest golden output; existing sync operation tiers remain unchanged.
- Validation:
  - `RUSTC_WRAPPER= timeout 300s cargo test -p evidence-vm -- --test-threads=1`
  - `RUSTC_WRAPPER= timeout 300s cargo test -p infer -- --test-threads=1`
  - `RUSTC_WRAPPER= timeout 900s cargo test -p yulang source::tests -- --test-threads=1`
  - `RUSTC_WRAPPER= timeout 180s cargo run -q -p yulang -- contract --case std_io_net_server_accept_public_signature --case std_io_net_server_respond_public_signature tests/yulang/cases.toml`
  - `RUSTC_WRAPPER= timeout 180s cargo test -p yulang public_contract_manifest_cli_cases_hold -- --test-threads=1`
  - `cargo fmt --all -- --check`

## Case chain method fallback readiness

- Diagnosed the file line chain failures as early record fallback, not missing
  var-var propagation. Trace showed the concrete `str` lower reaching the
  receiver after record fallback had already selected the field path.
- Kept record field fallback out of typeclass method fallback batches. This
  lets `.each` instantiate receiver bounds before downstream `.update` /
  `.replace_once` selection commits to record projection.
- Added source and runtime regressions for `lines.each` chained method
  resolution. `($doc.lines.each).replace_once ...` now runs natively; the
  `(&doc.lines.each).update ...` chain reaches the existing downstream
  specialize2 `[std::io::file::file] vs []` conflict after method resolution.
- Corrected the file-line ergonomics note: `&(...)` is reserved for future
  destructuring assignment syntax, not an open computed-ref assignment design.

## File line chain specialize2 follow-up

- Diagnosed the downstream specialize2 conflict as row candidate solving, not
  wrong slot sharing. The failing ref residual slot received concrete `[file]`
  and pure `[]` candidates through different call contexts; an upper-row meet
  then collapsed to `[]`.
- Kept nominal type constructor arguments as default value-kind evidence unless
  the constructor argument is explicitly an effect context. Structural
  occurrences such as `ref 'e str` now win and materialize `'e` as an effect
  slot.
- Normalized effect slot candidates so only registered effect families, effect
  tails, stacks, and pure rows enter effect rows. This drops value `unit` from
  effect candidates instead of wrapping it as an effect item.
- Extended effect slot conflict refinement: when each non-pure upper row
  candidate structurally covers the lower row items at top level or inside a
  nested effect-row payload, keep the lower row as the row-join candidate
  instead of letting the upper meet erase it to `[]`.
- `(&doc.lines).index 0` now runs natively and is fixed as
  `file_ref_lines_index_chain_native`. `(&doc.lines.each).update ...` reaches
  the runtime `nondet::branch` handler boundary instead of specialize2.
- Validation:
  - `RUSTC_WRAPPER= cargo test -p specialize -- --test-threads=1`
  - `RUSTC_WRAPPER= cargo test -p mono -p evidence-vm -- --test-threads=1`
  - `RUSTC_WRAPPER= cargo test -p infer -- --test-threads=1`
  - `RUSTC_WRAPPER= YULANG_CACHE_DIR=/tmp/yulang-cache-source-tests-specialize2-final cargo test -p yulang source::tests -- --test-threads=1`
  - `RUSTC_WRAPPER= YULANG_CACHE_DIR=/tmp/yulang-cache-contract-file-ref-lines-index-final cargo run -q -p yulang -- --std-root lib contract --case file_ref_lines_index_chain_native tests/yulang/cases.toml`
  - Related contract cases: `file_line_view_ref_update_native`,
    `file_value_lines_each_replace_once_chain_native`,
    `file_line_range_for_loop_native`

## std net serve mock-server slice

- Added `std.io.net.net.listen`, high-level `std.io.net.listen`, and
  `std.io.net.serve`. `serve` obtains a listener, then delegates to
  `std.io.net.server.accept`; the public signature is
  `int -> [std::io::net::net, std::io::net::net_err, std::io::net::server] std::io::net::request`.
- Added a `listener.accept()` value-shape helper that keeps the accept effect on
  the server host act instead of adding resolver- or name-string special cases.
- Wired an evidence-vm in-process mock server host behind `run --host
  mock-server` and contract `host = "mock-server"`. The driver registers
  `listen`, `accept`, and `respond`, emits two queued requests (`first`,
  `second`) through the multi-shot accept token, logs responses, and returns
  typed `net_err::closed` for double respond.
- Added Yulang runtime fixtures for `serve -> payload -> respond` over two
  accept resumes and for double respond typed failure. Added clean public
  signature checks for host `net.listen`, wrapper `listen`, and `serve`.
- Left the unscoped one-line spelling `my req = net::serve 8080` for the next
  resolver/prelude slice. Adding a `pub mod net` re-export to the prelude did
  not expose `net::serve` at root, so finishing that spelling requires explicit
  module-alias/import semantics rather than a natural std-only fall-through.
- Validation:
  - `RUSTC_WRAPPER= timeout 300s cargo test -p evidence-vm -- --test-threads=1`
  - `RUSTC_WRAPPER= timeout 300s cargo test -p infer -- --test-threads=1`
  - `RUSTC_WRAPPER= YULANG_CACHE_DIR=/tmp/yulang-cache-server-slice-source-final timeout 900s cargo test -p yulang source::tests -- --test-threads=1`
  - `RUSTC_WRAPPER= YULANG_CACHE_DIR=/tmp/yulang-cache-server-slice-net-contract-final timeout 240s cargo run -q -p yulang -- --std-root lib --no-cache contract --case std_io_net_server_accept_public_signature --case std_io_net_server_respond_public_signature --case std_io_net_net_listen_public_signature --case std_io_net_listen_public_signature --case std_io_net_serve_public_signature --case server_serve_echo_mock_server --case server_double_respond_typed_failure tests/yulang/cases.toml`
  - `RUSTC_WRAPPER= YULANG_CACHE_DIR=/tmp/yulang-cache-server-slice-cli-final timeout 600s cargo test -p yulang --test cli -- --test-threads=1`

## Prelude net module alias and unscoped server slice

- Diagnosed the unscoped `net::serve` failure as a module import precedence
  issue, not missing std file collection. `std::io` exports both the child
  module `std::io::net` and names re-exported from `std::io::net::*`; the
  companion host-act module `std::io::net::net` was replacing the high-level
  child module as the `net` path prefix through the prelude glob.
- Changed glob import view construction so declared child modules remain the
  path-prefix surface when same-named re-exported companion modules are also
  visible from the same target.
- Added resolver regressions for glob-imported child modules as value, type,
  and act path prefixes, for prelude glob re-export through an actual
  same-name companion conflict, and for local user `mod net` winning over the
  imported module alias.
- Added mock-server runtime contracts for unscoped `net::serve 8080` and
  unscoped `net::listen 8080` followed by `listener.accept()`.
- Final spelling is `my req = net::serve 8080`; a direct `run --host
  mock-server --print-roots -e` run produced two mock responses and roots
  `["first", "second"]`.
- Validation:
  - `RUSTC_WRAPPER= timeout 180s cargo test -q -p infer lowering::tests::case_06 -- --test-threads=1 --nocapture`
  - `RUSTC_WRAPPER= timeout 300s cargo test -p infer -- --test-threads=1`
  - `RUSTC_WRAPPER= timeout 300s cargo test -p evidence-vm -- --test-threads=1`
  - `RUSTC_WRAPPER= YULANG_CACHE_DIR=/tmp/yulang-cache-unscoped-serve-source-tests timeout 900s cargo test -p yulang source::tests -- --test-threads=1`
  - `RUSTC_WRAPPER= YULANG_CACHE_DIR=/tmp/yulang-cache-unscoped-serve-net-contract2 timeout 300s cargo run -q -p yulang -- --std-root lib --no-cache contract --case std_io_net_server_accept_public_signature --case std_io_net_server_respond_public_signature --case std_io_net_net_listen_public_signature --case std_io_net_listen_public_signature --case std_io_net_serve_public_signature --case server_serve_echo_mock_server --case server_unscoped_serve_echo_mock_server --case server_unscoped_listen_accept_echo_mock_server --case server_double_respond_typed_failure tests/yulang/cases.toml`
  - `RUSTC_WRAPPER= YULANG_CACHE_DIR=/tmp/yulang-cache-unscoped-serve-cli2 timeout 600s cargo test -p yulang --test cli -- --test-threads=1`
  - `RUSTC_WRAPPER= YULANG_CACHE_DIR=/tmp/yulang-cache-unscoped-serve-direct-run timeout 180s cargo run -q -p yulang -- --std-root lib --no-cache run --host mock-server --print-roots -e '{ ... }'`
  - `cargo fmt --all -- --check`

<!-- daily-append-anchor: 2026-07-04 -->
