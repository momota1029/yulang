# 2026-07-01

## Evidence VM state-frame continuation snapshots

- Pulled ChatGPT Pro's follow-up answer:
  - new head: `f6c6ee57`
  - `notes/research/consultation/2026-06-30-evidence-vm-direct-state-route-proof-followup-answer.md`
- Implemented the answer's Stage C route:
  - did not enable request-free route-specific `DirectStateGet/Set`.
  - added `EvidenceContinuationFrame::StateFrames` as a dynamic-scope wrapper
    for runtime known-state handler snapshots.
  - snapshot is created only when `defer_catch_body` crosses a known-state
    catch boundary, not when a request is created.
  - resuming `StateFrames` forks restored state frames and pops them after the
    resumed segment finishes.
  - if the resumed segment escapes again as a generic request, routed yield, or
    direct-tail resumptive call, it re-snapshots the updated state frame suffix
    and rewraps the outgoing continuation.
- Added counters:
  - `known_state_frame_snapshots`
  - `known_state_frame_snapshot_missing_frame`
  - `known_state_frame_snapshot_max_depth`
  - `known_state_frame_forks`
  - `known_state_frame_multishot_forks`
  - `known_state_frame_resume_entries`
  - `known_state_frame_resume_exits`
  - `known_state_frame_resume_request_rewraps`
- Added canaries:
  - internal runtime tests for snapshot fork/multishot counting and request
    rewrap with an updated state snapshot.
  - CLI assertions that straight-line local refs and the current
    state-outside-nondet case do not snapshot or fork state frames.
- Representative check:
  - `target/release/yulang debug evidence-vm-run --compare-control bench/loop_for_sum_ref_20_discard.yu`
    - `known_state_direct_gets: 800`
    - `known_state_direct_sets: 400`
    - state-frame snapshots/forks/rewraps: `0`
    - `compare.control: match`
- Validation:
  - `cargo fmt --all`
  - `cargo fmt --all -- --check`
  - `timeout 180s cargo check -q -p evidence-vm -p yulang`
  - `timeout 180s cargo test -q -p evidence-vm state_frame_ -- --test-threads=1`
    - `2 passed`
  - `timeout 180s cargo test -q -p yulang --test cli debug_runtime_evidence_run_handles_ref_set -- --test-threads=1`
    - `1 passed`
  - `timeout 180s cargo test -q -p yulang --test cli debug_runtime_evidence_run_known_state_frame_matches_control_across_nondet_resume -- --test-threads=1`
    - `1 passed`
  - `timeout 240s cargo test -q -p evidence-vm -- --test-threads=1`
    - `77 passed`
  - `timeout 240s cargo test -q -p yulang --test cli -- --test-threads=1`
    - `70 passed`
  - `timeout 240s cargo build -q -p yulang --release`

## Evidence VM known-state operation route proof D1

- Implemented the plan-only `KnownStateOperationRouteProof` slice from the
  follow-up consultation:
  - `poly::SyntheticVarEffect` now carries optional compiler operation defs for
    generated local-var `get` / `set`.
  - compiled-runtime import remaps those defs when a cached/imported arena is
    loaded.
  - lowering registers the generated synthetic-var operation defs for the
    compiler local ref effect.
  - specialize emits `known_state_accesses` from compiler-certified operation
    call sites.
  - Evidence VM joins those access facts with `KnownHandlerPlan::State` and
    emits per-control-call `KnownStateOperationRouteProof` objects.
- D1 remains plan-only:
  - route-proven local ref operations still keep `GenericFallback` execution.
  - `KnownOperationPlan::direct_ready` remains `false`.
  - rejects move from `no-candidate-handler` / `no-known-state-access-proof` to
    `direct-execution-disabled` once a route proof exists.
- Important boundary:
  - `RuntimeEvidenceSurface` currently carries poly expr ids, while Evidence VM
    uses control expr ids.
  - Therefore D1 uses compiler-issued `synthetic_var + operation role + operation
    def existence` as the stable bridge and emits per-control-call proofs.
  - A later D2/D3 implementation should carry operation origin/def into the
    control-side operation plan before enabling request-free direct execution.
- Added/updated debug counters and output:
  - `plan_known_state_operation_route_proofs`
  - `plan_known_operation_reject_no_known_state_access_proof`
  - `plan_known_operation_reject_known_state_access_handler_mismatch`
  - `plan_known_operation_reject_known_state_access_boundary_unsafe`
  - `plan_known_operation_reject_direct_execution_disabled`
- Representative check:
  - `target/release/yulang --std-root lib --no-cache debug evidence-vm-plan bench/loop_for_sum_ref_20_discard.yu`
    - `route_proofs 3`
    - `direct_gets 0`
    - `direct_sets 0`
    - `direct_execution_disabled 3`
  - `target/release/yulang --std-root lib --no-cache debug evidence-vm-run --compare-control bench/loop_for_sum_ref_20_discard.yu`
    - `evidence.plan_known_state_operation_route_proofs: 3`
    - `evidence.plan_known_operation_reject_direct_execution_disabled: 3`
    - `runtime_evidence.known_state_direct_gets: 800`
    - `runtime_evidence.known_state_direct_sets: 400`
    - `compare.control: match`
- Validation:
  - `cargo fmt --all`
  - `cargo fmt --all -- --check`
  - `timeout 180s cargo check -q -p poly -p infer -p specialize -p evidence-vm -p yulang`
  - `timeout 180s cargo test -q -p evidence-vm known_state -- --test-threads=1`
    - `1 passed`
  - `timeout 180s cargo test -q -p evidence-vm state_frame_ -- --test-threads=1`
    - `2 passed`
  - `timeout 180s cargo test -q -p yulang --test cli debug_evidence_vm_plan_marks_local_var_known_state_handler -- --test-threads=1`
    - `1 passed`
  - `timeout 180s cargo test -q -p yulang --test cli debug_runtime_evidence_run_handles_ref_set -- --test-threads=1`
    - `1 passed`
  - `timeout 180s cargo test -q -p yulang --test cli debug_runtime_evidence_run_known_state_frame_matches_control_across_nondet_resume -- --test-threads=1`
    - `1 passed`
  - `timeout 240s cargo build -q -p yulang --release`

## Evidence VM known-state route proof D2 shadow guard

- Tightened the D1 route proof bridge:
  - `mono::ExprKind::EffectOp` now carries optional compiler operation def.
  - `control_vm::Expr::EffectOp` and `ControlEffectEvidence` preserve that def.
  - `EvidenceVmOperationPlan` records `operation_def`.
  - `KnownStateOperationRouteProof` is now emitted only when the control-side
    operation def matches the compiler-certified known-state access def.
- Added D2 runtime shadow guard:
  - runtime keeps a table of `KnownStateOperationRouteProof`.
  - known local-state operation hits still execute through the existing generic /
    late known-state path.
  - in parallel, the VM checks whether the route proof would find the active
    state frame by `plan_id + catch_expr` and records counters.
- Added counters:
  - `known_operation_route_shadow_hits`
  - `known_operation_route_shadow_get_hits`
  - `known_operation_route_shadow_set_hits`
  - `known_operation_route_shadow_missing_proof`
  - `known_operation_route_shadow_missing_frame`
  - `known_operation_route_shadow_role_mismatch`
- Representative check:
  - `target/release/yulang --std-root lib --no-cache debug evidence-vm-plan bench/loop_for_sum_ref_20_discard.yu`
    - operation plan prints `d706` / `d707` for generated local-state get/set.
    - route proofs keep `def d706` / `def d707`.
    - `route_proofs 3`, `direct_execution_disabled 3`.
  - `target/release/yulang --std-root lib --no-cache debug evidence-vm-run --compare-control bench/loop_for_sum_ref_20_discard.yu`
    - `runtime_evidence.known_operation_route_shadow_hits: 1200`
    - `runtime_evidence.known_operation_route_shadow_get_hits: 800`
    - `runtime_evidence.known_operation_route_shadow_set_hits: 400`
    - shadow missing proof/frame/role mismatch: `0`
    - `compare.control: match`
- Validation:
  - `cargo fmt --all`
  - `cargo fmt --all -- --check`
  - `timeout 180s cargo check -q -p mono -p mono-runtime -p control-vm -p specialize -p evidence-vm -p yulang`
  - `timeout 180s cargo test -q -p control-vm records_ -- --test-threads=1`
    - `3 passed`
  - `timeout 180s cargo test -q -p specialize effect_operation_ref_as_effect_op -- --test-threads=1`
    - `2 passed`
  - `timeout 180s cargo test -q -p evidence-vm known_state -- --test-threads=1`
    - `1 passed`
  - `timeout 180s cargo test -q -p evidence-vm state_frame_ -- --test-threads=1`
    - `2 passed`
  - `timeout 180s cargo test -q -p yulang --test cli debug_evidence_vm_plan_marks_local_var_known_state_handler -- --test-threads=1`
    - `1 passed`
  - `timeout 180s cargo test -q -p yulang --test cli debug_runtime_evidence_run_handles_ref_set -- --test-threads=1`
    - `1 passed`
  - `timeout 180s cargo test -q -p yulang --test cli debug_runtime_evidence_run_known_state_frame_matches_control_across_nondet_resume -- --test-threads=1`
    - `1 passed`
  - `timeout 240s cargo build -q -p yulang --release`

## Evidence VM known-state route proof D3 direct get

- Enabled request-free known-state `get` only when the compiler route proof is
  present and clean:
  - `known_operation_direct_ready` now marks route-proven `StateGet` as direct.
  - `StateSet` remains on the existing generic / late known-state path.
  - runtime direct get checks `direct_ready`, route proof id, role, handler id,
    payload proof, unit payload, and active state frame before returning the
    frame state.
  - guard failure falls through to the existing request path.
- Added D3 counters:
  - `known_operation_route_direct_get_attempts`
  - `known_operation_route_direct_get_hits`
  - `known_operation_route_direct_get_missing_proof`
  - `known_operation_route_direct_get_missing_frame`
  - `known_operation_route_direct_get_role_mismatch`
  - `known_operation_route_direct_get_payload_mismatch`
- Representative check:
  - `target/release/yulang --std-root lib --no-cache debug evidence-vm-plan bench/loop_for_sum_ref_20_discard.yu`
    - `direct_gets 2`
    - `direct_sets 0`
    - `route_proofs 3`
    - `direct_execution_disabled 1`
  - `target/release/yulang --std-root lib --no-cache debug evidence-vm-run --compare-control bench/loop_for_sum_ref_20_discard.yu`
    - `runtime_evidence.known_operation_route_direct_get_attempts: 800`
    - `runtime_evidence.known_operation_route_direct_get_hits: 800`
    - direct-get proof/frame/role/payload misses: `0`
    - `runtime_evidence.known_state_frame_reads_late: 0`
    - `runtime_evidence.known_state_frame_writes_late: 400`
    - `compare.control: match`
    - `timing.runtime_evidence_execute: 18.4ms`
- Validation:
  - `cargo fmt --all`
  - `timeout 180s cargo fmt --all -- --check`
  - `timeout 240s cargo check -q -p evidence-vm -p yulang`
  - `timeout 240s cargo test -q -p evidence-vm known_state -- --test-threads=1`
    - `1 passed`
  - `timeout 240s cargo test -q -p evidence-vm state_frame_ -- --test-threads=1`
    - `2 passed`
  - `timeout 240s cargo test -q -p yulang --test cli debug_evidence_vm_plan_marks_local_var_known_state_handler -- --test-threads=1`
    - `1 passed`
  - `timeout 240s cargo test -q -p yulang --test cli debug_runtime_evidence_run_handles_ref_set -- --test-threads=1`
    - `1 passed`
  - `timeout 240s cargo test -q -p yulang --test cli debug_runtime_evidence_run_known_state_frame_matches_control_across_nondet_resume -- --test-threads=1`
    - `1 passed`
  - `timeout 600s cargo build -q --release -p yulang`

## Evidence VM known-state route proof D4 direct set

- Enabled request-free known-state `set` for compiler route-proven local state:
  - `StateSet` is now direct-ready under the same boundary/proof checks as
    `StateGet`.
  - runtime direct set validates route proof id, role, handler id, payload
    proof, and active state frame before writing the frame state.
  - guard failure falls through to the existing request path.
- Added direct set counters:
  - `known_operation_route_direct_set_attempts`
  - `known_operation_route_direct_set_hits`
  - `known_operation_route_direct_set_missing_proof`
  - `known_operation_route_direct_set_missing_frame`
  - `known_operation_route_direct_set_role_mismatch`
  - `known_operation_route_direct_set_payload_mismatch`
- Added a small generic catch value-arm fast path:
  - `_` value arms with no guard first try `eval_immediate_value_expr`.
  - this avoids cloning the catch env for immediate value/local/alias arm bodies.
- Representative check:
  - `target/release/yulang --std-root lib --no-cache debug evidence-vm-run --compare-control bench/loop_for_sum_ref_20_discard.yu`
    - `known_operation_route_direct_get_hits: 800`
    - `known_operation_route_direct_set_hits: 400`
    - direct proof/frame/role/payload misses: `0`
    - `request_continuation_steps: 0`
    - `known_state_frame_reads_late: 0`
    - `known_state_frame_writes_late: 0`
    - `compare.control: match`
- Runtime execute repeat-10 rough medians after D4:
  - `bench/loop_recursive_sum_20_discard.yu`: about `1.5ms`
  - `bench/loop_for_no_ref_20_discard.yu`: about `5.9ms`
  - `bench/loop_for_sum_ref_20_discard.yu`: about `7.2ms`
  - before direct set, the ref sum case was about `18-19ms`.
- Remaining bottleneck:
  - `for` still creates thousands of loop-control thunks/adapters and catch
    value-boundary checks.
  - the next large step is a loop-control representation/lowering change, not a
    small request-path fix.
- Validation:
  - `cargo fmt --all -- --check`
  - `timeout 240s cargo check -q -p evidence-vm -p yulang`
  - `timeout 240s cargo test -q -p evidence-vm known_state -- --test-threads=1`
    - `1 passed`
  - `timeout 240s cargo test -q -p evidence-vm state_frame_ -- --test-threads=1`
    - `2 passed`
  - `timeout 240s cargo test -q -p yulang --test cli debug_evidence_vm_plan_marks_local_var_known_state_handler -- --test-threads=1`
    - `1 passed`
  - `timeout 240s cargo test -q -p yulang --test cli debug_runtime_evidence_run_handles_ref_set -- --test-threads=1`
    - `1 passed`
  - `timeout 240s cargo test -q -p yulang --test cli debug_runtime_evidence_run_known_state_frame_matches_control_across_nondet_resume -- --test-threads=1`
    - `1 passed`
  - `timeout 240s cargo test -q -p yulang --test cli debug_runtime_evidence_run_falls_through_value_arm_patterns_and_guards -- --test-threads=1`
    - `1 passed`
  - `timeout 600s cargo build -q --release -p yulang`

## Evidence VM loop-control adapter/thunk value inline

- Added value-only inline paths for return-like adapter/thunk work:
  - `apply_adapter_result` now executes arg adapt -> function apply -> return
    adapt in-place when each step returns a `Value`.
  - if any step produces an effect signal, it falls back to the existing
    continuation frames.
  - value-to-thunk adaptation now returns `Thunk::Value` directly instead of
    resuming a `WrapThunkValue` frame.
  - forcing `Thunk::Adapter` now adapts the inner value directly when the inner
    force returns a value.
  - `MakeThunk` with an immediate value/local/alias body and empty provider env
    now creates `Thunk::Value` directly.
- Added counters:
  - `apply_adapter_inline_attempts`
  - `apply_adapter_inline_hits`
  - `apply_adapter_inline_effect_fallbacks`
  - `adapt_value_inline_thunk_wraps`
  - `thunk_force_adapter_inline_hits`
  - `make_thunk_inline_value_hits`
- Added CLI canary:
  - pure `for` callbacks hit the adapter inline route and leave
    `continuation_resume_adapter_steps: 0`.
- Representative checks:
  - `bench/loop_for_no_ref_20_discard.yu`
    - `apply_adapter_inline_hits: 840`
    - `adapt_value_inline_thunk_wraps: 840`
    - `make_thunk_inline_value_hits: 840`
    - `continuation_resume_steps: 0`
    - `continuation_resume_adapter_steps: 0`
    - `compare.control: match`
  - `bench/loop_for_sum_ref_20_discard.yu`
    - `apply_adapter_inline_hits: 840`
    - `thunk_force_adapter_inline_hits: 420`
    - `continuation_resume_adapter_steps: 0`
    - known-state direct get/set still hit `800` / `400`
    - `request_continuation_steps: 0`
    - `compare.control: match`
- Runtime execute repeat-10 rough range after this slice:
  - `bench/loop_for_no_ref_20_discard.yu`: about `5.2-6.0ms`, median around
    `5.5ms`.
  - `bench/loop_for_sum_ref_20_discard.yu`: about `7.1-8.7ms`, median around
    `7.4ms`.
  - `bench/loop_recursive_sum_20_discard.yu`: about `1.4-1.5ms`.
- Remaining bottleneck:
  - adapter continuation frames are gone for the pure loop path.
  - the remaining loop gap is mostly `catch`/`Thunk::Expr` evaluation and env
    cloning around the std loop `sub` / `judge` encoding.
  - closing that gap likely needs a structural proof for these catch-wrapped
    control handlers, not another adapter continuation cleanup.
- Validation:
  - `cargo fmt --all -- --check`
  - `timeout 240s cargo check -q -p evidence-vm -p yulang`
  - `timeout 240s cargo test -q -p yulang --test cli debug_runtime_evidence_run_inlines_pure_for_adapter_thunks -- --test-threads=1`
    - `1 passed`
  - `timeout 240s cargo test -q -p yulang --test cli debug_runtime_evidence_run_carries_function_adapter_hygiene_markers -- --test-threads=1`
    - `1 passed`
  - `timeout 240s cargo test -q -p yulang --test cli debug_runtime_evidence_run_resumes_carried_callback_markers -- --test-threads=1`
    - `1 passed`
  - `timeout 240s cargo test -q -p yulang --test cli debug_runtime_evidence_run_handles_for_last_and_nullary_constructors -- --test-threads=1`
    - `1 passed`
  - `timeout 240s cargo test -q -p yulang --test cli debug_runtime_evidence_run_handles_ref_set -- --test-threads=1`
    - `1 passed`
  - `timeout 240s cargo test -q -p yulang --test cli debug_runtime_evidence_run_known_state_frame_matches_control_across_nondet_resume -- --test-threads=1`
    - `1 passed`
  - `timeout 600s cargo build -q --release -p yulang`

## Evidence VM catch body env-preserving proof

- Added a static `env_preserving_exprs` table to `RuntimeEvidenceRunner`.
  - The table proves only this local property: evaluating an expression does not
    extend the `Env` value passed by its caller.
  - `Stmt::Let` and `Stmt::Module` remain non-preserving.
  - `Catch` and `RefSet` are preserving because they isolate their mutable body /
    reference work behind their own env clones.
  - `Apply` is preserving when the callee expression is preserving; non-immediate
    args are already evaluated in an internal cloned env.
- `eval_catch` now reuses the caller env for catch bodies whose table entry is
  preserving, and keeps the old cloned-body path otherwise.
- Added counters:
  - `catch_body_env_clone_elided`
  - `catch_body_env_clone_kept`
- Extended the pure `for` CLI canary:
  - `catch_body_env_clone_elided: 20`
  - `catch_body_env_clone_kept: 0`
- Representative checks:
  - `bench/loop_for_no_ref_20_discard.yu`
    - `env_clones: 28017`
    - `catch_body_env_clone_elided: 2562`
    - `catch_body_env_clone_kept: 0`
    - `catch_body_checks: 2562`
    - `compare.control: match`
  - `bench/loop_for_sum_ref_20_discard.yu`
    - `env_clones: 35269`
    - `catch_body_env_clone_elided: 2563`
    - `catch_body_env_clone_kept: 0`
    - `catch_body_checks: 2563`
    - known-state direct get/set still hit `800` / `400`
    - `compare.control: match`
- Runtime execute repeat-10 rough range after this slice:
  - `bench/loop_for_no_ref_20_discard.yu`: about `5.7-6.7ms`.
  - `bench/loop_for_sum_ref_20_discard.yu`: about `7.4-8.2ms`.
  - `bench/loop_recursive_sum_20_discard.yu`: about `1.5-1.8ms`.
- Reading:
  - `Env` is a persistent one-binding chain, so this removes thousands of
    shallow clones but not a dominant wall-time cost.
  - Remaining pure loop cost still shows up as `Thunk::Expr` force plus
    catch/marker execution.
  - Moving marker frames after thunk-producing applies would need a structured
    proof that the `Apply` itself is effect-free. Current runtime `Apply` nodes
    do not carry that proof, so doing it here by shape would be a hygiene risk.
- Validation:
  - `cargo fmt --all -- --check`
  - `timeout 240s cargo check -q -p evidence-vm -p yulang`
  - `timeout 240s cargo test -q -p evidence-vm -- --test-threads=1`
  - `timeout 240s cargo test -q -p yulang --test cli debug_runtime_evidence_run_inlines_pure_for_adapter_thunks -- --test-threads=1`
    - `1 passed`
  - `timeout 300s cargo test -q -p yulang --test cli -- --test-threads=1`
    - `71 passed`
  - `timeout 600s cargo build -q --release -p yulang`

## README loop/effect benchmark snapshot

- Added a small loop/effect microbenchmark table to `README.md`.
- Measurement command:
  - `target/release/yulang --std-root lib --no-cache debug runtime-evidence-run --compare-control <bench>`
  - 20 samples each.
  - table reports only `timing.runtime_evidence_execute`.

| Program | Shape | Median runtime execute | Sample range | Median vs recursive |
| --- | --- | ---: | ---: | ---: |
| `bench/loop_recursive_sum_20_discard.yu` | direct recursive 20x20 sum | `1.5ms` | `1.4-3.8ms` | `1.0x` |
| `bench/loop_for_sum_ref_20_discard.yu` | std `for` loop with mutable sum | `7.5ms` | `7.0-8.3ms` | `5.0x` |
| `bench/nondet_20_discard.yu` | `(each 1..20 + each 1..20).list` discard | `10.4ms` | `9.4-11.1ms` | `6.9x` |

- Reading:
  - direct recursion is still the expected fastest baseline.
  - std `for` sum and nondet are now in the same order of magnitude once both
    pass through the effect-runtime machinery.
- Validation:
  - one `--compare-control` run for each benchmark:
    - `compare.control: match`

<!-- daily-append-anchor: 2026-07-01 -->
