# 2026-09-08

## Accepted-input authority and recovery simplification

- Recorded the user's explicit decision: reference Yulang2 only for formally
  accepted input and select reasonable successor recovery. The user also
  authorized recovery simplification, documentation/environment maintenance,
  and consideration of globally useful `chasa-recover` shorthand.
- `2026-09-08-successor-recovery-authority-amendment.md` supersedes the
  legacy-malformed parity prerequisites without weakening accepted grammar,
  current-Item ownership, typed output, structured extents/reservations,
  frozen/header consistency, or atomic public cutover. The shared delimiter
  gate selects numeric-head-plus-Call recovery explicitly instead of requiring
  the old numeric-head/separate-payload split.
- Independent architect/compiler/spec work predates the latest instruction.
  The authority repair is clean; the primary resolved the final numeric
  pre-write gap with explicit witnesses, fields, continuation and accepted
  controls. The user then requested direct work without agents, so subsequent
  implementation/verification is primary-owned and is not described as
  independent review.
- Preserved the former 2,710-line `tasks/current.md` verbatim under
  `notes/progress/rewrite-state-before-recovery-authority-2026-09-08.md`, with a
  historical banner. The new task entry is concise navigation. Updated the
  design index and former scalar/payload/primary status pointers.
- This checkpoint changes records only. The existing two-file delimiter
  candidate stays unstaged until its selected successor proof closes. No
  broad compiler tests or performance measurements are needed for records.

## Environment audit

- The workspace-local `chasa-recover` provides `token`, `maybe`, and zero-copy
  `with_str`; inspected repeated call sites do not justify a new generic
  recovery API. CST construction, fences and delimiter policy stay with
  grammar owners. No library API or dependency change was made.
- Before the no-agent direction, read-only environment work ran
  `cargo xtask check-graph`: passed. It validates dependency direction, not
  parser behavior.
- That audit also ran
  `cargo test -p yu-syntax rewrite::tests::type_decl -- --test-threads=1`:
  33 passed, six known assertion failures. The failures match the recorded
  `b85c32f3` baseline and are now listed in `tasks/current.md`; they are not
  build, linker or cache failures. No expectation was weakened.
- No `rust-toolchain.toml` is present. Toolchain pinning is not a repair for
  these failures, and is not introduced without an actual reproducibility
  requirement. Safe focused commands are recorded in the task entry.

## Shared delimiter integration

- Authority/navigation checkpoint `c74b2944` was committed and pushed to
  `origin/yulang3`. The subsequent implementation integrates the retained
  two-file shared delimiter candidate, with no `variants.rs` or public change.
- Added one common fresh/frozen completion assertion and selected P/E/numeric
  structured-carrier tests. These verify full records, origin translation,
  emitted CST extents, native outer closes, outer path continuation, prefix
  and recursive reservations, and accepted numeric/PV Types.
- Initial `shared_delimited` filter: six passed, one test-construction error.
  The new test accidentally required the horizontal rule's Missing node in
  the excluded no-gap E row. Corrected that expectation to preserve the
  already-approved unchanged no-gap handling; no production patch was made
  to accommodate it. Missing-close publication for that E row remains T4E.
- Final focused commands:
  - `cargo test -p yu-syntax --lib rewrite::tests::type_expr:: -- --test-threads=1`:
    124 passed;
  - `cargo test -p yu-syntax --lib rewrite::tests::output:: -- --test-threads=1`:
    4 passed;
  - `cargo test -p yu-syntax --lib rewrite::tests::recovery_output:: -- --test-threads=1`:
    25 passed;
  - `cargo check -p yu-syntax`: passed, with existing warnings;
  - scoped `rustfmt --edition 2024 --check` and `git diff --check`: passed.
- Sibling check reused the built binary:
  `target/debug/deps/yu_syntax-b491637626fa8c73 rewrite::tests::type_decl:: --test-threads=1`.
  The same six tests fail, 33 pass. Concrete failures include accepted path
  `A::with` and nested Call `A(B with Inner)`: contextual boundary decisions
  stop them early. They are not missing recovery-harness plumbing. The next
  repair prioritizes accepted grammar, retaining the assertions.
- Static cost: bounded extra checks of the current Item and at most its one
  coalesced whitespace part; no new allocation, traversal or state. Zero
  performance samples/processes. Broad/public certification remains deferred.
- No agents were used after the user's direct-work instruction. Prior review
  evidence is retained, but this completion pass is primary verification.

## Library API documentation consistency

- `f50c4c20` completes the private delimiter checkpoint. Separately corrected
  the stale any-state `In::check` statement in the direct-function amendment
  by marking its narrow supersession explicitly. The approved minimal-token
  amendment §2 and implementation-status repair already require unit-state
  `check`; `input.rs`, `README.md` and `DESIGN-0.2.md` agree.
- Clarified the minimal-token amendment's supersession list and removed its
  misleading description of outer-state `check` reborrowing. History and the
  no-`IsCut` decision remain intact; no API/behavior was changed.
- `with_str` already expresses the global consumed-text operation across
  identifier, integer and operator scanning. Candidate shortening of token
  construction or visible-suffix probes would leak Yulang token/fence/recovery
  policy into the library. No feature is added without a genuinely common
  transaction contract. This is a present finding, not a ban on future API.
- Record-only checks: governing-source/implementation cross-reference and
  `git diff --check`. No repeat compiler suite or timing measurement.

## Contextual Type boundary correction

- Corrected accepted `A::with`-class paths so same-line contextual identifiers
  win the initial PathSegment slot. LF, CRLF and newline-bearing comment
  controls retain the contextual Item for the declaration owner and publish the
  existing PathSegment Missing at the pre-Item frontier.
- Removed the enclosing `TypeOuterBoundary` from the shared delimiter
  signature. Only Call had ever carried a nonempty value; its committed body
  now parses every argument with a fresh `NONE` boundary and resumes the outer
  value after return. Explicit Stops, actual outer closes, fences, Type-ML
  provenance and owner-local horizontal trivia remain unchanged.
- Classified three stale declaration expectations under current authority.
  Outer-TypeApply Parenthesized controls use explicit commas to remain
  no-recovery accepted syntax under T4. The ambiguous malformed
  `(Eq::@ = Int)` row no longer demands speculative future-close knowledge,
  and the `(A else` row reflects the already-approved owner-local horizontal
  trivia rule. No Yulang2 malformed recovery was treated as authority.
- Final direct checks: Type 126 passed; TypeDeclaration 39 passed; output 4
  passed; recovery output 25 passed; package check, scoped rustfmt and diff
  check passed. Existing warnings remain. Static cost removes one carried
  argument and adds only bounded current-Item predicates; zero timing
  samples/processes. No subagents were used.

## PV current-Item recovery and typed publication

- The environment's read-only `.git` restriction was removed. The verified
  contextual-Type checkpoint was committed and pushed as `f8a7743f` on
  `yulang3` without repeating unchanged tests.
- Selected scoped Type-expression recovery for wrong-kind PV names, including
  tight tails, and immediate tag-owner handoff for boundaryless malformed
  Items. The old primary-completion/scalar-parity proposals are superseded;
  accepted PV grammar and structured-output invariants remain in force.
- Migrated every PV-owned raw Error/Missing site. Shared one total forward
  malformed-run operation between tag and payload recovery and fixed tag retry
  swallowing an explicit caller stop. Retry leading remains with the owning
  tag/payload or returned caller Item. Reused the Type token-kind classifier.
- Eight new focused tests verify all PV-owned record fields, native/error
  extents, distinct frozen IDs, accepted controls, tight-tail structure,
  recursive reservation order, explicit punctuation/word caller stops,
  LF/CRLF, Unicode/comments, shifted origins and quoted fence boundaries.
  Corrected only the new test table's miscounted LF/CRLF EOF offsets after its
  first run; ownership and production code were unchanged by that correction.
- `cargo test -p yu-syntax --lib rewrite::tests::type_expr:: -- --test-threads=1`:
  134 passed. Reused `target/debug/deps/yu_syntax-b491637626fa8c73` for
  `rewrite::tests::type_decl::` (39), `rewrite::tests::output::` (4), and
  `rewrite::tests::recovery_output::` (25), all with `--test-threads=1`, passed.
  `cargo check -p yu-syntax`, scoped rustfmt and diff check passed. Existing
  warnings remain; zero timing samples/processes and no subagents.
- Updated the PV design/site ledger, superseded proposal/index and task entry.
  Remaining Type-owned raw sites and aggregate embedded/header/public gates
  remain open. No new `chasa-recover` API was needed.

## Parenthesized/EffectRow typed current-Item recovery

- Completed the P/E-owned Item, Separator and Close sites under
  `2026-09-08-successor-pe-current-item-recovery.md` (M2, primary-only per the
  user). Retry leading is owner-emitted outside Item Error; actual matching
  closes win; unclaimed mismatches use native typed close Errors and retry;
  explicit caller/fence boundaries remain complete pending Items.
- Activated EffectRow's approved OuterTypeApply-only Type-ML provenance.
  Standalone TypeApply remains accepted. Reused one local post-Error resume
  operation for Item and close recovery; no new `chasa-recover` capability,
  partial-Item terminal, lookahead or retained source is needed.
- Updated twelve construction-stage/replaced-recovery tests while retaining
  exact source/pending/CST/cursor checks. Added seven focused tests. The new
  fence test's raw-Item emitter misuse and mistaken P-style E anchor were
  fixture errors, corrected against the boundary API and written contract.
- `cargo test -p yu-syntax --lib rewrite::tests::type_expr:: -- --test-threads=1`:
  141 passed. Reused `target/debug/deps/yu_syntax-b491637626fa8c73` for
  `rewrite::tests::type_decl::` (39), `rewrite::tests::output::` (4), and
  `rewrite::tests::recovery_output::` (25), with `--test-threads=1`: all passed.
  `cargo check -p yu-syntax`, scoped rustfmt and diff checks passed. Existing
  warnings remain; zero timing samples/processes. No independent review or
  broad workspace suite was claimed.
- Synchronized the P/E design, supersession notes, index and task entry.
  BracketRow/arrow/head, record, forall and aggregate embedded/header/public
  certification remain open; private Type progress is not public cutover.

## BracketRow typed current-Item recovery

- Completed BracketRow Item/Separator/Close construction under
  `2026-09-08-successor-bracket-row-current-item-recovery.md` (M2, primary-only).
  Kept accepted attachment/full-Type/layout grammar and the distinct close-only
  retry. Fixed explicit caller-word/known-outer-close priority before opening
  or retry-leading emission. Filled the approved no-gap separator site.
- P/E/B now share one total Item-error scan with the owner's newline stop;
  B retry leading is row-owned outside Error. Removed its duplicate raw scan
  and raw close helper; the shared delimiter module contains no raw
  Error/Missing construction. No new generic API or partial-Item terminal.
- Six new focused tests pass, including exact fresh/frozen/shifted records,
  native punctuation, five caller phases, close-only handoff, quoted fences
  and nested PV reservations. Three older tests changed only the selected
  retry-leading/raw-B expectations; accepted controls remain unchanged.
- `cargo test -p yu-syntax --lib rewrite::tests::type_expr:: -- --test-threads=1`:
  147 passed. Reused `target/debug/deps/yu_syntax-b491637626fa8c73` with
  `--test-threads=1` for TypeDeclaration (39), output (4) and recovery output
  (25): all passed. Package check, scoped rustfmt and diff check passed.
  Zero benchmark samples/processes; no independent-review or broad-suite claim.
- Synchronized the design/site ledger, supersession note, index and task entry.
  Mandatory row arrow/head, record/forall and embedded/header/O6/public
  adoption remain open. Checkpoint before this slice: P/E `e3b34fea`, pushed.

## BracketRowArrow typed current-Item recovery

- Completed the private T4A producer under
  `2026-09-08-successor-bracket-arrow-current-item-recovery.md` (M2,
  primary-only). Filled malformed arrow retry with one total typed Error run,
  preserving retry trivia outside Error and caller/newline/fence Items.
- Typed all Arrow Missing sites and filled the previously silent non-EOF
  incomplete-row handoff. Row close and required arrow remain distinct slots;
  recovered arrow/RHS retry and Error boundary exits do not cascade Missing
  ArrowRhs. Valid-path leading classification is reused rather than repeated.
- Five focused tests pass. B-boundary expectations gain the parent arrow
  record; row counts stay owner-specific. Two older non-EOF node-count tests
  gain the selected arrow slot and explicit complete record assertions.
- `cargo test -p yu-syntax --lib rewrite::tests::type_expr:: -- --test-threads=1`:
  152 passed. Reused `target/debug/deps/yu_syntax-b491637626fa8c73` with
  `--test-threads=1` for TypeDeclaration (39), output (4) and recovery output
  (25): all passed. Package check, scoped rustfmt and diff check passed.
  Existing warnings remain. Zero benchmark samples/processes; no independent
  review or broad workspace suite was claimed.
- Updated the design, supersession note, index and task entry. Next:
  LeadingEffectTypeHead, record/forall and aggregate embedded/header/O6/public
  adoption. The B checkpoint `32db97d3` was committed and pushed before this
  slice; public/root cutover remains unchanged.

## LeadingEffectTypeHead typed current-Item recovery

- Completed all mandatory-head Missing/Error publication under
  `2026-09-08-successor-leading-row-head-current-item-recovery.md` (M2,
  primary-only). A recovery-local delimiter stack and one total Item run
  replace the ordinary transactional and dedicated fenced balanced suffix.
  Removed approximately 240 lexer lines; accepted Type grammar is unchanged.
- The run retains native tokens, keeps comments opaque, and returns complete
  caller/non-continuation-newline/fence Items. Unclosed second rows consume
  their safe prefix without replay. Initial/retry leading stays outside Error;
  incomplete row-close and required-head records remain distinct.
- Seven new head tests and one new normalized foreign-prefix boundary control
  were added. Four old normalized controls now follow current-Item ownership;
  the ordinary transactional claim was renamed. Two older Type routing tests
  gain only their newly typed parent head record, preserving nested Call
  ownership and full frozen-record comparisons. New-test API/import mistakes
  were corrected before behavioral verification.
- `cargo test -p yu-syntax --lib rewrite::tests::type_expr:: -- --test-threads=1`:
  159 passed. Reused `target/debug/deps/yu_syntax-b491637626fa8c73` with
  `--test-threads=1` for `rewrite::tests::normalized::normalized_type` plus
  `rewrite::tests::normalized::ordinary_type_unmatched` (27), and for
  `rewrite::tests::type_decl::` (39), `rewrite::tests::output::` (4), and
  `rewrite::tests::recovery_output::` (25): all passed. Package check, scoped
  rustfmt and diff checks passed. Existing warnings remain. Zero benchmark
  samples/processes; no independent review or broad workspace certification.
- Audited the T7a/T7b register: its quoted EffectRows cannot witness a missing
  head after a bare leading BracketRow. Recorded corrected bare-row literals
  and linked them from the historical matrix without claiming embedded proof.
  Updated the design result, index and task entry. Record/forall, caller-owned
  Type Missing publication and aggregate embedded/header/public gates remain
  open. The prior arrow checkpoint `ae54e9f1` was committed and pushed.

## Named-record field-internal typed recovery

- Completed RecordFieldName, RecordFieldColon and RecordFieldType under
  `2026-09-08-successor-record-field-current-item-recovery.md` (M2,
  primary-only). Colon and RHS share a total forward Error run; fresh and
  recovered slots protect caller words before NUD admission. Missing uses the
  remaining-start/abstract anchor and leaves boundary leading to the sequence.
- Removed the parent's now-unused raw record RHS helper and ordinary wrapper.
  The name authority probe's existing algorithm/depth policy remains a
  sequence-preflight obligation, not independently certified by typed emission.
- Found and fixed the accepted empty-trivia nested-record gap `{a:{b:B}}`:
  the lexer's one-byte PolymorphicVariantColon is an actual field colon in a
  field-colon slot. A local shared predicate fixes field dispatch, retry and
  the next-field query without splitting `::` or changing actual PV RHS.
  Two old malformed normalized controls deliberately change from omitted
  field-colon/PV recovery to actual colon/nested record. Their original inputs
  remain, with explicit-PV retry and exact inner/outer role controls added.
- Eight focused tests pass. Final
  `cargo test -p yu-syntax --lib rewrite::tests::type_expr:: -- --test-threads=1`:
  167 passed. Reused `target/debug/deps/yu_syntax-b491637626fa8c73` with
  `--test-threads=1` and the combined filters
  `rewrite::tests::normalized::normalized_type`,
  `rewrite::tests::normalized::ordinary_type_unmatched`,
  `rewrite::tests::type_decl::`, `rewrite::tests::output::`, and
  `rewrite::tests::recovery_output::`: 95 passed (27 + 39 + 4 + 25).
  Package check, scoped rustfmt and diff checks passed. Existing warnings
  remain; zero benchmark samples/processes and no independent review.
- Updated the design result, index and task entry. Next: record whole-field,
  separator/close and forall, then the remaining typed-output and actual
  embedded/header/public gates. Leading-head checkpoint `269ae763` was
  committed and pushed before this slice. Public dispatch is unchanged.

## Named-record sequence and close typed recovery

- Completed all record-owned recovery sites under
  `2026-09-08-successor-record-sequence-current-item-recovery.md` (M2,
  primary-only). One phase-aware sequence loop owns all exits; one total
  kind-matching run handles whole-field/separator/name/close Errors. Removed
  the raw duplicate loops and the parent's two record-only Missing helpers.
- The malformed-name probe now includes its initial opener and caller-word
  spelling, stops before any protected boundary and matches delimiter kinds.
  A nested caller claim is carried only to the immediate handoff, preventing
  the outer sequence from reopening that Item as a local field. Abstract
  fences do not manufacture an implicit separator; ordinary EOF-newline
  fields, post-comma slots and nested close instances retain distinct records.
- Nine new sequence tests cover exact/frozen/shifted/seeded records, native
  close-only recovery, complete caller Items, actual outer Call continuation,
  name authority, nested delimiters, quoted fences and structured PV order.
  Existing field controls gained their previously raw outer records; the
  selected nested-colon authority correction changes Name to whole Field.
  Original literal controls were retained, including the two unclaimed-close
  cases. The independent lexical control governs the new EOF exit line-entry.
- `cargo test -p yu-syntax --lib rewrite::tests::type_expr:: -- --test-threads=1`:
  176 passed. Reused `target/debug/deps/yu_syntax-b491637626fa8c73` with the
  combined filters `rewrite::tests::normalized::normalized_type`,
  `rewrite::tests::normalized::ordinary_type_unmatched`,
  `rewrite::tests::type_decl::`, `rewrite::tests::output::`, and
  `rewrite::tests::recovery_output::`, plus `--test-threads=1`: 95 passed
  (27 + 39 + 4 + 25). Final extra nested-caller/real-Call controls passed via
  `cargo test -p yu-syntax --lib rewrite::tests::type_expr::record_ -- --test-threads=1`
  (17 tests); no implementation changed after the full Type pass.
  Package check and scoped rustfmt/diff checks passed, with the existing 87
  package / 38 test warnings. No independent review or broad certification.
  Zero benchmark samples/processes. Initial test compilation took 2m24s;
  one read-only resource sample found rustc around 1.8 GiB RSS, with memory
  available. Subsequent rebuilds took 38s/31s; test execution was sub-second.
- Corrected the matrix's T5a–T5h coverage-label typo to its actual seven-row
  T5a–T5g inventory. The T5f literal also accidentally closes a valid trailing
  comma record while claiming an EOF missing field; the design records a real
  unclosed-reference EOF witness and the matrix links that correction. Its
  aggregate embedded row remains open. No chasa-recover extension was needed:
  existing token operations suffice without moving owner policy into the library.
- Updated task, field/sequence designs, index, matrix navigation and this
  daily record. Next: forall, caller-owned required-Type Missing, remaining
  typed-owner/embedded/header evidence and public/root replacement. The prior
  field-internal checkpoint is `2860ae6a`; public dispatch is unchanged.

## Forall typed phase-local recovery

- Completed private Forall-owned construction under
  `2026-09-08-successor-forall-current-item-recovery.md` (M2, primary-only).
  Recorded the selected malformed ownership before editing code/expectations:
  first-binder malformed runs remain Binder, fresh post-binder comma/semicolon
  stays BinderBoundary, other post-binder runs are Colon, and actual-colon RHS
  uses Body. An existing Colon Error survives later binders without a duplicate
  Missing. The driver is iterative and needs no target-dependent source probe.
- Corrected the mandatory one-byte colon before `{` so `for 'a:{b:B}` has a
  valid record body. Kept original malformed compound-colon controls, recorded
  their deliberate record/PV ancestry changes, and added a genuine PV retry.
  Full pending Items survive nested caller stops, mismatches and fences;
  malformed-run matching state returns its immediate target to the owner.
- Ten new tests cover exact/frozen/shifted/seeded records, native Error nodes,
  slot separation, contextual-body suspension, quoted CRLF fences, real Call
  continuation, structured PV ordering and bounded long punctuation. The
  `forall` filter passed 32 tests, including six legacy grammar controls;
  no malformed-output parity was inferred from them. Full Type passed 186.
  The existing test binary's combined normalized Type/unmatched head,
  TypeDeclaration, output and recovery-output filters passed 95 (27+39+4+25).
  Exact commands are in the design's construction result. Package check and
  scoped rustfmt/diff checks passed, with the existing 87/38 warnings.
- One implementation pass, zero repairs, no independent review or broad
  certification; zero benchmark samples/processes. Test build took 2m04s;
  one environment observation found active rustc around 570 MiB RSS. Existing
  sealed Error/chasa-recover primitives suffice; no library API was added.
- Synchronized design, index, task and daily record. Next: the caller-owned
  required-Type sites and the complete typed-owner ledger/SCC migration, then
  actual embedded/header/full/public gates. The preceding record-sequence
  checkpoint is `196570d6`; public dispatch remains unchanged.

## Required-Type caller-role publication checkpoint

- Implemented the explicit missing-role transport under
  `2026-09-08-successor-required-type-missing-roles.md` (M2, primary-only).
  Pattern, declaration, derives, Struct and Enum/Error variant/field callers
  choose their own fresh Missing role. Existing malformed Type::Primary and
  nested Type roles are unchanged. The variant shell now supplies its small
  Enum/Error identity; shared fields receive only the selected missing-Type
  role. Removed five zero-caller ordinary required-Type wrappers, and made
  the three isolated normalized wrappers test-only with explicit Primary role.
- The two raw required-Type Missing branches now share one typed emission
  using remaining-start or inspected-boundary coordinates. Existing ArrowRhs,
  LeadingEffectTypeHead and BracketRowArrow Missing factories retain their
  exact Type roles. Type/PV implementation has no raw Missing/Error sites;
  this does not certify callers' separate raw bypass construction.
- Five new tests cover seeded/frozen/shifted output, complete caller/fence
  Items, actual declaration/Pattern Missing roles and unchanged malformed/
  nested roles. Fixed the new derives harness's false assumption that a
  nominal declaration consumes `;`: the original literal is retained with an
  exact pending-semicolon check, following the existing TypeDeclaration test.
  The quoted derives case gains only its RoleReference Missing at 6215.
- `cargo test -p yu-syntax --lib rewrite::tests::type_expr:: -- --test-threads=1`:
  191 passed. Package check, scoped rustfmt and diff check passed (87/38
  existing warnings). Two bounded repairs: mechanical argument/draft
  conversion, then the new nominal-terminator harness. Zero benchmark samples/
  processes, no independent review, no generic chasa-recover API change.
- Wider serial caller filters: **424 passed, 5 failed**, with no skips.
  The design lists the exact command filters and failing tests. Open issues:
  nested Pipe versus Unknown materialization; spaced arrows in inherited
  Parenthesized Type-ML under role and impl; Pattern P/E caller-close leading;
  Pattern named-record close-leading ancestry. Those expectations were not
  changed, and an unmodified baseline was not executed. This checkpoint does
  not close wider caller conformance. Static tracing also found tuple-field
  `=` can reenter an unchanged pending Type boundary; bounded reproduction
  and the owning progress fix are next. Matrix D4e's outer Error role needs
  reconciliation with SD-T, not an implicit recovery-oracle assumption.
- Synchronized design, index, task and this daily record. Forall checkpoint
  `33cfab97` is committed and pushed. Public/root dispatch is still unchanged;
  O3b/ledger, embedded, header/full, Yumark and public adoption remain open.

## Type caller-control conformance

- Closed the five failures from required-Type checkpoint `2918fa51` under
  `2026-09-08-successor-type-caller-conformance.md`. Pre-write causal audit
  identified stale expectations against T3 Call Unknown payloads, inherited
  Type-ML all-nonempty-trivia stops, P/E horizontal caller-gap ownership, and
  record-sequence rather than failed-field ownership of close-leading trivia.
- All original source literals remain. Added tight-arrow and ordinary Call
  accepted examples, explicit role/impl spaced-arrow Error controls, and
  comment-bearing caller-gap preservation. Stronger bar ancestry and record
  direct-child assertions retain source/outer-close ownership. No production
  parser code, accepted grammar, or public fixture changed.
- Pattern filter: 25 passed; role/impl/variant filters: 43 passed. The same
  16 owner/output filters from the checkpoint now pass 432 with no failures
  or skips (1.19s execution). Test build 39s, 38 existing warnings. Scoped
  rustfmt/diff checks passed; unchanged package check carried forward.
- M2 primary-only, one pass, zero repairs, no independent certification, zero
  benchmark samples/processes. Synchronized both designs, index, task and this
  record. Next: bounded tuple-field nonprogress fix, D4e role reconciliation,
  typed ledger and remaining SCC/embedded/header/full/public gates.

## Required-Type Equals ownership and tuple forward progress

- Fixed the unowned-Equals boundary in the mandatory Type kernel under
  `2026-09-08-successor-required-type-equals-ownership.md`. Tuple loops had
  received the same unconsumed Equals plus a Missing repeatedly. Existing
  TypeOuterBoundary::EQUALS now determines ownership; Pattern explicitly
  forwards its active Equal stop. Named-field and other unclaimed mandatory
  Type siblings recover through the ordinary typed Primary Error run.
- Consolidated Pattern's three Type calls without changing IN completion,
  removed the unused plain wrapper, and made the remaining isolated adapter
  test-only. No field-specific Equals branch, loop cap, fallback scanner,
  policy flag, stop bit, or chasa-recover API was added.
- Bounded single-callee reproduction failed before, passed after. Five Type
  tests, plus new Pattern/binding tests, cover exact roles/evidence, fresh/
  frozen/shifted/seeded output, repeated/malformed Equals, same-slot retry,
  actual Struct/Enum/Error fields, complete outer close/EOF/fence Items,
  accepted controls and real binding Equals after nested recovery.
- Capped subprocess: 5 tests passed in 0.06s under 5s / 1 GiB limits. Type/
  Pattern: 222 passed; 19 expanded caller/output filters: 470 passed, no
  failures/skips, 1.10s. Exact commands are in the design result. Package,
  scoped rustfmt and diff checks passed (87/38 existing warnings).
- M2 primary-only, one pass, zero repairs, zero benchmark samples/processes.
  Final test build 1m08s; one rustc observation about 1.53 GiB RSS. No
  independent certification or broad workspace check. Synced design/index/
  task/daily; preceding caller-alignment checkpoint `471e2f9f` is pushed.
  Next: Type/PV ledger, D4e correction and O3b SCC; public parser unchanged.

## O3a Type/PV construction ledger and documentation audit

- Created the accumulating callsite ledger at
  `notes/progress/successor-typed-recovery-ledger.md`, following the typed-output
  amendment's 15-column contract. It maps all Type/PV publication helpers,
  all 28 TypeRole names, six close owners, caller-Missing transport, range/
  evidence/expectation/order rules, local tests and RB assignments. The
  admitted-only ApplyArgument path has an explicit no-own-recovery proof.
- Static Type/PV search finds zero raw Missing/Error constructors. Reference
  validation resolved 34 source functions, 50 test names/prefixes and all
  TypeRole names, with no failures. This is construction inventory and local
  evidence, not independent or O4/O6 certification. Open SCC groups remain
  visible in the same ledger.
- Recorded D4e's narrow role correction in its own Authoritative clarification:
  the unchanged `R({struct S { f: @ }})` witness has Type::Primary Error, not
  Struct::FieldType Error, under SD-T/SD-R. The historical matrix cell remains
  with explicit successor navigation; its actual embedded evidence is open.
- Corrected T3's stale suspension header to point to the already implemented
  recovery-authority §4 rule. Preserved historical decisions. Synchronized
  typed-output construction status, design index, task and daily record.
- M0 record-only pass, primary-only, zero benchmarks and no repeated broad
  build/test. Reused the 470-test/package/format proof from committed/pushed
  `634d5b46`; diff and reference checks pass. Next: Pattern primary/symbol/
  alias/alternation within O3b, then delimiters and the remaining SCC.
  Public/root/header dispatch is unchanged.

## O3b Pattern primary and tail-slot typed construction

- Recorded the bounded current-Item contract before implementation in
  `2026-09-08-successor-pattern-primary-current-item-recovery.md`, following
  the user's recovery-selection and no-subagent directions. Primary,
  SymbolName, AliasBinding and AlternationRhs are typed; the latter is an
  explicit immediate slot role. No raw recovery remains in `pattern.rs`.
- Error ends before a retry's leading; that leading belongs directly to
  Pattern or PatternAliasTail. Malformed alias retry now checks layout/IN
  before accepting a name. Fresh accepted multiline names and a retried
  same-line `as` retain their grammar. The shared lexical scanner uses
  existing `with_str`; no new generic API or partial-leading primitive.
- Retained all old source literals. The first run passed 23/26; the three
  failures were the preselected `@ x`, list-item and nested-record retry-gap
  change. Recorded exact adjudication before updating those assertions and
  strengthened direct-owner whitespace checks. Added seven exact-record,
  fresh/frozen/seeded/shifted, caller/fence, native-token and rejected-symbol
  tests. Two mechanical new-harness compilation corrections were needed.
- `cargo test -p yu-syntax --lib rewrite::tests::pattern:: -- --test-threads=1`:
  33 passed; the 19 serial owner/output filters recorded in the design pass
  477, no failures/skips, 1.89s. Package check (16.16s), scoped rustfmt and
  diff check passed; existing 87/38 warnings. Final test build 1m11s.
- M2 primary-only construction/delta reread, no independent certification,
  zero behavioral repair rounds and zero benchmark samples/processes. Added
  P1–P4/P8 callsites and local RB proof to the accumulating ledger and synced
  task/design/index/daily records. Next: Pattern delimiter roles/recovery,
  then the remaining SCC. Actual embedded/header/full/Yumark/public parser
  replacement remains open; public/root/header dispatch is unchanged.

## O3b Pattern delimiter Missing and delegated roles

- Completed the bounded publication gate in
  `2026-09-08-successor-pattern-delimited-slot-publication.md`: explicit
  ParenthesizedElement/ListItem/ListSpreadRhs/RecordNestedPattern/
  RecordSpreadRhs roles, typed record-item/separator Missing and three
  close-Missing owners. Nested symbols, aliases and alternations retain their
  own roles. Shared primary-run extents from `afaa3b24` are unchanged.
- Unified close-Missing handling while preserving all caller/fence leading
  and ordinary EOF emission. Removed only the unreachable record-field
  Missing fallback after proving its one caller admits a name or spread;
  the name precondition is explicit. No accepted grammar or CST expectation
  changed. Four raw sequence/close Error and two default-Expression Missing
  sites remain open and listed in the same ledger.
- Added seven tests through the shared seeded/frozen/shifted harness:
  exact roles/ranges/direct parents, inner-before-close order, true outer
  close ownership, full pending Item/EOF, quoted fences, accepted layout,
  spreads, defaults and duplicates. Pattern: 40 passed, build 33.50s,
  execution 0.10s. The same 19 owner/output filters: 484 passed, no skips/
  failures, 1.88s. Package check passed in 6.67s; scoped formatting and diff
  checks passed, with existing 87/38 warnings.
- M2 primary-only, one pass, zero repairs, no independent certification,
  zero benchmark samples/processes. Synced design/index/task/ledger/daily.
  Prior `afaa3b24` is committed/pushed. Next: sequence/close Error, default
  Expression and remaining SCC; embedded/header/full/Yumark/public gates
  and the old-parser replacement remain open.

## O3b Pattern sequence Error and literal-boundary correction

- Completed `2026-09-08-successor-pattern-sequence-current-item-recovery.md`
  under the user's recovery-selection delegation. One phase-aware owner loop
  replaces four raw Error sites with native close records, maximal lexical
  units and two scoped Record structured roles. Their canonical Pattern child
  protects nested delimiters/literals; reservations and exact emitted extent
  remain the existing output contract. Initial/retry trivia stays outside
  lexical Error, with no same-slot or separator Missing cascade.
- Bounded pre-change tests failed for `{"""}""", a}` (premature close)
  and accepted layout literals. Both now pass. Corrected sequence admission
  to the canonical Pattern-primary judge and preserved whole literal openers
  in tail Items. LC-5/comma-or-layout acceptance is unchanged. All original
  malformed fixed-spelling sources remain, with pre-adjudicated direct leading.
- Eight tests cover exact roles/ranges/native evidence, seeded/frozen/shifted
  ordering, recursive structured reservations, actual outer closes, EOF,
  full caller/fence Items and foreign-prefix emitted bounds. The first pass
  passed 46/48; one repair bundle fixed a boundary-first phase query and a
  test misclassification of the accepted field-colon `{a :tag,b}`. That input
  remains an accepted control; newline Symbol exercises the intended phase.
  An earlier compile attempt needed two mechanical corrections.
- Focused Pattern: 48 passed, 0.21s. The existing 19 owner/output filters plus
  `normalized_pattern` and `literal::`: 533 passed, no failures/ignored,
  1.95s. Package check passed in 5.65s; scoped rustfmt/diff passed; existing
  38/87 warnings. One pre-change build observation was about 1.5 GiB rustc;
  no benchmark was run (zero samples/processes).
- M2 primary-only, no independent certification. Synced design/index/task/
  single ledger/daily. No new chasa-recover API; the existing sealed lexical
  and reserved structured operations suffice. Two RecordDefaultExpression
  raw Missing calls remain; no raw Pattern Error remains. SCC, actual
  embedded/header/full/Yumark and atomic public cutover stay open.

## O3b Pattern default Expression and zero raw Pattern-owned recovery

- After committing/pushing sequence recovery as `5de04545`, completed
  `2026-09-08-successor-pattern-default-expression-publication.md` under the
  existing P7c/default grammar and user recovery delegation. Consolidated two
  raw Missing branches into typed RecordDefaultExpression inside the required
  empty OperatorChain. Both field forms forward explicit caller-close authority;
  nonlocal close/fence Items keep all leading, while local RBrace still wins.
- There are zero raw Missing/Error constructors in `pattern.rs` and `pattern/`.
  This is the Pattern-owned inventory, not the recursive owner closure:
  Expression, literal, Statement/declaration dependencies remain open. Default
  NUD admission, canonical Expression dispatch and exact-Equals lexical policy
  are unchanged; no new generic library or output API was needed.
- The pre-change `{a=}` test failed for absent metadata. Six tests now cover
  required ancestry, ordered nested/structured/sequence records, complete
  caller/fence Items, shifted/frozen/seeded output, EOF and accepted defaults.
  Initial 52/53 (related 537/538) failures localized to a newly added accepted
  control that violated maximal exact-Equals spelling. Retained both original
  quote-adjacent sources as rejected-Equals/source-identity controls, and used
  spaces for accepted literal defaults. No production repair; the lexical
  test needed one explicit LexIn annotation. The old `{a =}` source now asserts
  the already-authoritative OperatorChain wrapper, not a direct field Missing.
- Final Pattern: 54 passed, build 32.09s / execution 0.25s. Same 21 related
  filters: 539 passed, no failures/ignored, 2.22s. Package check passed in
  6.96s; scoped rustfmt/diff checks passed. Existing 38/87 warnings; zero
  benchmark samples/processes. No workspace or actual header/full/Yumark/public
  certification. Exact replay commands are in the default-Expression design.
- M2 primary-only, one test-premise adjudication and no independent review.
  Synced task/design/index/single ledger/daily, including stale ledger counts
  and test-module locators. Next: Expression required operands and their
  callers, then remaining SCC; old-parser replacement stays active.

## O3b Expression operand design and user-requested pause

- Last completed production checkpoint: committed/pushed `7832ab2f`, following
  `5de04545`; related 539 tests and package checks passed at that gate. Current
  Expression work is preparation, not another completed construction step.
- Recorded `2026-09-08-successor-expression-operand-current-item-recovery.md`
  under delegated recovery selection. The shared mandatory callee receives
  explicit Nud/If condition/CaseLike scrutinee or guard/For iterable or body
  roles; nested accepted owners keep their roles. Selected protected-boundary
  Missing, one sealed lexical Error run, unchanged threshold/ML and the
  existing total current-Item scanner. No new generic library API is needed.
- A temporary exact-record test reproduced silent mandatory-callee handoff
  for ` ]`: actual records `[]`, expected one Nud Missing at `0..0`.
  `cargo test -p yu-syntax --lib rewrite::tests::expression_recovery:: --
  --test-threads=1` failed as expected (0 passed, 1 failed, 1438 filtered out;
  build 39.68s, test 0.00s, existing 38 warnings). The first proposed `? ]`
  witness was rejected by optional entry, so it remains a rejection control
  rather than evidence for an operator-judge change.
- The user requested a stopping point before production edits. No production
  patch was applied. Preserved the full failing regression in the design and
  removed only its temporary source/module registration from the build; no
  existing source, assertion or test name changed. The local test binary is
  stale with that witness; rebuild through Cargo before direct binary use.
- M2 primary-only preparation, no independent review, no completed producer
  pass or production repair; zero benchmark samples/processes. Final pause
  checkpoint is record-only: synchronized task/design/index/single ledger/
  daily and checked diff/references, with no repeated broad build/tests.
  Resume by restoring the witness and implementing the recorded kernel and
  caller gate. The remaining SCC and actual embedded/header/full/Yumark/public
  parser replacement remain open; this pause is not completion or a blocker.

<!-- daily-append-anchor: 2026-09-08 -->

## O3b Expression required-operand current-Item recovery

- Resumed the authoritative required-operand gate after the user pause. The
  driver now takes an explicit initial role, publishes typed Missing/Error
  records, classifies unread closes/bracket openers as mandatory absence, and
  preserves protected Item leading. Ordinary EOF alone emits its remaining
  leading before its Missing anchor.
- Ordinary scanning and sealed Error-run retry share one total lexical
  expression-Item operation. Error runs emit native token kinds and one
  `OtherCharacter` record; a retry NUD resumes with unchanged threshold, ML,
  line/fence and ambient context. No source replay, retained run or generic
  `chasa-recover` API was added.
- If Condition, CaseLike Scrutinee/Guard, For Iterable and For inline Body
  pass their selected roles. The For iterable bypass invokes the same Missing
  helper; its absence flag now shares the kernel predicate, preventing `]`
  from being repurposed as a body introducer.
- Restored the saved exact ` ]` regression and added boundary, Error/retry,
  frozen reconciliation, infix and actual caller-role checks. The
  expression-recovery filter passed 4 tests; operators 11, If 8, CaseLike 12
  and For 12. `cargo check -p yu-syntax`, scoped rustfmt and `git diff --check`
  passed. Existing warnings remain. No benchmark sample/process or broad
  workspace suite was run.
- M2 construction is complete. A read-only specification audit and regression
  audit were used after the user requested subagents; the two accepted findings
  (shared lexical scanner and For absence-flag parity) are included above.
  The regression agent's final summary was unavailable due model capacity, so
  this is not claimed as independent joint certification. Tails/delimiters,
  literal/Statement/declaration owners, O4 and public cutover remain open.
