# 2026-09-08

## Accepted-input authority and recovery simplification

- Recorded the user's explicit decision: reference Yulang2 only for formally
  accepted input and select reasonable successor recovery. The user also
  authorized recovery simplification, documentation/environment maintenance,
  and consideration of globally useful `chasa-recover` shorthand.
- `2026-09-08-successor-recovery-authority-amendment.md` supersedes the
  legacy-malformed parity prerequisites without weakening accepted grammar,
  current-Item ownership, typed output, structured extents/reservations,
  frozen/header consistency, or atomic public cutover. The shared delimiter
  gate selects numeric-head-plus-Call recovery explicitly instead of requiring
  the old numeric-head/separate-payload split.
- Independent architect/compiler/spec work predates the latest instruction.
  The authority repair is clean; the primary resolved the final numeric
  pre-write gap with explicit witnesses, fields, continuation and accepted
  controls. The user then requested direct work without agents, so subsequent
  implementation/verification is primary-owned and is not described as
  independent review.
- Preserved the former 2,710-line `tasks/current.md` verbatim under
  `notes/progress/rewrite-state-before-recovery-authority-2026-09-08.md`, with a
  historical banner. The new task entry is concise navigation. Updated the
  design index and former scalar/payload/primary status pointers.
- This checkpoint changes records only. The existing two-file delimiter
  candidate stays unstaged until its selected successor proof closes. No
  broad compiler tests or performance measurements are needed for records.

## Environment audit

- The workspace-local `chasa-recover` provides `token`, `maybe`, and zero-copy
  `with_str`; inspected repeated call sites do not justify a new generic
  recovery API. CST construction, fences and delimiter policy stay with
  grammar owners. No library API or dependency change was made.
- Before the no-agent direction, read-only environment work ran
  `cargo xtask check-graph`: passed. It validates dependency direction, not
  parser behavior.
- That audit also ran
  `cargo test -p yu-syntax rewrite::tests::type_decl -- --test-threads=1`:
  33 passed, six known assertion failures. The failures match the recorded
  `b85c32f3` baseline and are now listed in `tasks/current.md`; they are not
  build, linker or cache failures. No expectation was weakened.
- No `rust-toolchain.toml` is present. Toolchain pinning is not a repair for
  these failures, and is not introduced without an actual reproducibility
  requirement. Safe focused commands are recorded in the task entry.

## Shared delimiter integration

- Authority/navigation checkpoint `c74b2944` was committed and pushed to
  `origin/yulang3`. The subsequent implementation integrates the retained
  two-file shared delimiter candidate, with no `variants.rs` or public change.
- Added one common fresh/frozen completion assertion and selected P/E/numeric
  structured-carrier tests. These verify full records, origin translation,
  emitted CST extents, native outer closes, outer path continuation, prefix
  and recursive reservations, and accepted numeric/PV Types.
- Initial `shared_delimited` filter: six passed, one test-construction error.
  The new test accidentally required the horizontal rule's Missing node in
  the excluded no-gap E row. Corrected that expectation to preserve the
  already-approved unchanged no-gap handling; no production patch was made
  to accommodate it. Missing-close publication for that E row remains T4E.
- Final focused commands:
  - `cargo test -p yu-syntax --lib rewrite::tests::type_expr:: -- --test-threads=1`:
    124 passed;
  - `cargo test -p yu-syntax --lib rewrite::tests::output:: -- --test-threads=1`:
    4 passed;
  - `cargo test -p yu-syntax --lib rewrite::tests::recovery_output:: -- --test-threads=1`:
    25 passed;
  - `cargo check -p yu-syntax`: passed, with existing warnings;
  - scoped `rustfmt --edition 2024 --check` and `git diff --check`: passed.
- Sibling check reused the built binary:
  `target/debug/deps/yu_syntax-b491637626fa8c73 rewrite::tests::type_decl:: --test-threads=1`.
  The same six tests fail, 33 pass. Concrete failures include accepted path
  `A::with` and nested Call `A(B with Inner)`: contextual boundary decisions
  stop them early. They are not missing recovery-harness plumbing. The next
  repair prioritizes accepted grammar, retaining the assertions.
- Static cost: bounded extra checks of the current Item and at most its one
  coalesced whitespace part; no new allocation, traversal or state. Zero
  performance samples/processes. Broad/public certification remains deferred.
- No agents were used after the user's direct-work instruction. Prior review
  evidence is retained, but this completion pass is primary verification.

<!-- daily-append-anchor: 2026-09-08 -->
