# 2026-09-22

## F5b lifecycle boundary complete

The independently authorized F5b lifecycle substage is complete. `yu-types`
now owns sealed generative closed-type finalization, result-attached aggregate
accounting, and the fallible terminal finish. `yu-solver` retains its existing
availability boundary and publishes neither a partial `SolvedModule` nor final
aggregate counters when finalization fails.

Collected constraint terms are opaque `(lineage, index)` identities. A batch
owns the immutable collected prefix, and `ConstraintStore::from_batch` transfers
that exact lineage rather than reconstructing terms from HIR. Batch aliases
share the prefix but claim disjoint sparse solve-time pages. Each page has
exactly 256 `MaybeUninit<TermNode>` slots, with reserve/allocate/claim/publish
ordering, one-probe lookup, and initialized-prefix-only destruction. The
standalone HIR-only store constructor is removed.

M2 specification and performance review closed after the repair bundle:
production collection no longer clones the Term builder, component-to-Term
lookup is indexed, admission validates both Term handles before occurrence or
cause ownership, and bounded lifecycle evidence covers prefix/page boundaries,
alias isolation, lookup classes, overflow, and opaque public APIs. A final
specification delta review confirmed the collection/seal overflow witness.

Focused `yu-solver` verification passed 65 library tests and 7 doctests;
formatting, diff checks, and a warning-free workspace check passed. F5e's full
physical resource matrix and public counter certification remain deliberately
deferred. The next F5 work is the remaining approved F5b live-variable, level,
and batch-recipe translation structure; F5a continues to emit no source
Function facts until F5d.

## Thread handoff

The exact continuation boundary, confirmed Yulang2 ownership map, required
invariants, and first port sequence are recorded in
[`2026-09-22 F5b live-port handoff`](../handoffs/2026-09-22-f5b-live-port-handoff.md).
