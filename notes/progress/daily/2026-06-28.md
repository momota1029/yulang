# 2026-06-28

## Evidence VM direct resumptive route

`DirectTailResumptive` を provider-env 経由の operation に広げるため、まず
request 経路との差分を確認した。

分かったこと:

- `DirectTailResumptive` を `MayEscapeYield` 全体へ無条件に広げると、
  `std::control::flow::sub::return` を扱う callback / returned thunk 系の
  hygiene regression が出る。
- `YieldFallback` は provider grant がまだ有効かを確認してから routed yield にするが、
  `DirectTailResumptive` は同じ gate を持っていなかった。
- `case` scrutinee や ref-set の force 後に direct resumptive / routed yield が出た場合、
  request 経路と同じ continuation frame を積む必要がある。
- `bench/nondet_20_discard.yu` の `nondet.list` は、`k(true).list` が
  `make-thunk((k true)).list` として下がるため、handler arm classifier からは
  `MayEscapeYield` に見える。
- provider grant の strict gate を path-aware に緩めても、nondet では対象 path の
  guard が実際に増えているため、request-free direct-tail には落ちなかった。
- provider route を gate 失敗時に `RoutedYield` へ早めに固定する実験は、
  returned thunk 系の `sub::return` regression を再発させたため採用しない。
- `direct_tail_gate_fail_add_id_foreign_path` の内訳を一時 trace で確認したところ、
  `bench/nondet_20_discard.yu` の 420 件はすべて
  `std::control::nondet::nondet::branch` に対する
  `std::control::junction::junction` marker だった。
  `junction` は `or` / `and` / `ret` を `my` operation として持ち、foreign path を守る
  add-id marker を積む。したがって、provider route lookup が失敗しているのではなく、
  別 handler 境界の衛生性により request-free direct-tail が止められている。

採用した変更:

- direct-tail force は `request_free_yield` または provider grant の path-aware gate を
  通った場合だけ request-free にする。
- `MayEscapeYield` は direct-tail execution plan へ分類できるようにしたが、
  provider-env route では上記 gate により unsafe な returned thunk は request fallback に残る。
- force 後の `case` scrutinee / ref-set continuation を request 経路と揃えた。

現時点の性能:

- `bench/nondet_20_discard.yu` はまだ direct-tail に乗らず、`direct_tail_continuation_appends`
  は 0 のまま。
- 次に大きく速くするには、guard / handler-boundary evidence を持った direct resumptive
  表現を入れるか、`MakeThunk` 内の direct continuation use を安全に分類する必要がある。
- 今回の trace から、少なくとも nondet/list 系では単に gate を緩めるのは危険だと分かった。
  次の本命は、`junction` の foreign-path guard を request object ではなく lightweight
  hygiene state として direct-tail 側に保持し、catch 境界で同じ可視性判定を行うこと。

## Evidence VM guarded direct-tail / forced effect fuse

`AddIdShadowed` で落ちていた provider direct-tail route を、request fallback ではなく
guarded direct-tail として表すようにした。request と同じ `guard_ids` / `carried_guards`
を集め、そのまま `EvidenceDirectTailResumptive` に載せる。`ScopeMissing` と
`HandlerShadowed` は従来通り fallback に残す。

あわせて `ForceThunk(Apply(EffectOp, arg))` の exact pattern だけを fuse した。ここで
重要なのは、provider route / grant は元の EffectOp apply と同じく marker frame の外で
作り、force だけを marked thunk force 相当の marker frame 内で行うこと。route lookup
まで marker frame 内へ入れると、grant baseline がずれて direct-tail guard が消えるので
採用しない。

採用しなかった案:

- `apply_scoped_value_result` を飛ばして EffectOp を直接 force する案。
  一時的には速く見えたが、function-call marker を省くため衛生性の根拠がなくなる。
- release build でも `ActiveAddIdIndex` を使う案。
  scan count は減ったが、path bucket の維持と candidate sort が重く、実時間は悪化した。

確認した数値:

- `bench/nondet_20_discard.yu`: `runtime_evidence_execute` は release repeat で
  33.6ms / 34.5ms / 37.6ms。`forced_effect_call_fusions: 861`。
- `examples/showcase.yu`: `runtime_evidence_execute` は release repeat で
  60.6ms / 61.7ms / 62.9ms。`forced_effect_call_fusions: 2457`。
- どちらも `compare.control: match`。

## Evidence VM AddId marker sharing

`DirectTailResumptive` と forced effect fuse 後も evidence VM が ControlVM に勝てない
主因は、provider lookup ではなく marker scope の再構築に残っていた。特に
`EvidenceAddIdMarker` は path を持つため、continuation resume / marker frame のたびに
deep clone すると、nondet / showcase のような workload で clone cost が支配的になる。

そこで `EvidenceValueMarker::AddId` と active add-id marker を `Rc<EvidenceAddIdMarker>`
へ変更し、通常の marker frame push では marker 本体を共有するようにした。関数呼び出しや
continuation resume で depth / guard flag を変える場合だけ、内側の marker を clone して
新しい `Rc` に包む。

採用しなかった案:

- release build で `ActiveAddIdIndex` を有効にする案。scan count は下がったが、HashMap
  bucket の維持と candidate sort が重く、`bench/nondet_20_discard.yu` は約 55ms、
  `examples/showcase.yu` は約 90ms まで悪化した。

確認した数値:

- `bench/nondet_20_discard.yu`: `runtime_evidence_execute` は release repeat で
  31.2ms / 31.9ms / 32.2ms。`compare.control: match`。
- `examples/showcase.yu`: `runtime_evidence_execute` は release repeat で
  49.1ms / 50.2ms / 51.5ms。`compare.control: match`。
- 同じ release binary の ControlVM は、`--runtime-phase-timings` で
  `bench/nondet_20_discard.yu` が `runtime_execute: 19.2ms`、
  `examples/showcase.yu` が `runtime_execute: 42.5ms`。

現時点の読み:

- showcase では ControlVM にかなり近づいたが、nondet はまだ大きく負けている。
- 残る差は、request-free direct route の不足だけでなく、
  `continuation_resume_marker_steps` / `marker_frame_entries` / `active_add_id_scans`
  が ControlVM と同程度に残っているのに、evidence VM 側では別表現への積み直しも
  払っている点が大きい。
- 次に成果が出る可能性があるのは、AddId marker の path 自体を intern すること、
  provider direct-tail が通った operation で marker frame を閉じ直さない fast path を
  作ること、または direct resumptive 専用 continuation を request fallback と完全に
  分けること。

## Evidence VM marked force cleanup

Koka 型の evidence passing に寄せるため、`Marked(callable)` を callable 側の hygiene
carrier へ吸わせる実験を行った。結果として、単に carrier を持たせるだけでは
`apply` / `force` 時に従来通り marker frame を開くため、速くならなかった。
さらに `Thunk::Value` へ hygiene を持たせないと marker を落としうるため、この形は採用しない。

採用した小さい fast path:

- `Marked(Thunk::Value(v), markers)` の force は request を出さない。
  現行意味論では marker frame の中で `v` を返し、frame close で `v` に marker を付けるだけなので、
  `mark_runtime_value_shared(v, markers)` へ直接置き換えた。

採用しなかった案:

- callable hygiene carrier 全体。`bench/nondet_20_discard.yu` は 30.3ms / 31.1ms 程度まで
  出ることもあったが、`examples/showcase.yu` は 51ms 台へ悪化し、
  `marker_frame_entries` も増えた。
- `Marked(Thunk::Effect)` の direct-abortive fast path。forced effect call fuse 後は
  対象が 0 件だった。
- identity continuation thunk の fast path。対象が 0 件だった。
- trivial expr thunk の fast path。対象が 0 件だった。

確認した数値:

- `bench/nondet_20_discard.yu`: `marked_force_value_fast_paths: 420`、
  `marker_frame_entries: 60566 -> 60146`、
  `marker_frame_marked_force_entries: 3444 -> 3024`。
- `examples/showcase.yu`: `marked_force_value_fast_paths: 815`、
  `marker_frame_entries: 98804 -> 97989`、
  `marker_frame_marked_force_entries: 8500 -> 7685`。

実時間の改善は小さく、支配項はまだ `continuation_resume_marker_steps` と
request/continuation fallback 側に残っている。ここから先は、値 wrapper の置き換えではなく、
operation 発生時に closed marker を request/direct route へ直接解釈する設計が必要。

## Evidence VM closed marker interpretation attempt

`MarkedForce` の残りを、marker frame ではなく closed marker evidence として解釈できるか
調べた。

まず `Marked(Thunk::Effect)` だけを直接 request/direct route へ解釈する fast path を
検討したが、現在の workload では対象がほぼ無かった。`bench/nondet_20_discard.yu` は
effect thunk fallback が 0、`examples/showcase.yu` は 7 件だけで、いずれも marker path の
条件により fast path には乗らなかった。

次に、active AddId を持たない marker set なら active frame/add-id を積まず、
active marker plan だけを積む `plan-only` closed marker として扱えるか試した。
これは sound そうだったが、実際の fallback marker set はすべて `depth == 0` の AddId を
持っており、対象は 0 件だった。

さらに攻めて、`carry_after_frame == false` の active AddId なら外へ持ち越さないので
plan-only にできるのではないかと試した。これは失敗した。

- `debug_runtime_evidence_run_resumes_carried_callback_markers` が
  `runtime-evidence-run escaped effect request: tick::ping` で落ちた。
- `carry_after_frame == false` でも、callback marker の resume 中には active AddId frame が
  必要になる。
- したがって「外へ carry しない marker」でも、continuation resume の局所衛生性には
  active frame として解釈する必要がある。

現在残したもの:

- `Marked(Thunk::Value)` の fast path。
- `MarkedForce` fallback の分類 counter。

現在の分類:

- `bench/nondet_20_discard.yu`
  - value fast path: 420
  - expr thunk fallback: 1323
  - continuation thunk fallback: 840
  - active AddId markers: 4306
  - carry-after-frame markers: 0
- `examples/showcase.yu`
  - value fast path: 815
  - expr thunk fallback: 3566
  - effect thunk fallback: 7
  - continuation thunk fallback: 1655
  - active AddId markers: 9632
  - carry-after-frame markers: 0

結論:

- 残りの本丸は `Marked(Thunk::Expr)` と `Marked(Thunk::Continuation)`。
- 単純な plan-only 化はできない。
- 次にやるなら、active frame を丸ごと消すのではなく、AddId frame の意味を
  continuation resume 専用の軽い hygiene state として表現し直す必要がある。

## Evidence VM local AddId frame entry fast path

continuation resume 専用の marker plan を試したが、これは採用しなかった。
`EvidenceContinuationFrame::MarkerFrame` に `frame_ids` / `add_ids` を持つ plan を保存し、
resume 時の marker slice 走査を避ける案だったが、plan 構築と追加 allocation の方が重く、
`bench/nondet_20_discard.yu` は 35ms 台、`examples/showcase.yu` は 55ms 台へ悪化した。

代わりに、通常の `push_marker_frame` に小さい fast path だけを残した。
`EvidenceValueMarker::AddId` は多くの場合、同じ marker slice の直前にある
`EvidenceValueMarker::Frame` と同じ guard id を指す。この場合は
`active_frames` 全体を `rposition` で探さず、直前の local frame entry をそのまま
`entry_frame_len` に使う。直前 frame と一致しない場合は従来どおり
`entry_frame_len_for_marker` に fallback する。

確認した数値:

- `bench/nondet_20_discard.yu`: `runtime_evidence_execute` は release 5 回で
  30.9ms / 31.8ms / 31.5ms / 31.9ms / 31.2ms。`compare.control: match`。
- `examples/showcase.yu`: `runtime_evidence_execute` は release 5 回で
  50.0ms / 50.2ms / 51.1ms / 53.4ms / 49.5ms。`compare.control: match`。

読み:

- marker の意味を変えずに入れられる改善としては安全だが、構造を変えるほどの効果はない。
- plan を別 object として保存する案は、この粒度では allocation が勝ってしまう。
- 本当に大きく削るには、marker slice の再解釈ではなく、operation / continuation
  表現そのものを request path から分ける必要がある。

## Evidence VM follow-up optimization sweep

追加で候補を一通り試した。今回残すコード変更はない。

試した案:

- DirectTailResumptive の no-continuation arm fast path。
  handler の該当 operation arm が continuation を実値として使わない場合だけ
  DirectAbortive として扱う実験を入れた。`bench/nondet_20_discard.yu` と
  `examples/showcase.yu` では `direct_tail_no_continuation_fast_paths` がどちらも 0。
  現在の代表 workload には刺さらないため戻した。
- `Marked(Thunk::Expr)` / `Marked(Thunk::Continuation)` の二重 dispatch 削減。
  `Marked` の内側が `Thunk` だと分かっているので、marker frame 内で
  `force_thunk_result(value.clone())` へ戻らず直接 thunk body を force する実験を入れた。
  回帰テストは通ったが、実時間は改善せず、showcase はむしろやや悪化したため戻した。
- active AddId marker に frame position を保存する案。
  request に guard を追加した後、`EvidenceRequestEntryExceptIndex` が
  `active_frames` を再検索する部分を避けるため、active marker に frame position を持たせた。
  回帰テストは通ったが、active marker のサイズ増加が勝ったようで実時間は改善せず戻した。
- continuation flattening。
  現在の `Then` の多くは `MarkerFrame` / `ProviderEnv` の scope boundary を保つために残っている。
  ここへ tail を押し込むと dynamic scope が変わるため、小さい局所変更としては触らない判断にした。
  本当にやるなら control VM の frame segment / snapshot に近い表現変更が必要。
- nondet/list。
  `ListTree` は persistent concat で、代表 workload の `list_values_copied` は 0。
  `list_merge_calls` も nondet 420、showcase 832 程度なので支配項ではない。
- allocator pressure。
  `/usr/bin/time` で全体は nondet が約 0.07s / 20MB、showcase が約 0.12s / 28MB。
  allocator 差し替えを試すには依存追加が必要で、構造改善なしに入れる段階ではない。

読み:

- いまの小技系はかなり頭打ち。
- 残る本命は、direct operation / continuation / marker scope を現行の
  request-passing 表現から切り離す大きめの runtime 表現変更。
- 特に `continuation_resume_marker_steps` と `direct_tail_continuation_then_appends` は、
  marker の意味を保ったまま単発 fast path で消すのは難しい。

## Evidence VM runtime redesign first slice

`notes/design/2026-06-28-evidence-vm-runtime-redesign-pro-review.md` の方針を受けて、
最初の小さい slice として `EvidenceResumeMarkerPlan` を入れた。

目的は速度改善ではなく、payload marking と resume dynamic scope をコード上で分けること。
従来は `close_marker_request` / `close_marker_direct_tail` / `close_marker_routed_yield`
がそれぞれ `markers_for_continuation_resume(...)` と
`EvidenceContinuation::marker_frame(...)` を直接扱っていた。今回から、まず
`resume_marker_plan(...)` で `EvidenceResumeMarkerPlan` を作り、最後に
`plan.attach(continuation)` で旧 marker-frame continuation へ戻す。

現時点では挙動は変えない。`ResumeMarkerPlan` はまだ旧 `MarkerFrame` への bridge であり、
push/pop 削減はしていない。次に `PureValueMark` / `DynamicScope` へ分ける足場として扱う。

確認:

- `cargo fmt --all --check`
- `cargo check -q -p evidence-vm -p yulang`
- `cargo test -q -p evidence-vm`
- `cargo test -q -p yulang runtime_evidence -- --test-threads=1`
- release compare-control:
  - `bench/nondet_20_discard.yu`: 31.1ms / 31.5ms / 31.3ms、`compare.control: match`
  - `examples/showcase.yu`: 51.4ms / 51.6ms / 50.0ms、`compare.control: match`

次の候補:

- `RouteCert` を first-class にして `EffectThunkEvidence` の `route_origin` 依存を薄める。
- `EvidenceRoutedYield` から `request` を剥がす。ただし最初は `to_request()` bridge で
  旧 path へ戻す。
- `ResumeMarkerPlan` を `PureValueMark` / `DynamicScope` に分け、値 mark と resume scope を
  実際に別経路へ逃がす。

## Evidence VM route certificate surface

runtime redesign の次 slice として、`EffectThunkEvidence` が
`Option<EvidenceRouteOrigin>` を直接持つ形をやめ、`EvidenceRouteCert` を持つようにした。

この変更の目的は速度改善ではなく、operation thunk が「どの経路で解決されたか」を
request / direct-tail の gate から分離して扱えるようにすること。現時点の
`EvidenceRouteCert` は `None` / `StaticDirect` / `ProviderGrant(id)` だけを持ち、
provider grant gate も従来と同じく `ProviderGrant` だけを読む。
つまり、direct 化の条件は広げていない。

あわせて、旧 `EvidenceRouteOrigin` は `EvidenceRouteCert` に吸収した。二重表現を残すと
次の `RouteCertId` 化や `EffectSignal` 分離で読み替えが増えるため、この段階で一本化した。

確認:

- `cargo fmt --all --check`
- `cargo check -q -p evidence-vm -p yulang`
- `cargo test -q -p evidence-vm`
- `cargo test -q -p yulang runtime_evidence -- --test-threads=1`
- release compare-control:
  - `bench/nondet_20_discard.yu`: 30.7ms / 32.1ms / 29.7ms、`compare.control: match`
  - `examples/showcase.yu`: 51.8ms / 50.1ms / 50.8ms、`compare.control: match`

次の候補:

- `EvidenceRoutedYield` から `EvidenceRequest` を剥がし、`EffectSignal` 的な surface を先に作る。
- `EvidenceRouteCert` を `RouteCertId` 化し、provider grant / static direct / fallback 理由を
  runner state に寄せる。
- `ResumeMarkerPlan` と `RouteCert` を使って、request fallback と direct-tail の marker
  closing を同じ surface から作れるようにする。

## Evidence VM effect signal surface

runtime redesign の次 slice として、`EvidenceRoutedYield` が `EvidenceRequest` を直接持つ形を
やめ、`EvidenceEffectSignal` を持つようにした。

現時点の `EvidenceEffectSignal` はまだ `EvidenceRequest` への bridge だけを持つ。
`request()` / `request_mut()` / `into_request()` で旧 request path へ戻すため、意味論は変えていない。
ただし `RoutedYield` の表面から request field を隠したことで、次に
`EffectSignal` を request-free direct operation と generic request fallback に割る準備ができた。

hot path で余計な clone が増えないよう、provider close / marker close は
`RoutedYield` から request を move で取り出してから戻す形にした。

確認:

- `cargo fmt --all --check`
- `cargo check -q -p evidence-vm -p yulang`
- `cargo test -q -p evidence-vm`
- `cargo test -q -p yulang runtime_evidence -- --test-threads=1`
- release compare-control:
  - `bench/nondet_20_discard.yu`: 30.9ms / 32.6ms / 32.0ms、`compare.control: match`
  - `examples/showcase.yu`: 50.0ms / 50.3ms / 49.0ms、`compare.control: match`

次の候補:

- `EvidenceEffectSignal` に `GenericRequest(EvidenceRequest)` 相当の enum variant を入れ、
  後続 slice で `DirectAbortive` / `DirectTail` signal を足せる形にする。
- `RoutedYield` の continuation append を `Signal` 側へ寄せ、request fallback に入るまで
  request-specific な関数名を通らないようにする。
- marker close が `Request` / `RoutedYield` / `DirectTail` で分かれている部分を、
  signal / direct-tail 共通の scope close surface に寄せる。

## Evidence VM signal variant shell

`EvidenceEffectSignal` を struct bridge から enum に変更し、現時点の request fallback を
`GenericRequest(EvidenceRequest)` variant として明示した。

まだ variant は一つだけなので挙動は変わらない。ただし、次に `DirectAbortiveSignal` や
`DirectTailSignal` を足すとき、`RoutedYield` / continuation append / marker close が
「常に request を持つ」前提から少し離れられる。これは Pro 案の
`EffectSignal -> GenericRequest` 分離へ向けた足場。

確認:

- `cargo fmt --all --check`
- `cargo check -q -p evidence-vm -p yulang`
- `cargo test -q -p evidence-vm`
- `cargo test -q -p yulang runtime_evidence -- --test-threads=1`
- release compare-control:
  - `bench/nondet_20_discard.yu`: 33.0ms / 30.8ms / 31.5ms、`compare.control: match`
  - `examples/showcase.yu`: 51.5ms / 50.3ms / 53.6ms、`compare.control: match`

次の候補:

- request continuation append を `EvidenceEffectSignal` の method に寄せる。
- marker close の handler-boundary / payload mark / resume-plan attach を signal 側へ寄せる。
- direct-tail signal を足す前に、どの operation が request-free にできるかを
  `RouteCert` と `ResumeMarkerPlan` の条件で表せるようにする。

## Evidence VM signal close surface

`EvidenceEffectSignal` 側に provider close / marker close / handler-boundary attach を寄せた。
runner は `RoutedYield` の中身が request であることを直接書き換えず、
`call.close_provider_env(...)`、`call.with_handler_boundary(...)`、
`call.close_marker_frame(...)` を呼ぶ形になった。

現時点では `GenericRequest` variant しかないので、挙動は同じ。目的は、後で
request-free signal variant を足したときに、provider scope / marker scope の close を
runner 側へ分岐として増やさず、signal 側の意味論へ閉じ込めること。

注意:

- 速度改善目的の変更ではない。
- `close_provider_env` / `close_marker_frame` は request を move で取り出すため、
  以前の direct request field 書き換えと同じく continuation / payload の deep clone は増やしていない。

確認:

- `cargo fmt --all --check`
- `cargo check -q -p evidence-vm -p yulang`
- `cargo test -q -p evidence-vm`
- `cargo test -q -p yulang runtime_evidence -- --test-threads=1`
- release compare-control:
  - `bench/nondet_20_discard.yu`: 33.8ms / 32.7ms / 32.4ms、`compare.control: match`
  - `examples/showcase.yu`: 51.6ms / 52.0ms / 52.3ms、`compare.control: match`

次の候補:

- `continue_routed_request_with_continuation` を signal-aware にする。ただしこれは広いので、
  request fallback と direct signal の共通 interface を先に切る必要がある。
- request-free `DirectTail` signal を入れる前に、既存の `EvidenceDirectTailResumptive` と
  `EvidenceEffectSignal` の payload / guard / continuation surface を揃える。

## Evidence VM routed signal continuation entry

`EvidenceEffectSignal` に continuation append を追加し、`RoutedYield` の
identity / whole-continuation append だけは signal のまま返すようにした。

request boundary を含む continuation は従来通り `continue_routed_request_with_continuation`
へ戻す。したがって、catch boundary の処理や visible operation 判定は変えていない。

確認:

- `cargo fmt --all --check`
- `cargo check -q -p evidence-vm -p yulang`
- `cargo test -q -p evidence-vm`
- `cargo test -q -p yulang runtime_evidence -- --test-threads=1`
- release compare-control:
  - `bench/nondet_20_discard.yu`: 32.6ms / 31.0ms / 32.7ms、`compare.control: match`
  - `examples/showcase.yu`: 53.7ms / 53.4ms / 53.0ms、`compare.control: match`

読み:

- `request_continuation_appends` と `request_whole_continuation_appends` は変わらない。
  現時点の signal は `GenericRequest` variant だけなので、append の意味は従来と同じ。
- 速度改善としては主張しない。次に request-free signal variant を足すための入口整理。

## Evidence VM direct-tail close surface

`EvidenceDirectTailResumptive` に `from_request`、`with_handler_boundary`、
`close_provider_env`、`close_marker_frame` を追加し、runner が direct-tail の内部 field を
直接書き換える箇所を減らした。

これで `RoutedYield` / `EvidenceEffectSignal` 側と同じく、provider scope と marker scope の
close が value object 側の method になった。`guarded_direct_tail_resumptive` も
`EvidenceRequest` から `EvidenceDirectTailResumptive::from_request(...)` へ変換する形にして、
guard / carried guard / handler boundary / continuation の対応を一箇所へ寄せた。

確認:

- `cargo fmt --all --check`
- `cargo check -q -p evidence-vm -p yulang`
- `cargo test -q -p evidence-vm`
- `cargo test -q -p yulang runtime_evidence -- --test-threads=1`
- release compare-control:
  - `bench/nondet_20_discard.yu`: 31.1ms / 31.1ms、`compare.control: match`
  - `examples/showcase.yu`: 49.9ms / 51.4ms、`compare.control: match`

読み:

- 意味論は変えていない。次に direct-tail を signal variant へ寄せるための表面整理。
- 速度改善としては主張しない。runner の field mutation を減らし、request と direct-tail の
  変換点を明示する変更。

## Evidence VM AddId marker reuse

DirectTail signal 化の前に、実測上の支配項を再確認した。`DirectTail` を
`EvidenceEffectSignal` variant に移すだけでは二重表現になる一方で、
`continuation_resume_marker_steps` と marker frame の変換がまだ太い。

まず `EvidenceRequest` / `EvidenceDirectTailResumptive` の `guard_ids` /
`carried_guards` を `SmallVec` にする実験をしたが、`bench/nondet_20_discard.yu` が
36〜39ms まで悪化したため戻した。空 Vec の allocation 削減より、request/direct-tail 本体の
サイズ増加が勝ったと見ている。

採用した変更:

- `markers_for_function_call`
- `markers_for_continuation_call`
- `markers_for_continuation_resume`

で AddId marker を変換するとき、変換後の marker が元と同じなら新しい `Rc` を作らず、
既存の `Rc<EvidenceAddIdMarker>` を clone するようにした。特に continuation resume は
同じ resume-ready marker を何度も通すので、ここで allocation を減らせる。

確認:

- `cargo fmt --all --check`
- `cargo check -q -p evidence-vm -p yulang`
- `cargo test -q -p evidence-vm`
- `cargo test -q -p yulang runtime_evidence -- --test-threads=1`
- release compare-control:
  - `bench/nondet_20_discard.yu`: 28.0ms / 28.0ms / 28.2ms / 29.5ms / 27.9ms、`compare.control: match`
  - `examples/showcase.yu`: 47.4ms / 47.5ms / 50.2ms / 47.8ms / 48.7ms、`compare.control: match`

読み:

- これは初めてはっきり実時間に効いた marker 側の改善。
- 意味論は変えていない。AddId marker の正規化結果が同じ場合だけ allocation を省く。
- 次の本命は、resume-ready marker set を `EvidenceActiveMarkerPlan` などに cache できるか。

## Evidence VM effect signal variants

`EvidenceEvalResult` から `Request` / `DirectAbortive` / `DirectTailResumptive` /
`RoutedYield` の個別 variant を外し、`Value` と `Effect(EvidenceEffectSignal)` の二択にした。
effect 側の signal は次の variant を持つ。

- `GenericRequest`
- `DirectAbortive`
- `DirectTailResumptive`
- `RoutedYield`

`RoutedYield` は再帰型を避けるため、内部には `EvidenceEffectSignal` ではなく
`EvidenceRequest` を直接持つ。routed yield は現在の意味論上 request 由来の signal なので、
この方が循環せず、request field の意味も明確になる。

あわせて、runner 側の主要入口を signal-aware にした。

- `close_provider_env_result` は `Effect(signal)` を受けて
  `close_provider_env_signal` へ分配する。
- `close_marker_frame_result` と `close_marked_result` は signal close helper を通る。
- `continue_result` は `continue_signal` へ分配する。
- `continue_catch_body_result` は `continue_catch_body_signal` へ分配する。

目的:

- `EvidenceEvalResult` を value / effect の境界に戻す。
- request-free direct signal を増やすとき、result enum の variant を増やさずに済むようにする。
- provider / marker / continuation / catch の境界で、direct abortive / direct tail /
  routed yield / generic request の分岐を signal layer に寄せる。

確認:

- `cargo fmt --all --check`
- `cargo check -q -p evidence-vm -p yulang`
- `cargo test -q -p evidence-vm`
- `cargo test -q -p yulang runtime_evidence -- --test-threads=1`
- release compare-control:
  - `bench/nondet_20_discard.yu`: 29.7ms / 30.1ms / 33.7ms / 28.8ms / 27.4ms、`compare.control: match`
  - `examples/showcase.yu`: 51.9ms / 50.2ms / 47.6ms / 47.9ms / 46.9ms、`compare.control: match`

読み:

- 今回も意味論は変えていない。
- 実時間は前回とほぼ同じ。速度改善としては主張しない。
- ここから先の大きい変更は、`EvidenceEffectSignal` を runner の request path へ戻すのではなく、
  direct signal のまま operation arm / continuation / marker hygiene state を処理する方向。

## Evidence VM shared operation paths

次の候補として、continuation resume 用 marker set の cache を試した。`Rc<[EvidenceValueMarker]>`
を key にした content HashMap で、`markers_for_continuation_resume` の再計算を避ける案。

結果:

- `bench/nondet_20_discard.yu`: cache hit 27571 / miss 2564 だったが、
  `runtime_evidence_execute` は 33.5ms / 34.2ms / 33.8ms / 36.2ms / 37.5ms へ悪化。
- `examples/showcase.yu`: cache hit 31426 / miss 5230 だったが、
  `runtime_evidence_execute` は 62.0ms / 60.4ms / 64.7ms / 64.3ms / 59.9ms へ悪化。
- content hash / lookup の cost が、marker 変換削減より重かったと見て採用しない。

採用した変更:

- operation path を `Vec<String>` から `Rc<[String]>` へ寄せた。
- 対象は `RuntimeEvidenceValue::EffectOp`、`RuntimeEvidenceThunk::Effect`、
  `EvidenceRequest`、`EvidenceDirectAbortive`、`EvidenceDirectTailResumptive`。
- diagnostic / escaped effect の外向き境界だけ `to_vec()` へ戻す。

確認:

- `cargo fmt --all --check`
- `cargo check -q -p evidence-vm -p yulang`
- `cargo test -q -p evidence-vm`
- `cargo test -q -p yulang runtime_evidence -- --test-threads=1`
- release compare-control:
  - `bench/nondet_20_discard.yu`: 29.4ms / 29.0ms / 28.4ms / 27.7ms / 28.3ms、`compare.control: match`
  - `examples/showcase.yu`: 50.5ms / 49.6ms / 49.5ms / 50.7ms / 49.3ms、`compare.control: match`

読み:

- 大きな高速化ではないが、request/direct signal の path clone を削る安全な軽量化。
- marker resume cache はこの形では戻す。やるなら content HashMap ではなく、marker plan 自体に
  precomputed resume view を持たせる方がよい。

## Evidence VM shared AddId marker paths

operation path の共有化に続けて、`EvidenceAddIdMarker.path` も `Rc<[String]>` にした。
AddId marker は function call / continuation call / continuation resume で marker 本体を
clone することがあり、そのとき path `Vec<String>` まで deep clone する必要はない。

確認:

- `cargo fmt --all --check`
- `cargo check -q -p evidence-vm -p yulang`
- `cargo test -q -p evidence-vm`
- `cargo test -q -p yulang runtime_evidence -- --test-threads=1`
- release compare-control:
  - `bench/nondet_20_discard.yu`: 28.2ms / 27.6ms / 27.4ms / 28.7ms / 27.9ms / 29.1ms / 28.1ms、`compare.control: match`
  - `examples/showcase.yu`: 47.7ms / 48.2ms / 49.2ms / 47.9ms / 49.2ms / 48.3ms / 46.8ms、`compare.control: match`

読み:

- これも大きな構造変更ではないが、path clone をさらに減らす安全な軽量化。
- `ActiveAddIdIndex` の debug-only HashMap key は `Vec<String>` のままにし、debug index へ入れる
  ときだけ `to_vec()` する。release hot path には載らない。

## Evidence VM marker transform no-op sharing

continuation resume 用 marker set の content HashMap cache は悪化したので採用しなかったが、
marker 変換そのものには no-op の場合が多い。そこで function call / continuation call /
continuation resume の marker 変換で、変換結果が入力と同じで dedup も不要な場合は、既存の
`Rc<[EvidenceValueMarker]>` をそのまま返すようにした。

変更:

- `EvidenceActiveMarkerPlan::function_call_markers` は precomputed view を `Rc` のまま共有する。
- marked value apply 時の function / continuation call marker 変換も、no-op なら marker set を再確保しない。
- continuation resume でも `Rc` 入力の経路は no-op fast path を使い、slice 入力の generic request 経路だけ従来の Vec 変換を残す。

確認:

- `cargo fmt --all --check`
- `cargo check -q -p evidence-vm -p yulang`
- `cargo test -q -p evidence-vm`
- `cargo test -q -p yulang runtime_evidence -- --test-threads=1`
- release compare-control:
  - `bench/nondet_20_discard.yu`: 26.0ms / 25.3ms / 27.7ms / 26.7ms / 25.9ms / 26.1ms / 26.2ms、`compare.control: match`
  - `examples/showcase.yu`: 46.3ms / 43.9ms / 45.7ms / 45.3ms / 48.4ms / 47.2ms / 45.3ms、`compare.control: match`

読み:

- HashMap cache と違い、content hash を払わずに allocation と deep clone だけを避けるので効いた。
- marker set が大きくなると prefix duplicate scan が重くなりうるが、現状の workload では改善している。
- さらに詰めるなら、次は marker set の canonical view を marker plan 作成時に持たせる方向。

## Rejected: SmallVec guard sets in Evidence VM

`EvidenceRequest` / `EvidenceDirectTailResumptive` の `guard_ids`、`carried_guards`、
`EvidenceCarriedGuard.exposed_guard_ids` を `SmallVec` にする実験をした。
request ごとの小さい heap allocation を減らす狙いだったが、release 実測では悪化した。

結果:

- `bench/nondet_20_discard.yu`: 32.7ms / 31.2ms / 31.1ms / 31.0ms / 30.9ms / 30.4ms / 30.7ms、`compare.control: match`
- `examples/showcase.yu`: 51.2ms / 50.5ms / 51.1ms / 51.6ms / 49.3ms / 52.0ms / 49.8ms、`compare.control: match`

判断:

- 直前の Vec 版は `bench/nondet_20_discard.yu` が 25-27ms、`examples/showcase.yu` が 44-48ms 程度だったので採用しない。
- guard set は小さいが、request/direct-tail payload 内で move / clone されるため、inline storage のサイズ増加が効いた可能性が高い。
- 今後この方向をやるなら、`SmallVec` ではなく guard set の persistent/intern 表現か、そもそも request/direct-tail が guard set を持たない fast path にする方がよい。

## Rejected: leading duplicate marker frame collapse

continuation resume で `MarkerFrame(M, MarkerFrame(M, next))` のような隣接重複を狭い条件で畳む実験をした。
`handler_path` がなく、`carry_after_frame` / `preserve_own_on_resume` を含まない marker だけを対象にした。

結果:

- `bench/nondet_20_discard.yu`: `continuation_resume_marker_duplicate_fast_paths: 0`
- `examples/showcase.yu`: `continuation_resume_marker_duplicate_fast_paths: 0`
- 実行結果はいずれも `compare.control: match`

判断:

- 現在の重さは同じ marker frame が隣接していることではなく、resume ごとに別々の marker frame を本当に再開していることにある。
- hit しない分岐を hot path に足すだけなので採用しない。

## Evidence VM borrowed expr dispatch

`RuntimeEvidenceRunner::eval_expr_result` が dispatch のたびに `RuntimeEvidenceExpr` 全体を
`.cloned()` していた。`RuntimeEvidenceExpr` は多くの arm で `Rc` を持つとはいえ、
`Type` / `FunctionAdapterHygiene` / `String` / `RecordSpread` なども含み、`expr_evals` が
数万から十数万回になる workload では不要な clone が積み上がる。

変更:

- `runtime_exprs` から借用したまま match し、各 arm で mutable call 前に必要な値だけ copy/clone する。
- `Value` は `SharedValue` clone、`Tuple` / `Record` / `Block` は `Rc` clone、`FunctionAdapter` は従来必要だった `Type` 等だけ clone する。
- 意味論は変えず、dispatch の full clone だけを消した。

確認:

- `cargo fmt --all --check`
- `cargo check -q -p evidence-vm -p yulang`
- `cargo test -q -p evidence-vm`
- `cargo test -q -p yulang runtime_evidence -- --test-threads=1`
- release compare-control:
  - `bench/nondet_20_discard.yu`: 26.2ms / 26.5ms / 24.8ms / 25.6ms / 24.9ms / 26.1ms / 25.4ms / 24.7ms / 26.5ms / 25.9ms、`compare.control: match`
  - `examples/showcase.yu`: 44.9ms / 42.1ms / 44.9ms / 42.2ms / 43.1ms / 44.0ms / 45.5ms / 46.7ms / 43.1ms / 45.2ms、`compare.control: match`

読み:

- nondet は no-op marker sharing 後の 25-27ms 帯と同等から少し良い程度。
- showcase は 43-46ms 帯へ寄り、full expr clone 削減の効果が見える。
- まだ本体は marker frame resume / continuation append だが、dispatch 側の無駄は削れた。

## Rejected: syntactic force-apply continuation fusion

`ForceThunk` の対象が syntactic `Apply` で、callee が `RuntimeEvidenceValue::Continuation` なら、
continuation thunk を作らず直接 resume する fusion を試した。これは `force (k arg)` を
`resume k arg` に近づける狙いだった。

結果:

- `bench/nondet_20_discard.yu`: `forced_continuation_call_fusions: 0`
- `examples/showcase.yu`: `forced_continuation_call_fusions: 0`
- 実行結果はいずれも `compare.control: match`

判断:

- 現在の continuation force は単純な `ForceThunk(Apply(...))` という形では現れていない。
- hot path に構文分岐を足すだけになるので採用しない。
- この方向を進めるなら、runtime で構文を覗くのではなく、plan 側で handler arm の continuation use / tail arg / multi-shot use を surface に出す必要がある。

## Evidence VM handler continuation-use surface

`EvidenceVmHandlerArmPlan` / `EvidenceVmHandlerObjectPlan` に continuation-use summary を追加した。
runtime semantics は変えず、handler arm の `direct_calls`、`delayed_calls`、`escapes`、`tail_arg` を
plan dump と object graph に出すだけの観測パッチ。

結果:

- `bench/nondet_20_discard.yu`: `handler_continuations: direct_calls 0 delayed_calls 10 escape_arms 5 tail_arms 0`
- `examples/showcase.yu`: `handler_continuations: direct_calls 0 delayed_calls 16 escape_arms 8 tail_arms 0`
- `std::control::nondet::nondet::branch` と `flip::coin` の handler は、直 tail resume ではなく `make-thunk` 内の delayed continuation call として現れている。

判断:

- 今の遅さは単純な `k arg` tail-resume fast path では削れない。
- 次に必要なのは、`make-thunk((k arg) ...)` を通常の expr thunk ではなく evidence continuation thunk / delayed continuation plan として扱う設計。
- `escapes true` は従来通り安全側の分類として残しているので、このパッチ自体は request path の選択を変えない。

確認:

- `cargo fmt --all --check`
- `cargo check -q -p evidence-vm -p yulang`
- `cargo test -q -p evidence-vm`
- `cargo test -q -p yulang debug_evidence_vm_plan -- --test-threads=1`
- `cargo test -q -p yulang runtime_evidence -- --test-threads=1`

## Rejected: continuation-call expr thunk shortcut

`make-thunk(k immediate)` を通常の `RuntimeEvidenceThunk::Expr` ではなく専用の
`ExprContinuationCall` thunk として保持する実験をした。狙いは、`all_paths` のような
handler body に出る delayed continuation call で、body の `Apply(Local(k), arg)` 評価を短絡すること。

途中で分かったこと:

- continuation と arg を thunk 作成時に取り出す版は、`debug_runtime_evidence_run_resumes_carried_callback_markers` で `tick::ping` が escape した。
- force 時に env/provider を復元する版でも、`apply_value_result_inner` に直行すると同じ hygiene 回帰が出た。
- `apply_scoped_value_result` を通すと回帰は消えるが、release 実測では速くならず、むしろ marker/continuation force が増えた。

結果:

- `bench/nondet_20_discard.yu`: `continuation_call_expr_thunks: 840`、`compare.control: match`、`timing.runtime_evidence_execute: 26.7ms`
- `examples/showcase.yu`: `continuation_call_expr_thunks: 1655`、`compare.control: match`、`timing.runtime_evidence_execute: 43.0ms`
- 直前の採用済み実装は nondet が 24.7-26.5ms 帯、showcase が 42-46ms 帯なので、明確な改善ではない。

判断:

- `Apply` 経路は単なる関数適用ではなく、function-call marker を閉じる意味論を持つ。
- ここを飛ばす最適化は handler hygiene を壊す。
- scoped apply を残すと、安全にはなるが、extra continuation thunk/marked force が増えて速くならない。
- 実行器変更は revert し、観測用の continuation-use surface だけ残す。

## Rejected: continuation append COW spine

`EvidenceContinuation::then_counted` で、`Rc::get_mut` に失敗したときも frame spine を clone して
tail append を試す COW 実験をした。`Then` node を減らし、`continuation_resume_then_steps` を下げる狙い。

結果:

- `bench/nondet_20_discard.yu`: release 10回が 26.3-34.6ms、`compare.control: match`
- `examples/showcase.yu`: release 10回が 45.5-55.6ms、`compare.control: match`
- `continuation_append_steps` / `continuation_resume_then_steps` は期待したほど下がらなかった。

判断:

- hot な append 失敗の主因は単なる `Rc` 共有ではなく、`MarkerFrame` / `ProviderEnv` の scope boundary で append を止める必要があること。
- COW してもその境界は越えられないため、clone 分のコストだけ増えやすい。
- 実行器変更は revert した。

## Evidence VM pure thunk marker refinement

`RuntimeEvidenceThunk::Expr` に `needs_hygiene_mark` を持たせた。`MakeThunk` の target type から、
effect が pure かつ返り値型も hygiene mark 不要なら、その thunk value 自体を marker 対象から外す。

狙い:

- effectful thunk / continuation thunk / adapter thunk は従来通り mark 対象。
- pure な値だけを返す thunk は、marker frame で包んでも新しい effect や hygiene-sensitive value を生まないので、marker を巻かない。

確認:

- `cargo check -q -p evidence-vm -p yulang`
- `cargo test -q -p evidence-vm`
- `cargo test -q -p yulang runtime_evidence -- --test-threads=1`
- release `bench/nondet_20_discard.yu`: 25.1-30.2ms、`compare.control: match`
- release `examples/showcase.yu`: 41.6-50.2ms、`compare.control: match`

読み:

- nondet/showcase の主経路は effectful thunk / continuation thunk が中心なので、大きな速度差は出ない。
- ただし pure thunk への過剰 marker を避ける規則としては一般的で、単体テストも追加した。

## Evidence VM call-boundary surface

`notes/design/2026-06-28-evidence-vm-hygiene-pro-review.md` の整理に合わせ、
`apply_scoped_value_result` / `force_effect_call_scoped_result` が直接
`active_marker_plans.last()` を読んでいた箇所を `EvidenceCallBoundary` へ寄せた。

変更:

- `EvidenceCallBoundary` を追加し、source / marker set / active AddId 化 / handler path を一つの
  hygiene boundary として扱う入口を置いた。
- `EvidenceCallBoundaryBypassCert` を追加し、function-call marker close を迂回する理由を
  `NoActiveMarkerPlan` / `NoFunctionCallMarkers` として明示した。
- `ScopedApply` と `MarkedForce` は `active_function_call_boundary` を通してから、
  frame を開くか、frame なし close にするか、通常実行へ戻すかを選ぶ。

判断:

- これは速度改善ではなく、`apply_scoped_value_result` を意味論 primitive として扱うための足場。
- 以前失敗した continuation-call shortcut は、ここでいう boundary close の証明を持たずに
  `apply_value_result_inner` へ直行したことが問題だった。
- 次に fast path を足す場合は、`EvidenceCallBoundaryBypassCert` かそれに続く
  `BoundaryAlreadyClosed` / `StaticDirectNoResume` 相当の証明を持たせる。

確認:

- `cargo check -q -p evidence-vm -p yulang`
- `cargo test -q -p evidence-vm`
- `cargo test -q -p yulang runtime_evidence -- --test-threads=1`
- release compare-control:
  - `bench/nondet_20_discard.yu`: `timing.runtime_evidence_execute: 30.6ms`、`compare.control: match`
  - `examples/showcase.yu`: `timing.runtime_evidence_execute: 45.5ms`、`compare.control: match`

## Evidence VM provider grant gate

provider-env route の安全判定で使っていた `scope_id` と `hygiene_baseline` の field 比較を、
`EvidenceProviderGrantGate` として名前付きにした。

変更:

- `EvidenceProviderGrant::gate()` を追加し、provider grant から request-free route の gate を取り出す。
- `RuntimeEvidenceProviderFrame::matches_gate` / `gate_is_unshadowed` を追加した。
- `provider_grant_gate_passes` と `provider_grant_path_gate_failure` は field 比較ではなく
  gate 経由で判定する。

判断:

- 挙動は変えず、`scope + hygiene baseline` が provider route の証明であることをコード上に出した。
- 次に `StaticDirectNoResume` や guarded direct-tail fast path を増やすとき、この gate を
  `EvidenceCallBoundary` の cert と並べて扱える。

確認:

- `cargo fmt --all --check`
- `cargo check -q -p evidence-vm -p yulang`
- `cargo test -q -p evidence-vm`
- `cargo test -q -p yulang runtime_evidence -- --test-threads=1`
- release `bench/nondet_20_discard.yu`: `timing.runtime_evidence_execute: 25.8ms`、`compare.control: match`

## Evidence VM direct-abortive close cert

`DirectAbortive` が marker/call boundary close で素通りする根拠を、
`EvidenceDirectAbortiveCloseCert` として signal 側へ持たせた。

変更:

- `EvidenceDirectAbortive` に `close_cert` を追加した。
- 現在の生成箇所は `EvidenceDirectAbortive::static_handler` に集め、
  `StaticAbortiveHandler` cert を付ける。
- `close_marked_signal` / `close_marked_result_for_type` / `close_marker_frame_signal` は、
  `close_direct_abortive` を通して cert を見てから素通りさせる。

判断:

- 挙動は変えず、DirectAbortive が boundary close を迂回できる理由を構造化した。
- 今後 provider由来の abortive route や boundary already closed route を増やす場合、
  cert variant を増やさずに素通りさせない。

確認:

- `cargo fmt --all --check`
- `cargo check -q -p evidence-vm -p yulang`
- `cargo test -q -p evidence-vm`
- `cargo test -q -p yulang runtime_evidence -- --test-threads=1`

## Evidence VM signal hygiene state

guarded direct-tail route が active marker から guard state を得るためだけに
`EvidenceRequest` を仮組みしていた箇所を外した。

変更:

- `EvidenceSignalHygiene` を追加し、`guard_ids` / `carried_guards` の計算を request から分離した。
- `mark_request_with_active_markers` は request から hygiene state を取り出し、同じ helper で更新して戻すだけにした。
- `guarded_direct_tail_resumptive` は request を作らず、
  `signal_hygiene_with_active_markers` から得た hygiene state を直接 `EvidenceDirectTailResumptive` へ載せる。
- request 専用になっていた entry-except / carried guard helper は、path と guard slice を受け取る形へ寄せた。

判断:

- 挙動は変えず、DirectTailResumptive が request fallback の内部表現を借りない形へ一段進めた。
- これだけで大きく速くなる変更ではないが、request-free direct-tail 専用の continuation / hygiene 表現へ進む足場になる。

確認:

- `cargo fmt --all --check`
- `cargo check -q -p evidence-vm -p yulang`
- `cargo test -q -p evidence-vm`
- `cargo test -q -p yulang runtime_evidence -- --test-threads=1`
- release `bench/nondet_20_discard.yu`: `timing.runtime_evidence_execute: 27.5ms`、`compare.control: match`

## Evidence VM direct-tail hygiene field

`EvidenceDirectTailResumptive` の中に残っていた `guard_ids` / `carried_guards` の裸フィールドを、
`EvidenceSignalHygiene` として持つ形へ寄せた。

変更:

- `EvidenceDirectTailResumptive` は `hygiene: EvidenceSignalHygiene` を持つ。
- direct-tail の request fallback 変換では、その hygiene state を `EvidenceRequest` の
  `guard_ids` / `carried_guards` へ移す。
- 可視性判定と handler boundary 計算は `call.hygiene` を読む形にした。

判断:

- これも挙動変更ではなく、request-free direct-tail signal が自身の hygiene state を
  request とは独立に持つための足場。
- 次に continuation / marker-frame state を direct-tail 専用に軽くする時、
  guard/carried guard の所有境界が見えやすくなる。

確認:

- `cargo fmt --all --check`
- `cargo check -q -p evidence-vm -p yulang`
- `cargo test -q -p evidence-vm`
- `cargo test -q -p yulang runtime_evidence -- --test-threads=1`

## Evidence VM request hygiene field

`EvidenceRequest` も `guard_ids` / `carried_guards` を直接持つ形から
`EvidenceSignalHygiene` を持つ形へ寄せた。

変更:

- `EvidenceRequest` に `hygiene: EvidenceSignalHygiene` を持たせた。
- request / direct-tail の相互変換は、同じ hygiene object を移すだけにした。
- request の可視性判定と handler boundary 計算は `request.hygiene` を読む形にした。

判断:

- request と direct-tail が同じ hygiene carrier を持つようになった。
- これで次に `FallbackRequest` と request-free signal の橋を作るとき、
  guard/carried guard の変換を別々に保守しなくてよくなる。

確認:

- `cargo fmt --all --check`
- `cargo check -q -p evidence-vm -p yulang`
- `cargo test -q -p evidence-vm`
- `cargo test -q -p yulang runtime_evidence -- --test-threads=1`

## Evidence VM handler boundary in signal hygiene

`handler_boundary` も request / direct-tail 個別のフィールドではなく、
`EvidenceSignalHygiene` の一部として持つ形へ寄せた。

変更:

- `EvidenceSignalHygiene` に `handler_boundary` を追加した。
- `EvidenceRequest` と `EvidenceDirectTailResumptive` から個別の `handler_boundary` field を外した。
- marker frame close や routed yield close は `signal.hygiene.set_handler_boundary` を通して境界を閉じる。
- request / direct-tail の可視性判定は `hygiene.handler_boundary` を読む形にした。

判断:

- `guard_ids` / `carried_guards` / `handler_boundary` が同じ signal hygiene carrier にまとまった。
- request-free signal と fallback request の相互変換で、衛生性に関わる状態を一つの値として扱えるようになった。

確認:

- `cargo fmt --all --check`
- `cargo check -q -p evidence-vm -p yulang`
- `cargo test -q -p evidence-vm`
- `cargo test -q -p yulang runtime_evidence -- --test-threads=1`

## Evidence VM permission visibility surface

handler hygiene を拒否リストから許可制へ移す準備として、挙動を変えずに
evidence object graph へ capability / allowed-set の名前付き surface を足した。

変更:

- `EvidenceVmHandlerCapId` と `EvidenceVmAllowedSetId` を追加した。
- `EvidenceVmHandlerObjectPlan` に `capability_id` を持たせた。
- `EvidenceVmObjectPlan` に `handler_capabilities` と `allowed_sets` を持たせた。
- `EvidenceVmOperationObjectPlan` に `EvidenceVmOperationVisibilityPlan` を持たせた。
- 現段階の allowed set は、direct candidate の singleton capability か
  `LegacyGuardBridge` の二種類だけにした。
- plan dump に capability / allowed-set / operation visibility を出すようにした。

判断:

- plain color ではなく handler capability として扱う。
- runtime の捕捉判定はまだ旧 `EvidenceSignalHygiene` / marker / guard を読む。
- 今回の surface は、後で旧 guard 判定と allowed-set 判定を debug で突き合わせるための足場。
- fallback は `LegacyGuardBridge` として明示し、allowed set が未実装の部分を
  empty と誤表示しないようにした。

確認:

- `cargo fmt --all --check`
- `cargo test -q -p evidence-vm -- --test-threads=1`
- `cargo test -q -p yulang runtime_evidence -- --test-threads=1`
- release `bench/nondet_20_discard.yu`: `timing.runtime_evidence_execute: 26.5ms`、`compare.control: match`
- release `examples/showcase.yu`: `timing.runtime_evidence_execute: 46.5ms`、`compare.control: match`

## Evidence VM permission visibility shadow check

operation visibility surface を runtime signal へ運び、旧 guard hygiene と permission
visibility の併走チェックを追加した。

変更:

- `RuntimeEvidenceRunContext` に operation call から visibility を引く table を追加した。
- `EffectThunkEvidence` に `RuntimeEvidenceOperationVisibility` を載せた。
- `EvidenceSignalHygiene` に `operation_visibility` を載せた。
- `EvidenceRequest` / `EvidenceDirectTailResumptive` は operation 発生時の visibility を保持する。
- debug build では、guard / carried guard / handler boundary が空で、
  `LegacyGuardBridge` ではない clean signal に限り、旧 guard 判定と
  allowed handler 判定が一致することを `debug_assert_eq!` する。

判断:

- まだ permission model を主 semantics にはしない。
- guarded direct-tail や marker boundary 付き signal は、allowed-set transform が未実装なので
  比較対象から外した。
- ここで比較範囲を絞ることで、plain capability surface の正しさだけを先に固定し、
  marker / resume 変換の問題を混ぜない。

確認:

- `cargo fmt --all --check`
- `cargo check -q -p evidence-vm -p yulang`
- `cargo check -q --release -p evidence-vm -p yulang`
- `cargo test -q -p evidence-vm -- --test-threads=1`
- `cargo test -q -p yulang runtime_evidence -- --test-threads=1`
- release `bench/nondet_20_discard.yu`: `timing.runtime_evidence_execute: 27.7ms`、`compare.control: match`
- release `examples/showcase.yu`: `timing.runtime_evidence_execute: 48.1ms`、`compare.control: match`

## Evidence VM permission transform flags

permission visibility を単なる operation visibility id ではなく、signal 内の
transform state として持つ形へ一段進めた。

変更:

- `EvidenceSignalHygiene` の permission 側状態を `EvidenceSignalPermissionVisibility` に分けた。
- operation 発生時の visibility を base として保持する。
- `push_guard_id` は permission 側の `guard_mask` を記録する。
- `push_carried_guard` を追加し、resume に持ち越す guard は `resume_delta` として記録する。
- `set_handler_boundary` は `handler_boundary_mask` を記録する。
- debug shadow check は、base visibility が legacy bridge ではなく、transform が identity の時だけ走る。

判断:

- これはまだ allowed-set transform の実行ではない。
- ただし「なぜ shadow check の対象外か」が guard / resume / boundary の三種類に分かれた。
- 次にやる allowed-set transform は、この三種類のどれを permission model 上で表現するかとして切れる。

確認:

- `cargo fmt --all --check`
- `cargo check -q -p evidence-vm -p yulang`
- `cargo check -q --release -p evidence-vm -p yulang`
- `cargo test -q -p evidence-vm -- --test-threads=1`
- `cargo test -q -p yulang runtime_evidence -- --test-threads=1`
- release `bench/nondet_20_discard.yu`: `timing.runtime_evidence_execute: 28.2ms`、`compare.control: match`
- release `examples/showcase.yu`: `timing.runtime_evidence_execute: 48.2ms`、`compare.control: match`

## Evidence VM provider permission visibility metrics

permission visibility の runtime stats を追加し、provider-env route で選んだ handler を
provider grant 由来の allowed handler として signal へ載せるようにした。

変更:

- `RuntimeEvidenceOperationVisibilitySource` を追加し、plan allowed set と provider grant を分けた。
- `provider_route_for_call` は、provider env から handler を選んだ時点で
  `RuntimeEvidenceOperationVisibility::provider_grant(handler_id)` を返す。
- runner 側では provider route の visibility を、operation object の legacy visibility より優先する。
- `RuntimeEvidenceRunStats` に permission visibility の分布 counter を追加した。
- `debug runtime-evidence-run` に permission visibility stats を表示するようにした。

分かったこと:

- `bench/nondet_20_discard.yu`:
  - `permission_visibility_signals: 819`
  - `legacy_bridge: 399`
  - `guard_mask: 420`
  - `handler_boundary_mask: 420`
- `examples/showcase.yu`:
  - `permission_visibility_signals: 1511`
  - `legacy_bridge: 682`
  - `guard_mask: 829`
  - `handler_boundary_mask: 829`
- 代表 workload では identity visibility はまだ 0。
  provider grant visibility は届いたが、direct-tail 候補は guard mask と handler boundary mask の
  両方で旧 hygiene に戻っている。

判断:

- 次の本命は、provider grant visibility に対する guard / handler-boundary transform の定義。
- `resume_delta` は代表 workload では 0 なので、まずは guard mask と boundary mask を先に見るのが自然。

確認:

- `cargo fmt --all --check`
- `cargo check -q -p evidence-vm -p yulang`
- `cargo check -q --release -p evidence-vm -p yulang`
- `cargo test -q -p evidence-vm -- --test-threads=1`
- `cargo test -q -p yulang runtime_evidence -- --test-threads=1`
- release `bench/nondet_20_discard.yu`: `timing.runtime_evidence_execute: 26.4ms`、`compare.control: match`
- release `examples/showcase.yu`: `timing.runtime_evidence_execute: 44.4ms`、`compare.control: match`

## Evidence VM permission transform overlap metrics

permission visibility の transform counter に、guard と handler boundary の重なりを見る
組み合わせ counter を追加した。

分かったこと:

- `bench/nondet_20_discard.yu`:
  - `guard_mask: 420`
  - `handler_boundary_mask: 420`
  - `guard_and_boundary_mask: 420`
  - `guard_without_boundary: 0`
  - `boundary_without_guard: 0`
- `examples/showcase.yu`:
  - `guard_mask: 829`
  - `handler_boundary_mask: 829`
  - `guard_and_boundary_mask: 829`
  - `guard_without_boundary: 0`
  - `boundary_without_guard: 0`

判断:

- 代表 workload では、provider grant visibility が届いた direct-tail 候補は
  guard mask と handler-boundary mask を同時に持つ。
- 次の transform はこの pair を一体として扱うのが自然。
  片方だけを先に fast path 化しても、代表 workload には刺さらない可能性が高い。

確認:

- `cargo fmt --all --check`
- `cargo check -q -p evidence-vm -p yulang`
- `cargo check -q --release -p evidence-vm -p yulang`
- `cargo test -q -p evidence-vm -- --test-threads=1`
- `cargo test -q -p yulang runtime_evidence -- --test-threads=1`
- release `bench/nondet_20_discard.yu`: `timing.runtime_evidence_execute: 27.4ms`、`compare.control: match`
- release `examples/showcase.yu`: `timing.runtime_evidence_execute: 47.2ms`、`compare.control: match`

## Evidence VM named guard-boundary transform

permission visibility の transform helper を追加し、hot path で見えている
`guard_mask + handler_boundary_mask` を `is_guard_boundary_pair` として名前付けした。

変更:

- `EvidenceSignalPermissionTransform::is_guard_boundary_pair` を追加した。
- `is_guard_only` / `is_boundary_only` も追加した。
- stats の overlap 分類を raw bool tuple ではなく helper 経由にした。
- debug shadow check はまだ identity transform だけを対象にする。

判断:

- これは意味論変更ではなく、次の pair shadow check のための命名整理。
- 代表 workload に刺さる transform が pair であることを、コード上でも読める形にした。

確認:

- `cargo fmt --all --check`
- `cargo check -q -p evidence-vm -p yulang`
- `cargo test -q -p evidence-vm -- --test-threads=1`

## Evidence VM guard-boundary permission shadow

provider grant 由来の permission visibility について、`guard_mask + handler_boundary_mask` の
pair transform を debug shadow check の対象に広げた。

変更:

- permission visibility の shadow 対象を `EvidencePermissionShadowKind` として分類した。
- identity transform は従来通り direct permission 判定で shadow する。
- provider grant 由来かつ guard-boundary pair の transform は、`GuardBoundaryPairBridge`
  として shadow する。
- pair bridge はまだ新しい実行意味論ではない。allowed handler の一致を先に確認し、
  その上で旧 guard / handler-boundary 判定と同じ結果になることだけを debug assert する。
- release 実行の可視性判定は変えていない。

確認:

- `cargo fmt --all --check`
- `cargo check -q -p evidence-vm -p yulang`
- `cargo check -q --release -p evidence-vm -p yulang`
- `cargo test -q -p evidence-vm -- --test-threads=1`
- `cargo test -q -p yulang runtime_evidence -- --test-threads=1`
- debug `bench/nondet_20_discard.yu`: `permission_visibility_guard_and_boundary_mask: 420`、
  `compare.control: match`
- debug `examples/showcase.yu`: `permission_visibility_guard_and_boundary_mask: 829`、
  `compare.control: match`
- release `bench/nondet_20_discard.yu`: `timing.runtime_evidence_execute: 28.7ms`、
  `compare.control: match`
- release `examples/showcase.yu`: `timing.runtime_evidence_execute: 46.9ms`、
  `compare.control: match`

判断:

- 代表 workload の provider grant pair は、旧 hygiene と同値に見えている。
- ただし bridge はまだ旧 guard / boundary state を読むため、permission semantics への置換ではない。
  次は pair の意味を `provider grant permission + boundary exposure` として名前付きの runtime surface に
  近づける段階。

## Evidence VM provider grant permission surface

`RuntimeEvidenceOperationVisibility` を struct から enum へ変え、provider grant 由来の visibility を
`RuntimeEvidenceProviderGrantPermission` として表すようにした。

変更:

- `PlanAllowedSet { allowed_set_id, allowed_handler_id, legacy_guard_bridge }` と
  `ProviderGrant(RuntimeEvidenceProviderGrantPermission)` を分けた。
- provider grant permission は handler id を必ず持つ。`ProviderGrant` なのに
  `allowed_handler_id == None` という invalid state は作れない。
- guard-boundary pair shadow は `provider_grant_permission()` を通して provider grant 由来だけを対象にする。
- release 実行の visible 判定は変えていない。

確認:

- `cargo fmt --all --check`
- `cargo check -q -p evidence-vm -p yulang`
- `cargo check -q --release -p evidence-vm -p yulang`
- `cargo test -q -p evidence-vm -- --test-threads=1`
- `cargo test -q -p yulang runtime_evidence -- --test-threads=1`
- debug `bench/nondet_20_discard.yu`: `permission_visibility_guard_and_boundary_mask: 420`、
  `compare.control: match`
- debug `examples/showcase.yu`: `permission_visibility_guard_and_boundary_mask: 829`、
  `compare.control: match`
- release `bench/nondet_20_discard.yu`: `timing.runtime_evidence_execute: 32.6ms`、
  `compare.control: match`
- release `examples/showcase.yu`: `timing.runtime_evidence_execute: 46.9ms`、
  `compare.control: match`

判断:

- 速度改善ではないが、permission visibility を実行意味論へ移す前の型表面としては一段進んだ。
- 次は `GuardBoundaryPairBridge` の中身を、旧 guard/boundary state を読む bridge から、
  provider grant permission と boundary exposure を持つ surface へ寄せる。

## Evidence VM named visibility merge

provider env route が見つかったときの `provider_visibility.or(static_visibility)` を
`merge_operation_visibility(static_visibility, provider_visibility)` に置き換えた。

判断:

- provider grant visibility は active provider env から選ばれた runtime evidence なので、
  static operation visibility より具体的に扱う。
- 今回は名前付けだけで、merge の意味は従来の provider 優先のまま。
- 将来 `static allowed set + provider grant` の合成が必要になったとき、この関数を入口にする。

確認:

- `cargo fmt --all --check`
- `cargo check -q -p evidence-vm -p yulang`
- `cargo check -q --release -p evidence-vm -p yulang`
- `cargo test -q -p evidence-vm -- --test-threads=1`
- release `bench/nondet_20_discard.yu`: `timing.runtime_evidence_execute: 26.5ms`、
  `compare.control: match`
- release `examples/showcase.yu`: `timing.runtime_evidence_execute: 44.4ms`、
  `compare.control: match`

## Evidence VM provider permission shadow payload

guard-boundary pair の shadow kind を、単なる `GuardBoundaryPairBridge` から
`ProviderGrantBoundaryPair(RuntimeEvidenceProviderGrantPermission)` へ変えた。

変更:

- `EvidencePermissionShadowKind` が provider grant permission を payload として持つ。
- pair bridge は `RuntimeEvidenceOperationVisibility` を再解釈せず、
  渡された `RuntimeEvidenceProviderGrantPermission` が catch 内で許可されるかを見る。
- `RuntimeEvidenceRunContext::catch_has_provider_grant_permission` を追加し、
  handler id と request family の確認を context 側へ置いた。
- release 実行の visible 判定は変えていない。

確認:

- `cargo fmt --all --check`
- `cargo check -q -p evidence-vm -p yulang`
- `cargo check -q --release -p evidence-vm -p yulang`
- `cargo test -q -p evidence-vm -- --test-threads=1`
- `cargo test -q -p yulang runtime_evidence -- --test-threads=1`
- debug `bench/nondet_20_discard.yu`: `permission_visibility_guard_and_boundary_mask: 420`、
  `compare.control: match`
- debug `examples/showcase.yu`: `permission_visibility_guard_and_boundary_mask: 829`、
  `compare.control: match`
- release `bench/nondet_20_discard.yu`: `timing.runtime_evidence_execute: 33.9ms`、
  `compare.control: match`
- release `examples/showcase.yu`: `timing.runtime_evidence_execute: 48.8ms`、
  `compare.control: match`

判断:

- pair shadow は「provider grant permission が見えるか」を入口にできた。
- まだ boundary exposure 自体は旧 guard / handler-boundary state を bridge として読んでいる。
  次の段階は、boundary exposure を別 surface にして、旧 state 依存をさらに狭めること。

## Evidence VM guard-boundary exposure surface

旧 hygiene の `handler_boundary` / `guard_ids` / `carried_guards` を直接
`visible_operation_resumptive_parts` へ渡すのをやめ、
`EvidenceGuardBoundaryExposure` としてまとめた。

変更:

- `EvidenceGuardBoundaryExposure::from_hygiene` を追加した。
- request / direct-tail / permission shadow bridge の visible 判定は、この exposure surface を通す。
- まだ exposure の中身は旧 hygiene state への参照。意味論は変えていない。

確認:

- `cargo fmt --all --check`
- `cargo check -q -p evidence-vm -p yulang`
- `cargo check -q --release -p evidence-vm -p yulang`
- `cargo test -q -p evidence-vm -- --test-threads=1`
- `cargo test -q -p yulang runtime_evidence -- --test-threads=1`
- debug `bench/nondet_20_discard.yu`: `permission_visibility_guard_and_boundary_mask: 420`、
  `compare.control: match`
- debug `examples/showcase.yu`: `permission_visibility_guard_and_boundary_mask: 829`、
  `compare.control: match`
- release `bench/nondet_20_discard.yu`: `timing.runtime_evidence_execute: 27.7ms`、
  `compare.control: match`
- release `examples/showcase.yu`: `timing.runtime_evidence_execute: 47.6ms`、
  `compare.control: match`

判断:

- guard/boundary visibility の旧 state 依存を一箇所へ寄せられた。
- 次に permission semantics へ寄せるなら、この exposure surface の生成元を
  provider grant permission / boundary exposure に差し替えるのが自然。

## Evidence VM provider boundary shadow metrics

provider grant 由来の guard-boundary pair について、release でも shadow stats を取るようにした。
実行の visible 判定はまだ変えていない。

追加した stats:

- `permission_shadow_provider_boundary_pair`
- `permission_shadow_provider_boundary_pair_legacy_visible`
- `permission_shadow_provider_boundary_pair_permission_visible`
- `permission_shadow_provider_boundary_pair_match`
- `permission_shadow_provider_boundary_pair_mismatch`
- `permission_shadow_provider_boundary_pair_no_allowed_handler`

結果:

- debug `bench/nondet_20_discard.yu`
  - pair: 420
  - match: 420
  - mismatch: 0
  - no allowed handler: 0
- debug `examples/showcase.yu`
  - pair: 829
  - match: 829
  - mismatch: 0
  - no allowed handler: 0
- release `bench/nondet_20_discard.yu`
  - pair: 420
  - match: 420
  - mismatch: 0
  - no allowed handler: 0
  - `timing.runtime_evidence_execute: 28.0ms`
- release `examples/showcase.yu`
  - pair: 829
  - match: 829
  - mismatch: 0
  - no allowed handler: 0
  - `timing.runtime_evidence_execute: 53.2ms`

確認:

- `cargo fmt --all --check`
- `cargo check -q -p evidence-vm -p yulang`
- `cargo check -q --release -p evidence-vm -p yulang`
- `cargo test -q -p evidence-vm -- --test-threads=1`
- `cargo test -q -p yulang runtime_evidence -- --test-threads=1`
- debug / release の `bench/nondet_20_discard.yu` と `examples/showcase.yu` は
  どちらも `compare.control: match`

判断:

- 代表 workload では、provider grant + guard-boundary pair の permission shadow は旧 hygiene と完全一致した。
- 次は `PermissionVisibleDecision` を入れて `NotApplicable` と invisible を分けた上で、
  `DirectTailResumptive` の provider pair だけ release visible 判定に使う条件が揃った。

## Evidence VM provider boundary direct-tail decision

`PermissionVisibleDecision` を追加し、`DirectTailResumptive` の provider grant + guard-boundary pair を
permission visible decision 経由で返すようにした。

変更:

- `PermissionVisibleDecision::NotApplicable` と `Handled(EvidencePermissionVisibleResult)` を分けた。
- provider grant + guard-boundary pair では `Handled(None)` を invisible として扱い、
  `NotApplicable` と混ぜないようにした。
- `visible_direct_tail_resumptive` は対象 pair なら permission result を返す。
- ただし release でも legacy visible を並行計算し、shadow stats と debug assert は残している。
  そのため、この commit はまだ速度改善を主張しない。

結果:

- debug `bench/nondet_20_discard.yu`
  - pair: 420
  - match: 420
  - mismatch: 0
- debug `examples/showcase.yu`
  - pair: 829
  - match: 829
  - mismatch: 0
- release `bench/nondet_20_discard.yu`
  - pair: 420
  - match: 420
  - mismatch: 0
  - `timing.runtime_evidence_execute: 27.1ms`
- release `examples/showcase.yu`
  - pair: 829
  - match: 829
  - mismatch: 0
  - `timing.runtime_evidence_execute: 46.3ms`

確認:

- `cargo fmt --all --check`
- `cargo check -q -p evidence-vm -p yulang`
- `cargo check -q --release -p evidence-vm -p yulang`
- `cargo test -q -p evidence-vm -- --test-threads=1`
- `cargo test -q -p yulang runtime_evidence -- --test-threads=1`
- debug / release の `bench/nondet_20_discard.yu` と `examples/showcase.yu` は
  どちらも `compare.control: match`

判断:

- release visible 判定の返り値は permission decision 側へ移った。
- まだ旧 exposure bridge を読んでいるため、性能改善は小さい。
- 次の速度面の本命は、shadow stats を維持しつつ legacy visible の並行計算を外せる条件を作るか、
  `EvidenceGuardBoundaryExposure` の中身を permission-native な boundary exposure へ変えること。

## Evidence VM provider boundary fast path counters

release default では、`DirectTailResumptive` の provider grant + guard-boundary pair について
legacy visible の並行計算を外し、permission result をそのまま返すようにした。
debug build では従来通り legacy visible を計算し、shadow stats と debug assert を残す。

追加した fast path stats:

- `permission_provider_boundary_pair_fast_paths`
- `permission_provider_boundary_pair_fast_path_visible`
- `permission_provider_boundary_pair_fast_path_invisible`
- `permission_provider_boundary_pair_fast_path_no_allowed_handler`

追加したテスト:

- provider pair だが catch 側に allowed handler がない場合、
  `Handled(NoAllowedHandler)` になり、`NotApplicable` と混ざらないこと。
- provider pair ではない transform は `NotApplicable` になること。

結果:

- debug `bench/nondet_20_discard.yu`
  - shadow pair: 420
  - fast paths: 0
  - `compare.control: match`
- debug `examples/showcase.yu`
  - shadow pair: 829
  - fast paths: 0
  - `compare.control: match`
- release `bench/nondet_20_discard.yu`
  - shadow pair: 0
  - fast paths: 420
  - fast visible: 420
  - no allowed handler: 0
  - 5回実行: 27.9ms / 28.2ms / 30.2ms / 27.5ms / 29.6ms
- release `examples/showcase.yu`
  - shadow pair: 0
  - fast paths: 829
  - fast visible: 829
  - no allowed handler: 0
  - 5回実行: 49.4ms / 48.7ms / 48.4ms / 45.8ms / 47.5ms

確認:

- `cargo fmt --all --check`
- `cargo check -q -p evidence-vm -p yulang`
- `cargo check -q --release -p evidence-vm -p yulang`
- `cargo test -q -p evidence-vm -- --test-threads=1`
- `cargo test -q -p yulang runtime_evidence -- --test-threads=1`
- debug / release の `bench/nondet_20_discard.yu` と `examples/showcase.yu` は
  どちらも `compare.control: match`

判断:

- release default では、direct-tail provider pair の旧 visible 並行計算を外せた。
- visible 判定の返り値は permission result 側へ移り、debug では同値確認を残せている。
- 実時間は大きくは動かない。次は active marker scan / signal construction か、
  permission-native な boundary exposure へ進むのが本命。

## Evidence VM provider AddId shortcut verification

`AddIdShadowed` から provider permission direct-tail へ落とす shortcut について、
debug build だけ旧 full marker scan hygiene も作り、shortcut hygiene との差分を測る
counter を追加した。release build では旧 full scan を作らない。

追加した stats:

- `provider_add_id_shortcut_attempts`
- `provider_add_id_shortcut_used`
- `provider_add_id_shortcut_fallback_carry_after_frame`
- `provider_add_id_shortcut_fallback_no_provider_permission`
- `provider_add_id_shortcut_full_scan_guard_visible_match`
- `provider_add_id_shortcut_full_scan_guard_visible_mismatch`
- `provider_add_id_shortcut_full_scan_extra_guards`
- `provider_add_id_shortcut_full_scan_extra_carried_guards`

結果:

- debug `bench/nondet_20_discard.yu`
  - attempts / used: 420 / 420
  - fallback: 0
  - guard visible match / mismatch: 1 / 419
  - extra guards: 420
  - `compare.control: match`
- debug `examples/showcase.yu`
  - attempts / used: 829 / 829
  - fallback: 0
  - guard visible match / mismatch: 3 / 826
  - extra guards: 829
  - `compare.control: match`
- release `bench/nondet_20_discard.yu`
  - attempts / used: 420 / 420
  - full-scan guard visible counters: 0
  - `active_add_id_scans: 23940`
  - `timing.runtime_evidence_execute: 25.2ms`
  - `compare.control: match`
- release `examples/showcase.yu`
  - attempts / used: 829 / 829
  - full-scan guard visible counters: 0
  - `active_add_id_scans: 26611`
  - `timing.runtime_evidence_execute: 45.2ms`
  - `compare.control: match`

読み:

- shortcut hygiene は旧 full scan hygiene の完全コピーではない。
  operation site の guard 可視性だけを見ると、多くのケースで旧 full scan とズレる。
- それでも catch 側の provider permission visible 判定と compare-control は一致するため、
  ここで守るべき同値は guard vector の同一性ではなく、permission-native visible の観測同値。
- release では検証用 full scan を作らないため、hot path の active scan 削減は維持される。

確認:

- `cargo fmt --all --check`
- `cargo check -q -p evidence-vm -p yulang`
- `cargo check -q --release -p evidence-vm -p yulang`
- `cargo test -q -p evidence-vm -- --test-threads=1`
- `cargo test -q -p yulang runtime_evidence -- --test-threads=1`
- `tests/yulang/yulang-adversarial-corpus/probe.sh`
- debug / release の `bench/nondet_20_discard.yu` と `examples/showcase.yu` は
  どちらも `compare.control: match`

## Evidence VM permission-native resume marker candidates

`EvidenceContinuationFrame::MarkerFrame` をすぐ消すのではなく、resume 結果が
permission-native boundary へ置き換えられる候補かどうかを分類する counter を追加した。
実行挙動は変えていない。

追加した stats:

- `resume_marker_permission_native_candidates`
- `resume_marker_permission_native_provider_pair`
- `resume_marker_permission_native_reject_no_provider_permission`
- `resume_marker_permission_native_reject_resume_delta`
- `resume_marker_permission_native_reject_handler_path`
- `resume_marker_permission_native_reject_value_result`
- `resume_marker_permission_native_reject_legacy_signal`
- `resume_marker_permission_native_reject_legacy_bridge`
- `resume_marker_permission_native_reject_other_transform`
- `resume_marker_permission_native_reject_error_result`

release `bench/nondet_20_discard.yu`:

- `continuation_resume_marker_result_direct_tail_provider_boundary_pair: 21148`
- `resume_marker_permission_native_candidates: 12559`
- `resume_marker_permission_native_reject_handler_path: 8589`
- `resume_marker_permission_native_reject_value_result: 20201`
- `resume_marker_permission_native_reject_legacy_signal: 6951`
- `resume_marker_permission_native_reject_resume_delta: 0`
- `resume_marker_permission_native_reject_legacy_bridge: 0`
- `compare.control: match`

release `examples/showcase.yu`:

- `continuation_resume_marker_result_direct_tail_provider_boundary_pair: 23995`
- `resume_marker_permission_native_candidates: 13448`
- `resume_marker_permission_native_reject_handler_path: 10547`
- `resume_marker_permission_native_reject_value_result: 7703`
- `resume_marker_permission_native_reject_legacy_signal: 28654`
- `resume_marker_permission_native_reject_other_transform: 8`
- `resume_marker_permission_native_reject_resume_delta: 0`
- `resume_marker_permission_native_reject_legacy_bridge: 0`
- `compare.control: match`

読み:

- permission-native boundary の候補は代表 workload に十分ある。
  `MarkerFrame` 置換が刺さる可能性は残っている。
- reject の太い理由は handler path、value result、legacy signal。
  `resume_delta` と legacy bridge は代表 workload では支配項ではない。
- 次に実装するなら、候補だけを対象にした `PermissionBoundary` surface を並行表現として置き、
  value / legacy signal / handler path は当面 `MarkerFrame` fallback に残すのが安全。

確認:

- `cargo fmt --all --check`
- `cargo check -q -p evidence-vm -p yulang`
- `cargo check -q --release -p evidence-vm -p yulang`
- `cargo test -q -p evidence-vm -- --test-threads=1`
- `cargo test -q -p yulang runtime_evidence -- --test-threads=1`
- `tests/yulang/yulang-adversarial-corpus/probe.sh`
- release の `bench/nondet_20_discard.yu` と `examples/showcase.yu` は
  どちらも `compare.control: match`

## Evidence VM permission boundary segment surface

permission-native resume marker candidate を boolean で数えるだけでなく、
`EvidencePermissionBoundarySegment` と `EvidencePermissionBoundaryExposure` として表した。
現時点の exposure は `ProviderGuardBoundaryPair` のみ。

この変更では continuation frame はまだ増やしていない。
`record_resume_marker_permission_native_candidate` が provider guard-boundary permission を見つけたとき、
一度 `EvidencePermissionBoundarySegment` を作り、その surface を経由して candidate / handler-path reject を
数えるようにした。

狙い:

- 次に `PermissionBoundary` frame を入れるときの入力形を先に固定する。
- value result / legacy signal / handler path は引き続き `MarkerFrame` fallback に残す。
- 旧 full marker stack をまだ消さない。

確認:

- `cargo fmt --all --check`
- `cargo check -q -p evidence-vm -p yulang`
- `cargo check -q --release -p evidence-vm -p yulang`
- `cargo test -q -p evidence-vm -- --test-threads=1`
- `cargo test -q -p yulang runtime_evidence -- --test-threads=1`
- `tests/yulang/yulang-adversarial-corpus/probe.sh`
- release の `bench/nondet_20_discard.yu` と `examples/showcase.yu` は
  どちらも `compare.control: match`

## Evidence VM direct-tail permission boundary append candidates

`DirectTailResumptive` の continuation append が既存 `MarkerFrame` blocker で止まる地点を調べた。
最初は `continue_direct_tail_resumptive_with_continuation` が `MarkerFrame` を直接見る branch に
hook を置いたが、そこは代表 workload では 0 件だった。
実際の blocker は `call.continuation.then_counted(...)` の内側にあるため、
append 前に既存 continuation の append blocker を再帰的に見て分類する形へ変えた。

追加した stats:

- `direct_tail_permission_boundary_append_candidates`
- `direct_tail_permission_boundary_append_provider_pair`
- `direct_tail_permission_boundary_append_reject_no_provider_permission`
- `direct_tail_permission_boundary_append_reject_resume_delta`
- `direct_tail_permission_boundary_append_reject_handler_path`
- `direct_tail_permission_boundary_append_reject_legacy_bridge`
- `direct_tail_permission_boundary_append_reject_other_transform`

release `bench/nondet_20_discard.yu`:

- `direct_tail_continuation_append_blocked_by_marker_frame: 15120`
- `direct_tail_permission_boundary_append_provider_pair: 15120`
- `direct_tail_permission_boundary_append_candidates: 6090`
- `direct_tail_permission_boundary_append_reject_handler_path: 9030`
- `compare.control: match`

release `examples/showcase.yu`:

- `direct_tail_continuation_append_blocked_by_marker_frame: 18680`
- `direct_tail_permission_boundary_append_provider_pair: 18646`
- `direct_tail_permission_boundary_append_candidates: 7134`
- `direct_tail_permission_boundary_append_reject_handler_path: 11512`
- `direct_tail_permission_boundary_append_reject_other_transform: 34`
- `compare.control: match`

読み:

- direct-tail continuation の `MarkerFrame` blocker は、代表 workload ではほぼ provider pair 由来。
- ただし handler path 付き blocker が太いので、全部を permission boundary に置き換えるのは危険。
- まずは handler path なしの candidate だけを対象にした `PermissionBoundary` frame を試す価値がある。

追加で試したが残さなかった案:

- `PermissionBoundary` frame を実際に attach し、resume 時は従来の `MarkerFrame` と同じ
  fallback 処理へ流す案。
- `compare.control` は一致したが、fallback が全部 marker frame へ戻るため単純に遅くなった。
  - `bench/nondet_20_discard.yu`: `runtime_evidence_execute` は約 30.7ms
  - `examples/showcase.yu`: `runtime_evidence_execute` は約 50.3ms
- したがって、`PermissionBoundary` frame を作るだけでは不十分。
  次にやるなら、resume の中で provider guard-boundary pair を request/marker fallback なしで
  実行できる条件を先に切る必要がある。

採用した小さい改善:

- direct-tail append 前に既存 continuation の `MarkerFrame` blocker を見ているため、
  marker blocker が確定した場合は `then_counted` に入らず、同じ `Then` を直接作るようにした。
- 同じ仕組みで `ProviderEnv` blocker も扱うようにし、既知 scope blocker では append 探索を
  一度で止めるようにした。
- これにより、観測のために行った blocker traversal と、`then_counted` 内の append traversal が
  二重にならない。

結果:

- release `bench/nondet_20_discard.yu`
  - `direct_tail_continuation_append_blocked_by_marker_frame: 15120`
  - `direct_tail_continuation_append_blocked_by_provider_env: 840`
  - `direct_tail_permission_boundary_append_candidates: 6090`
  - `timing.runtime_evidence_execute: 27.6ms`
  - `compare.control: match`
- release `examples/showcase.yu`
  - `direct_tail_continuation_append_blocked_by_marker_frame: 18680`
  - `direct_tail_continuation_append_blocked_by_provider_env: 1644`
  - `direct_tail_permission_boundary_append_candidates: 7134`
  - `timing.runtime_evidence_execute: 45.4ms`
  - `compare.control: match`

確認:

- `cargo fmt --all --check`
- `cargo check -q -p evidence-vm -p yulang`
- `cargo check -q --release -p evidence-vm -p yulang`
- `cargo test -q -p evidence-vm -- --test-threads=1`
- `cargo test -q -p yulang runtime_evidence -- --test-threads=1`
- `tests/yulang/yulang-adversarial-corpus/probe.sh`
- release の `bench/nondet_20_discard.yu` と `examples/showcase.yu` は
  どちらも `compare.control: match`

## Evidence VM provider AddId shortcut visible shadow

目的:

- `provider_permission_guarded_direct_tail_resumptive` の AddId shortcut は、
  full active marker scan を避けて単一 guard だけを signal hygiene に載せる。
- これまでの `provider_add_id_shortcut_full_scan_guard_visible_*` は guard vector
  だけを比較していたため、full scan 側に extra guard がある代表 workload では mismatch が多い。
- 実際に重要なのは catch arm から見た operation visibility なので、debug build 限定で
  full-scan hygiene snapshot を保持し、`visible_operation_resumptive_parts` の結果を比較する。

実装:

- `EvidenceSignalHygiene` に debug-only の `EvidenceFullScanHygieneSnapshot` を持たせた。
- AddId shortcut の検証上限内では、従来の full active marker scan hygiene を snapshot として保存する。
- provider guard-boundary permission の debug verify 時に、shortcut hygiene の visible 結果と
  full-scan snapshot の visible 結果を比較する。
- release build では snapshot field も visible shadow も消える。

debug `examples/showcase.yu`:

- `provider_add_id_shortcut_used: 829`
- `provider_add_id_shortcut_full_scan_guard_visible_match: 3`
- `provider_add_id_shortcut_full_scan_guard_visible_mismatch: 826`
- `provider_add_id_shortcut_visible_verify: 829`
- `provider_add_id_shortcut_visible_match: 829`
- `provider_add_id_shortcut_visible_mismatch: 0`
- `provider_add_id_shortcut_full_scan_visible: 829`
- `compare.control: match`

debug `bench/nondet_20_discard.yu`:

- default stack では debug binary が stack overflow した。
- `ulimit -s 65532` を付けると実行できる。
- `provider_add_id_shortcut_used: 420`
- `provider_add_id_shortcut_full_scan_guard_visible_match: 1`
- `provider_add_id_shortcut_full_scan_guard_visible_mismatch: 419`
- `provider_add_id_shortcut_visible_verify: 420`
- `provider_add_id_shortcut_visible_match: 420`
- `provider_add_id_shortcut_visible_mismatch: 0`
- `provider_add_id_shortcut_full_scan_visible: 420`
- `compare.control: match`

release:

- `bench/nondet_20_discard.yu`
  - `provider_add_id_shortcut_used: 420`
  - `provider_add_id_shortcut_visible_verify: 0`
  - `runtime_evidence_execute: 28.3ms`
  - `compare.control: match`
- `examples/showcase.yu`
  - `provider_add_id_shortcut_used: 829`
  - `provider_add_id_shortcut_visible_verify: 0`
  - `runtime_evidence_execute: 48.1ms`
  - `compare.control: match`

読み:

- guard vector だけを見ると full scan と shortcut は大きく drift する。
- しかし代表 workload では、catch-visible な結果は shortcut と full scan で一致する。
- したがって、AddId shortcut の妥当性確認は guard vector 完全一致ではなく、
  visible equivalence を見る方が適切。
- debug `nondet_20_discard` の stack overflow は、今回の release hot path には影響しないが、
  debug verify が深い再帰に弱いことは別途残る。

確認:

- `cargo fmt --all --check`
- `cargo check -q -p evidence-vm -p yulang`
- `cargo check -q --release -p evidence-vm -p yulang`
- `cargo build -q -p yulang`
- `cargo build -q --release -p yulang`
- `cargo test -q -p evidence-vm -- --test-threads=1`
- `cargo test -q -p yulang runtime_evidence -- --test-threads=1`
- `tests/yulang/yulang-adversarial-corpus/probe.sh`

## Evidence VM resume marker provider-pair close shadow

目的:

- `PermissionBoundary` frame を新しく積む案は、resume 時に marker fallback へ戻るだけなら遅くなる。
- 次は frame を増やすのではなく、既存 `MarkerFrame` close の中で
  `DirectTailResumptive + provider grant + guard-boundary pair` を permission-native に
  close できる狭い条件を調べる。
- close 時点には catch arms が無いので、実行は変えず、provider pair candidate を signal hygiene に
  debug-only で印付けし、後段の catch-visible 判定で legacy と permission-native を比較する。

実装:

- `resume_marker_provider_pair_close_*` stats を追加した。
- `close_marker_direct_tail` で handler boundary を適用した後の direct-tail hygiene を分類する。
- provider guard-boundary pair かつ handler path なし、carry-after-frame AddId なしのものを candidate とした。
- debug build では candidate に `EvidenceResumeMarkerCloseShadow` を持たせ、
  `visible_direct_tail_resumptive` の verify block で legacy visible と permission visible を比較する。
- release build では candidate/reject stats だけを記録し、visible shadow は持たない。

debug `bench/nondet_20_discard.yu` (`ulimit -s 65532`):

- `resume_marker_provider_pair_close_candidates: 14280`
- `resume_marker_provider_pair_close_shadow: 420`
- `resume_marker_provider_pair_close_shadow_match: 420`
- `resume_marker_provider_pair_close_shadow_mismatch: 0`
- `resume_marker_provider_pair_close_shadow_legacy_visible: 420`
- `resume_marker_provider_pair_close_shadow_permission_visible: 420`
- `resume_marker_provider_pair_close_reject_handler_path: 9450`
- `resume_marker_provider_pair_close_reject_other_transform: 420`
- `compare.control: match`

debug `examples/showcase.yu`:

- `resume_marker_provider_pair_close_candidates: 17002`
- `resume_marker_provider_pair_close_shadow: 829`
- `resume_marker_provider_pair_close_shadow_match: 829`
- `resume_marker_provider_pair_close_shadow_mismatch: 0`
- `resume_marker_provider_pair_close_shadow_legacy_visible: 829`
- `resume_marker_provider_pair_close_shadow_permission_visible: 829`
- `resume_marker_provider_pair_close_reject_handler_path: 12341`
- `resume_marker_provider_pair_close_reject_other_transform: 856`
- `compare.control: match`

release:

- `bench/nondet_20_discard.yu`
  - `resume_marker_provider_pair_close_candidates: 14280`
  - `resume_marker_provider_pair_close_shadow: 0`
  - `runtime_evidence_execute: 27.2ms`
  - `compare.control: match`
- `examples/showcase.yu`
  - `resume_marker_provider_pair_close_candidates: 17002`
  - `resume_marker_provider_pair_close_shadow: 0`
  - `runtime_evidence_execute: 46.0ms`
  - `compare.control: match`

読み:

- handler path なしの close candidate は代表 workload でかなり太い。
- ただし catch-visible shadow として実際に後段で踏まれる数は、provider AddId shortcut の
  operation 数と同程度に見える。
- debug shadow では mismatch 0 なので、少なくとも catch-visible の意味では
  provider pair close を permission-native に寄せる余地がある。
- handler path 付き reject はまだ太いので、ここは引き続き触らない。

確認:

- `cargo fmt --all --check`
- `cargo check -q -p evidence-vm -p yulang`
- `cargo check -q --release -p evidence-vm -p yulang`
- `cargo build -q -p yulang`
- `cargo build -q --release -p yulang`
- `cargo test -q -p evidence-vm -- --test-threads=1`
- `cargo test -q -p yulang runtime_evidence -- --test-threads=1`
- `tests/yulang/yulang-adversarial-corpus/probe.sh`

## Evidence VM provider-pair marker close native path

変更:

- `close_marker_direct_tail` で provider guard-boundary pair の candidate になった場合、
  legacy の `close_marker_frame` に流さず、その direct-tail call をそのまま返すようにした。
- 条件は前節の shadow と同じ:
  - handler boundary 適用後に provider guard-boundary pair である
  - resume marker plan に handler path が無い
  - close 対象 marker に carry-after-frame AddId が無い
- handler path 付き、carry-after-frame、legacy bridge、resume delta、その他 transform は従来通り fallback。

結果:

- release `bench/nondet_20_discard.yu`
  - `resume_marker_provider_pair_close_candidates: 1721`
  - `resume_marker_provider_pair_close_reject_handler_path: 9450`
  - `resume_marker_provider_pair_close_reject_other_transform: 420`
  - `runtime_evidence_execute: 22.0ms`
  - `compare.control: match`
- release `examples/showcase.yu`
  - `resume_marker_provider_pair_close_candidates: 3554`
  - `resume_marker_provider_pair_close_reject_handler_path: 12341`
  - `resume_marker_provider_pair_close_reject_other_transform: 856`
  - `runtime_evidence_execute: 40.2ms`
  - `compare.control: match`
- debug `bench/nondet_20_discard.yu` (`ulimit -s 65532`)
  - `resume_marker_provider_pair_close_shadow: 420`
  - `resume_marker_provider_pair_close_shadow_match: 420`
  - `resume_marker_provider_pair_close_shadow_mismatch: 0`
  - `compare.control: match`
- debug `examples/showcase.yu`
  - `resume_marker_provider_pair_close_shadow: 829`
  - `resume_marker_provider_pair_close_shadow_match: 829`
  - `resume_marker_provider_pair_close_shadow_mismatch: 0`
  - `compare.control: match`

読み:

- 単に `PermissionBoundary` frame を足すのではなく、既存 `MarkerFrame` close の狭い branch を
  permission-native にした方が速い。
- native close により、後続の同じ close 候補が減るため、前節の shadow-only 時より
  `resume_marker_provider_pair_close_candidates` は小さく出る。
- handler path 付き reject はまだ太く、ここは意味論を別に固めるまで触らない。

確認:

- `cargo fmt --all --check`
- `cargo check -q -p evidence-vm -p yulang`
- `cargo check -q --release -p evidence-vm -p yulang`
- `cargo build -q -p yulang`
- `cargo build -q --release -p yulang`
- `cargo test -q -p evidence-vm -- --test-threads=1`
- `cargo test -q -p yulang runtime_evidence -- --test-threads=1`
- `tests/yulang/yulang-adversarial-corpus/probe.sh`

## Evidence VM provider marker close cert

目的:

- `record_resume_marker_provider_pair_close_candidate(...) -> bool` は、stats 記録と実行判定が
  名前の上で混ざっていた。
- provider marker close native path を広げる前に、native close 条件を cert と decision として
  明示する。

変更:

- `EvidenceMarkerCloseDecision::{NativeProviderPair, Legacy}` を追加した。
- `EvidenceProviderMarkerCloseCert` を追加し、provider pair close の native path は cert を経由して
  判定するようにした。
- reject reason を `EvidenceProviderMarkerCloseReject` に分け、stats 更新は
  `record_provider_marker_close_reject` / `record_provider_marker_close_candidate` に寄せた。
- native close が成立する条件を `provider_marker_close_decision` のコメントとして固定した。

確認:

- `cargo fmt --all`
- `cargo check -q -p evidence-vm -p yulang`
- `cargo check -q --release -p evidence-vm -p yulang`
- release `bench/nondet_20_discard.yu`
  - `resume_marker_provider_pair_close_candidates: 1721`
  - `runtime_evidence_execute: 21.9ms`
  - `compare.control: match`
- debug `examples/showcase.yu`
  - `resume_marker_provider_pair_close_shadow: 829`
  - `resume_marker_provider_pair_close_shadow_mismatch: 0`
  - `compare.control: match`

## Evidence VM provider marker close hit/fallback stats

目的:

- `resume_marker_provider_pair_close_candidates` だけでは、native path に入った回数と
  legacy fallback の総数が読みづらい。
- native close の効果を見るため、hit と fallback を明示的に分ける。

変更:

- `resume_marker_provider_pair_close_native_hits` を追加した。
- `resume_marker_provider_pair_close_legacy_fallbacks` を追加した。
- native hit は provider pair close candidate と同時に増やし、legacy fallback は reject reason と同時に増やす。

結果:

- release `bench/nondet_20_discard.yu`
  - `resume_marker_provider_pair_close_candidates: 1721`
  - `resume_marker_provider_pair_close_native_hits: 1721`
  - `resume_marker_provider_pair_close_legacy_fallbacks: 9870`
  - `runtime_evidence_execute: 20.8ms`
  - `compare.control: match`
- release `examples/showcase.yu`
  - `resume_marker_provider_pair_close_candidates: 3554`
  - `resume_marker_provider_pair_close_native_hits: 3554`
  - `resume_marker_provider_pair_close_legacy_fallbacks: 13197`
  - `runtime_evidence_execute: 38.2ms`
  - `compare.control: match`

確認:

- `cargo fmt --all --check`
- `cargo check -q -p evidence-vm -p yulang`
- `cargo check -q --release -p evidence-vm -p yulang`
- `cargo test -q -p evidence-vm -- --test-threads=1`
- `tests/yulang/yulang-adversarial-corpus/probe.sh`

## Evidence VM provider marker close handler-path fallback profile

目的:

- provider marker close native path で残る fallback の主成分は handler path 付き。
- ここはまだ最適化しないが、次に意味論を考えるために blocked/unblocked と
  same-family/foreign-family を測る。

変更:

- `resume_marker_provider_pair_close_reject_handler_path_blocked` を追加した。
- `resume_marker_provider_pair_close_reject_handler_path_unblocked` を追加した。
- `resume_marker_provider_pair_close_reject_handler_path_no_boundary` を追加した。
- `resume_marker_provider_pair_close_reject_handler_path_same_family` を追加した。
- `resume_marker_provider_pair_close_reject_handler_path_foreign_family` を追加した。
- same-family は `handler_path` が operation path の prefix である場合として数える。

結果:

- release `bench/nondet_20_discard.yu`
  - `resume_marker_provider_pair_close_reject_handler_path: 9450`
  - `resume_marker_provider_pair_close_reject_handler_path_blocked: 0`
  - `resume_marker_provider_pair_close_reject_handler_path_unblocked: 9450`
  - `resume_marker_provider_pair_close_reject_handler_path_no_boundary: 0`
  - `resume_marker_provider_pair_close_reject_handler_path_same_family: 8610`
  - `resume_marker_provider_pair_close_reject_handler_path_foreign_family: 840`
  - `compare.control: match`
- release `examples/showcase.yu`
  - `resume_marker_provider_pair_close_reject_handler_path: 12341`
  - `resume_marker_provider_pair_close_reject_handler_path_blocked: 0`
  - `resume_marker_provider_pair_close_reject_handler_path_unblocked: 12341`
  - `resume_marker_provider_pair_close_reject_handler_path_no_boundary: 0`
  - `resume_marker_provider_pair_close_reject_handler_path_same_family: 10697`
  - `resume_marker_provider_pair_close_reject_handler_path_foreign_family: 1644`
  - `compare.control: match`

読み:

- handler path 付き fallback は代表 workload では blocked ではない。
- ただし same-family が太いので、ここを触るなら handler capability と boundary exposure を
  permission-native に対応させる必要がある。
- foreign-family は `ProviderEnv` blocker 由来の数に近く、別経路として扱う方がよさそう。

確認:

- `cargo fmt --all --check`
- `cargo check -q -p evidence-vm -p yulang`
- `cargo check -q --release -p evidence-vm -p yulang`
- `cargo test -q -p evidence-vm -- --test-threads=1`
- `tests/yulang/yulang-adversarial-corpus/probe.sh`

## Evidence VM provider marker close handler-path identity profile

目的:

- handler-path fallback をすぐ native 化せず、まず boundary identity を測る。
- provider permission の handler id は plan handler id で、runtime の `EvidenceGuardId` とは
  まだ直接対応していない。
- そのため、現段階では handler family と call boundary の対応を測り、ID 対応は unknown として記録する。

変更:

- `RuntimeEvidenceRunContext::provider_grant_permission_family` を追加した。
- handler-path reject に以下の stats を追加した。
  - call boundary path との一致/不一致
  - provider permission handler family との exact 一致/不一致/unknown
  - boundary id と permission handler id の一致/不一致/unknown
- 実行意味論は変えていない。

結果:

- release `bench/nondet_20_discard.yu`
  - `handler_path: 9450`
  - `matches_call_boundary: 9450`
  - `differs_call_boundary: 0`
  - `matches_allowed_handler_family: 0`
  - `differs_allowed_handler_family: 9450`
  - `permission_handler_unknown: 0`
  - `boundary_id_permission_handler_unknown: 9450`
  - `compare.control: match`
- release `examples/showcase.yu`
  - `handler_path: 12341`
  - `matches_call_boundary: 12341`
  - `differs_call_boundary: 0`
  - `matches_allowed_handler_family: 0`
  - `differs_allowed_handler_family: 12341`
  - `permission_handler_unknown: 0`
  - `boundary_id_permission_handler_unknown: 12341`
  - `compare.control: match`

読み:

- handler-path fallback は、代表 workload では常に call boundary path と一致する。
- 一方で provider permission の handler family とは exact には一致しない。
- これは handler path が allowed handler family そのものではなく、その prefix boundary を表していることを示す。
- runtime guard id と plan handler id の対応表がないため、ID レベルの same-handler 判定はまだできない。
- 次に進むなら、handler marker / active handler frame に plan handler id を運ぶ surface を作るか、
  prefix boundary と provider permission family の関係を permission exposure として定義する必要がある。

確認:

- `cargo fmt --all --check`
- `cargo check -q -p evidence-vm -p yulang`
- `cargo check -q --release -p evidence-vm -p yulang`
- `cargo test -q -p evidence-vm -- --test-threads=1`
- `cargo test -q -p yulang runtime_evidence -- --test-threads=1`
- `tests/yulang/yulang-adversarial-corpus/probe.sh`

## Evidence VM provider marker close permission-family prefix profile

目的:

- 前節で、handler path は provider permission handler family と exact には一致しないことが分かった。
- ただし same-family count は太いので、handler path と permission family の prefix 関係を明示して確認する。

変更:

- `resume_marker_provider_pair_close_reject_handler_path_prefixes_allowed_handler_family` を追加した。
- `resume_marker_provider_pair_close_reject_handler_path_allowed_handler_family_prefixes` を追加した。
- 実行意味論は変えていない。

結果:

- release `bench/nondet_20_discard.yu`
  - `same_family: 8610`
  - `foreign_family: 840`
  - `matches_allowed_handler_family: 0`
  - `differs_allowed_handler_family: 9450`
  - `handler_path_prefixes_allowed_handler_family: 8610`
  - `allowed_handler_family_prefixes_handler_path: 0`
  - `compare.control: match`
- release `examples/showcase.yu`
  - `same_family: 10697`
  - `foreign_family: 1644`
  - `matches_allowed_handler_family: 0`
  - `differs_allowed_handler_family: 12341`
  - `handler_path_prefixes_allowed_handler_family: 10697`
  - `allowed_handler_family_prefixes_handler_path: 0`
  - `compare.control: match`

読み:

- same-family fallback は、handler path が permission handler family の prefix になっているケースと一致する。
- つまり native 化の次候補は「同じ handler id」ではなく、
  「provider permission の handler family を含む prefix boundary」をどう permission exposure へ落とすか。
- ID 対応は依然 unknown なので、handler object id surface は別途必要。

確認:

- `cargo fmt --all --check`
- `cargo check -q -p evidence-vm -p yulang`
- `cargo check -q --release -p evidence-vm -p yulang`
- `cargo test -q -p evidence-vm -- --test-threads=1`
- `cargo test -q -p yulang runtime_evidence -- --test-threads=1`
- `tests/yulang/yulang-adversarial-corpus/probe.sh`

## Evidence VM provider prefix boundary shadow

目的:

- handler-path fallback の same-family 部分は、`handler_path` が provider permission family の
  prefix であることが分かった。
- すぐ実行変更せず、`ProviderPrefixBoundaryExposure` を shadow-only で持たせ、
  legacy visible と permission-visible が一致するかを確認する。

candidate 条件:

- provider guard-boundary pair である。
- `handler_path` が存在する。
- `handler_boundary` が存在し、blocked ではない。
- `handler_boundary.path == handler_path`。
- `handler_path` が permission family の真の prefix。
- close 対象 marker に carry-after-frame AddId が無い。

変更:

- debug-only の `EvidenceProviderPrefixBoundaryExposure` を追加した。
- `EvidenceSignalHygiene` に debug-only の prefix boundary close shadow を追加した。
- prefix boundary candidate / reject / shadow stats を追加した。
- permission family と request path の関係も併せて測るようにした。
- 実行意味論は変えていない。

結果:

- release `bench/nondet_20_discard.yu`
  - `resume_marker_provider_prefix_boundary_candidates: 8610`
  - `resume_marker_provider_prefix_boundary_reject_foreign_family: 840`
  - `resume_marker_provider_prefix_boundary_permission_family_equals_request_path: 9450`
  - `resume_marker_provider_prefix_boundary_permission_family_prefixes_request_path: 9450`
  - `resume_marker_provider_prefix_boundary_request_path_prefixes_permission_family: 9450`
  - `compare.control: match`
- debug `bench/nondet_20_discard.yu` (`ulimit -s 65532`)
  - `resume_marker_provider_prefix_boundary_shadow: 420`
  - `resume_marker_provider_prefix_boundary_shadow_match: 420`
  - `resume_marker_provider_prefix_boundary_shadow_mismatch: 0`
  - `compare.control: match`
- release `examples/showcase.yu`
  - `resume_marker_provider_prefix_boundary_candidates: 10697`
  - `resume_marker_provider_prefix_boundary_reject_foreign_family: 1644`
  - `resume_marker_provider_prefix_boundary_permission_family_equals_request_path: 12341`
  - `resume_marker_provider_prefix_boundary_permission_family_prefixes_request_path: 12341`
  - `resume_marker_provider_prefix_boundary_request_path_prefixes_permission_family: 12341`
  - `compare.control: match`
- debug `examples/showcase.yu`
  - `resume_marker_provider_prefix_boundary_shadow: 829`
  - `resume_marker_provider_prefix_boundary_shadow_match: 829`
  - `resume_marker_provider_prefix_boundary_shadow_mismatch: 0`
  - `compare.control: match`

読み:

- provider permission family は、代表 workload では request path と exact に一致する。
- same-family handler-path fallback は、その request/permission family を包む prefix boundary として見える。
- prefix boundary shadow は代表 workload で mismatch 0 なので、次に native close 条件へ入れる候補になる。
- ただし handler path なし native close より意味論が重いため、実行変更は別 commit に分ける。

確認:

- `cargo fmt --all --check`
- `cargo check -q -p evidence-vm -p yulang`
- `cargo check -q --release -p evidence-vm -p yulang`
- `cargo test -q -p evidence-vm -- --test-threads=1`
- `cargo test -q -p yulang runtime_evidence -- --test-threads=1`
- `tests/yulang/yulang-adversarial-corpus/probe.sh`

<!-- daily-append-anchor: 2026-06-28 -->
