# 2026-09-19

## Gate 3 first HIR operator-chain association — implemented

The approved first `yu-hir` slice is implemented in `crates/yu-hir`. It adds
the `yu-syntax -> yu-hir` dependency and exposes `AssociatedChains`: a minimal
pre-HIR product of owned, range-preserving associated expressions. It does not
introduce `HirModule`, declarations, names, `DefId`s, types, diagnostics, or a
`yu-types` dependency.

`associate_operator_chains(&ParsedFile)` reads the exact immutable operator
table retained by parsing through the new read-only `OperatorTable` definition
view. It handles prefix, infix, suffix and nullfix roles under one association
authority; fixed postfix/ML continuations; annotation and terminal-tail
barriers; nested chains; and recovery `Missing`, raw `Error`, and structured
`Invalid` facts without changing the Rowan CST or reissuing parser diagnostics.

### Ownership amendment

A performance audit found that retaining every chain in a flat result while
also embedding owned nested results could make valid deeply nested input retain
quadratic duplicate trees. The user selected top-level-only `AssociatedChains`.
The authoritative decision is recorded in
`notes/design/2026-09-19-hir-associated-chains-ownership-amendment.md`: nested
chains are associated once and retained only in the enclosing owned `HirExpr`.

### Verification and review

- `RUSTC_WRAPPER= cargo test -p yu-hir`: 10 unit tests and doc tests pass.
- `RUSTC_WRAPPER= cargo test -p yu-syntax --lib operator_table`: 9 focused
  tests pass.
- `RUSTC_WRAPPER= cargo check -p yu-hir`, `cargo fmt --check`, and `git diff
  --check` pass.
- M2 compiler-referee review established the repair boundary for total
  CST-to-operand projection. Final independent specification delta review found
  no blocking, major, or minor findings.
- No timing experiment ran: the ownership risk was settled by static complexity
  analysis, and the selected topology removes the duplicate owned-tree path.

Existing unrelated `yu-syntax` warnings remain in
`declaration_companion.rs`, `operator_chain.rs`, and `cursor.rs`. Workspace-wide
verification was deferred: this Gate 3 slice received focused package checks
only.

## syntax-v0 content-model reference

The bilingual `syntax-reference` now has a syntax-v0 content-model entrypoint.
It separates accepted source placement from direct Rowan CST placement and
recovery, inventories root/nested Statement placement, declarations,
expressions, patterns, types, literals, declaration companions, and derives
attachments, and identifies Authoritative sources rather than treating code or
fixtures as normative.

Both books build with `mdbook build syntax-reference/en` and
`mdbook build syntax-reference/ja`; `git diff --check -- syntax-reference`
passes. M3 specification review required three repair rounds; the user
authorized one final narrow closure review for the Expression `RuleExpression`
row, which found no blocking or major finding. `tasks/current.md` was not
updated because it has concurrent HIR-task edits; this daily entry avoids that
overlap.
