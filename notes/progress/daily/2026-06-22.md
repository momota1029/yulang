# 2026-06-22

## Directed stack weight proof

- Read `research/effect-mini-language/directed_stack_weight_principal_letrec_colored_soundness_ja.tex`.
- The colored soundness draft makes the implementation target sharper:
  - left and right weights are distinct structures;
  - row split consumes only `J = K ∩ Common(L)`;
  - `protect` is `PWeight(take(Empty), rho)`;
  - filter is a static check and has no runtime marker;
  - soundness is for normalized, well-bracketed colored marker elaboration.
- Wrote implementation notes in `notes/design/2026-06-22-directed-stack-weight-implementation.md`.
- Immediate implementation risk:
  `crates/infer/src/constraints/row_effect.rs` combines `weights.left.compose(&weights.right)` before row split,
  which conflicts with the directed pair interpretation.
- Started implementation slice 1 by adding `crates/infer/src/constraints/directed_weight.rs`.
  This module is not wired into the solver yet; it fixes the new same-id monoid and W-Mix behavior with unit tests.
- Ported `crates/infer/src/constraints/row_effect.rs` to the directed row split contract:
  - no left/right composition before row-head subtraction;
  - row head uses `Common(L)` from left active stack entries only;
  - filters are checked and erased before residual propagation;
  - right-side pop is carried to the residual tail and mixed only after split;
  - row residual tests no longer expect legacy `floor` residuals.
- Moved W-Mix normalization into `ConstraintWeights` replay composition:
  - bound replay now composes weights and applies directed W-Mix while keeping exact pop counters;
  - var-var replay uses the same exact-counter W-Mix path, while non-empty left pop-only alias replay is keyed by `(lower, upper, pop ids, right weight)` so the same pair is not rewalked just to grow pop counts;
  - the old `pop(n) -> pop(1)` alias caps were removed;
  - `row_effect.rs` now reuses the shared normalization instead of a local helper.
- Updated `Neg::Stack` absorption so filter is checked and then erased instead of being stored as a right weight.
- Split `ConstraintWeights.right` to a dedicated `RightStackWeight`:
  - right weights now structurally store only pure pops;
  - compact collection materializes right pop weights explicitly with `to_stack_weight()`;
  - alias-neutral and row-tail-boundary checks no longer treat right weights as carrying family budgets;
  - tests no longer construct impossible right push/floor/filter weights.
- Moved left-side filter handling out of stored bound weights:
  - lower-bound insertion checks the weighted lower type and erases the left filter before storing;
  - upper and var-var bound insertion register the filter on the source/lower variable, then store filter-free weights;
  - replay and compact paths no longer see left filters that have already crossed a variable boundary.
- Isolated legacy floor from active-family extraction:
  - added `StackWeight::active_stack_items()` for currently visible push families;
  - replaced the remaining `stack_items()` callers in inference with active-push reads;
  - removed the old mixed helper so floor is not accidentally treated as an active family.
- Switched replay W-Mix to the directed weight implementation:
  - `ConstraintWeights::normalize_directed_mix()` uses `LeftStackWeight` and `RightStackWeight` for W-Mix;
  - legacy floor entries no longer absorb right pops;
  - added a regression test for right pop plus legacy floor.
- Migrated stored left bound weights to directed left storage:
  - added `LeftConstraintWeight`, which carries `LeftStackWeight` plus a temporary filter check;
  - `ConstraintWeights.left` no longer stores legacy `StackWeight` or `floor`;
  - row split and replay operate on directed left entries directly;
  - compact/generalize/test helper boundaries materialize directed left weights back to `StackWeight` only when needed.
- Aligned cleanup liveness with the colored proof:
  - live stack ids are now collected from covariant active pushes only;
  - bare legacy floor entries no longer keep an id live;
  - added a regression for non-empty bare floor cleanup.
- Confirmed:
  - `cargo fmt --check`
  - `cargo test -q -p infer constraints::directed_weight -- --test-threads=1`
  - `cargo test -q -p infer constraints::tests::case_01 -- --test-threads=1`
  - `cargo test -q -p infer constraints::tests::case_02 -- --test-threads=1`
  - `cargo test -q -p infer generalize::tests::cleanup -- --test-threads=1`
  - `timeout 180s cargo test -q -p infer -- --test-threads=1`
  - `cargo test -q -p infer compact::tests::case_01 -- --test-threads=1`
  - `cargo test -q -p poly -- --test-threads=1`
  - `timeout 180s cargo test -q -p infer -- --test-threads=1`
  - `timeout 240s cargo test -q -p yulang -- --test-threads=1`

## Guard entry-except snapshot

- Updated `directed_stack_weight_principal_letrec_colored_soundness_ja.tex` so guard coloring no longer uses the
  current `visible(kappa, Phi)` predicate.
- Each guard activation stores the active color set at scope entry as an entry-except snapshot.
  `guard(c, H, -)` adds `c` for `h notin H` only when the request color set is disjoint from that snapshot.
- This handles both nesting directions:
  - an outer marker still colors a request that is only hidden by a later inner marker;
  - an inner marker does not repaint a request that was already hidden before the inner marker started.
- Updated `control-vm` and `mono-runtime` active add-id marker state to carry the entry frame prefix.
- Confirmed:
  - `cargo fmt --check`
  - `cargo test -q -p control-vm marker -- --test-threads=1`
  - `cargo test -q -p mono-runtime marker -- --test-threads=1`
  - `cargo test -q -p control-vm -- --test-threads=1`
  - `cargo test -q -p mono-runtime -- --test-threads=1`
  - `timeout 240s cargo test -q -p yulang -- --test-threads=1`

## Public spec/docs refresh

- Rewrote `spec/2026-05-31-effect-variable-subtractable.md` around the directed stack weight model:
  - left/right weights are separate structures;
  - row split consumes `J = K ∩ Common(L)` from active left pushes only;
  - right pop is carried as `NWeight` on the residual tail; ordinary W-Mix happens later through wrapper absorption/propagation;
  - `protect` is represented as `take(Empty)`;
  - filter is a static check and is erased after bound insertion;
  - `floor` is not an active family; the termination-note ambient/residual floor is a separate static measure.
- Updated `web/docs/reference/type-theory.md` and `web/docs/ja/reference/type-theory.md` so public docs no longer explain
  effect inference as `W = L + R` or floor-backed row subtraction.
- Updated `spec/2026-06-13-runtime-guard-markers.md` with the entry-except snapshot rule for `add_id` coloring.
- Updated `spec/README.md` to point to the directed stack weight version of the effect subtraction spec.
- Re-read `research/effect-mini-language/directed_stack_weight_principal_letrec_colored_soundness_ja.tex` and
  `research/effect-mini-language/directed_weight_row_solver_termination_ja.tex`, then corrected the docs/spec refresh:
  - omitted effect-only slots are wildcard-like, not "no budget";
  - right pop is represented as `NWeight(R, beta)` on the tail during row split;
  - the termination-note ambient/residual floor is a separate static measure, not a colored-soundness active family.
- Restored the large worked-example part of `spec/2026-05-31-effect-variable-subtractable.md` in directed form:
  shallow vs recursive/deep, `outer/local/repeat`, `m`/`j`, `AllExcept(ref_update _)` / `ref::update`,
  and one-layer compact variable expansion.
- Confirmed:
  - `npm run build` in `web/docs`
  - `git diff --check`
