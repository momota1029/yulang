# 2026-09-01

## Declaration companion Gate 8 Enum/Error-owner closure

Gate 8 wires Enum and Error at their shared Header and actual-complete braced trailing positions.
Both owners accept a typed Header/Trailing derives tail before an exact companion handoff. Enum also
maps the typed equals-inline Variant tail to its singular companion field and direct
`DeclarationCompanion` child. Error deliberately discards that equals-inline tail and returns the
original gap plus exact `with` to its outer Statement owner; predecessor Variant and FromType
recovery remains local and is not duplicated.

The new operator-aware Enum/Error AST and direct entrypoints pass the caller's existing operator
table into now-reachable companion Canonical Statements. Their existing tableless wrappers remain
empty-table compatibility entries, while only direct-root and canonical-Statement callers that
already hold a table use the new entrypoints.

M2 verification remained focused:

- `grammar::declaration::tests::gate8_enum_and_error_owners_preserve_equals_inline_distinction`:
  1 passed, 0 failed, 576 filtered;
- scoped `rustfmt --edition 2024 --check` and `git diff --check`: passed;
- compiler and specification delta reviews: approved;
- no package/workspace suite or timing run.

The table covers both owner families across Header, Header derives, actual-close trailing, trailing
derives, all equals-inline handoffs, rejected semicolon/layout/close forms, exact predecessor
recovery roles and ranges, and direct owner-child topology. Two registered `+!` infix rows prove
that the table reaches a companion Canonical Binding in both AST and direct CST; its committed cut
is the pre-existing canonical infix behavior. Gate 8 is complete. Gate 9 Act owner integration is
next.

## Declaration companion Gate 9 Act-owner closure

Gate 9 wires only Act's post-Head and post-Source owner positions. The existing typed Act episode
tails and Header derives handoff are consumed by the Act AST and direct-CST cores; an accepted
companion terminates the declaration before Source/body classification. Consequently `act A with {}
= B with {}` owns only the first companion and returns the first `=` onward to the outer Statement.

Trailing derives after an actual Act braced close intentionally continues through the ordinary
derives driver. The focused `act A {} derives Eq with: item;tail` row therefore proves that `with`
remains a raw identifier in the one RoleReference Type ML application, not a `WithKw` or a second
companion. It also pins the outer role range, nested TypeExpression CST topology, and the retained
`: item;tail` suffix.

M2 verification remained focused:

- `grammar::declaration::tests::gate9_act_owner_wires_terminating_companions`: 1 passed, 0 failed,
  577 filtered;
- scoped `rustfmt --edition 2024 --check` and `git diff --check`: passed;
- compiler and specification reviews: approved;
- no package/workspace suite or timing run.

The table covers Head/Source and Header-derives handoffs, fresh Missing Head/Source rows, nested
`with` suspension, one-companion termination, semicolon and post-body rejection, registered
operator propagation, AST/direct recovery/CST cardinality, and owner-range parity. Gate 9 is
complete. Gate 10 atomic public/final integration is next.

## Declaration companion Gate 10 public/final closure

Gate 10 completes the shared declaration-owned `with:` companion across all five owners.  Its
public-root matrix covers the 12 authorized positions: Type Header/Equality, Struct Header and
actual-close trailing, Enum Header/trailing/equals-inline, Error Header/trailing/equals-inline
outer yield, and Act post-Head/post-Source.  Each row checks AST, `parse_file`, and direct-root
range/remainder parity; losslessness; direct owner-child and `WithKw` topology; derives ordering;
and recovery kind/role/range/source order.  The Error equals-inline rows retain exact `with` for
the outer Statement recovery rather than creating an Error companion.

The former deferred fixture `type box 'e 'a with:\n  struct self:` is now an authorized Type
Header companion.  It is asserted as an indented canonical Statement item containing a Struct
declaration; this recognizes no receiver/member semantics.  A final package failure also exposed
a real recovery defect in the shared companion-aware Derives Via scanner: it omitted the ordinary
owner-tail classifier and could consume an Act bodyless semicolon.  Delegating that scanner to its
originating `DerivesDriverSpec` restores owner retention; `act A derives Eq via ;` now has one
zero-width `Derives::ViaTarget` Missing recovery at byte 21 and an Act-owned bodyless semicolon.
The repair is recovery-only and does not add accepted-path loop/allocation work.

Verification was bounded to the final candidate:

- focused Act recovery fixture: 1 passed, 0 failed, 579 filtered;
- Gate 10 public matrix: 1 passed, 0 failed, 579 filtered;
- scoped rustfmt and `git diff --check`: passed;
- compiler, specification, and regression delta reviews: approved;
- final `cargo test -p yu-syntax`: 578 passed, 0 failed, 2 ignored; doc tests: 0;
- no workspace suite.

The deferred Gate 10 public production measurement is recorded in
`notes/progress/evidence/2026-09-01-gate10-public-companion-measurement.md` with raw output in
the paired `...-raw.md`.  It used a candidate-only 10k braced Struct companion kernel (eight
`parse_file` calls per process): one fresh warm-up and three measured samples, after two retained
invalid attempts, for six processes / 459.18 seconds within the 8-process / 10-minute budget.
The measured kernel median was 86.740094588 s; whole-process peak RSS median was 24,484 KiB.  No
baseline comparison is claimed because no earlier revision can parse an equivalent public route.

Gates 1–10 of the declaration companion addendum are complete; no Gate 11 is defined.
