# 2026-05-20

- Corrected the 20x20 measurement method back to generated native executable
  benchmarking. `target/debug/yulang run --native ...` includes CLI/JIT
  generation time and is not the right hot-path comparison for this work.
- Generated two effects executables from `target/yulang/bench_20x20_list_say.yu`:
  `target/yulang/bin/bench_20x20_owned_on_token` with owned perform
  specialization enabled, and `target/yulang/bin/bench_20x20_owned_off_token`
  with `YULANG_CPS_DISABLE_OWNED_PERFORM_SPECIALIZATION=1`.
- Runtime counter sanity check:
  owned-on reports `make_resumption=441` / `make_owned_resumption=420`;
  owned-off reports `make_resumption=861` / `make_owned_resumption=0`.
- Generated executable benchmark:
  `hyperfine --warmup 3 --runs 20 'target/yulang/bin/bench_20x20_owned_on_token >/dev/null' 'target/yulang/bin/bench_20x20_owned_off_token >/dev/null'`
  measured owned-on at `646.1 ms ± 35.3 ms` and owned-off at
  `636.4 ms ± 23.1 ms`. The owned token path deletes heap allocation for the
  owned resumption object, but it is not a speed win yet because the captured
  return-frame / handler snapshots are still built and replayed.
