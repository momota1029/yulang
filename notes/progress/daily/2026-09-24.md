# 2026-09-24

## F5c incoming route sampling continuation

Added the changed-post-reserve `DiagnosticDelta` failure witness in
`crates/yu-solver/src/tests/f5c_value_exact_upper_route.rs`. This route has
multiple completed capacity events, so the test now requires an earlier
`FreshValueBounds` growth and compares `DiagnosticDelta` aggregate totals and
peaks against the immediately preceding completed event snapshot. It also
checks successful event-sample completion, a fresh independent post-rollback
ledger, exact route checkpoint restoration, one conditional final sample,
failure consumption, and retry. The F5c working set was preserved in checkpoint
commit `bb1ec56c`.

The fresh M1 specification delta review is clean. It specifically confirmed
that the preceding event is real and that the target event's contribution is
not masked by earlier capacity growth. The focused sidecar has seven passing
tests. `cargo check -p yu-solver --tests`, `cargo fmt --all -- --check`, and
`git diff --check` pass. The last completed `yu-solver` library suite remains
202 passed. A 209-test single-threaded run was later interrupted after more
than six minutes in the F4 scale tests; before interruption,
`direct_root_n_and_2n_counters_remain_linear` was reported failed. Its isolated
rerun passed 1/1, but the cause remains unadjudicated; the full suite is not
certified by this checkpoint. No benchmark or resource
matrix was run; the prior eight-process sampler-cost budget remains consumed
without valid timing evidence.

This closes only the `DiagnosticDelta` witness. Remaining reachable diagnostic
scratch and error/diagnostic lanes still need reconciliation against the §3
failed-route boundary. The complete owner-to-route matrix, §3 gate, F5c, and
F5e remain open. The test/trace slice did not modify `lib.rs`, which remains
28,456 lines; keep the separate 63-line `ResourceSampleChecked` extraction
deferred until the accounting/measurement checkpoint closes. The code and
records are preserved in local checkpoint `bb1ec56c`; inspect the current
branch status for remote synchronization.

## F5c `Errors` changed-capacity route witness

Added `f5c_incoming_errors_growth_samples_after_first_union_member_and_retries`
in the route-sampling test sidecar. It routes a normalized Int/Function Union
against an existing Int upper bound, so the Int member completes before the
Function member reports an incompatibility. `ReportedErrors` and its journal
key lane are pre-reserved; the injected failure targets the `Errors` Vec after
growth. The witness checks event/sample pairing, exact retained/session delta
and peaks from the immediately preceding event, complete RouteCheckpoint
restoration, retained capacity, a single conditional post-rollback sample with
independent ledger reconciliation, and a successful retry.

The first M1 specification review required one additional §44 assertion: after
retry, the only public fact must have lower `Leaf::IntPositive`. That assertion
is now present, and a fresh M1 delta review is clean. The full route sidecar
passes eight tests; the exact new test passes. Package test-check, format, and
diff checks pass. No production code or `lib.rs` was changed in this slice.
The wider diagnostic scratch and `ReportedErrors` lanes remain open.

## F5c `ReportedErrors` changed-capacity route witness

Added `f5c_incoming_reported_errors_growth_samples_after_first_union_member_and_retries`
to the same sidecar. This fixture pre-reserves the `Errors` vector and route
journal key so failure injection isolates `ReportedErrors` growth after the
first private Union member completes. It checks event-time byte and peak delta
against the immediately preceding completed sample, retained set capacity,
full logical route checkpoint restoration, one conditional post-rollback
sample with an independently enumerated ledger, and retry. Retry reports the
Function-vs-Int incompatibility while the sole public fact remains the
canonical Int representative.

A fresh M1 specification delta review found no blocker. The focused sidecar
passes nine tests; package test-check, format, and whitespace checks pass. The
full library-suite discrepancy remains unresolved and no benchmark/resource
matrix was run. This closes only the `ReportedErrors` changed-capacity witness;
other diagnostic scratch lanes and the wider owner-to-route matrix remain
open. The test stayed outside `lib.rs`.

## F5c `DiagnosticDeltaIndices` changed-capacity route witness

Added `f5c_incoming_diagnostic_delta_indices_growth_samples_before_rollback_and_retries`
to the value-route sidecar. Its fixture pre-reserves the typed-pair map,
diagnostic delta Vec, and route-journal key lane, leaving only the diagnostic
index map at zero capacity. The injected post-reserve failure proves the exact
map-slot byte and peak delta against the prior completed event, retained
capacity after rollback, full checkpoint restoration, one conditional
post-rollback sample with independent ledger reconciliation, and retry.

The fresh M1 specification delta review found the event-time and rollback
sampling contract sound. It flagged a minor retry-linkage gap; the primary
closed it with assertions linking the fact to provenance and the routed-use
record, checking the consumed receipt and use-position marker, then reran the
exact test. The entire value-route sidecar passes ten tests; package test-check,
format, and whitespace checks pass. The full library discrepancy remains
unresolved, no benchmark/resource matrix was run, and no production code or
`lib.rs` changed. The other diagnostic scratch lanes and wider owner-to-route
matrix remain open.

## F5c `DiagnosticReverseOffsets` changed-capacity route witness

Added `f5c_incoming_diagnostic_reverse_offsets_growth_samples_before_rollback_and_retries`
to the value-route sidecar. The fixture pre-reserves typed-pair/delta state and
the route-journal key lane, then leaves `DiagnosticReverseOffsets` at zero
capacity. It checks the `usize` slot-byte delta and event peak against the
previous completed sample, retained capacity after rollback, full checkpoint
restoration, one conditional post-rollback sample with independent retained
ledger reconciliation, and retry identity across fact, provenance, consumed
receipt, routed-use record, and use-position marker.

The M1 specification review initially questioned whether event peaks were
independently validated because the retained-resource helper accepts the event
peak. Primary adjudication identified the separate exact peak formula from the
preceding sample plus observed capacity delta; the reviewer withdrew the
finding. The full value-route sidecar passes eleven tests; package test-check,
format, and whitespace checks pass. No production code or `lib.rs` changed.
The lane-specific capacity setup and slot-size formulas remain explicit. A
follow-up M1 refactor consolidated the shared route/trace/rollback/retry checks
for `DiagnosticDelta`, `DiagnosticDeltaIndices`, and `DiagnosticReverseOffsets`
into one sidecar helper, removing 325 net lines. A fresh specification delta
review found no weakened assertion. The remaining diagnostic scratch lanes and
owner-to-route matrix remain open.

## F5c `DiagnosticReverseCursors` changed-capacity route witness

Added a `DiagnosticReverseCursors` case to the shared diagnostic changed-
reserve helper. It leaves that target vector at zero capacity and explicitly
pre-reserves two `DiagnosticReverseOffsets` slots, matching the one-pair plus
terminal-offset request. The shared witness checks event-time slot bytes and
peaks, post-rollback retained accounting, one final sample, checkpoint restore,
and retry identity.

The M1 review found a minor fixture-proof gap because the predecessor reserve
was only one slot; the fixture now asserts capacity of at least two. The exact
test passes after repair, and the focused value-route sidecar passes twelve
tests. Package test-check, formatting, and whitespace checks pass. No
production code or `lib.rs` changed; additional diagnostic scratch lanes and
the wider owner-to-route matrix remain open.

## F5c per-pair diagnostic scratch changed-capacity witnesses

Extended the common sidecar helper with eight one-pair `prepare_diagnostic_scratch`
lanes: DFS stack, finish order, SCC indices, SCC nodes, SCC offsets, SCC pending
children, SCC worklist, and node witnesses. Target capacity is isolated at zero;
predecessors are reserved for the exact request, including two offset slots.
The shared assertions prove the event delta/peak, rollback and retry, and one
conditional post-rollback sample reconciled against retained capacities.

The M1 specification delta review found no issue. The value-route sidecar
passes thirteen tests; package test-check, format, and whitespace checks pass.
No production code or `lib.rs` changed. `DiagnosticReverseEdges` remains out of
scope because this fixture creates zero edges; bucket lanes remain open because
the simple route emits no diagnostic seed. Continue with an edge/error-producing
fixture before claiming the remaining diagnostic lanes reconciled.

Follow-up diagnosis for the earlier partial library-suite run: the exact
`direct_root_n_and_2n_counters_remain_linear` test and a filtered pair of
linearity tests passed. A read-only performance audit found that randomized
HashMap layout could plausibly affect the canonical-map probe-ratio assertion,
and saw no direct F5c path, but the original failed assertion and counter
values were not captured. Keep the discrepancy open; do not report the full
library suite as passing.

## F5c reverse-edge and diagnostic-bucket changed-capacity witnesses

Extended the shared incoming-route test helper with four changed-post-reserve
failure cases: `DiagnosticReverseEdges`, `DiagnosticBucketHeads`,
`DiagnosticBucketTails`, and `DiagnosticBucketCandidates`. These use the
normalized Int/Function Union shape plus a negative Function bound, which
yields diagnostic child edges and a mismatch seed. Each selected target lane
is isolated at zero capacity. The helper continues to assert event-time
capacity delta/peaks, full RouteCheckpoint restoration, exactly one
conditional post-rollback sample, independent retained-ledger reconciliation,
and retry linkage. Its independent ledger now derives retained child-edge
payload bytes directly from typed-pair memo capacities.

The M1 review initially found one major §44 omission: retry linkage covered a
single fact, provenance, receipt, and routed use, but did not assert that the
fact projected the first normalized Union member. The repair observes the
finalized Union's first member as Int and checks the retried sole fact's lower
endpoint is `Leaf::IntPositive`; the fresh M1 delta review is clean.

Verified by primary: the exact diagnostic-lane matrix test passes; the full
route sidecar passes 13 tests; `cargo check -p yu-solver --tests`,
`cargo fmt --all -- --check`, and `git diff --check` pass. This is a test and
progress-record change only; `lib.rs` remains 28,456 lines. No benchmark or
broad solver suite was run. At that point, `TypedWorklist` remained the next
changed-capacity event/sample gap; the following section records its closure.
The §3 accounting gate, F5c, and F5e remain open.

## F5c TypedWorklist changed-capacity witness

Generalized the sidecar helper from diagnostic-only naming to incoming-route
reserve lanes and added `f5c_incoming_typed_worklist_growth_samples_before_rollback_and_retries`.
The simple quantified route retains an earlier `FreshValueBounds` event, then
isolates the worklist at zero capacity. The shared assertions check the exact
`TypedWorkItem` slot-byte delta and event peaks, full RouteCheckpoint restore,
one conditional post-rollback sample, independent retained ledger, and retry
linkage across fact/provenance/receipt/routed-use state. Previous diagnostic
lane selections and assertions remain in the same helper.

Fresh M1 specification review found no issue. Primary verification: the exact
new test passes, the full route sidecar passes 14 tests,
`cargo check -p yu-solver --tests`, `cargo fmt --all -- --check`, and
`git diff --check` pass. No production code or `lib.rs` changed. Next audit the
exact event/sample witnesses for `TypedPairs` and `DiagnosticEdges`; §3, F5c,
and F5e remain open.

## F5c TypedPairs and DiagnosticEdges changed-capacity witnesses

Extended the incoming-route reserve helper with isolated `TypedPairs` and
`DiagnosticEdges` cases. The map lane starts at zero capacity. The edge lane
uses an edge-producing normalized Union; because rollback drops its newly
created per-pair memo, the test proves event-time allocation/peak and then
reconciles the independent retained post-state instead of expecting the child
vector capacity to survive.

Both cases check ordered event/sample linkage, exact slot-byte delta and peaks,
RouteCheckpoint restoration, one conditional post-rollback sample,
independent retained-ledger reconstruction, and retry linkage. The edge case
also checks the first canonical Union member as the sole public representative.
A fresh M1 specification review is clean. Primary verification: the focused
`f5c_incoming_` filter passes 55 tests; the full value-route sidecar passes 16
tests; `cargo fmt --all -- --check` and `git diff --check` pass. No production
code or `lib.rs` changed. Remaining: the broader owner-to-route matrix, per-use
rollback audit, §3 sampling/accounting gate, F5c, and F5e certification.

## F5c route-journal undo-key changed-reserve witnesses

Added two incoming-route witnesses for `RouteMutationJournal::typed_pair_keys`
and `reported_error_keys`. A small cfg(test)-only matching-reserve skip seam
targets each journal Vec reserve after the shared-lane map/set reserve, while
leaving existing first-match injection behavior unchanged. Both tests verify
exact slot-byte event deltas and peaks, rollback into the spare owner, one
conditional post-rollback sample, independent retained-ledger reconciliation,
and retry. The Union case checks the canonical Int representative and public
fact/provenance/receipt/routed-use linkage.

The fresh M1 spec review found one minor gap: the retried canonical fact index
was not explicitly tied to the fact. The primary added that assertion; the two
new tests plus existing journal-seen tests pass (5 total), and the full
value-route sidecar passes 18 tests. `cargo fmt --all -- --check` and
`git diff --check` pass. `lib.rs` grew by 17 lines, all cfg(test) injection
support; the 284-line witness addition remains in the test sidecar. Next: exact
event/sample and independent-ledger witnesses for the value/effect row-seen
setup reserves. F5c and the full §3/F5e gates remain open.

## F5c journal setup test relocation

Mechanically moved the three existing journal-seen setup failure tests from
`lib.rs` into `tests/f5c_value_exact_upper_route.rs` without changing their
bodies or expectations. `lib.rs` is 91 lines smaller; the focused journal
filter still passes all five tests, and format/whitespace checks pass. This is
only a test-location change. Next, add exact trace and independent-ledger
assertions for the two seen-vector changed-capacity events.

## F5c journal seen-vector setup event witnesses

The partial journal-setup failure test now captures both ordered events,
`journal/value_row_seen` and `journal/effect_row_seen`, from zero capacity
through the post-reserve EffectBounds failure. It checks observed `u32` slot
deltas and semantic/session peaks, then verifies failed-begin rollback,
retained spare capacities with empty logical vectors, one final sample, and an
independently reconstructed retained ledger. Existing successful retry remains.

Fresh M1 specification review is clean. The primary's exact journal filter
passes five tests; formatting and diff checks pass. The added proof is 103 net
lines in the sidecar; `lib.rs` was not touched in this slice. Next, reconcile
the residual owner-to-route matrix against §3. Sampler-cost remeasurement is
deferred pending a new budget and a method that isolates the measured work.

## F5c incoming value-row undo-owner event witness

Moved `f5c_incoming_value_undo_growth_precedes_later_provenance_failure` from
`lib.rs` into `tests/f5c_value_exact_upper_route.rs`. Its trace now binds the
`journal/value_rows` capacity event to one exact sample and checks the
`ValueRowUndo` byte delta, retained-byte and event-peak deltas, active-to-spare
journal retention across rollback, full checkpoint restoration, one conditional
post-rollback sample, independent retained/session/nested totals, and retry.

A fresh M1 specification delta review found no blocker or major issue. The
focused test, package test-check, format, and diff checks pass. Independent
post-rollback accounting recomputes retained totals from live capacities and
carries forward the event peak already checked against the preceding sample.
No production code changed; the move removes 67 lines from `lib.rs`. Next,
reconcile remaining ConstraintStore and routed-use owner traces against §3.
F5c and the full §3/F5e gates remain open.

## F5c ConstraintStore incoming-route changed-reserve witnesses

Moved the four-lane `ConstraintStore` failed-reserve test from `lib.rs` into
the route sidecar. It now binds every ordered store growth—including the
injected changed-failure reserve—to its event sample, using per-lane slot sizes.
Since store capacity belongs only to the session aggregate, semantic totals are
asserted unchanged and session totals carry the exact delta. The witness checks
rollback, one post-rollback sample, retained capacities, independent retained
and nested totals, then retries and verifies canonical fact/receipt/provenance/
routed-use linkage for each lane.

The first test failure came from asserting a store delta in semantic bytes; the
production aggregation was correct. A later checkpoint failure was resolved by
separating monotone physical growth/rebuild counters from logical rollback and
checking those counters against the event sequence. The original shared
closed-Union fixture remains. M1 spec review found one minor retry-link gap;
primary added the canonical, receipt, provenance, and route assertions, then
ran the full 23-test sidecar successfully. Package test-check, format, and diff
checks pass. No production code changed; moving the test removes 46 lines from
`lib.rs`. Next: exact event/sample and independent-ledger witnesses for
`RoutedUses` and `RoutedUsePositions`. F5c and §3/F5e remain open.

## F5c routed-use incoming-route changed-reserve witnesses

Moved the routed-use owner failure test from `lib.rs` into the existing route
sidecar, removing 54 lines. It now traces both `RoutedUsePositions` and
`RoutedUses` changed reserves in their actual order, checks exact lane-byte
deltas in the correct semantic/session aggregates, and derives event peaks from
the preceding completed sample plus each observed delta. The independent
post-rollback ledger receives those test-derived peaks while independently
rebuilding retained and nested totals. The witness verifies one conditional
post-rollback sample, logical rollback, retained capacities, and retry linkage
through canonical fact, receipt, provenance, routed use, and position.

The first M1 specification review found two major gaps in semantic delta and
peak-seed assertions. Both were addressed; a fresh M1 specification delta
review found no remaining issue in this per-owner witness. The focused test,
package test-check, format, and whitespace checks pass. No production code
changed. Next: independently reconcile post-rollback retained state for the
existing `ValueLevels` witness, then strengthen `FreshValueBounds`. The full
§3 gate, F5c, and F5e remain open.

## F5c ValueLevels incoming-route retained-ledger witness

Extended the existing changed-reserve witness to capture the completed
post-rollback sample and independently reconstruct retained/session and nested
bound totals from surviving state. The event-time peak is derived exactly from
saved pre-attempt peaks, the observed ValueLevels capacity delta, and
finish-output bytes. Test-derived expected peaks seed the independent ledger;
the completed post-rollback sample is compared with that ledger and with the
production resource ledger and execution counters. Existing checkpoint,
physical capacity, no-public-route, and successful retry checks remain.

The specification review caught expected peak locals referenced before
definition; primary added them from the saved pre-attempt state and exact delta,
then received reviewer confirmation that the blocker was closed. The focused
test, package test-check, format, and whitespace checks pass. No production
code changed. Next: consolidate the split `FreshValueBounds` incoming-route
witnesses with exact event/sample and independent retained-ledger evidence.
F5c and the full §3/F5e gates remain open.

The verified ValueLevels slice remains local at HEAD `0b2957a8`. The earlier
`.git` read-only failure is cleared in the resumed session; checkpoint and
push this slice before starting the next code change. Keep the next
FreshValueBounds witness in the existing sidecar to avoid further `lib.rs`
growth.

## F5c FreshValueBounds incoming-route rollback/retry witness

Moved the incoming fresh outer-row failure test from `lib.rs` to the existing
value-route sidecar and strengthened it to prove the exact observed
`VariableBounds` capacity delta and event-time aggregate/peak sample. The
failure now asserts one completed post-rollback sample, complete logical
RouteCheckpoint restoration, surviving outer capacity, independently rebuilt
retained/session/nested totals and peaks, and successful retry linkage through
the canonical fact, receipt, provenance, and routed-use records. The move
removed 75 lines from `lib.rs`; no production code changed.

The focused test and the single-threaded `f5c_incoming_` subset pass (57 tests),
as do `cargo check -p yu-solver --tests`, `cargo fmt --all -- --check`, and
`git diff --check`. Work stayed primary-only per the user's instruction; no
independent reviewer was used. No benchmark or sampler-cost experiment was run.
The slice is checkpointed as `e226f8b4` and pushed to `origin/yulang3` before
continuing the residual §3 per-use lane audit. The successful-path sampler
measurement remains unresolved and F5c/F5e remain open.

The earlier ValueLevels checkpoint is now committed as `38d40b01` and pushed
to `origin/yulang3` after the prior `.git` metadata write restriction cleared.

## F5c ValueExactUpper fresh-row transient-bound witness

Strengthened the incoming `ValueExactUpper` failure witness with exact
event-to-sample evidence. Its observed `ValueEndpointKey` capacity delta
updates event-time semantic/session retained bytes, nested bytes, and peaks
exactly. This lane grows inside the fresh row appended at the pre-attempt
outer-row length, so rollback drops the upper payload; a simultaneously traced
`ValueExactLower` growth on a preexisting row remains retained. The independent
post-rollback ledger now proves the upper lane returns to baseline while the
lower lane keeps its exact growth, and reconciles retained totals/peaks with
the one final sample and production counters. Retry assertions cover the
canonical fact, consumed receipt, provenance, routed-use, and position links.
No production code changed.

The focused test, all 57 single-threaded `f5c_incoming_` tests,
`cargo check -p yu-solver --tests`, format, and whitespace checks pass. Work
stayed primary-only per the user's instruction; there was no independent
review or benchmark. This slice is checkpointed as `7c07b7af` and pushed to
`origin/yulang3`. The paired `ValueExactLower` event/rollback repair is now
recorded below; next is the residual §3 owner-to-route audit. The full §3,
F5c, and F5e gates remain open.

## F5c ExtrusionStack incoming-route transient/retained witness

Moved the incoming ExtrusionStack failure test from `lib.rs` into the existing
value-route sidecar. The witness now ties the observed `ExtrusionEndpoint`
capacity delta to its completed event sample and exact semantic/session peak
transition, verifies that this is the last capacity event before rollback,
and contrasts event-time transient retained bytes with the lower
post-rollback retained state. An independent ledger reconstructs surviving
session/nested capacities and reconciles the one post-rollback sample and
production counters. Full RouteCheckpoint restoration and successful retry
links through canonical fact, consumed receipt, provenance, and routed-use
records remain asserted. No production code changed.

The focused test and single-threaded `f5c_incoming_` subset pass (57 tests);
the package test check, format check, and whitespace check pass. Work stayed
primary-only per the user's instruction, with no independent reviewer or
benchmark. This slice is checkpointed as `559a2d91` and pushed to
`origin/yulang3`; the successful-path sampler-cost question and complete
§3/F5c/F5e gates remain open.

## F5c ValueExactLower retained-row rollback witness

Strengthened the `ValueExactLower` incoming-route test to identify the growth
on its preexisting use row and derive its exact endpoint-slot delta. The
completed event sample now reconciles semantic/session retained bytes,
nested-bound bytes, and peaks against the preceding event sample. After route
rollback, the exact-lower payload remains allocated; an independent ledger
matches its observed retained delta and the one post-rollback sample. The test
also checks sampling counters, complete RouteCheckpoint restoration, and
successful retry links through the canonical fact, consumed receipt,
provenance, and routed-use records. No production code changed, and the test
stays in the existing sidecar rather than growing `lib.rs`.

The focused witness and all 57 single-threaded `f5c_incoming_` tests pass, as
do `cargo check -p yu-solver --tests`, `cargo fmt --all -- --check`, and
`git diff --check`. Work stayed primary-only per the user's request, with no
independent review or benchmark. The verified slice is checkpointed as
`f69dbb76` and pushed to `origin/yulang3`; the direct value-row witnesses
below now extend this audit. F5c/F5e remain open.

## F5c ValueDirectLower/Upper event and rollback witnesses

Strengthened the paired direct-bound failure test to identify the exact
preexisting lower row and fresh upper row and derive each `u32` slot-byte
delta from its completed event. Event-time retained/nested bytes and peaks
must match the immediately preceding sample. The one post-rollback sample and
independent ledger distinguish retained lower-row capacity from the dropped
fresh upper row. RouteCheckpoint restoration and retry linkage through
canonical fact, receipt, provenance, and routed-use records are checked.
The small event/sample helper remains in the sidecar; neither production code
nor `lib.rs` changed.

The focused witness and all 57 single-threaded `f5c_incoming_` tests pass, as
do `cargo check -p yu-solver --tests`, `cargo fmt --all -- --check`, and
`git diff --check`. Work stayed primary-only per the user's request, with no
independent review or benchmark. This slice is checkpointed as `8b118260` and
pushed to `origin/yulang3`; next is the residual §3 owner-to-route audit.
F5c/F5e remain open.

## F5c §3 per-use owner-to-route audit and final-sample overflow

Reconciled the remaining per-use changed-capacity witness matrix against the
authoritative §3 list for the approved closed-pure Function routes. The
crosswalk covers nested and outer value-bound storage, metadata/extrusion and
direct/exact rows, typed-pair plus all diagnostic scratch lanes, seven
instantiation scratch owners, six inference-Term owners, active/spare journal
owners, all four ConstraintStore owners, and both routed-use owners. Existing
no-change and partial-begin cleanup witnesses also match the addendum. Live
effect-row mutation remains outside the approved pure-Function scope.

The audit found that checked-byte overflow had not been isolated at the
conditional post-rollback sample boundary itself. Added a cfg(test)-only probe
and two sidecar witnesses: direct `route_incoming` proves full checkpoint
restoration, one attempted but uncompleted boundary sample, and no boundary or
test-ledger sample publication; consuming `run()` proves the same terminal
`IdentityExhausted` path after changed setup capacity and returns no result.
Only the test probe variant/condition was added to `lib.rs`; test bodies remain
in `f5c_value_exact_upper_route.rs`.

`cargo test -p yu-solver --lib f5c_ -- --test-threads=1` passes 137 tests,
including both new witnesses. The full single-threaded solver-library run was
started but interrupted during `f4_unbounded_cycle_scale_4k_keeps_direct_frontier_linear`
after over seven minutes; it produced no suite result and is not counted as
passing. `cargo check -p yu-solver --tests`, `cargo fmt --all -- --check`, and
`git diff --check` pass. No benchmark was run. This closes only
the per-use owner-to-route witness crosswalk; the full §3 measurement gate,
F5c, and F5e remain open. Work stayed primary-only with no independent review.

## F5c producer-order implementation slice and mixed-height decision

The user selected §36 height-major descriptor order over §25 structural-first
order for mixed-height normalized children. The choice is recorded in the
2026-09-24 design addendum; §44's first-member Union representative remains
unchanged.

Implemented the core producer-order binder assignment in `lib.rs`: removed
factorial alpha-label search from R/Q selection, select R by first surviving
producer trace, and assign Q by retained-predicate then R lower/upper first
occurrence. Tests now exercise reversed producer encounters, exact stored R
trace order, and shared Q identity across the predicate and multiple R bounds.
R ordinal arithmetic is checked. The normalizer rejects leftover live
`Variable`/`Shared` nodes in both polarities. `lib.rs` is 27,892 lines, 200 fewer than the
28,092-line resume point. No sidecar or public API was added.

Primary-only M2 work follows the user's instruction; no independent reviewer
or benchmark was used. `cargo fmt --check`, `cargo check -p yu-solver --tests`,
`cargo test -p yu-solver --lib f5c_ -- --test-threads=1` (136 passed), and
`git diff --check` pass. This is a partial producer-order checkpoint, not gate
closure. The final R-prune/census assertions remain incomplete, and the
height-major normalizer, stack-safe finalization, component-wide §36 resource
accounting, and §44 end-to-end representative linkage remain open. No §24 API
change is authorized; stop and seek a separately reviewed/user-approved
accounting-boundary choice if the implementation cannot fit the existing
callback.

## F5c height-major normalization candidate (2026-09-24)

Implemented the user's option B in `crates/yu-solver/src/f5c_normalization.rs`:
component-wide iterative postorder discovery, height-grouped stable
descriptor-word mergesort ranking, exact rank-based Union/Intersection
deduplication, iterative rebuild, and checked capacity/accounting for the
normalization index's 11 owned `Vec` lanes. `lib.rs` delegates normalization to
the private module and wires the §34/§36 public logical counters and the
`closed_normalization_index` resource family into production counters. A
mixed-height positive Union route now verifies that §44 publishes exactly the
first member under option B, even where §25 structural-first order would pick
the other member. A 4,096-Function chain passes on a 64 KiB thread stack.

Focused verification:
`cargo check -p yu-solver --tests` and
`cargo test -p yu-solver --lib f5c_ -- --test-threads=1` (147 passed). A
separate integer-descriptor mergesort oracle test now confirms the measured
18/24 comparison counts for the two root orders; a checked-counter-overflow
test confirms normalization counter publication is all-or-nothing. The
test-only independent ledger now samples each of the 11 live Vec capacities
and slot sizes before release, then reconciles their co-resident peak.
Normalizer reserves use amortized fallible `try_reserve` rather than exact
one-slot growth. The normalizer-specific and public-counter tests also pass.
No full library suite, benchmark, or F5e matrix was run. This is not
normalization-gate closure.

The independent oracle preserves the specification-conflict evidence. Five
component roots with the same quantified leaves, rotated as a set, normalize
to the same values, but the exact stable-merge comparison counter is 18 versus
24. That is input-sensitive work in the mandated mergesort, yet §36 also
requires counters to be identical under root-order permutations. Do not
silently change the counter into a predicted/worst-case value or add an
unapproved canonical pre-ordering pass. Await the user's choice between
keeping exact comparison counts and narrowing the invariance clause, or adding
a deterministic pre-ordering/schedule and its resource cost. Until then, keep
the code as an incomplete local checkpoint `b07de8fe` and do not push it.

The independent ledger reconciles the normalization index's own physical
lanes; it does not certify the full co-resident F5c draft/output-tree scratch
or any F5e public resource surface. Existing open gates remain open. No
independent reviewer was used, following the user's primary-only direction.

## F5c normalization counter-invariance option B (2026-09-24)

The user selected option B for the §36 counter conflict: retain all-counter
permutation invariance and canonicalize sort inputs before the specified
stable mergesort. The decision and cost model are in
`notes/design/2026-09-24-f5c-normalization-counter-invariance-addendum.md`.

The private normalizer now preorders child keys and per-height descriptor keys
with stable insertion sort for three through eight inputs (one or two need no
preorder) and iterative MSD radix distribution for larger groups. Exact
stable-mergesort word comparisons and
subsequent equality checks remain the measured counter. The index ledger now
includes an explicit radix-frame stack and one fixed 771-`usize` workspace.
`lib.rs` only shares the lane-count constant; the normalization work remains
in `f5c_normalization.rs`.

The former five-root 18/24 probe now produces 18/18, and complete normalization
stats match under root rotation and reversed Union members. Tests also cover
multi-byte `u32` ordering, variable-length descriptor prefixes, large radix
paths, and independent physical-lane reconciliation. Verification passes:
`cargo fmt --all -- --check`, `git diff --check`,
`cargo test -p yu-solver --lib f5c_ -- --test-threads=1` (152 passed), and
`cargo check --workspace`. No benchmark, full library suite, or F5e matrix ran;
the added pass is statically `O(N+W)` within §36's bound. Work remained
primary-only as directed, with no independent review. This is an implementation
checkpoint, not F5c/F5e closure.

<!-- daily-append-anchor: 2026-09-24 -->

## F5c iterative summary-to-draft materialization checkpoint

The summary graph builder and summary-to-boxed-tree conversion were already
iterative. I moved the remaining recursive `F5cGeneralizer` conversion of
boxed Function/Union/Intersection trees into a private task/value worklist in
`crates/yu-solver/src/f5c_materialization.rs`. It keeps polarity, child order,
effect fields, `Shared` marking, and the §44 first-member projection intact.
Two new capacity lanes are included in the existing generalization walker
observer and independent ledger. `lib.rs` is now 27,700 lines (112 fewer than
the prior checkpoint); the new regression remains in a test sidecar. The
implementation and records are checkpointed in `68952716`.

Alternating Function chains of depths 2,048 (positive root) and 2,049
(negative root) pass with a 64 KiB stack and reconcile task/value lane
requests, growths, peaks, release, and ledger totals. All 153 tests in
`cargo test -p yu-solver --lib f5c_ -- --test-threads=1` pass, as do
`cargo check --workspace`, formatting, and whitespace checks. The extra work
is O(N) and allocates two O(N)-capacity task/value lanes; no timing benchmark
was run, and full co-resident output-tree accounting remains open.

This is only one stack-safety slice. Recursive paths remain in Term row
collection, guarded-owner search, fixed-point replay, owner/reference/incidence
and occurrence walks, boxed-tree clone/drop, and the closed-tree finalizer. The
last one runs inside the §24 higher-ranked callback, where F5b §6 bars new
solver-owned scratch allocation/mutation and transaction-branded handles
cannot be retained in solver-owned slots. The indexed `yu-types` API remains an
unapproved, reviewed-as-underspecified Draft. The immediate decision is to
preserve the current API and leave finalizer stack safety open, or commission
fresh reviewed design for that new API after resolving producer graph and
capacity-accounting gaps. No API change was made here.

The full single-threaded library suite was not rerun: its latest attempt stopped
after more than seven minutes in an F4 scale test and is not passing evidence.
No benchmark or F5e matrix ran. Work stayed primary-only at the user's
direction; no independent review was performed. F5c/F5e remain open.

## F5c iterative producer-analysis traversals

Moved guarded-owner search, recursive-owner reference closure, polarity
incidence, first-occurrence ordering, and Term value-row collection into
`crates/yu-solver/src/f5c_tree_analysis.rs`, replacing recursive traversal with
one reusable explicit-stack visitor. DFS order, Function guarding, and Term
effect omission are preserved. A new `AnalysisTasks` lane is included in the
existing walker capacity observer and independent ledger. The traversal
sidecar checks positive/negative parity and runs 4,096-deep positive and
negative boxed Function chains and a 4,096-deep Term chain on 64 KiB stacks.
`lib.rs` is 27,542 lines, down 158 lines from the previous checkpoint.

The filtered `f5c_` suite passes 155 tests both with default features and with
`--no-default-features`; `cargo check -p yu-solver --tests`,
`cargo check --workspace`, `cargo fmt --check`, and `git diff --check` pass. An
unfiltered no-default-feature library run was interrupted in the broad F4
scale area and is not passing evidence. No benchmark or F5e matrix ran; zero
measurement processes were used. Work stayed primary-only at the user's
direction, with no independent review. Recursive replay and binder
substitution, clone/drop, §24 finalization, and the other F5c/F5e gates remain
open. The code and initial record sync are pushed at `2366de39`; the exact
checkpoint link is included in the F5c handoff. The next bounded slice is
iterative candidate replay/binder substitution with separately accounted
scratch.
