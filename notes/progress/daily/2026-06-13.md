# 2026-06-13

## role 入力 variance

- `RoleInputVariance` / `RoleInputOccurrence` / `RoleInputVarianceTable` を追加した。
  - role method signature に receiver を第1通常引数として補った `SignatureType` を歩き、
    role通常引数ごとに `Unused | Covariant | Contravariant | Invariant` を記録する。
  - `Function` では極性反転、`Apply` 引数と effect row は保守的に invariant。
  - `AnalysisSession` が role path ごとの variance table を持つ。
- residual `where` へ残る role predicate の compact 簡約だけ、宣言varianceを反転して使うようにした。
  - role solve 前の demand/candidate 照合は従来どおり invariant。
  - `Covariant` な where input は interval の lower 境界だけ、`Contravariant` は upper 境界だけを
    polarity / co-occurrence で読む。
  - std `list.sort` は `list 'a -> list 'a where 'a: Ord` まで簡約される。
  - `sort_split` は pivot と要素の supertype 関係を持つ補助関数なので
    `list('b & 'a) -> 'a -> ... where ('b|'a): Ord` のまま残る。
- spec 同期:
  - `spec/2026-06-02-role-system.md` に role 入力 variance の導出、where での反転、
    residual 簡約での片側境界読みを追記。
- コミット:
  - `f700e167 infer: record role input variances from signatures`
  - `f9f2f3e7 infer: simplify residual roles with input variance`

## 確認

- `cargo fmt --check`
- `git diff --check`
- `cargo test -q -p infer -p poly -p sources -p yulang2`
- `cargo build -q -p yulang2 && timeout 90 ./target/debug/yulang2 check-poly-std examples/showcase.yu`
- `timeout 90 ./target/debug/yulang2 dump-poly-std-in examples/showcase.yu std::data::list`
