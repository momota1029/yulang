# 2026-07-14

## Candidate-independent fallback classifier Slice 3

- Slice 1/2 の readiness predicates と SCC frontier を前提に、test-only classifier の実構造を調査。
- `SelectionUseTable`、local/global role-method table、finalized `Scheme`、owner 別 role constraints、既存の merge-recording coalescer を確認。
- scheme instantiation を行わず、target scheme の binder occurrence と selection の negative compact view を読み取り専用で対応付ける方針。
- classifier は component 全体を all-or-nothing で判定し、projection / existing demand / coalescing の unapplied merge を fail-closed で拒否する test-only module として実装。
- real std の `std::core::ops::#op:infix:+` / `#op:infix:<` / `#op:infix:>` wrapper と `.add` / `.lt` / `.gt` selection は全て eligible。concrete receiver、global candidate 重複、複数 role predicate、projection/coalescing merge の負例は全て ineligible。
- `cargo test -p infer`: 717 passed、既知の `mark_expr_block_*` 5 failures。`cargo check -p infer` 通過。std comparison は `998/3/297/0` で不変。

<!-- daily-append-anchor: 2026-07-14 -->
