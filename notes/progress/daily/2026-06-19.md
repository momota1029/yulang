# 2026-06-19

## control VM arm payload lookup

- 残っている `shared_frame_unwrap_clones` のうち `case` / `catch` bucket を見た。
  `examples/showcase.yu` の単発 debug timing では
  `case=1655`、`catch=3285`、`block=966`、`apply=831`。
- `CaseGuard` / `CatchValueGuard` / `CatchRequestGuard` だけを borrowed execution に載せる案を試したが、
  `shared_frame_unwrap_clones` が動かず、該当 clone は guard frame 由来ではなかったため戻した。
- `BindThen::CaseArm` / `CatchValue` / `CatchRequestPayload` が共有済み arm table の要素を
  さらに所有していたため、`arm` payload を外した。
  bind 後は `arms + index` から `guard` / `body` / `continuation` を読む。
  `eval_case_arm` / `handle_catch_*_arm` も `arms[index].clone()` せず、
  `bind_pat` に必要な pattern だけ clone する。
- repeat 5 の `bench/nondet_20_discard.yu` release / no-cache:
  `runtime_execute` 56.9ms / 56.3ms / 59.9ms / 56.3ms / 54.8ms。
- repeat 5 の `examples/showcase.yu` release / no-cache:
  `runtime_execute` 126.9ms / 122.5ms / 128.7ms / 122.7ms / 122.9ms。
  `shared_frame_unwrap_clones` は変わらないため、これは arm payload clone 削減として見る。

確認:

- `cargo fmt --all -- --check`
- `timeout 120s cargo check -q -p control-vm`
- `timeout 180s cargo test -q -p control-vm -p mono-runtime -- --test-threads=1`
- `timeout 180s cargo test -q -p yulang case_02 -- --test-threads=1`
- `timeout 240s cargo test -q -p yulang -- --test-threads=1`
- `timeout 300s bench/static_analysis_bench.sh --repeat 5 bench/nondet_20_discard.yu examples/showcase.yu`

## control VM shared frame borrowed execution

- `apply_shared_result_frame` の `Rc::try_unwrap` 失敗時に `Frame` 全体を clone せず、
  result frame を borrowed execution で処理するようにした。
  `CatchResult` / `RefSetHandleResult` / `RefSetHandleValueResult` は、
  継続再開に必要な payload だけ clone する。
- value frame 側も `ApplyForcedThunk` / `ApplyArg` / `ApplyCallee` /
  `ApplyAdapterArg` / `CaseScrutineeForce` / `CaseScrutinee` /
  `BlockLetValue` / `BlockExprValue` を borrowed execution に載せた。
  共有 snapshot から frame 全体を unwrap-clone する代わりに、次の VM state に必要な
  `env` / shared arm table / statement table / marker table だけを clone する。
- 段階計測では、result + case までで `shared_frame_unwrap_clones` が
  `bench/nondet_20_discard.yu=881`、`examples/showcase.yu=1797` まで低下。
  apply frame を載せた後は `bench/nondet_20_discard.yu=21`、`examples/showcase.yu=966`。
  block frame まで載せると、repeat 5 の両対象で `shared_frame_unwrap_clones=0`。
- repeat 5 の `bench/nondet_20_discard.yu` release / no-cache:
  `runtime_execute` 55.2ms / 54.2ms / 53.8ms / 53.5ms / 52.4ms。
- repeat 5 の `examples/showcase.yu` release / no-cache:
  `runtime_execute` 119.3ms / 117.3ms / 120.6ms / 127.4ms / 121.7ms。

確認:

- `cargo fmt --all -- --check`
- `timeout 120s cargo check -q -p control-vm`
- `timeout 180s cargo test -q -p control-vm -p mono-runtime -- --test-threads=1`
- `timeout 180s cargo test -q -p yulang case_02 -- --test-threads=1`
- `timeout 240s cargo test -q -p yulang -- --test-threads=1`
- `timeout 240s bench/static_analysis_bench.sh --repeat 5 bench/nondet_20_discard.yu examples/showcase.yu`

## control VM shared marker function-call path

- request に guard を付ける `active_add_ids` は、request 時点で効く `depth == 0` の
  `AddIdMarker` だけを積むようにした。
  `depth > 0` の marker は `active_marker_plans` 側に残り、call / resume 変換で
  depth が落ちた後に request-visible stack へ入る。
- request がすでに live guard を持つ場合と、scan 中に live guard を見つけた場合は、
  以降の add-id scan を打ち切るようにした。
  `active_add_id_scans` は repeat 計測で
  `bench/nondet_20_discard.yu: 21273 -> 1302`、
  `examples/showcase.yu: 41150 -> 4106`。
- `apply_scoped_value` と direct-known callee apply で、function-call marker 変換が
  identity になる場合は `Rc<[ValueMarker]>` をそのまま共有するようにした。
  これに合わせて shared marker frame の enter / close 経路を追加し、
  identity path では `Vec<ValueMarker>` の clone と `markers.to_vec() -> Rc` を避ける。
- handler path ごとの `HashMap` index も試したが、path prefix check は減る一方で
  marker push 側の map 更新が重く、repeat 5 の `runtime_execute` が悪化したため採用しない。
- repeat 5 の `bench/nondet_20_discard.yu` release / no-cache:
  `runtime_execute` 48.5ms / 51.3ms / 53.0ms / 54.3ms / 50.9ms。
- repeat 5 の `examples/showcase.yu` release / no-cache:
  `runtime_execute` 108.6ms / 108.6ms / 108.4ms / 109.8ms / 108.2ms。

確認:

- `cargo fmt --all -- --check`
- `timeout 120s cargo check -q -p control-vm`
- `timeout 180s cargo test -q -p control-vm -p mono-runtime -- --test-threads=1`
- `timeout 180s cargo test -q -p yulang case_02 -- --test-threads=1`
- `timeout 240s cargo test -q -p yulang -- --test-threads=1`
- `timeout 300s bench/static_analysis_bench.sh --repeat 5 bench/nondet_20_discard.yu examples/showcase.yu`

## control VM structural expr payload IDs

- tuple / record / poly variant の静的 payload を `EvalExpr` や `Frame` へ clone せず、
  frame 側には元の `ExprId` と進行中の index だけを持たせる形にした。
  item / field / tag は `Program` の immutable IR から index で読み直す。
- `Program` への raw pointer slice 参照も検討したが、crate は `#![forbid(unsafe_code)]` のため不採用。
  safety を崩さずに同じ方向へ寄せるため、`ExprId` 経由の静的 payload lookup にした。
- `HashMap` cache と dense `Vec<Option<...>>` cache も試したが、lookup / allocation の固定費が増えて
  `examples/showcase.yu` が悪化したため採用しない。
- repeat 5 の `bench/nondet_20_discard.yu` release / no-cache:
  `runtime_execute` 49.7ms / 48.2ms / 50.9ms / 50.5ms / 48.9ms。
- repeat 5 の `examples/showcase.yu` release / no-cache:
  `runtime_execute` 108.4ms / 109.5ms / 110.9ms / 109.9ms / 112.1ms。

確認:

- `cargo fmt --all -- --check`
- `timeout 120s cargo check -q -p control-vm`
- `timeout 180s cargo test -q -p control-vm -p mono-runtime -- --test-threads=1`
- `timeout 180s cargo test -q -p yulang case_02 -- --test-threads=1`
- `timeout 240s cargo test -q -p yulang -- --test-threads=1`
- `timeout 300s bench/static_analysis_bench.sh --repeat 5 bench/nondet_20_discard.yu examples/showcase.yu`

## control VM static frame payload IDs

- `Coerce` / `ForceThunk` / `MakeFunctionAdapter` frame が `Type` /
  `FunctionAdapterHygiene` を所有しないようにし、元の `ExprId` だけを持つ形にした。
  resume 時は `Program` の static payload から必要な型や hygiene を読み直す。
- `EvalExpr::from_expr` でも、上記三種の `source` / `target` / `target.value` /
  `hygiene` clone をやめた。型 clone は実際に boundary adaptation や
  `Value::FunctionAdapter` を作る時点まで遅延する。
- closure の `Pat` も `ExprId` 化する案を試したが、apply ごとの `Program` lookup が増え、
  `examples/showcase.yu` が悪化したため不採用。closure は従来どおり `Pat` / `body` を持つ。
- repeat 5 の `bench/nondet_20_discard.yu` release / no-cache:
  `runtime_execute` 47.3ms / 48.5ms / 46.5ms / 46.8ms / 48.5ms。
- repeat 5 の `examples/showcase.yu` release / no-cache:
  `runtime_execute` 105.7ms / 105.5ms / 108.2ms / 111.4ms / 99.9ms。

確認:

- `cargo fmt --all -- --check`
- `timeout 120s cargo check -q -p control-vm`
- `timeout 180s cargo test -q -p control-vm -p mono-runtime -- --test-threads=1`
- `timeout 180s cargo test -q -p yulang case_02 -- --test-threads=1`
- `timeout 240s cargo test -q -p yulang -- --test-threads=1`
- `timeout 300s bench/static_analysis_bench.sh --repeat 5 bench/nondet_20_discard.yu examples/showcase.yu`

## control VM env / frame allocation counters

- 外部レビュー後の次 slice として、実装変更前に支配項を見る counter を追加した。
  `CapturedEnv` は insert 前の `Rc::strong_count` と entry 数から COW clone と copied entry を測る。
- `RuntimeStats` と `--runtime-phase-timings` に次を追加した:
  `env_lookups`, `env_lookup_hits`, `env_lookup_misses`, `env_inserts`,
  `env_cow_clones`, `env_cow_entries_copied`, `env_max_size`,
  `frame_allocs`, `marker_scope_frame_touches`。
  `bench/static_analysis_bench.sh` の表にも同じ列を出す。
- 単発 release / no-cache の `bench/nondet_20_discard.yu`:
  `runtime_execute=50.3ms`, `env_lookups=12606`, `env_inserts=10169`,
  `env_cow_clones=2606`, `env_cow_entries_copied=3488`,
  `frame_allocs=13968`, `marker_scope_frame_touches=571487`。
- 単発 release / no-cache の `examples/showcase.yu`:
  `runtime_execute=116.0ms`, `env_lookups=42495`, `env_inserts=35257`,
  `env_cow_clones=4837`, `env_cow_entries_copied=7573`,
  `frame_allocs=42773`, `marker_scope_frame_touches=452587`。
  env COW は確かにあるが、frame allocation と marker scope touch も次の本命として濃い。

確認:

- `cargo fmt --all -- --check`
- `timeout 120s cargo check -q -p control-vm -p yulang`
- `bash -n bench/static_analysis_bench.sh`
- `timeout 180s cargo test -q -p control-vm -p mono-runtime -- --test-threads=1`
- `timeout 180s cargo test -q -p yulang case_02 -- --test-threads=1`
- `timeout 180s bench/static_analysis_bench.sh --repeat 1 bench/nondet_20_discard.yu`
- `timeout 180s bench/static_analysis_bench.sh --repeat 1 examples/showcase.yu`

## control VM persistent CapturedEnv chain

- `CapturedEnv` を `Rc<HashMap<DefId, Value>>` + COW から、
  one-binding persistent frame chain に変えた。
  `insert` は新しい frame を先頭に積むだけで、lookup は新しい frame から親へ辿る。
  同じ `DefId` の shadowing は先頭 frame が勝つため、既存 HashMap の上書きと同じ結果になる。
- `env_lookup_steps` を追加し、parent chain 化で lookup がどれだけ増えるかを測るようにした。
- repeat 5 の `bench/nondet_20_discard.yu` release / no-cache:
  `runtime_execute` 45.1ms / 44.7ms / 45.0ms / 44.7ms / 46.4ms。
  `env_cow_clones=0`, `env_lookup_steps=21302`, `env_max_size=5`。
- repeat 5 の `examples/showcase.yu` release / no-cache:
  `runtime_execute` 113.1ms / 102.6ms / 100.4ms / 101.3ms / 99.2ms。
  `env_cow_clones=0`, `env_lookup_steps=70440`, `env_max_size=5`。
  `showcase` は揺れが大きいが、前段の 105〜116ms から下側へ寄った。

確認:

- `cargo fmt --all -- --check`
- `timeout 120s cargo check -q -p control-vm -p yulang`
- `bash -n bench/static_analysis_bench.sh`
- `timeout 180s cargo test -q -p control-vm -p mono-runtime -- --test-threads=1`
- `timeout 180s cargo test -q -p yulang case_02 -- --test-threads=1`
- `timeout 240s cargo test -q -p yulang -- --test-threads=1`
- `timeout 300s bench/static_analysis_bench.sh --repeat 5 bench/nondet_20_discard.yu examples/showcase.yu`

## control VM im_rc env experiment

- `CapturedEnv` の代替として `im_rc::HashMap<DefId, Value, FxBuildHasher>` も試した。
  lookup step は常に 1 になるが、永続 HashMap の insert / node sharing の定数が重く、
  one-binding persistent frame chain より遅かったため採用しない。
- repeat 5 の `bench/nondet_20_discard.yu` release / no-cache:
  `runtime_execute` 55.3ms / 52.3ms / 53.3ms / 53.0ms / 53.5ms。
  親ポインタ版は同条件で 45.1ms / 44.7ms / 45.0ms / 44.7ms / 46.4ms。
- repeat 5 の `examples/showcase.yu` release / no-cache:
  `runtime_execute` 119.1ms / 125.4ms / 124.2ms / 120.8ms / 114.7ms。
  親ポインタ版は同条件で 113.1ms / 102.6ms / 100.4ms / 101.3ms / 99.2ms。
- `env_max_size=5` の現状では、HashMap lookup を O(1) にする利益より、
  frame chain の小さい allocation と浅い線形探索の方が勝つ。
  dense slot 化へ進むまでは親ポインタ版を採用する。

確認:

- `cargo check -q -p control-vm -p yulang`
- `timeout 240s cargo test -q -p control-vm -p mono-runtime -- --test-threads=1`
- `timeout 300s bench/static_analysis_bench.sh --repeat 5 bench/nondet_20_discard.yu examples/showcase.yu`

## control VM cheap-clone control values

- `Value::Closure` / `Value::RecursiveClosure` / `Value::Thunk` /
  `Value::FunctionAdapter` が大きな runtime payload を直に所有しないようにし、
  `Rc` handle を持つ形へ変えた。
  env lookup や continuation / frame 内の `Value` clone では、closure の `Pat`、
  thunk payload、function adapter の `Type` と inner function を複製しない。
- thunk は `Rc::try_unwrap` で一意なら従来どおり payload を move し、
  共有中だけ clone に落とす。`Rc` 化による force hot path の余分な clone を避けるため。
- repeat 5 の `bench/nondet_20_discard.yu` release / no-cache:
  `runtime_execute` 41.9ms / 42.5ms / 42.3ms / 44.5ms / 41.7ms。
  親ポインタ env 直後は 45.1ms / 44.7ms / 45.0ms / 44.7ms / 46.4ms。
- repeat 5 の `examples/showcase.yu` release / no-cache:
  `runtime_execute` 95.3ms / 91.6ms / 98.3ms / 95.0ms / 92.6ms。
  親ポインタ env 直後は 113.1ms / 102.6ms / 100.4ms / 101.3ms / 99.2ms。
  `showcase` は特に `Value` clone の重さが効いていた可能性が高い。

確認:

- `cargo fmt --all -- --check`
- `bash -n bench/static_analysis_bench.sh`
- `timeout 240s cargo test -q -p control-vm -p mono-runtime -- --test-threads=1`
- `timeout 180s cargo test -q -p yulang case_02 -- --test-threads=1`
- `timeout 300s cargo test -q -p yulang -- --test-threads=1`
- `timeout 300s bench/static_analysis_bench.sh --repeat 5 bench/nondet_20_discard.yu examples/showcase.yu`

## control VM marker scope frame-delta experiment

- active marker scopes の `frames_remaining` を各 frame consume / extend ごとに全 scope へ反映せず、
  `frame_delta` を一つ持つ案を試した。
  completed 判定だけ `frames_remaining + frame_delta` を見て、request で innermost marker を閉じる時だけ
  outer scope の remaining を materialize する形。
- `cargo check -q -p control-vm -p yulang` と
  `timeout 240s cargo test -q -p control-vm -p mono-runtime -- --test-threads=1` は通ったが、
  repeat 5 の `runtime_execute` は改善しなかった。
  `bench/nondet_20_discard.yu` は 41.6ms / 41.5ms / 40.7ms / 42.9ms / 43.8ms、
  `examples/showcase.yu` は 91.5ms / 96.3ms / 96.6ms / 96.7ms / 93.9ms。
  cheap-clone control values 後の揺れ幅と同程度で、複雑性に見合う差ではない。
- この方向は現状では採用しない。
  marker scope は単なる counter cursor 化より、active marker plan / handler state 自体を
  `ScopeId` 風の pointer state に畳む方を次に見る。

## control VM owned frame slot experiment

- 通常 request continuation では `Frame` を owned のまま積み、catch で multi-shot continuation として
  捕捉する時だけ `Rc<Frame>` snapshot へ変換する `FrameSlot::{Owned, Shared}` 案を試した。
  狙いは通常 path の frame ごとの `Rc::new(Frame)` を外すこと。
- `cargo fmt --all -- --check` と
  `timeout 240s cargo test -q -p control-vm -p mono-runtime -- --test-threads=1` は通った。
  repeat 5 では `frame_allocs` が `bench/nondet_20_discard.yu` で 13,968 -> 4,768、
  `examples/showcase.yu` で 42,773 -> 12,760 まで落ちた。
- ただし `runtime_execute` は悪化した。
  `bench/nondet_20_discard.yu` は 53.6ms / 57.3ms / 55.9ms / 54.8ms / 58.5ms、
  `examples/showcase.yu` は 116.5ms / 115.6ms / 120.5ms / 123.9ms / 111.9ms。
  `FrameSlot` 分岐、owned frame の大きさ、capture 時 snapshot 化の定数が、
  allocator 削減より重かったと見る。
- この方向は採用しない。
  current stack / captured snapshot の分離をやるなら、`Frame` enum 自体の payload を
  先にさらに小さくするか、chunked snapshot のように branch を増やさない表現へ寄せる必要がある。

## control VM shared resume marker fast path

- `SharedMarkers` を持っている marker close / scoped close の request path で、
  continuation resume marker 変換が identity の場合は `Rc<[ValueMarker]>` をそのまま共有するようにした。
  function-call marker 変換には同じ fast path が既にあり、resume 側にも揃えた。
- repeat 5 の `bench/nondet_20_discard.yu` release / no-cache:
  `runtime_execute` 41.7ms / 41.2ms / 45.7ms / 43.6ms / 41.6ms。
  cheap-clone control values 後とほぼ同程度。
- repeat 5 の `examples/showcase.yu` release / no-cache:
  `runtime_execute` 96.0ms / 91.3ms / 90.6ms / 91.8ms / 90.3ms。
  cheap-clone control values 後の 95.3ms / 91.6ms / 98.3ms / 95.0ms / 92.6ms より
  下側へ寄った。

確認:

- `cargo fmt --all -- --check`
- `cargo check -q -p control-vm -p yulang`
- `timeout 240s cargo test -q -p control-vm -p mono-runtime -- --test-threads=1`
- `timeout 180s cargo test -q -p yulang case_02 -- --test-threads=1`
- `timeout 300s cargo test -q -p yulang -- --test-threads=1`
- `timeout 300s bench/static_analysis_bench.sh --repeat 5 bench/nondet_20_discard.yu examples/showcase.yu`

## control VM empty marker scopes

- `Continuation` の marker scopes を `Rc<[ContinuationMarkerScope]>` 直持ちから
  `Option<Rc<[ContinuationMarkerScope]>>` に変えた。
  `Continuation::default()` は hot path で多く作られるため、空 scope では `Rc` 自体を作らない。
- non-empty scope は従来どおり shared slice として clone する。
  `prepend_marker_scope` だけが `Some(Rc<[...]>)` を作り、resume 側は `None` を空として扱う。
- repeat 5 の `bench/nondet_20_discard.yu` release / no-cache:
  `runtime_execute` 39.0ms / 39.3ms / 41.7ms / 38.8ms / 39.3ms。
  shared marked value markers 後の 39.7ms / 39.7ms / 39.2ms / 39.8ms / 42.6ms より
  少し下側へ寄った。
- repeat 5 の `examples/showcase.yu` release / no-cache:
  `runtime_execute` 84.1ms / 83.7ms / 82.2ms / 87.5ms / 86.9ms。
  shared marked value markers 後の 86.8ms / 88.9ms / 83.8ms / 84.9ms / 84.8ms と比べて
  下側へ寄った。

確認:

- `cargo fmt --all -- --check`
- `cargo check -q -p control-vm -p yulang`
- `timeout 240s cargo test -q -p control-vm -p mono-runtime -- --test-threads=1`
- `timeout 180s cargo test -q -p yulang case_02 -- --test-threads=1`
- `timeout 300s cargo test -q -p yulang -- --test-threads=1`
- `timeout 240s bench/static_analysis_bench.sh --repeat 5 bench/nondet_20_discard.yu examples/showcase.yu`

## control VM shared marked value markers

- `Value::Marked` / `Frame::MarkValue` の marker payload を `Vec<ValueMarker>` から
  `Rc<[ValueMarker]>` に変えた。
  escaping marked value や frame を clone しても marker list を複製しないため。
- marked thunk を force する path は、既存の `with_shared_marker_frame` を使って
  shared slice をそのまま active marker plan へ積む。
- repeat 5 の `bench/nondet_20_discard.yu` release / no-cache:
  `runtime_execute` 39.7ms / 39.7ms / 39.2ms / 39.8ms / 42.6ms。
  shared structural values 後の 40.6ms / 44.8ms / 41.2ms / 42.7ms / 41.9ms より
  少し下側へ寄った。
- repeat 5 の `examples/showcase.yu` release / no-cache:
  `runtime_execute` 86.8ms / 88.9ms / 83.8ms / 84.9ms / 84.8ms。
  shared structural values 後の 88.6ms / 86.9ms / 94.2ms / 88.9ms / 86.0ms より
  下側へ寄った。

確認:

- `cargo fmt --all -- --check`
- `cargo check -q -p control-vm -p yulang`
- `timeout 240s cargo test -q -p control-vm -p mono-runtime -- --test-threads=1`
- `timeout 180s cargo test -q -p yulang case_02 -- --test-threads=1`
- `timeout 300s cargo test -q -p yulang -- --test-threads=1`
- `timeout 300s bench/static_analysis_bench.sh --repeat 5 bench/nondet_20_discard.yu examples/showcase.yu`

## control VM shared structural values

- `Value::Tuple` / `Value::Record` / `Value::PolyVariant` /
  `Value::DataConstructor` の payload を `Vec` 直持ちから `Rc<[...]>` に変えた。
  pattern bind、record projection、continuation / frame 内の `Value` clone で、
  structural payload 全体を複製しないため。
- ref-set resolution のように mutable `Vec` が必要な rare path では、
  shared slice から `Vec` へ戻して従来の処理に渡す。
  normal construction は最後に `Vec::into_boxed_slice()` 経由で共有 slice 化する。
- repeat 5 の `bench/nondet_20_discard.yu` release / no-cache:
  `runtime_execute` 40.6ms / 44.8ms / 41.2ms / 42.7ms / 41.9ms。
  shared resume marker fast path 後とほぼ同程度。
- repeat 5 の `examples/showcase.yu` release / no-cache:
  `runtime_execute` 88.6ms / 86.9ms / 94.2ms / 88.9ms / 86.0ms。
  shared resume marker fast path 後の 96.0ms / 91.3ms / 90.6ms / 91.8ms / 90.3ms から
  さらに下側へ寄った。

確認:

- `cargo fmt --all -- --check`
- `cargo check -q -p control-vm -p yulang`
- `timeout 240s cargo test -q -p control-vm -p mono-runtime -- --test-threads=1`
- `timeout 180s cargo test -q -p yulang case_02 -- --test-threads=1`
- `timeout 300s cargo test -q -p yulang -- --test-threads=1`
- `timeout 300s bench/static_analysis_bench.sh --repeat 5 bench/nondet_20_discard.yu examples/showcase.yu`

## control VM shared pattern markers

- record/list pattern bind が保持する marker payload を `Vec<ValueMarker>` から
  `Rc<[ValueMarker]>` に変えた。
  `BindThen::RecordField` と `Frame::BindRecordDefault` も shared marker を持つ。
- `value_view` で一度 marker list を作る点はそのままだが、record/list pattern の後続 frame や
  recursive bind step では marker list 本体を clone しない。
- repeat 5 の `bench/nondet_20_discard.yu` release / no-cache:
  `runtime_execute` 38.9ms / 38.8ms / 39.3ms / 38.4ms / 38.4ms。
  empty marker scopes 後の 39.0ms / 39.3ms / 41.7ms / 38.8ms / 39.3ms より
  下側へ寄った。
- repeat 5 の `examples/showcase.yu` release / no-cache:
  `runtime_execute` 81.2ms / 81.3ms / 83.6ms / 82.6ms / 80.6ms。
  empty marker scopes 後の 84.1ms / 83.7ms / 82.2ms / 87.5ms / 86.9ms より
  明確に下がった。

確認:

- `cargo fmt --all -- --check`
- `cargo check -q -p control-vm -p yulang`
- `timeout 240s cargo test -q -p control-vm -p mono-runtime -- --test-threads=1`
- `timeout 180s cargo test -q -p yulang case_02 -- --test-threads=1`
- `timeout 300s cargo test -q -p yulang -- --test-threads=1`
- `timeout 240s bench/static_analysis_bench.sh --repeat 5 bench/nondet_20_discard.yu examples/showcase.yu`

## control VM shared marked apply markers

- marked callee の apply で、call/resume 用 marker 変換後の payload も
  `Rc<[ValueMarker]>` として扱うようにした。
  function-call marker 変換は既存の shared fast path を使い、continuation-call marker 変換にも
  identity fast path を追加した。
- これで `Value::Marked` apply path は、identity 変換なら marker list を `Vec` に戻さず、
  `with_shared_marker_frame` / `close_shared_scoped_result` へそのまま渡す。
- repeat 5 の `bench/nondet_20_discard.yu` release / no-cache:
  `runtime_execute` 36.4ms / 38.2ms / 36.3ms / 38.2ms / 37.1ms。
  shared pattern markers 後の 38.9ms / 38.8ms / 39.3ms / 38.4ms / 38.4ms より
  下側へ寄った。
- repeat 5 の `examples/showcase.yu` release / no-cache:
  `runtime_execute` 78.5ms / 76.9ms / 74.2ms / 73.5ms / 76.1ms。
  shared pattern markers 後の 81.2ms / 81.3ms / 83.6ms / 82.6ms / 80.6ms から
  さらに下がった。

確認:

- `cargo fmt --all`
- `cargo check -q -p control-vm -p yulang`
- `timeout 240s cargo test -q -p control-vm -p mono-runtime -- --test-threads=1`
- `timeout 180s cargo test -q -p yulang case_02 -- --test-threads=1`
- `timeout 300s cargo test -q -p yulang -- --test-threads=1`
- `timeout 240s bench/static_analysis_bench.sh --repeat 5 bench/nondet_20_discard.yu examples/showcase.yu`

## control VM reversed bind sequences

- `bind_pat_sequence` が `Vec::remove(0)` で先頭要素を取り出していたため、
  sequence bind の残りを reversed stack として保持し、以後は `pop()` で進めるようにした。
- `BindThen::Sequence` の field 名も `remaining_reversed` にして、保持順序の前提を見えるようにした。
- repeat 5 の `bench/nondet_20_discard.yu` release / no-cache:
  `runtime_execute` 37.4ms / 34.8ms / 35.6ms / 34.9ms / 39.1ms。
  shared marked apply markers 後の 36.4ms / 38.2ms / 36.3ms / 38.2ms / 37.1ms と比べて
  同程度から少し下側。
- repeat 5 の `examples/showcase.yu` release / no-cache:
  `runtime_execute` 75.5ms / 74.2ms / 74.3ms / 75.5ms / 73.4ms。
  shared marked apply markers 後の 78.5ms / 76.9ms / 74.2ms / 73.5ms / 76.1ms より
  少し下側へ寄った。

確認:

- `cargo fmt --all -- --check`
- `cargo check -q -p control-vm -p yulang`
- `timeout 240s cargo test -q -p control-vm -p mono-runtime -- --test-threads=1`
- `timeout 180s cargo test -q -p yulang case_02 -- --test-threads=1`
- `timeout 300s cargo test -q -p yulang -- --test-threads=1`
- `timeout 240s bench/static_analysis_bench.sh --repeat 5 bench/nondet_20_discard.yu examples/showcase.yu`

## control VM lazy marker request-close offset

- active marker scope の touch counter を consume / extend / request-close に分けた。
  実装前の単発測定では、request-close の outer scope 補正が
  `bench/nondet_20_discard.yu` で 716080、`examples/showcase.yu` で 571492 あり、
  `marker_scope_frame_touches` の過半を占めていた。
- request が innermost marker scope を抜けるたびに外側 scope の
  `frames_remaining` を全更新するのをやめ、resume 中の `request_close_offset` で
  遅延適用するようにした。
  完了判定と次の request split だけ `frames_remaining - request_close_offset` を見る。
- repeat 5 の `bench/nondet_20_discard.yu` release / no-cache:
  `runtime_execute` 36.7ms / 34.3ms / 33.9ms / 34.6ms / 35.9ms。
  `marker_scope_frame_touches` は 1287567 から 571487 へ低下し、
  `marker_scope_request_close_touches` は 0。
- repeat 5 の `examples/showcase.yu` release / no-cache:
  `runtime_execute` 74.0ms / 75.3ms / 78.0ms / 79.3ms / 73.8ms。
  `marker_scope_frame_touches` は 1024079 から 452587 へ低下し、
  `marker_scope_request_close_touches` は 0。
  実時間は揺れの範囲だが、request-close の O(depth) 更新は hot path から消えた。

確認:

- `cargo fmt --all -- --check`
- `cargo check -q -p control-vm -p yulang`
- `bash -n bench/static_analysis_bench.sh`
- `timeout 240s cargo test -q -p control-vm -p mono-runtime -- --test-threads=1`
- `timeout 180s cargo test -q -p yulang case_02 -- --test-threads=1`
- `timeout 300s cargo test -q -p yulang -- --test-threads=1`
- `timeout 300s bench/static_analysis_bench.sh --repeat 5 bench/nondet_20_discard.yu examples/showcase.yu`
