# 2026-06-24

## Research consultation brief

`notes/research/consultation/technical-brief.md` を、内部用語から始める構成から、
AE/H の直感的な例を先に見せる構成へ組み直した。

- 冒頭を `flip/all_paths` にし、`act`、`catch`、operation request、限定継続 `k` を先に説明する。
- `all_paths : 'a [flip; 'b] -> ['b] list 'a` を、`flip` だけを処理し residual row を残す型として説明する。
- `flip` と `amount` を重ねる例を `notes/research/consultation/examples/nested_handler_contracts.yu` として追加した。
- `sub` の callback hygiene 例を本文に置き、callback 由来の effect を局所 handler が偶然捕捉しないことを説明する。
- `compose` まで進んでから hidden weighted scheme と public projection の問題を導入する構成にした。
- `docs/effect-inference-brief.md` は旧 1 ページ版のままなので、研究相談で使う場合は同じ構成へ作り直す。

確認:

- `timeout 120s cargo run -q -p yulang -- run-control-std notes/research/consultation/examples/nested_handler_contracts.yu`
- `timeout 120s cargo run -q -p yulang -- dump-poly-std notes/research/consultation/examples/nested_handler_contracts.yu | rg 'all_paths|total_amount|flip.coin|amount.coin'`
- `timeout 120s cargo test -q -p control-vm runs_stack_handler_hygiene_to_outer_handler -- --test-threads=1`
