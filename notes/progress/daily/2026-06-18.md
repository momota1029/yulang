# 2026-06-18

## infer timing baseline commit

- 2026-06-17 から続いていた infer / constraint timing counter を先にコミットした。
- commit: `d51f8593 Add infer timing counters`

## infer small optimization loop

- `instantiate_use` で `Def::Let.scheme` を丸ごと clone してから instantiate していたため、
  `poly.defs` 上の scheme を直接参照するようにした。
  `analysis.instantiate_clone_scheme` の内訳には出ない外側 clone なので、wall/infer 全体で見る。
- `quantify_component` の finalize loop で def parent map を def ごとに作り直していたため、
  component ごとに一回だけ作るようにした。`q_fin` は 13ms 前後から 5ms 前後へ低下。
- role resolve prepass は reachable role が空でも `compact` を clone していたため、空なら `None` で返し、
  resolver を呼ばないようにした。効果は小さめで、`g_rrole` が少し下がる程度。
- constraint machine は `Pos::Bot <: _` / `_ <: Neg::Top` を enqueue 後の `step_subtype` で no-op にしていたため、
  enqueue 前に捨てるようにした。`showcase` では `constraint.work_items` が 42159 から 37994 へ低下。
- no-op / duplicate subtype constraint は public entry 側で empty drain を起こしていたため、
  `enqueue_subtype` が実際に queue へ積めたかを返し、何も積まなかった場合は drain しないようにした。
  `showcase` では `constraint.drains` が 18118 から 15020 へ低下。

## 確認（infer small optimization loop）

- `cargo fmt --all -- --check`
- `timeout 90s cargo check -q -p infer -p yulang`
- `timeout 240s bench/static_analysis_bench.sh --repeat 5 --infer-only examples/showcase.yu`
  - scheme clone 削除 + parent map 再利用後:
    `infer` 309〜324ms、`q_fin` 4.6〜5.4ms、`constraint.work_items` 42159。
  - role empty clone 回避後:
    `g_rrole` 25.4〜26.9ms 付近。効果は小さめ。
  - Bot/Top no-op early return 後:
    `constraint.work_items` 37994、`constraint.subtype_work_items` 37932。
- `timeout 240s bench/static_analysis_bench.sh --repeat 5 --infer-only examples/showcase.yu`
  - empty drain skip 後:
    `infer` 305〜321ms、`constraint.drain` 102〜109ms、`constraint.drains` 15020、
    `constraint.work_items` 37994。
- `timeout 180s cargo test -q -p infer --lib -- --test-threads=1`
- `timeout 180s cargo test -q -p yulang --lib -- --test-threads=1`
- `git diff --check`

## source collection module discovery skip

- source collection は実ファイルを二度 `read_to_string` してはいなかった。
  `Collector` が読んだ `source` を `CollectedSource` として後段へ渡し、`sources::load` はその in-memory source を読む。
- ただし collect 側の `discover_module_loads` は module load request の有無を見るだけなのに、
  各ファイルを full parse していた。`sources::load` も header parse + full parse を行うため、collect/load の時間が似ていた原因は二重 read ではなく二重 parse。
- module load request は `mod foo;` / `use mod foo::*` だけなので、source text に `"mod"` が無いファイルでは parse せず空を返すようにした。
  false positive は parse するだけで、false negative は keyword が ASCII `mod` である限り起きない。
- `showcase` infer-only repeat 5 では `collect` が 39〜41ms から 8ms 前後へ低下。

## 確認（source collection module discovery skip）

- `cargo fmt --all -- --check`
- `timeout 90s cargo check -q -p yulang`
- `timeout 240s bench/static_analysis_bench.sh --repeat 5 --infer-only examples/showcase.yu`
- 一時的に `sources::load` の subphase timing を入れて確認した。
  `showcase` の 36 files では `load total` 42.9ms のうち `full_parse` が 39.5ms。
  `header` は 2.8ms、`fixed_point` は 193us、`table` は 291us、`module_loads` は 55us。
- `read_header` の keyword skip と full parse 後の module-load scan skip も試したが、
  `load` は 41.9〜45.5ms 程度で改善が見えなかったため棄却した。
  残る `load` は局所テーブル構築ではなく parser / std source artifact cache 側で見る。

## embedded std loaded-prefix cache

- CLI の `.yuir` / `.yuvm` cache は source collection 後の whole source-set artifact cache なので、
  `check` や playground の in-memory embedded std route では `sources::load` の full parse が残っていた。
- `sources::load_with_loaded_prefix(prefix, suffix)` を追加した。
  `prefix` は suffix に依存しない dependency-closed な loaded file set という不変条件を持ち、
  suffix の op table は prefix の header / export surface から組み直す。
  これは将来の source-SCC compiled-unit import の小さい前段として扱う。
- `yulang::source` は embedded full std / embedded playground std の loaded prefix を thread-local に cache し、
  root source だけを suffix として parse する入口を追加した。
  既存の `CollectedSource` route はそのまま残し、FS std route や CLI artifact cache の意味は変えていない。
- wasm playground の colorize と compact playground std run path は、新しい loaded-prefix route を使う。
  full std fallback と no-std route は既存 route のまま。
- 一時 example で旧 route と新 route を同一プロセス比較した。
  - load-only（120 iter, debug）: 旧 `168.410ms/iter`、新 `3.169ms/iter`、`53.147x`。
  - build poly まで（6 iter, debug）: 旧 `1169.368ms/iter`、新 `998.976ms/iter`、`1.171x`。
  - build 全体は lowering/infer が支配的なので改善は限定的。colorize / parse warm path では大きく効く。

## 確認（embedded std loaded-prefix cache）

- `timeout 90s cargo check -q -p sources -p yulang -p wasm`
- `cargo fmt --all -- --check`
- `timeout 120s cargo test -q -p sources --lib -- --test-threads=1`
- `timeout 180s cargo test -q -p yulang --lib -- --test-threads=1`
- `timeout 180s cargo test -q -p wasm --lib -- --test-threads=1`

## infer generalize role view reuse

- `generalize_root_with_prepasses` の内訳をさらに追えるようにした。
  - merge / subtype / cast / role restart 回数。
  - role input / reachable / coalesced / dominance / resolve-input 件数。
  - analysis work の種別別時間と件数。
  - unready role impl dependency scan の時間と件数。
- role resolve 用の簡約 view は、同じ iteration 内ですでに作った coalesced role constraints を再利用するようにした。
  `simplified_reachable_role_constraints` は test-only helper に残し、通常 path は reachable role compact を二重に走らせない。
- final role collection でも、最後の loop iteration で作った coalesced role constraints をそのまま使うようにした。
  break 時点で追加制約は発生していないため、直後の final roles で同じ compact/root から role view を作り直す必要がない。
- direct var-var merge path は、replay queue が空なら empty drain を起こさないようにした。
- 試したが残さなかったもの:
  - instantiate predicate が `Pos::Var` の場合に direct var-var path へ流す案。
    `showcase` では `i_vardir=0` で該当なしだったため棄却した。
- `showcase` infer-only repeat 3 では、final role reuse 後に `g_froles` が 9〜10ms 付近から 7ms 前後へ低下。
  `infer` は 288〜300ms 程度で、wall はノイズ込みだが少し改善傾向。
- `a_udep` は 3ms 前後で、`ApplySelectionResolution` の主因ではなかった。
  `w_asel` は `route_scc_events` 由来の quantify / instantiate と重なって見えている可能性が高い。

## 確認（infer generalize role view reuse）

- `cargo fmt --all -- --check`
- `timeout 120s cargo check -q -p infer -p yulang`
- `timeout 180s cargo test -q -p infer analysis::tests::case_02 -- --test-threads=1`
- `timeout 240s bench/static_analysis_bench.sh --repeat 3 --infer-only examples/showcase.yu`

## instantiate predicate direct lower-bound path

- `instantiate_subtype_predicate` / constraint replay の内訳を見るために counters を追加した。
  - instantiate predicate の root shape: var / stack / non-subtract / fun / con / other。
  - lower / upper bound 追加時の replay input / enqueue / var-var replay。
  - direct var-var replay 時の replay input / enqueue / empty replay skip。
- `showcase` では instantiate predicate は `Fun` 853、`Con` 43、`Stack` 4 で、
  `Var` / `NonSubtract` は 0 だった。
- predicate root が直接 `lower <: target-var` の lower bound 追加へ落とせる形なら、
  `Neg::Var(target)` を allocate して `subtype` queue 経由で drain する代わりに、
  `add_lower_bound(target, lower, empty)` を直接呼ぶ入口を追加した。
  対象は `Con` / `Fun` / record / polyvariant / tuple / row。
- `Stack` / `NonSubtract` / `Union` / `Var` は直接化しない。
  `Stack` と `NonSubtract` は weight / pre-pop の意味を持ち、`Union` は分配が必要で、
  `Var` は lower/upper 両側の var-var bound 登録が必要なため。
- direct var-var replay は、両側 weight が空で transitive closure を作らない replay を clone 前に除外するようにした。
  これは既存の「empty var-var replay は閉じない」挙動を、queue へ入れる前に早めただけ。

## 確認（instantiate predicate direct lower-bound path）

- `cargo fmt --all -- --check`
- `timeout 120s cargo check -q -p infer -p yulang`
- `timeout 180s cargo test -q -p infer analysis::tests::case_02 -- --test-threads=1`
- `timeout 240s bench/static_analysis_bench.sh --repeat 5 --infer-only examples/showcase.yu`
  - `analysis.instantiate_direct_lower_predicates`: 896。
  - `constraint.subtype_calls`: 17584 から 16688 へ低下。
  - `constraint.work_items`: 37994 から 37135 へ低下。
  - `constraint.drains`: 15007 前後から 14919 へ低下。
  - `analysis.instantiate_subtype_predicate`: warm sample で 43.8〜48.9ms 付近。
  - best warm sample は `infer` 287.7ms、`constraint.drain` 95.5ms、`analysis.instantiate_subtype_predicate` 43.8ms。

## bound replay action snapshot

- `add_lower_bound` / `add_upper_bound` は、既存 upper/lower bound 全体を clone してから replay していた。
  replay を適用すると同じ bounds table が変化しうるため snapshot 自体は必要だが、
  snapshot は元 bound の clone ではなく、実際に適用する replay action だけでよい。
- lower/upper replay と direct var-var replay を `BoundReplayAction` の snapshot へ変えた。
  `ConstraintWeights` は replay 合成済みのものだけを持ち、元 bound row の clone を避ける。
- `showcase` infer-only repeat 5 で同じ HEAD から差分あり/なしを測った。
  - 元実装 warm sample: `infer` 298〜310ms、`constraint.drain` 99〜104ms。
  - action snapshot 版 warm sample: `infer` 278ms 前後、`constraint.drain` 91〜94ms。
  - constraint work 件数は同じなので、再計算数ではなく replay snapshot の clone / allocation cost を減らせたと見る。

## apply selection target timing

- `analysis.work_apply_select` が 120ms 前後で残っているため、selection target ごとの timing / item counters を追加した。
- `showcase` infer-only repeat 1 では `ApplySelectionResolution` 217 items の内訳は次の通り。
  - record field: 0 items / 0us。
  - nominal method: 116 items / 28.5ms。
  - effect method: 18 items / 1.6ms。
  - typeclass method: 83 items / 92.2ms。
- 次に見るべき太い場所は、typeclass method selection 自体ではなく、
  typeclass selection apply 後の SCC routing / method dependency resolved / instantiate の再走行だと考える。
