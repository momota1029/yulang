# 2026-09-07

## Successor typed output/recovery O1

- Completed the Authoritative O1 output shell. One private `RewriteOutput`
  owns the sole Rowan builder and exposes only inline forwarding operations;
  `RewriteIn`, Item/trivia/fragment emission, and isolated harnesses now use
  that surface. No Deref, raw builder accessor, typed recovery state, or public
  dispatch was added.
- Audited all 23 production `Option`-returning procedures that accept
  `RewriteIn`. Fourteen nontrivial entries have callable rejection witnesses;
  nine selector/source-only/lexical-transaction helpers have exact static
  proofs. Structural `GreenNode` comparison against independent seeded controls
  detects empty-node mutations that rendered-text comparison would miss.
- One repair round added the structural/exhaustive rollback evidence and inline
  annotations requested by compiler and performance review. Both delta reviews
  are clean. Producer verification covered 195 focused tests, `cargo check -p
  yu-syntax`, full formatting, and diff checks; the primary reran the four O1
  output tests. The package/workspace suites remain budgeted for O4/O7, and no
  timing measurement was needed. O2 evidence-kernel construction is next.

## Successor typed output/recovery O2

- Completed the Authoritative O2 evidence kernel: validated ID-less recovery
  drafts, fresh and borrowed-frozen diagnostic sequencing, committed recovery
  storage in the single rewrite output, checked Item extents, typed Missing,
  one-Item Error, total malformed Error runs, and the exact literal recovery
  vocabulary. No grammar owner or public dispatch was migrated.
- Item extents use only the threaded successor coordinate and owned Item bytes.
  One checked traversal accumulates total and already-emitted leading bytes;
  fragment-carrier ranges, source reread, replay, and a second builder remain
  forbidden. The sealed Error-run surface permits owner-selected token
  retagging without exposing node, builder, or recovery operations.
- Review round one found and repaired a doubled leading scan, incomplete frozen
  complexity wording, and the missing Use-compatible
  `TokenKind::Operator -> SyntaxKind::Unknown` path. Architecture adjudication
  clarified that node/record atomicity is a normal-return guarantee: an
  invariant panic invalidates the current output rather than authorizing
  unwind reuse. Compiler and performance delta reviews are clean.
- Producer checks covered 13 recovery-output, 4 output, 45
  lexical/current-item, and 11 Use tests plus `cargo check -p yu-syntax`, full
  formatting, and diff checks. The primary reran all 13 recovery-output tests,
  the package check, formatting, and diff checks. Existing warnings were
  unchanged. The O4/O7 broad suites remain deferred and timing usage is zero.
  O3a TypeExpression/PolymorphicVariant owner migration is next.

## Structured-recovery reservation design

- The O3a Type/PV inventory mapped 96 raw recovery sites and found one existing
  PV CST shape that cannot use the O2 lexical-only Error-run capability:
  `PolymorphicVariantTag > Error > TypeExpression`. A nested type recovery
  requires the outer tag-name record/ID to be reserved before recursion.
- Recorded a narrow proposal using recovery-only ordered slots, a borrowed
  frozen record, an O(1) active-parent link, LIFO completion, and one final
  O(R) move into the public record vector. Valid input retains zero recovery
  allocation and no extra traversal; finish may temporarily hold O(R) slots
  plus O(R) final records without cloning record evidence.
- M3 compiler/recovery, specification, and performance review closed in two
  rounds. Repairs covered frozen-exhausted fresh IDs, a real recursive PV
  fixture, complete Gate3b tuples, exact O3a placement, and peak resource
  accounting. All delta reviews are clean and timing usage is zero. The
  proposal is Reviewed and awaits user approval before implementation.

- The user approved the amendment on 2026-09-07. Its status is now
  Authoritative; the output prerequisite and its one narrow PV O3a construction
  checkpoint are authorized. The remaining Type/PV migration, joint O4
  certification, and public cutover are still separate work.

## Structured-recovery output prerequisite and extent validation

- Completed the private output prerequisite: ordered `Reserved`/`Complete`
  recovery slots, borrowed frozen records, fresh/frozen ID reservation, an
  O(1) LIFO active link, and one consuming final extraction into the public
  recovery vector. No grammar caller or public dispatch changed.
- A compiler/recovery delta found that caller-supplied structured Error ranges
  lacked an independent emitted-extent check. The separate extent-validation
  addendum was reviewed by compiler/recovery, specification, and performance,
  then user-approved. It seals start derivation to the moved Item plus successor
  origin, rejects un-emitted physical leading, and checks each reservation's
  explicit range length against its byte-counter delta. It retains no source,
  Item range, replay state, event buffer, or second builder.
- The one additional valid-input cost is a checked byte-length increment in the
  inline token forwarder. Performance review found no allocation, traversal,
  clone, scan, lookup, dynamic dispatch, or asymptotic change; timing budget
  remains zero.
- Primary verification: 17 `recovery_output` tests, 4 output-shell tests,
  `cargo check -p yu-syntax`, `cargo fmt --all -- --check`, and `git diff
  --check` all passed. Existing compiler warnings were unchanged. Broad
  package/workspace tests remain deferred to the O4/O7 barrier. The next scope
  is only the first PV wrong-kind tag-name caller checkpoint.

## Structured-recovery prerequisite ordering O3a checkpoint

- Completed the Authoritative prerequisite-ordering checkpoint: the NT-8
  malformed PV prefix now publishes its sealed Error-run record, the explicit
  four-owner Type delimiter route publishes a typed Missing only for an
  outer-owned `ParenthesizedTypeGroup` close, and the approved structured
  PV tag-name caller follows those two producers. The decisive `:{@ (A}`
  fixture has the exact fresh/frozen three-record order and IDs `0..2`; the
  recursive TagName fixture retains outer-before-inner LIFO completion.
- Parenthesized mismatched closes preserve the prior three-way ownership:
  outer-owned closes publish the typed close record, explicit caller-owned
  closes retain their raw Missing CST slots, and local mismatches remain raw
  unconsumed handoffs. Recovery-aware test harness finalization is test-only;
  no production API, dispatch, source retention, replay, or legacy rollback
  carrier was added.
- Initial M3 compiler/spec review found an explicit-caller Missing regression,
  test-harness finalization failures, and incomplete rollback/frozen evidence.
  One batched repair restored all four classifier paths (including separator
  continuation), added independent successor-boundary controls, and verified
  frozen cursor/slot immutability on rejection. Compiler and specification
  delta review were clean. Static performance review found no material
  valid-path or asymptotic change, so timing usage remains zero.
- Primary verification passed: 68 focused Type/PV tests, 25 normalized-Type
  tests, the Pattern caller-close and TypeDeclaration nested-owner regressions,
  `cargo check -p yu-syntax`, `cargo fmt --all -- --check`, and `git diff
  --check`. Existing warnings are unchanged. Broad O4/O7 suites, the remaining
  Type/PV recovery causes, public dispatch, and legacy cutover remain deferred.

## O3a T1 required Type-primary checkpoint

- Migrated only the callee-owned nonempty malformed required-Type primary run
  in `required_type_expr_inner_normalized` to the sealed committed Error-run
  surface. It emits one `Type(Primary)` Error record with primary expected
  `TypeExpression`, committed-rule source flag, exact run extent, and O2's
  exhaustive per-Item unexpected evidence in lexical order. Raw CST token
  kinds, retry, boundaries, and Item/trivia ownership remain unchanged.
- Initial M3 review caught two apparent ownership defects. The PendingBoundary
  range concern and a shared-core Type-primary-Missing publication were
  adjudicated against the legacy oracle and Gate3b: T1 owns only the malformed
  Error; it remains callee-owned across current callers. The two zero-width
  Missing branches were restored to raw CST because their caller-specific P/D/V
  roles are separate future migration cells. No Type-primary Missing record is
  published by this checkpoint.
- Evidence now covers fresh/frozen `@A`, multi-Item Error ordering and CST,
  fresh/frozen `@ ,A` boundary handoff with pending comma leading/remainder and
  `primary_found == false`, raw comma/EOF/fence Missing controls, and Pattern
  annotation separation (`x:` raw/no record; `x: @A` one nested T1 Error).
  RB-T uses an independent preseeded approved close record to prove optional
  Type probe rejection preserves output, diagnostic cursor, input, and unit
  mark state.
- Compiler/recovery and specification delta reviews are clean. Primary checks:
  75 focused TypeExpression tests, 25 normalized-Type tests, one Pattern
  annotation delegation regression, `cargo check -p yu-syntax`, full format,
  and diff checks all passed with only existing warnings. Static performance
  assessment: recovery-only work, no valid-path allocation/traversal/asymptotic
  change; timing budget remains zero. Broad O4/O7 suites, T2–T7/PV1,
  caller-owned Missing migration, public dispatch, and legacy cutover remain
  deferred.
