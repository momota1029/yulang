# 2026-09-07

## Successor typed output/recovery O1

- Completed the Authoritative O1 output shell. One private `RewriteOutput`
  owns the sole Rowan builder and exposes only inline forwarding operations;
  `RewriteIn`, Item/trivia/fragment emission, and isolated harnesses now use
  that surface. No Deref, raw builder accessor, typed recovery state, or public
  dispatch was added.
- Audited all 23 production `Option`-returning procedures that accept
  `RewriteIn`. Fourteen nontrivial entries have callable rejection witnesses;
  nine selector/source-only/lexical-transaction helpers have exact static
  proofs. Structural `GreenNode` comparison against independent seeded controls
  detects empty-node mutations that rendered-text comparison would miss.
- One repair round added the structural/exhaustive rollback evidence and inline
  annotations requested by compiler and performance review. Both delta reviews
  are clean. Producer verification covered 195 focused tests, `cargo check -p
  yu-syntax`, full formatting, and diff checks; the primary reran the four O1
  output tests. The package/workspace suites remain budgeted for O4/O7, and no
  timing measurement was needed. O2 evidence-kernel construction is next.

## Successor typed output/recovery O2

- Completed the Authoritative O2 evidence kernel: validated ID-less recovery
  drafts, fresh and borrowed-frozen diagnostic sequencing, committed recovery
  storage in the single rewrite output, checked Item extents, typed Missing,
  one-Item Error, total malformed Error runs, and the exact literal recovery
  vocabulary. No grammar owner or public dispatch was migrated.
- Item extents use only the threaded successor coordinate and owned Item bytes.
  One checked traversal accumulates total and already-emitted leading bytes;
  fragment-carrier ranges, source reread, replay, and a second builder remain
  forbidden. The sealed Error-run surface permits owner-selected token
  retagging without exposing node, builder, or recovery operations.
- Review round one found and repaired a doubled leading scan, incomplete frozen
  complexity wording, and the missing Use-compatible
  `TokenKind::Operator -> SyntaxKind::Unknown` path. Architecture adjudication
  clarified that node/record atomicity is a normal-return guarantee: an
  invariant panic invalidates the current output rather than authorizing
  unwind reuse. Compiler and performance delta reviews are clean.
- Producer checks covered 13 recovery-output, 4 output, 45
  lexical/current-item, and 11 Use tests plus `cargo check -p yu-syntax`, full
  formatting, and diff checks. The primary reran all 13 recovery-output tests,
  the package check, formatting, and diff checks. Existing warnings were
  unchanged. The O4/O7 broad suites remain deferred and timing usage is zero.
  O3a TypeExpression/PolymorphicVariant owner migration is next.

## Structured-recovery reservation design

- The O3a Type/PV inventory mapped 96 raw recovery sites and found one existing
  PV CST shape that cannot use the O2 lexical-only Error-run capability:
  `PolymorphicVariantTag > Error > TypeExpression`. A nested type recovery
  requires the outer tag-name record/ID to be reserved before recursion.
- Recorded a narrow proposal using recovery-only ordered slots, a borrowed
  frozen record, an O(1) active-parent link, LIFO completion, and one final
  O(R) move into the public record vector. Valid input retains zero recovery
  allocation and no extra traversal; finish may temporarily hold O(R) slots
  plus O(R) final records without cloning record evidence.
- M3 compiler/recovery, specification, and performance review closed in two
  rounds. Repairs covered frozen-exhausted fresh IDs, a real recursive PV
  fixture, complete Gate3b tuples, exact O3a placement, and peak resource
  accounting. All delta reviews are clean and timing usage is zero. The
  proposal is Reviewed and awaits user approval before implementation.

- The user approved the amendment on 2026-09-07. Its status is now
  Authoritative; the output prerequisite and its one narrow PV O3a construction
  checkpoint are authorized. The remaining Type/PV migration, joint O4
  certification, and public cutover are still separate work.

## Structured-recovery output prerequisite and extent validation

- Completed the private output prerequisite: ordered `Reserved`/`Complete`
  recovery slots, borrowed frozen records, fresh/frozen ID reservation, an
  O(1) LIFO active link, and one consuming final extraction into the public
  recovery vector. No grammar caller or public dispatch changed.
- A compiler/recovery delta found that caller-supplied structured Error ranges
  lacked an independent emitted-extent check. The separate extent-validation
  addendum was reviewed by compiler/recovery, specification, and performance,
  then user-approved. It seals start derivation to the moved Item plus successor
  origin, rejects un-emitted physical leading, and checks each reservation's
  explicit range length against its byte-counter delta. It retains no source,
  Item range, replay state, event buffer, or second builder.
- The one additional valid-input cost is a checked byte-length increment in the
  inline token forwarder. Performance review found no allocation, traversal,
  clone, scan, lookup, dynamic dispatch, or asymptotic change; timing budget
  remains zero.
- Primary verification: 17 `recovery_output` tests, 4 output-shell tests,
  `cargo check -p yu-syntax`, `cargo fmt --all -- --check`, and `git diff
  --check` all passed. Existing compiler warnings were unchanged. Broad
  package/workspace tests remain deferred to the O4/O7 barrier. The next scope
  is only the first PV wrong-kind tag-name caller checkpoint.

## Structured-recovery prerequisite ordering O3a checkpoint

- Completed the Authoritative prerequisite-ordering checkpoint: the NT-8
  malformed PV prefix now publishes its sealed Error-run record, the explicit
  four-owner Type delimiter route publishes a typed Missing only for an
  outer-owned `ParenthesizedTypeGroup` close, and the approved structured
  PV tag-name caller follows those two producers. The decisive `:{@ (A}`
  fixture has the exact fresh/frozen three-record order and IDs `0..2`; the
  recursive TagName fixture retains outer-before-inner LIFO completion.
- Parenthesized mismatched closes preserve the prior three-way ownership:
  outer-owned closes publish the typed close record, explicit caller-owned
  closes retain their raw Missing CST slots, and local mismatches remain raw
  unconsumed handoffs. Recovery-aware test harness finalization is test-only;
  no production API, dispatch, source retention, replay, or legacy rollback
  carrier was added.
- Initial M3 compiler/spec review found an explicit-caller Missing regression,
  test-harness finalization failures, and incomplete rollback/frozen evidence.
  One batched repair restored all four classifier paths (including separator
  continuation), added independent successor-boundary controls, and verified
  frozen cursor/slot immutability on rejection. Compiler and specification
  delta review were clean. Static performance review found no material
  valid-path or asymptotic change, so timing usage remains zero.
- Primary verification passed: 68 focused Type/PV tests, 25 normalized-Type
  tests, the Pattern caller-close and TypeDeclaration nested-owner regressions,
  `cargo check -p yu-syntax`, `cargo fmt --all -- --check`, and `git diff
  --check`. Existing warnings are unchanged. Broad O4/O7 suites, the remaining
  Type/PV recovery causes, public dispatch, and legacy cutover remain deferred.

## O3a T1 required Type-primary checkpoint

- Migrated only the callee-owned nonempty malformed required-Type primary run
  in `required_type_expr_inner_normalized` to the sealed committed Error-run
  surface. It emits one `Type(Primary)` Error record with primary expected
  `TypeExpression`, committed-rule source flag, exact run extent, and O2's
  exhaustive per-Item unexpected evidence in lexical order. Raw CST token
  kinds, retry, boundaries, and Item/trivia ownership remain unchanged.
- Initial M3 review caught two apparent ownership defects. The PendingBoundary
  range concern and a shared-core Type-primary-Missing publication were
  adjudicated against the legacy oracle and Gate3b: T1 owns only the malformed
  Error; it remains callee-owned across current callers. The two zero-width
  Missing branches were restored to raw CST because their caller-specific P/D/V
  roles are separate future migration cells. No Type-primary Missing record is
  published by this checkpoint.
- Evidence now covers fresh/frozen `@A`, multi-Item Error ordering and CST,
  fresh/frozen `@ ,A` boundary handoff with pending comma leading/remainder and
  `primary_found == false`, raw comma/EOF/fence Missing controls, and Pattern
  annotation separation (`x:` raw/no record; `x: @A` one nested T1 Error).
  RB-T uses an independent preseeded approved close record to prove optional
  Type probe rejection preserves output, diagnostic cursor, input, and unit
  mark state.
- Compiler/recovery and specification delta reviews are clean. Primary checks:
  75 focused TypeExpression tests, 25 normalized-Type tests, one Pattern
  annotation delegation regression, `cargo check -p yu-syntax`, full format,
  and diff checks all passed with only existing warnings. Static performance
  assessment: recovery-only work, no valid-path allocation/traversal/asymptotic
  change; timing budget remains zero. Broad O4/O7 suites, T2–T7/PV1,
  caller-owned Missing migration, public dispatch, and legacy cutover remain
  deferred.

## O3a T2a ArrowRhs capability finding

- The first T2a candidate migrated ArrowRhs's two Missing sites and contiguous
  Error run, but M3 review rejected it before integration. Its abstract
  PendingBoundary Missing reconstructed an Item extent (`8..8`) instead of the
  required boundary coordinate (`9..9`). More importantly, the authoritative
  `A ->@ B` record is `Type(ArrowRhs)` Error `4..6` while its Error CST is only
  `@` (`4..5`) and the same-line space remains owned by valid retry `B`.
- O2's four-method sealed Error-run derives its one record extent only from
  emitted Error-node segments. It cannot include that retry-leading byte
  without either publishing the wrong `4..5` record, reparenting CST trivia,
  consuming/splitting the retry Item, or using forbidden raw range/source
  state. Architecture adjudicated this as a genuine durable capability
  mismatch, not a test gap.
- The candidate was manually restored only in its two owned files with
  `apply_patch`; no checkout/reset, commit, or push occurred. The worktree is
  clean at T1 commit `51d3d033`.
- Recorded Proposed
  `2026-09-07-successor-retry-leading-diagnostic-extent-amendment.md`: one
  fifth sealed Error-run operation may borrow an immediate valid retry Item and
  extend only the committed record through its contiguous same-line leading;
  it neither emits nor mutates that Item, so Error CST and Item ownership stay
  unchanged. It adds separate private node/record extents, forbids all newline,
  boundary, EOF, caller/outer, payload, prefix, source, replay, and arbitrary
  range paths, and authorizes no implementation pending M3 review and explicit
  user approval.
- M3 compiler/recovery, specification, and performance review completed with
  one batched repair and clean delta. The reviewed design now preserves the
  legacy one whole-run `OtherCharacter` fact at `record_extent`, makes sealing
  one-shot and terminal, centrally rejects newline/CRLF, foreign carrier,
  boundary/EOF, gap, payload, double-seal, and post-seal operations, pins the
  embedded `R({type T = A ->@ B})` tuple at `19..21` and the fenced Missing at
  `9..9`, and accounts for recovered-only O(L) retry-leading work (zero valid
  path work, zero timing processes). User approval remains the only blocker.

- The user approved the reviewed retry-leading diagnostic-extent amendment.
  Its fifth sealed terminal operation and only the T2a ArrowRhs owner slice
  may now proceed. T2b PathSegment, all later Type/PV causes, caller-owned
  Missing, public dispatch, and legacy cutover remain excluded.
- User selected option 1 for the T2a embedded-evidence contradiction: preserve
  the complete local successor ArrowRhs construction contract and defer only
  actual embedded AST/direct fact equality to O6 successor-Yumark convergence.
  The Draft timing correction records the narrow scope; legacy AST owner
  adoption, grammar/session publication, and public dispatch remain excluded.

- M3 compiler/recovery, specification, and regression review accepted the
  timing correction after one authority repair.  It is Authoritative with the
  user's option-1 approval: O4 may close only T2a's local successor subproof;
  the T2 matrix row remains Open until O6 proves real successor-Yumark
  AST/direct equality.  Matrix §7 remains an independent fixed control.

- The authorized legacy Yumark baseline then disproved the retry-leading
  amendment's CST premise: legacy embedded direct has `Error("@ ")` at
  `19..21`, while the successor candidate has `Error("@")` plus retry-owned
  leading space.  Compiler/recovery and specification adjudication found that
  neither the timing correction nor current rewrite authority supersedes exact
  CST compatibility.  T2a is therefore blocked with no construction credit;
  the candidate remains unstaged pending a user-approved topology decision.

- User selected the one-cell successor-topology decision.  The Draft T2a
  embedded CST-topology amendment preserves the recovery tuple and matrix
  fact equality while explicitly adopting successor `Error("@")` plus
  retry-owned space for this cell only; M3 review remains pending.

- M3 compiler/recovery, specification, and regression review accepted the
  one-cell topology amendment after one authority repair and clean delta.
  It is Authoritative with the user's option-1 approval: only the U+0020 byte
  in local `A ->@ B` and its mapped witness receive the successor CST delta;
  O3 local Rowan offsets remain distinct from global records, and O6/O7 must
  assert exact whitespace parentage.  All other retry-leading controls and
  owners remain unchanged.

## O3a T2a ArrowRhs local construction complete

- Completed only the authorized ArrowRhs owner slice: both typed Missing
  branches now use the existing exact PendingBoundary coordinate rule, and the
  contiguous malformed Error run uses the fifth sealed Error-run operation to
  extend its record/evidence through one eligible retry-leading suffix.  The
  operation is terminal, source-free, recovery-only, and leaves the borrowed
  retry Item unchanged.
- The successor contract is pinned for the exact `A ->@ B` one-cell decision:
  Error CST `@` at `4..5`, retry-owned space `5..6`, RHS `B` at `6..7`, and
  one Type(ArrowRhs) Error record plus whole-run `OtherCharacter` at `4..6`.
  The shifted local successor run proves global record/evidence `19..21` and
  arithmetic `19..20`/`20..21`/`21..22`; the distinct legacy embedded baseline
  keeps Error CST `@ ` at `19..21` and asserts its own parentage.
- M3 compiler/recovery, specification, and regression implementation-delta
  review are clean.  Primary verification passed: 86 focused TypeExpression
  tests, 20 recovery-output tests, the one legacy Yumark baseline, `cargo
  check -p yu-syntax`, six-file rustfmt check, and `git diff --check`.
  Existing compiler warnings are unchanged.  No broad O4/O7 suite ran and the
  static recovery-only cost remains `O(L)` with zero timing processes.
- O4 can close only T2a's local direct/CST/Item/frozen/RB-T subproof.  The
  aggregate T2 matrix row remains Open until O6 runs real successor Yumark and
  proves AST/direct fact equality plus the successor topology; public cutover
  remains O7 work.  T2b PathSegment receives no credit from this result.
- A separate known T1 harness residual remains: filtered test
  `derives_normalized_recovers_role_and_via_slots_before_the_fence` finalizes a
  recovery-producing builder with raw `finish()`.  Its later isolated repair
  must use `finish_with_recoveries()` and assert the existing Type(Primary)
  record; no T2a change may hide, discard, or weaken it.

## T1 derives harness residual resolved

- Repaired only `rewrite/tests/derives.rs`.  The one recovery-producing
  derives row now uses recovery-aware finalization and asserts the complete
  existing singleton Type(Primary) Error record at `6211..6213`; the second,
  raw Via Error remains CST-only and an extra typed record fails the assertion.
  Every non-record derives harness path still uses raw `finish()`, so it keeps
  rejecting unexpected publication rather than silently discarding it.
- Pre-write specification audit and post-write regression audit are clean.
  The focused derives module has 4 passing tests; file rustfmt and diff checks
  pass.  No production parser, output API, session, grammar, Yumark, frozen,
  or public-dispatch code changed.  No broad suite or timing measurement ran.

## T2b PathSegment architecture re-entry

- T2b preflight mapped the raw successor PathSegment Missing/Error sites and
  found three durable contradictions: current successor retries `A::@ B`
  inside the path while legacy ends the path and lets `B` become outer
  TypeApply; block-comment retry leading must be partially emitted to preserve
  legacy continuation; and source-free Item emission cannot reproduce legacy's
  one Unknown token for `@/*x*/` without an explicit narrow token-child delta.
  The legacy embedded AST adapter also has no PathSegment fact, so its direct
  record cannot certify matrix equality before O6.
- Recorded Draft
  `2026-09-07-successor-t2b-pathsegment-recovery-amendment.md`.  It proposes
  one Type(PathSegment)-only sixth sealed Error-run operation, the exact
  continuation table and comment-token boundary, and T2b-local O3/O6 timing.
  It is not implementation authority.  M3 compiler/specification/performance
  review and user approval remain required; all code and expected output stay
  unchanged while that gate is open.

- M3 review found and repaired the Draft's initial over-narrow CST exception,
  missing all-or-nothing sixth-operation transaction, incomplete authority
  locator, newline/boundary priority gaps, and unnamed comment O6 witness.
  The Reviewed contract now covers only physical direct children emitted by the
  T2b Error run, requires legacy baselines for every named tokenization cell,
  distinguishes fifth and sixth completion modes, and names base/multi-item/
  comment real-successor O6 controls.  Static performance review proves a
  constant number of recovery-only `O(L)` scans plus one emission pass; timing
  remains zero.  User approval is the sole remaining implementation blocker.

- User selected option 1.  The amendment is now Authoritative: only its
  bounded T2b PathSegment construction may proceed.  The implementation must
  preserve the recorded owner/token/continuation limits, and O6 remains the
  first gate allowed to assert actual successor-Yumark AST/direct equality.

## O3a T2b PathSegment local construction complete

- Completed only the authorized `Type(PathSegment)` slice: the three accepted
  `TypePathTail` Missing branches now publish exact typed records, and the
  malformed PathSegment Error run publishes its one exact Error record while
  preserving the legacy continuation table.  The sixth sealed operation emits
  only an immediately adjacent nonempty block-comment prefix, advances that
  Item frontier once, and leaves all ordinary whitespace with the returned
  Item.  Its direct native token children are confined to this owner.
- Initial M3 implementation review found five concrete closure gaps: a
  non-PathSegment successful fixture, missing legacy AST continuation controls,
  no two-comment positive control, payload-driven horizontal-trivia ownership,
  and a LineComment eligibility hole.  One batched repair restricted fixtures,
  added the legacy/direct AST controls and contiguous-comment coverage, made
  whitespace handoff phase-aware, and rejects LineComment anywhere in physical
  leading.  A final micro-delta added the required `A::@/*x*/@B` legacy
  PathSegment-retry AST control.  Compiler/recovery, specification, and
  regression delta reviews are clean.
- Primary verification passed: 94 focused rewrite TypeExpression tests, 23
  recovery-output tests, focused legacy grammar TypeExpression and Gate3b
  baseline controls, `cargo check -p yu-syntax`, seven-file rustfmt check, and
  `git diff --check`.  Existing warnings and expected caught-panic output only.
  The recovery-only work retains a constant number of `O(L)` scans plus one
  accepted-prefix emission, with no valid-path work, cloning, replay, source
  buffer, or timing process; measurement use is zero.
- O4 can close only T2b's local direct/CST/Item/frozen/RB-T subproof.  The
  aggregate T2 matrix row remains Open until O6 exercises real successor
  Yumark and proves embedded AST/direct fact equality plus successor topology.
  T2a, every later Type/PV owner, legacy production, Yumark transport, public
  dispatch, public cutover, and O4/O7 broad suites remain excluded or deferred.

## T3 TypeCall architecture preflight

- The next matrix row is TypeCall item, separator, and close recovery.  Read-
  only architecture preflight found that its typed Missing cells are specified
  but full T3 cannot use current output authority unchanged: `T(@ A)` requires
  the valid retry Item's same-line space inside the CallArgument Error CST,
  whereas T2a's fifth operation leaves CST unchanged and T2b's sixth operation
  is PathSegment-only and leaves ordinary space outside Error.
- Recorded Draft `2026-09-07-successor-t3-typecall-recovery-amendment.md` with
  two ordered gates.  T3a is Call-only typed Missing slots plus inherited
  type-ML CallArgumentSeparator recovery.  T3b proposes a seventh,
  CallArgument-only sealed terminal and an equally narrow direct-child
  topology exception; Call close Error recovery remains separately bounded.
  Local O3/O4 and real-successor O6 evidence timing are explicit.
- This is a new durable recovery/CST decision.  No T3 code or expected output
  has changed.  M3 compiler/recovery, specification, and performance review
  and explicit user approval are the next required steps; all non-Call owners,
  Yumark transport, public dispatch/cutover, and legacy deletion stay deferred.

- M3 review found three blocking Draft gaps (no narrow supersession, no O6
  CallArgument Error witness, and ambiguous separator/Error ownership) plus
  compiler details around ordinary multi-Item Error topology, valid retry
  payload eligibility, inherited-ML LF/CRLF, and direct Error/close evidence.
  One repair made the T3b exception explicitly CallArgument-only, fixed exact
  legacy/successor token and range controls, and added Error/close O6 rows.
  Compiler/specification delta review is clean; performance review accepts the
  recovery-only constant-pass `O(L)` account with zero timing processes.  The
  design is Reviewed; explicit user approval is the sole implementation
  blocker.

- The user's active replacement objective explicitly authorizes proceeding
  with the recommended reviewed proposal unless a material issue needs a new
  decision.  Recorded that approval: the T3 amendment is Authoritative for its
  two ordered subgates, beginning with T3a Call Missing/inherited type-ML
  construction.  T3b remains bounded to its own later gate; all other owners,
  O6 fact equality, and public cutover remain deferred.

## O3a T3a TypeCall local construction complete

- Completed only Authoritative T3a: `TypeDelimitedOwner::Call` now publishes
  exact typed Missing records for CallArgument, CallArgumentSeparator, and the
  TypeCall parenthesis close.  It carries inherited type-ML only into Call item
  parsing; same-line and deeper newline retry publish the separator Missing,
  while equal/shallow implicit LF/CRLF remain ordinary Call separators.
- Initial M3 implementation review found two priority defects: qualifying
  implicit newline was tested after inherited-ML recovery, and raw malformed
  Call retry emitted an active outer boundary Item's leading before classifying
  it.  One repair restores implicit-separator priority and classifies
  Call-aware outer/caller/close boundaries before mutation.  Fresh/frozen
  controls pin LF/CRLF, initial/post-separator WITH, caller/outer close,
  abstract fence, shifted origin, exact Error ranges, retained leading, and
  close-Missing anchors.  Compiler/recovery, specification, and regression
  delta review are clean.
- Primary verification passed: 6 focused T3a tests, 100 focused rewrite
  TypeExpression tests, four legacy TypeCall contract tests, `cargo check -p
  yu-syntax`, four-file rustfmt, and `git diff --check`.  Existing warnings and
  expected caught-panic output only.  Added valid-path work is bounded
  Call-phase/type-ML branching; no new traversal, allocation, clone, replay,
  source buffer, or measurement process exists.
- T3b CallArgument Error completion is the next ordered gate.  It alone may
  use the seventh terminal and owner-local direct-child exception.  O6 real
  successor-Yumark equality, other Type/PV owners, legacy production,
  transport, public dispatch/cutover, and O4/O7 broad suites remain deferred.

## O3a T3b TypeCall Error local construction complete

- Completed only Authoritative T3b.  `Type(CallArgument)` now has the seventh
  sealed Error terminal: after full pre-mutation eligibility it emits one valid
  retry Item's same-line physical leading prefix, advances only that frontier,
  publishes equal Error/record/fact ranges, and returns the payload to the same
  argument slot.  Every physical Item or eligible leading part emitted by that
  CallArgument Error preserves native direct children; no other Error owner
  receives the exception.  Call mismatched-close recovery emits one `Unknown`
  Error per local malformed Item with leading outside Error until matching
  close, safe boundary, or EOF.
- Initial M3 review found an early close-handoff defect after a mismatch and a
  dead `T(@ , A)` separator-ownership assertion.  One repair makes Call close
  recovery retain non-boundary malformed Items through the actual close/safe
  boundary/EOF, and the test-only micro-delta moves the whitespace `3..4` and
  comma `4..5` parentage checks into the live T3b case.  Compiler/recovery,
  specification, and regression delta review are clean.
- Primary verification passed: 8 focused T3b tests, 25 recovery-output tests,
  105 rewrite TypeExpression tests, the TypeCall legacy direct baseline,
  `cargo check -p yu-syntax`, six-file rustfmt, and `git diff --check`.
  Existing warnings and expected caught-panic output only.  Recovery work is a
  constant number of `O(L)` leading scans plus one emission pass; no source
  buffer, clone, replay, valid-path rescan, or timing process was added.
- T3's local direct/CST/Item/frozen/RB-T subproof is complete.  The aggregate
  T3 matrix row remains Open until O6 runs real successor Yumark and proves
  actual embedded AST/direct fact equality plus topology.  T4--T7/PV1,
  legacy production, transport, public dispatch/cutover, and O4/O7 broad
  suites remain deferred.

## T4 delimited register architecture re-entry

- T4 preflight found a concrete false premise in the later recovery matrix:
  `(A B)`, `'[A B]`, and `[A B]` are valid same-line TypeApply items under the
  governing architecture, not separator-Missing cells.  The matrix also
  mislabels quoted `'[,]` as BracketRow, assigns TypeExpression rather than
  punctuation Arrow to BracketRowArrow, omits EffectRow/BracketRow close cells,
  and duplicates BracketRowArrow through T7c.  Implementing the old register
  would change valid syntax/CST and is forbidden.
- Recorded Draft
  `2026-09-07-successor-t4-delimited-register-correction-amendment.md`.  It
  corrects the finite register to ten cells, reuses the existing typed Missing
  and close-Error contracts, and proposes an eighth, T4-Error-owner-only sealed
  terminal plus native direct-child delta for malformed Item/Arrow recovery.
  No T4 code or expected output changed.  M3 compiler/recovery, specification,
  and performance review precede the active objective's recommended-proposal
  approval; all later owners and O6/public work remain deferred.

- M3 compiler/recovery, specification, and performance review found the T7c
  stale-witness, T3 transport, native-leading, and BracketRow S/B terminal
  boundary defects.  One authority repair made T7c a distinct `F [e] @`
  evidence cell, kept its embedded `21..22` locator source-counted until its
  direct legacy baseline, and bounded S/B to the BracketRow owner.  All three
  delta reviews are clean; static cost remains `O(B + P)` only on recovery and
  timing usage is zero.
- The user's active replacement objective authorizes reviewed recommended
  proposals absent a material issue.  That approval is recorded in the now
  Authoritative T4 amendment.  Only ordered T4P Parenthesized construction may
  begin; no T4 code or expected output has changed in this architecture commit.

- T4P preflight found a material contradiction before integration.  The
  governing legacy tests execution-pin scoped outer-TypeApply inherited type-ML
  behavior: `G (F A)` has ParenthesizedSeparator Missing `5..5`, and
  `G '[F\n  A]` has EffectRowSeparator Missing `8..8`.  The T4 amendment's
  claim that both are zero-recovery controls was false, although standalone
  `(A B)` and `'[A B]` remain one-TypeApply controls.  The uncommitted T4P
  candidate touched only `rewrite/emit.rs`, `rewrite/item.rs`, and
  `rewrite/type_expr/delimited.rs`; it was discarded with an explicit-path
  restore, leaving clean commit `d39d7e5d` and no code/output change.
- Architecture re-entry requires a narrow correction that restores the two
  inherited-context separator witnesses without broadening T3 transport or any
  other owner.  T4P/T4E are suspended pending M3 review and a fresh user
  decision; T4B/T4A/T7c and O6 remain deferred.

- Recorded Draft
  `2026-09-07-successor-t4-inherited-type-ml-correction-amendment.md`.  It
  preserves standalone `(A B)` / `'[A B]` TypeApply controls, restores only
  outer-TypeApply scoped context into immediate Parenthesized/EffectRow item
  parsing, and makes those owners publish their own separator Missing before
  retry.  It adds no Error terminal, topology exception, source retention, or
  general transport.  M3 compiler/recovery, specification, and performance
  review precede a fresh user decision; implementation remains stopped.

- The M3 Draft review closed after three bounded repairs.  The last
  specification finding required the private context to preserve T3 Call
  exactly: Call is provenance-blind and passes its incoming active/dormant phase
  unchanged to each immediate argument, so `G T(F A)` retains its existing
  CallArgumentSeparator `6..6`; only P/E gain new OuterTypeApply-provenance
  reactivity.  Architecture adjudicated the transition and a targeted
  independent specification closure is clean; compiler/recovery and performance
  review remain clean over their unaffected deltas, with zero timing processes.
- The user selected the narrow outer-TypeApply-only option.  The amendment is
  Authoritative for T4P local construction only: execution-pin the listed
  source-derived direct legacy baselines, then perform one T4P construction
  pass.  T4E, NonTypeApply PV/declaration P/E parity, T4B/T4A/T7c, O4/O6,
  public/Yumark, and production expected-output changes remain excluded.

- Execution-pinned the direct legacy Parenthesized (T4P) baselines at
  `7ed1d35b`, then completed the single authorized T4P local construction pass at
  `3bedfbef`.  The normalized TypeExpression cone now carries the private,
  by-value `TypeMlContext`: a committed outer TypeApply activates only its
  immediate Parenthesized items; Call preserves its inherited phase under T3;
  record, forall, PV, EffectRow, BracketRow, arrow, and declaration paths carry
  dormant provenance without gaining a separator role.  No Error terminal,
  public interface, Yumark integration, fixture/golden expectation, or
  EffectRow/BracketRow positive recovery changed.
- M3 implementation review required two bounded test-evidence-only repairs.
  The final compiler/recovery, specification, and regression deltas are clean.
  The local proof covers exact CST/token ranges and native trivia, priority,
  fresh/frozen equality, all four seeded context phases, RB-T rejection and
  handoff restoration, lexical non-leak, T3 Call preservation, and explicit
  no-record EffectRow/BracketRow controls.  Focused T4P (12), T3 Call (11),
  direct legacy baseline, Gate3b, and nested-call legacy tests passed; so did
  `cargo check -p yu-syntax`, `cargo fmt --all -- --check`, and diff check.
  Existing warnings remain 39 for lib tests and 87 for package check; timing
  processes remain zero.  The full package/workspace suites, clippy,
  O4/O6/Yumark/public work, T4E/T4B/T4A, and the excluded declaration/PV P/E
  parity route were not run or expanded.  The current declaration ingress has
  no incoming dormant-Outer entry, so no artificial declaration witness was
  added.

- T4E legacy preflight at `820dca87` found a material shared P/E
  boundary-priority contradiction before any successor T4E code changed.  In
  `G '[F )`, legacy EffectRow consumes the horizontal space `5..6`, emits its
  local close Missing at `6..6`, and leaves only raw `)` for the caller; nested
  `G ('[F )` behaves the same before Parenthesized consumes `)`.  ASOB differs:
  `G '[F else: 0` leaves the full space-plus-`else` gap and emits close Missing
  at `5..5`.  This contradicts the inherited amendment's unqualified
  pre-trivia caller/outer/abstract handoff rule.  The same legacy driver owns P
  and E, so T4P provenance/separator construction remains retained but its
  boundary-priority certification is reopened; T4E is suspended.  Next safe
  evidence is direct Parenthesized horizontal active caller/outer-close baselines plus
  a bounded T3 Call sibling audit, followed by a reviewed and fresh
  user-approved correction.  No T4P repair, T4E code, or expected-output change
  is authorized.

- The shared Parenthesized/Call evidence at `c54c78c6` completed that safe audit
  and widened the suspension only to the matching T3a post-item horizontal
  boundary certificate.  Legacy `G (F ]` and `G T(F ]` consume the horizontal
  gap into P/Call, leave raw `]`, and publish their local close Missing at
  `5..5` / `6..6`; the BracketRow outer-close forms continue losslessly after
  P/Call Missing at `7..7` / `8..8`.  P/Call ASOB controls retain the whole
  gap, while actual local `)` controls remain complete without recovery.  The
  completed successor T3a/T4P boundary tests deliberately do the opposite
  (retain the gap pending), so T3a Call and T4P P/E provenance/separator cores
  remain retained but their common horizontal boundary certification is
  suspended.  The next evidence-only work is initial and post-explicit-separator
  horizontal-gap baselines; only then can a reviewed, fresh user-approved
  correction choose legacy-compatible repair or intentional divergence.  T3b,
  separator behavior, Error terminals, T4E construction, T4B/T4A, O4/O6,
  public/Yumark, and expected-output changes remain excluded.

## Fresh-slot horizontal-gap legacy baseline closure

- `b4c790db` execution-pinned the remaining shared initial and
  post-explicit-separator (`,` / `;`) horizontal-gap phases for Call,
  Parenthesized, and EffectRow.  Each generic-caller-stop control leaves raw
  `}` unconsumed, publishes the exact owner Item and close Missing records at
  the post-gap cursor, and distinguishes initial/post-separator ASOB treatment
  from the already-pinned post-completed-item ASOB handoff.  Matching local
  closes are zero-recovery controls.
- M1 specification audit found that the first test did not retain direct CST,
  so a single test-only repair added owner-child checks.  The native horizontal
  Whitespace token is now pinned as a direct Call/Parenthesized/EffectRow child
  at its exact range, before the owner Item/close Missing sequence for caller
  stops and before the native matching close for local-close controls.  The
  existing zero-width attempted TypeExpression in an empty slot remains
  deliberately unfrozen.  Fresh delta specification review found no remaining
  issue.  The focused test, `rustfmt --check --edition 2024
  crates/yu-syntax/src/grammar/type_expr.rs`, and `git diff --check` passed;
  existing 39 lib-test warnings remain and timing processes are zero.
- This closes only the prerequisite evidence.  It grants no successor behavior,
  expected-output update, T3/T4P repair, or T4E construction.  Next is a
  reviewed correction that distinguishes abstract whole-gap handoff from
  delimited-owner horizontal-trivia consumption before raw generic
  caller/outer payload handoff, then a fresh user decision.

- The architecture pass found that fresh-slot nested outer closes remained
  uncharacterized, so the same legacy test now adds the nine Initial/Comma/
  Semicolon controls for Parenthesized-in-BracketRow, Call-in-BracketRow, and
  EffectRow-in-Parenthesized.  In every case the immediate owner retains the
  one-space gap and ordered Item/close Missing pair at the post-gap coordinate,
  while the outer `]` or `)` is an actual completed close with no recovery and,
  for BracketRow, continues through `-> U`.  AST, direct CST, recovery order,
  and losslessness are asserted; the existing zero-width attempted
  TypeExpression wrapper remains deliberately unfrozen.  M1 pre-write and
  fresh delta specification review are clean.  The same focused test, file
  format check, and diff check passed; timing remains zero.  The evidence set
  is now sufficient to draft the reviewed correction, but it still authorizes
  neither successor behavior nor implementation.

## Reviewed shared delimited boundary-priority correction

- Recorded and reviewed
  `2026-09-07-successor-delimited-boundary-priority-correction.md`.  It
  conditionally supersedes only the suspended T3 post-item and T4 P/E
  horizontal-priority clauses.  It keeps abstract whole-gap handoff distinct
  from phase-local ordinary-horizontal ownership before raw generic/outer
  handoff; Call, ParenthesizedGroup, and EffectRow retain their existing
  owner-specific Missing-record publication.
- M3 independent compiler/recovery and specification review required two
  batched Draft repairs.  The first made the conditional supersession and the
  Parenthesized typed/untyped close-publication split explicit.  The second
  retained fresh caller-stop admission exactly: active `STOP_ELSE` hands off
  from Call, while ParenthesizedGroup and EffectRow retain Type-NUD admission.
  Both final compiler/recovery and specification deltas are clean.
- The correction remains a Reviewed proposal, not construction authority.  A
  fresh user decision is required between §6's legacy-compatible correction and
  intentional successor divergence.  No rewrite source, expectation, fixture,
  or golden changed in this record-only step; timing remains zero and focused
  tests were not rerun.

- The user selected §6 option 1, the legacy-compatible correction.  The design
  is now Authoritative and grants one private M3 construction pass only for the
  phase-aware Call/ParenthesizedGroup/EffectRow horizontal-gap priority.  T3,
  T4P, owner-specific caller-stop admission, record publication, and all stated
  proof obligations remain retained; T4E construction, public/output scope,
  and O6 remain excluded.

## Legacy PV outer-Error extent characterization

- The unintegrated owner-local candidate exposed `:{(A }`: Parenthesized
  correctly owns the horizontal gap and shifts its local close frontier, while
  the already-open PolymorphicVariantTagName Error derives its endpoint from the
  returned pending Item.  M3 compiler/recovery found that clamping only the
  outer Error would violate structured Error extent ownership; specification
  review held the outer PV oracle outside the approved correction scope.
- `legacy_polymorphic_variant_structured_parenthesized_gap_extents_are_execution_pinned`
  now records six direct legacy bases, each with `::Next` continuation.  The
  decisive `:{(A }` outer Error is `2..5` (no-gap `:{(A}` is `2..4`), actual PV
  close remains native, and parsing is lossless.  Legacy instead flattens the
  malformed primary to `PolymorphicVariantTag > Error > Unknown`: it has no
  inner Parenthesized, native Whitespace, Missing, or inner record.  M1
  specification audit is clean; the exact focused test, format check, and diff
  check passed with zero timing processes.
- This evidence authorizes no successor PV record/expectation change.  Retain
  the candidate unintegrated while a narrow reviewed addendum decides whether
  the mechanically matching outer `2..5` successor range may accompany the
  approved owner-local correction; that extension requires a fresh user
  decision.

- The resulting
  `2026-09-07-successor-delimited-pv-extent-correction-addendum.md` is
  Reviewed after M3 compiler/recovery and specification review, one bounded
  Draft repair, and clean deltas.  It covers the three known Parenthesized
  Initial/post-separator/post-item range consequences, conditionally
  supersedes only the original private expected-output and embedded-recovery-
  fact exclusions for those existing TagName records, and retains NonTypeApply
  PV/declaration parity plus every Call/EffectRow/`variants.rs` exclusion.  A
  fresh user choice is now required before the candidate can be integrated.

- The user selected the reviewed recommended PV extent correction.  It is now
  Authoritative for one private integration/proof pass over the existing
  Parenthesized-in-TagName Error interaction.  The user also directed that
  future comparably narrow, fully reviewed direct-compatibility corrections
  proceed with their documented recommendation; material semantic, public, or
  scope expansions still return for an explicit decision.  The current gate
  still requires regression and compiler/specification delta review before
  integration.

## Legacy PV EffectRow and apparent-Call carrier characterization

- The approved P-in-TagName sibling audit exposed reachable EffectRow and
  apparent Call shapes, so the shared candidate remains unintegrated rather
  than accepting an unreviewed outer oracle change.  The direct legacy test
  `legacy_polymorphic_variant_effect_and_call_gap_carriers_are_execution_pinned`
  records all initial/comma/semicolon/post-item/no-gap/matching-local rows with
  `::Next`, plus prefix and nested witnesses.
- EffectRow rows are flat TagName `Error > Unknown` ranges (`2..5`, `2..7`,
  `2..7`, `2..6`, `2..5`, `2..7`) without inner records.  The decisive
  counterfact is numeric apparent Call: legacy reports TagName Error `123`
  `2..5`, then a separate Parenthesized PV payload and PayloadBoundary Missing;
  it has no `TypeCallTail` in the TagName Error.  Nested E and Call witnesses
  each flatten to one outer TagName Error.  All forms are lossless and retain
  their native outer PV close/continuation.
- M1 specification review is clean; the exact focused test, format, and diff
  checks passed with zero timing processes.  This evidence grants no successor
  E/Call expectation or record update.  Architecture must now distinguish the
  E compatible outer-extent route from the Call counterfact before any shared
  candidate integration.

- The E route is now Authoritative through the user's recorded recommendation
  delegation: its direct-legacy-compatible TagName Error range/evidence cells
  are bounded to the existing E carrier, retain no E record publication, and
  leave Call/P/PV admission/`variants.rs`/public scope excluded.  M3
  compiler/recovery and specification review are clean with zero timing
  processes.  The candidate still cannot integrate until the distinct numeric
  Call PV head-admission contradiction is independently characterized and
  reviewed.

## Legacy PV wrong-kind primary-completion preflight

- `legacy_polymorphic_variant_primary_completion_preflight` records 19 direct
  bases, three `::Next` continuations, and both forall controls.  Numeric,
  grouped, EffectRow, and record heads finish at their own primary boundary;
  outer `(`/`::`/`->`/application forms enter PV payload handling, while tails
  wholly inside the composite remain part of its flat TagName Error.  Forall
  owns its complete body as a primary.
- The test pins AST tag/payload ranges, direct CST Error/payload ancestry and
  extents, actual PV closes, empty remainder/losslessness, and complete ordered
  recovery records.  M1 specification audit is clean; its focused test,
  format, and diff checks passed with zero timing processes.  It grants no
  successor behavior: base/candidate successor facts must now distinguish this
  pre-existing PV head-admission divergence from the approved delimiter gap
  correction before a reviewed private entry/primary-completion gate can exist.

- `b6349f9c` adds the required nested-PV external-tail control
  `:{:{A}(B)}`.  It pins flat TagName Error/Unknown `2..6`, a separate
  Parenthesized payload `6..9`, PayloadBoundary Missing `6..6`, outer native
  close `9..10`, exact two-record order, and no unpinned continuation.

## PV wrong-kind primary-completion correction — Reviewed

- Recorded
  `2026-09-07-successor-pv-wrong-kind-primary-completion-amendment.md` after
  comparing the isolated base and shared-delimited candidate.  The divergence
  predates that candidate: successor `variants.rs` calls full Type-expression
  completion after a wrong tag head, while direct legacy ends the TagName Error
  at one completed primary and lets external Call/path/arrow/application forms
  enter the existing PV payload judge.
- M3 compiler/recovery and specification review found one shared construction
  correction and four contract/evidence repairs.  The Reviewed route uses a
  private primary-completion input policy.  `ReturnPrimary` is threaded only
  to immediate Parenthesized/EffectRow/NamedRecord/nested-PV producer endings;
  their internal child expressions and forall body retain ordinary full
  completion.  Leading BracketRow is explicitly excluded and requires a
  no-delta successor control.  The proof set now distinguishes pre-reservation
  rejection from committed frozen-mismatch invalidation, retains recursive
  `:{:{123}}` reservation evidence, and limits `::Next` claims to the three
  execution-pinned sources.
- Both reviewers closed the repaired delta.  Static review found no new
  allocation, traversal, scan, clone, replay, or timing process; the timing
  budget remains zero.  This is a material recovery/scope decision, so the
  recorded same-carrier recommendation delegation is inapplicable.  A fresh
  user selection of option 1 (recommended primary completion) or option 2
  (retain intentional divergence) is the only construction blocker.  The
  unintegrated delimiter candidate remains isolated and unstaged.

- The user selected option 1 on 2026-09-07.  The amendment is Authoritative
  for exactly one M3 private construction/proof pass.  Immediate
  primary-owner endings, the PV wrong-kind call site, and focused successor
  evidence are the only code scope; valid names, leading BracketRow, public
  dispatch, O6, fixtures/goldens, legacy grammar, and topology changes remain
  excluded.

## PV primary-completion construction contradiction

- The authorized private candidate correctly stopped `:{123::T}`'s structured
  TagName Error at `2..5`, then exposed a false premise: the unchanged PV
  payload judge rejects unspaced `::` and outer tag recovery creates a second
  Tag Error `5..7`.  Direct legacy has PayloadBoundary Error `5..7` followed
  by payload `T`.  The candidate was reverted before integration; the shared
  delimited candidate remains isolated.
- Architecture localizes the owning cause to conditional malformed payload
  admission shared by valid and wrong-kind tag heads.  Legacy admits an
  unspaced invalid run only when it reaches an admissible non-ambient retry
  primary; dangling/ambient paths remain tag-owned.  The successor currently
  emits before that retry witness exists, so token-specific handling or
  unconditional consumption would be unsound.  Direct legacy table evidence,
  then a new M3 payload-adapter addendum, are required before any fresh user
  choice or construction.  Timing remains zero.

- `legacy_polymorphic_variant_conditional_payload_admission_is_execution_pinned`
  now fixes all 12 direct legacy rows: valid/wrong-head unspaced `::`/`->`
  retries, valid/wrong-head dangling `::`, visible ambient retry rejection,
  spaced malformed payload, repeated payload, newline, native close, and EOF.
  It asserts AST slots/ranges, exhaustive direct CST preorder and token ranges,
  complete ordered recovery tuples, close ownership, payload presence/shape,
  caller remainder, and ambient-context balance.  M1 independent specification
  review is clean.  This closes only evidence; successor code remains
  unchanged, and the new M3 payload-adapter addendum is next.

## PV payload-admission capability re-entry

- Direct-facility architecture review found a capability mismatch, not an
  alternate existing helper.  To decide an unspaced malformed payload, the
  shared successor judge would need a later retry-primary/ambient-owner fact
  before it emits the current invalid Item.  Advancing creates Items that a
  later decline cannot restore without forbidden rescan/buffer; lexical
  rollback cannot roll back Rowan/recovery, and output checkpoints do not
  rollback.  Existing source-only probes are limited to candidate-token or
  prospective-trivia facts and cannot classify an arbitrary malformed suffix.
- The payload interface also lacks an ambient claim independent of stop bits;
  `4ff97ddc`'s visible-no-stop `else` row proves it cannot be ignored.  Drafted
  `2026-09-07-successor-pv-payload-admission-capability-amendment.md`: it
  proposes only a finite source-only witness plus explicit ambient claim,
  prohibits retained source/run/offset/Item/cache/output/recovery state, and
  requires direct evidence, aggregate work accounting, and M3
  compiler/specification/performance review before a fresh user choice.  No
  successor source changed; timing processes remain zero.

- M3 compiler/recovery, specification, and performance review rejected that
  initial construction shape.  A suffix-only witness starts after successor has
  completed `::{B}` as a PathSeparator Item, but direct legacy emits a one-colon
  PayloadBoundary Error and retries the nested PV at the second colon.  The
  same split occurs after two error colons in `:::{B}`.  The initial Draft also
  overgeneralized its twelve examples, lacked position-sensitive ambient
  provenance, and did not prohibit all probe allocations/retentions or prove
  total witness work.
- `legacy_polymorphic_variant_payload_colon_overlap_is_execution_pinned` now
  fixes all four valid/wrong-head, two-colon/three-colon nested-PV rows with
  exact AST, CST, recovery, close, and remainder facts.  M1 specification
  review is clean.  The next design must start at the pre-Item scalar boundary,
  carry ambient ownership by live position, prohibit Item/Token/trivia/source/
  run/offset/cache/recovery/output retention, and account for malformed bytes,
  witness bytes, admissions/declines, and classifications before any fresh M3
  construction review.  No successor implementation changed; timing remains
  zero.

- `legacy_polymorphic_variant_payload_scalar_run_boundaries_are_execution_pinned`
  adds exact AST/CST/recovery evidence for `->`, `+`, `@@`, and `::/*c*/`
  inline retries into a nested PV.  Each has one PayloadBoundary Error over the
  exact scalar/comment prefix and retries at `:{B}`.  The paired `::\r\n:{B}`
  control is intentionally different: its first Error is outer-tag-owned,
  CRLF remains a distinct newline, and the outer tag loop owns the malformed
  nested starter.  M1 specification review is clean; this extends direct
  evidence only and does not authorize successor construction.

- Architecture re-entry separates two prerequisites.  First, an immutable
  call-stack `AmbientClaimView` must carry only the nearest visible statement
  baseline and visible If-companion chain: root/indented owners replace the
  baseline, inline forwards, braced clears, and an If arm prepends a temporary
  frame.  It needs no mutable `Recover` scope state, source retention, or
  rollback mark because frames cannot escape their calls.  Its query consumes
  only position-derived newline/indentation/word facts.  Virtual-statement and
  Yumark barrier classification remain unproven.  Only after this private
  prerequisite can pre-Item scalar payload admission be designed; no successor
  code or user decision is authorized by this record.
