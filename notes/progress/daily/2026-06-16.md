# 2026-06-16

## specialize2 thunk computation shape

- 推論で使う関数型 `['a] 'b` と、runtime/代入で使う `thunk['a, 'b]` を分ける方向で
  `specialize2` の emit 境界を見直した。
- `emit_expr` の内部返り値を `Expr` だけでなく `ComputationShape { effect, value }` 付きにした。
  - `[] thunk['a, 'b]` を force したら、以後は `['a] 'b` として持ち回る。
  - 後続の make は古い `actual` ではなく、この carried computation shape を `source` に使う。
  - これで `force` 後に stale `actual` で再度 `make` する二重 force/make を避ける。
- `catch` body も構文別の force 判定をやめ、現在の emitted shape が pure thunk value のときだけ force する。
- raw expression の値型を構文ごとに分けた。
  - constructor spine は raw mono では constructor value / partial constructor value を返すので、
    型上 effectful function に見えても thunk として force しない。
  - App / Select / Catch / Block などは「raw が返す値」と「計算としての effect/value」を分けて扱う。
- 境界注釈として mono へ出す `ForceThunk` / `MakeThunk` の source/target は runtime surface に閉じる。
  推論用の `arg_effect` / `ret_effect` を mono VM type に漏らさないため。

## verification

- 成功:
  - `cargo fmt --check`
  - `cargo check -q -p specialize`
  - `cargo test -q -p yulang source::tests::run_control_source_text_with_embedded_std_imports_prelude_reexports -- --exact --nocapture`
  - `cargo test -q -p yulang source::tests::run_control_source_text_with_embedded_std_forces_effectful_block_let -- --exact --nocapture`
  - `cargo test -q -p yulang source::tests::run_control_source_text_with_embedded_std_runs_junction_tour_example -- --exact --nocapture`
  - `cargo test -q -p yulang source::tests::run_control_source_text_with_embedded_std_runs_nondet_triples -- --exact --nocapture`
  - `cargo test -q -p yulang source::tests::run_control_source_text_with_embedded_std_lowers_sub_syntax_return -- --exact --nocapture`
  - `cargo test -q -p wasm tests::run_inner_uses_embedded_std_only_when_needed -- --exact --nocapture`
  - `npm run build` in `web/playground`
- 残り:
  - `cargo test -q -p specialize` は
    `tests::string_input_specializes_stack_quantified_handler_annotation` が失敗する。
  - `dump_mono_without_std_lowers_wildcard_stack_handler_annotation` /
    `dump_mono_without_std_lowers_apply_colon_polymorphic_stack_handler_call` も同じ根で失敗する。
  - 観測した形は、`[_]` handler の residual が root call で pure に落ちず、
    `thunk[[signal], int] -> thunk[[signal], int]` のように返りにも handled effect が残るもの。
    今回の thunk computation shape 修正とは別の、effect row subtraction / `α & [handled; α]`
    の消し込み問題として追う。

## playground residual specialization checkpoint

- `['e]` のような tail-only effect annotation は、target effect と注釈 tail を双方向に接続するようにした。
  `['e]` を fresh proxy の下限としてだけ扱うと、後続の `thunk['e, 'a]` 化で residual が分離するため。
- specialize2 側では、effect row candidate の比較と関数 subtyping で runtime `thunk[...]` shape と
  inference `ret_effect + ret` shape を分けて扱うようにした。
- `local_change` はまだ未解決。
  - 直近の失敗は `DefId(408)` 相当の `range.fold` specialization で、
    callback residual `[std.control.flow.loop.last, &total]` と pure upper が同じ effect slot に入る。
  - upstream を見ると、`std.control.flow.loop.for_in` の principal scheme 自体が
    `['e]` を失い、戻り効果 pure へ縮んでいる。
  - 次は specialize の後段補正ではなく、infer/lowering 側で `for_in` 本体の handled effect residual が
    principal scheme に残らない原因を追う。
- 確認:
  - `cargo fmt --check`
  - `cargo check -q -p specialize`
  - `cargo test -q -p infer annotation::tests::tail_only_effectful_annotation_is_current_effect -- --exact --nocapture`
  - `cargo test -q -p infer lowering::tests::result_effect_annotation_reuses_callback_tail -- --exact --nocapture`
  - `cargo test -q -p specialize specialize2::tests::effect_row_lower_with_tail_refines_to_closed_upper -- --exact --nocapture`
  - `cargo test -q -p specialize specialize2::tests::function_subtyping_compares_split_runtime_return_shapes -- --exact --nocapture`
  - 既知失敗: `cargo test -q -p yulang source::tests::run_control_source_text_with_embedded_playground_std_runs_local_change_example -- --exact --nocapture`
