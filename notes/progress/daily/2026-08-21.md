# 2026-08-21

## Phase 2: diagnostics 設計の確定と grammar layer の実装着手

### diagnostic-expectation-merge / `DiagnosticId` reconciliation の決定

- 前日から残っていた chasa architecture 文書の未決定 2 点(diagnostic expectation の merge、
  header/full reparse の `DiagnosticId` 照合 key)を、ユーザーの「頑健に」という方針指示のもと
  Sol(xhigh)で具体化した。`DiagnosticId` は typed `GrammarRole + parser-native ByteRange` の
  exact key で照合し、role/range が一致しない場合は fuzzy dedupe せず新規 full-origin ID を
  発行する。expectation merge は `LatestSink` の最遠 active group 内の全 distinct candidate を
  union し、committed site との grammar-role affinity(tier 0〜5 の優先順位表)と明示的な
  stable tie-break key で主表示を一件選ぶ。branch 順や message 文言には依存しない。設計書の
  question 7 / 8 は Resolved へ移動した。コミットは `2ac108e4`。

### grammar layer の最初の vertical slice 実装

- `grammar::declaration` と最小 Pratt expression grammar をスライス単位で実装した。leading
  `use` 宣言(`ed519982`)、`my <ident> = <expr>` をサポートする最小 expression grammar
  (`637fd42c`)、operator header 宣言の parse(`9d1152c4`)、そして `operator.rs` の immutable
  trie table と `scan::operator` の NUD/LED site-aware scanner を使った Pratt expression parser
  (`1d461783`)を通し、`+!a` が `+ (! a)` に、`a+!b` が infix `+!` に、動的 operator の
  文脈依存トークン化として正しく解決できることを確認した。まだ `scan_header` / `parse_file` への
  接続は行っていない。
- Codex sandbox の `.git` が read-only になる既知の制約により、各スライスの実装・テスト確認後の
  commit は Claude が代行した。

### use 宣言構文の誤りの発見と修正

- ユーザーから「`use path.component` なんて構文ないけど」という指摘を受け調査したところ、
  `tests/contracts/phase2-parser/v0/cases/leading-use-plain/main.yu` の fixture 自体が
  `use std.data` という誤った dot 区切りになっていたと判明した。yulang2-oracle の正規 path
  区切りは `::` と `/` のみで、`.` は使わない。同じ誤りは `malformed-header-followed-by-valid-header`
  と `late-use-after-body` にも残っていた。
- 3 つの fixture を `use std::data` 等の正しい区切りへ修正し(`a1699e48`、`d8cccf32`)、
  `parse_use_declaration` を `::` 受理へ修正した(`9f3f56e9`)。旧 `HeaderCursor` の
  `scan_plain_use` も同じ誤りで `TokenKind::Dot` を期待していたため、既存の
  `TokenKind::ColonColon` を見るよう 1 行修正した(`b8b97cc5`)。
- 続けて、CST 上で `::` が `SyntaxKind::Unknown` として出力されていた技術的負債を解消するため
  `SyntaxKind::ColonColon` を追加し(`4405bad6`)、`scan::punctuation` に `/` を追加し
  (`62abc7bd`)、`parse_use_declaration` が `::` / `/` どちらも区切りとして受理するよう拡張して
  realm 形式(`use realm/tools::format`)の regression test を追加した(`76961c23`)。
  フォーマット修正(`ad99fb69`)を含め全て push 済み。

### use 宣言の完全な文法・意味論設計

- 残っていた `mod` sugar・group import・alias・glob の実装可能性を調査したところ、現在の
  `UseDeclaration { range, path: Vec<WordSpan> }` という構造ではこれらを表現できず、先に
  Use AST・group 展開規則・mod sugar の責務分担を設計する必要があると判明した。yulang2-oracle の
  専用 use parser(504 行)と scanner(167 行)を精読し、Sol(xhigh)で use 宣言の完全な文法・
  意味論設計を `notes/design/2026-08-20-yu-syntax-chasa-architecture.md` に追記した(`f1e235a1`)。
- form 分類規則: `mod` は scanner 側で専用 contextual keyword、`realm` / `band` はどちらも
  専用 tag ではなく一般 `Ident` で、spelling 直後の区切り文字(`realm` + `/`、`band` + `::`)
  でのみ marker と判定する。`realm::x`、`band/x`、任意の `other/x` は全部 Plain のまま。
- Use AST は `UseTree` を中心にした再帰構造(`UseTerminal::Single/Group/Glob`、`UsePath`、
  `UseQualifiers`)とし、alias 反復・glob・`without`・operator segment・version / `with` anchor
  を表現できるようにした。version / anchor の意味論(manifest 連携、precedence 等)は
  future scope として構文だけ保持する。
- group 展開は depth-first で common prefix と form を継承し、完成した leaf だけを独立 record
  として commit する。`use mod` sugar の責務分担は、syntax 層が `form = Mod` と normalized path
  を保持するだけで synthetic module declaration は作らない、実際の module load 効果は
  source-loader 側の責任、と確定した。§18 の非 header `mod` / late `use` の source-set
  discovery とは別問題として切り分けた。
- version validation、`with` anchor 解決の precedence、schema revision の具体的 field shape、
  non-header `mod` の discovery mechanism の 4 点は Still open として明示的に残した。

## 本日の区切りと次の作業

- diagnostics 設計と use 宣言の完全な文法・意味論設計、両方とも正本候補の chasa architecture
  文書に反映された。実装済みなのは grammar layer の最初の vertical slice(use 宣言の Plain/Realm
  形、`my` 束縛、operator header、Pratt expression parser)のみで、Use AST 自体の型置き換えと
  group / alias / glob / mod sugar の実装はまだ着手していない。
- `scan_header` / `parse_file` への新 grammar 接続、旧 `HeaderCursor` / `lex` / `FullCstBuilder`
  の削除も未着手のまま残っている。

## コミット一覧

yulang3 branch の本日分のコミットを時系列順に記す。

- `2ac108e4` docs: resolve chasa recovery diagnostic design
- `ed519982` feat(yu-syntax): add shared use declaration grammar
- `637fd42c` feat(yu-syntax): add minimal binding expression grammar
- `9d1152c4` feat(yu-syntax): parse operator header declarations
- `1d461783` feat(yu-syntax): parse dynamic operators with Pratt grammar
- `a1699e48` test: correct leading plain use path separator
- `d8cccf32` test: correct remaining plain use fixtures
- `9f3f56e9` fix: parse use paths with double colon
- `b8b97cc5` fix: recognize `::` in old lexer's plain use scan
- `4405bad6` fix: classify double colon in syntax trees
- `62abc7bd` fix: scan slash as use path punctuation
- `76961c23` feat(yu-syntax): accept `/` as a use path separator
- `ad99fb69` style: fix module declaration order in yu-syntax lib.rs
- `f1e235a1` docs: specify complete use declaration design

<!-- daily-append-anchor: 2026-08-21 -->
