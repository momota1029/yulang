# 2026-06-17

## infer local recursive effect passthrough

- local recursive binding の scheme finalize で、再帰本体が環境側の effect residual を通すケースを追跡。
- `my loop b = if b: loop (judge (f ())) ()` のような局所再帰で、callback effect が `any` に潰れず、外側の `h` に `(() -> ['a] any) -> ['a] ()` として残ることを確認。
- `for_in` 系の注釈付き local loop は、注釈あり引数を passthrough 対象にしないようにして、std check のハングを避けた。
- bound machine に live variable adjacency を持たせ、scheme の root effect var から環境へ到達する代表を拾えるようにした。

## 確認

- `timeout 60s cargo fmt --all -- --check`
- `timeout 120s cargo test -q -p infer -- --test-threads=1`
- 以前の確認で `check-poly-std` の空入力も通過済み。

## labeled sub lambda の local scheme

- `\sub 'inner x -> 'outer.return x` の local scheme が `any -> never` に落ち、
  外側ラベルの return effect が消える問題を追跡。
- local binding の RHS で出た subtype constraints を、scheme finalize 前に drain して、
  同じ block の後続文が読む local scheme に閉包済みの制約を反映するようにした。
- surface `sub` / labeled `sub` は、body effect から handler が扱う return effect を除き、
  残りを結果 effect へ通す制約を lowering 時点で明示するようにした。
- act operation の参照は signature から value へ subtype 制約を張る。ただし現在の bare
  `return` は既存の `current_sub_return_target` 経路で扱うため、この immediate 制約からは外す。

## 確認（labeled sub lambda）

- `timeout 60s cargo fmt --all -- --check`
- `timeout 60s cargo test -q -p infer lowering::tests::case_05::local_sub_lambda_scheme_keeps_escaping_labeled_return_effect -- --test-threads=1 --nocapture`
- `timeout 60s cargo test -q -p infer lowering::tests::case_05::defined_arg_call_inside_anonymous_lambda_keeps_sub_residual -- --test-threads=1 --nocapture`
- `timeout 120s cargo test -q -p infer -- --test-threads=1`
- `timeout 120s cargo test -q -p specialize -- --test-threads=1`
- `timeout 90s cargo test -q -p yulang run_control_source_text_with_embedded_std_labeled_sub_lambda_lets_outer_return_escape -- --test-threads=1 --nocapture`

## 残り

- `timeout 120s cargo test -q -p yulang -- --test-threads=1` は thunk 表記を含む mono dump 期待値差分と、
  playground std の list update specialization conflict が残っている。

## specialize2 effect row payload merge

- playground std の list update で、同じ effect slot に `ref_update int` と
  `ref_update (list int)` が入ったとき、slot lower candidate 同士を同一 family という理由で
  invariant に payload 統合していたのが衝突原因だった。
- same-slot lower candidate の共存では effect row payload を即時 exact 比較せず、
  effect row merge / resolver 側で explicit union として残すようにした。
- resolver の explicit `Union` / `Intersection` は candidate join/meet で合流できない場合も、
  explicit 型として保持する。effect payload の `int | list int` を候補衝突として落とさないため。
- mono dump の nondet list/say テストは、method specialization 自体を見る assertion に弱めた。
  resolver の grouping 変更で括弧位置だけが変わるため、挙動の固定対象から外した。

## 確認（specialize2 effect row payload merge）

- `timeout 90s cargo test -q -p yulang run_control_source_text_with_embedded_playground_std_runs_list_update_example -- --test-threads=1 --nocapture`
- `timeout 90s cargo test -q -p yulang run_control_source_text_with_embedded_std_runs_nondet_once_triple -- --test-threads=1 --nocapture`
- `timeout 90s cargo test -q -p yulang dump_mono_with_std_specializes_nondet_sum_list_say_benchmark -- --test-threads=1 --nocapture`
- `timeout 90s cargo test -q -p yulang run_with_std_nondet_sum_list_say_reports_unhandled_out -- --test-threads=1 --nocapture`
- `timeout 90s cargo test -q -p yulang run_with_std_collects_nondet_sum_list_benchmark -- --test-threads=1 --nocapture`
- `timeout 90s cargo test -q -p yulang run_mono_with_std_collects_nondet_each_list -- --test-threads=1 --nocapture`
- `timeout 120s cargo test -q -p yulang --lib -- --test-threads=1`
- `timeout 90s cargo test -q -p specialize -- --test-threads=1`
- `timeout 120s cargo test -q -p infer -- --test-threads=1`
- `timeout 60s cargo fmt --all -- --check`

## wildcard effect thunk / act method row boundary

- `[_]` の関数引数 effect 注釈は、通常の effect row 制約ではなく stack boundary を持つ fresh effect として lowering する。
  lambda lowering 側では関数の `arg_eff` に stack wrapper ではなく内側 effect var を出し、body/output 側で pop を持ち回る。
- specialize2 は def body が concrete な関数 arg effect signature を持つ lambda のときだけ、signature を先に与えて thunk 化する。
  OpenVar arg effect の lambda では signature seed を避け、未使用 effectful 引数が二重 force / make にならないようにした。
- `once` の合成 act method で `['a & [nondet; 'a]]` が `['a]` に落ちていた原因は、制約保存時に
  `[E; tail]` と `tail` を同じ tail という理由で subsume / prune していたこと。
  stack/floor の row-tail boundary を持つ source では row upper を保持し、row subtraction が明示的に reduced upper を保存した場合だけ stale row を prune するように分けた。
- `std.control.nondet.nondet.#act-method:once` の型が
  `'a ['b & [std::control::nondet::nondet; 'b]] -> ['b] std::data::opt::opt 'a`
  として export されることを `dump-poly-std-in` で確認した。

## 確認（wildcard effect thunk / act method row boundary）

- `timeout 60s cargo fmt --all`
- `timeout 60s cargo test -q -p infer constraints::tests::case_02::tail_alias_keeps_row_upper_across_stack_boundary -- --test-threads=1 --nocapture`
- `timeout 60s cargo test -q -p infer constraints::tests::case_02::direct_tail_upper_prunes_stale_unweighted_row_upper_from_aliases -- --test-threads=1 --nocapture`
- `timeout 60s cargo test -q -p infer annotation::tests::tests::function_arg_wildcard_effect_annotation_keeps_stack_boundary -- --test-threads=1 --nocapture`
- `timeout 60s cargo test -q -p infer lowering::tests::case_05::lambda_param_effect_annotation_uses_inner_arg_effect_and_subtracts_output -- --test-threads=1 --nocapture`
- `timeout 60s cargo test -q -p specialize string_input_keeps_unused_effectful_thunk_argument_suspended -- --test-threads=1 --nocapture`
- `timeout 90s cargo test -q -p yulang run_control_source_text_with_embedded_std_runs_nondet_once_triple -- --test-threads=1 --nocapture`
- `timeout 90s cargo test -q -p yulang run_control_source_text_with_embedded_playground_std_runs_list_update_example -- --test-threads=1 --nocapture`
- `timeout 90s cargo test -q -p yulang dump_mono_with_std_specializes_nondet_sum_list_say_benchmark -- --test-threads=1 --nocapture`
- `timeout 180s cargo test -q -p infer -- --test-threads=1`
- `timeout 90s cargo test -q -p specialize -- --test-threads=1`
- `timeout 90s cargo test -q -p control-vm -- --test-threads=1`
- `timeout 180s cargo test -q -p yulang --lib -- --test-threads=1`

## 追加確認（crate sweep）

- `timeout 90s cargo test -q -p parser -- --test-threads=1`
- `timeout 90s cargo test -q -p poly -- --test-threads=1`
- `timeout 90s cargo test -q -p mono -- --test-threads=1`
- `timeout 90s cargo test -q -p list-tree -- --test-threads=1`
- `timeout 90s cargo test -q -p mono-runtime -- --test-threads=1`
- `timeout 90s cargo test -q -p sources -- --test-threads=1`
- `timeout 90s cargo test -q -p yulang-editor -- --test-threads=1`
- `timeout 90s cargo test -q -p wasm -- --test-threads=1`

## parameter effect annotation の full effect / handler view 分離

- `std.control.nondet.nondet.#act-method:once` は shallow handler ではないため、
  `α [nondet; β] -> [β] opt α` として export されるべきだった。
- parameter の effectful annotation で、通常参照に使う full effect と、`catch x:` が使う
  stack-protected view を分けた。`x` を単に forward するだけなら full effect が結果に残り、
  handler と同じ `α & [handled; α] -> [α]` にはならない。
- caller-facing な `arg_eff` は concrete row では `full & row` とし、`catch` は full effect に
  handled row upper を張りつつ、stack view で subtraction hygiene を保つ。
- `[_]` の stack quantified handler annotation だけは、既存どおり lambda output の subtract
  id を持ち回る。具体 row annotation では output 全体に機械的な subtract を混ぜない。
- `StackWeight` は `floor[Empty]` 後の concrete stack を吸収せず、後続の `Empty` stack だけを
  吸収するようにした。既存の pop まで concrete stack を残す性質を保つため。

## 確認（parameter effect annotation）

- `timeout 30s cargo test -q -p infer lowering::tests::case_05::effectful_parameter_forwarding_keeps_unhandled_effect -- --test-threads=1 --nocapture`
- `timeout 30s cargo test -q -p infer lowering::tests::case_05::lambda_param_effect_annotation_exposes_row_arg_effect_without_subtracting_body -- --test-threads=1 --nocapture`
- `timeout 30s cargo test -q -p infer lowering::tests::case_05::lambda_param_effect_annotation_with_tail_exposes_full_effect_and_row -- --test-threads=1 --nocapture`
- `timeout 30s cargo test -q -p infer annotation::tests::tests::parameter_effectful_annotation_with_tail_exposes_full_effect_and_row -- --test-threads=1 --nocapture`
- `timeout 30s cargo test -q -p yulang dump_poly_without_std_keeps_annotated_shallow_handler_type_clean -- --test-threads=1 --nocapture`
- `timeout 30s cargo test -q -p yulang dump_poly_std_nondet_once_act_method_uses_deep_handler_effect -- --test-threads=1 --nocapture`
- `timeout 30s cargo test -q -p specialize tests::tests::string_input_specializes_stack_quantified_handler_annotation -- --test-threads=1 --nocapture`
- `timeout 30s cargo test -q -p control-vm tests::runs_stack_handler_hygiene_to_outer_handler -- --test-threads=1 --nocapture`
- `timeout 180s cargo test -q -p infer -- --test-threads=1`
- `timeout 180s cargo test -q -p yulang --lib -- --test-threads=1`
- `timeout 90s cargo test -q -p specialize -- --test-threads=1`
- `timeout 90s cargo test -q -p control-vm -- --test-threads=1`
- `timeout 90s cargo test -q -p poly -- --test-threads=1`
- `timeout 90s cargo test -q -p parser -- --test-threads=1`
- `timeout 90s cargo test -q -p mono -- --test-threads=1`
- `timeout 90s cargo test -q -p mono-runtime -- --test-threads=1`
- `timeout 90s cargo test -q -p sources -- --test-threads=1`
- `timeout 90s cargo test -q -p list-tree -- --test-threads=1`
- `timeout 90s cargo test -q -p yulang-editor -- --test-threads=1`
- `timeout 90s cargo test -q -p wasm -- --test-threads=1`
- `timeout 60s cargo fmt --all -- --check`
- `git diff --check`
