# `str.lines`
`&str.lines`はあるのに`str.lines`はないので。

# `&str[range]`
`str[range]`はあるから

# `.guard`
`.tap`みたいなやつ。undetを枝刈りはするけどそのまま自分を返す

# replace/replace_with
ruleで文字を探索して、単に文字として置き換えるやつとruleのオブジェクトを取得して置き換えるやつ


