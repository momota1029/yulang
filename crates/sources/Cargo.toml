[package]
name = "sources"
version = "0.1.0"
edition = "2024"

[dependencies]
parser = { path = "../parser" }
rowan = "0.15"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
toml = "0.8"
