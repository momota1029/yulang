use std::data::index::*
use std::data::range::*

type bytes with:
    our b.len = std::text::bytes::len b
    our b.is_empty = std::text::bytes::is_empty b
    our b.concat other = std::text::bytes::concat b other
    our b.to_utf8_raw = std::text::bytes::to_utf8_raw b
    our b.to_utf8_lossy = std::text::bytes::to_utf8_lossy b

pub is_empty(b: bytes): bool = std::int::eq(std::text::bytes::len b, 0)

pub to_utf8_lossy(b: bytes): str = case std::text::bytes::to_utf8_raw b:
    (text, _) -> text

impl Index bytes int:
    type value = int
    our b.index i = std::text::bytes::index_raw b i

impl Index bytes range:
    type value = bytes
    our b.index r = std::text::bytes::index_range b r
