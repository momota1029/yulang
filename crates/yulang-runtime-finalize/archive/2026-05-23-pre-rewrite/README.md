# yulang-runtime-finalize pre-rewrite archive

This directory preserves the committed `yulang-runtime-finalize` implementation
before the demand/fixpoint rewrite started on 2026-05-23.

The files here are not part of the active crate. They are kept only as a
reference for tests, diagnostics wording, and previous coverage while the
active `src/` tree is rebuilt around global demands and Top/Bottom-aware
bounds.
