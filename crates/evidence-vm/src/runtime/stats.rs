//! Evidence VM runtime counters.
//!
//! This module is intentionally just the data surface. Runtime code increments these
//! counters, while CLI/debug code decides which groups to print.

#[derive(Debug, Clone, Copy, Default, PartialEq, Eq)]
pub struct RuntimeEvidenceRunStats {
    pub runtime_breakdown_stats_enabled: bool,
    pub effect_calls: usize,
    pub direct_effect_calls: usize,
    pub plan_provider_slots: usize,
    pub plan_provider_candidates: usize,
    pub plan_env_provider_slots: usize,
    pub plan_env_provider_candidates: usize,
    pub plan_direct_candidates: usize,
    pub plan_effect_routes: usize,
    pub plan_direct_effect_routes: usize,
    pub plan_direct_abortive_effect_routes: usize,
    pub plan_direct_tail_resumptive_effect_routes: usize,
    pub plan_known_handlers: usize,
    pub plan_known_state_handlers: usize,
    pub plan_known_state_handler_compiler_certificates: usize,
    pub plan_known_operation_calls: usize,
    pub plan_known_operation_state_get_candidates: usize,
    pub plan_known_operation_state_set_candidates: usize,
    pub plan_known_operation_state_direct_gets: usize,
    pub plan_known_operation_state_direct_sets: usize,
    pub plan_known_state_operation_route_proofs: usize,
    pub plan_known_operation_reject_no_operation_object: usize,
    pub plan_known_operation_reject_not_call: usize,
    pub plan_known_operation_reject_no_visibility: usize,
    pub plan_known_operation_reject_no_candidate_handler: usize,
    pub plan_known_operation_reject_no_known_state_access_proof: usize,
    pub plan_known_operation_reject_known_state_access_handler_mismatch: usize,
    pub plan_known_operation_reject_known_state_access_boundary_unsafe: usize,
    pub plan_known_operation_reject_direct_execution_disabled: usize,
    pub plan_known_operation_reject_no_known_handler: usize,
    pub plan_known_operation_reject_wrong_handler: usize,
    pub plan_known_operation_reject_wrong_operation: usize,
    pub plan_known_operation_reject_blocked: usize,
    pub plan_known_operation_reject_delayed: usize,
    pub plan_known_operation_reject_provider_dirty: usize,
    pub runtime_provider_env_values: usize,
    pub runtime_provider_env_slots: usize,
    pub runtime_provider_env_candidates: usize,
    pub runtime_provider_env_reads: usize,
    pub runtime_provider_env_read_slots: usize,
    pub runtime_provider_env_read_candidates: usize,
    pub runtime_provider_env_route_lookups: usize,
    pub runtime_provider_env_route_hits: usize,
    pub runtime_provider_env_route_hit_direct_abortive: usize,
    pub runtime_provider_env_route_hit_direct_tail_resumptive: usize,
    pub runtime_provider_env_route_hit_yield_fallback: usize,
    pub runtime_provider_env_route_hit_blocked_fallback: usize,
    pub runtime_provider_env_route_hit_generic_fallback: usize,
    pub runtime_provider_env_route_hit_unhandled: usize,
    pub route_cert_none: usize,
    pub route_cert_static_direct: usize,
    pub route_cert_provider_grant: usize,
    pub route_cert_provider_grant_clean: usize,
    pub route_cert_provider_grant_dirty_scope: usize,
    pub route_cert_provider_grant_dirty_add_id: usize,
    pub route_cert_provider_grant_dirty_handler: usize,
    pub route_cert_provider_grant_dirty_missing: usize,
    pub route_cert_request_free: usize,
    pub route_cert_legacy_request_fallbacks: usize,
    pub direct_tail_gate_fail_no_grant: usize,
    pub direct_tail_gate_fail_missing_grant: usize,
    pub direct_tail_gate_fail_scope_missing: usize,
    pub direct_tail_gate_fail_add_id_shadowed: usize,
    pub direct_tail_gate_fail_add_id_all_path: usize,
    pub direct_tail_gate_fail_add_id_own_path: usize,
    pub direct_tail_gate_fail_add_id_foreign_path: usize,
    pub direct_tail_gate_fail_handler_shadowed: usize,
    pub direct_tail_guarded_add_id_shadowed: usize,
    pub direct_tail_guarded_add_id_all_path: usize,
    pub direct_tail_guarded_add_id_own_path: usize,
    pub direct_tail_guarded_add_id_foreign_path: usize,
    pub permission_visibility_signals: usize,
    pub permission_visibility_identity: usize,
    pub permission_visibility_legacy_bridge: usize,
    pub permission_visibility_guard_mask: usize,
    pub permission_visibility_resume_delta: usize,
    pub permission_visibility_handler_boundary_mask: usize,
    pub permission_visibility_guard_and_boundary_mask: usize,
    pub permission_visibility_guard_without_boundary: usize,
    pub permission_visibility_boundary_without_guard: usize,
    pub permission_shadow_provider_boundary_pair: usize,
    pub permission_shadow_provider_boundary_pair_legacy_visible: usize,
    pub permission_shadow_provider_boundary_pair_permission_visible: usize,
    pub permission_shadow_provider_boundary_pair_match: usize,
    pub permission_shadow_provider_boundary_pair_mismatch: usize,
    pub permission_shadow_provider_boundary_pair_no_allowed_handler: usize,
    pub permission_provider_boundary_pair_fast_paths: usize,
    pub permission_provider_boundary_pair_fast_path_visible: usize,
    pub permission_provider_boundary_pair_fast_path_invisible: usize,
    pub permission_provider_boundary_pair_fast_path_no_allowed_handler: usize,
    pub permission_provider_boundary_pair_native_shadow: usize,
    pub permission_provider_boundary_pair_native_shadow_legacy_visible: usize,
    pub permission_provider_boundary_pair_native_shadow_native_visible: usize,
    pub permission_provider_boundary_pair_native_shadow_match: usize,
    pub permission_provider_boundary_pair_native_shadow_mismatch: usize,
    pub permission_provider_boundary_pair_native_shadow_no_allowed_handler: usize,
    pub provider_add_id_shortcut_attempts: usize,
    pub provider_add_id_shortcut_used: usize,
    pub provider_add_id_shortcut_fallback_carry_after_frame: usize,
    pub provider_add_id_shortcut_fallback_no_provider_permission: usize,
    pub provider_add_id_shortcut_full_scan_guard_visible_match: usize,
    pub provider_add_id_shortcut_full_scan_guard_visible_mismatch: usize,
    pub provider_add_id_shortcut_full_scan_extra_guards: usize,
    pub provider_add_id_shortcut_full_scan_extra_carried_guards: usize,
    pub provider_add_id_shortcut_visible_verify: usize,
    pub provider_add_id_shortcut_visible_match: usize,
    pub provider_add_id_shortcut_visible_mismatch: usize,
    pub provider_add_id_shortcut_full_scan_visible: usize,
    pub expr_evals: usize,
    pub env_clones: usize,
    pub env_entries_cloned: usize,
    pub apply_value_calls: usize,
    pub apply_adapter_calls: usize,
    pub apply_adapter_inline_attempts: usize,
    pub apply_adapter_inline_hits: usize,
    pub apply_adapter_inline_effect_fallbacks: usize,
    pub adapt_value_calls: usize,
    pub adapt_value_inline_thunk_wraps: usize,
    pub primitive_apply_calls: usize,
    pub forced_effect_call_fusions: usize,
    pub known_state_direct_gets: usize,
    pub known_state_direct_sets: usize,
    pub known_state_direct_missing_state: usize,
    pub known_state_direct_non_resumptive: usize,
    pub known_state_frame_entries: usize,
    pub known_state_frame_exits: usize,
    pub known_state_frame_reads_late: usize,
    pub known_state_frame_writes_late: usize,
    pub known_state_frame_missing_late: usize,
    pub known_state_frame_shadow_compat_fallbacks: usize,
    pub known_state_frame_snapshots: usize,
    pub known_state_frame_snapshot_missing_frame: usize,
    pub known_state_frame_snapshot_max_depth: usize,
    pub known_state_frame_forks: usize,
    pub known_state_frame_multishot_forks: usize,
    pub known_state_frame_resume_entries: usize,
    pub known_state_frame_resume_exits: usize,
    pub known_state_frame_resume_request_rewraps: usize,
    pub ref_set_evals: usize,
    pub ref_set_update_effect_calls: usize,
    pub ref_set_assignment_ref_update_requests: usize,
    pub ref_set_value_ref_update_requests: usize,
    pub known_operation_state_get_candidate_hits: usize,
    pub known_operation_state_set_candidate_hits: usize,
    pub known_operation_state_get_active_frame_hits: usize,
    pub known_operation_state_set_active_frame_hits: usize,
    pub known_operation_state_get_active_frame_misses: usize,
    pub known_operation_state_set_active_frame_misses: usize,
    pub known_operation_state_direct_get_plan_hits: usize,
    pub known_operation_state_direct_set_plan_hits: usize,
    pub known_operation_route_shadow_hits: usize,
    pub known_operation_route_shadow_get_hits: usize,
    pub known_operation_route_shadow_set_hits: usize,
    pub known_operation_route_shadow_missing_proof: usize,
    pub known_operation_route_shadow_missing_frame: usize,
    pub known_operation_route_shadow_role_mismatch: usize,
    pub known_operation_route_direct_get_attempts: usize,
    pub known_operation_route_direct_get_hits: usize,
    pub known_operation_route_direct_get_missing_proof: usize,
    pub known_operation_route_direct_get_missing_frame: usize,
    pub known_operation_route_direct_get_role_mismatch: usize,
    pub known_operation_route_direct_get_payload_mismatch: usize,
    pub known_operation_route_direct_set_attempts: usize,
    pub known_operation_route_direct_set_hits: usize,
    pub known_operation_route_direct_set_missing_proof: usize,
    pub known_operation_route_direct_set_missing_frame: usize,
    pub known_operation_route_direct_set_role_mismatch: usize,
    pub known_operation_route_direct_set_payload_mismatch: usize,
    pub known_operation_reject_no_visibility_hits: usize,
    pub known_operation_reject_no_candidate_handler_hits: usize,
    pub known_operation_reject_no_known_handler_hits: usize,
    pub known_operation_reject_wrong_handler_hits: usize,
    pub known_operation_reject_wrong_operation_hits: usize,
    pub known_operation_reject_blocked_hits: usize,
    pub known_operation_reject_delayed_hits: usize,
    pub known_operation_reject_provider_dirty_hits: usize,
    pub thunk_forces: usize,
    pub thunk_force_expr: usize,
    pub thunk_force_effect: usize,
    pub thunk_force_continuation: usize,
    pub thunk_force_value: usize,
    pub thunk_force_adapter: usize,
    pub thunk_force_adapter_inline_hits: usize,
    pub make_thunk_inline_value_hits: usize,
    pub continuation_appends: usize,
    pub continuation_owned_tail_appends: usize,
    pub continuation_append_steps: usize,
    pub request_continuation_appends: usize,
    pub request_continuation_owned_tail_appends: usize,
    pub request_continuation_then_appends: usize,
    pub direct_tail_continuation_appends: usize,
    pub direct_tail_continuation_owned_tail_appends: usize,
    pub direct_tail_continuation_then_appends: usize,
    pub continuation_append_blocked_by_marker_frame: usize,
    pub continuation_append_blocked_by_provider_env: usize,
    pub continuation_append_blocked_by_rc_shared: usize,
    pub request_continuation_append_blocked_by_marker_frame: usize,
    pub request_continuation_append_blocked_by_provider_env: usize,
    pub request_continuation_append_blocked_by_rc_shared: usize,
    pub direct_tail_continuation_append_blocked_by_marker_frame: usize,
    pub direct_tail_continuation_append_blocked_by_provider_env: usize,
    pub direct_tail_continuation_append_blocked_by_rc_shared: usize,
    pub direct_tail_permission_boundary_append_candidates: usize,
    pub direct_tail_permission_boundary_append_provider_pair: usize,
    pub direct_tail_permission_boundary_append_reject_no_provider_permission: usize,
    pub direct_tail_permission_boundary_append_reject_resume_delta: usize,
    pub direct_tail_permission_boundary_append_reject_handler_path: usize,
    pub direct_tail_permission_boundary_append_reject_legacy_bridge: usize,
    pub direct_tail_permission_boundary_append_reject_other_transform: usize,
    pub direct_tail_segment_candidates: usize,
    pub direct_tail_segment_materialized_continuations: usize,
    pub direct_tail_segment_created: usize,
    pub direct_tail_segment_to_tree_fallbacks: usize,
    pub direct_tail_segment_identity: usize,
    pub direct_tail_segment_eval_only: usize,
    pub direct_tail_segment_eval_frames: usize,
    pub direct_tail_segment_then_frames: usize,
    pub direct_tail_segment_scope_marker_frames: usize,
    pub direct_tail_segment_scope_marker_dynamic: usize,
    pub direct_tail_segment_scope_marker_empty: usize,
    pub direct_tail_segment_scope_provider_env_frames: usize,
    pub direct_tail_segment_request_boundary_rejected: usize,
    pub resume_plan_shadow_candidates: usize,
    pub resume_plan_shadow_direct_tail_candidates: usize,
    pub resume_plan_shadow_resume_pack_candidates: usize,
    pub resume_plan_shadow_eval_frames: usize,
    pub resume_plan_shadow_then_frames: usize,
    pub resume_plan_shadow_catch_boundaries: usize,
    pub resume_plan_shadow_catch_same_handler: usize,
    pub resume_plan_shadow_catch_no_routed_handler: usize,
    pub resume_plan_shadow_catch_foreign_handler: usize,
    pub resume_plan_shadow_ref_set_boundaries: usize,
    pub resume_plan_shadow_marker_frames: usize,
    pub resume_plan_shadow_marker_dynamic_frames: usize,
    pub resume_plan_shadow_marker_empty_frames: usize,
    pub resume_plan_shadow_marker_active_add_id_ops: usize,
    pub resume_plan_shadow_marker_handler_boundary_ops: usize,
    pub resume_plan_shadow_provider_env_deltas: usize,
    pub resume_plan_shadow_provider_grant_dirty_add_id: usize,
    pub resume_plan_shadow_multi_shot: usize,
    pub resume_plan_shadow_eval_only: usize,
    pub resume_plan_shadow_with_catch_boundary: usize,
    pub resume_plan_shadow_with_dynamic_marker: usize,
    pub resume_plan_shadow_with_provider_delta: usize,
    pub resume_plan_candidate_candidates: usize,
    pub resume_plan_candidate_direct_tail_candidates: usize,
    pub resume_plan_candidate_resume_pack_candidates: usize,
    pub resume_plan_candidate_planned: usize,
    pub resume_plan_candidate_rejected: usize,
    pub resume_plan_candidate_reject_ref_set: usize,
    pub resume_plan_candidate_multi_shot: usize,
    pub resume_plan_candidate_eval_frames: usize,
    pub resume_plan_candidate_estimated_resume_steps: usize,
    pub resume_plan_candidate_with_trace: usize,
    pub resume_plan_candidate_with_eval_delta: usize,
    pub resume_plan_candidate_request_delta_none: usize,
    pub resume_plan_candidate_request_delta_catch_same: usize,
    pub resume_plan_candidate_request_delta_catch_no_routed: usize,
    pub resume_plan_candidate_request_delta_catch_foreign: usize,
    pub resume_plan_candidate_with_request_delta: usize,
    pub resume_plan_candidate_with_marker_delta: usize,
    pub resume_plan_candidate_with_provider_delta: usize,
    pub resume_plan_candidate_with_both_scope_deltas: usize,
    pub resume_plan_candidate_provider_dirty_add_id: usize,
    pub resume_plan_cert_candidates: usize,
    pub resume_plan_cert_provider_dirty_foreign_scope: usize,
    pub resume_plan_cert_direct_tail_provider_dirty_foreign_scope: usize,
    pub resume_plan_cert_resume_pack_provider_dirty_foreign_scope: usize,
    pub resume_plan_cert_provider_dirty_foreign_scope_handlers: usize,
    pub resume_plan_cert_provider_dirty_foreign_scope_estimated_resume_steps: usize,
    pub resume_plan_cert_rejected: usize,
    pub resume_plan_cert_reject_not_planned: usize,
    pub resume_plan_cert_reject_missing_trace: usize,
    pub resume_plan_cert_reject_missing_eval_delta: usize,
    pub resume_plan_cert_reject_missing_request_delta: usize,
    pub resume_plan_cert_reject_missing_marker_delta: usize,
    pub resume_plan_cert_reject_missing_provider_delta: usize,
    pub resume_plan_cert_reject_route_not_provider_dirty_add_id: usize,
    pub resume_plan_cert_reject_multi_shot: usize,
    pub resume_plan_cert_reject_request_none: usize,
    pub resume_plan_cert_reject_request_catch_same_or_no_routed: usize,
    pub resume_plan_cert_reject_request_no_foreign_catch: usize,
    pub resume_plan_plans: usize,
    pub resume_plan_direct_tail_plans: usize,
    pub resume_plan_resume_pack_plans: usize,
    pub resume_plan_provider_dirty_foreign_scope_plans: usize,
    pub resume_plan_trace_plans: usize,
    pub resume_plan_trace_direct_tail_plans: usize,
    pub resume_plan_trace_resume_pack_plans: usize,
    pub resume_plan_trace_steps: usize,
    pub resume_plan_trace_eval_steps: usize,
    pub resume_plan_trace_request_steps: usize,
    pub resume_plan_trace_marker_steps: usize,
    pub resume_plan_trace_provider_steps: usize,
    pub resume_plan_trace_max_steps: usize,
    pub resume_plan_scoped_trace_plans: usize,
    pub resume_plan_scoped_trace_scopes: usize,
    pub resume_plan_scoped_trace_provider_child_scopes: usize,
    pub resume_plan_scoped_trace_steps: usize,
    pub resume_plan_scoped_trace_root_marker_steps: usize,
    pub resume_plan_scoped_trace_child_marker_steps: usize,
    pub resume_plan_scoped_trace_root_marker_matches_marker_delta: usize,
    pub resume_plan_scoped_trace_root_marker_mismatches_marker_delta: usize,
    pub resume_plan_sidecar_shadow_candidates: usize,
    pub resume_plan_sidecar_shadow_ready: usize,
    pub resume_plan_sidecar_shadow_reject_root_marker_mismatch: usize,
    pub resume_plan_sidecar_shadow_reject_provider_scope_mismatch: usize,
    pub resume_plan_sidecar_shadow_marker_frames: usize,
    pub resume_plan_sidecar_shadow_provider_frames: usize,
    pub resume_plan_sidecar_shadow_child_marker_frames: usize,
    pub scope_plan_candidates: usize,
    pub scope_plan_planned: usize,
    pub scope_plan_rejected: usize,
    pub scope_plan_weighted_resume_steps: usize,
    pub scope_plan_weighted_planned_resume_steps: usize,
    pub scope_plan_root_marker_match: usize,
    pub scope_plan_root_marker_mismatch: usize,
    pub scope_plan_provider_child_scopes: usize,
    pub scope_plan_scope_count: usize,
    pub scope_plan_eval_steps: usize,
    pub scope_plan_request_steps: usize,
    pub scope_plan_marker_steps: usize,
    pub scope_plan_child_marker_steps: usize,
    pub scope_plan_request_lane_catch_same: usize,
    pub scope_plan_request_lane_catch_foreign: usize,
    pub scope_plan_request_lane_catch_no_routed: usize,
    pub scope_plan_request_lane_ref_set: usize,
    pub scope_plan_legacy_tree_fallback_available: usize,
    pub scope_plan_foreign_catch_candidates: usize,
    pub scope_plan_foreign_catch_ready: usize,
    pub scope_plan_foreign_catch_boundaries: usize,
    pub scope_plan_foreign_catch_signal_passthrough_boundaries: usize,
    pub scope_plan_foreign_catch_value_resume_boundaries: usize,
    pub scope_plan_foreign_catch_legacy_materialize_fallback_available: usize,
    pub scope_plan_catch_boundary_delta_plans: usize,
    pub scope_plan_catch_boundary_delta_boundaries: usize,
    pub scope_plan_catch_boundary_delta_foreign_pass_through: usize,
    pub scope_plan_catch_boundary_delta_same_handler: usize,
    pub scope_plan_catch_boundary_delta_no_routed: usize,
    pub scope_plan_catch_foreign_boundary_delta_ready: usize,
    pub scope_plan_catch_foreign_boundary_delta_signal_ready: usize,
    pub scope_plan_catch_foreign_boundary_delta_value_resume_ready: usize,
    pub scope_plan_catch_foreign_boundary_delta_shape_digest_ready: usize,
    pub scope_plan_catch_foreign_boundary_delta_signal_shadow: usize,
    pub scope_plan_catch_foreign_boundary_delta_signal_match: usize,
    pub scope_plan_catch_foreign_boundary_delta_signal_mismatch: usize,
    pub scope_plan_catch_foreign_boundary_delta_generic_fallback_shadow: usize,
    pub scope_plan_catch_foreign_boundary_delta_generic_fallback_match: usize,
    pub scope_plan_catch_foreign_boundary_delta_generic_fallback_mismatch: usize,
    pub scope_plan_catch_foreign_boundary_delta_materialized_shape_shadow: usize,
    pub scope_plan_catch_foreign_boundary_delta_materialized_shape_match: usize,
    pub scope_plan_catch_foreign_boundary_delta_materialized_shape_mismatch: usize,
    pub scope_plan_catch_foreign_boundary_delta_value_resume_shadow: usize,
    pub scope_plan_catch_foreign_boundary_delta_value_resume_match: usize,
    pub scope_plan_catch_foreign_boundary_delta_value_resume_mismatch: usize,
    pub catch_foreign_boundary_delta_direct_tail_appends: usize,
    pub catch_foreign_boundary_delta_direct_tail_scope_blockers: usize,
    pub catch_foreign_boundary_delta_direct_tail_probe_enabled: usize,
    pub catch_foreign_boundary_delta_direct_tail_probe_skipped: usize,
    pub catch_foreign_boundary_segment_direct_tail_appends: usize,
    pub catch_foreign_boundary_segment_merged_boundaries: usize,
    pub catch_foreign_boundary_segment_resume_entries: usize,
    pub catch_foreign_boundary_segment_resume_boundaries: usize,
    pub catch_foreign_boundary_segment_resume_effect_fallbacks: usize,
    pub catch_foreign_boundary_segment_signal_materialize_fallbacks: usize,
    pub scope_exec_hits: usize,
    pub scope_exec_foreign_catch_hits: usize,
    pub scope_exec_tree_fallbacks: usize,
    pub scope_exec_signal_passthrough_hits: usize,
    pub scope_exec_value_resume_hits: usize,
    pub scope_exec_reject_not_one_shot: usize,
    pub scope_exec_reject_root_marker_mismatch: usize,
    pub scope_exec_reject_non_foreign_catch: usize,
    pub scope_exec_reject_ref_set: usize,
    pub scope_exec_reject_missing_materialize_fallback: usize,
    pub scope_exec_reject_shape_mismatch: usize,
    pub scope_plan_foreign_catch_reject_root_marker_mismatch: usize,
    pub scope_plan_foreign_catch_reject_missing_request_delta: usize,
    pub scope_plan_foreign_catch_reject_same_handler: usize,
    pub scope_plan_foreign_catch_reject_no_routed: usize,
    pub scope_plan_foreign_catch_reject_ref_set: usize,
    pub resume_plan_exec_attempts: usize,
    pub resume_plan_exec_hits: usize,
    pub resume_plan_exec_fallbacks: usize,
    pub resume_plan_exec_reject_shape: usize,
    pub resume_plan_exec_reject_boundary: usize,
    pub resume_plan_exec_materialized_legacy: usize,
    pub resume_plan_exec_eval_steps_saved_estimate: usize,
    pub resume_plan_exec_shadow_checks: usize,
    pub resume_plan_exec_shadow_ready: usize,
    pub resume_plan_exec_shadow_direct_tail_ready: usize,
    pub resume_plan_exec_shadow_resume_pack_ready: usize,
    pub resume_plan_exec_shadow_steps: usize,
    pub resume_plan_exec_shadow_estimated_saved_steps: usize,
    pub resume_plan_exec_shadow_reject_missing_trace: usize,
    pub resume_plan_exec_shadow_reject_missing_eval_delta: usize,
    pub resume_plan_exec_shadow_reject_missing_request_delta: usize,
    pub resume_plan_exec_shadow_reject_missing_marker_delta: usize,
    pub resume_plan_exec_shadow_reject_missing_provider_delta: usize,
    pub resume_plan_exec_shadow_reject_trace_eval_bounds: usize,
    pub resume_plan_exec_shadow_reject_trace_request_bounds: usize,
    pub resume_plan_exec_shadow_reject_trace_marker_bounds: usize,
    pub resume_plan_exec_shadow_reject_trace_provider_bounds: usize,
    pub resume_plan_exec_shadow_reject_trace_unused_payload: usize,
    pub resume_plan_shadow_request_delta_candidates: usize,
    pub resume_plan_shadow_request_delta_direct_tail_candidates: usize,
    pub resume_plan_shadow_request_delta_resume_pack_candidates: usize,
    pub resume_plan_shadow_request_delta_reject_no_catch_boundary: usize,
    pub resume_plan_shadow_request_delta_reject_ref_set_boundary: usize,
    pub resume_plan_shadow_request_delta_requires_marker_delta: usize,
    pub resume_plan_shadow_request_delta_requires_provider_delta: usize,
    pub resume_plan_shadow_request_delta_requires_both_scope_deltas: usize,
    pub resume_plan_shadow_request_delta_provider_dirty_add_id: usize,
    pub resume_plan_eval_delta_plans: usize,
    pub resume_plan_eval_delta_direct_tail_plans: usize,
    pub resume_plan_eval_delta_resume_pack_plans: usize,
    pub resume_plan_eval_delta_frames: usize,
    pub resume_plan_eval_delta_force_frames: usize,
    pub resume_plan_eval_delta_apply_frames: usize,
    pub resume_plan_eval_delta_adapter_frames: usize,
    pub resume_plan_eval_delta_case_frames: usize,
    pub resume_plan_eval_delta_aggregate_frames: usize,
    pub resume_plan_eval_delta_select_frames: usize,
    pub resume_plan_eval_delta_block_frames: usize,
    pub resume_plan_eval_delta_ref_set_frames: usize,
    pub resume_plan_eval_delta_max_frames: usize,
    pub resume_plan_request_delta_plans: usize,
    pub resume_plan_request_delta_direct_tail_plans: usize,
    pub resume_plan_request_delta_resume_pack_plans: usize,
    pub resume_plan_request_delta_frames: usize,
    pub resume_plan_request_delta_same_handler_frames: usize,
    pub resume_plan_request_delta_no_routed_handler_frames: usize,
    pub resume_plan_request_delta_foreign_handler_frames: usize,
    pub resume_plan_request_delta_max_frames: usize,
    pub resume_plan_marker_delta_candidates: usize,
    pub resume_plan_marker_delta_direct_tail_candidates: usize,
    pub resume_plan_marker_delta_resume_pack_candidates: usize,
    pub resume_plan_marker_delta_unique: usize,
    pub resume_plan_marker_delta_reused: usize,
    pub resume_plan_marker_delta_frames: usize,
    pub resume_plan_marker_delta_active_add_id_ops: usize,
    pub resume_plan_marker_delta_handler_boundary_ops: usize,
    pub resume_plan_marker_delta_max_frames: usize,
    pub resume_plan_provider_delta_candidates: usize,
    pub resume_plan_provider_delta_direct_tail_candidates: usize,
    pub resume_plan_provider_delta_resume_pack_candidates: usize,
    pub resume_plan_provider_delta_unique: usize,
    pub resume_plan_provider_delta_reused: usize,
    pub resume_plan_provider_delta_frames: usize,
    pub resume_plan_provider_delta_slots: usize,
    pub resume_plan_provider_delta_handler_candidates: usize,
    pub resume_plan_provider_delta_max_frames: usize,
    pub resume_pack_candidates: usize,
    pub resume_pack_thunks_forced: usize,
    pub resume_pack_multi_shot_required: usize,
    pub resume_pack_can_share_segment: usize,
    pub resume_pack_to_tree_fallbacks: usize,
    pub resume_pack_identity: usize,
    pub resume_pack_eval_frames: usize,
    pub resume_pack_then_frames: usize,
    pub resume_pack_scope_marker_frames: usize,
    pub resume_pack_scope_provider_env_frames: usize,
    pub resume_pack_request_boundary_rejected: usize,
    pub continuation_resume_steps: usize,
    pub continuation_resume_then_steps: usize,
    pub continuation_resume_then_first_marker_frame: usize,
    pub continuation_resume_then_first_provider_env: usize,
    pub continuation_resume_then_first_other: usize,
    pub continuation_resume_then_second_marker_frame: usize,
    pub continuation_resume_then_second_provider_env: usize,
    pub continuation_resume_then_second_other: usize,
    pub continuation_resume_then_plain: usize,
    pub continuation_resume_force_steps: usize,
    pub continuation_resume_apply_steps: usize,
    pub continuation_resume_adapter_steps: usize,
    pub continuation_resume_case_steps: usize,
    pub continuation_resume_catch_steps: usize,
    pub continuation_resume_marker_steps: usize,
    pub continuation_resume_marker_identity_fast_paths: usize,
    pub continuation_resume_marker_empty_markers: usize,
    pub continuation_resume_marker_with_active_add_id: usize,
    pub continuation_resume_marker_with_handler_path: usize,
    pub continuation_resume_marker_result_value: usize,
    pub continuation_resume_marker_result_direct_tail: usize,
    pub continuation_resume_marker_result_direct_tail_provider_permission: usize,
    pub continuation_resume_marker_result_direct_tail_provider_boundary_pair: usize,
    pub continuation_resume_marker_result_legacy_signal: usize,
    pub continuation_resume_marker_result_error: usize,
    pub resume_marker_permission_native_candidates: usize,
    pub resume_marker_permission_native_provider_pair: usize,
    pub resume_marker_permission_native_reject_no_provider_permission: usize,
    pub resume_marker_permission_native_reject_resume_delta: usize,
    pub resume_marker_permission_native_reject_handler_path: usize,
    pub resume_marker_permission_native_reject_value_result: usize,
    pub resume_marker_permission_native_reject_legacy_signal: usize,
    pub resume_marker_permission_native_reject_legacy_bridge: usize,
    pub resume_marker_permission_native_reject_other_transform: usize,
    pub resume_marker_permission_native_reject_error_result: usize,
    pub resume_marker_provider_pair_close_candidates: usize,
    pub resume_marker_provider_pair_close_shadow: usize,
    pub resume_marker_provider_pair_close_shadow_match: usize,
    pub resume_marker_provider_pair_close_shadow_mismatch: usize,
    pub resume_marker_provider_pair_close_shadow_legacy_visible: usize,
    pub resume_marker_provider_pair_close_shadow_permission_visible: usize,
    pub resume_marker_provider_pair_close_reject_no_provider_permission: usize,
    pub resume_marker_provider_pair_close_reject_resume_delta: usize,
    pub resume_marker_provider_pair_close_reject_handler_path: usize,
    pub resume_marker_provider_pair_close_reject_handler_path_blocked: usize,
    pub resume_marker_provider_pair_close_reject_handler_path_unblocked: usize,
    pub resume_marker_provider_pair_close_reject_handler_path_no_boundary: usize,
    pub resume_marker_provider_pair_close_reject_handler_path_same_family: usize,
    pub resume_marker_provider_pair_close_reject_handler_path_foreign_family: usize,
    pub resume_marker_provider_pair_close_reject_handler_path_matches_call_boundary: usize,
    pub resume_marker_provider_pair_close_reject_handler_path_differs_call_boundary: usize,
    pub resume_marker_provider_pair_close_reject_handler_path_matches_allowed_handler_family: usize,
    pub resume_marker_provider_pair_close_reject_handler_path_differs_allowed_handler_family: usize,
    pub resume_marker_provider_pair_close_reject_handler_path_prefixes_allowed_handler_family:
        usize,
    pub resume_marker_provider_pair_close_reject_handler_path_allowed_handler_family_prefixes:
        usize,
    pub resume_marker_provider_pair_close_reject_handler_path_permission_handler_unknown: usize,
    pub resume_marker_provider_pair_close_reject_handler_path_boundary_id_matches_permission_handler:
        usize,
    pub resume_marker_provider_pair_close_reject_handler_path_boundary_id_differs_permission_handler:
        usize,
    pub resume_marker_provider_pair_close_reject_handler_path_boundary_id_permission_handler_unknown:
        usize,
    pub resume_marker_provider_pair_close_reject_carry_after_frame: usize,
    pub resume_marker_provider_pair_close_reject_legacy_bridge: usize,
    pub resume_marker_provider_pair_close_reject_other_transform: usize,
    pub resume_marker_provider_pair_close_native_hits: usize,
    pub resume_marker_provider_pair_close_legacy_fallbacks: usize,
    pub resume_marker_provider_prefix_boundary_candidates: usize,
    pub resume_marker_provider_prefix_boundary_native_hits: usize,
    pub resume_marker_provider_prefix_boundary_legacy_fallbacks: usize,
    pub resume_marker_provider_prefix_boundary_shadow: usize,
    pub resume_marker_provider_prefix_boundary_shadow_match: usize,
    pub resume_marker_provider_prefix_boundary_shadow_mismatch: usize,
    pub resume_marker_provider_prefix_boundary_shadow_legacy_visible: usize,
    pub resume_marker_provider_prefix_boundary_shadow_permission_visible: usize,
    pub resume_marker_provider_prefix_boundary_reject_no_boundary: usize,
    pub resume_marker_provider_prefix_boundary_reject_blocked: usize,
    pub resume_marker_provider_prefix_boundary_reject_call_boundary_mismatch: usize,
    pub resume_marker_provider_prefix_boundary_reject_foreign_family: usize,
    pub resume_marker_provider_prefix_boundary_reject_permission_unknown: usize,
    pub resume_marker_provider_prefix_boundary_reject_exact_family: usize,
    pub resume_marker_provider_prefix_boundary_reject_permission_family_request_mismatch: usize,
    pub resume_marker_provider_prefix_boundary_reject_carry_after_frame: usize,
    pub resume_marker_provider_prefix_boundary_permission_family_equals_request_path: usize,
    pub resume_marker_provider_prefix_boundary_permission_family_prefixes_request_path: usize,
    pub resume_marker_provider_prefix_boundary_request_path_prefixes_permission_family: usize,
    pub provider_foreign_boundary_candidates: usize,
    pub provider_foreign_boundary_with_provider_env_blocker: usize,
    pub provider_foreign_boundary_without_provider_env_blocker: usize,
    pub provider_foreign_boundary_provider_env_grants_permission: usize,
    pub provider_foreign_boundary_provider_env_misses_permission: usize,
    pub provider_foreign_boundary_blocked_by_marker_frame: usize,
    pub provider_foreign_boundary_with_any_provider_env: usize,
    pub provider_foreign_boundary_without_any_provider_env: usize,
    pub provider_foreign_boundary_any_provider_env_grants_permission: usize,
    pub provider_foreign_boundary_any_provider_env_misses_permission: usize,
    pub provider_foreign_boundary_nearest_provider_env_grants_permission: usize,
    pub provider_foreign_boundary_nearest_provider_env_misses_permission: usize,
    pub provider_foreign_boundary_nearest_provider_env_none: usize,
    pub provider_foreign_boundary_marker_before_nearest_provider_env: usize,
    pub provider_foreign_boundary_any_later_provider_env_grants_permission: usize,
    pub provider_foreign_boundary_any_later_provider_env_misses_permission: usize,
    pub provider_foreign_boundary_provider_env_under_then_first: usize,
    pub provider_foreign_boundary_provider_env_under_then_second: usize,
    pub provider_foreign_boundary_nearest_provider_env_depth_sum: usize,
    pub provider_foreign_boundary_nearest_provider_env_max_depth: usize,
    pub provider_foreign_boundary_permission_family_equals_request_path: usize,
    pub provider_foreign_boundary_permission_family_prefixes_request_path: usize,
    pub provider_foreign_boundary_request_path_prefixes_permission_family: usize,
    pub provider_foreign_boundary_handler_path_prefixes_request_path: usize,
    pub provider_foreign_boundary_request_path_prefixes_handler_path: usize,
    pub provider_env_foreign_boundary_shadow_candidates: usize,
    pub provider_env_foreign_boundary_shadow_reject_nearest_misses: usize,
    pub provider_env_foreign_boundary_shadow_reject_no_provider_env: usize,
    pub provider_env_foreign_boundary_shadow_reject_marker_before_provider_env: usize,
    pub provider_env_foreign_boundary_shadow_reject_depth: usize,
    pub provider_env_foreign_boundary_shadow_reject_then_second: usize,
    pub provider_env_foreign_boundary_shadow_reject_later_grant: usize,
    pub provider_env_foreign_boundary_shadow_reject_permission_family_request_mismatch: usize,
    pub provider_env_foreign_boundary_shadow_reject_handler_path_related_to_request: usize,
    pub provider_env_foreign_boundary_shadow_reject_carry_after_frame: usize,
    pub provider_env_foreign_boundary_shadow_legacy_visible: usize,
    pub provider_env_foreign_boundary_shadow_naive_visible: usize,
    pub provider_env_foreign_boundary_shadow_naive_match: usize,
    pub provider_env_foreign_boundary_shadow_naive_mismatch: usize,
    pub provider_env_foreign_boundary_shadow_blocked_visible: usize,
    pub provider_env_foreign_boundary_shadow_blocked_match: usize,
    pub provider_env_foreign_boundary_shadow_blocked_mismatch: usize,
    pub provider_env_foreign_boundary_native_hits: usize,
    pub provider_env_foreign_boundary_legacy_fallbacks: usize,
    pub provider_env_foreign_boundary_reject_nearest_misses: usize,
    pub provider_env_foreign_boundary_reject_no_provider_env: usize,
    pub provider_env_foreign_boundary_reject_marker_before_provider_env: usize,
    pub provider_env_foreign_boundary_reject_depth: usize,
    pub provider_env_foreign_boundary_reject_then_second: usize,
    pub provider_env_foreign_boundary_reject_later_grant: usize,
    pub provider_env_foreign_boundary_reject_permission_family_request_mismatch: usize,
    pub provider_env_foreign_boundary_reject_handler_path_related_to_request: usize,
    pub provider_env_foreign_boundary_reject_carry_after_frame: usize,
    pub provider_env_foreign_miss_boundary_shadow_candidates: usize,
    pub provider_env_foreign_miss_boundary_shadow_legacy_visible: usize,
    pub provider_env_foreign_miss_boundary_shadow_invisible_match: usize,
    pub provider_env_foreign_miss_boundary_shadow_invisible_mismatch: usize,
    pub provider_env_foreign_miss_boundary_reject_no_provider_env: usize,
    pub provider_env_foreign_miss_boundary_reject_marker_before_provider_env: usize,
    pub provider_env_foreign_miss_boundary_reject_depth: usize,
    pub provider_env_foreign_miss_boundary_reject_then_second: usize,
    pub provider_env_foreign_miss_boundary_reject_later_grant: usize,
    pub provider_env_foreign_miss_boundary_reject_permission_family_request_mismatch: usize,
    pub provider_env_foreign_miss_boundary_reject_handler_path_related_to_request: usize,
    pub provider_env_foreign_miss_boundary_reject_carry_after_frame: usize,
    pub provider_env_foreign_later_grant_candidates: usize,
    pub provider_env_foreign_later_grant_under_then_first: usize,
    pub provider_env_foreign_later_grant_under_then_second: usize,
    pub provider_env_foreign_later_grant_in_provider_env_next: usize,
    pub provider_env_foreign_later_grant_depth_sum: usize,
    pub provider_env_foreign_later_grant_max_depth: usize,
    pub provider_env_foreign_later_grant_legacy_visible: usize,
    pub provider_env_foreign_later_grant_naive_visible: usize,
    pub provider_env_foreign_later_grant_naive_match: usize,
    pub provider_env_foreign_later_grant_naive_mismatch: usize,
    pub provider_env_later_grant_placement_calls: usize,
    pub provider_env_later_grant_placement_calls_for_shadow: usize,
    pub provider_env_later_grant_placement_calls_for_rejected_native_candidate: usize,
    pub provider_env_later_grant_placement_frame_steps: usize,
    pub provider_env_later_grant_placement_then_steps: usize,
    pub provider_env_later_grant_placement_provider_env_steps: usize,
    pub provider_env_later_grant_placement_marker_frame_stops: usize,
    pub provider_env_later_grant_placement_found: usize,
    pub provider_env_later_grant_placement_not_found: usize,
    pub provider_env_foreign_later_grant_native_eligible_if_enabled: usize,
    pub provider_env_foreign_later_grant_native_hits_if_enabled: usize,
    pub provider_env_foreign_later_grant_native_shape_then_frames: usize,
    pub provider_env_foreign_later_grant_native_shape_marker_frames: usize,
    pub provider_env_foreign_later_grant_native_shape_provider_env_frames: usize,
    pub provider_env_foreign_later_grant_native_shape_depth_sum: usize,
    pub provider_env_foreign_later_grant_native_shape_max_depth: usize,
    pub provider_env_foreign_later_grant_legacy_shape_then_frames: usize,
    pub provider_env_foreign_later_grant_legacy_shape_marker_frames: usize,
    pub provider_env_foreign_later_grant_legacy_shape_provider_env_frames: usize,
    pub provider_env_foreign_later_grant_legacy_shape_depth_sum: usize,
    pub provider_env_foreign_later_grant_legacy_shape_max_depth: usize,
    pub continuation_resume_provider_steps: usize,
    pub continuation_resume_aggregate_steps: usize,
    pub continuation_resume_select_steps: usize,
    pub continuation_resume_block_steps: usize,
    pub continuation_resume_ref_set_steps: usize,
    pub request_whole_continuation_appends: usize,
    pub request_continuation_steps: usize,
    pub catch_body_env_clone_elided: usize,
    pub catch_body_env_clone_kept: usize,
    pub catch_body_checks: usize,
    pub catch_boundary_entries: usize,
    pub catch_boundary_value_results: usize,
    pub catch_boundary_effect_signals: usize,
    pub catch_boundary_direct_tail_signals: usize,
    pub catch_boundary_direct_abortive_signals: usize,
    pub catch_boundary_generic_request_signals: usize,
    pub has_request_boundary_catch_same_handler: usize,
    pub has_request_boundary_catch_no_routed_handler: usize,
    pub has_request_boundary_catch_foreign_handler_recurse: usize,
    pub has_request_boundary_ref_set: usize,
    pub direct_tail_blocked_by_catch_boundary: usize,
    pub direct_tail_target_catch_boundary_visible: usize,
    pub direct_tail_target_catch_boundary_provider_grant_clean: usize,
    pub direct_tail_target_catch_boundary_provider_grant_dirty: usize,
    pub direct_tail_target_catch_boundary_to_generic_request: usize,
    pub marker_frame_entries: usize,
    pub marker_frame_markers: usize,
    pub marker_frame_add_id_markers: usize,
    pub marker_frame_active_add_id_markers: usize,
    pub marker_frame_frame_only_entries: usize,
    pub marker_frame_no_active_add_id_no_handler_entries: usize,
    pub marker_frame_expr_entries: usize,
    pub marker_frame_scoped_apply_entries: usize,
    pub marker_frame_marked_apply_entries: usize,
    pub marker_frame_adapter_entries: usize,
    pub marker_frame_continuation_resume_entries: usize,
    pub marker_frame_marked_force_entries: usize,
    pub resume_marker_plan_empty: usize,
    pub resume_marker_plan_pure_value: usize,
    pub resume_marker_plan_dynamic_scope: usize,
    pub resume_marker_plan_enter_ops_total: usize,
    pub resume_marker_plan_handler_boundary: usize,
    pub resume_marker_plan_active_add_id_ops: usize,
    pub resume_marker_plan_to_legacy_push_pop: usize,
    pub marked_force_value_fast_paths: usize,
    pub marked_force_fallback_expr_thunks: usize,
    pub marked_force_fallback_effect_thunks: usize,
    pub marked_force_fallback_continuation_thunks: usize,
    pub marked_force_fallback_adapter_thunks: usize,
    pub marked_force_fallback_other: usize,
    pub marked_force_active_add_id_markers: usize,
    pub marked_force_carry_after_frame_markers: usize,
    pub active_marker_plan_pushes: usize,
    pub active_marker_plan_dedupes: usize,
    pub active_add_id_scans: usize,
    pub active_add_id_path_candidates: usize,
    pub active_add_id_path_rejects: usize,
    pub active_add_id_entry_except_rejects: usize,
    pub active_add_id_already_carried_rejects: usize,
    pub active_add_id_hits: usize,
    pub function_call_marker_plans: usize,
    pub function_call_marker_inputs: usize,
    pub function_call_marker_outputs: usize,
    pub list_merge_calls: usize,
    pub list_view_raw_calls: usize,
    pub list_values_copied: usize,
    pub resume_plan_sidecar_shadow_marker_active_add_id_ops: usize,
    pub resume_plan_sidecar_shadow_marker_handler_boundary_ops: usize,
    pub resume_plan_sidecar_shadow_provider_slots: usize,
    pub resume_plan_sidecar_shadow_provider_handler_candidates: usize,
    pub resume_plan_sidecar_shadow_legacy_frame_avoid_estimate: usize,
    pub scope_instruction_value_resume_attempts: usize,
    pub scope_instruction_value_resume_hits: usize,
    pub scope_instruction_value_resume_instructions: usize,
    pub scope_instruction_value_resume_reject_missing_plan: usize,
    pub scope_instruction_value_resume_reject_non_foreign_boundary: usize,
    pub scope_sidecar_value_resume_build_attempts: usize,
    pub scope_sidecar_value_resume_hits: usize,
    pub scope_sidecar_value_resume_reject_request_boundary: usize,
    pub scope_sidecar_value_resume_reject_ref_set: usize,
    pub scope_sidecar_value_resume_reject_non_foreign_boundary: usize,
}
